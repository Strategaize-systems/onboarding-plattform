---
id: RPT-077
date: "2026-04-24"
skill: /qa
slice: SLC-034
feature: FEAT-022
type: review
status: reviewed
reviewOf: RPT-076
result: "Post-Deploy-Verification Phase 1 (Autonomous) PASS. Deploy auf 18c6f61 live, alle 16 Container healthy, RPCs persistent, 4/4 Token-Page-Szenarien rendern korrekt via curl. Phase 2 (Browser-Interaktiv) liegt beim User: SMTP-Roundtrip, tatsaechlicher Accept-Flow, /employee-Dashboard, Konflikt-Fall."
title: "SLC-034 — Post-Deploy-Verification Phase 1"
---

# SLC-034 — Post-Deploy-Verification Phase 1 (Autonomous)

## 1. Context

Coolify-Redeploy auf Commit `18c6f61` (= SLC-034 Code `d7f2cfd` + RPT-076 Docs) durch User. Diese Phase prueft alles, was ohne Browser-Interaktion verifizierbar ist: Infrastruktur, DB-Persistenz, HTTP-Endpoints, Rendering der Token-Page. Die interaktiven Flows (echte E-Mail, Passwort setzen, Dashboard-Check) bleiben dem User vorbehalten.

## 2. Gesamt-Ergebnis Phase 1

**PASS.** 5 von 5 autonom pruefbaren Checks gruen.

## 3. Container-Health nach Redeploy

Alle 16 Container `Up About a minute` mit Image-Tag `18c6f61bd7d102928d5ff90b50f42318077bb53b`:

| Container | Status |
|---|---|
| app | healthy |
| worker | healthy |
| supabase-db | healthy |
| supabase-kong | healthy |
| supabase-meta | healthy |
| supabase-studio | ohne Healthcheck (DEC-041) |
| supabase-auth/rest/realtime/storage | Up |
| jitsi-web/prosody/jicofo/jvb/jibri | Up |
| whisper | Up |

DB-Container-Name nach Redeploy: `supabase-db-bwkg80w04wgccos48gcws8cs-154223803012` (vorher `065921571210`). Datenvolume unveraendert, alle SLC-033 + SLC-034 Schema-Objekte persistent.

App-Container-Logs 2 min post-Deploy: **keine Errors, Failures oder Exceptions**.

## 4. DB-Schema-Persistenz

Migration 072 RPCs haben den Redeploy ueberlebt:

```
 nspname |                 proname                 
---------+-----------------------------------------
 public  | rpc_accept_employee_invitation_finalize
 public  | rpc_create_employee_invitation
 public  | rpc_revoke_employee_invitation
```

Alle 3 RPCs weiterhin im `public`-Schema. Kein `storage`-Duplikat (IMP-120).

## 5. HTTP-Endpoint-Sanity

| Endpoint | Erwartet | Ergebnis |
|---|---|---|
| `GET /` | 307 → `/login` (Middleware: unauthenticated) | **307** ✓ |
| `GET /accept-invitation/<64-hex-nicht-in-DB>` | 200 + ErrorCard | **200** ✓ |
| `GET /admin/team` | 307 → `/login` (Middleware: unauthenticated) | **307** ✓ |
| `GET /accept-invitation/<invalid-format>` | 200 + ErrorCard "Ungueltig" | **200** ✓ |

Middleware-Wiring korrekt: `/accept-invitation/*` ist public, `/admin/team` ist geschuetzt.

## 6. Token-Page-Rendering (4 Szenarien)

Test-Invitation via Direct-DB-INSERT angelegt (tenant: Demo Onboarding GmbH, inviter: a317e861...). Dann je Szenario DB-Feld mutiert + `curl -sL https://onboarding.strategaizetransition.com/accept-invitation/<token>` gegrept.

| Szenario | DB-Zustand | Erwartet im HTML | Gefunden |
|---|---|---|---|
| **Valid** | status=pending, expires_at=now()+14d | Form mit Tenant-Name, Display-Name, Role-Hint, Email, Passwort-Inputs | ✓ "Demo Onboarding GmbH", "QA Test Mitarbeiter", "QA Tester", "qa-test-valid@example", "Passwort wählen", "Passwort bestätigen", "Mindestens 8 Zeichen", "Einladung annehmen" |
| **Expired** | expires_at=now()-1d | ErrorCard "abgelaufen" | ✓ "abgelaufen" |
| **Revoked** | status=revoked | ErrorCard "widerrufen" | ✓ "widerrufen" |
| **Already Accepted** | status=accepted + accepted_user_id gesetzt | ErrorCard "bereits angenommen" | ✓ "bereits angenommen" |

Die `loadInvitation`-Logik in `page.tsx` mappt alle 4 DB-Zustaende korrekt auf die entsprechende UI-Komponente. Cleanup: Test-Invitation geloescht.

## 7. Autonom NICHT pruefbare Flows (User-Phase 2)

Die folgenden 5 Szenarien erfordern echte Browser-Interaktion und bleiben dem User vorbehalten. Jedes ist eine Pflicht-Verifikation vor dem SLC-034-Release-Status.

### 7.1 Echte Einladung per UI senden (AC-1 + AC-2 Browser-Runtime)

Ablauf:
1. Login als tenant_admin (z.B. demo-tenant-admin@onboarding.test oder deine eigene tenant_admin-Rolle).
2. Navigiere auf `/admin/team`.
3. Klick "Mitarbeiter einladen" → Dialog.
4. Email = **echte Adresse** (z.B. eine eigene Zweitadresse), Display-Name + Rolle optional.
5. Submit → Toast "Einladung verschickt".
6. Inbox checken: E-Mail kommt in korrekter Sprache an (de/en/nl nach Tenant-Language), enthaelt Link, Link funktioniert.

**Wenn E-Mail nicht ankommt:** SMTP-Config pruefen. Action ist Entscheidung 1 — Invitation bleibt pending, "Erneut senden"-Button zeigt Toast "Einladung angelegt, aber E-Mail konnte nicht zugestellt werden".

### 7.2 Echter Accept-Flow (AC-3 + AC-4 Browser-Runtime)

Ablauf:
1. Link aus E-Mail in **Inkognito**-Tab oeffnen.
2. Form sichtbar mit Tenant + eigener Email (Display-Name falls gesetzt).
3. Passwort setzen (min 8 Zeichen) + Bestaetigung.
4. Submit → Redirect direkt auf `/employee`.
5. Dashboard-Landing: "Willkommen bei Demo Onboarding GmbH", eigene Email, "Noch keine Aufgaben"-Card.

**Audit-Log-Check nach Accept** (via SSH + psql):
```sql
SELECT u.id AS auth_id, u.email, p.role, p.tenant_id, i.status, i.accepted_user_id, i.accepted_at
FROM auth.users u
JOIN public.profiles p ON p.id = u.id
LEFT JOIN public.employee_invitation i ON i.accepted_user_id = u.id
WHERE u.email = '<deine-test-email>';
```

Erwartet: Eine Zeile mit `role='employee'`, `tenant_id=<demo>`, `status='accepted'`, `accepted_user_id = auth_id`, `accepted_at` gesetzt.

**Integritaets-Check** (keine verwaisten auth.users):
```sql
SELECT u.email FROM auth.users u LEFT JOIN public.profiles p ON p.id = u.id WHERE p.id IS NULL;
```
Erwartet: **0 Zeilen**. Rollback-Pfad in acceptEmployeeInvitation garantiert das.

### 7.3 Cross-Tenant-Revoke (AC-10 Browser)

Zweiten tenant_admin aus anderem Tenant waere zum direkten Test noetig. Alternativ via Direct-DB: Einladung von Tenant A sehen in `/admin/team` als tenant_admin B → erwartet: NICHT sichtbar.

### 7.4 Konflikt-Fall "E-Mail existiert bereits" (AC-9 variant)

Ablauf:
1. tenant_admin laedt sich selbst ein (email = eigene tenant_admin-Adresse).
2. Link oeffnen im Inkognito → Form rendert (Page prueft nicht auf Dubletten-Email).
3. Passwort setzen → Submit.
4. Erwartet: Fehler-Alert "Ein Account mit dieser E-Mail existiert bereits. Bitte logge dich direkt ein."
5. Invitation bleibt pending, tenant_admin kann sie revoken oder weiter leer laufen lassen.

### 7.5 Double-Accept (AC-9 Kern)

Ablauf:
1. Erfolgreich accepten (aus 7.2).
2. Gleichen Link nochmal in neuem Inkognito-Tab oeffnen.
3. Erwartet: ErrorCard "Einladung bereits angenommen" statt Form. Rendering bereits in 6 verifiziert.

## 8. Offene Risiken / Missing

- **SMTP-Konfiguration** der Produktions-Instance nicht verifiziert. Wenn 7.1 fehlschlaegt: `src/lib/email.ts` pruefen ob `SMTP_HOST/PORT/USER/PASS/FROM` richtig gesetzt, `docker exec app printenv | grep SMTP`.
- **Next.js 16 Server-Action-Cookie-Pattern** beim Transit `createAdminClient` → `createClient` + `signInWithPassword` in 7.2. Falls Auto-Login fehlschlaegt, faellt der Flow auf `/login` zurueck — kein Blocker, aber UX-Loss. Wenn das auftritt: `next/headers cookies()`-Verhalten in dieser Next.js-Version recherchieren.
- **Race-Condition UI-Update nach inviteEmployee**: `revalidatePath("/admin/team")` triggert Server-Refetch. Wenn Liste kurz leer scheint trotz erfolgreicher Einladung — Hard-Refresh loest es.

## 9. Readiness Assessment

| Dimension | Status |
|---|---|
| Infrastruktur nach Redeploy | ✓ |
| DB-Schema persistent | ✓ |
| HTTP-Endpoints reagieren korrekt | ✓ |
| Token-Page rendert alle 4 Zustaende | ✓ |
| App-Logs clean | ✓ |
| **Code-Path geliefert, DB live, Rendering OK.** | **PASS** |
| Echte E-Mail-Zustellung | ✗ (User-Phase 2) |
| Echter Passwort-Set-Flow + Auto-Login | ✗ (User-Phase 2) |
| Audit-Log-Integritaet nach Accept | ✗ (User-Phase 2) |
| Konflikt-Fall | ✗ (User-Phase 2) |

## 10. Recommended Next Step

**User-Phase 2 ausfuehren (Punkte 7.1-7.5).**

Bei allen 5 PASS:
1. SLC-034 in `/slices/INDEX.md` bleibt `done`.
2. REL-007 in `/docs/RELEASES.md` eintragen mit Deploy-Commit `18c6f61` (oder neuer).
3. `/docs/MIGRATIONS.md` MIG-023 Progress um "072 in Produktion seit 2026-04-24" ergaenzen.
4. `/docs/STATE.md` Last Stable Version → V4-SLC-034.
5. **`/backend SLC-035`** — Bridge-Engine Backend (Migration 073 + Worker-Job `bridge_generation`).

Bei Findings:
- SMTP-Problem: `/doctor` + Fix-Commit.
- Accept-Flow-Fehler: Next.js-Cookie-Pattern recherchieren, ggf. Server-Action-Struktur anpassen, neuer Redeploy, Smoke wiederholen.
- Konflikt-Fall ohne korrekte Copy: Error-Mapping in `acceptEmployeeInvitation` nachschaerfen.

**AC-11 (RLS-Matrix-Erweiterung) bleibt auf SLC-037** — unveraendert.
