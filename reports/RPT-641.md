---
id: RPT-641
date: "2026-07-10"
skill: /backend
slice: SLC-195
feature: FEAT-112
type: completion
status: completed
reviewOf: null
result: "SLC-195 Auth/Secrets-Cleanup /backend code-done — 5 MTs (account-scoped Login-Lockout P-081, verifyCronSecret-Sweep 7 Routes, Logger-Redaction P-092-Port, recording-ready timingSafe+Path-Guard, partner-slug .eq). +27 Tests, tsc0/eslint0/build0, Full-Suite 0 Regression (619 DB-env-gated/2346 pass). ISSUE-123/126/128/131/132 resolved. Alle 3 V20-Slices code-done. NICHT gemerged/deployt. NEXT /qa SLC-195 -> Gesamt-/qa V20."
title: "SLC-195 V20 Auth/Secrets-Cleanup — /backend code-done (5 MTs, letzter V20-Slice)"
---

# SLC-195 — V20 Auth/Secrets-Cleanup (/backend)

## Outcome summary
Alle 5 Micro-Tasks code-complete im kumulativen Worktree `v20-security-hardening` (auf SLC-194-HEAD `898ba4f`). Keine Migrationen. tsc 0 / eslint 0 / next build PASS. +27 neue/erweiterte Pure-Mock-Tests, Full-Suite 0 Regression. **Nicht gemerged/deployt** (kumulativer Branch). Damit sind **alle 3 V20-Slices (SLC-193/194/195) code-done**. Reines Pattern-Reuse (P-081/P-092/service-key.ts) — kein DEC noetig.

## MT-Ergebnisse

### MT-1 — Account-scoped Login-Lockout (ISSUE-126, P-081)
- `rate-limit.ts`: `createRateLimiter` um `peek(id)` (read-only) + `clear(id)` erweitert; neuer `loginAccountLimiter` (5/15min, Key = email lowercase).
- `login/actions.ts`: peek-before-signin (gesperrte Anfrage beruehrt GoTrue nicht), Count nur bei Fehlversuch, `clear()` bei Erfolg; **generische Fehlermeldung** ("E-Mail oder Passwort ungültig") statt verbatim `error.message` → schliesst zugleich das User-Enumeration-Leak.
- Tests: rate-limit.test.ts (peek/clear/IP-unabhaengig, +4) + login/actions.test.ts (Lockout ueber rotierende IPs, generische Message, +4).

### MT-2 — verifyCronSecret timingSafe-Helper + 7-Routen-Sweep (ISSUE-131)
- Neu `src/lib/auth/cron-secret.ts` `verifyCronSecret` (Struktur aus service-key.ts: Length-Check + crypto.timingSafeEqual, fail-closed bei null/undefined).
- Alle 7 Cron-Routes: `secret !== expected` → `!verifyCronSecret(secret, expected)` + Import. **Done-Gate: 0 Treffer `secret !== expected` in src/app/api/cron/** (verifiziert). 503-("ENV missing")/403-Pfade bleiben unterscheidbar.
- Test: cron-secret.test.ts (+5).

### MT-3 — Logger-Redaction (ISSUE-132, P-092-Port)
- Neu `src/lib/logger/redact.ts` — 1:1-Port aus BS (`redactSecrets`, key-basiert rekursiv, DEFAULT_REDACT_KEYS 17, Circular-safe, extraKeys).
- `logger.ts`: `logToDb` wendet `redactSecrets` auf `metadata` an vor error_log-INSERT.
- Test: redact.test.ts (+6).

### MT-4 — recording-ready timingSafe + Path-Traversal-Guard (ISSUE-123)
- Bearer-Secret timing-safe via `verifyServiceKey` (statt `authHeader !== ` + `Bearer ${secret}`).
- Path-Guard: `body.file_path` via path.resolve gegen `JIBRI_RECORDINGS_DIR`-Prefix + `.mp4` + `..`-Reject, **fail-closed** (ohne ENV → jeder Pfad 400). `readFile` nutzt den resolveden Pfad.
- Test: route.test.ts (+7, 401/400-Varianten/404-passed-guard).

### MT-5 — partner-slug .ilike→.eq (ISSUE-128)
- `api/public/partner/[slug]/route.ts` + `api/public/signup/route.ts`: `.ilike("slug", x)` → `.eq("slug", x.toLowerCase())` — `%`/`_` sind exakte Literale, kein Wildcard-Oracle.
- Tests: partner route.test.ts erfasst `.eq`-Arg ("Acme%" → "acme%", +1); beide Mock-Builder auf `.eq` umgestellt (Change-Impact).

## Files created (7)
- `src/lib/auth/cron-secret.ts` (+ `.test.ts`) — MT-2.
- `src/lib/logger/redact.ts` (+ `.test.ts`) — MT-3.
- `src/app/login/actions.test.ts` — MT-1.
- `src/app/api/dialogue/recording-ready/route.test.ts` — MT-4.
- `reports/RPT-641.md`.

## Files changed (16)
- `src/lib/rate-limit.ts` (peek/clear + loginAccountLimiter, MT-1) + `src/lib/__tests__/rate-limit.test.ts`.
- `src/app/login/actions.ts` (peek-before-signin + generic error, MT-1).
- 7× `src/app/api/cron/*/route.ts` (verifyCronSecret, MT-2).
- `src/lib/logger.ts` (redactSecrets on metadata, MT-3).
- `src/app/api/dialogue/recording-ready/route.ts` (timingSafe + path guard, MT-4).
- `src/app/api/public/partner/[slug]/route.ts` + `src/app/api/public/signup/route.ts` (.eq, MT-5) + beide `__tests__/route.test.ts` (Mock `.ilike`→`.eq`).
- Records: `docs/KNOWN_ISSUES.md` (ISSUE-123/126/128/131/132 resolved), `docs/STATE.md`, `slices/INDEX.md`, `features/INDEX.md`, `planning/backlog.json`.

## Files intentionally not changed
- Kein DEC (reines Pattern-Reuse, keine Architektur-Abweichung).
- ISSUE-132 Teil-b (Regex-Scrub interpolierter Secrets in `message`/`stack`-Strings): bewusst deferred — nicht P-092-Kern (key-basiert deckt keine String-Interpolation), Low-Residual da OP-Call-Sites IDs uebergeben.

## Problems found
- **Change-Impact (MT-5):** beide bestehenden Route-Tests (partner + signup) mockten `.ilike` → auf `.eq` umgestellt, sonst haetten sie beim `.eq`-Call gebrochen. Full-Suite bestaetigt 0 Regression.
- **Change-Impact (MT-3):** redactSecrets auf metadata angewandt — Full-Suite geprueft, KEIN Test asserierte auf einen redacteten Key im geloggten metadata (0 neue Failures).
- **MT-1 Tradeoff:** account-scoped Lockout ermoeglicht DoS-by-lockout (5 Fails sperren einen Account 15min). Standard-Tradeoff bei P-081; dokumentiert in ISSUE-126-Resolution.
- **MT-4 fail-closed:** ohne `JIBRI_RECORDINGS_DIR` weist die (ohnehin middleware-unerreichbare) Route jeden Pfad ab — die ENV ist Pflicht vor jeder Re-Aktivierung (dokumentiert).

## Verifikation
- tsc --noEmit EXIT 0; eslint (alle geaenderten Files) EXIT 0; next build EXIT 0 ('use server'-Gate: login/actions.ts nur 2 async Exports; GENERIC_LOGIN_ERROR ist nicht exportiert).
- Full-Suite: **619 failed / 2346 passed / 65 skipped**. 619 = exakter Baseline (TEST_DATABASE_URL-env-gated DB-Suiten) → **0 neue Failures**; +27 neue Pure-Mock-Tests grün.
- **NICHT verifiziert (an /qa/Founder-Selbsttest delegiert):** Live-Login-Lockout-Verhalten gegen den deployten Stand (optional, Founder-Selbsttest).

## Open points / deferred
- ISSUE-132 Teil-b (String-Regex-Scrub) deferred (s.o.).
- Gesamt-/qa V20 holt die SLC-194-Carry-Forward-Live-Smokes (Handbook-XSS, SVG-Reject, CSP-Funktional-Smoke + enforce-Flip) nach.

## Project record updates
ISSUE-123/126/128/131/132 → resolved (Resolution-Zeilen). SLC-195 → in_progress (code-done, slices/INDEX), FEAT-112 → in_progress (features/INDEX), BL-539 → in_progress (backlog.json). STATE Focus/Phase/Next-Steps neu (NEXT /qa SLC-195 → Gesamt-/qa V20). RPT-641 erstellt.

## Recommended next step
**`/qa` SLC-195** — mandatorisch nach /backend (Pure-Mock-Verifikation der 5 MTs; optionaler Live-Login-Lockout-Smoke). Danach **Gesamt-/qa V20** über SLC-193/194/195 (mit den gebündelten Live-Smokes) → /final-check → `/code-review ultra` (DEC-219-Closure) → /deploy (MIG-133/134 Live-Apply) → /post-launch.
