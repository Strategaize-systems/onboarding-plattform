---
id: RPT-569
date: "2026-07-04"
skill: /qa
slice: SLC-184
feature: FEAT-101
type: completion
status: completed
reviewOf: null
result: "Code-Side PASS — RAG-Frage-Antwort + Coverage-Guard + Sprach-Eingabe; Live-Smoke (Bedrock/Whisper/Browser) an /deploy verlagert"
title: "QA SLC-184 — RAG-Frage-Antwort + Coverage-Guard + Sprach-Eingabe (Code-Side)"
---

# QA SLC-184 — RAG-Frage-Antwort + Coverage-Guard + Sprach-Eingabe

## QA-Scope
Code-Side-QA (V10.2 INERT-Branch, Live-Smoke an /deploy verlagert). Geprueft: AC-Abdeckung,
Stub-Scan, Wiring-Chain (Frontend→Action→Embed/Search/RPC/LLM; Voice→Route; Re-Embed),
Security-Posture (Gate/Fail-closed/DEC-258/DEC-259), Gates (tsc/eslint/vitest/next build).

Branch `v10-2-requirements`, Worktree `…-v10-2-req`. Commits: e73c4c1 (MT-1), 9ef3cbf (MT-2),
eb93986 (MT-3), + QA-Auto-Fix (AnswerPanel-Quellen-Render).

## Ergebnis: PASS (Code-Side)

## Acceptance-Criteria-Abdeckung
| AC | Status | Nachweis |
|---|---|---|
| AC-184-1 Text→belegte Antwort + Quellen | PASS | `askRag` Happy-Path (Test 1); Sonnet via `chatWithLLM` (Sonnet-4 EU); Quellen aus Search-Rows gemappt |
| AC-184-2 Fail-closed, tenant server-derived | PASS | `bindTenant` validiert tenant vs `tenants`-Tabelle nach Gate; UI sperrt Submit ohne Mandant; DEC-258 |
| AC-184-3 Coverage-Guard | PASS | none/partial/full + ehrlicher Hinweis + Re-Embed-Trigger; 4 Tests (none/gap/partial/miss) |
| AC-184-4 Sprach-Eingabe | PASS (Code) | MediaRecorder→`/api/admin/transcribe` (Reuse questionnaire-form); Route-Test 4/4; **Browser-MediaRecorder-Smoke → /deploy** |
| AC-184-5 error_log-audit, fail-open, kein ai_cost_ledger | PASS | `captureException` auf allen Fehlerpfaden; kein Ledger-Write in `rag.ts`; Re-Embed ledger-frei (DEC-259 + ai_jobs-FK-safe) |
| AC-184-6 Hermetische Tests + tsc/eslint/build + Browser-Smoke | PASS (Code) | 13 neue Tests grün; tsc 0; eslint 0; `next build` EXIT 0; **Browser-Smoke → /deploy** |

## Gates
- `tsc --noEmit`: **0 Fehler**
- `eslint` (12 geänderte Files): **0**
- `vitest` neu: **13/13** (rag.test 9, transcribe route.test 4); Workspace-Dir gesamt **22/22** (SLC-183-Tests unbeschädigt nach Gate-Refactor)
- `next build`: **EXIT 0**; `/admin/mein-tag` + `/api/admin/transcribe` im Manifest

## Stub-Scan
Keine TODO/FIXME/leere-Handler/Hardcodes/Dummy-Daten. Einziges `console.error` in
`QuestionBox.transcribeRecording` = fail-open Voice-Handler (1:1 aus Reuse-Quelle
questionnaire-form.tsx) → **reviewed & accepted**.

## Wiring-Chain (verifiziert)
QuestionBox.onSubmit → WorkspaceShell.handleSubmitQuestion → `askRagAction(tenant, q)`
→ Gate + `bindTenant` → `askRag(admin,…)` → Coverage-Counts (`knowledge_unit`/`knowledge_chunks`)
→ Titan `embed` → `rpc_search_knowledge_chunks` (Name+Signatur+GRANT service_role deckungsgleich
mit MIG-036) → `chatWithLLM` (Sonnet-4 EU). Voice: fetch `/api/admin/transcribe` → Route (Gate+Whisper).
Re-Embed: AnswerPanel.onReembed → `reembedTenantAction` → `reembedTenantKnowledge`. TenantSelector
via page.tsx `tenants`-Prop. Alle Glieder aufgelöst.

## Findings

### Medium
- **Keine.**

### Low
- **Re-Embed reused Pattern statt Funktion (`embedKnowledgeUnits`).** Spec sagte „Reuse
  `embedKnowledgeUnits`"; die Funktion schreibt jedoch `ai_cost_ledger` mit `job_id`-FK →
  `ai_jobs`, was für einen ad-hoc-Re-Embed ohne echte `ai_jobs`-Row FK-verletzen würde und
  DEC-259 (RAG-Pfad ohne Ledger) widerspricht. Umgesetzt als ledger-freier `reembedTenantKnowledge`
  mit identischem Embed+Upsert-Shape (Quelle zitiert). Bewusst, dokumentiert. **Accepted.**
- **RAG-Antwort-Modell folgt `LLM_MODEL`-ENV (shared `chatWithLLM`-Sonnet-Pfad),** anders als
  `fazit.ts` (ISSUE-111 pinned Haiku). Bewusst — der kanonische OP-Sonnet-Primitive ist
  env-getrieben; ein hartes Pinning würde vom Repo-Standard divergieren. **Accepted** (Default =
  `eu.anthropic.claude-sonnet-4-…`, EU-Region → data-residency-konform).
- **`rag-action.ts` (Gate + `bindTenant`) nicht unit-getestet.** Dünner „use server"-Wrapper;
  konsistent mit SLC-183 `actions.ts` (nur Lib-Tests). Logik trivial + security-relevant → beim
  /deploy Live-Smoke (fail-closed ohne Mandant) mitprüfen.

### Auto-Fix (Deviation-Rule 2, in diesem QA)
- **AnswerPanel** rendert Quellen jetzt auch bei leerer LLM-Antwort (`answer||warning||sources.length`)
  — verhindert Blank-Panel-Edge bei leerem Sonnet-Output trotz Treffern. Commit separat.

## Deferred (an /deploy)
- Live-Bedrock-RAG-Smoke (echte Frage → geerdete Antwort + Quellen)
- Live-Whisper-Voice-Smoke (Browser MediaRecorder → Transkript in Frage-Box)
- Coverage-Guard-Live (Mandant ohne Chunks → Hinweis + Re-Embed → Antwort)
- 0-Console-Errors Browser-Smoke

## Nicht angefasst (bewusst)
- `docs/STATE.md` (304KB, pathologisch — IMP-1373): Precedent SLC-182/183; Resume-Anker liegt im
  Memory. Bewusste Deviation, nicht Vergessen.

## Readiness
SLC-184 Code-Side abgeschlossen. FEAT-101 = letztes V10.2-Feature → als Nächstes **Gesamt-QA V10.2**
(SLC-182+183+184 zusammen) vor /final-check.

## Next workflow step
`/qa` **Gesamt-QA V10.2** (letzter Slice der Version erreicht) → danach `/final-check` → `/go-live`
→ `/deploy` (trägt die verlagerten Live-Smokes) → `/post-launch`.
