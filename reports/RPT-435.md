---
id: RPT-435
date: "2026-06-10"
skill: /qa
slice: SLC-V9.1-A
feature: FEAT-075 + FEAT-076
type: completion
status: completed
reviewOf: RPT-434
result: "PASS (Code-Side) fuer MT-5 Lambda-Source — AC-V9.1-A-9 Code-Side erfuellt, Mock-Smoke 7/7 (HMAC byte-identisch zu OP-Webhook), ESLint=0, 0 Stubs, 0 Blocker/High/Medium, 2 Low (MT-6-deferred)"
title: "QA SLC-V9.1-A MT-5 Code-Side (Lambda forward-ses-to-op-webhook + ZIP-Deploy)"
---

# QA SLC-V9.1-A MT-5 — Code-Side

## 1. QA-Scope
- Slice SLC-V9.1-A MT-5: Lambda-Source `infra/lambda/forward-ses-to-op-webhook/` (index.mjs + package.json + README) + `scripts/deploy-lambda.sh`.
- Worktree `c:/strategaize/strategaize-onboarding-plattform-v91`, Branch `v9-1-forward-bucket-email`, HEAD `ff426b3`.
- Verifiziert: AC-V9.1-A-9 (Code-Side). Live-Deploy + AWS-Console-Verify = MT-6.

## 2. Ergebnis: PASS (Code-Side)

## 3. Quality-Gates
| Gate | Ergebnis |
|---|---|
| ESLint `infra/lambda/.../index.mjs` | EXIT=0, 0 warnings |
| Local Mock-Smoke `node index.mjs` | 7/7 PASS |
| `bash -n scripts/deploy-lambda.sh` | Syntax OK |
| Git working tree | clean (committed `931a7fa` + `ff426b3`) |

## 4. Wire-Contract-Verifikation (Lambda → OP-Webhook)
Backend↔Backend-Integration — kritischste Prüfung dieses MTs:
- **HMAC-Signatur:** Lambda `computeSignature` = `"sha256=" + HMAC_SHA256(secret, body, utf-8).hex` — identisch zu `src/lib/inbound-email/hmac.ts:computeInboundSignature`. Mock-Smoke beweist **byte-identity** gegen Recompute über den exakt gesendeten Body → `verifyInboundHmac` (MT-3, in MT-4 live verifiziert) wird akzeptieren. ✓
- **Body-Felder:** `raw_eml_base64 / s3_key / message_id / recipient` matchen 1:1 `aws-ses.ts:parseEvent` (requireString-Felder). ✓
- **Vendor-Header:** `x-strategaize-vendor: ses-ireland` matcht `SES_IRELAND_VENDOR_ID` + Factory-Default. ✓
- **Deploy-Skript:** function-name `forward-ses-to-op-webhook` + region `eu-west-1` matchen ARCHITECTURE.md Step 5. ✓

## 5. Stub-Detection
0 echte Stubs in den 4 MT-5-Files. `try { JSON.parse } catch { throw new Error(...) }` ist meaningful re-throw (kein leerer Handler). `console.log` ausschließlich im self-runnable Smoke-Block (intendierter CLI-Output). `smoke-secret-...` ist Test-Fixture im Smoke-Block (acceptable, analog Test-File). `.env.deploy.example`-Placeholder (`GENERATE_ME_HEX_64`) = Config-Template (acceptable).

## 6. Findings

### Low
- **L-1 (DEC-204 Recipient-Source, MT-6-deferred):** Recipient-Extraktion setzt voraus, dass der Forward die `bulk-<slug>`-Adresse in To/X-Forwarded-To/Delivered-To trägt. Fallback = erste Adresse. Falsch/fehlend → OP `tenant_not_found`→200 silent-drop (kein Datenverlust). Robustheit gegen reale Mail-Clients in MT-6 Live-Smoke + SLC-V9.1-D Setup-Anleitungen zu bestätigen.
- **L-2 (Smoke-Coverage):** Der Mock-Smoke deckt den Happy-Path (1 S3-Object, valider To-Header) ab. Nicht abgedeckt: Multi-Record-SNS, fehlender Recipient-Header (→throw), non-2xx-Webhook-Response (→throw für Lambda-Retry). Logik ist im Code vorhanden + lesbar; reale Pfade werden in MT-6 Live-Smoke geprüft. Akzeptiert für Code-Side.

## 7. Problems found
- **P-1 (carry-over RPT-433/434, NICHT MT-5-blocking):** `INBOUND_WEBHOOK_HMAC_SECRET`/`INBOUND_VENDOR`/`INBOUND_CATCHALL_DOMAIN` noch nicht im Coolify-App-Container; AWS-Secrets-Manager-Wert muss byte-identisch sein (R6). MT-6/Founder-Setup-Vorbedingung.

## 8. Readiness
MT-5 Code-Side ready. SLC-V9.1-A bleibt `in_progress` (MT-6 offen). Keine Release-Gate-Änderung — V9.1 NICHT released/stable per IMP-950 + module-lifecycle-discipline.

## 9. Next workflow step
**MT-6** (Live-Smoke E2E SES→S3→SNS→Lambda→Webhook + RLS-Test-Matrix 12+ Cases + Records-Final-Bump) ist der letzte MT von SLC-V9.1-A. MT-6 ist BLOCKED bis **AWS-Founder-Setup Steps 1-6** (SES Receipt-Rule + MX + S3 + Secrets Manager + IAM/Lambda + SNS) + Coolify-/Secrets-Manager-ENV-Sync vorliegen. Empfehlung: Agent assistiert Founder Schritt-für-Schritt durch die AWS-Founder-Setup-Steps; sobald live, MT-6 ausführen. Danach Gesamt-V9.1-/qa erst nach SLC-V9.1-B/C/D. Kein Master-Merge / kein /go-live vor Gesamt-V9.1-/qa → /final-check → /go-live → /post-launch (IMP-950 BLOCKING).
