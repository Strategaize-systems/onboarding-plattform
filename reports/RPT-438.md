---
id: RPT-438
date: "2026-06-10"
skill: /qa
slice: SLC-V9.1-A
feature: FEAT-075
type: completion
status: completed
reviewOf: null
result: "PASS (Code-Side) — MT-R4/R5/R6 endpoint-resolver + imap-sync + cron, 18 neue Vitest GREEN, tsc=0, ESLint=0. MT-R7 RLS-Matrix + IONOS-Live-Smoke offen (gated OQ-R1-1)."
title: "QA SLC-V9.1-A MT-R4/R5/R6 — Endpoint-Resolver + IMAP-Sync + Cron"
---

# QA — SLC-V9.1-A MT-R4 + MT-R5 + MT-R6 (REVISION R1, DEC-205)

## 1. QA-Scope reviewed

Code-Side-QA des IMAP-Pull-Transports (REVISION R1):

- **MT-R4** `src/lib/inbound-email/endpoint-resolver.ts` — `resolveDefaultEndpoint()` (DEC-R1-2)
- **MT-R5** `src/lib/inbound-email/imap-sync.ts` — `syncInboundEmails()` (BS-Port + OP-Persist, Flow A R1)
- **MT-R6** `src/app/api/cron/inbound-email-imap-sync/route.ts` — POST-Cron mit `x-cron-secret`
- Typen-Erweiterung `src/lib/inbound-email/types.ts` (`ResolvedEndpoint`, `EndpointResolveMode`, `InboundSyncResult`)
- 3 neue Test-Files (resolver 6, imap-sync 8, cron 4 = 18 Tests)

**Nicht in diesem QA-Scope (MT-R7, separat):** RLS-Pen-Test-Matrix fuer `email_inbound_sync_state` (4 Rollen) + IONOS-End-to-End-Live-Smoke (gated auf OQ-R1-1 FOUNDER-ACTION) + finale Records-Synchronisation.

## 2. Result: PASS (Code-Side)

Alle drei MTs erfuellen ihre Acceptance-Criteria auf Code-Ebene. Keine Blocker, keine High-/Medium-Findings.

## 3. Findings grouped by severity

### Blocker
- keine

### High
- keine

### Medium
- keine

### Low / reviewed-and-accepted
- **Design-Entscheid `anchor_date` = Ingest-Tag (UTC):** Im Full-Pass nutzt `processMessage` `new Date().toISOString().slice(0,10)` als `p_anchor_date` (Daily-Roll-Over) UND als Storage-Pfad-Datum. Waehrend des initialen 90-Tage-Backfills landen damit alle Alt-Mails im heutigen Tagesbucket. Konsistent mit `rpc_inbound_record_message` (`received_at = now()`) + "forward-bucket continuous"-Semantik. Im Code dokumentiert. Reviewed & accepted.
- **`validation/setup-token.ts` Survivor ungenutzt im Single-Mailbox-Pfad:** Per DEC-R1-3 wird die Setup-Token-Schicht im `mode='single_mailbox'` bewusst uebersprungen; `verifySetupToken` bleibt fuer den spaeteren Catchall-Modus erhalten (eigener Test vorhanden). Reviewed & accepted (kein Dead-Code, Future-Path).

## 4. Stub-Detection

`grep` ueber `endpoint-resolver.ts`, `imap-sync.ts`, `route.ts`, `types.ts`:
- `return null`-Treffer (3x resolver, 1x imap-sync) sind designte Control-Flow-Contracts (DEC-R1-2-Ambiguitaets-Abort mit vorangehendem `captureWarning`; `extractDomain`-Null-Guard) — **keine Stubs**.
- Kein TODO/FIXME/HACK, kein `console.log`/`debugger`, kein leerer Catch, keine Hardcoded-Credentials.
- AC-R1-1-Cleanup-grep (`aws-ses|verifyHmac|forward-ses-to-op|INBOUND_VENDOR|INBOUND_WEBHOOK_HMAC|ParsedInboundEvent|InboundEmailVendor`) ueber `src/ infra/ scripts/`: **0 Code-Treffer** (1 History-Kommentar in `types.ts`, erlaubt).

## 5. Wiring-Verification (Backend-Chain)

`Cron POST → verify x-cron-secret → syncInboundEmails → resolveDefaultEndpoint → ImapFlow-Pull → parseEmlBuffer → Dedup(email_message) → evaluateSenderAllowlist → persistRawEml → admin.rpc(rpc_inbound_record_message) → sync_state-Update → captureInfo`

| Link | Verifikation | Status |
|---|---|---|
| Cron → Service | `syncInboundEmails()` import + Aufruf | ✓ |
| Service → Resolver | `resolveDefaultEndpoint(admin)` (DEC-R1-2) | ✓ |
| Resolver → DB | `lookupEndpointBySlug` (ENV) / `email_inbound_endpoint WHERE status='active'` | ✓ |
| Service → IMAP | `ImapFlow` connect + `search`+`fetch` last_uid-inkrementell | ✓ |
| Parse → Reuse | `parseEmlBuffer` (OP bulk-email-Parser, ParsedEmail-Shape) | ✓ |
| rpc-Param-Contract | 7 Keys `p_tenant_id..p_message` 1:1 = Migration 115 (MIG-060) Signatur | ✓ |
| rpc p_message-Felder | message_id/in_reply_to/references_array/from_address/to_addresses/cc_addresses/subject/date/body_text/body_html/has_attachments/attachment_metadata/raw_storage_path = Function-Body-Reads | ✓ |
| ON CONFLICT-Match | `inbound_source='forward_bucket'` (Function-hardcoded) + `endpoint_id IS NOT NULL` | ✓ |
| sync_state-Schema | INSERT/UPDATE-Spalten + Status-CHECK ('idle'\|'syncing'\|'error') = MIG-061 | ✓ |
| ENV-Contract | `IMAP_HOST/PORT/USER/PASSWORD/IMAP_INITIAL_SYNC_DAYS/INBOUND_DEFAULT_ENDPOINT_SLUG/CRON_SECRET` alle in `.env.deploy.example` | ✓ |

## 6. Test-Execution

| Suite | Tests | Ergebnis |
|---|---|---|
| `endpoint-resolver.test.ts` | 6 | GREEN |
| `imap-sync.test.ts` | 8 | GREEN |
| `cron .../route.test.ts` | 4 | GREEN |
| inbound-email-Modul gesamt (inkl. Survivor-Tests) | 34 | GREEN |
| `npx tsc --noEmit` | — | EXIT 0 (0 Errors) |
| `npx eslint` (7 Files) | — | EXIT 0, 0 Warnings |

Test-Abdeckung MT-R5: incremental-UID-Fetch ab last_uid, Dedup via message_id (skip+advance), tolerante Validation (kein Token → Pass im single_mailbox), Allowlist-Mismatch → reject_log + skip, Full-Pass → persistRawEml + rpc, last_uid-Persist, pro-Mail-Error bricht Lauf nicht ab (kein last_uid-Rueckschritt), sync_state-Anlage bei Erstlauf, Connection-Error → status='error'.

## 7. Acceptance-Criteria-Abgleich

| AC | Status | Notiz |
|---|---|---|
| AC-R1-1 (Cleanup + types-Trim + ENV) | ✓ | MT-R1; grep 0 Code-Treffer, ENV-Block vollstaendig |
| AC-R1-2 (MIG-061 LIVE + Schema-Vitest) | ✓ | MT-R2 (Server 6/6 GREEN, RPT-Vorlauf) |
| AC-R1-3 (imapflow-Dep + Shim) | ✓ | MT-R3 |
| AC-R1-4 (resolveDefaultEndpoint DEC-R1-2) | ✓ | 6 Vitest GREEN |
| AC-R1-5 (syncInboundEmails Flow A) | ✓ | 8 Vitest GREEN |
| AC-R1-6 (Cron 503/403/200/500) | ✓ | 4 Vitest GREEN |
| AC-R1-7 (RLS-Matrix sync_state) | ⏳ offen | MT-R7 |
| AC-R1-8 (IONOS-Live-Smoke) | ⏳ gated | MT-R7 / OQ-R1-1 FOUNDER-ACTION |
| AC-R1-9 (TSC=0 + ESLint=0 + Records-sync) | teilweise | TSC=0 + ESLint=0 ✓; Records-Final = MT-R7 |

## 8. Open Risks / Missing Checks

- **DB-Integrationstests dieser MTs:** MT-R4/R5/R6 erzeugen 0 neue DB-Tests (alle ImapFlow/admin/storage/rpc gemockt). Der reale rpc-/sync_state-Roundtrip wird erst im MT-R7-Live-Smoke gegen die Coolify-DB validiert. Mitigation: Param-Contract + Schema statisch gegen MIG-060/061 verifiziert (Abschnitt 5).
- **`next build` nicht ausgefuehrt** (Backend-Slice; `tsc --noEmit` deckt Typkompilierung ab). Kein RSC-/Bundle-Risiko (reine Server-Module + API-Route).
- **MT-R7 ausstehend:** RLS-Pen-Test-Matrix (`email_inbound_sync_state`, 4 Rollen, service_role-only-write) + IONOS-End-to-End-Live-Smoke. Live-Smoke BLOCKED bis OQ-R1-1 (IONOS-Postfach + Coolify-IMAP-ENVs, FOUNDER-ACTION). RLS-Matrix selbst ist IONOS-frei und kann vorab laufen.

## 9. Readiness-Assessment

Der Code-Side-Transport (Resolver + Sync-Service + Cron) ist **bereit** und sauber an die bereits LIVE/QA'ten Foundation-Layer (MIG-057..061, Validation, rpc, storage-persist) verdrahtet. Der Slice ist **nicht** komplett — MT-R7 (RLS-Matrix + IONOS-Live-Smoke + Records-Final) steht aus; Live-Smoke ist auf die Founder-IONOS-Aktion (OQ-R1-1) gated. Slice-Status bleibt `in_progress`.

Per IMP-950 + [[module-lifecycle-discipline]] + [[feedback-no-strategaize-live-until-all-systems-ready]] BLOCKING: kein Master-Merge / kein `released` vor Gesamt-V9.1-/qa → /final-check → /go-live → /post-launch; kein Customer-Outreach.

## 10. Next workflow step

**MT-R7** — RLS-Pen-Test-Matrix fuer `email_inbound_sync_state` (4 Rollen, SAVEPOINT-Pattern, gegen Coolify-DB im node:20-Container per [[coolify-test-setup]]). Dieser Teil ist IONOS-frei und kann sofort laufen. Der **IONOS-Live-Smoke**-Teil von MT-R7 (AC-R1-8) wartet auf **OQ-R1-1 = FOUNDER-ACTION** (IONOS-Postfach `bulk@strategaizetransition.com` + Coolify-IMAP-ENVs + Coolify-Scheduled-Task ≥5 Min).

Danach: SLC-V9.1-B/C/D → Gesamt-V9.1-/qa → /final-check → /go-live → /post-launch.
