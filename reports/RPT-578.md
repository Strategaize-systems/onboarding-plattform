---
id: RPT-578
date: "2026-07-05"
skill: /slice-planning
slice: SLC-185
feature: FEAT-102
type: completion
status: completed
reviewOf: RPT-577
result: "V10.2.1 = 1 Slice SLC-185 (4 MTs, 0 Migration, alle Tests hermetisch). Code-Reality + Schema + Reuse-Claims verifiziert; PRD-401-vs-DEC-403-Drift korrigiert. READY für /backend."
title: "Slice-Planning V10.2.1 — SLC-185 Embedding-Reconcile-Cron"
---

# Slice-Planning V10.2.1 — SLC-185 Embedding-Reconcile-Cron

## Slicing-Zusammenfassung

V10.2.1 ist bewusst **1 Slice** (SLC-185): dünner Cron-Orchestrator über bereits deployte V10.2-Primitiven, 0 Migration, kein UI. Ein Split würde nur Overhead erzeugen. Der Slice ist in 4 sequentielle Micro-Tasks zerlegt (Target-File-Batching, [backend]/[shared]-Tags):

| MT | Titel | Files | Dep |
|---|---|---|---|
| MT-1 | rag.ts de-drift: `getTenantCoverage`-Export (TDD) | rag.ts + rag.test.ts (MODIFY) | — |
| MT-2 | Orchestrator `reconcileEmbeddings` (injizierbare Deps, Cap 25, fail-open, captureInfo-Summary) | reconcile-embeddings.ts + Test (NEW) | MT-1 |
| MT-3 | Cron-Route (1:1-Port `pending-signup-cleanup`, 503/403/200/500) | route.ts + Test (NEW) | MT-2 |
| MT-4 | RUNBOOK-Sektion + Records-Flip | RUNBOOK.md u.a. | MT-3 |

AC-Matrix AC-185-1..6 mappt SC1–SC6 aus PRD §V10.2.1; AC-185-6 (SC6, Live-Heilung Founder-Mandant "5 von 35") ist explizit der /deploy-Phase zugeordnet, ebenso die Coolify-Task-Anlage.

## Step 0: Architecture-/DEC-Reconciliation

- Open Questions: keine blockierenden (RPT-577 §9). Die zwei Backend-Details sind per Architektur fixiert und in die Spec übernommen: `MAX_TENANTS_PER_RUN=25`; `getTenantCoverage` filtert wie der V10.2-Guard (`source_type='knowledge_unit'`, `status='active'`).
- DEC-262 vollständig auf SLC-185 gemappt (Route→MT-3, Orchestrator→MT-2, de-drift→MT-1, Coolify-Task→/deploy). Nichts deferred außer der explizit geparkten Aggregat-RPC-Skalierungsoption (rejected in DEC-262).
- **Spec-vs-DEC-Drift korrigiert:** PRD SC2 nennt "503/401"; reales Pattern (`pending-signup-cleanup/route.ts`) + DEC-262 nutzen **403** bei Secret-Mismatch. DEC gewinnt — Spec implementiert 503/403/200/500. In Slice-Spec + INDEX dokumentiert.

## Verified-Against-Code-Reality / Schema-Grounding / Reuse-Claims (Worktree slc178 @ `4624e8f`)

- **NEW-Kollisionen:** `src/app/api/cron/knowledge-embed-reconcile/` und `src/lib/workspace/reconcile-embeddings.ts` existieren nicht — frei.
- **MODIFY-Ziele existieren:** `src/lib/workspace/rag.ts` (`DEFAULT_RAG_DEPS` :111, `defaultCount` :93, `reembedTenantKnowledge` :350 exportiert/fail-open/`ReembedResult`), `src/lib/workspace/__tests__/rag.test.ts`, `docs/RUNBOOK.md` (Scheduled-Task-Doku-Vorlage :125ff).
- **Reuse-Claims real geöffnet:** Cron-Pattern `pending-signup-cleanup/route.ts` (503/403/200/500, `captureInfo` mit `metadata.category`, `runtime=nodejs` + `force-dynamic`); Tenants-Primitive `load-cross-tenant.ts:27` `from("tenants").select("id, name")`; `captureInfo/captureException/captureWarning` aus `@/lib/logger`; `createAdminClient` aus `@/lib/supabase/admin`.
- **Schema:** Count-Gap-Query verbatim aus `rag.ts` (`knowledge_unit` vs. `knowledge_chunks` mit `source_type`/`status`-Filter, count-only/head); Idempotenz über bestehenden Unique-Constraint `(source_type,source_id,chunk_index)`. 0 neue Tabellen/Spalten/RPCs — kein Migrations-Slice, MIG-Reservierung entfällt.
- **Test-Infra-Audit:** `vitest.config.ts` environment=node, include `src/**/*.test.ts` (geplante Test-Pfade matchen), `npm run test` = `vitest run`. Alle SLC-185-Tests klassifiziert als **Pure-Mock-Vitest (lokal, hermetisch)** — bewusste Abweichung vom Coolify-DB-Test des Vorbilds, da PRD explizit "hermetisch mit injizierten Deps" fordert und der Orchestrator DI-fähig gespecct ist. Test-AC inkrementell (Pre-MT-1-Baseline + ~12 neue Cases, konkrete Liste in der Spec).

## Parallel-Execution-Tabelle

| Slice ID | Parallel Group | MIG Reserved | File Touchpoints | Notes |
|---|---|---|---|---|
| SLC-185 | A (einziger Slice) | keine | rag.ts, reconcile-embeddings.ts, cron-route, 3 Test-Files, RUNBOOK.md | Worktree `v10-2-1-embed-reconcile` (SaaS-Pflicht); Pre-Merge-Re-Check minimum Rebase+Tests |

## Files created (1)

- `/slices/SLC-185-embedding-reconcile-cron.md` — vollständige Slice-Spec (Goal, Scope, Verified-Against-Code-Reality, Test-Klassifikation, 4 MTs mit Files/Verification/Deps, AC-Matrix SC-Mapping, Cross-Slice-Dependencies, Parallel-Readiness, Risks, Security).

## Files changed (3)

- `/slices/INDEX.md` — neue Sektion "V10.2.1 Slices" mit SLC-185 (Status `planned`, Spalte 4) + Parallel-Readiness/Pflicht-Gates.
- `/docs/STATE.md` — High-Level State → `slice-planning`; Current Focus/Phase auf Slice-Planning-DONE + nächster Schritt /backend SLC-185 MT-1; neuer Top-Eintrag in Immediate Next Steps.
- `/reports/RPT-578.md` — dieser Report (NEW, oben unter created gezählt wäre doppelt; formal: created).

## Files reviewed (unverändert)

`/docs/PRD.md` §V10.2.1 · `/docs/ARCHITECTURE.md` §V10.2.1-Addendum · `/docs/DECISIONS.md` DEC-262 · `/planning/backlog.json` (BL-528 bereits `in_progress`/V10.2.1 — korrekt, kein Edit) · `/planning/roadmap.json` (v10-2-1 bereits `active` — korrekt, kein Edit) · `/features/INDEX.md` (FEAT-102 bereits `in_progress` — korrekt, kein Edit) · `src/lib/workspace/rag.ts` · `src/app/api/cron/pending-signup-cleanup/route.ts` + `__tests__/route.test.ts` · `src/lib/cockpit/load-cross-tenant.ts` · `vitest.config.ts` · `docs/RUNBOOK.md`.

## Problems found

- PRD-SC2-Wortlaut "401" widerspricht DEC-262 + realem Pattern (403) — in Spec als korrigierter Drift dokumentiert (DEC gewinnt), kein PRD-Rewrite nötig.
- Sonst keine: alle Pfade/Symbole/Schema-Referenzen verifiziert, keine fehlenden Quellen.

## Open points / deferred

- AC-185-6 (SC6) + Coolify-Task-Anlage → /deploy V10.2.1.
- Only-missing-Re-Embed-Optimierung + Aggregat-RPC → geparkt (PRD Out-of-Scope / DEC-262 rejected).

## Recommended next step

`/backend SLC-185 MT-1` — Worktree `v10-2-1-embed-reconcile` anlegen (worktree-setup-Playbook), dann TDD-Extraktion `getTenantCoverage` in `rag.ts`. Danach ist per Workflow-Regel `/qa` nach dem MT-Block Pflicht.
