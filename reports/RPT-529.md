---
id: RPT-529
date: "2026-06-24"
skill: /architecture
slice: SLC-172
feature: FEAT-092
type: completion
status: completed
result: "Wiring fixiert nach Founder-Entscheid Vertiefung-Surfacing=A/adaptiv. DEC-249 + ARCHITECTURE §13 + MIG-127 reserviert. Korrigiert die 'migrationsfrei'-Annahme. Slice neu in 3 MTs. NEXT /backend SLC-172 MT-1."
title: "Architecture SLC-172 — Blueprint-Diagnostik Wiring (Reconciliation, adaptive Vertiefung, KU-Seed-RPC)"
---

# Architecture — SLC-172 Blueprint-Diagnostik (Wiring)

## Outcome
Fokussierter /architecture-Schritt für SLC-172, ausgelöst durch den Founder-Entscheid **Vertiefung-Surfacing = A (adaptiv)** (M-BP §7.7), der den Slice über die ursprüngliche „code-only/migrationsfreie Reuse"-Annahme hinaus erweitert hat. Vier Wiring-Fragen gegen den heutigen Code verifiziert und in **DEC-249** + **ARCHITECTURE §13** fixiert; **MIG-127** reserviert. Slice von 2 auf **3 MTs** restrukturiert + **AC-172-6** (adaptiv) ergänzt. **Vollständig** — bereit für /backend.

## Verifizierter Code-Befund (Reuse-Anker)
- `src/workers/diagnosis/handle-diagnosis-job.ts` — keyed ALLES auf **einen** `block_key`: lädt `diagnosis_schema.blocks[block_key]` UND KUs `WHERE capture_session_id=… AND block_key=…`. → Reconciliation-Kern.
- `src/app/admin/debrief/[sessionId]/[blockKey]/diagnosis-actions.ts` — `triggerDiagnosisGeneration` existiert: enqueued `diagnosis_generation` (Payload `{block_checkpoint_id, block_key, session_id}`), **bereits tier-gated** via `assertSessionTierAllows(…, "diagnosis_generation")`; nur `strategaize_admin`-Rollencheck ist debrief-spezifisch. `fetchDiagnosis` ist reusebar für den Reader.
- `src/lib/auth/assert-session-tier.ts` — `fn_session_tier_allows` mappt `diagnosis_generation` → Tier `blueprint` (Tier-Arbeit ist DONE).
- KU-Write-Muster aus `rpc_finalize_partner_diagnostic` (MIG-094) als Vorlage; **kein** Reuse direkt (das finalisiert die Session — Blueprint braucht KUs als Input, nicht als Deliverable).
- Adaptive Live-Per-Antwort-Ampel: **existiert nicht** (nur generischer Per-Block-Chat `api/chat/block`). → neu zu bauen.

## Architektur-Entscheidungen (DEC-249)
1. **Reconciliation:** KUs mit `block_key ∈ {A..G}` seeden (nicht `stufe1/stufe2`); pro A–G aus `diagnosis_schema.blocks[X].subtopics[].question_keys` einsammeln; **7** `diagnosis_generation`-Jobs → Engine läuft unverändert → 7 `block_diagnosis`.
2. **Adaptive Vertiefung (Choice A):** statische Kopplungstabelle (F-BP-004→016/a2, 005→017/b1, 007→018/c1, 009→019/d1, 013→020/f1) + synchroner `assessAnswerAmpel` (EU-Bedrock, temp 0, ~256 tok) → gelb/rot blendet gekoppelte Vertiefungsfrage ein; Ampel in `capture_session.answers._adaptive_ampel`; `error_log`-Audit; **kein** `ai_cost_ledger` in V1 (Tradeoff → KNOWN_ISSUES).
3. **KU-Seed:** thin atomarer RPC `rpc_seed_blueprint_diagnosis_input` (**MIG-127**, backend.md Decision-Tree). **Korrigiert** die Slice-Annahme „MIG reserviert: keine".
4. **Trigger:** tenant-scoped Klon von `triggerDiagnosisGeneration` (Owner-Auth statt `strategaize_admin`, Tier-Gate bleibt).
5. **Reader/Routing:** `SubtopicDiagnosisCard` (Ampel/Reifegrad/Empfehlung, Layout-Reuse `BerichtRenderer`/`BlockSectionCard`) + deterministisches Modul-Routing aus `metadata.routing[]` × `block_diagnosis`-Ampel (kein LLM).

## Files changed
- `/docs/DECISIONS.md` — DEC-249 ergänzt.
- `/docs/MIGRATIONS.md` — MIG-127 reserviert (oberhalb MIG-126).
- `/docs/ARCHITECTURE.md` — §13 (SLC-172 Wiring) ergänzt.
- `/slices/SLC-172-stb-blueprint-diagnostik.md` — MIG-Zeile korrigiert, Founder-Entscheid-Zeile, AC-172-5 erweitert + AC-172-6 neu, Micro-Tasks 2→3.
- `/docs/STATE.md` — Current Focus + Phase auf SLC-172 /architecture DONE.

## Files created
- `/reports/RPT-529.md` — dieser Report.

## Problems found
- Der Explore-Erstbefund „diagnosis_generation-Trigger existiert nicht" war **falsch** — er existiert (admin-gated). Korrigiert durch direkten grep/Read.
- **Migrationsbedarf-Korrektur:** SLC-172 ist NICHT migrationsfrei (MIG-127). Die DEC-244-Formulierung „migrationsfrei" galt nur dem Trigger-Pfad, nicht dem neuen KU-Seed.
- Slice-Spec wurde versehentlich zuerst im Main-Checkout editiert; nach Worktree verschoben, Main wieder clean (Konsistenz auf Branch `v10-slc170b-blueprint-seed`).

## Open points (für /backend)
- (1) Verlangt `rpc_create_diagnosis`/`block_diagnosis` einen Checkpoint mit gleichem `block_key=X` (→ 7 thin Checkpoints in MIG-127) oder darf der Capture-Checkpoint für alle 7 referenziert werden? MIG-050/052 + `rpc_create_diagnosis` lesen.
- (2) Wizard-Einblende-UX der adaptiven Vertiefungsfrage (inline vs. Folge-Step).
- (3) Latenz-Budget der bis zu 5 synchronen Assess-Calls (ggf. nur beim Block-Advance).

## Recommended next step
`/backend SLC-172 MT-1` (Capture-Eintritt + adaptive Vertiefung). Eigener Worktree-Branch `v10-slc172-blueprint` (SaaS-Pflicht) ab dem aktuellen V10-Stand; Merge nach /qa-PASS. MIG-127 wird in MT-2 geschrieben, Live-Apply erst im /deploy.
