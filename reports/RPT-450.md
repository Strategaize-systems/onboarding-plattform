---
id: RPT-450
date: "2026-06-12"
skill: /deploy
slice: SLC-V9.1-A..D
feature: FEAT-075..079
type: completion
status: completed
reviewOf: null
result: "Deploy V9.1 DONE als REL-031 + Live-Smoke PASS. Master-Merge 580ea77 -> main, Coolify-Redeploy auf App-Image 580ea77, App+Worker healthy, IMAP-ENVs injiziert, alle 3 Cron-Endpoints HTTP 200 + funktional, error_log-Audit live, Setup-UI-Route deployed. Real-IONOS-IMAP-End-to-End gated auf Founder-Setup-UI-Endpoint-Anlage (/post-launch)."
title: "Deploy V9.1 — Forward-Bucket-Email (REL-031, Master-Merge + Coolify-Redeploy + Live-Smoke)"
---

# Deploy V9.1 — Forward-Bucket-Email (SLC-V9.1-A..D)

## 1. Deployed what / to where

- **Version:** V9.1 Forward-Bucket-Email (FEAT-075..079, SLC-V9.1-A..D)
- **Merge:** `v9-1-forward-bucket-email` → `main` `--no-ff`, Merge-Commit **`580ea77`** (51 V9.1-Commits + Merge), gepusht `ed71f36..580ea77`
- **Target:** OP Coolify-Stack `159.69.207.29`, App-Container `app-bwkg80w04wgccos48gcws8cs-071901333668` + Worker, Domain `onboarding.strategaizetransition.com`
- **Image-Commit live:** `580ea77` (SOURCE_COMMIT verifiziert)
- **Delivery-Mode:** SaaS, aber V9.1 strikt Internal-Test-Mode (Founder-only)

## 2. Prerequisites (Phase 1) — confirmed

| Prerequisite | Status |
|---|---|
| Server + Coolify (159.69.207.29) | ✅ laufender Stack |
| DNS / Domain | ✅ live |
| IMAP-ENVs (HOST/PORT/USER/PASSWORD) + CRON_SECRET in Coolify | ✅ gegen Coolify-DB verifiziert, post-Redeploy im Container injiziert |
| `INBOUND_DEFAULT_ENDPOINT_SLUG` | ✅ bewusst weggelassen (Single-Mailbox-Fallback, DEC-R1-2) |
| 3 Coolify-Scheduled-Tasks | ✅ registriert (`*/5` / stuendlich / `0 2 * * *`), korrekte Endpoints + `x-cron-secret` |
| Migrationen MIG-057..063 (112-118) | ✅ bereits LIVE (additiv, pre-deploy) |

## 3. Pre-Merge-Re-Check — PASS

- **Rebase:** nicht noetig — `origin/main` 0 Commits voraus (Branch strikter Descendant). Clean.
- **Tests post-rebase:** Code byte-identisch zum QA-PASS-Stand (Diff seit `6a86c3e` = docs-only). `tsc --noEmit` EXIT 0 am Deploy-HEAD re-bestaetigt. V9.1-Vitest 291 pass (RPT-447) traegt 1:1.
- **MIG-Kollision:** keine (main unveraendert).
- **Pattern-Drift:** keine (kein paralleler Slice gemergt). IMAP-Sync BS-Origin in sync (RPT-447).
- **Cross-Repo-Pattern:** in sync.
- **Manual-Diff-Review:** Scope 113 Files in V9.1-Dirs (src/lib, src/app, sql/migrations, workers, tests, slices) — matcht FEAT-075..079, keine Ueberraschungs-Files.

## 4. Deployment Execution (Phase 2)

1. Master-Merge `--no-ff` → `580ea77`, `git push origin main` (`ed71f36..580ea77`).
2. **Coolify-Auto-Deploy feuerte NICHT** (GitHub→Coolify-Webhook inaktiv trotz `is_auto_deploy_enabled=t`) → Founder-Manual-Redeploy via Coolify-UI. → neuer Container `app-...-071901333668` + Worker, beide `(healthy)`.
3. Verifikation: `SOURCE_COMMIT=580ea77` ✅; IMAP-ENVs injiziert (`IMAP_HOST=imap.ionos.de`, USER 31B / PORT 4B / PASSWORD 17B / CRON_SECRET 65B) ✅; `/api/health` 200 ✅.

## 5. Live-Smoke — PASS

| Check | Ergebnis |
|---|---|
| `POST /api/cron/inbound-email-imap-sync` (auth) | **HTTP 200** `{success:true, synced:0, skipped:0, errors:0, lastUid:0}` — graceful no-op |
| `POST /api/cron/email-bulk-pipeline-trigger` (auth) | **HTTP 200** `{success:true, runs_evaluated:0, ...}` |
| `POST /api/cron/bulk-email-retention-sweep` (auth) | **HTTP 200** `{success:true, ..., duration_ms:16}` |
| Cron-Endpoints vor Deploy 404, jetzt 200 | ✅ Routen deployed |
| `error_log`-Audit-Trail live | ✅ 6 Eintraege fuer alle 3 Crons + `endpoint-resolver`-Warning |
| Setup-UI-Route `/dashboard/bulk-email-import/forward-setup` | ✅ 307 → /login (deployed, auth-guarded, kein 404) |
| App-Routing `/` | ✅ 307 → /login (keine Regression) |

**Wichtiger Befund (erwartbar, kein Defekt):** `inbound-email-imap-sync` abortet aktuell graceful VOR dem IMAP-Connect, weil noch **kein aktiver `email_inbound_endpoint`** existiert (`error_log`: `resolveDefaultEndpoint: no active email_inbound_endpoint and no INBOUND_DEFAULT_ENDPOINT_SLUG set`). Der **Real-IONOS-IMAP-Connect-Smoke** (und damit die finale Validierung der IMAP-Credentials) ist gated auf die **einmalige Founder-Endpoint-Anlage via Setup-UI** (DSGVO-Disclaimer → Endpoint + Setup-Token). Wird in `/post-launch` bestaetigt.

## 6. Project records updated

- `planning/roadmap.json` — v9-1 `active` → **`released`**
- `planning/backlog.json` — BL-153..158 `in_progress` → **`done`** (6 Items)
- `slices/INDEX.md` — SLC-V9.1-A..D `in_progress` → **`deployed`**
- `features/INDEX.md` — FEAT-075..079 `in_progress` → **`deployed`**
- `docs/RELEASES.md` — **REL-031** angelegt
- `docs/STATE.md` — High-Level State → `post-launch`, Current Focus/Phase → deployed + Live-Smoke + Open-Point, Last Stable bleibt V9
- `reports/RPT-450.md` — dieser Report

## 7. Problems found

- **Coolify-Auto-Deploy-Webhook feuert nicht** (Push triggert keinen Rebuild trotz `is_auto_deploy_enabled=t`). Manual-Redeploy noetig. Bekanntes Cross-Repo-Muster (IS: "kein Auto-Webhook"). Kein V9.1-Defekt — Operativ/Infra.
- Akzeptierte Residual (RPT-448/449): npm-audit pre-existing framework/SDK-transitiv; ISSUE-095/096 (Low); 2 pre-existing ESLint-Issues.

## 8. Open points / deferred

- **Founder-Setup-UI-Endpoint-Anlage** (einmalig) → entriegelt Real-IONOS-IMAP-Sync. Bis dahin synced der Cron 0 (graceful).
- **/post-launch V9.1 T+24h** (~2026-06-13): Burn-In + Real-IONOS-Smoke nach Endpoint-Anlage. Erst danach V9.1 STABLE + Last-Stable=V9.1.

## 9. Rollback awareness

`git revert 580ea77` + Redeploy, ODER Coolify-Image-Revert auf `00e43e9` (REL-030). Migrationen additiv+LIVE → kein DB-Rollback. Bei Rollback die 3 Scheduled-Tasks deaktivieren (sonst 404-Noise).

## 10. Phase 3 — Learning

Keine neuen Deploy-Blocker. Self-Hosted-Playbook unveraendert gueltig. Eine wiederkehrende Beobachtung (OP Coolify-Auto-Deploy-Webhook inaktiv → Manual-Redeploy) ist bereits Cross-Repo bekannt; kein neuer Playbook-Eintrag noetig.

## 11. Recommended next step

**`/post-launch V9.1`** (T+24h, ~2026-06-13) — nach Founder-Setup-UI-Endpoint-Anlage: Real-IONOS-IMAP-Smoke + Burn-In-Bestaetigung → V9.1 STABLE + Last-Stable=V9.1. Kein Customer-Outreach (module-lifecycle-discipline + IMP-950).
