---
id: RPT-625
date: "2026-07-09"
skill: /qa
slice: SLC-191
feature: FEAT-108
type: completion
status: completed
reviewOf: null
result: "Plan-QA V10.5 (SLC-191+192) PASS — Grounding-Gates gegen reales Repo verifiziert; 0 Blocker/0 High, 1 Medium (berater-Zweig-Quelle) + 2 Low, alle in Spec nachgetragen"
title: "Plan-QA V10.5 Exit-Ready Produkt 1 (SLC-191 Kern + SLC-192 Positionierung)"
---

# Plan-QA V10.5 — Exit-/Devil's-Advocate-Report (SLC-191 + SLC-192)

Pflicht-/qa nach /slice-planning (RPT-624). **Reine Plan-QA — noch kein Code.** Verifiziert wurden die Spec-Grounding-Gates (File-/Symbol-/Schema-/Test-Infra-/Reuse-Verifikation) gegen das reale OP-Repo, MT-Dependency-Konsistenz, DEC-Reconciliation (DEC-272..276) und Cross-Slice-Dependencies.

## 1. QA-Scope
- `slices/SLC-191-exit-report-kern.md` (MT-0..MT-5)
- `slices/SLC-192-exit-report-positionierung.md` (MT-1..MT-4)
- Grounding gegen reales Repo `c:\strategaize\strategaize-onboarding-plattform`

## 2. Verdict
**PASS** (Plan buildable). Grounding solide. 0 Blocker, 0 High, 1 Medium, 2 Low — alle direkt in die SLC-191-Spec nachgetragen (kein offener Blocker fuer den Implementierungsstart).

## 3. Grounding-Verifikation (gegen reales Repo)

### File-Existence — 6/6 ✓
| Claim | Realitaet |
|---|---|
| `src/lib/pdf/exit-report/` existiert NICHT | ✓ bestaetigt (NEU) |
| `src/app/admin/debrief/[sessionId]/exit-report/` existiert NICHT | ✓ bestaetigt (NEU) |
| `.../fahrplan-report/route.ts` existiert (Reuse-Vorlage) | ✓ route.ts + route.test.ts |
| `src/lib/pdf/fahrplan-report/{data,framing,types,renderer,index}` | ✓ alle + Tests vorhanden |
| `src/lib/pdf/mandanten-report-v2/{fonts,theme}` | ✓ fonts.ts + theme.ts |

### Symbol-Verifikation — ✓ (mit 1 Nuance)
- framing.ts: `SCOPE_SENTENCE`(10)/`ownerOrFallback`(15)/`exitCoupling`(35)/`prioritize`(88)/`scopeEstimate`(100) — alle exportiert ✓
- data.ts: `buildFahrplanInput`(120)/`loadFahrplanInput`(240) exportiert ✓
- renderer.tsx: `renderFahrplanReportPdf`(328) + `renderToBuffer`(import 15) ✓
- `getTemplateById`(template-queries.ts:73) ✓ · `computeModulReifeAmpel`(reife-ampel.ts:44) ✓
- supabase: `createClient`=cache(async) (server.ts:12) · `createAdminClient` sync (admin.ts:10) ✓
- Barrel `fahrplan-report/index.ts`: types + buildFahrplanInput/loadFahrplanInput + renderFahrplanReportPdf ✓
- **Nuance (Low):** `parseBlock`(49)/`parseQualityReport`(85) in data.ts sind **nicht exportiert** — Spec listete sie unter „Reuse". MTs nutzen sie nur transitiv via `loadFahrplanInput` bzw. bauen das Muster nach (SLC-192 MT-2 sagt bereits „Muster"). Kein Blocker; Spec um Direktimport-Warnung ergaenzt.

### Schema-Grounding — ✓
- `capture_session` MIG-021: tenant_id(29)/template_id(30)/template_version(31) ✓; `answers jsonb` MIG-030 ✓; `tier` existiert + wird von der Fahrplan-Route (`select("tenant_id, tier")`) genutzt ✓
- `template.diagnosis_schema` MIG-051 ✓ — **und mit `question_keys` geseedet** (Exit-Readiness-Template, `a1_grundverstaendnis`→[F-BP-001..003] etc.) → **statische Haelfte von Q-V10.5-H bereits GRUEN**
- `block_diagnosis` MIG-050 (content jsonb, RLS admin_full + tenant_read) ✓
- `block_checkpoint.quality_report` MIG-040 ✓ · `fn_tier_rank` MIG-121 ✓

### Route-Reuse (MT-5) — ✓ mit Erweiterung (Medium)
Fahrplan-Route bestaetigt als Vorlage: `createClient`(10) + `createAdminClient`(11/40) + capture_session select tenant_id,tier(43) + Tenant-Scope(51: `strategaize_admin` cross-tenant ODER same-tenant) + `fn_tier_rank`(56, BLUEPRINT_RANK=1). **Medium:** Die Fahrplan-Route hat **KEINEN berater-Zweig** — MT-5 fuegt ihn als bewusste Erweiterung hinzu (DEC-276 „admin/berater"). Quelle war in der Spec nur an die Fahrplan-Route gebunden, die den Zweig gar nicht enthaelt → Grounding-Luecke. Kanonische V10.4-Quelle lokalisiert und in Spec nachgetragen: `resolveWorkspaceScope()` (workspace-scope.ts:30) + `scopeTenants()` (tenant-scope.ts:32) + `berater_tenant_assignments`; Rollen-Literal `strategaize_berater`.

### Test-Infra — ✓
- `vitest.config.ts`: environment `node`, include `["src/**/*.test.ts", "src/**/*.test.tsx", ...]` → exit-report-Tests werden collected ✓; kein jsdom → Pure-Mock-Ansatz tragfaehig
- **Baseline gefahren:** `vitest run src/lib/pdf/fahrplan-report/` = **25/25 PASS** (data 7, framing 15, renderer 3, 1.23s) — inkl. `renderToBuffer`-Node-Env-Smoke (303ms), bestaetigt das Renderer-Smoke-Muster fuer MT-4. TEST_DATABASE_URL-Warnung erscheint, betrifft die Pure-Tests nicht.

### Records — ✓
DEC-272..276 vorhanden + inhaltlich deckungsgleich mit Spec-Referenzen · FEAT-108/109 planned · SLC-191/192 in slices/INDEX (Status-Spalte korrekt Pos. 4, planned) · roadmap V10.5 active.

### MT-Dependency- & Cross-Slice-Konsistenz — ✓
MT-0 blockiert MT-2/MT-3-Fallback · MT-1→MT-2/MT-3→MT-4→MT-5 zyklenfrei · SLC-192 blockiert-von SLC-191 (kumulativer Branch) · geteilte Dateien renderer.tsx + index.ts (191 NEU → 192 MODIFY), sequenziell, 0 MIG → keine Kollision.

## 4. Findings

### Medium
- **MT-5 berater-Zweig ohne gegroundete Quelle (Plan-Grounding-Luecke).** Spec band die berater-Autorisierung an `fahrplan-report/route.ts`, die keinen berater-Zweig hat. Ohne Pointer bestand das Risiko, einen unverifizierten authz-Zweig zu erfinden. **Behoben:** SLC-191 MT-5 um kanonische Quelle ergaenzt (`resolveWorkspaceScope`/`scopeTenants`/`berater_tenant_assignments`, Literal `strategaize_berater`, Zugang nur wenn `session.tenant_id ∈ allowedTenantIds`). Zu implementieren in MT-5 (blockiert MT-0..MT-4 nicht).

### Low
- **parseBlock/parseQualityReport non-exported.** Spec-Symbol-Liste implizierte Reuse; sie sind intern. **Behoben:** Direktimport-Warnung in SLC-191 Symbol-Verifikation ergaenzt.
- **`tier`-Spalten-Attribution kosmetisch.** Schema-Grounding schreibt `tier` MIG-021 zu; die Spalte existiert + wird live genutzt, stammt aber aus einer spaeteren Migration. Keine funktionale Auswirkung — nicht korrigiert (rein dokumentarisch).

## 5. Offene Risiken / verbleibende Checks
- **MT-0-Grounding-Spike bleibt Pflicht (Live-Haelfte):** statisch ist Q-V10.5-H GRUEN (diagnosis_schema in MIG-051 geseedet), aber **live**: (a) ist die Migration auf der Coolify-DB appliziert + fuer die genutzten Templates befuellt? (b) Q-V10.5-J: existiert ≥1 `capture_session` mit befuelltem `block_diagnosis` + beantworteten `owner_dependency`-Fragen? Entscheidet Block- vs. Subtopic-Granularitaet des Owner-Dependence-Index bzw. synthetische Fixture. Read-only, kein Blocker fuer MT-1.
- **Route-Auth/Tier/PDF (MT-5)** = Browser-/Live-Smoke in der Slice-/qa bzw. /deploy (401 unauth, 403 fremd-tenant/free-tier, 200 pdf) — nicht in dieser Plan-QA verifizierbar (kein Code).
- **Q-V10.5-G** (exakte Owner-Dependence-Index-Gewichtung) bleibt per TDD im Test zu fixieren (MT-2) — wie in der Spec vorgesehen.

## 6. Readiness-Assessment
Der V10.5-Plan ist **implementierungsreif.** Grounding gegen das reale Repo ist ueberdurchschnittlich sauber (Files/Symbole/Schema/Test-Infra/Reuse alle belegt, Reuse-Modul-Tests gruen). Die einzige echte Luecke (berater-Zweig-Quelle) ist geschlossen. Start-Empfehlung: MT-0-Spike zuerst, dann TDD-Kern.

## 7. Project-Record-Updates
- `docs/STATE.md` — Current Phase + Focus-Tail auf „Plan-QA PASS → SLC-191 MT-0" (surgical; STATE.md-Bloat = separater IMP-1708-Task, hier nicht adressiert)
- `slices/SLC-191-exit-report-kern.md` — 2 Grounding-Haertungen (Symbol-Nuance + MT-5 berater-Quelle) nachgetragen
- `reports/RPT-625.md` — dieser Report
- slices/INDEX unveraendert (Plan-QA implementiert nicht → SLC-191/192 bleiben `planned`)

## 8. Next workflow step
**`/backend` SLC-191 — beginnend mit MT-0 (Grounding-Spike, read-only) im Worktree `<repo>.worktrees/v10-5` (Branch `v10-5-exit-report`).** Danach TDD-Kern MT-1→MT-5, dann `/qa` SLC-191. SLC-192 in eigener Session nach SLC-191-Merge. (Kein Skip zu Cross-Repo/anderem Thema: V10.5 hat weder /final-check noch /go-live noch /post-launch — die stehen nach der Implementierung an.)
