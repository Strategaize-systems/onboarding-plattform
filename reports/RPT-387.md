---
id: RPT-387
date: "2026-06-02"
skill: /backend
slice: SLC-165
feature: FEAT-070
type: completion
status: completed
reviewOf: null
result: "V9 SLC-165 MT-6 Detail-View + 27/27 RLS-Pen-Test-Matrix Code-Side DONE, 0 Regression, 27/27 GREEN gegen Coolify-DB."
title: "V9 SLC-165 MT-6 — Bulk-Run-Detail-View + RLS-Pen-Test-Matrix (Code-Side)"
---

# RPT-387 — V9 SLC-165 MT-6 /backend Code-Side

## 1. Outcome Summary

MT-6 (Detail-View + 16+ RLS-Pen-Test-Matrix) ist **vollstaendig Code-Side abgeschlossen**.

- 3 neue Files unter dem V9-Worktree (Detail-Page + RLS-Tests + Fixtures), zusammen 825 Zeilen Code/Tests.
- 27 RLS-Pen-Test-Cases (24 Matrix + 3 Cross-Cut-Defense) **GREEN gegen Coolify-DB in 2.68s** via node:22-Sidecar.
- TSC + ESLint EXIT=0, Full-Suite lokal 1301 PASS / 462 deferred / 26 skipped = **0 Regression** vs MT-5-Baseline (Delta +27 = exakt die neuen MT-6-Tests).
- SLC-165 jetzt **8/9 MTs done** (MT-0 + MT-1b + MT-2a + MT-2b + MT-3 + MT-4 + MT-5 + MT-6). MT-1 (Test-Email-Corpus) bleibt User-Pflicht-blockiert.

Worktree HEAD nach 3 atomaren MT-6-Commits, **NICHT gepusht** (folgt /qa-Pattern aus RPT-384/385/386).

**Status**: fully complete fuer MT-6 Code-Side. Live-Smoke (Detail-View im Browser) bleibt fuer Gesamt-/qa nach Test-Corpus-Bereitstellung.

## 2. Files Reviewed

- `/slices/SLC-165-v9-foundation-upload.md` (MT-6-Spec Lines 174-185 + AC-Matrix Lines 197-211)
- `/sql/migrations/106_v9_bulk_email_schema.sql` (RLS-Policies Lines 60-450)
- `/src/__tests__/migrations/106-v9-bulk-email-schema.test.ts` (Coolify-DB-Test-Pattern)
- `/.claude/rules/coolify-test-setup.md` (TEST_DATABASE_URL + node:22 + SAVEPOINT)
- `/src/app/dashboard/bulk-email-import/page.tsx` (Auth-Gate + Detail-Link-Target)
- `/src/app/dashboard/bulk-email-import/actions.ts` (getBulkRunById Pre-Declaration aus MT-4)
- `/src/app/dashboard/bulk-email-import/helpers.ts` (BulkRunStatus + BulkRunSummary Types)
- `/src/__tests__/rls/v4-fixtures.ts` (V4-Fixtures-Pattern fuer Reuse)
- `/src/__tests__/rls/v4-perimeter-matrix.test.ts` (V4-Matrix-Vorlage)
- `/src/__tests__/rls/block-review-rls.test.ts` (SAVEPOINT-Wrap-Pattern fuer Reuse)
- `/src/__tests__/rls/v42-foundation-rls.test.ts` (V4.2-RLS-Matrix-Pattern)
- `/src/test/db.ts` + `/src/test/auth-context.ts` (Test-Helper)
- `/src/components/diagnose/BerichtPendingPoller.tsx` (Polling-Pattern-Kandidat)
- `/src/app/admin/handbook/AutoRefresh.tsx` (Polling-Pattern AUSGEWAEHLT)
- `/src/app/dashboard/bulk-email-import/BulkEmailUploadZone.tsx` (Pattern-Quelle Drag-Drop)

## 3. Files Changed

- `/docs/STATE.md` — Current Focus auf MT-6 Code-Side DONE umgestellt, vorheriges MT-5-/qa-Resultat als `(Vorheriger Outcome ...)` archiviert.

## 4. Files Created

- `/src/app/dashboard/bulk-email-import/[run_id]/page.tsx` (~270 Zeilen) — Bulk-Run-Detail-View Server-Component:
  - **Auth-Gate**: identisch zu `../page.tsx` (tenant_admin only; strategaize_admin redirect zu /dashboard — V9.0 hat keinen Cross-Tenant-Admin-View).
  - **Pipeline-Progress**: 8 logische Steps (`upload`, `parse`, `pre_filter`, `thread_redact`, `pattern_extract`, `curation`, `import`, `complete`). Jeder Step hat einen `StepState` (`done` / `active` / `pending`) via Pure-Function `stepStateFor(step, status)` ueber den `STATUS_ORDER`-Index. `failed`-Sonderfall: `upload` ist `done`, alles andere `pending` — Failure-Banner oberhalb zeigt die Ursache.
  - **Live-Polling**: reused `AutoRefresh` aus `src/app/admin/handbook/AutoRefresh.tsx` mit `intervalMs=3000`. Nur gerendert wenn `!isTerminal(run.status)` (nicht `completed`/`failed`).
  - **Failure-Banner**: bei `status='failed'` ein rotes Banner mit `failure_reason`.
  - **Kennzahlen-Card**: zeigt source_file_name, email_count, content_emails, thread_count, patterns_extracted/accepted/imported, total_cost_eur (formatiert als EUR), created_at, updated_at.
  - **404-Pfad**: `notFound()` bei RLS-Miss oder unbekannter ID — kein Unterschied nach aussen.
  - **Sensitiv-Dispatch**: `storage_path` bleibt **bewusst NICHT** im UI; RLS-Backstop reicht.

- `/src/__tests__/rls/v9-bulk-email-fixtures.ts` (~140 Zeilen) — V9-Fixtures-File:
  - Extends `seedV4Fixtures` (2 Tenants + 7 User).
  - Erzeugt je 1 Row pro Tenant in `email_bulk_run`, `email_thread`, `email_message` (mit thread_id FK), `email_pattern` (mit thread_id NOT NULL FK).
  - Setzt voraus: Migration 106 LIVE (per RPT-382 MT-2b durch).
  - Reihenfolge: bulk_run → thread → message → pattern (Late-Binding FK email_message.thread_id wird beim INSERT erfuellt).

- `/src/__tests__/rls/v9-bulk-email.rls.test.ts` (~615 Zeilen) — RLS-Pen-Test-Matrix mit 27 Cases:
  - **email_bulk_run** (6 Tests): strategaize_admin SELECT cross-tenant → 2 Rows; tenant_admin SELECT own + cross-DENY; tenant_admin INSERT own ALLOW; tenant_admin UPDATE own ALLOW; tenant_member default-deny (SELECT 0 + INSERT DENY); employee default-deny (analog).
  - **email_message** (6 Tests, analog).
  - **email_thread** (6 Tests, analog).
  - **email_pattern** (6 Tests, analog).
  - **Cross-Cut-Defense** (3 Tests): tenant_admin INSERT mit Cross-Tenant `tenant_id` → WITH CHECK Reject; tenant_admin UPDATE Cross-Tenant-Row → 0 Rows (RLS USING filtert); strategaize_admin INSERT → DENY (kein admin_insert-Policy).
  - **SAVEPOINT-Pattern** (IMP-044) ueber Helper `expectRlsReject(client, query, params)` fuer alle erwarteten RLS-Rejections.

- `/reports/RPT-387.md` (dieses File) — Completion-Report.

## 5. Files Intentionally Not Changed

- `/slices/INDEX.md` — SLC-165 bleibt `in_progress` (Slice nicht komplett: MT-1 User-Pflicht-blockiert, MT-7-Records-Update steht aus, Gesamt-/qa nach MT-6 ausstehend).
- `/planning/backlog.json` — BL-147 bleibt `in_progress` (analog SLC-165).
- `/features/INDEX.md` — FEAT-070 bleibt `in_progress` (Slice nicht final done).
- `/planning/roadmap.json` — V9 bleibt `active` (kein Status-Wechsel).
- `/docs/KNOWN_ISSUES.md` — ISSUE-088 + ISSUE-089 bleiben `open` (kein Mini-Fix in MT-6 vorgenommen, beide cosmetic/defense-in-depth, geplant fuer MT-6 /qa-Phase).
- `/docs/MIGRATIONS.md` — keine Schema-Aenderungen in MT-6.
- `/src/app/dashboard/bulk-email-import/actions.ts` — `getBulkRunById` war bereits in MT-4 als Pre-Declaration angelegt, kein Edit noetig.
- `/src/app/dashboard/bulk-email-import/helpers.ts` — `BulkRunStatus` + `BulkRunSummary` waren bereits in MT-4 definiert.

## 6. Problems Found

Keine.

Sub-Notizen:
- **Vitest-Sidecar-Setup**: das Test-Repo `/opt/onboarding-plattform-test` auf dem Server war auf Branch `v9-bulk-email-import` HEAD `23bafee` (post-MT-2b). Da die RLS-Test-Datei nur `seedV4Fixtures` (bereits da) und mein neues `v9-bulk-email-fixtures.ts` importiert, reichte das Hochkopieren der 2 Test-Files via base64+scp. Die Detail-Page wurde NICHT auf den Server transferiert, weil sie keinen Vitest-Lauf im MT-6-Scope hat (UI ohne Test-Coverage in MT-6 — Detail-View-Test waere V9.1+).
- **Polling-Pattern-Wahl**: zwei Kandidaten existierten — `BerichtPendingPoller` (komplex, eigene API-Route, maxPolls + timedOut, fetcht JSON) und `AutoRefresh` (simpel, `router.refresh()` auf Server-Component). Ich habe AutoRefresh gewaehlt, weil unser `getBulkRunById` Server-Action die Daten via RLS bereits laedt und der Server-Component-Render Source-of-Truth bleibt — kein separater API-Endpoint noetig. Conditional-Render via `pollActive = !isTerminal(run.status)`.
- **failed-Status-Sonderfall**: vereinfachte Logik — `upload` ist immer `done` (sonst gaebe es keinen Run-Record), alle weiteren Stufen bleiben `pending`; die Ursache zeigt der Failure-Banner.

## 7. Open Points / Deferred Decisions

- **ISSUE-088** (`rpc_complete_ai_job` Skip-Pfad ohne Error-Check, handle-parse-job.ts:135) — Status: open, Low, ~2 Zeilen Mitigation, bewusst nicht in MT-6 gefixt. Empfohlen: 10-Min-Mini-Fix vor Gesamt-/qa SLC-165.
- **ISSUE-089** (Storage-Path-Tenant-Prefix-Re-Validation fehlt im Worker) — Status: open, Low, ~2 Zeilen Mitigation, defense-in-depth-Luecke ohne Live-Risk. Empfohlen: 10-Min-Mini-Fix vor Gesamt-/qa SLC-165.
- **MT-7 Records-Update** (Slice-Final-Status auf done, FEAT-070 → done, BL-147 → done) — deferred bis Gesamt-/qa SLC-165 + Live-Smoke PASS.
- **AC-8 Browser-Smoke der Detail-View Pipeline-Progress** — deferred bis Gesamt-/qa SLC-165 (kein Smoke in MT-6-Scope).
- **AC-9 1000-Email-Wall-Clock <5min Live-Smoke** — deferred bis MT-1 Test-Email-Corpus durch User bereitgestellt.
- **MT-1 Test-Email-Corpus** — User-Pflicht-blockiert (Founder muss ~100 anonymisierte .mbox-Emails bereitstellen).

## 8. Project Record Updates

- `/docs/STATE.md` — Current Focus auf MT-6 Code-Side DONE, vorheriges MT-5-/qa-Resultat als archivierter Vorheriger Outcome.
- `/reports/RPT-387.md` — dieser Report angelegt.
- `/slices/INDEX.md`, `/features/INDEX.md`, `/planning/backlog.json`, `/planning/roadmap.json`, `/docs/KNOWN_ISSUES.md`, `/docs/MIGRATIONS.md` — KEINE Aenderung (alle Records bleiben in vorherigem Zustand, weil SLC-165/BL-147/FEAT-070 alle `in_progress` bleiben bis Gesamt-/qa durch und MT-7-Records-Update folgt).

**Workflow-Closure-Defense-Note** (IMP-950): MT-6 ist Code-Side, kein Slice-Closure, kein Release. Workflow-Closure folgt erst nach Gesamt-/qa SLC-165 + ggf. /final-check + /go-live + /post-launch. STATE.md `High-Level State` bleibt unverändert (`released` aus V8.1.1-Stable-Context); kein "released" oder "stable" Claim fuer V9 in dieser Session.

## 9. QA after Implementation Steps

Pro CLAUDE.md ist `/qa` nach jedem `/backend`-Step Pflicht. Naechster Schritt **/qa SLC-165 MT-6** wird in separater Session empfohlen (siehe Recommended Next Step).

Mini-Checkliste fuer /qa MT-6 — empfohlene Inhalte:
- Stub-Detection auf 3 neue Files.
- Wiring-Audit Detail-Page → getBulkRunById → RLS.
- AC-Matrix: AC-SLC-165-8 (Pipeline-Status-View korrekt), AC-SLC-165-10 (16+ RLS-Cases GREEN — bereits 27 nachgewiesen), AC-SLC-165-11 (TSC/ESLint/Vitest grün).
- Optional-Mini-Fix ISSUE-088 + ISSUE-089.

## 10. Recommended Next Step

**`/qa SLC-165 MT-6`** in einer frischen Session — Code-Side Stub-Detection + Wiring-Audit (Detail-Page → getBulkRunById → RLS-Chain) + AC-Verifikation (AC-8/10/11 fuer MT-6) + optional Mini-Fix ISSUE-088 + ISSUE-089 (~10min, 4 Zeilen Code + 2 Vitest-Cases). Aufwand ~30-45min.

Danach **Gesamt-/qa SLC-165** in nochmal frischer Session — alle 8 MTs + AC-1..12 + Live-Smoke 1000-Email-Wall-Clock <5min sobald Test-Corpus bereit + Pattern-Reuse-Audit.

Alternative Resume-Pfade:
- ISSUE-088 + ISSUE-089 Mini-Fix vorab (~10min) bevor /qa MT-6 — sauberer Issue-Backlog vor /qa.
- BS V8.7-A Burn-In bis 2026-06-03 ~11:31 UTC laeuft (keine Aktion noetig).
- immoscheckheft V3.2 SLC-321 Master-Merge nach Gesamt-/qa V3.2 (Worktree HEAD `e0276fb` PASS-CONDITIONAL).
- MT-1 Test-Email-Corpus-Bereitstellung durch Founder → entblockt AC-9 + MT-1 Cost-Validation.

## 11. Quality Gates

| Gate | Ergebnis |
|------|----------|
| TSC `--noEmit` | EXIT=0 (Repo-weit) |
| ESLint (3 neue Files) | EXIT=0 |
| Vitest neue RLS-Tests (lokal ohne TEST_DATABASE_URL) | 27 Tests discoverable, alle mit erwartetem "TEST_DATABASE_URL is not set"-Fehler — Strukturcheck PASS |
| Vitest neue RLS-Tests (Coolify-DB via node:22-Sidecar) | **27/27 GREEN in 2.68s** im `bwkg80w04wgccos48gcws8cs_strategaize-net` gegen `supabase-db-bwkg80w04wgccos48gcws8cs-084548596447` |
| Full-Suite lokal (Regression) | 1301 PASS / 462 deferred / 26 skipped — **0 Regression** vs RPT-386 Baseline (PASS-Count unverändert, deferred-Delta +27 = exakt neue MT-6-Tests) |

## 12. Pattern Reuse (strategaize-pattern-reuse.md)

Pflicht-Workflow eingehalten:

- **Search**: Glob `**/__tests__/rls/**/*.test.ts` → 3 Treffer im Onboarding-Repo. Grep `setInterval|router\.refresh` → 12 Treffer mit AutoRefresh + BerichtPendingPoller als Polling-Pattern-Kandidaten.
- **Inspect**: Read der relevanten Files (v4-perimeter-matrix.test.ts, v42-foundation-rls.test.ts, block-review-rls.test.ts, AutoRefresh.tsx, BerichtPendingPoller.tsx).
- **Decide**: 1:1-Portierung mit Quell-Pfad-Header-Kommentaren in v9-bulk-email-fixtures.ts + v9-bulk-email.rls.test.ts. AutoRefresh-Komponente direkt importiert (kein Code-Duplikat).

**Quellen-Liste (im Code dokumentiert)**:
- `src/__tests__/rls/v4-fixtures.ts` — Basis-Fixtures (2 Tenants + 7 User).
- `src/__tests__/rls/block-review-rls.test.ts` — SAVEPOINT-Wrap-Pattern.
- `src/__tests__/rls/v4-perimeter-matrix.test.ts` — Matrix-Aufbau pro Rolle x Tabelle.
- `src/app/admin/handbook/AutoRefresh.tsx` — Polling-via-router.refresh().
- `src/app/dashboard/bulk-email-import/page.tsx` — Auth-Gate-Pattern.
- `.claude/rules/coolify-test-setup.md` — node:22-Sidecar + TEST_DATABASE_URL.

Keine neuen Pattern erfunden, kein Code-Duplikat.
