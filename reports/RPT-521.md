---
id: RPT-521
date: "2026-06-22"
skill: /backend
slice: SLC-174
feature: FEAT-094
type: completion
status: completed
reviewOf: null
result: "SLC-174 Modul-Output-Synthese-Worker /backend DONE — module_output_synthesis-Worker (lean Fan-out Draft + Bounded-Critic) schreibt modul_output-Triple + KI-Hebel (Reifegrad deterministisch aus Template-Katalog, DEC-245). 6 Lib/Worker-Files + 7 Test-Files (66 hermetische Tests GREEN) + claim-loop/run.ts-Registrierung, keine Migration. Gates tsc0/eslint0/vitest66-66/build:worker/next build PASS. NEXT /qa."
title: "Backend SLC-174 — StB Modul-Output-Synthese-Worker (lean Fan-out + Bounded-Critic + Cost-Cap)"
---

# /backend SLC-174 — Modul-Output-Synthese-Worker

## Outcome
Vollstaendig implementiert (Code-Side). Neuer Worker-Job `module_output_synthesis`: liest die Capture-Antworten (`block_checkpoint.content`, SLC-173) einer Modul-Session, erzeugt via lean Fan-out (1 Draft-Call) + Bounded-Critic (1 Call) das Liefer-Triple (entscheidung/standard/implementierungsschritt) + KI-Hebel-Liste (Reifegrad 1-4) und schreibt sie als `modul_output`-Rows (MIG-124) via service_role. Tier-Gate + Enqueue laufen ueber `rpc_enqueue_module_output` (SLC-169). KEINE Migration (Job-Typ/Tier/2 Cost-Rollen waren in MIG-124/DEC-241 vorreserviert).

Verifikation: `tsc --noEmit` 0, `eslint` 0, `vitest` 66/66 GREEN (hermetisch), `npm run build:worker` PASS (dist/worker.js inkl. neuem Handler), `next build` PASS. DB-Sidecar-Test (`slc174-modul-output-write`) ist geschrieben, laeuft im /qa gegen die Coolify-DB (node:20-Sidecar, TEST_DATABASE_URL).

## Architektur-Entscheidungen (DEC-245)
- **Reifegrad deterministisch aus Template-KI-Hebel-Katalog** (R-174-2/AC-174-6): hebel_id-/name-Match → Katalog-Reifegrad (autoritativ); Nicht-Katalog-Hebel → Modell-Vorschlag geclampt [1,4]; sonst Fallback 2.
- **Worker-LLM nach bulk-email-Muster** (DEC-235): eigener DI-Caller mit echter `response.usage`-Token-Usage statt `chatWithLLM` (das keine Token liefert) — noetig fuer token-genaue `ai_cost_ledger` (AC-174-4). Verfeinerung des Spec-Wortlauts „via src/lib/llm.ts".
- **Cost-Cap-Adaption**: Run-Cap in-Memory (akkum. USD→EUR dieses Laufs) + Tenant-Monatscap als Summe `ai_cost_ledger` (Modul-Rollen, aktueller Monat) — `email_bulk_run`-Pro-Run-Kostenspalte existiert hier nicht.
- **Fail-closed + Cleanup**: jeder Fehler → Job 'failed' + DELETE partieller Rows (ai_job_id). Idempotenz: Skip+Complete bei vorhandenen Outputs.

## Files created
- `/src/lib/stb-vertikale/module-context.ts` — pure: Template-Metadaten-Extraktion (output_contract, themenmodell, ki_hebel-Katalog) + Q&A-Assembly aus block_checkpoints.
- `/src/lib/stb-vertikale/synthesis-prompt.ts` — zod ModuleDraftSchema (Triple + ki_hebel) + Draft-/Critic-Prompt-Builder.
- `/src/lib/stb-vertikale/synthesize-module-output.ts` — DI-testbarer Bedrock-Sonnet-Caller (eu-central-1) + lean Draft-Call + shared `invokeModuleJson`.
- `/src/lib/stb-vertikale/critic.ts` — Bounded-Critic (1 Call) + deterministische `inferReifegrad`.
- `/src/lib/stb-vertikale/cost-cap.ts` — Run-Cap (pure) + Tenant-Monatscap (ai_cost_ledger-Store).
- `/src/workers/stb-vertikale/handle-module-output-job.ts` — Worker-Handler (executeModuleOutputJob + thin Wrapper).
- `/src/lib/stb-vertikale/__tests__/module-context.test.ts`, `synthesis-prompt.test.ts`, `synthesize-module-output.test.ts`, `critic.test.ts`, `cost-cap.test.ts` — hermetische Lib-Tests.
- `/src/workers/stb-vertikale/__tests__/handle-module-output-job.test.ts` — 9 Worker-Tests (DI + chainable Admin-Stub).
- `/src/lib/db/__tests__/slc174-modul-output-write.test.ts` — DB-Sidecar Write-/CHECK-Test (AC-174-7, /qa).

## Files changed
- `/src/workers/condensation/claim-loop.ts` — JOB_TYPES + `moduleOutputHandler`-Param + Dispatch-Branch.
- `/src/workers/condensation/run.ts` — Import + Log-Zeile + Registrierung im startClaimLoop-Call.
- `/docs/DECISIONS.md` — DEC-245.
- `/docs/STATE.md` — Current Focus + Current Phase.
- `/slices/INDEX.md` — SLC-174 → in_progress.
- `/features/INDEX.md` — FEAT-094 → in_progress.
- `/planning/backlog.json` — BL-513 → in_progress.

## Problems found / Limitations
- Draft-Qualitaet (~70-80%, AC-174-3) ist Code-Side NICHT beweisbar — erst Live an echten M-04-Antworten messbar (R-174-3, post-deploy observational). Code-Side beweist Pfad + Schema + Cost-Cap + Persist-Shape.
- DB-Sidecar-Test wurde lokal NICHT ausgefuehrt (kein TEST_DATABASE_URL) — laeuft im /qa gegen Coolify-DB.
- `module_output_synthesis`/`module_output_critic` als ai_cost_ledger-Rollen + Job-Typ sind LIVE (MIG-124 deployed) — im /qa kurz bestaetigen.

## Open points
- /qa SLC-174 (inkl. DB-Sidecar) → Pre-Merge-Re-Check → Master-Merge. Kein Deploy noetig (Routen/Worker hinter V10-Release; Env-Flag OFF). Worker-Redeploy erst beim V10-Deploy.

## Recommended next step
`/qa SLC-174`.
