---
id: RPT-615
date: "2026-07-08"
skill: /deploy
slice: V10.4
feature: FEAT-105+FEAT-106+FEAT-107
type: completion
status: completed
reviewOf: RPT-614
result: "V10.4 DEPLOYED (REL-041). Alle 5 Gates PASS: G-1 Pre-Merge-Merge+Push (origin/main 8329b23, Konflikte in Worktree geloest, Tests 99/99), G-2 MIG-132 Live-Apply verifiziert (role-CHECK 5, Tabelle+2 Policies+2 Index, 3 Functions, idempotent, survived recreate, PostgREST 401), G-3 2 DB-Sidecars 12/12 gegen Live-DB, G-4 Runtime-Auth-E2E PASS (echter GoTrue-Login->JWT->Scope=Kanzlei+Cascade, Fremd-Tenant fail-closed, cleanup 0-Rest), G-5 Container healthy + SOURCE_COMMIT=8329b23. 0 berater-User in Prod. Rollback RPO 0. Naechster Schritt: /post-launch V10.4."
title: "Deploy V10.4 — strategaize_berater P2 (REL-041)"
---

# Deploy V10.4 — Rollenmodell V2 P2 `strategaize_berater` (REL-041)

## Outcome
**V10.4 live auf Prod** (`onboarding.strategaizetransition.com`, SOURCE_COMMIT `8329b23`). Agent-autonom deployt.
Alle 5 Deploy-Gates aus RPT-614 in Reihenfolge PASS, 0 Blocker. Interne Workflow-Closure (module-lifecycle:
Founder-only, kein Customer-Go-Live).

## Pre-Deploy-Server-Precheck (BLOCKING) — PASS
- Identitaet: `coolify-ubuntu-4gb-nbg1-1` / DB-Container `supabase-db-...162242937585` (vor Apply bestaetigt).
- Disk 74G/451G (17%) · MemAvailable 20,6 GB · Swap 0 — Headroom ok. SSH agent-seitig funktionsfaehig.
- Alle Container `Up 42h (healthy)` (stabiler V10.3-Ausgangsstand). Update-Deploy (kein First-Deploy) — Infra-Prereqs bereits erfuellt.
- Auto-Deploy empirisch geprueft: Coolify-Flag `is_auto_deploy_enabled=t`, aber **kein GitHub-Webhook** feuerte den Push → manueller API-Trigger (coolify-autonomous-redeploy.md).

## Gate-Ergebnisse (in Ausfuehrungs-Reihenfolge)

### G-1 — Pre-Merge-Re-Check + Merge + Push — PASS
- `git fetch` → Branch 9 ahead / 2 behind origin/main (2 = docs-only RPT-606/607 + MIG-132-MIGRATIONS-Eintrag).
- Rebase-Versuch verwarf (9-Commit-Kaskaden-Konflikt) → stattdessen `git merge origin/main` (ein Konflikt-Resolve, git-release.md-Intent erfuellt). 3 Konflikte (STATE.md, features/INDEX, slices/INDEX) in der **Worktree** geloest (ours = V10.4-Superset: alle Slices/Features done); backlog.json + MIGRATIONS.md auto-merged (MIG-132-Rich-Entry erhalten). Merge `8329b23`.
- **Pre-Merge-Re-Check-Verdikt:** Rebase/Merge clean (Konflikte in Worktree) · Tests post-merge **99/99 PASS** · MIG-Kollision: MIG-132 einzige V10.4-Migration, keine · Pattern-Drift: keine (single-slice-family) · Cross-Repo: n/a · Manual-Diff: 34 Files V10.4-Scope, keine Ueberraschung.
- Push `b89aaa3..8329b23 → origin/main`.

### G-2 — MIG-132 Live-Apply (VOR Redeploy) — PASS
- Pre-Apply-Dump `/root/mig132_rollback/`: `schema_pre_mig132.sql` (11674 Z.) + `handle_new_user_pre.sql` + `profiles_role_check_pre.txt` (4-Werte-Def).
- Apply (base64-Transfer, `psql -U postgres -v ON_ERROR_STOP=1`): alle Statements clean, 0 Errors.
- **Verify:** role-CHECK = **5 Werte** · `berater_tenant_assignments` + 2 Policies (bta_admin_all, bta_berater_select_own) + 2 Index (pkey, idx_tenant) · 3 Functions (handle_new_user, berater_assigned_tenant_ids, can_see_tenant) · **Re-Apply idempotent = 0 Errors**.
- PostgREST/Kong-Smoke: `/rest/v1/berater_tenant_assignments` → **401** (nicht 404 → Schema reloaded, anon fail-closed).
- **Post-Recreate re-verifiziert** (Compose-Recreate gab neue Container-Namen, Volume persistent): role-CHECK 5 / Tabelle / 3 Functions weiterhin da.

### G-3 — DB-Sidecar-Gate (0 Regression changed-surface) — PASS
- Sync-Gate: `/opt/onboarding-plattform-test` von 33885d2 → `git reset --hard origin/main` = **8329b23** (HEAD-Parity).
- node:22-Sidecar im `bwkg80w04wgccos48gcws8cs_strategaize-net`, TEST_DATABASE_URL → Live-DB:
  `berater-foundation-rls.test.ts` (9) + `berater-visibility.test.ts` (3) = **12/12 PASS** gegen die persistente Live-DB.
  Die exakten PENDING-LIVE-Tests aus RPT-612 sind damit live eingeloest (Cascade, can_see_tenant, handle_new_user, RLS, Filter=Kanzlei∪Cascade/Fremd=0/Admin=alle).

### G-4 — Runtime-Auth-Chain-E2E (echter Serving-Pfad) — PASS
Gegen den deployten Stack (GoTrue + PostgREST via internem Kong):
1. Berater-User via GoTrue-Admin angelegt → **Profile ohne tenant_id** (handle_new_user berater-Zweig live).
2. Kanzlei `bbbb…0001` zugewiesen.
3. **Echter Passwort-Login** (GoTrue token grant) → JWT.
4. Scoping-RPC `berater_assigned_tenant_ids` mit **Berater-JWT** → `[Kanzlei + 2 Cascade-Mandanten]`.
5. Assertions: **Kanzlei in scope ✓ · Fremd-direct-Tenant NICHT in scope (fail-closed) ✓ · `can_see_tenant(fremd)` = false ✓**.
6. Cleanup: Assignment + auth.identities + profile + auth.users per ID geloescht → **users=0/profiles=0/assignments=0** (Prod hat wieder **0 berater-User**).
- Hinweis: der wget-`--method=DELETE`-Cleanup-Versuch im E2E-Script griff nicht (leftover User) → sofort per SQL-DELETE-by-ID nachgeholt und verifiziert (→ ISSUE-Learnings).

### G-5 — Container-Health post-Redeploy — PASS
- app/worker/kong/db alle `Up (healthy)` (5–6 min), SOURCE_COMMIT `8329b238…` == merged HEAD, `/api/health` `{status:ok}`.
- Extern: `/login` 200, `/admin/mein-tag` 307 (unauth-Redirect, middleware live).

## Migrations
- **MIG-132 APPLIED** (Live, 2026-07-08). Rollback-Artefakte `/root/mig132_rollback/`. Additiv, idempotent.

## ENV
- Gate 2: **0 neue/geaenderte ENV** in V10.4 (package.json/lock unveraendert) — kein ENV-Presence-Gate noetig.

## Rollback-Awareness
- Code: Coolify-Redeploy des vorherigen SOURCE_COMMIT `33885d2` (V10.3).
- DB: MIG-132-Revert aus `/root/mig132_rollback/` (DROP 2 Functions + Tabelle; role-CHECK→4; handle_new_user aus Pre-Dump).
- **RPO 0** (additiv, keine Datentransformation).

## Cockpit-Consistency-Sweep (Gate 4)
| Artefakt | V10.4-Stand | Konsistenz |
|---|---|---|
| roadmap.json v10-4 | `released` | ✓ (final-check RPT-613 + go-live RPT-614 + Deploy vorhanden) |
| slices/INDEX SLC-188/189/190 | `deployed` (3/3) | ✓ |
| features/INDEX FEAT-105/106/107 | `deployed` (3/3) | ✓ |
| backlog.json BL-530/534/535 | `done` (3/3) | ✓ |
Zaehl-Check: 3 Slices deployed == INDEX · 3 Features deployed == INDEX · 3 BL done == backlog. Roadmap-Status == Backlog-Progress == Feature-Status.

## Problems found
- **E2E-Cleanup-Falle:** `wget --method=DELETE` gegen GoTrue-Admin/PostgREST entfernte den Test-User NICHT (kein Fehler, aber User blieb). Per SQL-DELETE-by-ID (auth.users + profiles + identities + assignment) nachgeholt, 0-Rest verifiziert. → KNOWN_ISSUES/Playbook-Learning.
- **Auto-Deploy-Webhook feuert nicht** trotz `is_auto_deploy_enabled=t` — manueller API-Trigger noetig (bekanntes Gate-3-Muster).

## Recommended next step
**`/post-launch V10.4`** (T+3h + T+24h Burn-In gegen den deployten Container). Last-Stable-Flip auf V10.4 erst nach /post-launch T+24h-PASS. Parallel offen: /post-launch V10.3 T+24h → Last-Stable-Flip V10.2.1→V10.3 (jetzt durch V10.4-Deploy ueberholt — im /post-launch konsolidieren).
