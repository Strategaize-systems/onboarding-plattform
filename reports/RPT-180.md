---
id: RPT-180
date: "2026-05-07"
skill: /backend
slice: SLC-072
feature: FEAT-035
type: completion
status: completed
reviewOf: null
result: "SLC-072 Walkthrough-Whisper-Worker code-side done. Neuer Job-Handler walkthrough_transcribe in src/workers/walkthrough/handle-transcribe-job.ts, in claim-loop JOB_TYPES + Dispatch + run.ts registriert. 6/6 Vitest-Cases gruen (3 happy + 3 fail). Lint clean, Worker-Bundle 193kb. Naechster Schritt: User-Coolify-Redeploy + /qa SLC-072 mit Live-Smoke an existing uploaded walkthroughs (33ea58be... + 75098a5d...)."
title: "SLC-072 /backend code-side — walkthrough_transcribe Handler + Tests"
---

# SLC-072 /backend code-side — walkthrough_transcribe Handler + Tests

## Outcome Summary

SLC-072 Backend-Implementierung **code-side done**. Der neue Job-Handler `walkthrough_transcribe` ist im Worker-Container vollstaendig implementiert, in den Claim-Loop registriert und durch 6 Vitest-Cases (3 Happy + 3 Failed) abgedeckt. Lint sauber, Worker-Bundle baut auf 193 KB inkl. Handler.

**Pflicht-Gates noch offen** (per Slice-Spec QA-Vorgaben):
- Live-Smoke End-to-End auf Hetzner (User nimmt 1min auf → Worker pickt → Status `uploaded → transcribing → pending_review` < 2min)
- Worker-Container-Logs sauber ueber 5 aufeinanderfolgende Jobs
- Backwards-Compat: alte V2/V3/V4-Job-Types (`voice_transcribe`, `condense_block`, `bridge_run`, `block_review_pre_filter`, `handbuch_snapshot`) weiterhin pickbar
- ffmpeg-Sanity am laufenden Worker-Container (`ffmpeg -version`)

Diese Gates kommen in `/qa SLC-072` nach User-Redeploy. Slice-Status bleibt `in_progress`.

## Files reviewed

- `/slices/SLC-072-walkthrough-whisper-worker.md` — Slice-Spec
- `/src/workers/condensation/run.ts` — Worker-Entry
- `/src/workers/condensation/claim-loop.ts` — Claim-Loop + Job-Types
- `/src/workers/dialogue/handle-transcription-job.ts` — Pattern-Vorlage (Whisper + ffmpeg)
- `/src/workers/dialogue/audio-extract.ts` — `extractAudioBuffer` (Container-agnostisch, wird wiederverwendet)
- `/src/workers/capture-modes/walkthrough-stub/handle.ts` — Stub-Pattern (DEC-040 Hook-Konvention)
- `/src/lib/ai/whisper/factory.ts` + `provider.ts` + `local.ts` — Whisper-Adapter (DEC-018, unveraendert)
- `/src/lib/logger.ts` — error_log + captureException Pattern
- `/src/app/actions/walkthrough.ts` — Quelle der `walkthrough_transcribe`-ai_jobs-Eintraege
- `/sql/migrations/082_v5_walkthrough_capture_mode.sql` — knowledge_unit.source CHECK + walkthrough_transcript
- `/sql/migrations/083_v5_walkthrough_session.sql` — Status-Maschine + RLS
- `/Dockerfile.worker` — `apk add --no-cache ffmpeg` (Z. 28)
- `/vitest.config.ts` — Vitest-Setup

## Files created

- `/src/workers/walkthrough/handle-transcribe-job.ts` — Job-Handler `handleWalkthroughTranscribeJob`. 11-Schritt-Pipeline (Payload-Validation → Load-Session → Status-Skip → Status='transcribing' → Storage-Download → ffmpeg-Audio-Extract → Whisper-Transcribe → knowledge_unit-INSERT → Status='pending_review' → rpc_complete_ai_job → /tmp-Cleanup). Failed-Path setzt `walkthrough_session.status='failed'` + `error_log` + re-throw, /tmp-Cleanup im finally. Whisper-Adapter (DEC-018) unveraendert wiederverwendet, ffmpeg via existierendem `extractAudioBuffer` (Container-agnostisch — funktioniert fuer WebM via Magic-Byte-Detection genauso wie fuer MP4). Service-Role-Pfad nimmt `tenant_id` aus walkthrough_session und schreibt KU mit identischem tenant_id (R5-Mitigation).

- `/src/workers/walkthrough/__tests__/handle-transcribe-job.test.ts` — 6 Vitest-Cases mit `vi.mock` auf supabase/admin, whisper/factory, dialogue/audio-extract und logger.
  - **MT-2 Happy Path:** Status-Flip `uploaded → transcribing → pending_review`, Adapter-Calls verifiziert, KU-Insert mit korrekten Feldern (tenant_id, capture_session_id, source='walkthrough_transcript', unit_type='observation', confidence='medium', evidence_refs.walkthrough_session_id, created_by_user_id), rpc_complete_ai_job aufgerufen.
  - **Status-Skip:** Session.status='approved' → kein Throw, kein Whisper-Call, captureWarning + rpc_complete_ai_job (idempotent kein Retry-Loop).
  - **MT-3 ffmpeg-Failure:** `audio-extract` rejects → Status='failed' + captureException + Whisper nicht aufgerufen + rpc_complete_ai_job NICHT aufgerufen (claim-loop schreibt rpc_fail_ai_job).
  - **MT-3 Whisper-Failure:** `whisper.transcribe` rejects → Status='failed' + KU-Insert NICHT aufgerufen.
  - **MT-3 Payload-Validation:** kein UUID → Throw vor jedem DB-Touch.

## Files changed

- `/src/workers/condensation/claim-loop.ts` — `JOB_TYPES` um `'walkthrough_transcribe'` erweitert (Round-Robin-Polling), Optionalen `walkthroughTranscribeHandler?: JobHandler` Parameter an `startClaimLoop` ergaenzt, Dispatch-Branch fuer `'walkthrough_transcribe'` hinzugefuegt. Bestehende Handler-Reihenfolge unveraendert.
- `/src/workers/condensation/run.ts` — Import `handleWalkthroughTranscribeJob`, Boot-Log `"walkthrough_transcribe handler registered"`, Handler als 11. Argument an `startClaimLoop` durchgereicht.

## Files intentionally not changed

- `/src/lib/ai/whisper/factory.ts`, `provider.ts`, `local.ts`, `azure.ts` — Whisper-Adapter aus V2 / DEC-018 bleibt strikt unveraendert (AC-8). Nur `language: 'de'` als Option durchgereicht.
- `/src/workers/dialogue/audio-extract.ts` — Bestehende Util `extractAudioBuffer` ist Container-agnostisch; Refactoring waere Scope-Drift. Im Handler dokumentiert, warum WebM-Input mit der existierenden Funktion korrekt arbeitet.
- `/sql/migrations/*` — Keine Migration in SLC-072 (Schema kommt aus SLC-071/Migration 082+083+084).
- `/src/app/actions/walkthrough.ts` — `confirmWalkthroughUploaded` queued bereits seit SLC-071 den `walkthrough_transcribe`-ai_job korrekt. Keine Aenderung noetig.

## Problems found

- **Pre-existing TS-Fehler in `src/lib/ai/whisper/local.ts:51`:** `'data' is of type 'unknown'.` — vorhanden auf `main` ohne meine Aenderungen (verifiziert via `git stash` + `tsc`). esbuild-Worker-Build (`npm run build:worker`) ist permissiv und produziert das Bundle trotzdem korrekt. **Empfehlung:** als IMP fuer Skill-Verbesserungen erfassen oder direkt im Whisper-Adapter fixen — nicht Teil von SLC-072-Scope.
- **Vitest-Suite hat 144 pre-existing DB-Test-Failures lokal**, weil `TEST_DATABASE_URL` nicht gesetzt ist. Per `feedback_no_local_docker.md` + `coolify-test-setup.md` gehoeren diese auf Hetzner via `node:20`-Container im Coolify-Netzwerk. Meine 6 neuen Tests sind reine Mock-Tests ohne DB und laufen lokal in 18ms gruen.
- **Live-Smoke noch offen** (s. o.): Pflicht-Gates aus Slice-Spec werden in `/qa` getestet, NICHT hier.

## Open points or deferred decisions

- **V5 Option 2 Pipeline-Trigger nicht implementiert:** Slice-Spec sagt SLC-072 selbst kann `pending_review` setzen; der Patch auf `redacting` + `walkthrough_redact_pii`-Job-Enqueue kommt in **SLC-076 MT-5**. Aktuelle Implementierung endet bei `pending_review` (korrekt fuer SLC-072 isoliert).
- **Recovery fuer haengendes `transcribing`:** Risk R3 — Crash mitten im Transcribe laesst Status haengen. SLC-074 plant Cleanup-Cron (`transcribing > 1h → failed`). Nicht Teil von SLC-072.
- **Retry-Mechanik fuer `failed`-Walkthroughs:** Out of Scope per Slice-Spec, V5.2+. V5-Workaround: User loescht Aufnahme + nimmt neu auf.
- **Multi-Template-Tenant-Strategie:** Bleibt offen aus SLC-075-Bridge — nicht SLC-072-Scope.

## Project record updates

**Slice / Backlog Stand:**
- `/slices/INDEX.md` Zeile 243 SLC-072 Status: muss von `planned` → `in_progress` (code-side done, Live-Smoke offen — flippt zu `done` nach `/qa SLC-072` PASS).
- `/planning/backlog.json` BL-078 (FEAT-035 Walkthrough Whisper-Transkription) Status bleibt `in_progress` (FEAT-035 ist erst nach SLC-074 vollstaendig done — Slice-Spec).

**STATE.md Stand:**
- Current Focus: V5 Option 2 — SLC-072 backend code-side done, awaiting Coolify-Redeploy + `/qa SLC-072` Live-Smoke. Naechster Slice danach: SLC-076 PII-Redaction.

**Reports:**
- Dieses RPT-180 (SLC-072 /backend completion).

## QA after implementation steps

`/qa SLC-072` MUSS direkt nach diesem `/backend` ausgefuehrt werden. Pflicht-Gates aus Slice-Spec:
1. **Live-Smoke E2E** mit existing uploaded Walkthroughs (33ea58be... + 75098a5d... aus SLC-075 Smoke). Wall-Time-Messung von ai_jobs.created_at bis walkthrough_session.transcript_completed_at (Soll: < 2min).
2. **Worker-Container-Logs** ohne Crash-Loop / Memory-Leak ueber 5 aufeinanderfolgende Jobs.
3. **Backwards-Compat:** mindestens 1 alter Job-Type (z.B. `condense_block` oder `voice_transcribe`) wird in derselben Session gepickt + erfolgreich verarbeitet.
4. **ffmpeg-Sanity:** `docker exec <worker> ffmpeg -version` zeigt ≥ 4.x.
5. **knowledge_unit-Verifikation:** `SELECT body FROM knowledge_unit WHERE source='walkthrough_transcript'` zeigt deutschen Transkript-Text.
6. **AC-13:** `docker ps` Worker-Container `(healthy)`.

Ohne User-Redeploy-Schritt (Coolify-Push) kann ich diese Gates nicht selbst ausfuehren.

## Recommended next step

**User-Aktion:** `git add` + Commit + Push auf `main`, dann **manueller Coolify-Redeploy** des Worker-Containers (`feedback_manual_deploy.md`).

**Danach:** `/qa SLC-072` — fuehrt Pflicht-Gates 1-6 aus.

Bei `/qa` PASS → SLC-072 → `done`, weiter mit `/backend SLC-076` (PII-Redaction, MIG-087, 5 MTs).
