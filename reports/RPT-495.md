---
id: RPT-495
date: "2026-06-19"
skill: /backend
slice: SLC-V9.8-A
feature: FEAT-089
type: completion
status: completed
reviewOf: null
result: "SLC-V9.8-A /backend CODE-COMPLETE — Mig 123 (knowledge_unit.themes + GIN + NOTIFY pgrst) + Tag-Export-Propagation im Promote-Pfad + Tests. tsc0/eslint0/hermetisch 55/55/next build mit-Env EXIT0. DB-Sidecar-Tests fuer /qa. Live-Apply Mig 123 = /deploy (R-A-1)."
title: "Backend SLC-V9.8-A — Tag-Export-Propagation (themes → knowledge_unit)"
---

# SLC-V9.8-A /backend — Tag-Export-Propagation

## Outcome summary
Code-complete. Beide Micro-Tasks umgesetzt: MT-1 (Migration 123 + DB-Test) und MT-2
(Propagation Quelle→Mapper→INSERT + Tests). Forward-only, additiv, 0 Regression der
bestehenden Promote-/Snapshot-Pipeline. Live-Apply der Migration ist bewusst NICHT Teil
der Slice (R-A-1 → /deploy). Worktree-Branch `v9-8-tag-vokabular`, noch nicht gemergt.

## Akzeptanzkriterien
- AC-A-1 (Mig 123: themes text[] NOT NULL DEFAULT '{}' + GIN, additiv, kein Backfill): erfuellt.
- AC-A-2 (Promote propagiert themes verlustfrei, Reihenfolge erhalten, null/leer → '{}'): erfuellt (Mapper + Caller + Tests).
- AC-A-3 (0 Regression title/body/block_key/confidence/metadata + rollbackLoop): erfuellt (bestehende Tests unveraendert GREEN, Propagation rein additiv).
- AC-A-4 (Queryable, Containment-Query GIN-gestuetzt, DB-Test): DB-Test geschrieben (Spalte + Index-amname=gin + @>/&& Containment); Ausfuehrung im /qa-Sidecar.
- AC-A-5 (tsc 0, eslint 0, hermetische Vitest GREEN, next build PASS): erfuellt — tsc EXIT0, eslint EXIT0 (changed files), hermetisch 55/55 (handbook-import 19 + actions 36), `next build` mit dummy-Env EXIT0 (Compiled successfully + 50/50 static pages). Ohne Env scheitert die Page-Data-Collection an `supabaseUrl is required` (bekanntes ENV-Muster, nicht code-bedingt).

## Files created
- `/sql/migrations/123_v98_knowledge_unit_themes.sql` — ADD COLUMN themes text[] NOT NULL DEFAULT '{}' + GIN-Index idx_knowledge_unit_themes + NOTIFY pgrst 'reload schema'. Idempotent, forward-only.
- `/src/lib/db/__tests__/migration-123-knowledge-unit-themes.test.ts` — DB-Test: Self-Apply der Migration in der gerollbackten withTestDb-Tx (BEGIN/COMMIT gestrippt), prueft Spalte (_text/NOT NULL/Default '{}'), GIN-Index (amname=gin), Default-'{}' bei Bestands-Row, Containment (@>/&&), Idempotenz (2. Apply ohne Fehler).

## Files changed
- `/src/lib/bulk-email/handbook-import.ts` — `SynthesizedUnitForImport` +`themes: string[] | null`; `KnowledgeUnitInsertInput` +`themes: string[]`; `mapSynthesizedUnitToKnowledgeUnit` setzt `themes: Array.isArray(args.unit.themes) ? args.unit.themes : []`; Docblock-Regel ergaenzt.
- `/src/app/dashboard/bulk-email-import/[run_id]/curation/actions.ts` — `email_synthesized_unit`-SELECT um `themes` erweitert; `units`-Cast-Typ +`themes: string[] | null`; `unitForImport` +`themes: unit.themes`. Der `.insert(insertInput)` spreadet themes automatisch (keine weitere Aenderung).
- `/src/lib/bulk-email/__tests__/handbook-import.test.ts` — `makeUnit`-Fixture +`themes`; 3 neue Mapper-Tests (Propagation 1:1, null→[], Reihenfolge erhalten).
- `/src/app/dashboard/bulk-email-import/[run_id]/curation/__tests__/actions.test.ts` — Mock-Typen + `makeUnitRow` +`themes`; Happy-Path-Assertion `knowledgeUnitInserts[0].themes`.
- `/src/lib/db/__tests__/v95-bulk-e2e.test.ts` — Mig-123-Self-Apply am Tx-Start; accepted-Unit-SELECT +`themes`; Mapper-Aufruf +`themes`; beide knowledge_unit-INSERT-Pfade (+ Fallback) +`themes`-Spalte; Assertions: kuRow.themes == ['pricing'] + Containment @> match/miss.
- `/docs/MIGRATIONS.md` — MIG-123 von GEPLANT → CODE-COMPLETE (Datei geschrieben + DB-Test, Live-Apply /deploy).
- `/docs/STATE.md` — High-Level State slice-planning → implementing; Current Focus V9.8-A code-complete + naechster Schritt /qa.
- `/slices/INDEX.md` — SLC-V9.8-A planned → in_progress.
- `/features/INDEX.md` — FEAT-089 planned → in_progress.

## Files intentionally not changed
- `/planning/backlog.json` — BL-505 bereits `in_progress` (korrekt).
- `/planning/roadmap.json` — v9-8 bereits `active` (korrekt).
- `/docs/DECISIONS.md` — DEC-228..232 bereits aus /architecture vorhanden (korrekt, keine neue Decision noetig).
- Worker/Prompt-Files (`email-synthesis*.ts`, `handle-synthesis-job.ts`) — gehoeren zu SLC-V9.8-B (FEAT-088), out of scope hier.

## Problems found
- **Auto-Fix (Deviation Rule 2): `NOTIFY pgrst, 'reload schema'` in Mig 123 ergaenzt.** Die /architecture-Skizze (MIG-123-Scope) listete NOTIFY; mein erster Migrations-Entwurf liess es weg. Da der Promote-Pfad `knowledge_unit.themes` ueber den supabase-js Admin-Client (PostgREST) INSERTet, wuerde der Live-INSERT ohne Schema-Cache-Reload `column "themes" does not exist` werfen — trotz vorhandener Spalte. NOTIFY ist damit korrektheitskritisch fuer /deploy.
- **DB-Sidecar-Tests sind in dieser Slice NICHT live ausgefuehrt.** MT-1-Migrationstest + e2e-Propagation laufen gegen die Coolify-DB (TEST_DATABASE_URL) und werden im /qa-node-Sidecar ausgefuehrt (coolify-test-setup.md). Sie sind so geschrieben, dass sie die Migration in der gerollbackten Tx self-applien (Mig 123 ist erst /deploy live, R-A-1) — damit /qa-gruen ohne Live-Apply moeglich ist. Verifikationsluecke bis /qa: reale Coolify-Ausfuehrung steht aus.
- **`next build` ist nur MIT (dummy-)Env gruen.** Ohne Supabase-Env scheitert die Page-Data-Collection an `supabaseUrl is required` (betrifft auch main, bekanntes Muster — V9.75 Final-Check dokumentierte „next build EXIT=0 mit Env"). Compile + TypeScript-Phasen sind unabhaengig davon gruen.

## Open points / deferred decisions
- Live-Apply Mig 123 + PostgREST-Reload erfolgt im /deploy V9.8 (R-A-1, BLOCKING: Migration VOR Code-Redeploy).
- SLC-V9.8-B (FEAT-088 Vokabular-Loader + Synthese-Prompt-Injektion, 0 Migration) liest die hier erzeugte Spalte — separate Session nach /qa SLC-V9.8-A.
- Out of Scope (bestaetigt): retroaktives Re-Tagging bestehender knowledge_unit-Rows; Tag-Facetten-Such-UI; email_pattern.themes-Extraktion (DEC-231).

## Recommended next step
`/qa SLC-V9.8-A` — inkl. node-Sidecar gegen Coolify-DB (MT-1-Migrationstest + e2e-Propagation), Mapper-/Action-Hermetik-Re-Run und 0-Regression-Check. Danach /backend SLC-V9.8-B.
