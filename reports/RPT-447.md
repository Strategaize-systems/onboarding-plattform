---
id: RPT-447
date: "2026-06-11"
skill: /qa
slice: SLC-V9.1-A..D
feature: FEAT-075..079
type: completion
status: completed
reviewOf: null
result: "Gesamt-V9.1-/qa PASS-WITH-LOW (Code-Side) — Cross-Slice-Wiring intakt, RLS-Matrix funktional fuer sync_state + Policy-Existenz fuer 3 Foundation-Tabellen, 0 Blocker / 0 High / 0 Medium / 4 Low. Bereit fuer /final-check; Master-Merge + DB-Integration-/IONOS-Live-Smoke gated."
title: "Gesamt-V9.1-/qa — Forward-Bucket-Email Cross-Slice-Wiring + RLS-Matrix (SLC-V9.1-A..D)"
---

# Gesamt-V9.1-/qa — Forward-Bucket-Email (SLC-V9.1-A..D)

**Scope:** Cross-Slice-Gesamt-QA ueber die 4 V9.1-Slices (FEAT-075..079) im Worktree `v9-1-forward-bucket-email` (HEAD `6a86c3e`). Fokus laut Resume-Punkt: **Cross-Slice-Wiring + RLS-Matrix-Completeness** — der Mehrwert ueber die bereits abgeschlossenen Einzel-Slice-/qa (RPT-442 SLC-C, RPT-445 SLC-D; SLC-A/B in frueheren Pre-Worktree-Sessions RPT-433/435).

## 1. Verdict

**PASS-WITH-LOW (Code-Side).** 0 Blocker / 0 High / 0 Medium / 4 Low.

Die V9.1-Pipeline (A IMAP-Sync persistiert `email_bulk_run` continuous/forward_bucket → B Pipeline-Trigger-Cron konsumiert + Cost-Cap → C Retention-Sweep raeumt run-level ab → D Setup-UI erzeugt Endpoints + Admin-Audit zeigt Forward-Source-Stats) ist Code-Side durchgaengig verdrahtet, ohne Cross-Slice-Drift. Hermetische Tests GREEN, TSC=0, V9.1-Source ESLint- und Stub-clean.

## 2. Verifikations-Ergebnisse

| Check | Ergebnis |
|---|---|
| TypeScript `tsc --noEmit` | **EXIT 0** |
| V9.1-Targeted Vitest (29 Files) | **291 passed / 4 skipped** (4 Skips = Coolify-DB-Integration, skip-guarded) |
| Full-Suite Vitest | 1797 passed / 508 failed / 36 skipped — **alle 508 = `TEST_DATABASE_URL is not set`** (Coolify-DB-Integration, environmental, KEINE V9.1-Logik-Failure; matcht Einzel-Slice-/qa) |
| ESLint `eslint .` (V9.1-Source) | **clean** — 0 Findings in allen 110 V9.1-Diff-Files |
| Stub-Scan V9.1-Source | **clean** — 0 TODO/FIXME/console.log/leere-catch/Hardcoded-Secrets |

## 3. Cross-Slice-Wiring (Kern-Scope) — VERIFIED CLEAN

Statisch verifiziert (2 unabhaengige Trace-Durchlaeufe, file:line-Belege):

- **Status-Enum-Vertrag A→B:** Writer (`imap-sync.ts` via `rpc_inbound_record_message`) schreibt `status='continuous'` + `inbound_source='forward_bucket'`; Reader (`pipeline-trigger.ts:145-146`) filtert exakt auf dieselben Strings. Transition `continuous→parsed` korrekt. Retention (C) ist bewusst source-agnostisch (run-level created_at/soft_delete_at).
- **Audit-Mechanismus uniform:** ALLE Slices schreiben in `error_log` via `captureInfo/captureException` — **kein einziger `audit_log`-Write** (OP hat keine audit_log-Tabelle). Konsistent mit DEC-208/209. Validierungs-Rejects zusaetzlich in `email_validation_reject_log`.
- **Cron-Auth uniform:** Alle 3 neuen Cron-Routes (`inbound-email-imap-sync`, `email-bulk-pipeline-trigger`, `bulk-email-retention-sweep`) nutzen identisches `x-cron-secret`-Pattern mit gleichen Status-Codes (503 unset / 403 mismatch / 200 ok / 500 throw).
- **Shared-Helper real verdrahtet (nicht gestubbt):** `checkContinuousCostCap` (pipeline-trigger:164+201), `requiresPerEmailApproval`+`estimatePatternExtractionCost` (handle-pattern-extraction-job:264-272 → setzt `awaiting_approval`), `isRunImportedToHandbook` (sweep:235), `deleteStorageObject` (sweep:255), `notifyFounderCapHit` (pipeline-trigger:167).
- **Schema-Spalten konsistent zu DEC-208/ISSUE-094:** Retention nutzt `email_bulk_run.soft_delete_at` (run-level), NICHT `email_message.deleted_at`. Forward-Source-Stats + continuous-cost-cap nutzen `vw_bulk_email_cost_monthly.month` (NICHT `month_start` — alter Prod-Bug, in MT-5/RPT-443 gefixt).

## 4. RLS-Matrix-Completeness (Kern-Scope)

4 neue V9.1-Tabellen, alle mit RLS-Enable + Policies (MIG-112 + MIG-116):

| Tabelle | RLS-Policies (Existenz) | Behavioral 4-Rollen-Pen-Test |
|---|---|---|
| `email_inbound_endpoint` | ✓ 4 (admin_all + tenant select/insert/update) — `112-test` | ⚠ nur Policy-Existenz + Schema |
| `email_forward_allowlist` | ✓ 4 (admin_all + tenant select/insert/delete) — `112-test` | ⚠ nur Policy-Existenz + Schema |
| `email_validation_reject_log` | ✓ 2 (admin_select + tenant_select, SELECT-only) — `112-test` | ⚠ nur Policy-Existenz + Schema |
| `email_inbound_sync_state` | ✓ 3 (admin_all + tenant_select + service_write) — `116-test` | ✓ **voll** — 10 Cases, SELECT/INSERT/UPDATE/DELETE pro Rolle, SAVEPOINT-Pattern (`v91-inbound.rls.test.ts`) |

Befund: Behavioral-Cross-Tenant-Pen-Test existiert nur fuer `email_inbound_sync_state`. Die 3 Foundation-Tabellen haben Policy-Existenz- + Schema-Verifikation (Policy-Count + RLS-enabled + CHECK), aber keinen Verhaltens-Pen-Test (kein `withJwtContext`-SELECT-returns-0 / INSERT-reject pro Rolle). → **Low-Finding L-1** (siehe unten). Mitigation: identisches V9-Standard-Pattern wie das behavioral getestete `sync_state` + das behavioral getestete `v9-bulk-email`; Internal-Test-Mode Founder-only.

## 5. Findings

### Low
- **L-1 (RLS-Matrix-Asymmetrie → ISSUE-096):** Behavioral 4-Rollen-Pen-Test nur fuer `email_inbound_sync_state`; `email_inbound_endpoint` / `email_forward_allowlist` / `email_validation_reject_log` nur Policy-Existenz-verifiziert. Vor Customer-Live (Modul 1+2+3 komplett, [[module-lifecycle-discipline]]) sollte die Behavioral-Matrix ergaenzt werden — diese Tabellen tragen dann Kunden-Email-Daten. Akzeptiert fuer Internal-Test-Mode (Policies existieren + korrekt typisiert, Pattern bewiesen).
- **L-2 (Pre-existing ESLint-Debt → Hand-off /final-check):** `eslint .` zeigt 1 Error (`src/app/admin/text-overrides/page.tsx:263` react/no-unescaped-entities) + 1 Warning (`src/lib/pdf/mandanten-report-v2/pages/cover.tsx:190` jsx-a11y/alt-text). **Beide NICHT in V9.1-Diff** (zuletzt SLC-144), reine V8-Era-Debt. KEIN V9.1-Finding — aber `/final-check` (full `eslint .`) wird darauf treten. Vorab-Hinweis fuer /final-check-Scope.
- **L-3 (Carry-over ISSUE-095, SLC-C):** Retention-Idempotency haengt allein an `knowledge_unit.metadata->>'bulk_run_id'`. Mitigiert durch V4-Foundation-metadata-Default `'{}'`; Belt-and-Suspenders V9.2 dokumentiert.
- **L-4 (Carry-over RPT-445-Lows, SLC-D):** DSGVO-Aktivierung nicht test-send-gated (phase-korrekt solange IONOS-Sync nicht live, OQ-R1-1); Single-Endpoint-per-Tenant-UI; keine RTL-Component-Render-Tests (OP-node-env-Konvention).

### Bewusst deferred (nicht Code-Side-blockierend)
- **DB-Integration-Vitest + RLS-Behavioral-Run gegen OP-Coolify-DB (159.69.207.29):** In Einzel-Slice-/qa (RPT-442/443/445) bereits gegen Coolify-DB gelaufen + PASS. Lokal nicht ausfuehrbar (kein `TEST_DATABASE_URL`). Re-Run gehoert natuerlich in die /deploy-Live-Smoke-Phase (ohnehin IONOS-gated). Fuer Gesamt-QA ist die statische Cross-Slice-Analyse + Targeted-Hermetic-Run die proportionale Tiefe.
- **IONOS-End-to-End-Live-Smoke (AC-R1-8) + Cron-Live-Smokes (AC-V9.1-B-9 / C-9 / D-6):** Founder-gated **OQ-R1-1** (IONOS-Postfach `bulk@strategaizetransition.com` + IMAP-ENVs + 2 Coolify-Scheduled-Tasks). → /deploy.

## 6. Readiness

V9.1 ist **Code-Side PASS** und bereit fuer `/final-check`. Cross-Slice-Wiring ohne Drift, Audit/Cron/Status-Enum/Schema-Spalten konsistent, hermetische Tests GREEN, TSC=0, V9.1-Source clean.

**Gated (NICHT in diesem Schritt):** Master-Merge `v9-1-forward-bucket-email → main` + Records-Flip auf `done` + roadmap `released` bleiben hinter /final-check → /go-live → /deploy → /post-launch (IMP-950 + [[module-lifecycle-discipline]]). Kein Customer-Outreach.

## 7. Next workflow step

**`/final-check V9.1`** (7-Dim Pre-Release-Audit). Mitzugeben: L-2 (pre-existing ESLint-Error in text-overrides/page.tsx — full-`eslint .`-Gate). Danach `/go-live` → `/deploy` (IONOS-Live-Smokes via OQ-R1-1) → `/post-launch`.
