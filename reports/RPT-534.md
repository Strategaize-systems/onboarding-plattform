---
id: RPT-534
date: "2026-06-24"
skill: /qa
slice: SLC-172
feature: FEAT-092
type: review
status: reviewed
reviewOf: RPT-533
result: "PASS — SLC-172 MT-2 (KU-Seed-RPC MIG-127 + tenant-scoped Trigger). 0 Blocker/High/Medium, 0 Low. Gates re-verifiziert (tsc 0, eslint 0, 7/7, 0 Stubs). DB-Sidecar transaktional gegen Live-Coolify-DB GREEN (7 CP / 13 KU / A–G-aligned / Antwort-reconciled / Idempotenz 7/13 / Tenant-Scope, 0 Persistenz). AC-172-5 PASS; AC-172-2 Code/Seed-verifiziert, voller Diagnose-Output → V10-Deploy."
title: "QA SLC-172 MT-2 — Blueprint-Diagnose KU-Seed-RPC + Trigger"
---

# QA — SLC-172 MT-2 (Blueprint-Diagnose KU-Seed-RPC + tenant-scoped Trigger)

**Verdict: PASS.** 0 Blocker / 0 High / 0 Medium / 0 Low. Worktree `…-v10`, Branch `v10-slc170b-blueprint-seed`, HEAD `2876868`.

## 1. Scope
SLC-172 **MT-2**: `rpc_seed_blueprint_diagnosis_input` (MIG-127) + `triggerBlueprintDiagnosis`. Bewertet: **AC-172-2** (strukturierte `block_diagnosis` via Seed→`diagnosis_generation`-Jobs) und **AC-172-5** (Tenant-Isolation, Tier-Gate, Gates, DB-Sidecar). AC-172-3 (Routing) = MT-3, nicht bewertet.

## 2. Ergebnis je AC
| AC | Verdict | Grundlage |
|---|---|---|
| AC-172-2 (Diagnose-Lauf → `block_diagnosis`-Rows) | PASS (Code-/Seed-verifiziert; voller LLM-Output → Deploy) | Seed erzeugt je A–G die KUs (`status='accepted'`, `source='questionnaire'`) die `handle-diagnosis-job` über `WHERE block_key=X` lädt (Worker 1:1 reused); Trigger enqueued 7 `diagnosis_generation`-Jobs mit korrektem Payload `{block_checkpoint_id, block_key, session_id}`. Chain Seed→Job→Worker→`rpc_create_diagnosis` type-/schema-/DB-verifiziert. Der eigentliche LLM-Diagnose-Output braucht einen Worker-Lauf (kein V10-Deploy unter Hold) → Deploy-Validation. |
| AC-172-5 (Tenant-Isolation, Trigger eigener Tenant, tsc/eslint 0, Tests GREEN, MIG-127 DB-Sidecar) | PASS | Action prüft `session.tenant_id === profile.tenant_id` (Defense-in-Depth) + `assertSessionTierAllows` (fail-closed); RPC schreibt `tenant_id` aus der Session auf alle Rows (DB-Sidecar PASS-3). tsc 0, eslint 0, 7/7 hermetisch. DB-Sidecar A–G-Count + Idempotenz + Tenant-Scope GREEN. |

## 3. Code-Gates (re-verifiziert)
- `npx tsc --noEmit` → **EXIT 0**.
- `npx eslint` (2 Files) → **0**.
- `npx vitest run blueprint-diagnosis.test.ts` → **7/7 GREEN**; kombiniert mit MT-1 22/22, 0 Regression.
- Stub-Scan über die 2 neuen Files → **0 echte Stubs** (1 False-Positive: Doku-Kommentar „F-BP-xxx" = frage_id-Format).

## 4. DB-Sidecar (AC-172-5, transaktional gegen Live-Coolify-DB, ROLLBACK)
Container `supabase-db-bwkg80w04...160450719820`, `BEGIN; \i 127_v10.sql; <Test-Session + 1 reale Antwort>; <Asserts>; ROLLBACK;`:
- **PASS-1:** 7 `blueprint_diagnosis_seed`-Checkpoints, 13 KUs (= 13 Unterthemen A–G), `KU.block_key == checkpoint.block_key`, 7 distinct A–G, reale F-BP-001-Antwort im Block-A/a1-KU-Body (Reconciliation frage_id→uuid→answer beweisbar).
- **PASS-2:** Idempotenz-Re-Run stabil 7/13 (2. Lauf neue Checkpoint-UUIDs → DELETE+CASCADE+Re-Seed, keine Duplikate).
- **PASS-3:** Tenant-Scope auf allen KU- + Checkpoint-Rows = Session-Tenant.
- **Post-Check:** Function 0 / Test-Session 0 persistiert → ROLLBACK belegt, 0 permanenter Prod-Eingriff (Founder/Coolify-Hold gewahrt).

## 5. Statische Korrektheit / Wiring
- **Checkpoint-Modell (DEC-250):** `block_diagnosis.block_checkpoint_id` NOT NULL ohne block_key-Match-Constraint (MIG-050) + `knowledge_unit.block_checkpoint_id` NOT NULL (MIG-021) → Pseudo-Checkpoint pro A–G ist korrekt + minimal; `checkpoint_type`-CHECK additiv erweitert (DROP+ADD, 6 Werte; Pattern MIG-091/110). ✓
- **Answer-Reconciliation:** RPC mappt `frage_id`→`{capture-block_key, uuid, text}` aus `template.blocks` und liest `answers['<bk>.<uuid>']` — konsistent mit dem MT-1-Writer (`questionnaire-form.tsx`, RPT-532 §4). Fehlende Antwort → „(nicht beantwortet)", fehlender frage_id → skip (Schema-Drift-tolerant). ✓
- **Worker-Reuse:** KU-Felder (`title`/`body`/`unit_type`/`confidence`/`status`) decken exakt den `buildDiagnosisUserPrompt`-Konsum; `status='accepted'` ∈ Worker-Filter `['proposed','accepted','edited']`. ✓
- **Enqueue-Idempotenz:** pending-`diagnosis_generation`-Jobs der Session werden vor Re-Enqueue gelöscht (Re-Trigger ohne Duplikate); laufende/abgeschlossene unberührt. `session_tier`-Stempel speist die MIG-121-Insert-Guard. ✓

## 6. Findings
Keine. (Verifikations-Limit, kein Defekt: voller LLM-Diagnose-Output AC-172-2 erst nach Worker-Lauf beim V10-Deploy sichtbar; MIG-127 nur transaktional getestet, permanenter Apply im /deploy.)

## 7. Files
### Files reviewed
- `/sql/migrations/127_v10_slc172_blueprint_diagnosis_seed_rpc.sql`
- `/src/lib/stb-vertikale/blueprint-diagnosis.ts`
- `/src/lib/stb-vertikale/__tests__/blueprint-diagnosis.test.ts`
- `/src/workers/diagnosis/handle-diagnosis-job.ts`, `/src/workers/diagnosis/diagnosis-prompt.ts`
### Files created
- `/reports/RPT-534.md` — dieser QA-Report.
### Files changed
- `/docs/STATE.md` — Current Focus + Phase auf MT-2 /qa PASS → NEXT MT-3.

## 8. Next workflow step
**`/backend SLC-172 MT-3`** — `SubtopicDiagnosisCard` (Ampel/Reifegrad/Empfehlung) + deterministisches Modul-Routing aus `metadata.routing[]` + Reader-Erweiterung der MT-1-Overview (`[sessionId]/page.tsx` erweitern, nicht ersetzen). SLC-172 bleibt `in_progress` bis MT-3 + Gesamt-QA. Voller Diagnose-Output-Smoke (AC-172-2) + MIG-127-Live-Apply → V10-Deploy-Validation.
