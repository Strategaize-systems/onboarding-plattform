---
id: RPT-012
date: "2026-04-16"
skill: /qa
slice: SLC-002b
feature: FEAT-001
type: review
status: reviewed
reviewOf: RPT-011
result: "PASS mit Low-Level-Findings. Alle Acceptance-Kriterien erfuellt, RLS-Tests 3/3 gruen auch nach Migration 027, Seed idempotent, Fail-Fast bei fehlender ENV verifiziert. ISSUE-009 bewusst an SLC-002d delegiert."
title: "QA SLC-002b Seed Admin + Demo-Tenant — Pass"
---

## QA scope reviewed

| Artefakt | Zustand |
|---|---|
| `sql/migrations/027_seed_demo_tenant.sql` | reviewed + zweiter Run verifiziert (INSERT 0 0) |
| `scripts/seed-admin.mjs` | reviewed + dritter Run verifiziert (skip create) |
| `Dockerfile` | reviewed (Stage-3 `COPY scripts/`) |
| `package.json` + `package-lock.json` | reviewed (Lockfile-Regeneration ok) |
| `.env.deploy.example` + `.env.local.example` | reviewed |
| `docs/RUNBOOK.md` | reviewed (Seed-Befehl, Rotation, Disaster-Recovery) |
| `docs/DECISIONS.md` (DEC-011) | reviewed |
| `slices/SLC-002b-admin-seed.md` | reviewed |
| Hetzner-DB (public.tenants, public.profiles, auth.users) | verifiziert |
| `/login`-Flow mit beiden Seed-Credentials | durch User verifiziert |
| `src/lib/db/__tests__/rls-isolation.test.ts` | 3/3 gruen gegen Live-Hetzner-DB (2026-04-16 08:59) |

## Result

**PASS** — mit drei Low-Level-Findings und zwei bekannten offenen Issues, die bewusst ausserhalb SLC-002b-Scope liegen.

## Findings

### Blocker
keine.

### High
keine.

### Medium
keine.

### Low
- **Finding 1 — listUsers-Pagination hart gesetzt.** `scripts/seed-admin.mjs:182` ruft `/auth/v1/admin/users?page=1&per_page=100`. Fuer V1 (< 10 User) ok und im Script-Kommentar erwaehnt, aber keine Assertion, dass die Liste nicht groesser als 100 wird. Low weil V1-Scope, Folge-Improvement wenn User-Bestand waechst.
- **Finding 2 — Profile-Reconcile ist unconditional.** `ensureUser` ruft jedes Mal PATCH /rest/v1/profiles?id=eq.<uuid>, auch wenn role + tenant_id bereits stimmen. Keine funktionale Gefahr, minimaler Extra-Overhead pro Lauf. Bewusst so, weil es Trigger-Verhalten-Aenderungen abfedert (im Kommentar begruendet). Low weil klein und dokumentiert.
- **Finding 3 — Dockerfile COPY scripts/ ist breit.** Kopiert alle Dateien aus `scripts/` inkl. `catalog-v1.0.json` (Blueprint-Erbe, 40+KB) + `generate-secrets.mjs` + `provision-customer.sh`. Unerheblich fuer Image-Groesse, aber Blueprint-Erbe landet stumm im runtime-Image. Low, gehoert in SLC-002d-Scope (Legacy-Cleanup).

### Offene Issues (ausserhalb SLC-002b-Scope, dokumentiert)
- **ISSUE-009** Blueprint-Profile-Flow Silent Failure — bewusst in SLC-002d.
- **ISSUE-008** Legacy /api/tenant/runs/[runId]/feedback-Route — bewusst in SLC-002d.
- **ISSUE-003** node_modules lokal nicht installiert — Dev-Convenience, kein Deploy-Blocker.
- **ISSUE-005** App-Branding — SLC-002c.
- **ISSUE-007** JWT-Randbedingung — kein Handlungsbedarf in V1.

## Acceptance-Criteria-Check

| # | Kriterium (aus SLC-002b) | Erfuellt |
|---|---|---|
| 1 | Login mit SEED_ADMIN_EMAIL + SEED_ADMIN_PASSWORD funktioniert | ja (User-Smoketest) |
| 2 | Login mit SEED_DEMO_TENANT_ADMIN_EMAIL + SEED_DEMO_TENANT_ADMIN_PASSWORD funktioniert | ja (User-Smoketest) |
| 3 | `SELECT * FROM profiles` zeigt genau 2 Zeilen mit korrekten Rollen und tenant_ids | ja (2 Zeilen, admin=strategaize_admin+NULL, demo=tenant_admin+Demo Onboarding GmbH, beide email_confirmed=t) |
| 4 | Re-Deploy (zweiter Lauf) wirft keinen Fehler, aendert keine Rows | ja (Migration 027: INSERT 0 0. Script: `skip create` mehrfach verifiziert) |
| 5 | Migration 027 in `/docs/MIGRATIONS.md` dokumentiert | ja (MIG-006) |

## Wiring verification

| Kette | Verifikation |
|---|---|
| SQL 027 -> `public.tenants` Row mit fester UUID | `SELECT ... WHERE id = '00000000-0000-0000-0000-0000000000de'` liefert Demo Onboarding GmbH |
| Script ENV -> createUser-Payload | Fail-Fast bei leerem SEED_ADMIN_EMAIL verifiziert (ERROR, exit) |
| GoTrue POST /auth/v1/admin/users -> `auth.users` + Trigger `handle_new_user` -> `public.profiles` | DB-Query zeigt fuer beide User korrekte role + tenant_id |
| Profile-Reconcile UPDATE -> `public.profiles` | UPDATE setzt Werte deterministisch (unconditional) |
| Login /api/auth/signin -> Session-Cookie -> /dashboard | User hat manuell verifiziert beide Wege |

## Stub detection

- TODO / FIXME / HACK: **keine** in SLC-002b-Files.
- Placeholder returns: **keine**.
- Empty handlers: **keine**.
- console.log in seed-admin.mjs: **akzeptiert** — CLI-Tool, Progress-Output, nicht Debug-Leftover.
- Hardcoded values: Demo-Tenant-UUID `00000000-0000-0000-0000-0000000000de` bewusst hardcoded (Konvention, in Script UND Migration dokumentiert). DEMO_TENANT_ID als Konstante sichtbar. Nicht Secret-Material — nur Identifier.
- Dummy data: keine.
- Commented-out code: keine.

## Record format compliance

| Datei | Format korrekt |
|---|---|
| `/docs/DECISIONS.md` DEC-011 | ja (`## DEC-011 — Titel`, Status/Reason/Consequence) |
| `/docs/MIGRATIONS.md` MIG-006 | ja (`### MIG-006 — Titel`, Date/Scope/Reason/AffectedAreas/Risk/RollbackNotes) |
| `/docs/KNOWN_ISSUES.md` ISSUE-009 | ja (`### ISSUE-009 — Titel`, Status/Severity/Area/Summary/Impact/Workaround/NextAction) |
| `/docs/SKILL_IMPROVEMENTS.md` (Dev-System) IMP-051, IMP-052 | ja (Date/Source/Observation/SuggestedImprovement/AffectedArea/Status) |
| `/slices/INDEX.md` | ja (Status in Spalte 4) |
| `/planning/backlog.json` BL-018, BL-021 | ja (Schema entspricht project-records-format) |
| `/reports/RPT-011.md` | ja (YAML-Frontmatter) |
| `/docs/RUNBOOK.md` | frei-strukturiert ok — keine Cockpit-Parse-Anforderung an RUNBOOK |

## Readiness assessment

SLC-002b ist **ready**. Alle Acceptance-Kriterien erfuellt, Implementation stabil gegen Re-Runs, keine Regression in den RLS-Tests, Smoketest durch User bestaetigt. Die drei Low-Findings sind bewusst akzeptiert (Pagination-Grenze, unconditional Reconcile) oder gehoeren zu SLC-002d (Dockerfile-Breite). Das beim Smoketest entdeckte ISSUE-009 ist **kein Blocker fuer SLC-002b** (Seed + Login funktionieren), sondern ein separates Blueprint-Legacy-Problem, das in SLC-002d geloest wird.

## Recommended next step

**SLC-002d starten** (Blueprint-Legacy-UI-Cleanup) — adressiert ISSUE-008 + ISSUE-009 und bereinigt den Code-Pfad vor SLC-003 (Template/Questionnaire-UI). SLC-002c (App-Branding) parallel oder danach.
