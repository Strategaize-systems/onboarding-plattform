---
id: RPT-288
date: "2026-05-17"
skill: /slice-planning
slice: SLC-130
feature: FEAT-045
type: completion
status: completed
reviewOf: null
result: "SLC-130 angelegt mit 5 MTs (Migration 096 + Code-Path-Umstellung + Vitest + Doku + Records). BL-105 in_progress, V6.4 active."
title: "V6.4 BL-105 Slice-Planning — SLC-130 Template-Versionierung UNIQUE(slug, version)"
---

# RPT-288 — V6.4 BL-105 Slice-Planning DONE

## Outcome Summary

**FULLY COMPLETE.** SLC-130 angelegt mit 5 strikt sequenziellen MTs gemaess BL-105 + `session_handoff_2026_05_17_v63_released.md` MT-Skizze. V6.4-Roadmap-Slot von `planned` auf `active` umgestellt. BL-105 von `open` auf `in_progress`. STATE.md Current Focus auf "V6.4 BL-105 Slice-Planning DONE". slices/INDEX.md V6.4-Sektion mit SLC-130 + Execution Order + Pflicht-Gates + Out-of-Scope + Pre-Conditions + Aufwand-Schaetzung ergaenzt.

**Skill-Sequenz fortlaufend:** Naechster Schritt `/backend SLC-130 MT-1..MT-5` ohne Zwischen-/qa pro MT (Slice-Schluss-/qa nach MT-5).

## Slice-Definition (SLC-130)

**Goal:** Architektur-Polish-Slice fuer das Diagnose-Werkzeug-Template-System. Schema-Constraint `UNIQUE(slug)` auf `public.template` -> `UNIQUE(slug, version)`. Damit alte Diagnose-Berichte ihre originalen Block-Titel und Block-Intros behalten, wenn `partner_diagnostic`-Template spaeter aktualisiert wird.

**Reuse-Quote 100%** — 0 neue Tabellen, 0 neue Worker, 0 neue Bedrock-Calls, 0 neue ENVs, 0 neue Container, 0 neue npm-Packages. Nur 1 Schema-Migration + 2 Code-Files + 1 Test-File + 1 Doku-Update.

**bericht/page.tsx KEIN Code-Touch verifiziert** — laedt Template via `session.template_id` direkt (Grep auf Z. 87 `.eq("id", session.template_id)` bestaetigt). Jede Session referenziert ihre eigene Template-Row.

## Micro-Tasks (5)

### MT-1: Migration 096 anlegen + Live-Apply auf Hetzner (~45min)

- File `sql/migrations/096_v64_template_slug_version_unique.sql` (NEU).
- Body: `ALTER TABLE public.template DROP CONSTRAINT IF EXISTS template_slug_key;` + `CREATE UNIQUE INDEX IF NOT EXISTS template_slug_version_unique ON public.template(slug, version);`.
- Idempotent via DROP IF EXISTS + CREATE IF NOT EXISTS.
- Live-Apply per `sql-migration-hetzner.md` Pattern: base64 -> docker exec psql -U postgres + Pre-Apply-Backup nach `/opt/onboarding-plattform-backups/`.
- Verifikation: `\d public.template` zeigt KEINEN `template_slug_key`-Constraint mehr aber `template_slug_version_unique`-Index.

### MT-2: Template-Lookup-Code-Path-Umstellung (~30min)

- `src/app/dashboard/diagnose/actions.ts` Z. 27-28: `PARTNER_DIAGNOSTIC_VERSION` als deprecated Comment.
- `src/app/dashboard/diagnose/actions.ts` Z. 117-126: Lookup `WHERE slug=... ORDER BY created_at DESC LIMIT 1` + `.maybeSingle()`. Error-Message "Template ... nicht gefunden (keine Version live)".
- `src/app/dashboard/diagnose/start/page.tsx` Z. 79-85: analog `.order("created_at", { ascending: false }).limit(1).maybeSingle()`.
- `src/app/dashboard/diagnose/[capture_session_id]/bericht/page.tsx` UNVERAENDERT (Verifikation per `grep`).

### MT-3: Vitest Cross-Version-Read (~60min)

- File `src/app/dashboard/diagnose/__tests__/template-versioning.test.ts` (NEU).
- Test 1: Insert 2 Templates (slug='test_versioning_x', version='v1') + ('test_versioning_x', 'v2'). Lookup `ORDER BY created_at DESC LIMIT 1` gibt v2. Direkt-Lookup auf v1-`id` gibt v1. Cleanup.
- Test 2: Doppel-Insert `(slug, version)` wirft `unique_violation`. SAVEPOINT-Pattern fuer erwarteten Fehler (`coolify-test-setup.md` Punkt 2 — sonst current transaction is aborted).
- Test 3: Insert 2 Templates mit gleichem Slug + verschiedenen Versions klappt nun (alter Constraint waere weg).
- Run gegen Coolify-DB im node:20-Container per TEST_DATABASE_URL.

### MT-4: docs/DIAGNOSE_TEMPLATE_EDITING.md Pattern-Update (~30min)

- Sektion "V6.4-Polish-Slice fuer echte Versionierung" -> umbenannt "Versionierung — V6.4 LIVE seit SLC-130".
- Sektion "Standard-Workflow: Template-Update via Migration" -> Migration-Pattern auf `INSERT ... ON CONFLICT (slug, version) DO UPDATE`.
- Sektion "Wann das problematisch wird" -> umformuliert auf "Vor SLC-130 war problematisch — seit SLC-130 V6.4 LIVE geloest".
- Header-Block "Stand: 2026-05-17 (V6.4-Release)".

### MT-5: Quality-Gates + Cockpit-Records (~30min)

- ESLint 0/0, tsc EXIT=0, `npm run build` PASS lokal, Vitest 0 Regression.
- `slices/INDEX.md`: SLC-130 Status `done`.
- `planning/backlog.json`: BL-105 `done`.
- `planning/roadmap.json`: V6.4 bleibt `active` bis /deploy.
- `docs/STATE.md`: Current Focus auf SLC-130 done + /qa naechster Schritt.
- `docs/MIGRATIONS.md`: MIG-040 Eintrag.

## Acceptance Criteria (12)

12 ACs definiert (AC-1..AC-12) — siehe `slices/SLC-130-template-versionierung-unique-slug-version.md`. Highlights:
- AC-1..AC-4: Migration 096 Korrektheit + Idempotenz + Live-Apply + Verifikation.
- AC-5..AC-7: Code-Path-Umstellung in 2 Files + Verifikation dass bericht/page.tsx UNVERAENDERT.
- AC-8: docs/DIAGNOSE_TEMPLATE_EDITING.md updated.
- AC-9: Vitest 3 Tests PASS gegen Coolify-DB.
- AC-10: Quality-Gates PASS.
- AC-11..AC-12: Live-Smoke nach Coolify-Redeploy fuer existierende V6.3-Session-Berichte + neue Session.

## Files Reviewed

- `/docs/STATE.md` — Current Focus pre-Update
- `/planning/backlog.json` BL-105 Full-Description
- `/planning/roadmap.json` V6.4 Roadmap-Slot
- `/slices/INDEX.md` — Format-Pattern V6.x-Sektionen
- `/slices/SLC-122-compliance-md-onboarding.md` — Slice-File-Pattern-Vorlage
- `/sql/migrations/093_v63_partner_diagnostic_seed.sql` — Migration-Pattern-Vorlage
- `/src/app/dashboard/diagnose/start/page.tsx` Z. 70-109 — Lookup-Stelle
- `/src/app/dashboard/diagnose/actions.ts` Z. 27-28 + Z. 100-140 — Konstanten + Lookup
- `/src/app/dashboard/diagnose/[capture_session_id]/bericht/page.tsx` Z. 51-89 — session.template_id-Pfad verifiziert (KEIN Code-Touch noetig)
- Memory `project_op_v63_released.md` + `session_handoff_2026_05_17_v63_released.md`

## Files Changed

- `/planning/backlog.json` — BL-105 status `open` -> `in_progress`
- `/planning/roadmap.json` — V6.4 status `planned` -> `active` + Summary updated mit RPT-288-Verweis
- `/slices/INDEX.md` — V6.4-Section (~50 Zeilen) angefuegt: SLC-130-Tabelle, Execution Order MT-1..MT-5, Pflicht-Gates SC-V6.4-1..5, Out-of-Scope, Pre-Conditions, Aufwand-Schaetzung
- `/docs/STATE.md` — Current Focus auf V6.4 BL-105 Slice-Planning DONE, High-Level State von `post-launch` auf `slice-planning`, V6.3-DEPLOY-Outcome als historischer Eintrag verschoben

## Files Created

- `/slices/SLC-130-template-versionierung-unique-slug-version.md` (~310 Zeilen) — vollstaendige Slice-Spezifikation: Goal, Feature, Background, In Scope (Schema + Code + Doku + Vitest + Quality-Gates), Out of Scope, 12 ACs, Pre-Conditions, Stop-Gates, 5 Micro-Tasks mit Goal/Files/Expected behavior/Verification/Dependencies, Execution Order, Effort-Schaetzung, Risks, Worktree-Isolation, Cross-Slice-Konsistenz, References
- `/reports/RPT-288.md` — dieser Report

## Files Intentionally Not Changed

- `/src/app/dashboard/diagnose/[capture_session_id]/bericht/page.tsx` — Verification via `grep template_id` zeigt Z. 87 `.eq("id", session.template_id)` — laedt schon korrekt via Session-FK, KEIN Code-Touch in SLC-130 noetig.
- `/sql/migrations/093_v63_partner_diagnostic_seed.sql` — bleibt wie ist (idempotent angelegt mit ON CONFLICT(slug), Live-Apply hat seine Wirkung schon gehabt). Neue Migrationen ab 096+ nutzen `ON CONFLICT(slug, version)`.
- `/sql/migrations/094_v63_finalize_partner_diagnostic_rpc.sql` — RPC nutzt `template.id` via Session-Lookup, kein Slug-Lookup. Bleibt funktional.
- `/src/workers/condensation/light-pipeline.ts` — laedt Template via `session.template_id`, kein Slug-Lookup. Bleibt funktional.
- `/features/INDEX.md` — FEAT-045 bleibt auf `deployed` (V6.3-Status). V6.4 ist Polish-Erweiterung der existierenden Feature, kein neuer Feature-Eintrag.

## Problems Found

**Keine Blocker, keine High, keine Medium.**

- **L-1 (Low, Doku-Drift in DIAGNOSE_TEMPLATE_EDITING.md):** Die existierende Sektion "V6.4-Polish-Slice fuer echte Versionierung" (Z. 132+) wurde 2026-05-17 als Empfehlung geschrieben. In MT-4 wird sie auf "LIVE seit SLC-130" umgestellt — Doku-Konsistenz dann erst nach MT-4.
- **L-2 (Low, Test-DB-Setup-Pre-Condition):** TEST_DATABASE_URL fuer Coolify-DB ist in `coolify-test-setup.md` dokumentiert. Bei MT-3-Run muss zur Laufzeit der Container-Name resolved werden (`docker ps --format '{{.Names}}' | grep ^supabase-db`) — Pattern aus IMP-497 bekannt.
- **L-3 (Low, V6.4-Doku-Inkonsistenz im V6.4-Roadmap-Summary):** Roadmap-Summary nennt Migration "ALTER TABLE template DROP CONSTRAINT", die Slice-File-Spec nennt es "DROP CONSTRAINT IF EXISTS" fuer Idempotenz. Beide korrekt — Slice-File ist die kanonische Quelle.

## Open Points or Deferred Decisions

- **Migration 094 (rpc_finalize_partner_diagnostic):** explicit als "NOT in Scope" markiert weil session-template_id-Lookup, kein Slug-Lookup. Falls spaeter doch geaendert werden muesste: separater Slice.
- **v2-Template-Seed:** keine Daten-Aenderung in SLC-130. Falls Inhalts-Workshop V2 entscheidet, dass Fragen/Score-Mappings angepasst werden muessen: neuer Slice in V6.5 oder V7.

## Project Record Updates

Alle Pflicht-Updates durchgefuehrt im Rahmen dieser Skill-Run:

1. **/slices/INDEX.md** — V6.4-Sektion mit SLC-130-Eintrag (Status `in_progress`, in Column 4 wie per `project-records-format.md` Pflicht-Format) + Execution Order + Pflicht-Gates + Out-of-Scope + Pre-Conditions + Aufwand-Tabelle hinzugefuegt.
2. **/planning/backlog.json** — BL-105 Status `open` -> `in_progress`.
3. **/planning/roadmap.json** — V6.4 Status `planned` -> `active`, Summary updated mit RPT-288-Verweis + "DONE 2026-05-17"-Marker.
4. **/docs/STATE.md** — Current Focus + High-Level State + Immediate Next Steps auf V6.4 BL-105 Slice-Planning DONE, V6.3-DEPLOY-Stand als historischer Eintrag mit (Vorheriger Outcome ...)-Praefix.

KEINE Aenderung an `/features/INDEX.md` (FEAT-045 bleibt `deployed` — V6.4 ist Erweiterung des V6.3-Release, nicht neuer Feature-Eintrag) und KEINE Aenderung an `/docs/MIGRATIONS.md` (MIG-040-Eintrag kommt in MT-5 wenn Migration 096 tatsaechlich angelegt).

## QA After Implementation Steps

**N/A fuer /slice-planning** — per `mandatory-completion-report.md` Sektion 9 ist QA nach `/discovery`, `/requirements`, `/architecture` NICHT noetig. `/slice-planning` faellt in die Pflicht-Kategorie, aber liefert keine Code-Aenderung — nur Slice-Spezifikation + Records. QA-Run kommt nach `/backend SLC-130` MT-1..MT-5.

## Recommended Next Step

**`/backend SLC-130 MT-1..MT-5`** ohne Zwischen-Skill-Calls, gemaess User-Cold-Start-Direktive "ohne Zwischenfragen durcharbeiten bis /deploy REL-019".

Reihenfolge: MT-1 (Migration 096 + Live-Apply auf Hetzner) -> MT-2 (Code-Path-Umstellung) -> MT-3 (Vitest 3 Tests gegen Coolify-DB) -> MT-4 (Doku-Update) -> MT-5 (Quality-Gates + Records).

Nach MT-5: `/qa SLC-130` als Slice-Schluss-QA + Browser-Smoke per Playwright MCP (existierende V6.3-Test-Mandanten-Session + neue Diagnose-Run).

Bei /qa PASS: `/final-check V6.4` -> `/go-live V6.4` -> `/deploy V6.4 als REL-019` (Git-Tag v6.4.0 + Coolify-Redeploy app + worker auf neuen HEAD).

**Aufwand-Schaetzung Restweg:** /backend ~3h + /qa ~30-60min + /final-check ~15min + /go-live ~10min + /deploy ~30min (User-Pflicht Coolify-Redeploy) = ~4.5-5h fuer Slice + Release in einer fokussierten Session.
