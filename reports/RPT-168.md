---
id: RPT-168
date: "2026-05-06"
skill: /frontend
slice: SLC-071
feature: FEAT-034
type: completion
status: in-progress
result: "MT-6 + MT-7 + MT-8 code-complete — WalkthroughCapture (7-state machine + Codec-Selection + 30min Auto-Stop), Status-Polling-Page + GET /api/walkthroughs/[id]/status, Capture-Page-Loader. 9/9 walkthrough Vitest-Cases GREEN (3 neue + 6 aus MT-4/5). TypeScript 0 Errors, Lint 0 Errors. AC-10..AC-12 sind code-vorhanden, brauchen Browser-Smoke (MT-9-Gate)."
title: "SLC-071 MT-6 + MT-7 + MT-8 — Walkthrough Capture-UI + Status-Polling + Capture-Page-Loader"
---

# RPT-168 — SLC-071 MT-6 + MT-7 + MT-8: Walkthrough Capture-UI

## Outcome summary

Drei Frontend-MTs des V5-Capture-Pfads in einer Session zusammengezogen, weil sie tightly coupled sind (MT-8 lädt MT-6, MT-6 redirected zu MT-7).

**Code-vollständig:**

- **MT-6 (Capture-Component):** `WalkthroughCapture.tsx` als `"use client"`-Komponente mit der vollständigen 7-State-Maschine (`idle | requesting | recording | stopping | uploading | uploaded | failed`), Codec-Selection vp9+opus mit Fallback vp8+opus, 30-Minuten-Hard-Stop via `setTimeout`, MediaRecorder über kombinierten Display+UserMedia-Stream, Direct-Upload via XMLHttpRequest mit `xhr.upload.onprogress`-Progressbar, Redirect zur Status-Page nach Confirm. Pure Logik (Reducer, Codec-Auswahl, Auto-Stop-Scheduler) extrahiert nach `walkthrough-capture-logic.ts`, damit Tests in vitest's node-Env ohne jsdom laufen.
- **MT-7 (Status-Polling):** `WalkthroughStatusPolling.tsx` Client-Component pollt `/api/walkthroughs/[id]/status` alle 5s, stoppt bei Terminal-States (`pending_review | approved | rejected | failed`). Server-Component `app/employee/walkthroughs/[id]/page.tsx` rendert SSR-initial + Component. GET-Route gibt `walkthrough_session`-Status zurück, RLS-getrieben über User-Client.
- **MT-8 (Capture-Page-Loader):** Server-Component `app/employee/capture/walkthrough/[id]/page.tsx` macht Auth + Role-Guard (`employee | tenant_member | tenant_admin`) + Tenant-Ownership-Check der `capture_session` und reicht den Sessionsauf `<WalkthroughCapture />` durch.

**Tests grün:**

- 3/3 neue Vitest-Cases in `WalkthroughCapture.test.tsx` (State-Machine-Transitions, Codec-Pick mit Fallback + No-Codec-Path, Auto-Stop-Scheduler mit `vi.useFakeTimers`).
- 6/6 Server-Action-Cases aus RPT-167 unverändert grün.
- Insgesamt 9/9 Walkthrough-Tests PASS.

**Quality Gates:**

- `npx tsc --noEmit` → 0 Errors.
- `npx eslint .` → 0 Errors / 0 Warnings.
- `npx next build` Compile-Phase + TypeScript PASS; Build-Sammelphase scheitert an pre-existing `/api/unsubscribe/[token]` mit `supabaseUrl is required` — Ursache: leere ENV im Worktree, keine Regression aus dieser Session.

**Status SLC-071: in_progress (8/9 MTs done).** MT-9 (Final-Gate inkl. AC-14 4-Rollen-Partial-RLS-Test gegen Live-DB + manueller User-Smoke-Test im Browser) verbleibt für die Folge-Session.

## Was nicht in dieser Session gemacht wurde

- AC-10 (Browser-Smoke Permission-Prompts → Recording → Stop → Upload-Progress → Status-Page-Redirect in Chrome+Edge): nicht durchgeführt. Erfordert Live-Bedrock-Whisper-Pipeline-Pickup für End-to-End-Status-Verlauf — Sinn macht das erst, wenn SLC-072 Worker existiert. Kann jetzt schon als Halb-Smoke (uploaded-State) gemacht werden, ist im MT-9-Gate vorgesehen.
- AC-11 (Auto-Stopp im Browser mit verkürztem Timer): Vitest-Test deckt die Logik ab; Browser-Verifikation ist MT-9.
- AC-12 (`uploaded → transcribing` Statusverlauf): wartet auf SLC-072.
- AC-14 (4-Rollen-Partial-RLS-Test gegen Live-DB via SAVEPOINT-Pattern): MT-9-Gate.
- AC-15 (`npm run build` 0/0): blockiert durch pre-existing Build-Issue, getrennt zu klären.
- Cockpit-Records-Update für Slice-Ende: erst nach MT-9.

## AC-Status

| AC | Beschreibung | Status |
|----|---|---|
| AC-1..AC-5 | MIG-031 (082+083+084) live + Backup | PASS (RPT-166) |
| AC-6..AC-9 | Server Actions request+confirm + Validierung | PASS (RPT-167) |
| AC-10 | Capture-UI Browser-Smoke Chrome+Edge end-to-end | deferred (MT-9) |
| AC-11 | 30min-Auto-Stopp greift im Browser | logic-PASS (Vitest), Browser-Verifikation MT-9 |
| AC-12 | Status-Polling-Page Statusverlauf | code-PASS, Vollverifikation nach SLC-072 |
| AC-13 | Vitest-Suite walkthrough.test.ts 6 Cases | PASS (RPT-167, weiter grün) |
| AC-14 | Partial RLS-Test (4 SELECT-Fälle) gegen Live-DB | deferred (MT-9) |
| AC-15 | npm run build + lint + test | lint+test PASS; build pre-existing-blocked |

## Files reviewed

- `/slices/SLC-071-walkthrough-foundation-capture-upload.md` — vollständig, insbesondere Section C/D/E + MT-6..MT-9 + AC.
- `/src/app/actions/walkthrough.ts` — Server-Action-Signaturen für die Component-Konsumation.
- `/src/app/actions/__tests__/walkthrough.test.ts` — Mock-Pattern für Konsistenz mit den neuen Component-Tests.
- `/src/app/employee/capture/[sessionId]/page.tsx` und `/src/app/employee/capture/[sessionId]/block/[blockKey]/page.tsx` — Auth-Pattern (inline `supabase.auth.getUser` + `profiles`-Lookup + `redirect`/`notFound`), Layout-Konventionen.
- `/src/app/api/capture/[sessionId]/evidence/list/route.ts` — Route-Handler-Pattern.
- `/src/components/capture-modes/walkthrough-stub/WalkthroughStubMode.tsx` und `/src/components/capture-modes/employee-questionnaire/EmployeeQuestionnaireMode.tsx` — Component-Konventionen.
- `/vitest.config.ts` und `/src/test/setup.ts` — Vitest-Env (`node`, kein jsdom — kritisch für die Test-Strategie).

## Files created

- `/src/components/capture-modes/walkthrough/walkthrough-capture-logic.ts` (108 LOC) — Pure-Logic-Modul für State-Machine, Codec-Selection, Auto-Stop-Scheduler, Restzeit-Format. Damit Tests in vitest's node-Env laufen, ohne jsdom oder @testing-library/react als neue Dependencies zu fordern.
- `/src/components/capture-modes/walkthrough/WalkthroughCapture.tsx` (~290 LOC) — `"use client"`-Component mit `useReducer` über die pure Reducer-Funktion, getDisplayMedia + getUserMedia + MediaRecorder + XHR-Upload-Progress + Redirect.
- `/src/components/capture-modes/walkthrough/__tests__/WalkthroughCapture.test.tsx` (3 Cases, 81 LOC) — State-Machine-Transitions, Codec-Pick (No-Codec + Fallback), Auto-Stop-Scheduler mit `vi.useFakeTimers`. Alle 3 GREEN.
- `/src/components/capture-modes/walkthrough/WalkthroughStatusPolling.tsx` (~130 LOC) — `"use client"`-Polling-Component, 5s-Tick, Stop bei Terminal-States.
- `/src/app/employee/walkthroughs/[id]/page.tsx` — Server-Component, lädt initial + rendert Polling-Component.
- `/src/app/employee/capture/walkthrough/[id]/page.tsx` — Server-Component, Auth + Role-Guard + Tenant-Check, rendert Capture-Component.
- `/src/app/api/walkthroughs/[id]/status/route.ts` — GET-Handler, RLS-bound über User-Client.
- `/reports/RPT-168.md` — dieser Bericht.

## Files changed

Keine. Alle MT-6..MT-8 sind Neuanlagen, keine Edits an Bestandsfiles.

## Files intentionally not changed

- `/src/components/capture-modes/registry.ts` — die Registry verwaltet Pseudo-Modes der Capture-Spike-Routen. Walkthrough läuft nicht über die Registry, sondern über eine eigene Route (`/employee/capture/walkthrough/[id]`). Registry-Cleanup (walkthrough_stub-Removal) ist explizit SLC-074-Scope.
- `/docs/STATE.md`, `/slices/INDEX.md`, `/planning/backlog.json` — kein Update, weil SLC-071 noch nicht done ist (MT-9 offen). Cockpit-Sync erfolgt nach MT-9-Abschluss.
- `/docs/MIGRATIONS.md` — keine neue Migration in dieser Session.

## Problems found

- **Pre-existing Build-Failure** in `/src/app/api/unsubscribe/[token]/route.ts`: `Error: supabaseUrl is required` während `next build` Page-Data-Collect. Ursache: keine `.env.local` im Worktree (SUPABASE-ENV fehlt). Tritt nur am Build-Schritt auf (Compile + TypeScript laufen sauber durch), keine Regression aus dieser Session. Für Live-Build via Coolify ist das unkritisch (ENV ist dort gesetzt). Wird beim MT-9-Gate auf dem Hauptbranch verifiziert.
- **Vitest-DB-Tests werden ohne TEST_DATABASE_URL übersprungen** (140 pre-existing Failures): keine Regression — meine 9 Tests sind grün, alle Pre-Existing-Failures werfen den TEST_DATABASE_URL-Error. Kein Bug.
- **Routing-Edge-Case:** `/employee/capture/walkthrough` ohne ID würde `[sessionId]/page.tsx` mit `sessionId='walkthrough'` treffen statt 404. Praktisch egal, weil keine UI dorthin verlinkt — alle Aufrufe haben `[id]`. Sollte trotzdem mit einem 404-Pattern abgesichert werden, wenn die Capture-Mode-Registry SLC-074 die Walkthrough-Route freischaltet. **Folge-Backlog:** BL-?? in MT-9-Cleanup.

## Open points or deferred decisions

- MT-9 (Final-Gate) als eigene Session — beinhaltet Browser-Smoke (Chrome+Edge), AC-14 4-Rollen-Partial-RLS-Test gegen Live-Coolify-DB via SAVEPOINT-Pattern, Bundle-Build auf Hauptbranch (nicht Worktree), Cockpit-Records-Sync (`slices/INDEX.md` SLC-071 → done, `MIGRATIONS.md` MIG-031 vollständig live, `planning/backlog.json` BL-077 bleibt in_progress weil FEAT-034 erst mit SLC-074 fertig ist).
- Nach MT-9: SLC-072 (Worker), erst dann ist die End-to-End-Status-Verlaufs-Verifikation möglich (`uploaded → transcribing → pending_review`).

## Recommended next step

`/qa` oder direkt MT-9-Gate als kombinierter Final-Block in der nächsten Session:

1. Worktree push + `git pull` auf dem Hauptbranch im Hauptrepo, dort `npm run build` mit echter ENV (Build sollte sauber durchlaufen, kein pre-existing-Issue).
2. `/qa SLC-071` als Skill-Run — beinhaltet die 4-Rollen-Partial-RLS-Test-Erstellung (`src/lib/db/__tests__/v5-walkthrough-rls.test.ts`) gegen Container `supabase-db-bwkg80w04wgccos48gcws8cs-083715570318`, Netzwerk `bwkg80w04wgccos48gcws8cs_strategaize-net`, mit SAVEPOINT-Pattern für expected RLS-Permission-Denials.
3. Manueller User-Smoke-Test in Chrome + Edge (Permission-Prompts → Recording → 1min-verkürzter-Auto-Stop-Test → Stop-Button → Upload-Progress → Redirect zur Status-Page).
4. Records-Update + Slice-Closing-Commit + Push.
