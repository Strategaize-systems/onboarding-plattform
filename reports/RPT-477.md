---
id: RPT-477
date: "2026-06-15"
skill: /deploy
feature: FEAT-083, FEAT-084
type: completion
status: completed
result: "V9.7 OKF Handbuch-Export DEPLOYED to production (REL-033). Live-Smoke PASS. One deploy incident (yaml devDep, ISSUE-103) found + fixed."
title: "Deploy V9.7 — OKF Handbuch-Export (DEPLOYED, Live-Smoke PASS)"
---

# Deploy V9.7 — OKF Handbuch-Export

## What was deployed / where
- V9.7 (SLC-V9.7-A + B), OP onboarding-plattform → production (Hetzner `159.69.207.29` / Coolify).
- Merge `3f58e26`; Deploy-HEAD `a8846ff` (incl. yaml dependency hotfix). Pushed to `origin/main`. Coolify **manual redeploy** of app + worker (webhook inactive).
- **0 migrations, 0 new ENVs, 0 new cron jobs.** One dependency change: `yaml` devDependencies → dependencies.

## Phase 1 — Prerequisites
Update deploy of an already-live stack (V9.5/REL-032 running). Push delta verified = V9.7 OKF code + records only (no foreign code). Founder handled the Coolify redeploy; agent drove push + SSH verification + Live-Smoke.

## Phase 2 — Execution
1. `git push origin main` → `a8846ff` (after hotfix), 0 ahead.
2. **First redeploy → worker crash-loop** `ERR_MODULE_NOT_FOUND 'yaml'` (ISSUE-103). Diagnosed: `yaml` was a devDependency; worker build externalizes packages → missing in `--omit=dev` runtime. Fixed: moved `yaml@^2.9.0` to dependencies (`a8846ff`), `npm install`, build:worker + OKF suite 64/64 GREEN locally.
3. **Second redeploy → worker `(healthy)`**, RestartCount=0, registered `handbook_snapshot_generation` + all handlers; app + DB healthy.
4. **Live-Smoke (R-B-4):** triggered real snapshot via `rpc_trigger_handbook_snapshot` (as tenant_admin `a317e861…`, session `…043a`, tenant `…00de`) → snapshot `07bb9461` reached `ready` (36379 bytes). Worker log: `OKF bundle: 38 files` (27 KU + 9 diagnoses + index.md + log.md). Extracted real ZIP from storage:
   - `handbuch/` 10 files (narrative, renderer code untouched) + `okf/` 38 files.
   - `okf/index.md`: `okf_version "0.1"` + `strategaize_okf_profile "1.0"` (SC-4).
   - `okf/log.md`: dated Creation entry "36 Concepts" (SC-5).
   - Concept frontmatter: type/title/description/timestamp/strategaize_source: op/strategaize_tenant/confidence (SC-1/2/3).
   - Cross-links `## Verwandte` → bundle-root-absolute, resolvable (same block_key).
   - **0 PII leak** (`recorded_by_user_id` in 0 files, DEC-223/DSGVO).
   - `okf/` present ⟹ in-worker `checkOkfConformance` ok=true on real bundle.
   - Server temp smoke files cleaned.

## Phase 3 — Learning
- **ISSUE-103** (resolved): worker-imported npm package must be in `dependencies` (worker build externalizes); /architecture's "already a dependency" claim was wrong (it was dev-only). Recorded in KNOWN_ISSUES + Dev-System IMP.
- Process gap → /architecture + /final-check must verify **dependency vs devDependency** for worker-imported packages, not just "present in package.json".

## Records updated
- Created: `/reports/RPT-477.md`. Modified: `/docs/RELEASES.md` (REL-033), `/planning/roadmap.json` (V9.7 → released), `/docs/STATE.md` (High-Level State → post-launch, phase → DEPLOYED), `/docs/KNOWN_ISSUES.md` (ISSUE-103 resolved).

## Problems found
- ISSUE-103 (worker yaml crash) — found + fixed during deploy. No other issues. Live-Smoke clean.

## Recommended next step
`/post-launch V9.7` T+24h (~2026-06-16) — confirm burn-in stability before declaring V9.7 the last stable version. Until then V9.5 remains last-stable.
