---
id: RPT-405
date: "2026-06-04"
skill: /qa
slice: SLC-166
feature: FEAT-071+072
type: completion
status: completed
reviewOf: RPT-404
result: "PARTIAL Live-Verifikations-Stack — Steps 1+2 PASS (Migration 107+108 LIVE-Apply + 35/35 RLS-Sidecar via node:22 gegen Coolify-DB). Steps 3+4 DEFERRED (AC-9 Live-Recall + MT-7 Browser-Smoke) bis V9-Master-Merge + Coolify-Redeploy weil V9-Code Branch-only ist und Live-App-Container auf Pre-V9-Commit ad94b60 laeuft. Bundle-Plan-Logik im RPT-404 Q-Gesamt-A+B hat App-Image-Drift gegen Branch-HEAD uebersehen. SLC-166 bleibt in_progress per IMP-950-Defense. CHECK-Constraints im Soll: ai_cost_ledger_role_check 18 Werte (incl. email_bulk_pre_filter + email_bulk_pii_redact), ai_jobs_job_type_check 18 Werte (incl. email_bulk_parse + email_bulk_pre_filter + email_bulk_thread_redact). MIG-052 + MIG-053 (Records-Sicht) werden mit LIVE-Apply-Datum 2026-06-04 dokumentiert. 1 neue Dev-System-IMP IMP-1041 (/qa-Live-Verify-Stack-Planning muss App-Container-Image-Commit gegen Branch-HEAD pruefen vor Bundle-Plan)."
title: "OP V9 SLC-166 Live-Verifikations-Stack PARTIAL — Migration LIVE + RLS-Sidecar PASS, Browser-Smoke DEFERRED bis V9-Master-Merge"
---

# OP V9 SLC-166 Live-Verifikations-Stack PARTIAL

## Scope

- **Slice:** SLC-166 (V9 Pre-Filter Haiku + Thread-Aggregation + PII-Redaction)
- **Stack-Definition:** RPT-404 Q-Gesamt-A + Q-Gesamt-B Bundle-Plan (5 Steps: Migration LIVE-Apply + RLS-Sidecar + AC-9 Recall + MT-7 Browser-Smoke + Records-Bundle)
- **Worktree:** `c:/strategaize/strategaize-onboarding-plattform-v9`
- **Branch:** `v9-bulk-email-import` HEAD `71ce2d9`
- **Server:** Hetzner Coolify `159.69.207.29` (Onboarding-Plattform)
- **Pflicht-Reads:** RPT-404 GESAMT-PASS-Verdict, Migrations 107+108 SQL-Files, sql-migration-hetzner.md, coolify-test-setup.md, MEMORY [[project-op-v9-slc166-qa-gesamt-pass-2026-06-03]]

## Verdict

**PARTIAL** — 2/5 Steps PASS, 2/5 Steps DEFERRED (V9-Branch-only-Block), 1/5 Steps NOW (Records-Bundle dieser Report).

| Step | Status | Detail |
|---|---|---|
| 1. Migration 107+108 LIVE-Apply | **PASS** | beide Constraints im Soll-State Live-verifiziert |
| 2. 32-RLS-Sidecar Live-Run | **PASS** | 35/35 Tests in 2.53s gegen Coolify-DB |
| 3. AC-9 Live-Recall 10% Threads | **DEFERRED** | V9-Worker-Code nicht im Live-App-Container |
| 4. MT-7 Browser-Smoke | **DEFERRED** | V9-UI-Routes nicht im Live-App-Container |
| 5. SLC-166 done-Setzen + Records-Bundle | **NOW (PARTIAL)** | dieser Report + Records reflektieren PARTIAL-Status, SLC-166 bleibt in_progress |

**SLC-166 Closure-Status:** bleibt `in_progress`. Voll-Close erst nach AC-9 + MT-7 Browser-Smoke nach V9-Master-Merge + Coolify-Redeploy (regulaerer /go-live + /post-launch nach V9-Komplett SLC-165+166+167+168).

## Step 1: Migration 107+108 LIVE-Apply (PASS)

### MIG-107 (`107_v9_cost_ledger_email_bulk_pre_filter_role.sql`, 51 Lines)

**Apply-Procedure** (sql-migration-hetzner.md):
1. `base64 -w 0 sql/migrations/107_v9_cost_ledger_email_bulk_pre_filter_role.sql` (lokal)
2. `ssh root@159.69.207.29 "echo '<B107>' | base64 -d > /tmp/m107.sql"` (Pattern aus DIAGNOSE_TEMPLATE_EDITING.md L117)
3. `ssh root@159.69.207.29 "DB=$(docker ps --format '{{.Names}}' | grep ^supabase-db); docker exec -i $DB psql -U postgres -d postgres < /tmp/m107.sql"`
4. Verify: `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='ai_cost_ledger_role_check';`

**Output:**

```
BEGIN
ALTER TABLE
ALTER TABLE
COMMIT
```

**Pre-State (before MIG-107):**
```sql
CHECK (role IS NULL OR role = ANY (ARRAY[
  'analyst', 'challenger', 'chat', 'memory', 'embedding', 'orchestrator',
  'sop_generator', 'diagnosis_generator', 'evidence_mapper', 'dialogue_extractor',
  'bridge_engine', 'walkthrough_pii_redactor', 'walkthrough_step_extractor',
  'walkthrough_subtopic_mapper', 'light_pipeline_block', 'v8_1_augmentation'
]))  -- 16 Werte
```

**Post-State (after MIG-107):**
```sql
CHECK (role IS NULL OR role = ANY (ARRAY[
  ... (16 vorherige) ..., 'email_bulk_pre_filter'
]))  -- 17 Werte
```

**Verdict:** PASS. Atomic Apply, idempotent (DROP IF EXISTS).

### MIG-108 (`108_v9_thread_redact_role_and_job_types.sql`, 98 Lines)

**Apply-Procedure** wie MIG-107.

**Output:**

```
BEGIN
ALTER TABLE
ALTER TABLE
ALTER TABLE
ALTER TABLE
COMMIT
```

**Post-State (after MIG-108):**

`ai_cost_ledger_role_check`:
```sql
CHECK (role IS NULL OR role = ANY (ARRAY[
  ... (17 vorherige) ..., 'email_bulk_pii_redact'
]))  -- 18 Werte
```

`ai_jobs_job_type_check`:
```sql
CHECK (job_type = ANY (ARRAY[
  'bridge_generation', 'diagnosis_generation', 'dialogue_extraction',
  'dialogue_transcription', 'evidence_extraction', 'handbook_snapshot_generation',
  'knowledge_unit_condensation', 'recondense_with_gaps', 'sop_generation',
  'walkthrough_extract_steps', 'walkthrough_map_subtopics', 'walkthrough_redact_pii',
  'walkthrough_stub_processing', 'walkthrough_transcribe', 'lead_push_retry',
  'email_bulk_parse', 'email_bulk_pre_filter', 'email_bulk_thread_redact'
]))  -- 18 Werte
```

**Verdict:** PASS. Atomic Apply, idempotent. **Pre-existing-Bug-Fix erfolgreich:** 3 V9-job_types (`email_bulk_parse` + `email_bulk_pre_filter` + `email_bulk_thread_redact`) sind jetzt im CHECK enthalten — SLC-165 MT-1+MT-2 + SLC-166 MT-2+MT-3 enqueue-Pfade wuerden Live nicht mehr crashen.

### MIG Records Status

- MIG-052 (SLC-166 MIG-107) → LIVE-applied 2026-06-04
- MIG-053 (SLC-166 MIG-108) → LIVE-applied 2026-06-04

Records-Update siehe Step 5 unten.

## Step 2: 32-RLS-Sidecar Live-Run (PASS)

### Setup

- **Test-Container:** `node:22` (forward-kompatibel, coolify-test-setup.md IMP-728)
- **Docker-Netz:** `bwkg80w04wgccos48gcws8cs_strategaize-net` (Coolify Strategaize-Project)
- **Postgres-Container:** `supabase-db-bwkg80w04wgccos48gcws8cs-084548596447` (dynamic Lookup per IMP-497)
- **Volume-Mount:** `/opt/onboarding-plattform-test:/app`
- **Repo-State:** Server-Pulled auf `71ce2d9` vor Test-Run

### Command

```bash
ssh root@159.69.207.29 "DB=\$(docker ps --format '{{.Names}}' | grep ^supabase-db); \
  PW=\$(docker exec \$DB printenv POSTGRES_PASSWORD); \
  NET=\$(docker network ls --format '{{.Name}}' | grep ^bwkg80w04wgccos48gcws8cs_strategaize-net); \
  docker run --rm --network \$NET \
    -v /opt/onboarding-plattform-test:/app -w /app \
    -e TEST_DATABASE_URL=\"postgresql://postgres:\$PW@\$DB:5432/postgres\" \
    node:22 npx vitest run src/__tests__/rls/v9-bulk-email.rls.test.ts"
```

### Output (extract)

```
 ✓ src/__tests__/rls/v9-bulk-email.rls.test.ts (35 tests) 2535ms

 Test Files  1 passed (1)
      Tests  35 passed (35)
   Duration  3.13s
```

### Verdict

**PASS** — 35/35 Tests GREEN in 2.53s gegen Live-Coolify-DB.

**Coverage-Aggregat (per RPT-404 Sektion):**
- 24 SLC-165 MT-6 Baseline-Cases (4 Rollen × 4 Tabellen × 6 Cases) — sensitive-Read + Default-Deny + Status-Transitions
- +8 SLC-166 MT-7 Cluster-Erweiterung (3 Sensitive-Read + 3 Status-Transitions + 2 Default-Deny auf MT-6+MT-7-Tabellen)
- +3 Cross-Cut-Defense (tenant_admin Cross-Tenant-INSERT/UPDATE-Reject + strategaize_admin INSERT-Default-Deny)

**AC-14 Live-Confirmation:** PASS Code-Side + Live-Sidecar gegen Coolify-DB.

## Step 3: AC-9 Live-Recall 10% Threads (DEFERRED)

### Blocker (Critical-Issue identifiziert)

V9-Code (Worker-Pipeline + UI) ist **NICHT** im Live-App-Container deployed.

**Evidence:**

```bash
$ ssh root@159.69.207.29 "docker ps --format 'table {{.Names}}\t{{.Image}}' | grep -E '^app-|^worker-'"
app-bwkg80w04wgccos48gcws8cs-084548571057    bwkg80w04wgccos48gcws8cs_app:ad94b6021329191e2778bd8282d5143bb844691d
worker-bwkg80w04wgccos48gcws8cs-084548585995  91c72b9df1c4
```

**Image-Tag-Analyse:**
- App-Container baut auf Commit `ad94b602` = `fix(SLC-164): V8.1.1 Hotfix — Partner-Resolution via Tenant-Chain` (Pre-V9-Master)
- V9-Branch HEAD ist `71ce2d9` — nicht in Live-Image enthalten
- V9-Branch `v9-bulk-email-import` ist Branch-only Cumulative pre-Master-Merge (per RPT-404 L337 "V9 ist Branch-only Cumulative-Branch")

**Konsequenz:** Live-Pipeline-Run unmoeglich:
- MT-2 Pre-Filter-Worker (`handle-pre-filter-job.ts`) existiert nur im V9-Branch-Code
- MT-6 Thread-Redact-Worker (`handle-thread-redact-job.ts`) existiert nur im V9-Branch-Code
- Worker-Dispatch (`handle-job.ts` 3-case-Branches) im Live-Worker kennt die V9-job_types `email_bulk_pre_filter` + `email_bulk_thread_redact` NICHT

Ein Bulk-Email-Run-Trigger wuerde im Live-Worker fehlschlagen mit unbekanntem `job_type`. Keine `redacted_body`-Erzeugung moeglich. Keine 10%-Sampling Basis.

### Deferred-Plan

AC-9 wird im regulaeren V9-Master-Merge-Workflow erledigt:
1. V9 Komplett (SLC-165 + SLC-166 + SLC-167 + SLC-168) Code-Side
2. /final-check V9
3. Master-Merge + Coolify-Redeploy (App + Worker auf V9-HEAD)
4. /go-live V9
5. /post-launch V9 → AC-9 + MT-7 Live-Smoke als Pflicht-Gate

### AC-9-Status

| Gate | Code-Side | Live-Sidecar | Live-Master-Merge |
|---|---|---|---|
| Pattern-Scan-Tests redacted_body | PASS (RPT-401) | n/a | n/a |
| 10% Threads-Sample Recall | n/a | n/a | DEFERRED bis V9-Master-Merge |

## Step 4: MT-7 Browser-Smoke (DEFERRED)

### Blocker

Gleiche Root-Cause wie Step 3:
- UI-Routes `/dashboard/bulk-email-import/[run_id]/stage-detail/...` und `/dashboard/bulk-email-import/[run_id]/filter-review` existieren nur im V9-Branch-Code
- Live-App-Container rendert sie nicht
- `FilterReviewLinkBanner` + `ThreadAggregationCard` Komponenten sind V9-Greenfield

### Deferred-Plan

MT-7 Browser-Smoke wird im regulaeren V9-Master-Merge-Workflow erledigt:
- Status-Walk pre_filtered → thread_redacting → thread_redacted erst nach V9-Live-App + Live-Worker
- Founder-Aktion im Browser nach V9-Master-Merge + Coolify-Redeploy

### Root-Cause Bundle-Plan-Optimismus (RPT-404 Q-Gesamt-A+B)

RPT-404 Q-Gesamt-B hat einen Bundle-Plan formuliert ("Step 1+2+3+4 in single Founder-Pause-Window ~33min") **ohne pruefen** ob V9-App-Code Live deployed ist. Da V9 explicit Branch-only ist (RPT-404 L337), war die Praemisse falsch.

**Lesson Learned → IMP-1041** (Dev-System): /qa-Live-Verify-Stack-Planning muss App-Container-Image-Commit gegen Branch-HEAD pruefen **bevor** Live-Pipeline-Steps gebundled werden. App-Image-Commit `!= ` Branch-HEAD → Steps die V9-Worker/V9-UI brauchen NICHT bundlable.

Siehe `docs/SKILL_IMPROVEMENTS.md` IMP-1041.

## Step 5: Records-Bundle (NOW — PARTIAL-Status)

### Records-State per IMP-950-Defense (UNVERAENDERT bzgl. SLC-166 done-Setzung)

| File | Aenderung | Begruendung |
|---|---|---|
| `slices/INDEX.md` | SLC-166 **bleibt** `in_progress` | AC-9 + MT-7 DEFERRED — Live-Verify-Stack NICHT komplett |
| `features/INDEX.md` | FEAT-071 + FEAT-072 **bleiben** `in_progress` | konsistent zu slices/INDEX |
| `planning/backlog.json` | BL-148 + BL-149 **bleiben** `in_progress` | konsistent |
| `planning/roadmap.json` | V9 **bleibt** `active` | Branch-only, kein Release-Status faellig |
| `docs/STATE.md` | Current Focus + Current Phase UPDATE | Cockpit-Visibility: "Pre-Live-Migrations LIVE-applied + RLS-Sidecar PASS, Browser-Smoke DEFERRED bis V9-Master-Merge" |
| `docs/MIGRATIONS.md` | MIG-052 + MIG-053 UPDATE auf `applied 2026-06-04` | Migration-Stack-Trace |
| `docs/KNOWN_ISSUES.md` | kein neuer ISSUE | DEFERRED-Status ist Workflow-Stand, kein offener Bug |
| `docs/SKILL_IMPROVEMENTS.md` (Dev-System) | **+ IMP-1041** | /qa-Live-Verify-Stack-Planning App-Image-Check-Pflicht |
| `reports/RPT-405.md` | **NEU** dieser Report | PARTIAL Live-Verify-Stack-Doku |

### Was NICHT in Records geschrieben wird (per IMP-950)

- SLC-166 nicht `done` (Voll-Close-Gate noch offen: AC-9 + MT-7)
- V9 nicht `released` / nicht `stable` (Branch-only)
- AC-9 PASS-Setzen nicht (DEFERRED)
- AC-14 PASS-Setzen JA (RLS-Sidecar PASS, Live-Confirmation erbracht)

## Findings (Aggregat)

### Blocker: 0

### High: 1

**H-1 Bundle-Plan-Logik im RPT-404 Q-Gesamt-A+B war optimistisch** (Severity High weil es zu einem fehlerhaften Workflow-Plan gefuehrt hat)

- **Root-Cause:** /qa-Skill hat App-Container-Image-Commit nicht gegen Branch-HEAD geprueft vor Bundle-Plan
- **Wirkung:** PRIMARY-Resume-Path im Handoff war nicht vollstaendig durchfuehrbar — User wurde aufgefordert ein ~33min-Slot zu blockieren, der nur ~15min gebraucht hat (Migrations + Sidecar), dann Critical-Issue-Stop
- **Mitigation:** IMP-1041 + PARTIAL-Closure-Pattern dieser Report
- **Auto-Fix Entscheidung:** N/A — Plan war bereits geschrieben, dies ist die Korrektur-Doku
- **Defense:** kuenftig in /qa-Live-Verify-Stack-Sektion App-Image-Check Pflicht-Sub-Step

### Medium: 0

### Low: 0

## Open Verification Gaps (Pre-V9-Master-Merge-Pendenzen)

| Gap | Zugehoeriger AC | Scheduling | Owner |
|---|---|---|---|
| AC-9 Live-Recall 10% Threads | AC-9 | Nach V9-Master-Merge + Coolify-Redeploy | GF (Eyeball auf redacted_body) + Agent (samplePctThreads-Setup) |
| MT-7 Browser-Smoke Status-Walk | AC-4 + AC-5 + AC-6 + AC-7 | Nach V9-Master-Merge + Coolify-Redeploy | GF (Browser-Aktion) |
| AC-2 1000-Emails Performance-Test | AC-2 | Nach V9-Master-Merge mit echtem Corpus | GF |
| AC-3 Cost <0.20 EUR Performance-Test | AC-3 | Nach V9-Master-Merge mit echtem Corpus | GF |

## Self-Check (Workflow-Discipline)

1. **Hat /qa SLC-166 die Live-Verifikations-Steps abgedeckt die jetzt erbracht werden konnten?** Ja — Migrations + RLS-Sidecar PASS.
2. **Hat /qa SLC-166 die Steps deferred die wegen Live-Code-Drift nicht moeglich waren?** Ja — AC-9 + MT-7 mit klarer Deferred-Plan-Doku.
3. **Wird SLC-166 in_progress gehalten?** Ja, per IMP-950-Defense bis AC-9 + MT-7 PASS dokumentiert.
4. **Wird V9-Release-Status korrekt gehalten?** Ja, V9 bleibt `active` Branch-only.
5. **Wird die /qa-Plan-Verbesserung dokumentiert?** Ja, IMP-1041.

## Recommended Next Step

**Cross-Repo Pivot — BS V8.13 SLC-895 Workflow-Closure** (~30-45min, Branch-Loc, kein Live-Deploy noetig)

Begruendung:
- V9 SLC-166 weitere Code-Arbeit ist nicht moeglich vor SLC-167 (Pattern-Extraktion Sonnet + Curation-UI + Cost-Cap, ~3-4 Tage eigene Session)
- BS V8.13 hat offenes Release-Gate per Memory [[project-bs-v813-slc895-done-2026-06-03]] (RPT-575 Code-Side done, Workflow-Closure /qa Gesamt-Smoke + /final-check + /go-live + REL-045 + /post-launch noch offen)
- IMP-950-Defense ruft Release-Gate-Closure als Pflicht
- Kein zusammenhaengender Founder-Slot noetig fuer BS-Workflow-Closure

Alternative Cross-Repo (kein Workflow-Closure-Skip):
- ImSch V3.2 SLC-323 MT-6 pushLeadToPipeline FK-Erweiterung (~60-90min)
- IS V4 SLC-125 MT-3 UI 5 Surfaces (~5-7h fresh Session)
- IS V1.5 + V1.5.1 T+24h ~2026-06-04 18:24 UTC (gemeinsam ~20-30min)
- BS V8.10 T+24h ~2026-06-04 07:00 UTC

**Forbidden Shortcuts** (BLOCKING per IMP-950):
- SLC-166 nicht `done` setzen (AC-9 + MT-7 noch DEFERRED)
- V9 nicht `released`/`stable` setzen (Branch-only)
- Memory schreiben "V9 stable/released" — verboten
