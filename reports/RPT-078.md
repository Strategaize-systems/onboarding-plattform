---
id: RPT-078
date: "2026-04-25"
skill: /backend
slice: SLC-035
feature: FEAT-023
type: completion
status: completed
reviewOf: null
result: "MT-1..MT-3 done: Migration 073 + 073b live auf Hetzner, Prompt-Builder + Worker-Handler implementiert mit Dependency-Injection-Pattern fuer BedrockCaller. 53 neue Tests (19 RPC-Integration + 34 Unit), Suite 197/197 PASS auf Coolify-DB. MT-4..MT-7 offen."
title: "SLC-035 — Bridge-Engine Backend MT-1..MT-3 (Backend-Kern)"
---

# SLC-035 — Bridge-Engine Backend MT-1..MT-3 (Backend-Kern)

## Outcome summary

**Status:** Partial complete (3/7 Micro-Tasks done).

MT-1, MT-2, MT-3 vollstaendig implementiert, getestet, committed und auf Hetzner deployed (Migration). Der Slice ist `in_progress` — vor `/qa` SLC-035 muessen MT-4 (Worker-Dispatcher-Registrierung), MT-5 (Stale-Trigger E2E-Test) und MT-6/7 (Tests + Records-Update) abgeschlossen werden.

## Files reviewed

- `/slices/SLC-035-bridge-engine-backend.md` — Slice-Definition
- `/docs/ARCHITECTURE.md` — V4-Sektion (Bridge-Engine)
- `/docs/MIGRATIONS.md` — MIG-023 V4-Plan
- `/sql/migrations/068_bridge_tables.sql` — bestehende Bridge-Tabellen-Schema
- `/src/workers/condensation/claim-loop.ts` — Worker-Dispatcher-Pattern (fuer MT-4)
- `/src/workers/run.ts` — Worker-Entry (fuer MT-4)

## Files changed

- `/docs/STATE.md` — Current Focus + Phase auf SLC-035 MT-1..MT-3 done aktualisiert
- `/slices/INDEX.md` — SLC-035 status: planned → in_progress
- `/slices/SLC-035-bridge-engine-backend.md` — MT-1..MT-3 als done markiert; bridge_proposal-Granularitaet bewusst auf Block-Level revertiert (siehe IMP-126 im Dev-System)

## Files created

### Migrationen
- `/sql/migrations/073_rpc_bridge.sql` — 3 RPCs: `rpc_trigger_bridge_run`, `rpc_approve_bridge_proposal`, `rpc_reject_bridge_proposal`. Live auf Hetzner via base64+psql-Pattern.
- `/sql/migrations/073b_cost_ledger_bridge_role.sql` — `ai_cost_ledger.role` CHECK additiv um `'bridge_engine'` erweitert (analog MIG-022). Live auf Hetzner.

### Worker
- `/src/workers/bridge/types.ts` — Type-Definitionen fuer BridgeJobInput, BridgeProposalDraft, BedrockCaller-Interface.
- `/src/workers/bridge/prompts.ts` — `buildTemplatePromptForSubtopic()` + `buildFreeFormPrompt()` mit JSON-Output-Schema. Includes `subtopicKeyToBlockKey()` Regex-Mapping als Workaround fuer Block-Granularitaet (siehe IMP-126).
- `/src/workers/bridge/parse-output.ts` — Defensive JSON-Parser mit Fallback-Logik bei LLM-Fehlern (Fallback auf unveraenderte Template-Schablone + erster role_hint).
- `/src/workers/bridge/process-bridge-run.ts` — Pure-Logic-Funktion `processBridgeRun(input, bedrock)` mit injected BedrockCaller fuer Test-Friendliness.
- `/src/workers/bridge/handle-bridge-job.ts` — DB-Orchestrator `handleBridgeJob(job)` mit Bedrock-Call + Cost-Ledger-Write + bridge_proposal-INSERT.

### Tests (53 neu, alle PASS)
- `/src/tests/rpc/bridge-rpcs.test.ts` — 19 RPC-Integration-Tests gegen Coolify-DB (alle 3 RPCs, RLS, idempotenz, error-cases).
- `/src/workers/bridge/tests/prompts.test.ts` — Unit-Tests fuer Prompt-Builder.
- `/src/workers/bridge/tests/parse-output.test.ts` — Unit-Tests fuer JSON-Parser + Fallback.
- `/src/workers/bridge/tests/process-bridge-run.test.ts` — Unit-Tests fuer Pure-Logic-Worker mit Mock-BedrockCaller.

## Files intentionally not changed

- `/src/workers/condensation/claim-loop.ts` — bewusst nicht geaendert. Dispatcher-Erweiterung ist MT-4 (naechster Schritt).
- `/src/workers/run.ts` — bewusst nicht geaendert. Handler-Wiring ist MT-4.
- `/sql/migrations/074_rpc_handbook.sql` — gehoert zu SLC-039, nicht SLC-035.

## Problems found

1. **Schema-Granularitaets-Mismatch (IMP-126 im Dev-System):** Slice und ARCHITECTURE.md beschreiben Subtopic-Filter, aber Schema `knowledge_unit` und `block_diagnosis` kennen nur Block-Level. Workaround per Regex-Mapping `subtopicKeyToBlockKey()` in `prompts.ts`. Slice wurde initial in falscher Richtung "korrigiert" (feature → role, basierend auf unvollstaendiger Migration-Recherche), nach Live-Schema-Inspect korrekt revertiert. Lesson dokumentiert als IMP-126 im Dev-System.
2. **Test-Repo-Drift auf Hetzner:** Test-Repo auf Hetzner (`/root/onboarding-test`) stand auf altem Commit `d7f2cfd`. Sync der neuen Test-Files via `scp`. Low-Friction, aber fragiler Pfad. Kein Cockpit-Impact, aber Erinnerung fuer kuenftige /qa-Laeufe.

## Open points or deferred decisions

- **MT-4 (Worker-Dispatcher-Registrierung)** — `claim-loop.ts` JOB_TYPES-Erweiterung + Dispatcher-Branch + `run.ts`-Handler-Registrierung. ~15 Min.
- **MT-5 (Stale-Trigger E2E-Test)** — `src/__tests__/integration/bridge-stale-flow.test.ts` mit Fixture: bridge_run completed → block_checkpoint INSERT → status='stale'. Pflicht-Verifikation DEC-039. ~30-45 Min.
- **MT-6/7 (Tests + RECORD-Updates)** — abschliessende Tests + MIGRATIONS.md MIG-023-Eintrag (in diesem Cockpit-Update bereits ergaenzt). ~10 Min.
- **/qa SLC-035** — End-to-End gegen real gefuellte capture_session, echter Bedrock-Call eu-central-1, Cost-Ledger-Verifikation. Erwartete Bedrock-Kosten ~$0.05–0.20.
- **Test-Tenant fuer /qa:** Demo-Tenant oder neuer Tenant (User-Entscheidung).

## Project record updates

- `/docs/STATE.md` — bereits in vorheriger Session aktualisiert
- `/slices/INDEX.md` — SLC-035 = in_progress
- `/features/INDEX.md` — FEAT-022 + FEAT-023 jetzt in_progress (war bisher planned trotz SLC-033/034 released)
- `/docs/MIGRATIONS.md` — MIG-023 Progress-Eintrag fuer SLC-035 MT-1..MT-3 ergaenzt (inkl. 073b out-of-scope-Erweiterung)
- `/planning/backlog.json` — BL-042 (Bridge-Engine) bereits in_progress
- `/planning/roadmap.json` — V4 active

## Recommended next step

`/backend` SLC-035 fuer MT-4 + MT-5 + MT-7 (~1-2 Stunden), direkt anschliessend `/qa` SLC-035.

Commits: 3226068 (Migration 073), 5c3f5ff (Worker-Skelett), 26086b8 (Migration 073b + Cost-Ledger-Fix), bc636a3 (Slice-Revert auf Block-Granularitaet).
