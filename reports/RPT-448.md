---
id: RPT-448
date: "2026-06-11"
skill: /final-check
slice: SLC-V9.1-A..D
feature: FEAT-075..079
type: completion
status: completed
reviewOf: null
result: "Final-Check V9.1 CONDITIONALLY READY — 0 Blocker / 0 High / 0 Medium / Low+accepted-residual. Code-Side release-ready; bedingt durch Founder-Deploy-Preconditions (OQ-R1-1 IONOS + 3 Coolify-Scheduled-Tasks, D4 Wording). npm-audit-Findings allesamt pre-existing framework/SDK-transitiv, kein V9.1-Intro."
title: "Final-Check V9.1 — Forward-Bucket-Email Pre-Release-Audit (7 Dimensionen)"
---

# Final-Check V9.1 — Forward-Bucket-Email (SLC-V9.1-A..D)

**Scope:** 7-Dim Pre-Release-Audit ueber V9.1 (FEAT-075..079) im Worktree `v9-1-forward-bucket-email` (HEAD `dcacd00`), aufbauend auf Gesamt-/qa PASS (RPT-447).

## Overall Assessment: CONDITIONALLY READY for /go-live

0 Blocker / 0 High / 0 Medium. Der **Code** ist release-ready. Die Bedingungen sind ausschliesslich **Founder-Deploy-Preconditions** (Infrastruktur-Setup zur Deploy-Zeit) — keine Code-Defekte.

## Findings by dimension

### 1. Code-Qualitaet — PASS
- `tsc --noEmit` EXIT 0. V9.1-Source (110 Diff-Files) ESLint-clean + Stub-clean (0 TODO/FIXME/console.log/leere-catch).
- Cross-Slice-Wiring drift-frei (RPT-447): Status-Enum, Audit (uniform `error_log`), Cron-Auth (uniform `x-cron-secret`), Shared-Helper real verdrahtet, Schema-Spalten run-level.
- **Low (pre-existing):** `eslint .` zeigt 1 Error (`src/app/admin/text-overrides/page.tsx:263`) + 1 Warning (`src/lib/pdf/mandanten-report-v2/pages/cover.tsx:190`). Beide NICHT im V9.1-Diff (zuletzt SLC-144), bereits auf deployed `main` → **build-proven non-blocking** (Coolify-Docker-Build von main laeuft mit diesem Lint-Stand; next.config setzt kein `ignoreDuringBuilds`, dennoch baut main). Cleanup-Empfehlung, kein V9.1-Blocker.

### 2. Security & Privacy — PASS
- Keine Hardcoded-Secrets in V9.1-Source. IMAP-Credentials + CRON_SECRET ENV-only. Setup-Token 32-byte URL-safe + UI-maskiert.
- RLS auf allen 4 neuen Tabellen (MIG-112/116). Policies existieren + korrekt typisiert (admin_all + tenant-scoped + service_write).
- **Low → ISSUE-096:** Behavioral-4-Rollen-Pen-Test nur fuer `email_inbound_sync_state`; 3 Foundation-Tabellen nur Policy-Existenz-verifiziert. Vor Customer-Live ergaenzen. Akzeptiert fuer Internal-Test-Mode.
- Security-Headers (next.config): X-Frame-Options DENY, nosniff, HSTS, Referrer-Policy, Permissions-Policy — vorhanden (pre-existing, kein V9.1-Scope).

### 3. Legal & Compliance — PASS
- COMPLIANCE.md Section 10 (V9.1 Forward-Bucket-Addendum, RPT-446): EU-Data-Residency, 4 Audit-Mechanismen, Retention run-level.
- **Data-Residency EU-konform:** IONOS-IMAP (Deutschland) + Bedrock eu-central-1 (Frankfurt) — KEIN Cross-Region (DEC-205; DEC-196 superseded). Erfuellt `data-residency.md`.
- DSGVO-Consent-Audit-Trail (`dsgvo_consent_*`-Spalten MIG-063 + error_log-Event, 7-Jahre-Markierung). Retention/Loesch-Lifecycle (SLC-C 60d Soft / 90d Hard).
- Neue Dependency `imapflow@^1.3.1` = **MIT** (license-clean).

### 4. Testing-Tiefe — PASS (mit dokumentierten Deferrals)
- Hermetische Vitest GREEN: V9.1-targeted 291 pass / 4 skip; Full-Suite 1797 pass / 508 env-fail (`TEST_DATABASE_URL`, Coolify-DB-Integration) / 36 skip.
- DB-Integration- + RLS-Behavioral-Run gegen OP-Coolify-DB: in Einzel-Slice-/qa (RPT-442/443/445) bereits gelaufen + PASS; lokal env-gated.
- IONOS-End-to-End-Live-Smoke + Cron-Live-Smokes: deferred /deploy (OQ-R1-1 founder-gated).
- Keine RTL-Component-Render-Tests (OP-node-env-Konvention; mitigiert durch hermetische Wiring-Tests + next-build COMPILE PASS).

### 5. CI/CD & Deploy-Readiness — CONDITIONALLY READY
- Migrationen **MIG-057..063 (112-118) alle LIVE** auf Coolify-Postgres.
- `.env.deploy.example` auf IMAP umgestellt (IMAP_HOST/PORT/USER/PASSWORD, INBOUND_DEFAULT_ENDPOINT_SLUG, V91_*-Cost-Cap/Retention-ENVs); **SES/HMAC-SES-ENVs entfernt**. CRON_SECRET vorhanden.
- next build proven (deployed main + RPT-444 COMPILE PASS auf diesem Branch). Standalone-Output, interne Supabase-Kong-Rewrites korrekt.
- **DEPLOY-PRECONDITION (Founder):** 3 Coolify-Scheduled-Tasks muessen vor produktiver Funktion existieren — `/api/cron/inbound-email-imap-sync` (≥5min), `/api/cron/email-bulk-pipeline-trigger` (stuendlich), `/api/cron/bulk-email-retention-sweep` (taeglich 02:00 UTC). Plus OQ-R1-1 IONOS-Postfach + IMAP-ENVs. Ohne diese laeuft die Inbound-Pipeline nicht (kein Code-Defekt — Infra-Setup).

### 6. Observability & Ops — PASS
- Audit-Trail durchgaengig `error_log` (captureInfo/captureException) inkl. Cron-Run-Stats + Retention-Sweep-Counts + DSGVO-Consent-Events.
- Founder-Reset/Override-Procedures in RUNBOOK (V9.1 Cost-Cap + Retention + Setup-UI-Walkthrough Sections).
- OP nutzt error_log-basiertes Monitoring (kein Sentry — OP-Standard, bewusst).
- Cost-Monitoring: 3-Schichten-Cost-Cap (Daily 5€ / Monthly 100€ / Per-Email-Approval 0.50€) + GF-Notification + Pipeline-Pause.

### 7. Post-Go-Live-Readiness — PASS
- KNOWN_ISSUES tracked (ISSUE-095/096 Low). RELEASES.md REL-XXX folgt in /go-live bzw. /deploy.
- V9.1 strikt Internal-Test-Mode (Founder-only), kein Customer-Outreach ([[module-lifecycle-discipline]] + IMP-950).

## Explicit Release Risks
1. **Inbound-Pipeline funktioniert erst nach Founder-Infra-Setup** (OQ-R1-1 IONOS + 3 Coolify-Cron-Tasks). Erwartete /deploy-Aktion, kein Code-Risiko.
2. **RLS-Behavioral-Coverage-Asymmetrie** (ISSUE-096) — vor Customer-Live zu schliessen, im Internal-Test-Mode akzeptiert.

## Required before /go-live
- Keine Code-Fixes erforderlich. /go-live ist eine GO/NO-GO-Entscheidung; die Founder-Preconditions (OQ-R1-1, D4) gehoeren in den /deploy-Schritt.

## Accepted residual risks
- **npm-audit (prod): 1 low / 3 moderate / 2 high — ALLE pre-existing framework/SDK-transitiv, KEIN V9.1-Intro.** `next` (HIGH), `fast-xml-builder` (HIGH, via AWS-SDK/Bedrock), `ws` (via `@supabase/supabase-js → realtime-js`), `esbuild`/`postcss`/`next-intl`/`brace-expansion` (MODERATE). Alle bereits auf deployed main. `imapflow` (einzige neue Prod-Dep) fuegt 0 Vulns hinzu. CRITICAL-Findings (`vitest`/`@vitest/coverage-v8`) sind **dev-only**, nicht shippable. → Eigenes Cross-Repo-Dependency-Maintenance-Window empfohlen, kein V9.1-Release-Blocker.
- Pre-existing ESLint-Debt (Dim 1, L-2) — Cleanup-Empfehlung.
- ISSUE-095 (Retention-Idempotency) — V9.2 Belt-and-Suspenders.

## Recommended next step
**`/go-live V9.1`** — GO/NO-GO mit dokumentierten akzeptierten Residual-Risks + Founder-Deploy-Preconditions (OQ-R1-1, D4) als GO-Bedingungen. Danach `/deploy` (Master-Merge + Coolify-Redeploy + IONOS-Setup + Live-Smokes) → `/post-launch`.
