---
id: RPT-598
date: "2026-07-06"
skill: /go-live
slice: SLC-187
feature: FEAT-104
type: completion
status: completed
reviewOf: null
result: "CONDITIONAL GO — Release-Freigabe V10.3 mit 3 bindenden Deploy-Conditions (Rebase-Re-Check, MIG-131-Reihenfolge, Live-Smokes AC-186-6/AC-187-5)"
title: "Go-Live-Entscheidung V10.3 — Rollenmodell V2 P1 (Passwort-Vergessen + Zwischenebene-Cleanup)"
---

# RPT-598 — /go-live V10.3

## Release target

Production (Coolify/Hetzner, Resource `bwkg80w04wgccos48gcws8cs`) — Merge `v10-3-rollenmodell-p1` → main + MIG-131-Apply + Redeploy. Delivery Mode SaaS, Internal-Test-Mode (kein Customer-Go-Live, module-lifecycle-discipline).

## Readiness decision: **CONDITIONAL GO**

Code-Readiness vollstaendig belegt (Gesamt-/qa PASS RPT-596, /final-check READY RPT-597, 0 Blocker/High/Medium). Operative Minimums live verifiziert. Die Conditions sind keine offenen Fixes, sondern die bindende Deploy-Prozedur.

## Operative Baseline (live verifiziert 2026-07-06 ~16:10 UTC)

- Container: app + worker + kong `(healthy)`, Up 6h — Stack stabil auf V10.2.1-Stand (`081034d`).
- `/api/health` intern: `{status:ok}`.
- **MIG-131-Annahmen driftfrei bestaetigt:** Live-pg_policies mit tenant_member/tenant_owner = **exakt 18** (== Migration-Scope) · profiles mit tenant_member/tenant_owner = **0** · Rollen-Verteilung: 1 strategaize_admin / 3 tenant_admin / 1 employee. Der defensive UPDATE in MIG-131 ist ein No-Op, der neue CHECK bricht keinen Bestands-User.

## Blockers

Keine.

## Conditions (bindend, in dieser Reihenfolge im /deploy)

1. **Pre-Merge-Re-Check mit Rebase** (git-release.md): Branch ist 7 ahead / 3 behind origin/main (`a2fd47a`, nur Records-Commits) → `git rebase origin/main` im Worktree + Tests auf rebased Stand, Manual-Diff-Review, dann Merge.
2. **MIG-131-Reihenfolge:** VOR Apply Pre-Apply-Live-Audit + Rollback-Dump sichern (pg_policies der 18 Policies + profiles_role_check-Def + handle_new_user prosrc) → MIG-131 applyen → Verifikations-Queries (0 Rest-Policies, CHECK 4 Werte) → DANN Server-DB-Suite via node:22-Sidecar → DANN Redeploy. Begruendung: neuer Code + alte Policies liesse tenant_member-Reste aktiv; Suite vor Apply testete alte Policy-Welt.
3. **Live-Smokes als Release-Validierung:** AC-186-6 (E2E-Passwort-Reset gegen Test-User — NIE Bestands-User [[never_touch_user_passwords]] — + GOTRUE_MAILER_OTP_EXP-TTL-Notiz in RELEASES) + AC-187-5 (CHECK/Policies-Live-Verify + Rollen-Login-Smoke) + externer Health/Login-Check nach Redeploy.

## Accepted risks

- 5 Policies `TO public` (Bestands-Shape, live-getreu; effektiv durch `auth.user_role()` gated) — Follow-up-Kandidat, kein Release-Risiko-Delta (RPT-597 Low-1).
- In-Memory-Rate-Limiter resetten bei Container-Restart (Bestands-Architektur aller OP-Limiter).
- GoTrue-recovery-TTL = GoTrue-Default (OTP_EXP unset) — bewusst, als TTL-Notiz zu dokumentieren.
- ISSUE-114 (Dummy-ENV-Build-Gate in Worktrees) — betrifft nur lokale Builds, nicht Prod-Build (Coolify hat echte ENV).

## Rollback-Plan

- **Code:** Coolify-Redeploy auf V10.2.1-Stand (`081034d` / origin/main vor Merge). RTO ~5min.
- **DB:** Re-Apply des Pre-Apply-Dumps aus Condition 2 (Policies + CHECK + Funktion — MIG-131 ist droppend/idempotent in beide Richtungen anwendbar). Alter Code laeuft auch mit neuen Policies (entfernte Rollen sind unvergeben), daher ist Code-only-Rollback ohne DB-Rollback zulaessig. RPO 0 (keine Datenmigration).

## Operational concerns

/post-launch V10.2.1 T+24h (~2026-07-07 10:30 UTC) laeuft parallel; V10.3-Redeploy ersetzt den Container (Uptime-Zaehler reset), Coolify-Task id=7 bleibt als DB-Row erhalten. Der V10.2.1-Burn-In-Check muss das beim T+24h-Review beruecksichtigen (Container-Neustart durch V10.3 ist KEIN Instabilitaets-Signal).

## Recommended next step

**/deploy V10.3** in der Condition-Reihenfolge; REL-040 + MIG-131-Flip (geplant→applied) als Records-Abschluss.
