---
id: RPT-019
date: "2026-04-17"
skill: /backend
slice: SLC-004
feature: FEAT-003
type: completion
status: completed
reviewOf: null
result: "Capture session start + block listing implemented. 5 MTs done, 6 unit tests green, build passes."
title: "SLC-004 Backend Completion — Capture-Session-Start + Block-Listing"
---

## Outcome Summary

SLC-004 is fully complete. A tenant_admin can now start a capture session for the Exit-Readiness template and see the block list with status badges. All 5 micro-tasks implemented, 6 unit tests green, build passes. No migrations needed — uses existing schema from SLC-001.

## Files Created

- `/src/app/capture/actions.ts` — Server Action `startCaptureSession`: auth check, template lookup, session INSERT, redirect.
- `/src/app/capture/new/page.tsx` — Server Component: auth guard, template list, renders StartSessionClient.
- `/src/app/capture/new/start-session-client.tsx` — Client Component: template card with start button, loading + error states.
- `/src/app/capture/[sessionId]/page.tsx` — Server Component: loads session + template + checkpoints + KU counts, renders BlockList.
- `/src/app/capture/[sessionId]/block-list.tsx` — Client Component: sorted block cards with status badges (open/submitted/reviewed/finalized).
- `/src/lib/capture/derive-block-status.ts` — Pure function: derives block status from checkpoints + KU presence.
- `/src/lib/capture/derive-block-status.test.ts` — 6 unit tests covering all status transitions.
- `/src/app/capture/__tests__/cross-tenant-access.test.ts` — 2 RLS isolation tests (session + checkpoint).
- `/reports/RPT-019.md` — This report.

## Files Changed

- `/src/lib/db/template-queries.ts` — (already fixed in SLC-003 QA: title schema + weight field)
- `/slices/INDEX.md` — SLC-004: planned -> done
- `/features/INDEX.md` — FEAT-003: planned -> in_progress
- `/planning/backlog.json` — BL-010: open -> done, BL-003 note added
- `/docs/STATE.md` — Current Focus, Phase, Next Steps, Last Stable Version

## Files Intentionally Not Changed

- No schema migration needed — all tables exist from SLC-001 (021-023).
- No RLS policy changes — existing policies handle tenant isolation correctly.
- No i18n changes — UI uses German strings directly for V1 (consistent with existing pattern).

## Architecture Decision: No (app) Route Group

The spec suggested `src/app/(app)/capture/` but the existing codebase does not use route groups. Created routes at `src/app/capture/` instead to match existing patterns. Not an architectural change — purely a file organization preference.

## Problems Found

- None. All micro-tasks implemented cleanly against existing schema and query layer.

## Verification Summary

- Build: `npx next build` PASS — `/capture/new` and `/capture/[sessionId]` routes present
- Unit tests: `vitest run derive-block-status.test.ts` — 6/6 PASS
- Cross-tenant test: written, requires Hetzner DB to run (consistent with ISSUE-003)
- Browser verification: requires Redeploy (pending)

## Open Points

- **App Redeploy needed** before browser verification. SLC-003 Zod fix + SLC-004 new routes both need it.
- **Dashboard link to /capture/new** not added — the existing dashboard shows Blueprint-era "runs". A navigation update should happen when more capture functionality is ready (SLC-005+).
- **i18n for status labels** (Offen/Eingereicht/Ausgewertet/Finalisiert) is hardcoded in German. Multi-language support can be added when i18n keys are extended.

## Recommended Next Step

`/qa` for SLC-004, then Redeploy, then SLC-005 Questionnaire-UI-Portierung.
