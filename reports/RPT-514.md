---
id: RPT-514
date: "2026-06-22"
skill: /deploy
slice: SLC-170
feature: FEAT-091
type: completion
status: completed
reviewOf: RPT-513
result: "MIG-124 + MIG-125 LIVE-applied auf Coolify-DB (159.69.207.29), beide verifiziert; origin/main gepusht (748dd8b). Migration-only Deploy (kein App-Redeploy), V10 bleibt active/implementing. 0 Findings."
title: "/deploy SLC-170 — MIG-124 + MIG-125 LIVE-Apply (Coolify-DB) + Push"
---

# /deploy SLC-170 — MIG-124 + MIG-125 LIVE-Apply

## 1. Outcome
**DONE.** Migration-only Deploy auf den bereits laufenden V9.8-Stack (kein Container-/Env-/App-Redeploy — der konsumierende Code SLC-173 Capture / SLC-174 Worker existiert noch nicht; die Migrationen gehen per R-170 / R-169-2 *vor* dem Code live). `origin/main` gepusht (`5f533d6..748dd8b`). MIG-124 (Schema) und MIG-125 (M-04-Seed) auf der Coolify-Postgres-DB live-appliziert und verifiziert. 0 Findings. V10 bleibt `active`/implementing — **keine Version-Freigabe** (kein /final-check-/go-live-Gate ausgeloest).

## 2. Phase 1 — Prerequisites (read-only verifiziert)
- SSH `root@159.69.207.29` erreichbar; DB-Container `supabase-db-bwkg80w04wgccos48gcws8cs-160450719820` (Suffix zur Laufzeit aufgeloest, IMP-497).
- Live-State vor Apply sauber (kein Partial): `modul_output` / `rpc_enqueue_module_output` / `template stb_modul_m04` allesamt ABSENT.
- Migration-Files in main committed (`748dd8b`).
- **CHECK-Superset-Vorpruefung (IMP-1228):** Live `ai_jobs_job_type_check` = 22 Werte, `ai_cost_ledger_role_check` = 21 Werte. MIG-124 baut beide CHECKs als strikte Supersets neu (22+1=23 / 21+2=23) — jeder Live-Wert ist enthalten (zeilenweise gegen Live-`pg_get_constraintdef` abgeglichen), kein Wert gedroppt → `DROP+ADD CONSTRAINT` kann keine Bestands-Row verletzen.

## 3. Phase 2 — Execution
1. **Push** `origin/main` → `748dd8b`. origin == local.
2. **Pre-Apply-Backup** (`/root/op-backups/`, TS 20260622-080055): `pg_dump --table=public.template` (template_pre125) + Constraint-Snapshot der beiden CHECKs (checks_pre124).
3. **MIG-124 apply** (base64→/tmp→`psql -U postgres -v ON_ERROR_STOP=1`): BEGIN…COMMIT, exit 0. (DROP-POLICY-NOTICEs = erwartete idempotente Guards.)
   - Verifiziert: `modul_output` exists=t; `rpc_enqueue_module_output` exists=t; `fn_min_tier_for_job('module_output_synthesis')`='blueprint'; `ai_jobs_job_type_check`=23 / `ai_cost_ledger_role_check`=23; 3 RLS-Policies auf `modul_output`.
4. **MIG-125 apply**: DO-Block, exit 0, NOTICE "26 questions [10 Kern/16 Workspace], 13 KI-Hebel".
   - Verifiziert: 1 Row `stb_modul_m04` v1.0; blocks=2; questions=26; ki_hebel=13; modul_key=m04; `output_contract.kinds`=[entscheidung, standard, implementierungsschritt] (mirror `modul_output.output_kind`).
5. Server-Temp (`/tmp/mig124.sql`, `/tmp/mig125.sql`) entfernt.

## 4. Files changed
- `/docs/MIGRATIONS.md` — MIG-124 + MIG-125 Heading `LIVE-Apply pending /deploy` → `applied 2026-06-22 /deploy SLC-170, RPT-514`.
- `/docs/STATE.md` — Current Focus/Phase/Next-Step → Migrationen live, NEXT /post-launch.

## 5. Files created
- `/reports/RPT-514.md` — dieser Deploy-Report.

## 6. Files intentionally not changed
- `/planning/roadmap.json` — V10 bleibt `active`. Migration-Apply ist keine Version-Freigabe (module-lifecycle-discipline: V10 nicht released bis Module komplett); kein `released`-Statuswechsel.
- `/planning/backlog.json` (BL-510 in_progress), `/features/INDEX.md` (FEAT-091 in_progress) — spannen SLC-170b (content-gated), korrekt unveraendert.
- `/slices/INDEX.md` — SLC-170 bereits `done` (post-merge `748dd8b`).
- `/docs/RELEASES.md` — kein REL-Eintrag: dies ist ein Intra-Version-Migration-Apply, keine Stack-/Version-Freigabe; Traceability via MIGRATIONS.md `applied` + dieser Report.
- `/docs/KNOWN_ISSUES.md` — keine Findings.

## 7. Problems found
Keine. Beide Migrationen additiv, idempotent, mit Pre-Apply-Backup; Live-State vor Apply sauber; CHECK-Supersets vorab verifiziert; App (V9.8) durch additive Schema-/Seed-Aenderung unberuehrt.

## 8. Open points / deferred
- `/post-launch` (T+0 jetzt, T+24h ~2026-06-23): DB-Objekte healthy, App V9.8 weiter erreichbar, 0 neue Errors — Light-Burn-In, da kein Code-Change.
- SLC-171 (Onboarding-Reuse + Env-Gate); SLC-173 Capture (nutzt `stb_modul_m04` + `rpc_enqueue_module_output`); SLC-174 Worker (schreibt `modul_output`). SLC-170b Blueprint/M-06/Rest = content-gated Founder-Autoring (BL-519/520).

## 9. Recommended next step
**`/post-launch` (T+0 Light-Verifikation)** — Container-Health + V9.8-Routing + 0 neue Errors; danach T+24h-Bestaetigung. Anschliessend SLC-171 oder Blueprint-Content-Autoring (entsperrt SLC-172).
