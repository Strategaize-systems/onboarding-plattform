---
id: RPT-510
date: "2026-06-21"
skill: /backend
slice: SLC-169
feature: FEAT-091
type: completion
status: completed
result: "MIG-124 modul_output-Domaene (Tabelle+RLS+RPC+Tier-Mapping+CHECK) geschrieben; tsc 0/eslint 0/build PASS; DB-Sidecar-Tests fuer /qa. R-169-1 live-verifiziert."
title: "/backend SLC-169 — MIG-124 StB modul_output-Schema + Enqueue-RPC + Tier/CHECK"
---

# /backend SLC-169 — MIG-124 StB Modul-Domaene-Schema

## Outcome
**Code-complete.** MIG-124 (`modul_output`-Tabelle + RLS + 3 Indizes + GRANTs + `rpc_enqueue_module_output` + `fn_min_tier_for_job`-Erweiterung + CHECK-Erweiterung + `NOTIFY pgrst`) geschrieben, plus zwei DB-Sidecar-Tests. Statische Verifikation: `tsc --noEmit` 0, `eslint` 0, `next build` PASS. Die DB-Sidecar-Tests laufen im `/qa` gegen die Coolify-DB (V9.8-Konvention: /backend = code-complete + statisch, /qa = DB-Sidecar). Beide Micro-Tasks (MT-1 Tabelle/RLS, MT-2 RPC/Tier/CHECK) liegen in der einen Migrationsdatei.

## R-169-1 (BLOCKING) — Live-CHECK-Stand verifiziert
`pg_get_constraintdef` gegen Coolify-DB (`supabase-db-bwkg80w04wgccos48gcws8cs`, 2026-06-21):
- `ai_jobs_job_type_check`: **22 LIVE-Werte** — live 3 mehr als die juengste Migrationsdatei (MIG-111: 19): `email_bulk_pipeline_trigger`, `email_bulk_retention_sweep`, `email_bulk_synthesis`. CHECK neu = 22 + `module_output_synthesis` = 23.
- `ai_cost_ledger_role_check`: **21 LIVE-Werte** (inkl. `email_bulk_synthesis`, `email_bulk_critic`). CHECK neu = 21 + `module_output_synthesis` + `module_output_critic` = 23.
- `ai_jobs_status_check`: **`pending|claimed|running|completed|failed|cancelled`** — `queued` ist KEIN gueltiger Wert.
- `modul_output` existiert nicht (frisch); `fn_min_tier_for_job` existiert (CREATE OR REPLACE).

Hätte ich die Werteliste aus der Migrationsdatei (statt live) genommen, wäre der CHECK ein Subset gewesen → drei Live-job_types hätten ihre Akzeptanz verloren. Genau die IMP-1228-Klasse.

## Deviation (Rule 1/3, dokumentiert als DEC-241)
Slice-AC-169-3 schrieb literal `status='queued'`. Das verletzt den Live-`ai_jobs_status_check` UND macht den Job für die claim-Loop (`rpc_claim_next_ai_job_for_type` claimt `status='pending'`, MIG-121) unsichtbar. → `status='pending'` verwendet (konsistent mit allen Dispatch-RPCs 032/047/074/073). Auto-Fix nach Deviation-Rule 1/3.

## Reuse-Anker (strategaize-pattern-reuse.md)
- RLS-Matrix (Zwei-Teil-USING `tenant_id = auth.user_tenant_id()` + `auth.user_role()`): MIG-022 (capture_session/block_checkpoint/knowledge_unit) + MIG-119 (email_synthesized_unit).
- Enqueue-RPC (SECURITY DEFINER, Auth/Ownership/Tier-Gate/session_tier-Stempel, idempotent): `rpc_create_block_checkpoint` (032) + `rpc_enqueue_recondense_job` (047) aus MIG-121.
- CHECK-Erweiterung (DROP IF EXISTS + ADD, Live-Superset): MIG-108/111/119/120.
- DB-Sidecar-Test (Self-Apply in gerollbackter Tx; `withJwtContext` + SAVEPOINT für erwartete RLS-Rejections): `migration-123-knowledge-unit-themes.test.ts` + `email-synthesized-unit-rls.test.ts` (coolify-test-setup.md).

## Files created
- `/sql/migrations/124_v10_stb_modul_domain.sql` — MIG-124. Teil 1: `CREATE TABLE modul_output` (Output-Triple + KI-Hebel, §4-DDL exakt; +`source`-CHECK als Daten-Integrität) + 3 Lookup-Indizes + RLS (admin_full / tenant_read / tenant_admin_update; kein tenant-INSERT = ai_draft nur service_role) + GRANTs (authenticated SELECT,UPDATE / service_role ALL). Teil 2: `fn_min_tier_for_job` +`module_output_synthesis`→`blueprint`; `ai_jobs.job_type`-CHECK (23) + `ai_cost_ledger.role`-CHECK (23) Live-Superset; `rpc_enqueue_module_output(p_capture_session_id, p_modul_key)` (tier-gated, Ownership-Pre-Check, idempotenter Re-Enqueue-Schutz, session_tier-Stempel, status='pending'); `NOTIFY pgrst`. Idempotent.
- `/src/lib/db/__tests__/migration-124-modul-output.test.ts` — Schema/Index/RLS-Pen-Test (Self-Apply 2x = 0 Drift; Spalten/CHECKs; 3 Indizes; RLS enabled; Tenant-Isolation SELECT/UPDATE; member-read; Fremd-Tenant-UPDATE 0 rows; tenant-INSERT denied).
- `/src/lib/db/__tests__/migration-124-enqueue-rpc.test.ts` — RPC + Tier-Mapping + CHECK-Akzeptanz (fn_min_tier_for_job='blueprint'; CHECK-Defs enthalten neue Werte; Enqueue blueprint→job pending+session_tier+payload; idempotenter Re-Enqueue=same job; free-Tier→tier_gate_denied; Fremd-Tenant→ownership-deny).

## Files changed (Records)
- `/docs/MIGRATIONS.md` — MIG-124 PLANNED → GESCHRIEBEN; R-169-1-Live-Counts + Deviation + Status ergänzt.
- `/docs/DECISIONS.md` — DEC-241 (status='pending' statt 'queued' + 2 Cost-Ledger-Rollen reserviert).
- `/docs/STATE.md` — High-Level State architecture→implementing; Current Focus/Phase auf SLC-169 /backend DONE, NEXT /qa; Next-IDs (DEC-242/RPT-511).
- `/slices/INDEX.md` — SLC-169 planned → in_progress.
- `/features/INDEX.md` — FEAT-091 planned → in_progress (Schema-Teil; Seed-Teil = SLC-170 offen).
- `/planning/backlog.json` — BL-510 open → in_progress.

## Files reviewed (nicht geändert)
- `/docs/ARCHITECTURE.md` §4/§9 (modul_output-DDL + MIG-Skizze) — Authority für DDL.
- `/sql/migrations/121_v975_tier_gating_foundation.sql`, `022_core_capture_rls.sql`, `108/111/119/120` — Reuse-Quellen.
- `/src/test/db.ts`, `/src/test/auth-context.ts`, `/src/test/fixtures/tenants.ts` — Sidecar-Test-Harness.

## Problems found
- **Spec-Inkonsistenz AC-169-3 (`status='queued'`)** vs. Live-CHECK + claim-Loop → korrigiert auf `'pending'` (DEC-241). Im /qa als gewünscht bestätigen.
- **DB-Sidecar-Tests noch nicht ausgeführt** (Konvention: laufen im /qa). Konfidenz für RLS/RPC/CHECK-Verhalten ist daher statisch (Self-Apply-Test-Code geschrieben, aber nicht live grün). Das ist der erwartete /backend→/qa-Übergang, kein verdeckter Blocker.
- `next build` benötigte Placeholder-`.env.local` (Impressum-ENV-Pflicht) — danach entfernt (gitignored, nur Platzhalter).

## Open points / deferred
- **/qa SLC-169:** DB-Sidecar-Run (node:20, Coolify-DB, SAVEPOINT) beider Test-Dateien; danach Pre-Merge-Re-Check + Master-Merge.
- **LIVE-Apply MIG-124 im /deploy VOR Worker-Code-Redeploy** (R-169-2).
- Cost-Ledger-Rollen `module_output_synthesis`/`module_output_critic` werden erst vom SLC-174-Worker geschrieben (hier nur reserviert).
- 6 Commits noch NICHT gemergt/gepusht (Worktree-Branch `v10-slc169-modul-schema`).

## Recommended next step
`/qa SLC-169` — DB-Sidecar-Tests gegen die Coolify-DB ausführen (Tenant-Isolation-Pen-Test + RPC-Tier-Gate + CHECK-Akzeptanz + 0-Drift-Idempotenz), dann Pre-Merge-Re-Check und Master-Merge.
