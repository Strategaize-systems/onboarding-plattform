---
id: RPT-497
date: "2026-06-19"
skill: /backend
slice: SLC-V9.8-B
feature: FEAT-088
type: completion
status: completed
reviewOf: null
result: "SLC-V9.8-B /backend CODE-COMPLETE — Controlled Tenant-Tag-Vokabular: on-the-fly Loader getTenantTagVocabulary (Top-60/Tenant aus knowledge_unit.themes, JS-agg, 0 RPC) + Synthese-Prompt-Injektion (existingTags-Block, leer→V9.5-Baseline byte-identisch) + Worker-Wiring (tagVocabularyLoader-DI, 1 Load/Run, graceful-degrade). tsc0/eslint0/hermetisch 46/46/next build EXIT0. DB-Sidecar Tenant-Isolation fuer /qa. 0 Migration."
title: "Backend SLC-V9.8-B — Controlled Tenant-Tag-Vokabular (Prompt-Injektion)"
---

# SLC-V9.8-B /backend — Controlled Tenant-Tag-Vokabular (Prompt-Injektion)

## Outcome summary
Code-complete. Alle drei Micro-Tasks umgesetzt: MT-1 (Vokabular-Loader), MT-2
(Prompt-Injektion), MT-3 (Synthesizer- + Worker-Wiring + Tests). Additiv, 0
Migration, 0 neue Deps/Jobs/Worker. Leeres Vokabular laesst den Synthese-Prompt
byte-identisch zur V9.5-Baseline (AC-B-2, 0 Regression). Worktree-Branch
`v9-8-tag-vokabular`, noch nicht gemergt. /qa pending — insbesondere die
DB-Sidecar Tenant-Isolation gegen die live Coolify-DB.

Status: **fully complete (Code-Side)**. Behavioral-Reuse (LLM reproduziert real
bestehende Tags statt Synonyme, SC-2/SC-3) ist nicht hermetisch beweisbar →
Live-Smoke/observational, R-B-3 als Confidence-Limit ausgewiesen.

## Akzeptanzkriterien (Code-Side)
- **AC-B-1 (PASS):** `getTenantTagVocabulary(adminClient, tenantId, cap=60)` liefert
  Top-N Tenant-Tags nach Haeufigkeit aus `knowledge_unit.themes`, strikt
  tenant-scoped (`.eq("tenant_id", tenantId)`), on-the-fly, keine neue Tabelle/RPC.
- **AC-B-2 (PASS):** `buildSynthesisUserPrompt` um optionalen `existingTags`-Block +
  Use-existing/only-add-if-novel-Regel erweitert. Leer → Prompt byte-identisch zur
  Baseline (Test: `withEmpty === withDefault`).
- **AC-B-3 (PASS):** `handle-synthesis-job.ts` holt das Vokabular nach run-Load
  (`run.tenant_id`) und reicht es durch `SectionSynthesizer` → `synthesizeSection`
  → `buildSynthesisUserPrompt`.
- **AC-B-4 (Code-Side PASS / Live im /qa):** Vokabular-Query strikt tenant-scoped
  (hermetisch: `.eq`-Call-Capture). Cross-Tenant-Leak-Pen-Test als DB-Sidecar
  (`tag-vocabulary-tenant-isolation-db.test.ts`) — laeuft im /qa gegen die live
  Coolify-DB (node:20-Sidecar).
- **AC-B-5 (PASS Code-Side):** Worker-Wiring-Test beweist, dass das Vokabular in den
  Synthesizer-Call fliesst. Dass das LLM real bestehende Tags reused, ist
  Live-Smoke (R-B-3).
- **AC-B-6 (PASS):** `tsc` 0, `eslint` 0, hermetische Vitest 46/46 GREEN,
  `next build` EXIT 0 (mit Placeholder-Env).

## Files reviewed
- `/slices/SLC-V9.8-B-controlled-tag-vokabular.md` — Slice-Spec (MTs, ACs, Risiken)
- `/src/lib/ai/bedrock-sonnet/email-synthesis-prompt.ts` — Prompt-Builder (Anker)
- `/src/lib/ai/bedrock-sonnet/email-synthesis.ts` — synthesizeSection (Anker)
- `/src/workers/bulk-email/handle-synthesis-job.ts` — Worker (Anker, run.tenant_id)
- `/src/lib/bulk-email/cost-cap.ts` — DI-/Adapter-Pattern (SupabaseClient-Typ)
- `/src/lib/supabase/admin.ts` — createAdminClient (Client-Typ)
- `/src/lib/bulk-email/handbook-import.ts` — SLC-A-Sibling (knowledge_unit-Spalten)
- `/src/lib/db/__tests__/migration-123-knowledge-unit-themes.test.ts` — withTestDb-Template
- `/src/test/fixtures/tenants.ts` — seedTestTenants (tenantA + tenantB)
- `/src/workers/bulk-email/__tests__/handle-synthesis-job.test.ts` — Worker-Test-Pattern

## Files created
- `/src/lib/bulk-email/tag-vocabulary.ts` — MT-1: `getTenantTagVocabulary`
  (on-the-fly Top-N aus knowledge_unit.themes, JS-Aggregation, freq desc /
  alpha-tie / cap, DEC-229/230). Default-Cap-Konstante `DEFAULT_TAG_VOCABULARY_CAP=60`.
- `/src/lib/bulk-email/__tests__/tag-vocabulary.test.ts` — MT-1: hermetische Vitest
  (9 Tests: Frequenz/Sort/Cap, leer→[], nur-leere-themes→[], Trim/Skip, tenant_id-Scope,
  DB-Fehler→throw, cap≤0→[]).
- `/src/lib/bulk-email/__tests__/tag-vocabulary-tenant-isolation-db.test.ts` — MT-1:
  DB-Sidecar Tenant-Isolation (AC-B-4 / SC-4). Self-apply Mig 123 in gerollbackter
  Tx + zwei-Tenant-Seed; beweist `WHERE tenant_id = $1` leakt keine Fremd-Tags.
  DB-gated (laeuft im /qa).
- `/src/lib/ai/bedrock-sonnet/__tests__/email-synthesis-prompt.test.ts` — MT-2:
  Prompt-Content-Test (4 Tests: Block+Regel bei Tags; byte-identisch zur Baseline
  bei []; Trim/Skip; nur-blank→Baseline).
- `/reports/RPT-497.md` — dieser Report.

## Files changed
- `/src/lib/ai/bedrock-sonnet/email-synthesis-prompt.ts` — MT-2: `buildSynthesisUserPrompt`
  Signatur um `existingTags: string[] = []` erweitert; bei nicht-leerem Vokabular
  kompakter `existingTags`-Block + Use-existing-where-fits-Regel injiziert. Leer →
  Block weggelassen (Baseline-identisch). Trim/Skip-Empty defensiv.
- `/src/lib/ai/bedrock-sonnet/email-synthesis.ts` — MT-3: `synthesizeSection` Signatur
  um `existingTags: string[] = []` (3. Positional, VOR `options?`) erweitert und an
  `buildSynthesisUserPrompt` durchgereicht. Bestehende 2-Arg-Caller unveraendert
  gueltig (Default []).
- `/src/workers/bulk-email/handle-synthesis-job.ts` — MT-3: `SectionSynthesizer`-Typ +
  `defaultSectionSynthesizer` um `existingTags` erweitert; neue optionale Dep
  `tagVocabularyLoader` (Default `getTenantTagVocabulary(adminClient, tenantId, 60)`);
  Vokabular-Load nach run-Load (Step 6b), genau 1 Load/Run, try/catch →
  graceful-degrade auf `[]` + captureWarning bei DB-Fehler; Vokabular an
  `synthesizer(section, inputPatterns, tagVocabulary)` durchgereicht.
- `/src/workers/bulk-email/__tests__/handle-synthesis-job.test.ts` — MT-3: +3 Tests
  (V1 Load-once + Pass-through, V2 graceful-degrade bei Loader-Error, V3 selbes
  Vokabular an jeden Section-Call / 1 Load/Run). Bestehende Synthesizer-Mocks bleiben
  signatur-kompatibel (zusaetzlicher Param ignoriert).
- `/slices/INDEX.md` — SLC-V9.8-B Status `planned` → `in_progress` (Code-Side fertig,
  /qa pending).
- `/docs/STATE.md` — Thread-A Current Focus + Immediate Next Steps: SLC-V9.8-B
  CODE-COMPLETE, naechster Schritt /qa SLC-V9.8-B.

## Files intentionally not changed
- `sql/migrations/*` — SLC-V9.8-B ist 0-Migration (DEC-229: on-the-fly-Aggregation,
  kein Aggregations-RPC, keine tenant_tag-Tabelle). Vokabular-Quelle ist die in
  SLC-V9.8-A erzeugte Spalte `knowledge_unit.themes` (Mig 123).
- `V95_SYNTHESIS_SYSTEM_PROMPT` — der System-Prompt bleibt unveraendert; die
  Vokabular-Steuerung sitzt im User-Prompt (kontextspezifisch pro Section/Tenant).
- `planning/backlog.json` BL-505 — bleibt `in_progress` (Umbrella fuer A+B; geht
  nach Gesamt-/qa + Merge auf done).
- `features/INDEX.md` FEAT-088 — bleibt `in_progress` (→ done nach /qa).

## Problems found
- Keine Code-Probleme. Zwei bewusste Designentscheidungen innerhalb des
  Architektur-Rahmens (kein neuer DEC noetig):
  1. **graceful-degrade auf leeres Vokabular bei DB-Fehler** im Worker — ein
     Vokabular-Load-Fehler darf den teuren Synthese-Run nicht failen; Soft-Enhancement,
     deckt sich mit AC-B-2 (leer→Baseline). Sichtbar via captureWarning.
  2. **`tagVocabularyLoader`-DI im Worker** (statt direktem Inline-Call) — haelt die
     bestehende Worker-Test-Suite hermetisch (kein knowledge_unit-Mock noetig) und
     spiegelt das etablierte DI-Pattern (synthesizer/critic/costStore).
- **Verifikations-Limit (R-B-3, ausgewiesen):** Behavioral-Reuse (LLM nutzt real
  bestehende Tags statt Synonyme) ist nicht hermetisch beweisbar. Hermetik prueft
  Prompt-Inhalt; SC-2/SC-3 sind Live-Smoke.
- **Row-Bound (R-B-2, dokumentiert):** Loader aggregiert JS-seitig; die PostgREST-
  Default-Row-Obergrenze ist fuer Top-60 aus dem bounded per-Tenant-Bestand
  unkritisch. Falls je gross: RPC/Materialisierung = V9.8+-Kandidat.

## Open points / deferred decisions
- **/qa SLC-V9.8-B**: muss die DB-Sidecar Tenant-Isolation gegen die live
  Coolify-DB fahren (node:20-Sidecar; 2 Test-Files explizit targeten — IMP-1299).
- **Live-Apply Mig 123 = /deploy** (R-A-1, SLC-A): die Vokabular-Quelle existiert
  in Prod erst nach dem Mig-123-Apply. Bis dahin liefert der Loader in Prod `[]`
  (graceful → Baseline-Prompt) — kein Fehler.
- Out of Scope (DEC-231/232): Injektion in den Extraktions-Prompt, Embedding-
  Normalisierung, Tag-Verwaltungs-UI, retroaktives Re-Tagging.

## Project record updates
- `/slices/INDEX.md` — SLC-V9.8-B → `in_progress`.
- `/docs/STATE.md` — Current Focus + Immediate Next Steps aktualisiert.
- `/reports/RPT-497.md` — dieser Report.
- BL-505 / FEAT-088 bewusst noch nicht auf `done` (→ nach /qa + Gesamt-/qa + Merge).

## Recommended next step
`/qa SLC-V9.8-B` — Code-Side-Re-Check + DB-Sidecar Tenant-Isolation (AC-B-4) gegen
die live Coolify-DB. Danach Gesamt-/qa V9.8 (A+B) → /final-check → /go-live →
EIN Master-Merge → /deploy (Mig 123 live VOR Code-Redeploy, R-A-1) → /post-launch.
