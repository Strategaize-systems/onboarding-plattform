---
id: RPT-647
date: "2026-07-10"
skill: /deploy
slice: null
feature: FEAT-110
type: completion
status: completed
result: "V20 Security-Hardening DEPLOYED zu Production (REL-045, SOURCE_COMMIT 04dd283). Merge v20-security-hardening->main (--no-ff, 25 Commits), MIG-133+134 live-applied+idempotenz-verifiziert (tier_default='free', 3 Guards, 48 search_path-swept), Coolify-Redeploy autonom (temp-Token, cleanup DELETE 1), Delivery-Chain PASS (Container-Commit==main-HEAD), 4/4 Container healthy. Live-Smoke: /login 200, fail-closed 307, CSP+COOP+nosniff+X-Frame live, error_log 0 error, app-logs clean. Auth-gated Funktional-Smokes (evidence-mapped-partner=200, XSS-Render, SVG-Reject, Login-Lockout) + CSP-enforce-Flip = Founder-Selbsttest. Last-Stable bleibt V10.4 bis /post-launch."
title: "V20 Security-Hardening DEPLOYED (REL-045, SOURCE_COMMIT 04dd283)"
---

# /deploy — V20 Security-Hardening → Production

## Outcome
V20 (SLC-193/194/195 + /code-review-Fixes) live auf Production (`onboarding.strategaizetransition.com`, Hetzner/Coolify). Full-Workflow geschlossen: /qa → Gesamt-/qa → /final-check → /go-live → /code-review → /deploy. **Last-Stable bleibt V10.4** bis /post-launch T+24h.

## Pre-Deploy
- **Pre-Deploy-Server-Precheck PASS:** host coolify-ubuntu 159.69.207.29 (korrekt), Disk 17% (74/451G), MemAvail ~20GB, Swap 0 (Headroom ok). 4/4 Container healthy (Up 25h) vor Deploy.
- **Pre-Merge-Re-Check PASS:** origin/main `942cebe` = Branch-Base (0 Commits zu rebasen); MIG-133/134 kollisionsfrei (main@132); Tests grün (618 env-gated/2355 pass = 0 neue Failures); Diff-Review = 65 V20-Files + 5 Review-Commits, Scope bestätigt. Live-Commit `8589b20` (V10.5) ist Ancestor von `942cebe` (8 dazwischen = nur docs/records, 0 Code).

## Gate 1 — Migration-Deploy
- **MIG-133 + MIG-134 live-applied** gegen supabase-db-...-145427518712, idempotenz-verifiziert (Re-Apply exit 0, 0 ERROR).
- Verify: `capture_session.tier` DEFAULT `handbook`→**`free`**; Trigger `capture_session_tier_change_guard` + `ai_jobs_session_tier_insert_guard` + **`profiles_role_change_guard`** (neu) present; **48** public-DEFINER-Functions mit gepinntem `search_path` (SEC-001-Sweep).
- **Post-Recreate re-verifiziert** (Compose recreate = neue DB-Container-Instanz, Volume persistent): tier_default='free', 3 Guards, 48 swept — persistiert.
- **PostgREST reload** (`NOTIFY pgrst`) nach Apply UND nach Stack-Recreate.
- ⚠️ **Ablauf-Abweichung (transparent):** der geplante In-tx-Apply-Probe (`BEGIN…ROLLBACK`) hat die Migrationen **real appliziert statt zurückgerollt**, weil MIG-133/134 je einen **eigenen `BEGIN;…COMMIT;`** enthalten — der innere COMMIT schloss die Probe-Transaktion. Apply war fehlerfrei + idempotent; Netto-Effekt = die ohnehin geplante Live-Apply, nur früher im Ablauf. Learning → IMP + Playbook (unten). Kein Datenverlust, keine Fehlanwendung.

## Gate 2 — ENV
- Keine neue/geänderte Pflicht-ENV in V20 (JIBRI_RECORDINGS_DIR nur für die middleware-unerreichbare recording-ready-Route, fail-closed ohne ENV). Presence-Gate N/A.

## Gate 3 — Delivery-Chain-Verify PASS
- `git log origin/main..HEAD` vor Merge: 25 Commits (erwartet). Merge `04dd283` → push `942cebe..04dd283`.
- **Auto-Deploy NICHT konfiguriert** (Push triggerte keinen Build) → autonomer Redeploy via temp-API-Token (coolify-autonomous-redeploy.md): Resource-UUID `bwkg80w04wgccos48gcws8cs`, deployment_uuid `lkwwg44oko804swwwwos04sg`, Status via `application_deployment_queues` gepollt bis `finished`.
- **Container-Commit == main-HEAD:** `docker exec app printenv SOURCE_COMMIT` = **`04dd283`** ✓.
- **PFLICHT-Cleanup:** `DELETE FROM personal_access_tokens WHERE name='temp-agent-deploy-v20'` = **DELETE 1**; `is_api_enabled` blieb `t` (war vorher an — nicht verändert).

## Live-Smoke (autonom)
| Check | Ergebnis |
|---|---|
| /login (ext FQDN) | **200** |
| /dashboard unauth | **307** (fail-closed redirect, nicht 404/500) |
| /api/capture/x/evidence/list unauth | **307** (fail-closed, nicht 500/404) |
| CSP Report-Only Header | **live** (default-src 'self'; script/frame-src +Jitsi; frame-ancestors 'none'; object-src 'none'; base-uri/form-action 'self') |
| COOP / X-Content-Type / X-Frame | **same-origin-allow-popups / nosniff / DENY** live |
| error_log (10 min) | **info=4, 0 error/warn/fatal** |
| app container logs | **0 error/fatal** |
| Container health | **4/4 healthy** (app/worker/kong/db) |

Tier-Guard-Behavioral-SQL-Probe: schlug am RLS-INSERT-Policy-Gate ab (Probe ohne JWT-Claims) — die Trigger-Semantik ist bereits 9/9 CHK in RPT-638 (In-tx-Probe mit Claims) bewiesen + Objekte hier verifiziert; kein Defekt.

## Deferred an Founder-Selbsttest (Internal-Test-Mode, [[test_first_sequence_before_live]])
Auth-gated Funktional-Smokes gegen den deployten Stand `04dd283`:
1. **evidence list+download: partner-admin eines GEMAPPTEN Client-Tenants → 200** (Fix #1 RLS-Gate; fremder Tenant → 404).
2. **App-Session-Creation** StB-Blueprint/Modul (tier-Auto-Grant → 'blueprint', nicht 'free'-gated).
3. **Handbook `<iframe srcdoc>`-XSS rendert nicht aus** · **SVG-Upload-Reject** + Logo nosniff.
4. **Login-Lockout** (5 Fehlversuche/account, IP-Rotation wirkungslos).
5. **CSP-Funktional-Browser-Smoke** (`tests/_probe/csp-check.mjs`, 0 Eigen-Asset-Violations über Login/Dashboard/Handbook/Diagnose/Jitsi) → **enforce-Flip-Entscheid** (Header-Key → `Content-Security-Policy`, ISSUE-127-Closure). CSP bleibt bis dahin **Report-Only** (non-breaking).

## Rollback-Awareness
Fallback-Ziel: vorheriger SOURCE_COMMIT **`8589b20`** (V10.5). MIG-133/134 sind additive Guards/DEFAULT/search_path (nicht destruktiv); ein Code-Rollback auf 8589b20 liefe gegen die neue DB-DEFAULT='free' → würde StB-Sessions stranden (altes Code elevated nicht) → bei Rollback zusätzlich `ALTER TABLE capture_session ALTER COLUMN tier SET DEFAULT 'handbook'` + Guards droppen. Detail via /rollback falls nötig. In Internal-Test-Mode (0 Kunden) unkritisch.

## Records
REL-045 (RELEASES.md), MIG-133/134 applied (MIGRATIONS.md), roadmap v20→released, SLC-193/194/195→deployed, FEAT-110/111/112→deployed, BL-537/538/539→done, ISSUE-124/125/129→resolved (live-applied), ISSUE-127 bleibt open (Report-Only bis enforce-Flip). STATE High-Level→deploying, Last-Stable=V10.4 (bis /post-launch).

## Next
**`/post-launch` V20** — T+3h/T+24h Burn-In gegen `04dd283`: Container-Health/RestartCount, error_log 0 error/fatal, Cron-Loop alive (7 Routen via requireCronSecret), ai_jobs 0 failed. Bei T+24h-PASS: Last-Stable-Flip V10.4 → V20. Parallel Founder-Selbsttest (Punkte 1-5) + enforce-Flip-Entscheid.
