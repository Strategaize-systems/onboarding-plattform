---
id: RPT-543
date: "2026-07-02"
skill: /discovery
slice: null
feature: null
type: completion
status: completed
reviewOf: null
result: "/module-delivery Scoring-/Interview-Engine — Scope-Baseline festgelegt (DEC-252), ready for /requirements"
title: "Discovery /module-delivery Scoring-/Interview-Engine (StB-Vertikale)"
---

# Discovery — /module-delivery Scoring-/Interview-Engine

## Kontext
Nach Merge von SLC-170b (MIG-128 Fachmodul-Seed, DEC-251) ist die neue Delivery-Schicht der nächste Fokus. DEC-251 legte fest: Scoring-Flags + adaptive Rückfragen leben NICHT in den M-04-treuen Fachmodulen, sondern in einer neuen `/module-delivery`-Schicht.

## Faktische Codebasis-Kartierung (Explore-Agent)
- **Blueprint-Diagnostik FERTIG:** Capture A–G → LLM-Assessment → Ampel je Subtopic → deterministisches Modul-Routing (`src/workers/diagnosis/*`, `src/lib/stb-vertikale/blueprint-*.ts`, MIG-126/127).
- **Modul-Synthese FERTIG:** Capture-Antworten → Bedrock-Sonnet Draft+Critic → `modul_output` (entscheidung/standard/implementierungsschritt/ki_hebel) + Reifegrad 1–4 (`src/workers/stb-vertikale/handle-module-output-job.ts`, MIG-124).
- **Scoring-Flags:** `owner_dependency`/`deal_blocker`/`sop_trigger`/`ko_hart`/`ko_soft` im `TemplateQuestionSchema` (`src/lib/db/template-queries.ts`) DEFINIERT, aber überall `false` geseedet und **nirgends im Runtime ausgewertet**.
- **Reifegrad:** bereits deterministisch aus `ki_hebel`-Katalog — /module-delivery erfindet hier nichts neu.
- **Capture-Modes:** questionnaire (aktiv), dialogue (aktiv, Meeting), evidence (teil-implementiert).
- **SOP-Pipeline:** Legacy (`src/workers/sop/*`), entkoppelt von `modul_output` — Brücke existiert nicht.

## Lücke (was /module-delivery neu bauen muss)
1. Scoring-Flag-Auswertung (existiert nicht).
2. Adaptive per-Frage-Rückfragen (nur block-level im Blueprint, nicht per-Modul/per-Frage).
3. Modul-Reife-/Ampel-Signal aus aggregierten Antwort-Signalen.
4. SOP-/Handbuch-Brücke (`modul_output` + Scoring → SOP-Sektionen).

## Founder-Entscheide (→ DEC-252)
- **Timing:** Echtzeit im Interview (adaptive Rückfragen live).
- **Artefakt:** Beides — Claude-Code-Skill (Autoring/Setup) + OP-Runtime-Feature (Delivery).
- **Scoring-Mechanik (beide Ebenen von Anfang an):** Ebene 1 = LLM-Autoring-Lauf setzt Flags automatisch an den 17 Modulen (Founder nimmt ab, kein Handsetzen, neue Seed-Migration, deterministisch); Ebene 2 = Live-LLM bewertet Antwort und triggert Echtzeit-Rückfragen. LLM über Bedrock Frankfurt.
- **Requirements-Umfang:** ganze Engine (Phase 1 Auswertung + Reife-Ampel, Phase 2 adaptive Rückfragen, Phase 3 SOP-Brücke).

## Phasen-Cut
- Phase 1: Scoring-Auswertung + Modul-Reife-Ampel (inkl. LLM-Flag-Autoring-Lauf + Seed).
- Phase 2: Echtzeit-adaptive Rückfragen (Live-LLM-Antwort-Urteil im Capture-Pfad).
- Phase 3: SOP-/Handbuch-Brücke (berührt Legacy-SOP-Pipeline).
- Parked: Cross-Modul-Personalisierung, Re-Interview-Loops, evidence-Mode-Ausbau.

## Readiness
**Ready for /requirements.** Alle gatenden Forks entschieden (DEC-252). Version-Slot (v11 geplant) wird in /requirements zugewiesen.

## Recommended next step
`/requirements` — ganze Engine (Phase 1+2+3), Version-Slot-Zuweisung, Abgrenzung zu Blueprint-Diagnostik + Modul-Synthese, LLM-Data-Residency (Bedrock Frankfurt) als Constraint.
