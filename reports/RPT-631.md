---
id: RPT-631
date: "2026-07-09"
skill: /go-live
slice: null
feature: FEAT-108, FEAT-109
type: completion
status: completed
reviewOf: null
result: "V10.5 = CONDITIONAL GO. Keine Blocker; /final-check READY (RPT-630), Gesamt-QA PASS (RPT-629). 6 Deploy-Gates formuliert (Merge, 0-Regression, Delivery-Chain, Live-Smoke R-1, Content-Real-Session R-2, 0-Migration). Interne Workflow-Closure, kein Customer-Go-Live."
title: "Go-Live-Entscheidung V10.5 — Exit-Ready Produkt 1"
---

# Go-Live V10.5 — Exit-Ready Produkt 1 (Exit-/Devil's-Advocate-Report)

## 1. Release-Target
V10.5 Exit-/Devil's-Advocate-Report (SLC-191 + SLC-192, FEAT-108 + FEAT-109) → Coolify/Hetzner Production (OP kein Auto-Deploy → manueller Redeploy nach main-Merge). 0 Migration, 0 LLM. Interne Workflow-Closure, **kein** Customer-Go-Live ([[module-lifecycle-discipline]]).

## 2. Entry-Conditions (erfüllt)
- Gesamt-/qa V10.5 PASS — RPT-629.
- /final-check-Report vorhanden, Verdict **READY** (conditionally) — RPT-630.
- Keine /final-check-Blocker.
- Pre-Merge-Re-Check ausgeführt (Rebase auf origin/main, Tests post-rebase 43/43, RPT-Kollision aufgelöst, Diff-Review sauber). Branch merge-ready, **noch nicht gemerged** → Merge = erstes Deploy-Gate.

## 3. Readiness-Entscheidung: **CONDITIONAL GO**
Keine Blocker, keine offenen High/Medium. Go unter der Bedingung, dass die 6 Deploy-Gates (§6) beim `/deploy` literal verifiziert werden — insbesondere die zwei PENDING-LIVE-Smokes (nur post-deploy prüfbar).

## 4. Blockers
Keine.

## 5. Accepted Risks
- **R-1 (Deploy-Smoke):** Route-HTTP-Verhalten (Auth/Tier/Berater-Scope/PDF) erst post-deploy verifizierbar.
- **R-2 (Deploy-Content):** Erst-Render gegen eine REALE `capture_session` steht aus (bisher synthetische Fixtures zur gegroundeten Live-Schema-Form; MT-0 fand keine reale Session).
- **R-3 (vor Customer-Live, NICHT go-live-blockend):** `/security-audit` empfohlen (Route nutzt `createAdminClient`; 1:1-Reuse der bereits deployten Fahrplan-Route). Kein Customer-Live jetzt.

## 6. Deploy-Gates (an /deploy übergeben — literal verifizierbar)
1. **Merge-Gate:** Rebasten Branch `v10-5-exit-report` nach `main` mergen (Pre-Merge-Re-Check bereits PASS) + pushen; Redeploy triggern.
2. **0-Regression-Gate (0 MIG):** 0 neue fehlschlagende Tests in der geänderten Surface — exit-report-Modul **43/43** (Baseline: geändert wurden nur additive exit-report-Files). Der Full-Suite-Rest (608 Fails) ist **pre-existing TEST_DATABASE_URL-Env-Gate, identisch auf mainline** — **KEIN** Deploy-Blocker, NICHT als „volle Prod-DB-Suite grün" fehlinterpretieren. DB-Suite = reine Regressions-Sanity auf unveränderter Schema-Surface (0 Migration).
3. **Delivery-Chain-Gate:** Nach Redeploy `SOURCE_COMMIT` des Containers == gemergter main-HEAD (Push→Trigger→Container verifizieren).
4. **Live-Smoke-Gate (R-1):** `GET /admin/debrief/[sessionId]/exit-report` gegen den deployten Container: 401 unauth · 403 fremd-Tenant/free-Tier · 403 berater-ohne-Zuweisung · 200 `application/pdf` (admin + berater-mit-Zuweisung).
5. **Content-Gate (R-2):** 200-PDF gegen eine REALE blueprint-Session (owner_dependency-Antworten + diagnosis_schema) → pdftotext-Extract zeigt Owner-Dependence-Index + Scorecard + Spur/Disclaimer + Coverage. Existiert keine reale Session → gegen die erste real geseedete Session nachholen; synthetic-only-Limitierung dokumentieren.
6. **Migration-Gate:** **keine** — 0 Migration, explizit nichts zu applyen.

## 7. Operational
- OP kein Auto-Deploy → Deploy = bewusster manueller Coolify-Trigger (Founder-Go, Production-Redeploy).
- Rollback-Pfad: vorheriger `SOURCE_COMMIT` (V10.4 stable, `cb72df1`-Klasse) — additive Änderung, 0 Migration → Rollback risikoarm.

## 8. Next workflow step
**`/deploy`** V10.5 (Merge nach main + Coolify-Redeploy + Gates 1–6). Danach `/post-launch`. Founder-Entscheid für den Production-Redeploy erforderlich (outward-facing).
