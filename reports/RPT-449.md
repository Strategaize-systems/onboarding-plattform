---
id: RPT-449
date: "2026-06-11"
skill: /go-live
slice: SLC-V9.1-A..D
feature: FEAT-075..079
type: completion
status: completed
reviewOf: null
result: "Go-Live V9.1 CONDITIONAL GO — Code release-ready (0 Blocker/0 High/0 Medium auf Basis RPT-447 Gesamt-/qa PASS + RPT-448 Final-Check). GO-Bedingungen sind ausschliesslich Founder-Deploy-Preconditions (OQ-R1-1 IONOS-Postfach + IMAP-ENVs + 3 Coolify-Scheduled-Tasks, D4 Wording) — keine Code-Defekte. Master-Merge + Live-Smokes + REL-031 gehoeren in /deploy."
title: "Go-Live V9.1 — Forward-Bucket-Email GO/NO-GO Decision"
---

# Go-Live V9.1 — Forward-Bucket-Email (SLC-V9.1-A..D)

## 1. Release target considered

- **Version:** V9.1 Forward-Bucket-Email (FEAT-075..079, SLC-V9.1-A..D)
- **Branch:** `v9-1-forward-bucket-email` HEAD `6733536` (origin synchron, NICHT in `main` gemergt)
- **Target environment:** OP Coolify-Stack `159.69.207.29` (Self-hosted Supabase + Next.js standalone), Internal-Test-Mode (Founder-only)
- **Delivery Mode:** SaaS — aber V9.1 strikt Internal-Test-Mode, KEIN Customer-Outreach (module-lifecycle-discipline + IMP-950)

## 2. Readiness decision: **CONDITIONAL GO**

Der Code ist release-ready. Auf Basis von **Gesamt-/qa PASS-WITH-LOW (RPT-447)** und **Final-Check CONDITIONALLY READY (RPT-448)** gilt: **0 Blocker / 0 High / 0 Medium**. Die einzigen offenen Punkte sind **Founder-Deploy-Preconditions** (Infrastruktur-Setup zur Deploy-Zeit), keine Code-Defekte. Daher CONDITIONAL GO — die Bedingungen werden im `/deploy`-Schritt erfuellt, nicht durch Code-Aenderungen.

## 3. Blockers

**Keine.** 0 Blocker / 0 High / 0 Medium ueber beide vorgelagerten Audits.

Cross-Slice-Wiring ist drift-frei (RPT-447): Status-Enum-Vertrag A→B (`continuous`/`forward_bucket` Writer↔Reader), Audit uniform `error_log` (kein `audit_log`), Cron-Auth uniform `x-cron-secret` (503/403/200/500), Shared-Helper real verdrahtet, Schema-Spalten run-level (`soft_delete_at`, cost-view `month`). RLS auf allen 4 neuen Tabellen aktiv (MIG-112/116). tsc=0, V9.1-targeted Vitest 291 pass/4 skip, V9.1-Source ESLint+Stub clean.

## 4. GO-Conditions (Founder-Deploy-Preconditions — BLOCKING fuer produktive Funktion, gehoeren in /deploy)

Ohne diese laeuft die Inbound-Pipeline nicht (kein Code-Risiko — reines Infra-Setup):

| # | Condition | Detail |
|---|---|---|
| **C1 (OQ-R1-1)** | IONOS-Postfach + IMAP-ENVs | Postfach `bulk@strategaizetransition.com` anlegen; `IMAP_HOST=imap.ionos.de`, `IMAP_PORT=993`, `IMAP_USER`, `IMAP_PASSWORD`, `INBOUND_DEFAULT_ENDPOINT_SLUG` in Coolify setzen |
| **C2** | 3 Coolify-Scheduled-Tasks | `/api/cron/inbound-email-imap-sync` (≥5 min), `/api/cron/email-bulk-pipeline-trigger` (stuendlich), `/api/cron/bulk-email-retention-sweep` (taeglich 02:00 UTC) — jeweils mit `x-cron-secret` |
| **C3 (D4)** | Conversational-First-Wording-Freigabe | Setup-UI-Assistant-Texte Founder-Review (~1h) |

Diese Conditions sind **Founder-Aktionen**. Sie werden im `/deploy`-Schritt zusammen mit Master-Merge + Coolify-Redeploy + Live-Smokes ausgefuehrt.

## 5. Accepted risks (dokumentiert, kein Release-Blocker)

- **npm-audit (prod): 1 low / 3 moderate / 2 high — ALLE pre-existing framework/SDK-transitiv, KEIN V9.1-Intro.** `next` (HIGH), `fast-xml-builder` (HIGH, via AWS-SDK/Bedrock), `ws` (via `@supabase/supabase-js → realtime-js`), `esbuild`/`postcss`/`next-intl`/`brace-expansion` (MODERATE). Alle bereits auf deployed `main`. Einzige neue Prod-Dep `imapflow@^1.3.1` (MIT) fuegt 0 Vulns hinzu. CRITICAL-Findings (`vitest`/`@vitest/coverage-v8`) sind dev-only, nicht shippable. → eigenes Cross-Repo-Dependency-Maintenance-Window, kein V9.1-Blocker.
- **ISSUE-096 (Low) — RLS-Behavioral-Coverage-Asymmetrie.** Voller 4-Rollen-Pen-Test nur fuer `email_inbound_sync_state`; die 3 Foundation-Tabellen (`email_inbound_endpoint`, `email_forward_allowlist`, `email_validation_reject_log`) nur Policy-Existenz-verifiziert. Policies existieren + korrekt typisiert + identisches V9-Standard-Pattern. **Vor Customer-Live** (Modul 1+2+3 komplett) zu schliessen; im Internal-Test-Mode (Founder-only, keine Kundendaten) akzeptiert.
- **2 pre-existing ESLint-Issues** (`text-overrides/page.tsx:263` Error + `mandanten-report/cover.tsx:190` Warning) — NICHT im V9.1-Diff, build-proven non-blocking auf deployed `main`. Cleanup-Empfehlung.
- **ISSUE-095 (Low) — Retention-Idempotency** haengt an `knowledge_unit.metadata->>'bulk_run_id'`. Mitigiert durch V4-Foundation-metadata-Default; Belt-and-Suspenders V9.2.

## 6. Operational concerns

- **Self-hosted-Readiness:** Migrationen **MIG-057..063 (112-118) alle LIVE** auf Coolify-Postgres. `.env.deploy.example` auf IMAP umgestellt (SES/HMAC-ENVs entfernt), `CRON_SECRET` vorhanden. `next build` proven (deployed main + RPT-444 COMPILE PASS). Standalone-Output + interne Supabase-Kong-Rewrites korrekt.
- **EU-Data-Residency konform:** IONOS-IMAP (Deutschland) + Bedrock eu-central-1 (Frankfurt), KEIN Cross-Region (DEC-205; DEC-196 superseded). Erfuellt `data-residency.md`.
- **Observability:** Audit-Trail durchgaengig `error_log` (captureInfo/captureException) inkl. Cron-Run-Stats + Retention-Sweep-Counts + DSGVO-Consent-Events. 3-Schichten-Cost-Cap (Daily 5€ / Monthly 100€ / Per-Email 0.50€) + GF-Notification + Pipeline-Pause.
- **Rollback-Awareness:** Master-Merge erfolgt im /deploy `--no-ff` → Revert via Merge-Commit. V9.1-Migrationen sind additiv + LIVE (kein DB-Rollback noetig; alte App-Version ignoriert die neuen Tabellen). Coolify-Image-Revert auf Vorgaenger-Commit.
- **Live-Smokes deferred /deploy:** IONOS-End-to-End (AC-R1-8) + Cron-Live-Smokes (AC-V9.1-B-9 / C-9 / D-6) sind founder-gated (C1/C2) und gehoeren naturgemaess in die Deploy-Phase.

## 7. Recommended next step

**`/deploy V9.1`** — Master-Merge `v9-1-forward-bucket-email → main --no-ff` (nach Pre-Merge-Re-Check) + Coolify-Redeploy + Founder-Infra-Setup (C1/C2/C3) + Live-Smokes + REL-031 + Records-Flip auf `done`/`released`. Danach `/post-launch V9.1` (T+24h STABLE-Bestaetigung).

**Founder-Input erforderlich vor /deploy:** C1 (IONOS-Postfach + IMAP-ENVs), C2 (3 Coolify-Scheduled-Tasks), C3 (D4 Wording-Freigabe). Solange die IONOS-Infrastruktur nicht bereitsteht, kann `/deploy` zwar Code mergen + redeployen, aber die Inbound-Pipeline bleibt funktional inert.
