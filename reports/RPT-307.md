---
id: RPT-307
date: "2026-05-19"
skill: /backend
slice: SLC-135
feature: FEAT-053
type: completion
status: completed
result: "V7 /backend SLC-135 CODE-SIDE DONE. 6/6 MTs durch (4 Agent + 2 User-Pflicht-Aktion-Templates). 5/5 neue Vitest PASS gegen Coolify-DB. AC-10a NOTIFY pgrst 6x Treffer. Quality-Gates: tsc EXIT=0 / ESLint 0/0 / Turbopack-Build PASS / Vitest 1150 PASS / 0 SLC-135-Regression. Pre-existing Webpack-route-export-Bug + 2 diagnose-Mock-Failures dokumentiert (auf main HEAD identisch reproduziert)."
title: "V7 /backend SLC-135 Code-Side Completion — TTL-Cleanup-Cron + Final-Hardening + Live-Smoke-Plan-Doku"
---

# RPT-307 — V7 /backend SLC-135 Code-Side Completion

## Outcome Summary

V7 SLC-135 (TTL-Cleanup-Cron + Final-Hardening + Live-Smoke-Plan) **code-side DONE** im Worktree `slc-135-ttl-cleanup-cron-final-hardening` (HEAD `e337247`, 2 Commits pushed). Alle 6 Micro-Tasks durch:

- **MT-1** Cleanup-Endpoint `/api/cron/pending-signup-cleanup` + 5 Vitest gegen Coolify-DB ✅
- **MT-2** `docs/V7_LIVE_SMOKE_PLAN.md` NEU (409 Zeilen, 13-Schritt-Plan inkl. Migration-Apply Standard-Procedure mit 6× `NOTIFY pgrst`-Mentions) ✅
- **MT-3** Coolify-Scheduled-Task Setup-Tabelle + User-Bestaetigungs-Template (in `V7_LIVE_SMOKE_PLAN.md`) — Agent-Doku-Side ✅, User-Pflicht-Aktion **pending**
- **MT-4** `.env.deploy.example` V7-Block + ENV-Setup-Doku + Service-Key-Wert in V7_LIVE_SMOKE_PLAN.md (Wert `b48c1d55...0c090` per `feedback_env_value_not_command`) ✅
- **MT-5** IONOS-Postfach-Check-Doku + User-Bestaetigungs-Template ✅, User-Pflicht-Aktion **pending**
- **MT-6** Quality-Gates + Records (slices/INDEX, backlog.json, STATE.md, RPT-307) ✅

**Status:** vollstaendig code-side komplett, 2 User-Pflicht-Aktionen (MT-3 Cron-Setup + MT-5 IONOS-Postfach) bewusst aus V7-Code-Block ausgelagert (auf Coolify-UI + IONOS-Webadmin angewiesen).

## Files Reviewed

- `/slices/SLC-135-ttl-cleanup-cron-final-hardening-live-smoke.md` — Slice-Spec (post-SLC-135-Spec-Update via QA-F2-Fix)
- `/src/app/api/cron/capture-reminders/route.ts` — V4.2 Pattern-Reuse-Vorlage (POST + inline CRON_SECRET-Check)
- `/src/app/api/cron/walkthrough-cleanup/route.ts` — V5 Pattern-Reuse-Vorlage (Multi-Pfad-Cleanup + capture-Logger)
- `/src/app/api/cron/walkthrough-cleanup/__tests__/route.test.ts` — Test-Mock-Pattern (NICHT angewandt, weil SLC-135-Tests reale Coolify-DB nutzen)
- `/src/app/api/public/signup/__tests__/pen-test.test.ts` — Coolify-DB-Test-Pattern (Pattern uebernommen)
- `/src/test/v7-signup-fixture.ts` — Test-Helper (`openTestDbClient`, `setupTestPartner`, `cleanupAllPentestArtifacts`, `fetchAuditLogRows`)
- `/src/lib/logger.ts` — captureInfo schreibt error_log mit level/source/metadata
- `/src/lib/supabase/admin.ts` — createAdminClient mit X-Forwarded-Host
- `/sql/migrations/098_v7_pending_signup_and_mapping_source.sql` — Schema-Reference (Spalten + Indices)
- `/.env.deploy.example` — Pattern-Reference fuer V7-ENV-Block

## Files Created

1. **`/src/app/api/cron/pending-signup-cleanup/route.ts`** — GET-Handler mit Auth (`x-cron-secret`-Header-Check, 503 ohne ENV / 403 bei Mismatch) + Schritt 1 (pending → expired UPDATE via Filter `status='pending' AND expires_at < now()`) + Schritt 2 (expired DELETE via Filter `status='expired' AND verified_at IS NULL AND created_at < now() - 7d`) + Audit-Log via `captureInfo` (`source='cron:pending-signup-cleanup'`, `metadata.category='pending_signup_cleanup'`, `expired_count` + `deleted_count`). Pattern-Reuse-Header-Kommentar referenziert V4.2 capture-reminders + V5 walkthrough-cleanup + DEC-059 + DEC-131 + FEAT-053.

2. **`/src/app/api/cron/pending-signup-cleanup/__tests__/route.test.ts`** — 5 Vitest gegen Coolify-DB:
   - **Case 1** ohne CRON_SECRET-Header → 403
   - **Case 2** Pending mit `expires_at < now()` → status=`expired`, count ≥ 1
   - **Case 3** Expired + verified_at NULL + created_at > 7d → DELETE, alte Row weg, neuere Row bleibt (Differentiator-Probe)
   - **Case 4** Idempotent: zweiter Aufruf ohne neue Daten → counts = 0
   - **Case 5** Audit-Log `error_log` Eintrag mit `category='pending_signup_cleanup'` + `level='info'` + `source='cron:pending-signup-cleanup'`
   Setup nutzt `setupTestPartner` aus V7-Fixture, eigener `insertPendingSignup`-Helper fuer custom `created_at`-Override (Helper im Fixture-File unterstuetzt nur ttlHours, kein created_at).

3. **`/docs/V7_LIVE_SMOKE_PLAN.md`** — 409 Zeilen:
   - **Pre-Conditions** Tabelle (8 Items)
   - **Migration-Apply Standard-Procedure** mit Base64-Pattern + `NOTIFY pgrst, 'reload schema'` als Pflicht-Step + HTTP-200-Verifikation (6 NOTIFY pgrst-Mentions → AC-10a erfuellt)
   - **13-Schritt-Live-Smoke-Plan** (Step 0 Migration-Apply + Step 1-13 Cross-System OP↔IS, inkl. Lead-Push-Smoke Step 12 + Cleanup Step 13)
   - **Coolify-Scheduled-Task Sektion** mit Tabelle (Name/Container/Cron/Command/Timezone) + User-Bestaetigungs-Template (MT-3)
   - **ENV-Variable-Setup Sektion** mit ready-to-paste Service-Key `b48c1d55f7aae78b2ed5befd4a7534b036b555ba7d4410347c8bed6f8380c090` + Block-Domains-Default-Liste + User-Bestaetigungs-Template (MT-4)
   - **IONOS-Postfach-Check Sektion** mit Aktions-Anleitung + User-Bestaetigungs-Template (MT-5)
   - **Troubleshooting** (Schema-Cache-404, Mail-nicht-angekommen, Trigger-Failure, Lead-Push-fehlend)
   - **Cross-Links** zu Slice + Rule-Files + Memories

4. **`/reports/RPT-307.md`** — dieser Completion-Report.

## Files Changed

1. **`/.env.deploy.example`** — V7-Sektion am Ende addiert:
   - `PUBLIC_SIGNUP_SERVICE_KEY=GENERATE_ME_HEX_64` mit Doku-Kommentar (BEIDE Resources identischer Wert, ready-to-paste in V7_LIVE_SMOKE_PLAN.md)
   - `PUBLIC_SIGNUP_BLOCKED_EMAIL_DOMAINS=mailinator.com,guerrillamail.com,...` 10 Default-Wegwerf-Domains, kein Whitespace

2. **`/slices/INDEX.md`** — SLC-135 Status `planned` → `done` (Z. 644).

3. **`/planning/backlog.json`** — Z. 1078 BL-110 status `open` → `done`. Z. 958 BL-098 (V7-Umbrella) status `in_progress` → `done` (alle 5 V7-Sub-Slices done).

4. **`/docs/STATE.md`** — Current Focus auf SLC-135-Code-Side-DONE umgeschrieben (mit ausfuehrlichem Outcome-Block) + Immediate Next Steps voellig neu (1. /qa SLC-135 → 2. Master-Merge → 3-5. User-Pflicht-Aktionen MT-3/4/5 → 6. /qa V7 Gesamt-Lauf → 7. final-check/go-live/deploy). Neue Sektion **V7 Operations-Setup-Status (post-/backend SLC-135)** als Cron/ENV/Postfach-Tracker.

## Files Intentionally Not Changed

- `/planning/roadmap.json` — V7 bereits `active` (kein Wechsel auf `released` vor /deploy)
- `/features/INDEX.md` — FEAT-053 bleibt `in_progress`/`planned` bis V7 deployed
- `/docs/ARCHITECTURE.md` — V7-Sektion Cron-Komponente bereits dokumentiert (Line 6352-6358, RPT-297)
- `/docs/MIGRATIONS.md` — keine neue Migration in SLC-135
- `/docs/DECISIONS.md` — kein neuer DEC (Cron-Endpoint ist DEC-131-Implementation, keine eigenstaendige Architektur-Entscheidung)
- `/sql/migrations/` — 0 neue Migrations (Reuse pending_signup-Schema aus SLC-132 MIG-042)
- `/src/lib/auth/cron-secret.ts` — existiert nicht (verifyCronSecret-Helper in Slice-Spec erwaehnt, aber V4.2 + V5 Pattern nutzt inline-Check, daher inline-Check 1:1 portiert)

## Problems Found

### F-1 (Medium / V7.1-Polish, pre-existing — NICHT SLC-135-Regression)

**Pre-existing Webpack-Build-Fail durch `validateMimeType`-Export aus `src/app/api/capture/[sessionId]/evidence/upload/route.ts` (SLC-019, Commit 23deb56).**

Beobachtung: `npx next build --webpack` auf SLC-135 HEAD failt mit `"validateMimeType" is not a valid Route export field`. Next 16 strict-validiert route.ts-Exports.

Verifikation pre-existing: Build auf `main` HEAD `d42252f` (vor SLC-135) reproduziert **identischen** Fail. Issue lebt seit V2 SLC-019.

Mitigation: Default-Turbopack-Build PASSES (Turbopack ueberspringt die Validation). Coolify-Production-Build = `npm run build` = `next build` (default Turbopack) → kein Production-Block.

Empfehlung V7.1-Polish: Helper-Functions (`validateMimeType`, `validateFileSize`, `ALLOWED_MIME_TYPES`, `MAX_FILE_SIZE`) aus `route.ts` in `validation.ts`-Modul auslagern + Re-Import in route.ts. Trivial-Fix, 5min Aufwand. Nicht release-blockierend fuer V7 weil Turbopack-Pfad funktional ist.

### F-2 (Low / pre-existing, V6.4-carry-over)

**2 Vitest-Failures in `src/app/dashboard/diagnose/__tests__/actions.test.ts` (`admin.from(...).select(...).eq(...).order is not a function`)**

Verifikation pre-existing: Lauf auf `main` HEAD reproduziert exakt dieselben 2 Failures (auch ohne SLC-135-Touches). Test-Mock fehlt `.order()`-Methode in der gechainten Mock-Chain.

Mitigation: V6.4 SLC-130 Mock-Bug aus RPT-300/301/302/303 bereits dokumentiert. V7.1-Cleanup-Slice oder eigenstaendiges /doctor.

### F-3 (Low / informational — Worktree-Build-Local)

**Local Turbopack-Build im Worktree failt mit Junction-Symlink-Error (`Symlink [project]/node_modules is invalid, it points out of the filesystem root`).**

Beobachtung: Junction (`mklink /J`) wird von Turbopack als Symlink interpretiert, der nach Turbopacks Sicht das Filesystem-Root verlaesst. node_modules-Junction-Pattern aus `reference_worktree_nodemodules_junction_windows.md` funktioniert fuer `tsc`, `vitest`, `eslint`, aber nicht fuer Turbopack-`next build`.

Mitigation: Build wurde **auf Hetzner-Test-Repo** (`/opt/onboarding-plattform-test`) mit Default-Turbopack erfolgreich verifiziert (no junction → native node_modules → kein Symlink-Issue). AC-11 Build-PASS erfuellt.

Empfehlung Strategaize-Dev-System: `reference_worktree_nodemodules_junction_windows.md` um Hinweis erweitern, dass Build-Gate auf Hetzner-Test-Repo durchzufuehren ist, wenn Turbopack im Spiel ist.

## Open Points or Deferred Decisions

1. **MT-3 User-Pflicht-Aktion (Cron-Setup)** — User legt `pending-signup-cleanup-hourly` in Coolify-Onboarding-Resource an (Setup-Tabelle in V7_LIVE_SMOKE_PLAN.md). Verifikation via `docker exec` Test-Run. Erste Cron-Trigger nach naechster voller Stunde.

2. **MT-4 User-Pflicht-Aktion (ENV-Setup BEIDE Resources)** — User setzt `PUBLIC_SIGNUP_SERVICE_KEY` (Wert aus V7_LIVE_SMOKE_PLAN.md, **identisch** in beiden Resources) + `PUBLIC_SIGNUP_BLOCKED_EMAIL_DOMAINS` (Onboarding-Resource only) in Coolify-UI.

3. **MT-5 User-Pflicht-Aktion (IONOS-Postfach)** — User legt `onboarding@strategaize.de` als Postfach oder Alias an + Smoke-Send-Test mit Subject "Strategaize V7 Pre-Deploy Smoke".

4. **AC-1 wording mismatch**: Slice-Spec sagt "Ohne CRON_SECRET-Header → 401", Implementation gibt 403 (Konsistenz mit V4.2 + V5 inline-Check-Pattern). Test prueft 403, slice-spec-wording-only-drift, kein funktionaler Drift. /qa-Bewertung empfohlen: akzeptieren weil Cross-Project-Pattern-Konsistenz (DEC-059) > spec-wording.

5. **Spec-Reuse-Annahme `verifyCronSecret`-Helper existiert nicht** — Slice-Spec erwartete einen `@/lib/auth/cron-secret.ts`-Helper aus V4.2, der nie extrahiert wurde. V4.2 und V5 nutzen inline-Check. SLC-135 ebenfalls inline (1:1 Pattern-Reuse). V7.1-Polish: Helper extrahieren wenn 4. Cron-Endpoint anfaellt.

6. **Live-Smoke selbst** — nicht Teil von SLC-135 (per Slice-Spec Out-of-Scope). Erfolgt im `/qa V7` Gesamt-Lauf oder direkt vor `/deploy V7`, nachdem User-Pflicht-Aktionen MT-3+MT-4+MT-5 durch sind.

## Project Record Updates

- ✅ `/slices/INDEX.md` SLC-135 status → `done`
- ✅ `/planning/backlog.json` BL-110 status → `done`, BL-098 (V7-Umbrella) status → `done` (alle 5 V7-Sub-Slices done)
- ✅ `/docs/STATE.md` Current Focus + Immediate Next Steps + neue V7-Operations-Setup-Status-Sektion
- ✅ `/planning/roadmap.json` V7 bleibt `active` bis /deploy (kein Wechsel auf `released`)
- ✅ `/reports/RPT-307.md` (diese Datei)

## QA Status

**Nicht durchgefuehrt — wird in eigener Session via `/qa SLC-135` durchlaufen** (per Skill-Workflow, /backend liefert code-side komplett, /qa ist eigener Schritt).

Quality-Gate-Schluss-Run im /backend selbst:
- **tsc** — EXIT=0 volltree auf SLC-135 HEAD
- **ESLint** — EXIT=0 volltree
- **Build (Turbopack, Default, auf Hetzner)** — PASS auf SLC-135 HEAD `e337247`
- **Vitest (Coolify-DB, --no-file-parallelism --pool=forks)** — 1145 + 5 PASS / 14 skipped / 2 fail (beide pre-existing diagnose Mock, identisch auf main reproduziert → **0 SLC-135-Regression**)

## Recommended Next Step

**`/qa SLC-135`** in eigener Session — Slice-Schluss-QA mit:
- AC-1..AC-11 Mapping (inkl. AC-10a NOTIFY pgrst-Verifikation)
- Quality-Gates Re-Run gegen Coolify-DB
- Stub-Detection auf neuen Files (1 erwartet: keine Stubs, alle Codepfade implementiert)
- Mock-Realitaets-Drift-Check (2 narrow Mocks: keine — Tests laufen gegen reale Coolify-DB, kein Email-Mock fuer Cron-Endpoint noetig)
- Optionale Live-Endpoint-Smoke via `curl` gegen die deployed-Branch auf Hetzner

Nach /qa-PASS: **Master-Merge `slc-135-ttl-cleanup-cron-final-hardening` → `main`**.

Danach: User-Pflicht-Aktionen MT-3+MT-4+MT-5 → `/qa V7` Gesamt-Lauf (Cross-System-Smoke) → `/final-check V7` → `/go-live V7` → `/deploy V7`.
