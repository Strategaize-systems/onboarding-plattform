---
id: RPT-129
date: "2026-05-01"
skill: /go-live
slice: V4.2-Go-Live
feature: V4.2
type: completion
status: completed
reviewOf: null
result: "/go-live V4.2 CONDITIONAL GO. Frontend-Anteil (Wizard + HelpSheet + InactiveCard + Filter + Settings + 5 Tooltips) bereits LIVE auf prod 918377a + verifiziert (RPT-127). Cron-Reminder-Pipeline gated bis 4 Pre-Deploy-Items: (1) CRON_SECRET in Coolify-ENV setzen [Wert generiert: 322b0a07...], (2) Coolify-Redeploy fuer fixed docker-compose.yml (commit 6f1fb67), (3) DKIM in IONOS aktivieren — DNS-Check 2026-05-01 zeigt s1/s2 LEER = noch NICHT aktiv (ISSUE-032), (4) Coolify-Scheduled-Task anlegen. Decision: V4.2 ist release-bereit als gestaffeltes Release — Frontend ist live, Cron wird nach DKIM-Pre-Deploy aktiviert. RELEASES.md REL-010 Draft angelegt, finalisiert in /deploy."
title: "/go-live V4.2 — Conditional GO mit 4 Pre-Deploy-Items"
---

# /go-live V4.2 — Conditional GO

## Release Target

V4.2 Self-Service-Onboarding (Wizard + Reminders + In-App-Hilfe).

- 3 Features (FEAT-031 + FEAT-032 + FEAT-033)
- 5 Slices (SLC-046..050)
- 1 Migration (MIG-029 = sql/migrations/080_v42_self_service.sql, deployed 2026-04-29)
- Domain: https://onboarding.strategaizetransition.com

**Deploy-Stand:**
- Production aktuell: commit `918377a` (Frontend V4.2 vollstaendig live)
- Repo HEAD: commit `6f1fb67` (= 918377a + Backlog/Records + docker-compose.yml CRON_SECRET-Fix)
- Code-Differenz `918377a` ↔ `6f1fb67`: nur `docker-compose.yml` (1 ENV-Var-Durchreichung) + Records

## Readiness Decision

**CONDITIONAL GO**

V4.2 wird als **gestaffeltes Release** behandelt:

| Feature | Status | Live? |
|---|---|---|
| FEAT-031 Wizard (SLC-046+047) | LIVE seit 918377a | ✅ Ja |
| FEAT-033 In-App-Hilfe (SLC-050) | LIVE seit 918377a | ✅ Ja |
| FEAT-032 InactiveCard + Filter + Settings (SLC-049 Frontend-Anteile) | LIVE seit 918377a | ✅ Ja |
| FEAT-032 Capture-Reminders Cron-Pipeline (SLC-048) | gated | ❌ Nein, bis 4 Pre-Deploy-Items |

Der Frontend-Anteil ist bereits voll release-aequivalent (Gesamt-/qa PASS RPT-127, alle Live-Pfade verifiziert). Die Cron-Reminder-Pipeline wartet bewusst auf User-Aktionen vor Aktivierung — das ist kein Bug, sondern explizite Architektur-Decision (DKIM-Pre-Deploy-Pflicht).

## Blockers vor Cron-Aktivierung

Keine Code-Blocker. 4 operative Pflicht-Items, alle als User-Aktion:

### Item 1 — CRON_SECRET in Coolify-ENV setzen
**Wert (zu kopieren):**
```
322b0a07031acb909909c937bb30492da0687365e678af991191c98803801b7a
```
**Pfad:** Coolify-UI → Onboarding-Plattform-Resource → "Environment Variables" → Add → Key=`CRON_SECRET`, Value=oben. Save.

### Item 2 — Coolify-Redeploy fuer fixed docker-compose.yml
Commit `6f1fb67` enthaelt die ENV-Durchreichung im app-service Block. Coolify-UI → Onboarding-Plattform-Resource → "Deployments" → "Redeploy" (waehlt automatisch latest from main).

Alternative: "Reload Compose File" + "Redeploy" wenn Coolify-UI das so erfordert.

### Item 3 — DKIM in IONOS aktivieren (ISSUE-032)
DNS-Check 2026-05-01 zeigt:
- `s1._domainkey.strategaizetransition.com` → LEER (kein DKIM)
- `s2._domainkey.strategaizetransition.com` → LEER (kein DKIM)
- DMARC: `v=DMARC1; p=none;` (vorhanden, aber p=none = monitoring only)
- SPF: `v=spf1 include:_spf-eu.ionos.com ~all` (vorhanden)

**Pfad:** IONOS Login → Domains & SSL → strategaizetransition.com → E-Mail-Sicherheit → "DKIM aktivieren" (1-Klick). IONOS publiziert automatisch s1+s2 Selektoren. Propagation 5-30 min.

**Verifikation nach Aktivierung:**
```bash
ssh root@159.69.207.29 "dig +short TXT s1._domainkey.strategaizetransition.com"
# Erwartet: "v=DKIM1; k=rsa; p=MII..."
```

### Item 4 — Coolify-Scheduled-Task fuer Capture-Reminders anlegen
Per `feedback_coolify_cron_node` + `feedback_cron_job_instructions`:

| Feld | Wert |
|---|---|
| Container | `app` |
| Schedule | `0 9 * * *` (taeglich 09:00 Europe/Berlin) |
| Command | `node -e "fetch('http://app:3000/api/cron/capture-reminders', { method: 'POST', headers: { 'x-cron-secret': process.env.CRON_SECRET }}).then(r => r.text()).then(t => console.log(t))"` |
| Timezone | Europe/Berlin |

**Reihenfolge wichtig:** Erst Item 1+2 (CRON_SECRET im Container verfuegbar) → dann Item 3 (DKIM, sonst Mails landen in Spam) → DANACH Item 4 (Cron anlegen).

## Accepted Risks

| ID | Finding | Begruendung Akzeptanz |
|---|---|---|
| ISSUE-007 | JWT-Legacy-Rolle bis Re-Login | V1-Pre-existing, naturalistic fix bei Re-Login |
| ISSUE-021 | Bridge-Proposal Edit-only-Pfad fehlt | V4-Pre-existing, V4.2 unbeeinflusst |
| ISSUE-022 | strategaize_admin /admin/bridge | V4-Pre-existing |
| ISSUE-026 | postcss 8.4.31 bundled in Next.js | wontfix per DEC, npm-Override-immun |
| ISSUE-028 | V3 RECORDING_WEBHOOK_SECRET | handled wenn V3-Recording inaktiv |
| BL-060 (V4.3) | Tooltip 1/5 Target zu klein | UX-Polish, nicht funktional |
| BL-061 (V4.3) | Help-Konsolidierung SLC-050 + Learning Center | UX-Verwirrung, nicht funktional |
| ESLint-9 flat-config-Gap | npm run lint failt | tsc deckt Code-Quality, Tooling-Gap |
| Privacy/Datenschutz/Impressum-Page | fehlt | V4.x ist intern/early, eigener /compliance-Skill nach V4.2 |
| Test-Re-Run in /final-check skipped | Slice-Tests bereits PASS | Code identisch zu deployed |

## Operational Concerns

### Rollback-Awareness
- Last Stable: V4.1 (REL-009, Commit `ec311a7`, Post-Launch STABLE 2026-04-30 RPT-117)
- Rollback-Pfad: Coolify-UI → Deployments → V4.1 selektieren → Redeploy. MIG-029 (Schema) ist additiv (3 ALTER + 2 CREATE TABLE) — V4.1-Code ignoriert die Spalten/Tabellen, kein DB-Rollback noetig.

### Zero-Downtime
- Coolify-Redeploy ohne Downtime (rolling update via app-Container-Restart).
- Cron-Setup hat keinen Live-Impact bis zum ersten 09:00-Run.

### Mail-Reputation-Schutz
- Reminder-Cron bleibt deaktiviert bis DKIM aktiv → Mails landen NICHT in Spam-Folder.
- Strategie schuetzt Sender-Reputation der Domain.

### Test-User-Daten
- demo-admin@strategaizetransition.com Tenant ist clean (0 Capture-Sessions, 1 Mitarbeiter inaktiv) nach Cleanup post-Gesamt-QA.
- Wizard-State auf demo-admin ist `completed` (von /qa-Walkthrough).

## RELEASES.md REL-010 Draft

Wird in /deploy V4.2 finalisiert mit echter Deploy-Time + Commit-Hash. Draft-Inhalt:

```markdown
### REL-010 — V4.2 Self-Service-Onboarding (Wizard + Reminders + Help)
- Date: TBD (in /deploy)
- Scope: 5 Slices (SLC-046..050), 3 Features (FEAT-031..033), 1 Migration (MIG-029).
  Tenant-Onboarding-Wizard (4-Schritte mit Step-Persistenz, Multi-Admin-Lock,
  Skip-Mechanismen). Capture-Reminders (E-Mail Stufe 1 nach 3 Werktagen +
  Stufe 2 nach 7 Werktagen, reminder_log Idempotenz, user_settings Opt-Out,
  Coolify-Scheduled-Task 09:00 Europe/Berlin). In-App-Hilfe (Right-Side-Sheet
  mit Markdown auf 5 Pages, 5 Tooltips an UI-Elementen). Deploy-Commit TBD.
- Summary: V4.2 macht die Plattform self-service-faehig fuer neue Tenants —
  Erst-Login-Wizard fuehrt durch Setup, Reminders sorgen fuer Mitarbeiter-
  Aktivierung, In-App-Hilfe reduziert Berater-Last bei Standard-Fragen.
  Frontend bereits live seit 918377a (2026-05-01), Cron-Pipeline aktiviert
  nach Pre-Deploy-Items (CRON_SECRET, DKIM, Coolify-Cron-Setup). Gesamt-/qa
  PASS (RPT-127), Final-Check CONDITIONALLY READY mit CRON_SECRET-Fix
  (RPT-128), Go-Live CONDITIONAL GO (RPT-129).
- Risks: 1 High Pre-Deploy-Pflicht (ISSUE-032 DKIM, gated by User-Action in
  IONOS). 2 Medium V4-Pre-existing (ISSUE-021/022). 5 Low Pre-existing
  (ISSUE-007/026/028 + BL-060/061 V4.3-Backlog). Privacy-Page-Gap als V4.3-
  Compliance-Item dokumentiert.
- Rollback Notes: Coolify-UI → V4.1 (Deploy-Commit ec311a7) waehlen → Redeploy.
  MIG-029 ist additiv (3 ALTER tenants + CREATE TABLE reminder_log + CREATE
  TABLE user_settings), V4.1-Code ignoriert die neuen Spalten/Tabellen — kein
  DB-Rollback noetig. Bei Bedarf: SQL-Reset der Wizard-State auf 'completed'
  fuer alle Tenants (`UPDATE tenants SET onboarding_wizard_state='completed'`).
- Post-Launch: TBD (Post-Launch-Window 18h nach Deploy).
```

## Recommended Next Step

**/deploy V4.2** — User fuehrt 4 Pre-Deploy-Items manuell aus, dann Coolify-Redeploy + Cron-Aktivierung. Skill begleitet Setup-Verifikation + finalisiert REL-010 Eintrag.

**Reihenfolge im /deploy:**
1. CRON_SECRET in Coolify-ENV setzen
2. Coolify-Redeploy (Commit 6f1fb67)
3. Verifikation: `docker exec <app-container> printenv CRON_SECRET` — sollte den Wert zeigen
4. DKIM in IONOS aktivieren + DNS-Recheck-Verifikation
5. Coolify-Scheduled-Task anlegen (siehe Item-4-Tabelle oben)
6. Manueller Test-Cron-Run via curl mit dem CRON_SECRET-Header
7. Smoke: Test-Mitarbeiter (richard@bellaerts.de) hat keinen Block-Submit, also nach 3 Werktagen ab `accepted_at = 2026-04-24` waere er stage1-faellig — pruefen, ob nach erstem 09:00-Run reminder_log-Eintrag entsteht
8. RELEASES.md REL-010 mit echtem Deploy-Hash + Datum finalisieren
