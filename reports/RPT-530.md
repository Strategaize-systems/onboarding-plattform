---
id: RPT-530
date: "2026-06-24"
skill: /backend
slice: SLC-172
feature: FEAT-092
type: completion
status: completed
result: "MT-1 Backend-Foundation: Blueprint-Capture-Eintritt + adaptive-Vertiefung-Logik (pure helpers + 2 Server-Actions + Entry-Page + 12 Tests). tsc/eslint 0, 12/12 GREEN. Wizard-Seiten + Reveal-UI = /frontend-Anteil von MT-1 (offen)."
title: "Backend SLC-172 MT-1 — Blueprint-Capture-Eintritt + adaptive Vertiefung (Foundation)"
---

# Backend — SLC-172 MT-1 (Blueprint-Capture-Eintritt + adaptive Vertiefung)

## Outcome
**Teilweise komplett** — der Backend-/Logik-Anteil von MT-1 ist fertig und gate-grün; der UI-Anteil (Wizard-Seiten + dynamische Vertiefungs-Einblendung) ist der **/frontend-Anteil von MT-1** und steht noch aus. End-to-End ist der Flow erst nach /frontend lauffähig (das Redirect-Ziel `/dashboard/stb/blueprint/[sessionId]` existiert noch nicht).

## Pattern-Reuse (BLOCKING-Check erfüllt)
- Session-Start 1:1 aus `src/app/dashboard/stb/modul/[modulKey]/actions.ts` (`startOrResumeModulSession`) portiert (Quell-Pfad-Header gesetzt): `createCaptureSession` + `setStbVerticalStage` + find-or-resume.
- Entry-Page aus `src/app/dashboard/stb/modul/[modulKey]/page.tsx` portiert.
- `splitBlocksByStufe` aus `src/lib/stb-vertikale/modul-capture.ts` wiederverwendet.
- LLM via bestehenden EU-Adapter `chatWithLLM` (`@/lib/llm`, eu-central-1) — data-residency.md gewahrt.
- metadata-Stash-Muster (fetch-merge-write) aus `src/lib/stb-vertikale/tenant-marker.ts`.

## Files created
- `/src/lib/stb-vertikale/blueprint.ts` — pure Helfer (hermetisch): `BLUEPRINT_SLUG`/`BLUEPRINT_BASE_PATH`, `deriveVertiefungCouplings` (leitet die 5 Kern→Vertiefung-Paare AUS dem Template ab via gemeinsames `unterbereich` — Single-Source statt hartkodiert), `coupledKernFrageIds`, `surfacedVertiefungFrageIds`, `isYellowOrRed`, `parseAmpel` (robust, fail-open auf yellow).
- `/src/app/dashboard/stb/blueprint/actions.ts` — `startOrResumeBlueprintSession` (Klon Modul-Flow) + **`assessAnswerAmpel`** (neu): Owner-Auth + Tenant-Defense-in-Depth, EU-Bedrock-Mini-Call (temp 0, 64 tok), `parseAmpel`, error_log-Audit (provider/region/model), non-blocking fail-open, Ampel-Stash in `capture_session.metadata.blueprint_adaptive_ampel`.
- `/src/app/dashboard/stb/blueprint/page.tsx` — Blueprint-Capture-Eintritt (env-gated via `dashboard/stb/layout`, Start/Resume-Button).
- `/src/lib/stb-vertikale/__tests__/blueprint.test.ts` — 12 hermetische Tests (Kopplungs-Ableitung, Surfacing green/yellow/red + fehlende Ampeln, parseAmpel JSON/Wort/de/Prioritaet/fail-open).

## Files changed (Records)
- `/docs/DECISIONS.md`, `/docs/ARCHITECTURE.md`, `/slices/SLC-172-*.md` — Korrektur: Live-Ampel-Stash in `metadata.blueprint_adaptive_ampel` statt `answers._adaptive_ampel` (Schema `record<string,string>`); Kopplung template-abgeleitet.
- `/docs/STATE.md` — Focus/Phase auf MT-1 Backend DONE.
- `/slices/INDEX.md` — SLC-172 → in_progress.
- `/planning/backlog.json` — BL-511 → in_progress.

## Verification
- `npx tsc --noEmit` → **EXIT 0**.
- `npx eslint` (4 neue Files) → **EXIT 0**.
- `npx vitest run blueprint.test.ts` → **12/12 GREEN** (9 ms).
- Tier-Befund (MIG-121): `capture_session.tier` DEFAULT `'handbook'` (rank 2) ≥ `blueprint` (rank 1 = min-tier `diagnosis_generation`) → der MT-2-Diagnose-Trigger ist by-default freigeschaltet, **kein tier-Set in MT-1 nötig** (identisch zum Modul-Flow).

## Deviations (Rule 1/3 — auto-fix, dokumentiert)
- **Stash-Ort metadata statt answers:** `capture_session.answers` ist `record<string,string>` → ein nested Ampel-Object hätte `CaptureSessionRowSchema.parse` gebrochen. In `metadata` verschoben (etablierter jsonb-Marker-Slot). Records nachgezogen.
- **Kopplung template-abgeleitet statt hartkodiert:** robuster (kein Drift, wenn der Seed sich ändert); funktional = die in DEC-249 genannte „statische" Tabelle, Single-Source = MIG-126.

## Problems found
- **MT-1 ist nicht end-to-end:** ohne die Wizard-Seiten (`[sessionId]/page.tsx` + `[sessionId]/block/[blockKey]/page.tsx`) läuft der Start-Redirect ins Leere. Das ist der /frontend-Anteil von MT-1.
- `assessAnswerAmpel` ist type-/lint-geprüft, aber **nicht runtime-getestet** (braucht DB + echten Bedrock-Call + die Wizard-Anbindung) — Runtime-Verifikation im /qa nach /frontend.

## Open points
- /frontend MT-1: Wizard-Seiten (Reuse Modul-`[sessionId]`-Flow) + dynamische Vertiefungs-Einblendung (ruft `assessAnswerAmpel`, blendet `surfacedVertiefungFrageIds` ein). R-172-2 beachten: shared Wizard mit SLC-173 → File-Touchpoints disjunkt halten (`dashboard/stb/blueprint/*`), die geteilte `questionnaire-form` möglichst NICHT modifizieren (adaptive Reveal auf Block-Ebene/nach Submit halten).
- ISSUE-107 (kein ai_cost_ledger für Assess-Calls in V1) bleibt offen.

## Recommended next step
`/frontend SLC-172 MT-1` (Wizard-Seiten + adaptive Reveal-UI), danach `/qa SLC-172 MT-1` (combined Backend+Frontend, AC-172-1 + AC-172-6 + Runtime-Smoke). MT-2 (KU-Seed-RPC MIG-127 + Tenant-Trigger) folgt danach.
