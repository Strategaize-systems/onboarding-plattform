---
id: RPT-606
date: "2026-07-08"
skill: /backend
slice: SLC-188
feature: FEAT-105
type: completion
status: completed
reviewOf: null
result: "SLC-188 strategaize_berater Foundation code-complete (MIG-132 + 2 SECURITY-DEFINER-Helper + Gate + Types + Tests). MT-1 Live-Spike loest R-ARCH-V-1/V-2; MIG-132 in-tx-ROLLBACK gegen Live-DB validiert (Functions korrekt). Code auf Branch v10-4-rollenmodell-p2 (c949aca, unmerged). Live-Apply = /deploy. Naechster Schritt: /qa."
title: "Backend SLC-188 — strategaize_berater Foundation (MIG-132)"
---

# Backend SLC-188 — strategaize_berater Foundation

## Outcome summary
SLC-188 (FEAT-105) **code-complete**. Alle 4 MTs umgesetzt: MT-1 Live-Cascade-Spike (read-only), MT-2 MIG-132, MT-3 DB-Sidecar-Tests, MT-4 TS-Types + Gate + role-check. MIG-132 wurde **in einer BEGIN…ROLLBACK-Transaktion gegen die Live-DB validiert** (pollution-frei) — die beiden Sichtbarkeits-Functions sind gegen echtes Schema bewiesen. Code isoliert auf Branch `v10-4-rollenmodell-p2` (`c949aca`, UNMERGED). **MIG-132 NICHT prod-appliziert** (= /deploy).

## Backend-Scope
Neue Rolle-Foundation: Zuweisungs-Tabelle + RLS, role-CHECK-Erweiterung, `handle_new_user`-Neufassung (cross-tenant ohne tenant_id), 2 SECURITY-DEFINER-Cascade-Helper, TS-Union + Server-Gate + Routing-Mirror. Keine UI, keine Berater-Anlage (SLC-189), keine gescopte Sicht (SLC-190).

## MT-1 — Live-Cascade-Spike (R-ARCH-V-1/V-2 aufgelöst)
Read-only gegen Live-DB `supabase-db-…-162242937585`:
- **R-ARCH-V-1:** Divergenz-Check `partner_client_mapping(accepted)` vs. `tenants.parent_partner_tenant_id` = **0 Rows in beide Richtungen** → beide Quellen kongruent → Cascade **nur via `partner_client_mapping(accepted)`** (= aktiver partner_admin-Pfad, DEC-268-Default).
- **R-ARCH-V-2:** `on_auth_user_created`-Trigger auf `auth.users` **live vorhanden**.
- role-CHECK live = 4 Werte; `handle_new_user` live-prosrc verbatim gezogen (Basis für MIG-132, partner_admin erhalten).

## Files created
1. `sql/migrations/132_v104_berater_foundation.sql` (Branch) — MIG-132: Tabelle+RLS+Index, role-CHECK 4→5, handle_new_user (Live-prosrc + strategaize_berater), `berater_assigned_tenant_ids(uuid)` + `can_see_tenant(uuid)` SECURITY DEFINER, Function-GRANTs (REVOKE PUBLIC/anon).
2. `src/lib/workspace/berater-gate.ts` (Branch) — `assertStrategaizeBerater()` (Muster admin-gate.ts).
3. `src/lib/workspace/berater-gate.test.ts` (Branch) — 5 Pure-Mock-Cases (Gate fail-closed).
4. `src/__tests__/rls/berater-foundation-rls.test.ts` (Branch) — DB-Sidecar (Cascade/can_see_tenant/RLS/handle_new_user + Regression), **Coolify-DB / PENDING-LIVE**.
5. `reports/RPT-606.md` (main) — dieser Report.

## Files changed
1. `src/types/db.ts` (Branch) — `UserRole` += `"strategaize_berater"`.
2. `src/lib/auth/role-check.ts` (Branch) — admin-PathClass erlaubt Berater; `defaultLandingForRole` → `/admin/mein-tag`.
3. `src/lib/auth/__tests__/role-check.test.ts` (Branch) — +4 Berater-Cases (admin allow, partner/dashboard block, Landing); Suite 52 grün.
4. `docs/MIGRATIONS.md` (main) — MIG-132-Eintrag auf „Datei geschrieben + in-tx-validiert, Live-Apply=/deploy".

## Files intentionally not changed
- `sql/functions.sql` — bewusst NICHT editiert (V1-Baseline, stale; MIG-132 baut aus Live-prosrc).
- `src/lib/supabase/middleware.ts` — Berater-Login-Redirect ist SLC-190 MT-2 (Plan-/qa F-1). role-check.ts hier ist nur der getestete Mirror.
- Die ~40 tenant-scoped RLS-Policies — DEC-269 (kein Berater-Zweig in P2).

## Migrations / Policies / Data-Model
MIG-132 (additiv, 1 neue Tabelle + 1 Index + 2 Policies + 1 CHECK-Neufassung + 3 Functions). **In-tx-ROLLBACK-Validierung gegen Live-DB PASS**: DDL sauber; `handle_new_user` legt `strategaize_berater` ohne tenant_id an (kein P0422); `berater_assigned_tenant_ids` Cardinality=2 (Kanzlei+Cascade-Mandant), Fremd nicht geleakt; `can_see_tenant` unter Berater-JWT korrekt (assigned true, fremd false); role-CHECK 5 Werte. Nichts persistiert.

**Live-Schema-Grounding-Funde (im Test korrigiert):** `tenants_parent_partner_consistency` verlangt `parent_partner_tenant_id` NOT NULL bei `partner_client` am INSERT; `partner_client_mapping.invitation_source` ∈ {`partner_invite`,`self_signup`}. Fixtures im Probe UND im MT-3-Test entsprechend gebaut.

## Verification
- tsc `--noEmit` 0 · eslint 0 (alle Touch-Files) · `next build` PASS (Dummy-ENV, worktree-setup P3).
- Pure-Mock-Vitest: role-check 52 + berater-gate 5 = **57 grün**.
- DB-Sidecar-Test (MT-3): geschrieben, **Coolify-DB / PENDING-LIVE** (läuft im /deploy nach MIG-132-Apply, wie V10.3 SLC-187). Live-Verhalten bereits durch In-tx-Probe bewiesen.
- **Change-Impact-Sweep (UserRole-Union):** additive Erweiterung; tsc-clean deckt Exhaustiveness; kein `Record<UserRole>`-Bruch; role-Whitelists ohne Berater = intendiertes fail-closed (P2). Kein geänderter Funktions-Kontrakt.

## Problems found
- **Verifikations-Scope (ehrlich):** Full-Suite-Regression NICHT als Stash-Baseline-Delta lokal gefahren (~600 env-gated DB-Fails by design, worktree-setup P6). Substituiert durch: tsc-clean + additive-only + role-check-Suite grün + In-tx-Live-Probe. Definitive DB-Regression = /deploy Server-DB-Suite (V10.3-Präzedenz).
- Duplikat-Test-File `src/lib/auth/role-check.test.ts` versehentlich angelegt → entfernt; Berater-Cases in die kanonische `__tests__/role-check.test.ts` konsolidiert.

## Open points / deferred
- MIG-132 Live-Apply + Pre-Apply-Live-Audit + Rollback-Dump (`/root/mig132_rollback/`) + MT-3-Sidecar-Run + Berater-E2E = /deploy V10.4.
- SLC-189 (Admin-Anlage+Zuweisung) + SLC-190 (Berater-Sicht) folgen auf demselben Branch.

## Project record updates
- `docs/MIGRATIONS.md` ✅ (MIG-132) · `reports/RPT-606.md` ✅ · `slices/INDEX.md` SLC-188 → `in_progress` ✅ · `planning/backlog.json` BL-530 → `in_progress` ✅ · `docs/STATE.md` → implementing ✅. Slice NICHT `done` (Slice-/qa steht aus, IMP-950-Defense).

## Recommended next step
**`/qa` SLC-188** (Slice-QA: Grounding-Kohärenz, Gate/RLS-Logik, Test-Coverage, DEC-269-Konformität, Wiring). Danach /backend SLC-189. MIG-132-Live-Apply + Sidecar + E2E im /deploy.
