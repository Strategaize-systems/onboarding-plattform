---
id: RPT-481
date: "2026-06-17"
skill: /architecture
slice: null
feature: FEAT-085
type: completion
status: completed
reviewOf: RPT-480
result: "V9.75 Architektur READY für /slice-planning. Alle 6 Forks Q-A..F entschieden (DEC-219..224). 2 lasttragende Schema-Fakten direkt verifiziert: quality_report existiert (mig 040), Verkaufs-Framing-Felder bereits in block_diagnosis.content (mig 051) → KEIN neuer LLM-Job (R2/Q-D aufgelöst). Uniformes session-scoped Gate, 2-Schichten-Enforcement, 3 Slices A→B→C, 2 Migrationen (Skizze), 0 neue Deps."
title: "/architecture V9.75 — Exit-Readiness-Produktisierung (Tier-Gating + Fahrplan-Report + Register)"
---

# /architecture V9.75 — Exit-Readiness-Produktisierung

## Outcome
**READY für /slice-planning V9.75.** Reine Verpackung des gebauten Engine-Stacks in die 3-Stufen-Leiter. Alle 6 architektonischen Forks (Q-V9.75-A..F) entschieden und als DEC-219..224 verankert. Architektur am realen Code gegroundet (Explore-Map + Direkt-Verifikation der zwei lasttragenden Schema-Fakten). Keine BLOCKING-OQ offen.

## Kern-Entscheidungen (DEC-219..224)
- **Q-A→DEC-219:** `capture_session.tier` (free/blueprint/handbook, NOT NULL DEFAULT handbook backfillt Bestand). Schreibpfad nur `set_capture_session_tier`-RPC (strategaize_admin) + service_role-aware `capture_session_tier_change_guard`-Trigger (Reuse BS-`profiles.role`-Pattern).
- **Q-B→DEC-220:** Gating-Matrix als SQL-Single-Source `fn_min_tier_for_job`. `recondense_with_gaps`=blueprint (Chef-Self-Backspelling). `free` getrennt vom V8-Teaser (R3 aufgelöst). free kann keinen gated Job auslösen.
- **Q-C→DEC-221:** 2-Schichten-Enforcement: Inline-Guard in 3 PL/pgSQL-RPCs + TS-`assertSessionTierAllows` an 5 TS-Dispatches + Worker-Defense via `ai_jobs.session_tier`-Stempel, fail-closed. Schließt ISSUE-097.
- **Q-D→DEC-222:** **KEIN neuer LLM-Job** — Aufwand/Owner/naechster_schritt liegen bereits in `block_diagnosis.content.subtopics[].fields` (mig 051); Exit-Kopplung getemplatet. R2 aufgelöst, data-residency-LLM-Pfad entfällt.
- **Q-E→DEC-223:** PDF via React-PDF, neues Modul `src/lib/pdf/fahrplan-report/` (Reuse mandanten-report-v2), Tier-Gate blueprint+, Web-View deferred.
- **Q-F→DEC-224:** `employee_roster_draft` session-scoped, ohne E-Mail, weiche Dedup; Brücke `promoteRosterEntryToInvitation` auf unveränderte `rpc_create_employee_invitation` (pending-email-Idempotenz respektiert).

## Schema-Grounding-Korrekturen (direkt verifiziert 2026-06-17)
1. **`block_checkpoint.quality_report` EXISTIERT** (mig `040_orchestrator_extensions.sql:19-20`, jsonb). Trägt OrchestratorOutput (coverage/gap_questions/recommendation; Typ `src/workers/condensation/types.ts:144-165`). RPT-480 war korrekt; eine frühe Explore-Vermutung war falsch und ist verworfen.
2. **Verkaufs-Framing-Felder bereits in den Daten** (mig `051_template_diagnosis_fields.sql:121` `diagnosis_prompt.output_instructions` erzeugt `aufwand`/`owner`/`naechster_schritt`/`empfehlung` pro Subtopic → `block_diagnosis.content`, Typ `DiagnosisContent` `src/workers/diagnosis/types.ts:38-50`, Lese-Präzedenz `lead-push-actions.ts:382`). → FEAT-086 braucht keinen LLM-Job (R2/Q-D-Auflösung).
3. **handbook-Trigger session-scoped** (`rpc_trigger_handbook_snapshot(p_capture_session_id)`, `074_rpc_handbook.sql:32`) → alle gated Dispatches session-scoped → **uniformes Gate** `fn_session_tier_allows(session_id, job_type)`, keine Tenant-vs-Session-Spannung.

## Gating-Surface (8 Dispatch-Eintrittspunkte, enumeriert)
PL/pgSQL: `rpc_create_block_checkpoint` (032), `rpc_enqueue_recondense_job` (047), `rpc_trigger_handbook_snapshot` (074). TS: `diagnosis-actions.ts`, `sop-actions.ts`, `walkthrough/pipeline-trigger.ts`, `bulk-email/pipeline-trigger.ts` (`enqueue()`), `api/dialogue/recording-ready/route.ts`. Kein zentraler Enqueue-Punkt → Gate in beiden Welten. Worker-Claim (`rpc_claim_next_ai_job_for_type`, 035) liefert heute keinen Session-Kontext → `ai_jobs.session_tier`-Denormalisierung für Worker-Defense.

## Slice-Plan (für /slice-planning)
A (Tier-Gating-Foundation, TDD-RED, Migration 121) → B (Fahrplan-PDF-Renderer, 0 Migrationen) → C (Register+Brücke, Migration 122, parallel zu B nach A). Cumulative-Single-Branch-Worktree `v9-75-exit-readiness`, EIN Master-Merge, AC gegen SC-V9.75-1..8. 2 Migrationen, **0 neue LLM-Jobs, 0 neue Deps**.

## Files reviewed
- `/docs/PRD.md` (Section V9.75, Z.2616-2733)
- `/reports/RPT-480.md` (Requirements-Baseline)
- `/features/FEAT-085..087-*.md` (Skeleton-Specs)
- Dev-System `docs/OPERATIVES_STUFEN_MAPPING_2026-06-15.md` §3 (Gating-Matrix-Quelle)
- Code (Explore + Direkt): `sql/migrations/{021,031,032,035,040,047,050,051,066,072,074,120}*.sql`, `src/workers/condensation/{claim-loop,types}.ts`, `src/workers/diagnosis/types.ts`, `src/lib/pdf/mandanten-report-v2/*`, `src/app/admin/debrief/[sessionId]/[blockKey]/{sop,diagnosis}-actions.ts`, `src/lib/{walkthrough,bulk-email}/pipeline-trigger.ts`, `src/app/api/dialogue/recording-ready/route.ts`, `src/app/admin/team/actions.ts`, `src/app/dashboard/diagnose/[capture_session_id]/lead-push-actions.ts`

## Files changed
- `/docs/ARCHITECTURE.md` — neue Section „## V9.75 Architecture Addendum" (§0 Schema-Korrekturen, §1-2 Komponenten, §3 Gating-Matrix, §4 Enforcement, §5 Migration-Skizze, §6 Report-Datenfluss, §7 Register, §8 Security, §9 Tradeoffs, §10 offene /slice-planning-Punkte, §11 nächster Schritt).
- `/docs/DECISIONS.md` — DEC-219..224 (6 neue Entscheidungen, je Status/Reason/Consequence).
- `/docs/STATE.md` — Current Focus + Current Phase auf „/architecture V9.75 DONE → /slice-planning" aktualisiert.
- `/planning/roadmap.json` — v9-75 summary: /architecture-DONE-Pointer + nächster Schritt /slice-planning (Status bleibt `active`).
- `/features/FEAT-085-tier-gating.md` — Section „/architecture-Auflösung" (DEC-219/220/221).
- `/features/FEAT-086-stufe1-fahrplan-report.md` — Section „/architecture-Auflösung" (DEC-222/223, kein LLM-Job).
- `/features/FEAT-087-mitarbeiter-register.md` — Section „/architecture-Auflösung" (DEC-224).

## Files created
- `/reports/RPT-481.md` — dieser Completion-Report.

## Files intentionally not changed
- `/docs/MIGRATIONS.md` — formale MIG-Einträge (121/122) entstehen bei Apply (mit Datum) im /backend bzw. /deploy; nur als Skizze in ARCHITECTURE.md.
- `/slices/INDEX.md` — keine Slices bis /slice-planning.
- `/docs/KNOWN_ISSUES.md` — ISSUE-097 bleibt `open`; wird erst durch SLC-V9.75-A-Implementation (server-side Gating) geschlossen, nicht durch Architektur.
- `/planning/backlog.json` — BL-506/507/508 bleiben `open` (Architektur ist nicht Implementation).

## Problems found
- **Explore-Fehleinschätzung korrigiert:** Der Explore-Agent vermutete, `block_checkpoint.quality_report` existiere nicht und sei in `block_diagnosis.content` gefaltet. Direkt-Verifikation (mig 040) widerlegt das — die Spalte existiert real. Hätte unverifiziert übernommen FEAT-086 falsch designt. Lehre: lasttragende Schema-Fakten vor dem Architektur-Commit direkt prüfen, nicht aus einer Explore-Vermutung übernehmen.
- **PRD-Default Q-D überholt (positiv):** PRD empfahl tentativ leichte LLM-Augmentation für Verkaufs-Framing-Felder. Verifikation zeigt diese Felder bereits in den Diagnose-Daten → kein LLM-Job nötig. Good-News-Deviation, in DEC-222 dokumentiert.
- **`bridge_generation`-Min-Tier nicht final:** an der realen Handler-Semantik in /slice-planning/backend zu bestätigen (blueprint angenommen). Matrix-Funktion macht eine Korrektur zur 1-Zeilen-Änderung.

## Open points / deferred decisions
- File-Split der Gating-Migration (eine vs. mehrere Files) → /backend.
- TS-Guard-Matrix-Quelle: RPC-Roundtrip vs. TS-Konstante-mit-Paritätstest → /slice-planning Empfehlung RPC-Call.
- Auswahlregel der Muster-Handbuch-Sektion im Report → /slice-planning.
- Test-Layout (colocated vs `__tests__/`, IMP-1262) an realer OP-Konvention prüfen.
- Web-View des Fahrplan-Reports deferred (PDF ist V9.75-Deliverable).

## QA
Nicht erforderlich nach /architecture (per Workflow-Regel). QA folgt nach /backend je Slice + Gesamt-QA vor /final-check.

## Recommended next step
**/slice-planning V9.75** — SLC-Schnitt A (Tier-Gating-Foundation, TDD-RED Bypass-Tests + Worker-Defense + ISSUE-097-Closure) → B (Fahrplan-PDF-Renderer) → C (Register+Brücke, parallel zu B nach A); AC-Matrizen gegen SC-V9.75-1..8; Cumulative-Single-Branch-Worktree `v9-75-exit-readiness`.
