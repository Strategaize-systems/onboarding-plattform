---
id: RPT-609
date: "2026-07-08"
skill: /qa
slice: SLC-189
feature: FEAT-106
type: completion
status: completed
reviewOf: null
result: "SLC-189 Slice-/qa PASS-CODE / PENDING-LIVE. 13/13 Pure-Mock-Vitest, tsc/eslint/build clean, Wiring-Chain vollständig, keine Stubs/Blocker. Live-Anlage/Zuweisung + Browser-E2E deferred bis MIG-132 live (/deploy)."
title: "SLC-189 Slice-/qa — Admin: Berater anlegen + Tenants zuweisen"
---

# SLC-189 Slice-/qa — Admin: strategaize_berater anlegen + Tenants zuweisen

## 1. QA scope reviewed
- `src/app/admin/berater/actions.ts` (createBerater / assignBerater / unassignBerater)
- `src/app/admin/berater/page.tsx` (Server-Page, Gate + Data-Load)
- `src/app/admin/berater/BeraterAdmin.tsx` (Client-UI, Form + Zuweisungs-Checkboxen)
- `src/app/admin/berater/__tests__/actions.test.ts` (13 Pure-Mock-Vitest)
- `src/components/admin-sidebar.tsx` (NAV „Berater")
- Grounding gegen `sql/migrations/132_v104_berater_foundation.sql`, `src/types/db.ts` (TenantKind).

## 2. Aggregiertes Ergebnis: **PASS-CODE / PENDING-LIVE** (Mixed)
Alle statisch/Code-verifizierbaren ACs PASS. Live-Anteile (echte Invite-Mail-Kette, Live-INSERT/DELETE, Browser-Interaktion) deferred bis /deploy — Voraussetzung ist der MIG-132-Live-Apply (Tabelle + `handle_new_user`-Berater-Zweig existieren live noch nicht). Konsistent zur SLC-188-Praxis (DB-Sidecar PENDING-LIVE).

### Per-AC-Verdicts
| AC | Verdict | Beleg |
|---|---|---|
| createBerater: Invite `type=invite` OHNE tenant_id, role=strategaize_berater, nur Admin | PASS-CODE / PENDING-LIVE | Test: generateLink-Args `data={role}` + `not.toHaveProperty(tenant_id)`; Gate unauthorized; email_exists/re-invite/emailFailed. Live: Invite→Set-Password-Kette + `handle_new_user`-Berater-Insert = /deploy (braucht MIG-132). |
| assignBerater: idempotenter Write auf `berater_tenant_assignments`, nur Admin | PASS-CODE / PENDING-LIVE | Test: upsert-Args `{berater_user_id,tenant_id,assigned_by}` + `onConflict PK, ignoreDuplicates`; invalid-id; assign_failed. Live: INSERT + PK-No-Op = /deploy. |
| unassignBerater: DELETE per PK, nur Admin | PASS-CODE / PENDING-LIVE | Test: `deleteEq(berater_user_id)` + `deleteEq(tenant_id)`; Gate; unassign_failed. Live: DELETE = /deploy. |
| Admin-UI `/admin/berater`: Liste + Anlage-Form + Tenant-Multiselect, nur Admin | PASS-CODE / PENDING-LIVE | `next build` PASS, Route im Manifest; page-Gate (redirect ≠ strategaize_admin); zuweisbare Tenants = `partner_organization`+`direct_client` (Mandanten via Cascade ausgeschlossen). Browser-Render/Interaktion = /deploy. |
| Non-Admin blockiert (Actions + Page) | PASS-CODE | `assertStrategaizeAdmin`-Gate in jeder Action (Tests: unauthorized), Page-Redirect + admin/layout + Sidebar admin-only. |

## 3. Findings nach Severity

### Blocker
- Keine.

### High
- **Live-Funktionalität an MIG-132-Apply gekoppelt (by-design Sequencing, kein Code-Bug):** `createBerater` schlägt gegen die Live-DB aktuell fehl, weil `handle_new_user` live noch die 4-Werte-role-CHECK trägt (`strategaize_berater` würde abgelehnt) und `berater_tenant_assignments` noch nicht existiert. Auflösung: MIG-132 wird im /deploy VOR dem Redeploy appliziert (Pre-Apply-Live-Audit MIG-131-Muster). Nicht slice-blockierend — geplante Reihenfolge. Bis dahin kein Live-Test möglich (kein `withTestDb`).

### Medium
- Keine.

### Low
- `createBerater` nutzt `captureInfo`-Audit statt `log_admin_event`-RPC (bewusst — RPC verlangt `p_tenant_id`, Berater ist tenant-los; Muster aus `admin/partners/actions.ts`). Reviewed & accepted.

## 4. Stub- / Wiring- / Silent-Skip-Audit
- **Stub-Scan:** 0 Treffer (kein TODO/FIXME/leerer catch/console.log/placeholder-return). `hashed_token`-Zeile = Token-Lesen aus Invite-Link, kein hardcoded Secret — accepted.
- **Wiring-Chain (Ende-zu-Ende):** (1) Caller: `BeraterAdmin.tsx` ruft alle 3 Actions; kein Test-only-Caller. (2) Erreichbarkeit: Sidebar-NAV `/admin/berater` + Invite-`verifyUrl`→ real existierende `/auth/callback`. (3) Render-Ende: page → BeraterAdmin rendert email/created/Checkboxen/kindLabel. (4) Rückkanal: `revalidatePath("/admin/berater")` == Page-Route + `router.refresh()`. (5) Bidirektional: geschriebene Spalten `berater_user_id/tenant_id/assigned_by` == MIG-132; jeder Server-Guard hat UI-Counterpart (admin-only Page+Sidebar). Kein Gap.
- **Silent-Skip / Test-Coverage:** 13/13 Pure-Mock-Vitest LIEFEN (nicht geskippt). DB-Contract-Suite bewusst NICHT geschrieben (MIG-132 nicht live) — Surrogat Pure-Mock deckt Args/Gate/Idempotenz; Live INSERT/DELETE/PK-No-Op + RLS = Nachhol-Pflicht im /deploy. Full-Suite (inkl. DB-gated) hier nicht gefahren (Sidecar PENDING-LIVE, Repo-Konvention) — mein Change ist additiv/isoliert (keine geänderten shared Exports), `next build` kompiliert das gesamte Repo fehlerfrei → keine Regression.

## 5. Offene Risiken / fehlende Checks
- **Browser-Smoke-Gate:** Slice hat User-Interaction (Form, optimistic Checkbox-Toggle, conditional Render) → max PASS-CODE ohne Browser. 2-Minuten-Smoke-Checkliste (im /deploy gegen deployed main auszuführen, als strategaize_admin):
  1. `/admin/berater` öffnen → Liste + Anlage-Form sichtbar; als tenant_admin → redirect `/admin/tenants`.
  2. Neue Berater-E-Mail eingeben → „Einladung verschicken" → Notice erscheint, Mail geht ein.
  3. Invite-Link → Set-Password-Kette durchlaufen → Berater-Login möglich, Profile ohne tenant_id.
  4. Bei Berater eine Kanzlei-Checkbox aktivieren → persistiert (reload), erneut deaktivieren → entfernt.
  5. `direct_client`-Tenant zuweisbar, `partner_client` (Mandant) NICHT in der Liste.
- **R-189-2 (tenant_id-Leak):** durch MT-4-Assert abgedeckt (PASS).

## 6. Readiness assessment
SLC-189 ist **code-complete und slice-QA-verifiziert (PASS-CODE / PENDING-LIVE)**. Keine Blocker, keine Stubs, Wiring vollständig. Freigabe für den nächsten Slice. Live-Verifikation ist an den MIG-132-Apply im /deploy gebunden und dort als Done-Bedingung geführt. Slice-Status → `done` (Code-Seite, PENDING-LIVE dokumentiert) analog SLC-188.

## 7. Next workflow step
`/backend SLC-190` (Berater-Sicht: /admin gescopt + Mein-Tag can_see_tenant-Scope, FEAT-107) — letzter Slice der V10.4. Danach: Gesamt-/qa über V10.4 → /final-check → /go-live → /deploy (dort MIG-132-Apply + Live-/Browser-Smoke SLC-188/189/190) → /post-launch. **Vor Merge:** Pre-Merge-Re-Check inkl. `git rebase origin/main` (Branch ist hinter main; main trägt SLC-188-QA RPT-606/607).
