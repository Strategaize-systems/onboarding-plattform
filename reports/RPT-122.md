---
id: RPT-122
date: "2026-05-01"
skill: /qa
slice: SLC-048
feature: FEAT-032
type: completion
status: completed
reviewOf: RPT-121
result: "SLC-048 /qa PASS — Live-SMTP-Smoke + Idempotenz-Live + Unsubscribe 3 Cases bestanden, 19/19 Unit-Tests gruen, 1 Middleware-Auth-Bug live entdeckt + gefixt (commit fd9e944)."
title: "QA SLC-048 — Capture-Reminders Cron + SMTP + Unsubscribe (Live-Smoke PASS)"
---

## Outcome summary

SLC-048 ist vollstaendig **/qa PASS**. Alle Pflicht-Gates aus dem Slice-Spec erfuellt:

- AC-1..AC-16 verifiziert (Code + Live).
- 19/19 SLC-048-Vitest-Cases gruen (workdays 7 + send-reminder 3 + cron 6 + unsubscribe 3).
- Live-SMTP-Test gegen `richard@bellaerts.de` (Demo Onboarding GmbH, accepted_at = 2026-04-24, 6d nach Annahme): Mail zugestellt, DKIM-Pass via IONOS-Signatur (User-Bestaetigung 2026-05-01).
- Idempotenz-Live-PASS: zweiter Cron-Trigger am selben Tag → `stage1_sent:0, skipped_already_sent:1`, `reminder_log` weiterhin 1 Eintrag.
- Unsubscribe 3 Cases: 404 Bogus / 404 Hex-no-match / 200 Real-Token + DB-Update + Rollback verifiziert.
- DKIM via IONOS-SMTP automatisch — kein DNS-Eintrag noetig (Server-DPA + Signatur durch IONOS).

Im /qa wurde **ein blockierender Auth-Bug live entdeckt + gefixt**: Auth-Middleware leitete `/api/cron/*` und `/api/unsubscribe/*` per HTTP 307 auf `/login` um, bevor die Route-eigenen Auth-Mechanismen (`x-cron-secret` / Token im Pfad) greifen konnten. Fix: `src/lib/supabase/middleware.ts` — beide Pfade von der Session-Auth-Pruefung ausgenommen. Commit `fd9e944` deployed via Coolify-Redeploy 06:06 UTC.

Status: **PASS — bereit fuer Slice-Status `done` + Cron-Aktivierung in Coolify.**

## Files reviewed

- `/slices/SLC-048-reminders-cron-backend.md` — Spec inkl. AC-1..16 + Pflicht-Gates
- `/src/lib/reminders/workdays.ts` — Werktage-Helper
- `/src/lib/reminders/send-reminder.ts` — SMTP-Send via nodemailer
- `/src/lib/reminders/process-reminders.ts` — Stage-Logic + Idempotenz
- `/src/app/api/cron/capture-reminders/route.ts` — Cron-Endpoint (POST + x-cron-secret)
- `/src/app/api/unsubscribe/[token]/route.ts` — Unsubscribe (GET + Token-Lookup)
- `/src/lib/reminders/__tests__/workdays.test.ts` — 7 Tests
- `/src/lib/reminders/__tests__/send-reminder.test.ts` — 3 Tests
- `/src/app/api/cron/capture-reminders/__tests__/route.test.ts` — 6 Tests inkl. Idempotenz-Pflicht
- `/src/app/api/unsubscribe/[token]/__tests__/route.test.ts` — 3 Tests
- `/src/lib/supabase/middleware.ts` — Session-Middleware (Bug-Fix-Stelle)
- `/src/lib/supabase/admin.ts` — Service-Role-Client
- `/.env.deploy.example` — `CRON_SECRET`-Doku
- `/reports/RPT-121.md` — /backend Completion-Report

## Files changed

- `/src/lib/supabase/middleware.ts` — `isApiCron` + `isApiUnsubscribe` als Auth-Bypass-Flags ergaenzt; Cron- und Unsubscribe-Routes umgehen jetzt die Session-Redirect-Logik (eigene Auth via `CRON_SECRET` / Token im Pfad). Commit `fd9e944`.

## Files created

- `/reports/RPT-122.md` — diese QA-Completion-Report.
- (Folgt: `/docs/SKILL_IMPROVEMENTS.md` Update mit IMP-Eintrag im Dev-System fuer Auth-Middleware-Coverage in /backend-Skill — siehe "Recommended next step".)

## Files intentionally not changed

- `/src/lib/reminders/process-reminders.ts` — produziert korrekte Aggregat-Result `{stage1_sent, stage2_sent, skipped_opt_out, skipped_already_sent, failed, errors}` (live-verifiziert mit beiden Trigger-Aufrufen).
- `/src/app/api/cron/capture-reminders/route.ts` — Auth-Logik (`x-cron-secret`-Header + 503/403-Pfade) live verifiziert, kein Anpassungsbedarf.
- `/src/app/api/unsubscribe/[token]/route.ts` — Token-Format-Validierung (16-128 Hex-Chars) + neutral-404-Pages live verifiziert.
- `/src/lib/reminders/send-reminder.ts` — nodemailer-Transport gegen IONOS-SMTP (smtp.ionos.de:587) funktioniert, Mail ist live zugestellt.

## Acceptance Criteria — Live-Verification

| AC | Beschreibung | Verifikation | Status |
|----|--------------|--------------|--------|
| AC-1 | `workdaysSince` korrekt | 7 Vitest-Cases gruen (Wochenend-Boundary, gleicher Tag) | ✅ |
| AC-2 | `sendReminder` sendet Stage 1+2 | 3 Vitest-Cases gruen + Live-Send an `richard@bellaerts.de` | ✅ |
| AC-3 | Cron-Endpoint POST-only | Route-File exportiert nur `POST`, Live-Test ohne POST → 405-implizit | ✅ |
| AC-4 | `x-cron-secret`-Validierung + 403 | Live-Tests: 403 ohne Header / 403 falscher Header / 200 korrekter Header | ✅ |
| AC-5 | service_role-Client + RLS-Bypass | `createAdminClient()` in route.ts; Live-Lookup ueber `employee_invitation` + `block_checkpoint` + `user_settings` ohne RLS-Block | ✅ |
| AC-6 | Stage-Logic 3-7 / >=7 / >=14 | process-reminders Vitest gruen + Live-Trigger fuer 6d-Mitarbeiter → stage1 | ✅ |
| AC-7 | Idempotenz UNIQUE-Constraint | Pflicht-Test gruen + Live: 2 Trigger heute → log_count=1, skipped_already_sent=1 | ✅ |
| AC-8 | Opt-Out Skip + Log | Vitest-Coverage gruen (Live-Verifikation in SLC-049) | ✅ |
| AC-9 | Audit-Log severity=info | `captureInfo("cron:capture-reminders done", ...)` aufgerufen, error_log-Eintrag verifizierbar | ✅ |
| AC-10 | Response-JSON-Shape | Live-Response: `{stage1_sent:1,stage2_sent:0,skipped_opt_out:0,skipped_already_sent:0,failed:0,errors:[]}` | ✅ |
| AC-11 | Unsubscribe GET | Route-File exportiert nur `GET` | ✅ |
| AC-12 | Valid Token → 200 + UPDATE | Live: `reminders_opt_out=t` nach 200-Response, Rollback durchgefuehrt | ✅ |
| AC-13 | Invalid Token → 404 neutral | Live: 2 Cases (Bogus + Hex-no-match) beide 404 mit `NEUTRAL_HTML` | ✅ |
| AC-14 | `CRON_SECRET` in `.env.deploy.example` | RPT-121 doku + ENV in Coolify-app-Container live verifiziert (= erwarteter Hex-Wert) | ✅ |
| AC-15 | Coolify-Cron-Tabelle | RPT-121 enthaelt Tabelle mit Name + Command + Frequency + Container | ✅ |
| AC-16 | `npm run test` + `npm run build` | Tests gruen; Build lokal failt nur wegen fehlender `.env.local` (Coolify-Build PASS, Container deployed) | ✅ |

## Live-Verification Summary

### Routes-Smoke (gegen https://onboarding.strategaizetransition.com)

| Test | Erwartet | Result |
|---|---|---|
| `POST /api/cron/capture-reminders` ohne Header | 403 | ✅ 403 |
| `POST` mit `x-cron-secret: wrong-secret` | 403 | ✅ 403 |
| `POST` mit korrektem `x-cron-secret` (1. Trigger) | 200 + Send | ✅ 200, `stage1_sent:1` |
| `POST` mit korrektem Header (2. Trigger gleicher Tag) | 200 + Skip | ✅ 200, `skipped_already_sent:1` |
| `GET /api/unsubscribe/notavalidtoken123` | 404 | ✅ 404 |
| `GET /api/unsubscribe/<64-hex-no-match>` | 404 | ✅ 404 |
| `GET /api/unsubscribe/<real-token>` | 200 + DB-Update | ✅ 200, `reminders_opt_out=t` |

### Mail-Empfang (live)

- Empfaenger: `richard@bellaerts.de`
- Tenant: `Demo Onboarding GmbH`
- Stage: `stage1`
- `reminder_log.status = 'sent'`, `error_message = NULL`
- Mail in Inbox, DKIM-Pass-Header (User-Bestaetigung 2026-05-01)
- IONOS signiert DKIM automatisch — kein DNS-CNAME noetig

### Idempotenz (live)

- 1. Trigger 06:08 UTC: `stage1_sent:1`, `reminder_log` = 1 Row
- 2. Trigger 06:09 UTC: `stage1_sent:0, skipped_already_sent:1`, `reminder_log` = 1 Row (unveraendert)
- UNIQUE-Constraint `(employee_user_id, reminder_stage, sent_date)` greift sauber via INSERT ON CONFLICT DO NOTHING.

## Stub Detection

Vollstaendiger Scan auf `TODO|FIXME|HACK|XXX|console.log|debugger` in:
- `/src/lib/reminders/` → 0 Funde
- `/src/app/api/cron/capture-reminders/` → 0 Funde
- `/src/app/api/unsubscribe/` → 0 Funde

## Findings

### Blocker
- aktuell keine.

### High
- **Auth-Middleware-Bug entdeckt + gefixt** in `/src/lib/supabase/middleware.ts:42`: Cron- und Unsubscribe-Routes wurden vor dem Route-Handler auf `/login` umgeleitet (HTTP 307), weil nur `/api/health` von der Session-Pruefung ausgenommen war. Beide Routes haben eigene Auth, die Middleware-Redirect-Logik machte sie effektiv unerreichbar. Fix in commit `fd9e944` (Bypass-Flags `isApiCron` + `isApiUnsubscribe`). Live-Verifikation nach Coolify-Redeploy: alle 7 Smoke-Cases PASS. **Status: resolved im selben /qa-Lauf.**

### Medium
- **Lokaler `npm run build` failt** ohne `.env.local` (kein `.env.local`-File im Repo, nur `.env.local.example`). Page-Data-Collection von `/api/unsubscribe/[token]` wirft `supabaseUrl is required`. Auf Coolify mit gesetzten ENVs erfolgreich gebaut (verifiziert: Build-Artefakte in Container `app-bwkg80w04wgccos48gcws8cs-060518962059` Apr 30 16:28 UTC). **Kein Code-Bug — Local-Env-Issue.** Mitigation: Entwickler kann lokal `.env.local` aus `.env.local.example` kopieren und Stub-Werte setzen, wenn Build-Verifikation lokal noetig.

### Low
- **DB-Integration-Tests** (`src/lib/db/__tests__/template-queries.test.ts`, `src/app/capture/.../persist.test.ts` u.a., 14 Files / 147 Tests) failen lokal mit `TEST_DATABASE_URL is not set` — erwartet per `.claude/rules/coolify-test-setup.md` (Tests laufen gegen Coolify-DB im Docker-Netzwerk). Nicht SLC-048-relevant. SLC-048-spezifische Tests (workdays/send-reminder/cron/unsubscribe) sind reine Unit-Tests ohne DB und laufen lokal gruen.

## Wiring Verification

| Step | Verifikation | Status |
|---|---|---|
| Cron-System → POST /api/cron/capture-reminders | Coolify-Cron-Setup in RPT-121 dokumentiert (Container `app`, Frequency `0 9 * * *`) | ✅ |
| Route → DB (employee_invitation + block_checkpoint + user_settings + reminder_log) | Live-Lookup loadCandidates produziert korrektes Result; INSERT ON CONFLICT in reminder_log greift | ✅ |
| Route → SMTP (IONOS smtp.ionos.de:587) | Live-Mail an richard@bellaerts.de, status=sent, kein error_message | ✅ |
| Mail → Empfaenger-Inbox | User-Bestaetigung mit DKIM-Pass-Header | ✅ |
| Mail → Unsubscribe-Link → /api/unsubscribe/[token] | 200 + reminders_opt_out=true Update live verifiziert | ✅ |
| Audit-Log → error_log severity=info | captureInfo wird im Cron-Pfad aufgerufen (Code-Inspektion) | ✅ |

## Open points or deferred decisions

- **Cron-Aktivierung in Coolify**: User-Aktion. Cron `capture-reminders-daily` wurde fuer den Smoke-Test pausiert; soll vom User wieder auf `enabled` gesetzt werden, sobald dieser QA-Report PASS gegengezeichnet ist.
- **Opt-Out-Skip-Live-Verifikation** (AC-8): Vitest-Coverage gruen, aber kein echter Live-Mitarbeiter mit `reminders_opt_out=true` getestet. Wird in SLC-049 (Settings-UI) Live-bedeckt.
- **Stage-2-Trigger-Live-Verifikation** (AC-6 Stage 2): Aktuell hat kein Mitarbeiter accepted_at >= 7 Werktage zurueck. Stage-2 ist via Vitest-Mock + Werktage-Helper-Logik gedeckt; Live-Stage-2 wird beobachtet sobald ein Tenant lange genug aktiv ist (Post-Launch-Smoke-Aufgabe).
- **Skill-Improvement-Eintrag im Dev-System** fuer Auth-Middleware-Coverage in /backend-Skill: wird im Anschluss an dieses /qa erfasst (siehe Recommended next step).

## Project record updates

- `/slices/INDEX.md` — SLC-048 `in_progress` → `done`.
- `/planning/backlog.json` — BL-060 (FEAT-032 Capture-Reminders) bleibt `in_progress` weil SLC-049 + SLC-050 noch offen sind; description aktualisiert "SLC-048 done /qa PASS 2026-05-01 (RPT-122)".
- `/docs/STATE.md` — Current Focus auf "V4.2 SLC-048 done (RPT-122), 3/5 Slices PASS, naechste: /frontend SLC-049"; Phase-Update.
- `/docs/KNOWN_ISSUES.md` — keine neuen offenen Issues. Auth-Middleware-Bug bewusst NICHT als Issue erfasst, weil im selben /qa-Lauf gefixt + verifiziert.
- `/docs/RELEASES.md` — kein Update (V4.2 noch nicht released).

## Recommended next step

1. **User-Aktion sofort:** Cron `capture-reminders-daily` in Coolify wieder auf `enabled` setzen.
2. **Dev-System: IMP-Eintrag** in `c:/strategaize/strategaize-dev-system/docs/SKILL_IMPROVEMENTS.md` fuer Auth-Middleware-Coverage in /backend-Skill: "Bei jeder neuen API-Route mit eigenem Auth-Mechanismus muss /backend explizit pruefen, dass die Session-Middleware diese Route nicht praeemptiv abfaengt. Pflicht-Check als Bestandteil von /backend-Completion-Report fuer Routes, die unauthenticated erreichbar sein muessen (Cron, Webhook, Public-Token-Routes)."
3. **/frontend SLC-049** (InactiveEmployeesCard + Mitarbeiter-Liste-Filter `?filter=inactive` + Settings-Page mit Opt-Out-Toggle). Profitiert von /qa-PASS SLC-048 (Reminder-Pipeline live), kann starten.
4. Nach SLC-049 + SLC-050: Gesamt-/qa V4.2 + /final-check + /go-live + /deploy.
