---
id: RPT-008
date: "2026-04-15"
skill: /qa
slice: SLC-002
feature: FEAT-001
type: completion
status: completed
reviewOf: null
result: "Pass (mit bekannter Scope-Entschuldung MT-4 an SLC-002a)"
title: "QA SLC-002 Rollen-Umbenennung tenant_owner -> tenant_admin"
---

# QA SLC-002 — Rollen-Umbenennung tenant_owner -> tenant_admin

## Scope

QA-Ziel: SLC-002 gegen Acceptance Criteria verifizieren, nachdem Migration 026 heute (2026-04-15) auf der Hetzner-Onboarding-DB ausgefuehrt wurde. Code-, Schema-, Policy- und Live-DB-Zustand geprueft.

Geprueft:
- Live-DB auf Container `supabase-db-bwkg80w04wgccos48gcws8cs-083715570318`
- `sql/migrations/026_rename_tenant_owner_to_admin.sql`
- `sql/schema.sql`
- `src/app/api/tenant/runs/[runId]/feedback/route.ts`
- `/docs/MIGRATIONS.md` (MIG-005)
- `/docs/KNOWN_ISSUES.md` (ISSUE-007 + ISSUE-008)
- `/slices/SLC-002-rollen-umbenennung.md`
- Backend-Implementierung aus RPT-007

## Ergebnis: **Pass** (mit Scope-Entschuldung fuer MT-4)

Alle Slice-Ziele umgesetzt und auf Live-DB verifiziert. MT-4 (automatisierter 2-Tenant-RLS-Test gegen neue Rolle) ist bewusst an SLC-002a verlagert, weil Test-Infra fehlt. Das ist kein neues Finding — es ist bereits als ISSUE-002/004 + Slice SLC-002a dokumentiert und war beim Slice-Kickoff so abgestimmt.

## Acceptance Criteria vs Befund

| # | Acceptance | Befund | Status |
|---|---|---|---|
| 1 | Kein Vorkommen von `tenant_owner` im Repo (ausser Migration-Historie + MIGRATIONS.md-Eintrag) | Grep src/ = 0 Treffer. SQL-Treffer nur in Legacy-Migrations 003/004/015/016/020/022 (historisch, nicht ausgefuehrt, ISSUE-006) + 026 selbst + MIGRATIONS.md + Slice-Record/Reports. | ✅ |
| 2 | Migration 026 auf Hetzner ausgefuehrt | Executed via base64-SSH + `docker exec psql -U postgres`. BEGIN/COMMIT sauber. | ✅ |
| 3 | Bestehende Tenant-User behalten Rechte | UPDATE 0 auf leerer DB (keine Profile-Rows). Idempotenz des UPDATE-Statements ist gegeben. Faktisch N/A. | ✅ (N/A) |
| 4 | `npm run test` gruen | Kein test-Script (ISSUE-002). MT-4 wird in SLC-002a MT-4 nachgeholt. Bewusste Scope-Entschuldung. | ⚠️ |

## Live-DB Verifikation

Reposting der QA-Queries und Antworten vom 2026-04-15 15:xx:

### CHECK-Constraint
```sql
SELECT pg_get_constraintdef(oid)
  FROM pg_constraint
 WHERE conrelid = 'public.profiles'::regclass
   AND conname  = 'profiles_role_check';
```
Output:
```
CHECK ((role = ANY (ARRAY['strategaize_admin'::text, 'tenant_admin'::text, 'tenant_member'::text])))
```
→ Enthaelt kein `tenant_owner`. ✅

### Policies mit Legacy-Wert
```sql
SELECT COUNT(*) AS policies_with_legacy
  FROM pg_policy
 WHERE polrelid IN ('public.capture_session'::regclass, 'public.block_checkpoint'::regclass,
                    'public.knowledge_unit'::regclass, 'public.validation_layer'::regclass)
   AND pg_get_expr(polqual, polrelid) ILIKE '%tenant_owner%';
```
Output: `0`. ✅

### Policy-Gesamtzahl (Sanity-Check)
`10` Policies ueber die 4 Capture-Tabellen (3+3+2+2 = 10). Passt zur Erwartung:
- capture_session: admin_full + tenant_read + tenant_admin_write = 3
- block_checkpoint: admin_full + tenant_read + tenant_admin_write = 3
- knowledge_unit: admin_full + tenant_read = 2
- validation_layer: admin_full + tenant_read = 2

### profiles.role Distinct-Werte
`(0 rows)` — DB noch ohne User (erwartet; Seed kommt mit SLC-002b). Nicht-Regression.

## Stub-Detection

Geaenderte Produktions-Files:

| Datei | Scan-Ergebnis |
|---|---|
| `sql/migrations/026_rename_tenant_owner_to_admin.sql` | Reines SQL, keine Stubs moeglich. Kommentare sind Run-Instruktionen + Verifikations-SQL. PASS. |
| `sql/schema.sql` | Eine CHECK-Wert-Reduktion, kein Logikcode. PASS. |
| `src/app/api/tenant/runs/[runId]/feedback/route.ts` | Der Scope dieses Slices war nur Kommentar-Entfernung. Die Route selbst ist toter Blueprint-Legacy-Code (referenziert `runs`/`run_feedback`, die im Schema nicht existieren) — Status `open` als ISSUE-008, nicht neuer Befund dieses QAs. |

Stub-Scan: **PASS** (keine neuen Stubs durch SLC-002 eingefuehrt).

## Wiring-Verifikation

Nicht anwendbar — reiner Backend/DB-Slice. Kein neuer Frontend-Pfad, kein neuer API-Endpoint, keine neue State-Kopplung.

## Findings

### High

**H1: MT-4 (automatisierter 2-Tenant-RLS-Test) nicht durchgefuehrt**
- **Fund:** Acceptance-Criterion 4 verlangt `npm run test` gruen. Test-Infra existiert nicht (ISSUE-002).
- **Analyse:** Bereits vor Slice-Start so abgestimmt — Test-Infra-Aufbau ist eigener Slice SLC-002a. Slice-Definition SLC-002 MT-4 war vorausschauend auf diese Entkopplung formuliert.
- **Impact:** Keine automatisierte Regressions-Absicherung fuer die Rollen-Rename-Semantik. Live-DB-Stand ist per SQL-Verifikation nachweislich sauber (0 Policies mit `tenant_owner`, CHECK ohne `tenant_owner`).
- **Empfehlung:** Kein Block fuer SLC-002-Fortsetzung. Wird in SLC-002a MT-4 nachgeholt.

### Low

**L1: Migration 022 im Repo enthaelt weiter `tenant_owner`**
- **Fund:** `sql/migrations/022_core_capture_rls.sql` wurde nicht rewriten (History-Prinzip).
- **Analyse:** Init-Script-Flow (`schema.sql` + `functions.sql` + `rls.sql`) ist Source-of-Truth fuer neue DBs. 022 wurde bereits auf Hetzner ausgefuehrt. Bei kuenftigen Cold-Starts einer neuen Hetzner-DB muss 022 modifiziert oder 026 direkt mitlaufen.
- **Empfehlung:** Bei naechster Cold-Start-Planung beruecksichtigen. Kein Handlungsbedarf jetzt.

**L2: Legacy-Route bleibt im Build**
- **Fund:** `src/app/api/tenant/runs/[runId]/feedback/route.ts` wuerde HTTP 500 liefern, wenn aufgerufen, weil Tabellen `runs` und `run_feedback` nicht existieren.
- **Analyse:** Kein Effekt in Produktion (niemand ruft diese URL auf). Bereits als ISSUE-008 dokumentiert. Analog zu ISSUE-006.
- **Empfehlung:** Cleanup-Slice (mit ISSUE-006) fuer spaeter.

## Open Risks

- **Test-Coverage-Gap bleibt** bis SLC-002a durch ist. Bei Migrations-Regressionen in naechsten Slices (z.B. SLC-003 Template-Seed koennte versehentlich `tenant_owner` wieder einfuehren) bleibt nur die SQL-Verifikation als Sicherheitsnetz.
- **JWT-Refresh-Randbedingung (ISSUE-007)** irrelevant solange keine Bestands-User existieren. Wird beim ersten Kundenlaunch relevant.

## Readiness

- **Fuer SLC-002a (Test-Infra)**: **Bereit.** Die neue Rolle `tenant_admin` ist konsistent in DB + Code, Test-Fixtures koennen direkt darauf bauen.
- **Fuer SLC-002b (Admin + Demo-Tenant Seed)**: **Bereit.** Seed-Migration 027 kann `tenant_admin` ohne Legacy-Rueckfall nutzen.
- **Fuer SLC-002c (App-Branding)**: **Bereit.** Unabhaengig von Rollen-Aenderung.
- **Fuer produktiven Kunden-Einsatz**: **Nicht bereit** bis SLC-002a + SLC-002b + manueller Login-Smoke-Test durch sind.

## Files Reviewed
- `/slices/SLC-002-rollen-umbenennung.md`
- `/sql/migrations/026_rename_tenant_owner_to_admin.sql`
- `/sql/schema.sql`
- `/src/app/api/tenant/runs/[runId]/feedback/route.ts`
- `/docs/MIGRATIONS.md`
- `/docs/KNOWN_ISSUES.md`
- `/reports/RPT-007.md`
- Live-DB auf Hetzner

## Files Changed (dieser QA-Run)
- `/reports/RPT-008.md` — dieser Report
- `/slices/INDEX.md` — SLC-002 Status auf `done`
- `/planning/backlog.json` — BL-008 Status auf `done`
- `/docs/STATE.md` — Current Focus + Next Steps + Phase aktualisiert

## Files Intentionally Not Changed (QA)
- Alle Production-Files — keine neuen Code-Aenderungen im QA-Skill; QA dokumentiert nur den Stand.

## Recommended Next Step

**`/backend` fuer SLC-002a Test-Infrastruktur starten.** Blocker-Priorit aus SLC-Planning. Nach SLC-002a wird MT-4 von SLC-002 nachgeholt und ISSUE-002/004 geschlossen. Danach 002b (Admin-Seed) + 002c (Branding) in beliebiger Reihenfolge.

Alternativ: Falls der User parallel eine kleine Kosmetik-Aufraeumung bevorzugt, kann SLC-002c (App-Branding) auch ohne Test-Infra laufen — aber das verschiebt nur das Test-Infra-Setup.
