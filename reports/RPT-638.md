---
id: RPT-638
date: "2026-07-09"
skill: /qa
slice: SLC-193
feature: FEAT-110
type: completion
status: completed
reviewOf: null
result: "SLC-193 V20 DB/Authz-Hardening (MT-0..MT-4): PASS (code-done). DB-Layer-ACs (tier-Guard INSERT-Coerce+UPDATE-Deny, berater-COALESCE, search_path-Sweep) LIVE via In-tx-Probe 9/9 CHK PASS gegen Coolify-DB (ROLLBACK, DB unveraendert); evidence-Ownership Pure-Mock 6/6; tsc0/eslint0/build0; 0 Regression (Baseline-Delta identisch). App-Flow-End-to-End + evidence-403-Live-Smoke -> Gesamt-QA/Deploy. MIG-133/134 Live-Apply erst /deploy."
title: "QA SLC-193 — V20 DB/Authz-Hardening PASS (code-done), In-tx-Probe 9/9 live"
---

# QA — SLC-193 V20 DB/Authz-Hardening

## QA-Scope
`/backend` SLC-193 (MT-0..MT-4) im Worktree `v20-security-hardening` (Branch-HEAD `c71139e`, off main `942cebe`). Geprueft gegen die 7 ACs: MIG-133 (tier-Guard INSERT+UPDATE + DEFAULT + berater-RPC-COALESCE + profiles.role-Guard), MIG-134 (search_path-Sweep), MT-2 (entitled-tier Auto-Grant), MT-3 (evidence-Ownership). Verifikation: tsc/eslint/next build, gezielter Vitest, In-tx-Full-Apply-Probe gegen die Live-Coolify-DB (BEGIN..ROLLBACK), Baseline-Delta-Regressionsvergleich, Stub-/Wiring-/Silent-Skip-Audit.

## Verdict: PASS (code-done)
DB-Layer-ACs live bewiesen (In-tx-Probe); Code-Gates gruen; 0 Regression. App-Flow-End-to-End + evidence-403-Live sind bewusst auf Gesamt-QA V20 / `/deploy` deferred (Mixed-Results → Slice-Status `code-done`). Migrationen bleiben Code-Side, Live-Apply erst `/deploy`.

## Per-AC-Verdikte

| AC | Verdikt | Beleg |
|---|---|---|
| AC-193-1 tier-Guard INSERT-Coerce + UPDATE-Deny, service_role unveraendert | **PASS-LIVE** | In-tx-Probe CHK1 (service_role INSERT behaelt handbook), CHK2 (non-service_role→free), CHK4 (UPDATE-Deny), CHK5 (service_role UPDATE ok). Committed `tier-guard-insert.test.ts` (assume-applied) fuer /deploy-Sidecar |
| AC-193-2 DEFAULT tier='free' live; App-Session-Creation gruen | **PASS-LIVE (DEFAULT) / PENDING-LIVE (App-Flow)** | CHK3 DEFAULT=free (Probe). MT-2 Auto-Grant tsc/eslint/build gruen + service_role-Elevation-Pfad via CHK5 bewiesen. End-to-End-Session-Creation-Browser-Smoke → Gesamt-QA/Founder-Test |
| AC-193-3 evidence/list + download 403 cross-tenant, konsistent mit upload | **PASS-CODE (Mock 6/6) / PENDING-LIVE (403-Smoke)** | `route.test.ts` 6/6 (401/401/404/403/200/200-admin); download 1:1 gespiegelt (Diff-Review). Live-403 → Gesamt-QA (ISSUE-124 open-risk RPT-637) |
| AC-193-4 berater-RPC authenticated nur eigene Tenants; admin-Pfad unveraendert | **PASS-LIVE** | CHK6 (auth.uid() gewinnt, fremd=false), CHK7 (p_uid-Fallback bei auth.uid()=NULL). Committed `berater-rpc-coalesce.test.ts` fuer /deploy |
| AC-193-5 SEC-001 Rest-Count 0 | **PASS-LIVE** | CHK9 rest-count=0; MIG-134 haertete live exakt die 12 MT-0-Funktionen. Committed `search-path-sweep.test.ts` (+idempotent) fuer /deploy |
| AC-193-6 profiles.role-Guard-Trigger vorhanden (INSERT+UPDATE, Defense-in-Depth) | **PASS-LIVE (mit Nuance)** | Trigger `profiles_role_change_guard` angelegt (Probe-Apply). CHK8: role-Escalation verhindert (role bleibt tenant_admin). Nuance s.u. (Low-1) |
| AC-193-7 tsc0/eslint0/build PASS/Vitest 0 Regression + neue Tests gruen; MIG idempotent | **PASS** | tsc 0, eslint 0, next build 0; evidence-Test 6/6; 0 Regression (Baseline-Delta); MIG-134 2x-idempotent (Probe+Test), MIG-133 CREATE OR REPLACE + DROP/CREATE-Trigger idempotent |

**Aggregat:** 4 PASS-LIVE + 2 PASS-CODE/PENDING-LIVE + 1 PASS. Kein Blocker/High.

## In-tx-Full-Apply-Probe (Live-Coolify-DB, `container ...-145427518712`)
`BEGIN; <MIG-133> <MIG-134> <DO-Probe> ROLLBACK;` via `psql -v ON_ERROR_STOP=1`. **9/9 CHK PASS:** CHK1 service_role-INSERT=handbook · CHK2 non-service_role-INSERT→free · CHK3 DEFAULT=free · CHK4 UPDATE-Deny · CHK5 service_role-UPDATE=blueprint · CHK6 berater COALESCE (own=t, fremd=f) · CHK7 p_uid-Fallback · CHK8 profiles-Escalation verhindert · CHK9 search_path rest-count=0 (12 Funktionen gehaertet). Kontroll-Query nach ROLLBACK: Live-DB unveraendert (tier-DEFAULT weiterhin 'handbook', 12 ungehaertete Funktionen) — Migrationen bleiben Code-Side bis /deploy.

## Findings

### Low
- **L-1 (AC-193-6) profiles-Guard: Probe-Pfad wurde von RLS zuerst geblockt.** CHK8 zeigt `exc=false, unchanged=true` — der authenticated-UPDATE auf `profiles.role` lieferte 0 Rows (RLS `USING` filtert die Fremd-/Self-Row) BEVOR der BEFORE-Trigger die `insufficient_privilege`-Exception werfen konnte. Ergebnis (Escalation verhindert) ist korrekt; der Trigger ist die **zweite** Defense-Schicht fuer den Fall, dass RLS die Row je durchliesse. Der reine Trigger-Reject-Pfad (`exc=true`) ist damit im Probe nicht direkt exerziert — Defense-in-Depth-Charakter, kein Defekt.
- **L-2 Pre-existing Mock-Flakes (24 Non-DB-Fails, Baseline-identisch).** Full-Suite 619 failed | 2293 passed | 65 skipped: 595 = `TEST_DATABASE_URL`-env-gated (laufen /deploy-Sidecar), 24 = pre-existing Mock-Infra-Flakes (`client.rpc`/`maybeSingle is not a function` in walkthrough/diagnose/bulk-email/pipeline-trigger/worker-Tests + 1 cost-timing-Flake). Baseline-Delta: 6 betroffene Mock-Files @942cebe = 23 failed/47 passed, @HEAD identisch → **0 Regression durch SLC-193**. Out-of-scope (bestehende Test-Infra).

### Hinweis (kein Finding)
- **MT-2 stb-modul tier = 'blueprint' (nicht 'handbook').** Der Founder-Entscheid war "Auto-Grant per Feature = der vom Feature benoetigte Tier". Grounding gegen mig 124:174 (DEC-239): `module_output_synthesis` = **blueprint**. Daher stb-modul auf 'blueprint' gesetzt (least-privilege; 'handbook' waere Over-Grant). Illustratives Label in der Frage sagte "handbook" — die Realitaet ist blueprint.

## Stub-/Wiring-/Silent-Skip-Audit
- **Stub-Scan:** keine TODO/FIXME/Platzhalter/leere Handler eingefuehrt. `setCaptureSessionEntitledTier` voll implementiert (service_role-UPDATE, wirft bei Fehler, No-Op bei 'free'). Migrationen mit Rollback-Kommentaren.
- **Wiring-Chain:** `setCaptureSessionEntitledTier` von 2 Production-Callern gerufen (stb/blueprint, stb/modul else-Branch) — verifiziert. `tier`-Felder in die 3 service_role-INSERTs eingebettet. evidence-Ownership-Checks VOR dem admin-Read in beiden GET-Handlern. MIG-133/134 in `sql/migrations/` (Apply /deploy).
- **Silent-Skip:** DB-Suiten explizit `TEST_DATABASE_URL`-gated (dokumentiert, laufen /deploy). Neue DB-Tests self-contained (Migration in-tx apply, Auto-ROLLBACK) → laufen im /deploy-Sidecar; jetzt via In-tx-Probe (psql) 1:1 gemappt bewiesen. Kein 0-gelaufener-Test-als-PASS.

## Open risks / Nachhol-Pflicht (Gesamt-QA V20 / /deploy)
1. **Live-403 evidence/list + download** (ISSUE-124) — Browser/JWT-Smoke gegen deployten Stand.
2. **App-Session-Creation End-to-End** — StB-Blueprint/Modul + bulk-email + diagnose + walkthrough gegen deployte DB mit gesenktem DEFAULT (Feature funktioniert mit Auto-Grant-Tier). Founder-Test/Browser-Smoke.
3. **MIG-133/134 Live-Apply + DB-Verify** im /deploy (node:20-Sidecar-DB-Suite gegen persistente DB) — die committed Vitest-Files (`tier-guard-insert`/`berater-rpc-coalesce`/`search-path-sweep`) mappen 1:1 auf die 9 Probe-CHK.
4. **ISSUE-124/125/129** bleiben `open` (code-addressed, Live-Resolve nach /deploy-Apply + Live-Verify).

## Records
RPT-638 erstellt. SLC-193 → `in_progress` (code-done). BL-537 → `in_progress`. DEC-287 (MT-2 Auto-Grant-per-Feature + stb-modul=blueprint-Grounding). ISSUE-124/125/129 bleiben open (Live-Resolve /deploy). STATE Focus/Next-Steps aktualisiert.

## Next workflow step
**SLC-194** (`/backend`/`/frontend` XSS/Headers, FEAT-111) im selben kumulativen Worktree-Branch `v20-security-hardening` (SLC-193 blockiert SLC-194, sequenziell). Danach SLC-195 → **Gesamt-/qa V20** (holt die PENDING-LIVE-Punkte 1-3 nach) → `/final-check` → `/code-review ultra` (DEC-219-Closure) → `/deploy` (MIG-133/134 Live-Apply) → `/post-launch`. Next IDs: RPT-639, DEC-288, MIG (SLC-194/195 = 0 Migration).
