---
id: RPT-433
date: "2026-06-10"
skill: /qa
slice: SLC-V9.1-A
feature: FEAT-075 + FEAT-076
type: completion
status: completed
reviewOf: null
result: "PASS-LIVE fuer MT-4 Webhook-Endpoint /api/inbound/email — AC-V9.1-A-5/6/7/8 live-verifiziert gegen Coolify-DB (4/4 Integration GREEN), TSC=0/ESLint=0/offline Vitest 41+4skip, 0 Stubs, 0 Blocker/High/Medium, 1 Low (Coverage)"
title: "QA SLC-V9.1-A MT-4 (Inbound-Webhook + 3-Schicht-Validation + Coolify-DB-Integration)"
---

# QA SLC-V9.1-A MT-4 — Webhook-Endpoint + 3-Schicht-Validation-Layer

## 1. QA-Scope

- Slice: SLC-V9.1-A — Inbound-Foundation + Validation-Layer (FEAT-075 + FEAT-076)
- Micro-Task: MT-4 (`src/app/api/inbound/email/route.ts` Flow A + 5 Helper + Integration-Test)
- Worktree: `c:/strategaize/strategaize-onboarding-plattform-v91`, Branch `v9-1-forward-bucket-email`, HEAD `7155752`
- Verifiziert: AC-V9.1-A-5, AC-A-6, AC-A-7, AC-A-8 + AC-A-12 (Quality-Gates) — Code-Side + LIVE gegen Production-Coolify-Supabase (159.69.207.29)

## 2. Ergebnis: PASS-LIVE

MT-4 ist funktional vollstaendig und live-verifiziert. Die Webhook-Flow-A-Kette
(HMAC-Verify → Vendor-Parse → Tenant-Lookup → Endpoint-Status → Setup-Token →
Optional-Allowlist → Storage-PUT → atomic rpc-Persist → error_log-Audit → 200/401)
arbeitet gegen die echte Coolify-DB + `bulk-email`-Bucket korrekt.

## 3. Quality-Gates

| Gate | Ergebnis |
|---|---|
| `npx tsc --noEmit` | EXIT=0 |
| `npx eslint src/lib/inbound-email src/app/api/inbound` | EXIT=0 |
| Offline Vitest (inbound scope) | 41 PASS + 4 gated-skip (6 Files) |
| Coolify-DB Integration (`route.test.ts`, RUN_V91_INBOUND_INTEGRATION=true) | 4 PASS + 1 skipIf-placeholder |

## 4. AC-Matrix

| AC | Inhalt | Status | Nachweis |
|---|---|---|---|
| AC-V9.1-A-5 | HMAC-Fail → 401 + reject_log(hmac_invalid) | PASS-LIVE | Integration `AC-A-5` 401 |
| AC-V9.1-A-6 | Reject-Pfade → 200 silent-drop + reject_log | PASS-LIVE (teilweise) | `tenant_not_found` + `setup_token_invalid` live; reject_log-Row geprueft |
| AC-V9.1-A-7 | Full-Pass: email_message + email_bulk_run + Storage + Audit | PASS-LIVE | Integration `AC-A-7` |
| AC-V9.1-A-8 | 2x same-day → 1 run, email_count=2, 2 messages | PASS-LIVE | Integration `AC-A-8` run=1/count=2/msgs=2/status='continuous' |
| AC-V9.1-A-12 | TSC=0 / ESLint=0 / Vitest GREEN | PASS | siehe Quality-Gates |

Implizit re-bestaetigt durch den Integration-Run: MIG-059 (`uploader_user_id`
nullable) + MIG-060 (`rpc_inbound_record_message`) sind LIVE — der atomare
Daily-Roll-Over + email_message-Insert ohne menschlichen Uploader lief durch.

## 5. Stub-Detection

0 echte Stubs in den MT-4-Files. Kein TODO/FIXME/HACK, keine leeren Catch-Handler,
kein `console.log`, keine hardcoded Secrets. `reject-log.ts` swallow+`captureWarning`
ist bewusstes best-effort-Audit (kein Stub). Test-Fallback `"test-secret-fallback"`
liegt korrekt in einer `.test.ts`-Datei (acceptable).

## 6. Wiring-Verification (Backend-Pfad)

Route → DB-Kette live-verifiziert: `rpc_inbound_record_message` (Daily-Roll-Over +
email_message), `email_validation_reject_log`-Insert, `email_forward_allowlist`-SELECT,
`email_inbound_endpoint`-Lookup und Storage-Upload in `bulk-email` haben alle gegen
die Production-DB geschrieben/gelesen. Component→API-Wiring N/A (Lambda-Caller = MT-5).

## 7. Findings

### Low
- **L-1 (Coverage):** Der Integration-Test deckt die Reject-Pfade `endpoint_inactive`,
  `setup_token_missing` und `allowlist_mismatch` nicht auf Route-Ebene ab. Die
  zugrundeliegende Logik ist offline unit-getestet (`setup-token.test.ts`,
  `sender-allowlist.test.ts`), und der `allowlist`-Skip-Pfad wird durch den Full-Pass
  (keine enabled Rows) implizit geprueft. Die vollstaendige Route-Matrix inkl.
  Cross-Tenant-RLS ist explizit MT-6-Scope (AC-A-11, `__tests__/rls/v91-inbound.rls.test.ts`).
  Akzeptiert fuer MT-4.

## 8. Problems found

- **P-1 (Coolify-ENV-Gap, NICHT MT-4-blocking):** Im Coolify-App-Container
  (`app-bwkg80w04wgccos48gcws8cs-162742787231`) sind die V9.1-Inbound-ENVs
  `INBOUND_WEBHOOK_HMAC_SECRET`, `INBOUND_VENDOR`, `INBOUND_CATCHALL_DOMAIN` noch
  NICHT gesetzt (nur `SUPABASE_SERVICE_ROLE_KEY` vorhanden). Fuer MT-4-Integration
  unkritisch (HMAC wird in-process signiert+verifiziert mit Test-Secret). Aber
  BLOCKING fuer MT-6 Live-Smoke (AC-V9.1-A-13 Coolify-ENV-Sync). Muss vor MT-6
  gesetzt werden.

## 9. Offene Punkte / Deferred

- MT-5: Lambda-Source `infra/lambda/forward-ses-to-op-webhook/` + `scripts/deploy-lambda.sh` (Code-only).
- MT-6: Live-Smoke E2E (SES→S3→SNS→Lambda→Webhook) + RLS-Test-Matrix 12+ Cases + Records-Final-Bump. Benoetigt AWS-Founder-Setup Steps 1-6 + Coolify-ENV-Sync (P-1).

## 10. Readiness

MT-4 ready. SLC-V9.1-A bleibt `in_progress` (MT-5 + MT-6 offen). Keine
Release-Gate-Aenderung — V9.1 NICHT released/stable per IMP-950 + module-lifecycle-discipline.

## 11. Next workflow step

`/backend SLC-V9.1-A MT-5` — Lambda-Source + ZIP-Deploy-Skript (~2-3h, AWS-Setup-frei
lauffaehig). MT-6 separat, da AWS-Founder-Setup + Coolify-ENV-Sync Pre-Condition.
Kein Master-Merge, kein /go-live, kein released vor Gesamt-V9.1-/qa + /final-check + /go-live + /post-launch.
