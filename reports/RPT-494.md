---
id: RPT-494
date: "2026-06-18"
skill: /slice-planning
slice: null
feature: FEAT-088, FEAT-089
type: completion
status: completed
reviewOf: null
result: "V9.8 in 2 strikt sequentielle Slices zerlegt (SLC-V9.8-A=FEAT-089 Tag-Export-Propagation + Mig 123, SLC-V9.8-B=FEAT-088 Controlled-Tag-Vokabular). A vor B (B liest die von A erzeugte knowledge_unit.themes-Spalte). 1 Migration, 0 neue Deps/Jobs/Worker. READY fuer /backend SLC-V9.8-A."
title: "Slice-Planning V9.8 — Controlled Tag-Vokabular + Tag-Export-Propagation"
---

# Slice-Planning V9.8 — Controlled Tag-Vokabular + Tag-Export-Propagation (BL-505)

## Outcome
V9.8 in **2 narrow, strikt sequentielle Slices** zerlegt. **READY fuer /backend SLC-V9.8-A.** Architektur (DEC-228..232, RPT-493) 1:1 umgesetzt: 1 additive Migration (123), 0 neue Deps/Jobs/Worker, Lean Prompt-Injektion nur im Synthese-Punkt.

## Slicing-Summary
Die Architektur diktiert die Reihenfolge: FEAT-088 (Vokabular) speist sich on-the-fly aus `knowledge_unit.themes` — genau der Spalte, die FEAT-089 erst erzeugt + befuellt (DEC-228/229). Daher A=FEAT-089 zuerst, B=FEAT-088 danach, **nicht parallelisierbar**.

## Slice-Liste (Reihenfolge)
1. **SLC-V9.8-A — Tag-Export-Propagation** (FEAT-089, High, Mig 123). `knowledge_unit.themes text[] NOT NULL DEFAULT '{}'` + GIN; `email_synthesized_unit.themes` → `knowledge_unit.themes` beim Promote. 2 MTs (Migration+DB-Test; Propagation-Code+Unit-Test). Erster Slice, keine Dependency, blockt B.
2. **SLC-V9.8-B — Controlled Tenant-Tag-Vokabular** (FEAT-088, High, 0 Mig). `getTenantTagVocabulary` (Top-60/Tenant, on-the-fly, JS-agg) + `existingTags`-Block in `buildSynthesisUserPrompt` + Worker-Wiring + use-existing-where-fits/only-add-if-novel-Regel. 3 MTs. Hard-Dep auf A.

## Code-Grounding (verifiziert in dieser Session)
- Migration 123 ist die naechste freie Nummer (Tail = 122).
- Alle Touchpoint-Files existieren: `handbook-import.ts` (414 Z.), `email-synthesis-prompt.ts` (100 Z.), `handle-synthesis-job.ts` (753 Z.).
- `SynthesizedUnitForImport` (Z.56) + `KnowledgeUnitInsertInput` (Z.103) tragen `themes` NICHT; `mapSynthesizedUnitToKnowledgeUnit` (Z.230) mappt es nicht.
- Caller `importToHandbook` (`curation/actions.ts`): `unitForImport` hand-gebaut (Z.769), `.insert(insertInput)` spreadet die Mapper-Ausgabe (Z.801) → `themes` im Mapper + Type flieesst automatisch in den INSERT; nur units-SELECT + unitForImport brauchen explizites `themes`.
- `buildSynthesisUserPrompt(sectionName, patterns)` wird in `email-synthesis.ts:142` (via `defaultSectionSynthesizer`, `handle-synthesis-job.ts:113`) gerufen; `run.tenant_id` ab Z.270. `getTenantTagVocabulary` existiert noch nicht (neuer Helper).

## Parallel-Readiness
| Slice | Group | MIG | Touchpoints (Kern) | Notes |
|---|---|---|---|---|
| SLC-V9.8-A | S1 (zuerst) | **123** | sql/migrations/123_*.sql, handbook-import.ts, curation/actions.ts | erzeugt+befuellt knowledge_unit.themes; blockt B |
| SLC-V9.8-B | S2 (nach A) | keine | tag-vocabulary.ts, email-synthesis-prompt.ts, email-synthesis.ts, handle-synthesis-job.ts | liest knowledge_unit.themes; Hard-Dep auf A |

**Nicht parallelisierbar** — B's Vokabular-Quelle ist die von A erzeugte Spalte. Cumulative-Single-Branch `v9-8-tag-vokabular` (matcht V9.5/V9.7/V9.75-Konvention), /qa pro Slice, EIN Master-Merge nach Gesamt-/qa + Pre-Merge-Re-Check.

## Risiken
- **R-A-1 (Deploy-Ordering, BLOCKING /deploy):** `.insert(insertInput)` spreadet `themes` → Mig 123 muss VOR Code-Redeploy live (sonst `column "themes" does not exist`). Bewusst keine defensive Retry-ohne-themes (themes ist Kern, Ordering kontrolliert).
- **R-B-2 (Aggregations-Ort):** JS-seitig statt SQL-RPC (Architektur sieht nur Mig 123 = Spalte+GIN vor). Per-Tenant-Rowcount bounded → billig. RPC/Materialisierung = V9.8+-Option.
- **R-B-3 (Behavioral non-deterministisch):** LLM-Tag-Reuse (SC-2/SC-3) ist Live-Smoke/observational; hermetisch wird nur Prompt-Inhalt (Block+Regel) geprueft. Im /qa als Confidence-Limit ausweisen.
- **R-B-4:** Embedding-Normalisierung deferred (DEC-232) — Prompt-Kontrolle ist die einzige Bremse in V1.

## Records geaendert/erstellt
- `slices/SLC-V9.8-A-tag-export-propagation.md` (neu), `slices/SLC-V9.8-B-controlled-tag-vokabular.md` (neu).
- `slices/INDEX.md` — V9.8-Sektion (2 Slices `planned`, Execution-Order, Parallel-Tabelle, Pflicht-Gates).
- `docs/STATE.md` — High-Level-State → slice-planning; Next-Step → /backend SLC-V9.8-A.
- `reports/RPT-494.md` (dieser Report).
- Unveraendert (schon korrekt aus /requirements+/architecture): `planning/roadmap.json` (v9-8 active), `planning/backlog.json` (BL-505 in_progress), `features/INDEX.md` (FEAT-088/089 planned), `docs/DECISIONS.md` (DEC-228..232), `docs/MIGRATIONS.md` (MIG-123 geplant).

## Problems found
- Keine BLOCKING. BL-505 bleibt bewusst der einzige V9.8-Backlog-Eintrag (Umbrella ueber beide Features — beide Feature-Specs binden an BL-505; Slice-Granularitaet lebt in slices/INDEX.md). Cockpit-Versionsfortschritt V9.8 = 1 Item (kein 50%-Zwischenstand, fuer 2-Slice-Version akzeptiert / „nicht ueberdesignen").

## Recommended next step
`/backend SLC-V9.8-A` — Worktree-Setup (MT-0) + Migration 123 + Propagation. Danach `/qa SLC-V9.8-A`, dann `/backend SLC-V9.8-B`. (Parallel-Thread: `/post-launch V9.75` T+24h ~2026-06-19 → bei PASS Last-Stable=V9.75.)
