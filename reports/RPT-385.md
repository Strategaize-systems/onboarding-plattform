---
id: RPT-385
date: "2026-06-02"
skill: /backend
slice: SLC-165
feature: FEAT-070
type: completion
status: completed
result: "MT-5 Worker `email_bulk_parse` Code-Side DONE — 3 new + 2 updated files, 11/11 Vitest GREEN, TSC + ESLint clean, 0 regression."
title: "OP V9 SLC-165 MT-5 Worker email_bulk_parse Code-Side"
---

# RPT-385 — V9 SLC-165 MT-5 Worker `email_bulk_parse` Code-Side DONE

## 1. Outcome summary

**Status:** fully complete (MT-5 Code-Side, frische Session per RPT-384-Recommendation).

Worker `email_bulk_parse` ist implementiert, in den Claim-Loop-Dispatcher verdrahtet, und mit 11 Vitest GREEN abgedeckt. Status-Maschine `uploaded → parsing → parsed/failed` + Crash-Recovery via Pre-Cleanup-DELETE + Batch-Insert 100er-Granularitaet. DI-Pattern fuer Testbarkeit. **3 Open Questions vorab geklaert per AskUserQuestion** — Batch-Strategie, Crash-Recovery, Storage-Read.

**SLC-165 jetzt 7/9 MTs done** (MT-0 + MT-1b + MT-2a + MT-2b + MT-3 + MT-4 + MT-5). MT-6 (Detail-View + RLS-Matrix) verbleibt + MT-1 (Test-Email-Corpus) ist User-Pflicht-blockiert.

## 2. Files reviewed (Pflicht-Reads)

- `/slices/SLC-165-v9-foundation-upload.md` (MT-5 Spec Lines 156-172)
- `/src/workers/condensation/claim-loop.ts` (Dispatcher-Source — Pattern-Erkenntnis: nicht `handle-job.ts`, sondern `claim-loop.ts` macht den Dispatch)
- `/src/workers/condensation/run.ts` (Worker-Entry-Point + startClaimLoop-Aufruf)
- `/src/workers/walkthrough/handle-transcribe-job.ts` (Pattern-Source: Status-Maschine + Storage-Download via service_role)
- `/src/workers/lead-push/handle-job.ts` (Pattern-Source: DI-Pattern fuer Testbarkeit)
- `/src/lib/bulk-email/parser.ts` (MT-3-API: parseMboxStream-Generator + parseEmlBuffer)
- `/src/lib/bulk-email/types.ts` (ParsedEmail-Schema-Mapping)
- `/src/lib/bulk-email/file-hash.ts` (MT-3 Hash-Funktion fuer Idempotenz-Kontext)
- `/src/lib/supabase/admin.ts` (AdminClient-Factory)
- `/src/lib/logger.ts` (captureException/Info/Warning-Interface)
- `/src/app/dashboard/bulk-email-import/actions.ts` (MT-4 Caller — payload-Format + JOB_TYPE-Konstante)
- `/src/app/dashboard/bulk-email-import/helpers.ts` (MT-4 Constants — Dual-Source-Hinweis)
- `/src/app/dashboard/bulk-email-import/__tests__/actions.test.ts` (Test-Pattern-Source — pure-mock vi.hoisted)
- `/sql/migrations/106_v9_bulk_email_schema.sql` (Lines 1-200: email_message-Spalten + bulk_run-Status-Werte)
- `/src/workers/walkthrough/__tests__/handle-transcribe-job.test.ts` (alternative Mock-Pattern verworfen zugunsten DI-Pattern)

## 3. Files changed

### UPDATED

- `/src/workers/condensation/claim-loop.ts` — `email_bulk_parse` zur JOB_TYPES-Konstante hinzugefuegt (Round-Robin-Polling sieht den Type). `emailBulkParseHandler?: JobHandler` als 15. Optional-Parameter. Dispatch-Branch `if (job.job_type === "email_bulk_parse" && emailBulkParseHandler)` nach lead_push_retry-Branch. **Warum**: V9-MT-5 braucht einen Dispatch-Pfad, claim-loop ist die kanonische Stelle.
- `/src/workers/condensation/run.ts` — Import `handleEmailBulkParseJob` aus `../bulk-email/handle-parse-job`. Console-Log `"email_bulk_parse handler registered"`. Als 15. Argument an `startClaimLoop()` durchgereicht. **Warum**: Worker-Entry-Point muss den Handler kennen, sonst greift der Dispatcher nicht.
- `/docs/STATE.md` — Current Focus auf MT-5 DONE umgestellt. **Warum**: per project-records-format.md MANDATORY nach jedem Slice-Schritt der State-Updaten.

## 4. Files created

- `/src/workers/bulk-email/job-types.ts` — 4 V9-Job-Type-Konstanten (`JOB_TYPE_EMAIL_BULK_PARSE` + 3 Pre-Declarations fuer SLC-166/167) + `BulkEmailJobType`-Union. **Purpose**: Pre-Declaration der naechsten 3 Job-Types damit SLC-166/167 nur ihre Worker hinzufuegen muessen, nicht diese Konstanten-Datei.
- `/src/workers/bulk-email/handle-parse-job.ts` — Worker-Handler mit `executeEmailBulkParse(job, deps)` Pure-Function (DI-Pattern fuer Tests, deps = `{adminClient, parseMbox?, parseEml?, batchSize?}`) + `handleEmailBulkParseJob(job)` Production-Wrapper mit Default-AdminClient. **Status-Maschine**: payload-validation → bulk_run-load → status-skip-wenn-!=uploaded → status='parsing' → pre-cleanup DELETE → storage-download → parse-loop-batch-INSERT → status='parsed' + email_count → rpc_complete_ai_job. Error-Path: status='failed' + failure_reason + re-throw fuer claim-loop rpc_fail_ai_job. **Purpose**: Hauptlogik des SLC-165 Worker-Layers.
- `/src/workers/bulk-email/__tests__/handle-parse-job.test.ts` — 11 Vitest mit DI-Pattern-Mocks: 2 Payload-Validation + 1 Bulk-Run-Load + 1 Status-Skip + 3 Happy-Path .mbox (150→2-Batch + Defense-vs-payload-tenant_id + Skipped-Emails-Path) + 1 Happy-Path .eml + 3 Failure-Paths (Storage / INSERT / Pre-Cleanup-DELETE). **Purpose**: Coverage fuer SLC-165 AC-9 (Worker funktional) + Spec-Statements (Skip-Logic, Crash-Recovery, Batch-Insert-Korrektheit).
- `/reports/RPT-385.md` (diese Datei) — Completion-Report per CLAUDE.md MANDATORY-Rule.

## 5. Files intentionally not changed

- `/src/app/dashboard/bulk-email-import/helpers.ts` — bewusst nicht refaktoriert zu Re-Export aus `job-types.ts`. **Begruendung**: helpers.ts ist sowohl von Server-Action (`actions.ts`) als auch von Client-Component (`BulkEmailUploadZone.tsx`) importiert. Re-Export aus `workers/`-Pfad in helpers.ts wuerde Server-only-Code in den Client-Bundle ziehen koennen (auch wenn pure-const-Re-Exports tree-shakeable sind, ist es ein Risiko). Dual-Source ist in job-types.ts-Header dokumentiert.
- `/slices/INDEX.md` — SLC-165 ist bereits `in_progress` (seit MT-0). Kein Status-Change noetig, da Slice noch nicht vollstaendig (MT-6 + MT-1-User-Pflicht offen).
- `/features/INDEX.md` — FEAT-070 ist bereits `in_progress`. Kein Status-Change noetig.
- `/planning/backlog.json` — BL-147 ist bereits `in_progress`. Kein Status-Change noetig.
- `/planning/roadmap.json` — V9 ist bereits `active`. Kein Status-Change noetig.
- `/docs/DECISIONS.md` — Die 3 Open-Question-Klaerungen (Batch-Strategie / Crash-Recovery / Storage-Read) sind keine architektur-relevanten neuen DECs, sondern Implementation-Choices innerhalb der existierenden DEC-185 (mailparser) + DEC-184 (V9.0 Pragmatic-Scope). Inline in handle-parse-job.ts-Kommentaren dokumentiert als DEC-MT-5-A/B/C. **Begruendung**: keine cross-slice-Implikationen, keine Architecture.md-Wirkung.
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues entdeckt.
- `/sql/migrations/106_v9_bulk_email_schema.sql` — kein Schema-Change in MT-5.

## 6. Problems found

**P-1 (Slice-Spec inaccuracy, Low):** Slice-Spec MT-5 referenziert `src/workers/handle-job.ts` als zu erweiternder Dispatcher. Tatsaechlich heisst die Datei `src/workers/condensation/claim-loop.ts` und dispatched per if/else-Chain. Spec wurde sinngemaess befolgt (Dispatcher-Branch ergaenzt), die Datei-Referenz im Spec ist veraltet. Kein neuer ISSUE — interne Spec-Drift, in der Implementation korrekt aufgeloest.

**P-2 (Test-Pattern-Drift Slice-Spec vs Repo-Realitaet, Low):** Slice-Spec MT-5 sagt "Vitest gegen Coolify-DB mit Mock-Storage". Repo-Pattern fuer Worker-Tests ist aber pure-mock vi.hoisted (siehe MT-4 actions.test.ts + walkthrough-Pattern). Coolify-DB-Tests sind im Repo nur fuer Migration-Schema-Asserts (MT-2a) + ggf. RLS-Pen-Tests. MT-5 folgt Repo-Realitaet (pure mocks). Coverage trotzdem gegeben fuer alle Status-Transitions + Failure-Paths.

**P-3 (Dual-Source Konstante, Low):** `JOB_TYPE_EMAIL_BULK_PARSE` existiert in 2 Files (`helpers.ts` und `job-types.ts`). Drift-Risiko theoretisch vorhanden, aber durch Kommentar in `job-types.ts` und Code-Konstanten-Charakter (String-Literal) gering. Mitigation: bei Aenderung beide Dateien gemeinsam editieren — String-Literal kommt auch in claim-loop.ts JOB_TYPES-Array vor (dort als Inline-String, 3. Stelle).

**P-4 (Vitest Full-Suite 435 FAIL alle pre-existing, dokumentiert):** Die 435 Failures sind alle Coolify-DB-Tests, die `TEST_DATABASE_URL` brauchen (per `.claude/rules/coolify-test-setup.md`-Pattern). 1301 PASS-Wert matched Baseline-1290 + 11 neue MT-5-Tests. **0 Regression**.

## 7. Open points or deferred decisions

- **MT-6 verbleibt**: Detail-View `src/app/dashboard/bulk-email-import/[run_id]/page.tsx` + komplette RLS-Test-Matrix `__tests__/rls/v9-bulk-email.rls.test.ts` (16+ Pen-Test-Cases). Geschaetzt ~3-4h.
- **MT-1 User-Pflicht**: Test-Email-Corpus (~100 anonymisierte .mbox-Emails) durch Founder bereitzustellen fuer Cost-Validation gegen Discovery-Schaetzung (Faktor-2-Heuristik). Blockt nicht MT-6.
- **Live-Smoke-Verifikation MT-5**: deferred bis MT-6 fertig + Master-Merge + User-Coolify-Redeploy. Coolify-Worker-Container wird nach Redeploy den neuen Job-Type pollen — Smoke-Pfad: Upload .mbox → MT-4-Action enqueued ai_jobs → Worker pickt up → email_message-Rows persistiert → bulk_run.status='parsed'. **Achtung**: Worker-Container braucht volle V9-Migration-LIVE-Apply (MT-2b PASS — RPT-382) + neue Coolify-Deploy mit MT-5-Code. ENV `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`AWS_REGION` sind bereits gesetzt (von V5/V7/V8-Workern).
- **Auto-Restart-on-Crash**: V9.0 nicht implementiert (Stale-Cleanup-Cron ist V9.1+, per Slice-Spec R4). Bei Worker-Crash mid-loop bleibt `bulk_run.status='parsing'` haengen — Pre-Cleanup-DELETE entfernt partielle email_messages beim Re-Try, aber kein automatischer Re-Try. Manuelle Wiederholung: erneuter ai_jobs-INSERT + Worker-Pick-Up.

## 8. Project record updates

- `/docs/STATE.md` — Current Focus auf MT-5 DONE umgestellt, MT-6 als naechster Schritt.
- `/slices/INDEX.md` — keine Aenderung (SLC-165 bleibt `in_progress`, da MT-6 + MT-1 offen).
- `/features/INDEX.md` — keine Aenderung (FEAT-070 bleibt `in_progress`).
- `/planning/backlog.json` — keine Aenderung (BL-147 bleibt `in_progress`).
- `/planning/roadmap.json` — keine Aenderung (V9 bleibt `active`).
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues, keine Resolution.
- `/reports/RPT-385.md` — diese Completion-Report-Datei.

## 9. QA after this step

Per CLAUDE.md MANDATORY: `/qa SLC-165 MT-5` ist der naechste Pflicht-Schritt. Inhaltlich:
- Stub-Detection ueber 3 NEUE Files (job-types.ts, handle-parse-job.ts, handle-parse-job.test.ts)
- Wiring-Audit: claim-loop.ts dispatch-branch + run.ts pass-through
- AC-SLC-165-9 Pruefung (1000 Emails in <5min Worker-Zeit — Live-Smoke deferred bis MT-6 + Test-Corpus)
- Risk-Audit: Payload-tenant_id-Injection (Test 4 deckt das ab), Crash-Recovery-Idempotenz, RLS-Bypass-via-service_role (intentional, Worker-Path)
- Code-Quality: Logger-Usage konsistent, Error-Re-Throw-Pattern matched lead-push, Status-Maschine-Coverage komplett

## 10. Recommended next step

**Option A (empfohlen):** `/qa SLC-165 MT-5` Code-Side direkt im selben Worktree. Kurz, fokussiert auf MT-5-Diff. Pflicht per CLAUDE.md nach jedem /backend-Step.

**Option B:** `/backend SLC-165 MT-6` Detail-View + RLS-Matrix direkt (~3-4h, frische Session empfohlen wegen Context-Pressure). QA fuer MT-5 dann als Teil der Gesamt-/qa SLC-165 nach MT-6.

**Option C (Alternative):** Cross-Repo-Wechsel — BS V8.7-A Burn-In bis 2026-06-03 ~11:31 UTC (keine Aktion noetig), oder ImSch V3.2 SLC-321 /qa Code-Side (Worktree HEAD `4544dcf`).

**Empfehlung A** weil: (1) Spec-Conformance pruefen vor MT-6 reduziert Compound-Risk, (2) Stub-Detection auf 3 neue Files ist schnell (<30min), (3) MT-6 ist deutlich groesser (UI + 16+ RLS-Tests) — saubere MT-5-Baseline vorab spart spaeter Debugging.

## Quality gates summary

| Gate | Result |
|---|---|
| TSC `--noEmit` (Repo-weit) | EXIT=0 |
| ESLint (5 MT-5-Files) | EXIT=0 |
| Vitest (MT-5-File alleine) | 11/11 PASS in 22ms |
| Vitest (Full-Suite Offline) | 1301 PASS / 435 deferred-FAIL (pre-existing) |
| 0 Regression vs MT-4-Baseline | ✓ (1290 + 11 = 1301) |
| Pattern-Reuse BLOCKING erfuellt | ✓ (DI-Pattern aus lead-push + Status-Maschine aus walkthrough) |
| 3 Open Questions vorab geklaert | ✓ (DEC-MT-5-A/B/C inline dokumentiert) |
