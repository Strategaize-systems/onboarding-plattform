---
id: RPT-459
date: "2026-06-12"
skill: /backend
slice: SLC-V9.5-C
feature: FEAT-081
type: completion
status: completed
reviewOf: null
result: "Bounded-Critic-Gate implementiert: critiqueUnits Pure-Fn + Worker-Critic-Phase + MIG-112 (LIVE). Gates GREEN."
title: "SLC-V9.5-C Bounded-Critic /backend — critiqueUnits + Worker-Integration + MIG-112"
---

# SLC-V9.5-C Bounded-Critic — /backend Completion Report

## Outcome

Vollstaendig. Der bounded Critic-Pass (DEC-216) ist als zweite LLM-Phase im
`email_bulk_synthesis`-Worker implementiert — genau 1 Sonnet-Call pro Run ueber
alle Draft-Units, Verdict `KEEP`/`REJECT` + Begruendung, Worker-Filter strict
`KEEP && evidence_count >= 2` (AC-C-2). Additiv am in SLC-V9.5-B vorbereiteten
`selectSurvivingUnits`-Hook, kein Rewrite.

## R-C-3-Befund (Slice-Spec-Korrektur)

Slice-Spec nahm an, `ai_cost_ledger.role` sei freitext ("kein CHECK-Add noetig
— im /backend verifizieren"). Live-Verify (`pg_get_constraintdef` gegen
Coolify-DB) zeigte einen CHECK mit 20 Werten (MIG-111-Stand) → **MIG-112**
(`sql/migrations/120_v95_critic_role.sql`) noetig: Rebuild vom Live-Stand
(IMP-1228) + `email_bulk_critic` = 21 Werte. **LIVE applied + verifiziert**
(base64+psql -U postgres, sql-migration-hetzner.md). KEIN `ai_jobs.job_type`-Add
— der Critic laeuft im selben Synthese-Job (job_id = Synthese-Job-ID).

## MT-1 — Critic Pure-Function (Commit 585774d)

- `/src/lib/ai/bedrock-sonnet/email-critic-prompt.ts` (NEU): `V95_CRITIC_SYSTEM_PROMPT`
  (ARCH §7, konservativ per R-C-1: REJECT nur bei klarem Trivial / Halluzination /
  Redundanz / evidence_count<2, im Zweifel KEEP) + `buildCriticUserPrompt`
  (Units mit explizitem 0-basiertem `unit_ref`) + `CriticInputUnit`-Interface
  (mit REKONZILIIERTER evidence_count) + Versions-Anker.
- `/src/lib/ai/bedrock-sonnet/email-critic.ts` (NEU): `critiqueUnits(units, options?)`
  — Struktur 1:1 analog `email-synthesis.ts` (Pattern-Reuse): eu-central-1
  hardcoded, eu-Sonnet-4-Default + ENV `BEDROCK_V9_SONNET_MODEL_ID`,
  Codeblock-Strip, zod-Parse, `SonnetSchemaError`-Reuse, Test-Injection-Hooks.
- `/src/lib/ai/bedrock-sonnet/types.ts` (GEAENDERT): `CriticVerdictSchema` /
  `CriticVerdictsSchema` (`unit_ref` int>=0, `verdict` enum KEEP|REJECT,
  `reason` 1..500) + Types.
- `/src/lib/ai/bedrock-sonnet/index.ts` (GEAENDERT): Barrel-Exports.
- `/src/lib/ai/bedrock-sonnet/__tests__/email-critic.test.ts` (NEU): 7 Tests
  (TDD RED→GREEN): valid-Parse, invalid-JSON, Schema-Drift (verdict "MAYBE"),
  Region eu-central-1, Verdict-Index-Mapping, Codeblock-Strip, Prompt-Inhalt.

## MT-2 — Worker-Integration (Commit cf52974)

- `/sql/migrations/120_v95_critic_role.sql` (NEU): MIG-112, s.o. LIVE.
- `/src/workers/bulk-email/handle-synthesis-job.ts` (GEAENDERT):
  - Neuer DI-Hook `deps.critic` (Default `critiqueUnits`), Typ `UnitCritic`.
  - Schritt 9 (neu): bei >0 Drafts GENAU 1 Critic-Call (AC-C-4; 0 Drafts → 0 Calls);
    Critic-Input projiziert mit rekonziliierter evidence_count;
    9a Cost-Akkumulation in `synthesis_cost_eur`; 9b Ledger-INSERT role
    `email_bulk_critic` (non-fatal); 9c Live-Total-Cap-Check NACH Critic —
    Cap-Hit → `status='failed'` + complete, KEIN Persist (R-C-2);
    9d Verdict-Map (out-of-range ignoriert+geloggt, Duplikat first-wins).
  - Critic-Throw (SonnetSchemaError/Bedrock-Error) ist Run-blocking → outer
    try/catch → `status='failed'`, kein Persist un-kritisierter Units.
  - `selectSurvivingUnits` finalisiert: strict gdw `verdict===KEEP && evidence>=2`
    (fehlendes Verdict = drop, AC-C-2); Reduktions-Statistik via captureInfo
    (drafts/surviving/rejected_by_critic/no_verdict/dropped_by_evidence).
- `/src/workers/bulk-email/__tests__/handle-synthesis-job.test.ts` (GEAENDERT):
  Critic-Stubs (`makeCritic`/`keepAllCritic`), Bestands-Tests auf KEEP-alles-Stub,
  Ledger-Assertion 1→2 Rows (beide Rollen), 8 neue Critic-Gate-Tests (C1..C8)
  + 1 neuer Hook-Test (strict-Semantik). 23/23 GREEN.

## Quality-Gates (AC-C-5)

- tsc `--noEmit` = 0, ESLint (alle 7 Touched-Files) = 0.
- Targeted: email-critic 7/7 + handle-synthesis-job 23/23 GREEN.
- Full-Suite: 1847 pass / 516 DB-gated-fail / 36 skipped — **0 Regression**
  (3 nicht-DB-Fails via git-stash-Gegenprobe als pre-existing ENV-gated bewiesen:
  diagnose/actions 2 + service-key-timing 1).

## Files

- Created: `/src/lib/ai/bedrock-sonnet/email-critic.ts`,
  `/src/lib/ai/bedrock-sonnet/email-critic-prompt.ts`,
  `/src/lib/ai/bedrock-sonnet/__tests__/email-critic.test.ts`,
  `/sql/migrations/120_v95_critic_role.sql`
- Modified: `/src/lib/ai/bedrock-sonnet/types.ts`,
  `/src/lib/ai/bedrock-sonnet/index.ts`,
  `/src/workers/bulk-email/handle-synthesis-job.ts`,
  `/src/workers/bulk-email/__tests__/handle-synthesis-job.test.ts`
- Intentionally unchanged: `/src/workers/condensation/run.ts` (Dispatcher —
  Critic laeuft im bestehenden Handler, kein neuer job_type),
  `/src/lib/ai/bedrock-sonnet/email-synthesis.ts` (Synthese-Phase unveraendert,
  Out-of-Scope), `/sql/migrations/119_v95_synthesis_stage.sql` (MIG-111 bleibt
  as-applied; Erweiterung als eigene MIG-112 statt Nachtraeglich-Edit).

## Problems found

- R-C-3 real: role-CHECK existiert (Spec-Annahme falsch) → MIG-112 (s.o., geloest).
- Strict-Verdict-Semantik (fehlendes Verdict = drop) ist eine bewusste
  AC-C-2-gdw-Auslegung; Hook-Semantik aus SLC-V9.5-B (nur REJECT filtert)
  entsprechend finalisiert inkl. Test-Update.

## Open points

- Echter Bedrock-Critic-Live-Smoke (End-to-End-Run + Reduktions-Quote auf
  Real-Korpus) Founder-gated → /deploy (analog SLC-V9.5-A/B).
- Branch `v9-5-bulk-deep-extraction` NICHT gepusht/gemergt — EIN Master-Merge
  nach Gesamt-/qa (SLC-V9.5-D MT-5).

## Next step

`/qa SLC-V9.5-C` (RPT-460) — danach `/backend SLC-V9.5-D` Curation-Contract-Shift.
