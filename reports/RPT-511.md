---
id: RPT-511
date: "2026-06-21"
skill: /qa
slice: SLC-169
feature: FEAT-091
type: completion
status: completed
reviewOf: RPT-510
result: "PASS — 16/16 DB-Sidecar-Tests GREEN gegen Coolify-DB, 0 Findings. R-169-1 final geschlossen (CHECK-Arrays als strikte LIVE-Superset direkt verifiziert)."
title: "QA SLC-169 — modul_output-Schema (MIG-124): DB-Sidecar gegen Coolify-DB"
---

# QA SLC-169 — StB Modul-Domaene-Schema (MIG-124)

## 1. QA-Scope reviewed

Verifikation der /backend-Lieferung SLC-169 (RPT-510): `sql/migrations/124_v10_stb_modul_domain.sql` (modul_output-Tabelle + Indizes + RLS + GRANTs; fn_min_tier_for_job-Erweiterung; ai_jobs.job_type/ai_cost_ledger.role CHECK-Rebuild; rpc_enqueue_module_output; NOTIFY pgrst) + 2 DB-Sidecar-Tests. Verfahren: node:20-Sidecar (pure `pg`) gegen die Live-Coolify-DB (`coolify-test-setup.md`), Migration self-applied in gerollbackter Test-Transaktion. Pure-Backend-Schema-Slice → Wiring-Verifikation N/A (kein Frontend).

## 2. Ergebnis: **PASS**

- **16/16 Tests GREEN** (1.05s): 9 in `migration-124-modul-output.test.ts`, 7 in `migration-124-enqueue-rpc.test.ts`.
- Lauf: `node:20` im Netz `bwkg80w04wgccos48gcws8cs_strategaize-net` gegen `supabase-db-bwkg80w04wgccos48gcws8cs-160450719820`.

### Abgedeckte Akzeptanzkriterien
- **AC-169-1:** Tabelle + Spalten/Nullability + 4 CHECKs (output_kind/status/source/reifegrad) + 3 Indizes; 2× Apply = 0 Drift (idempotent). ✓
- **AC-169-2:** RLS aktiv; tenant_admin A↔B isoliert (je 1 Row); strategaize_admin cross-tenant (2 Rows); tenant_member liest eigenen Tenant; tenant_admin UPDATE eigene Row (1), Fremd-Row 0 Rows (USING-Filter); authenticated INSERT denied (kein GRANT/Policy). ✓
- **AC-169-3:** rpc_enqueue_module_output → blueprint-Session: status='pending'-Job + session_tier='blueprint' + payload korrekt; idempotenter Re-Enqueue = selber job_id (count=1); free-Tier → `tier_gate_denied`; Fremd-Session → Ownership-Deny. ✓
- **AC-169-4:** fn_min_tier_for_job('module_output_synthesis')='blueprint'; CHECK akzeptiert neue job_type-/role-Werte. ✓
- **AC-169-5:** NOTIFY pgrst im Migrations-Ende (statisch verifiziert). ✓
- **AC-169-6:** tsc 0 / eslint 0 / next build PASS (RPT-510) + DB-Sidecar GREEN. ✓

## 3. Findings nach Severity

Keine. (0 Blocker / 0 High / 0 Medium / 0 Low-blockierend.)

### R-169-1 (BLOCKING-Risiko) — final geschlossen
Die Tests prüfen CHECK-Erweiterung nur via `toContain` (neue Werte vorhanden), NICHT die Erhaltung aller LIVE-Werte — genau die IMP-1228-Lücke. Daher manueller Direkt-Diff `pg_get_constraintdef` der LIVE-Constraints gegen die Migrations-Arrays:
- `ai_jobs_job_type_check`: alle **22** LIVE-Werte erhalten + `module_output_synthesis` = 23. Kein LIVE-Wert gedroppt.
- `ai_cost_ledger_role_check`: alle **21** LIVE-Werte erhalten + `module_output_synthesis` + `module_output_critic` = 23. Kein LIVE-Wert gedroppt.
Die Migrations-Arrays sind strikte Superset der LIVE-Constraints. Risiko ausgeräumt.

### Stub-Scan
Reine SQL-Migration + Test-Files, keine Produktiv-TS-Änderung. 0 Stubs. Kein TODO/FIXME/Placeholder-Return/Dummy-Data in Produktionspfaden.

### DEC-241 bestätigt
AC-169-3-Literal `status='queued'` wurde im /backend auf `status='pending'` korrigiert (LIVE-CHECK kennt 'queued' nicht + claim-Loop claimt 'pending'). Test beweist status='pending' → korrekt. Deviation als richtig bestätigt.

## 4. Offene Risiken / Test-Confidence-Limits
- **Untestete Rolle `tenant_owner`** in den RLS-Read/Update-Policies (Tests decken tenant_admin/member/strategaize_admin ab). Akzeptiert — spiegelt bestehende OP-RLS-Rollen-Konvention; kein Regressions-Risiko, da additive Tabelle.
- **Test-Robustheit (Low, observational):** CHECK-Assertions prüfen nur Anwesenheit neuer Werte, nicht Vollständigkeit der LIVE-Liste. In diesem /qa manuell geschlossen; künftig könnte ein `toEqual`-Snapshot der vollen Wertemenge die Lücke automatisieren. Nicht-blockierend für SLC-169.
- **R-169-2 (für /deploy):** MIG-124 muss LIVE-Apply VOR Worker-Code-Redeploy (SLC-174). Im /deploy zu beachten, nicht /qa-blockierend.

## 5. Readiness-Assessment
SLC-169 ist Code-Side + DB-Side vollständig verifiziert und merge-bereit. Schema/RLS/RPC/Tier/CHECK verhalten sich gegen die echte Produktionsschema-DB korrekt; alle Abhängigkeiten (fn_tier_allows, auth.user_role/user_tenant_id, capture_session.tier, handle_new_user-Trigger, ai_jobs_session_tier_insert_guard) existieren live und greifen. Noch NICHT in main gemergt.

## 6. Nächster Workflow-Schritt
**Pre-Merge-Re-Check + Master-Merge** (`git-release.md`): `git fetch && git rebase origin/main` (main = Branch-Base `f084eda`, Rebase erwartet no-op) → Tests post-rebase → MIG-124-Collision-Check → manueller Diff-Review → Merge `v10-slc169-modul-schema`→main. Danach **/backend SLC-170** (MIG-125 Template-Seed) als nächster V10-Slice (Group A). Gesamt-/qa + /final-check + /go-live + /deploy + /post-launch folgen nach Abschluss aller V10-Slices.
