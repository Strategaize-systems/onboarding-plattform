---
id: RPT-247
date: "2026-05-14"
skill: /qa
slice: SLC-106
feature: FEAT-046
type: completion
status: completed
reviewOf: RPT-246
result: "Quality-Gates Full PASS — Stub 0, ESLint 0/0, tsc 0, Build PASS, Vitest 88/987, Schema-Drift OK"
title: "V6 SLC-106 MT-6 — /qa Quality-Gates Full nach Worker-Implementation"
---

## QA-Scope reviewed

QA-Run gegen V6 SLC-106 MT-6 (Worker `lead_push_retry` + Migration 092a Live).
Scope:
- 2 neue Production-Files: `src/workers/lead-push/handle-job.ts` (393 Zeilen),
  `src/workers/condensation/run.ts`-Diff (+3 Zeilen Handler-Wiring)
- 1 modifiziertes File: `src/workers/condensation/claim-loop.ts` (+10 Zeilen
  JOB_TYPES + Dispatcher-Branch + Handler-Slot)
- 1 neue Test-File: `src/workers/lead-push/__tests__/handle-job.test.ts` (404 Zeilen, 6 Vitest)
- 1 neue Migration: `sql/migrations/092a_ai_jobs_claim_scheduled_at_filter.sql` (LIVE)

QA-Gates: Stub-Detection, ESLint volltree, tsc volltree, npm run build (dummy
ENV), npm audit --omit=dev, Vitest Full-Regression gegen Coolify-DB, Schema-
Drift-Check fuer rpc_claim_next_ai_job_for_type (Mandatory-Post-Migration-Smoke
per IMP-507).

## Result

**PASS.** Alle 7 Quality-Gates gruen. 0 Blocker / 0 High / 0 Medium / 0 Low NEU.
5 pre-existing residual Vulns (alle pre-V6 carry-over, AC-16 erfuellt). Worker-
Schicht ist production-ready code-side. Cross-System-Smoke (MT-12) bleibt
User-Pflicht.

## Quality Gates

### Gate 1 — Stub-Detection auf MT-6 Production-Files
- `src/workers/lead-push/handle-job.ts` (393 Zeilen): 0 Findings.
- `src/workers/condensation/claim-loop.ts` Diff (Dispatcher-Branch +
  JOB_TYPES + Handler-Slot): 0 Findings.
- `src/workers/condensation/run.ts` Diff (Handler-Registrierung): 0 Findings.
- Test-File `handle-job.test.ts` enthaelt erwartungsgemaess Dummy-UUIDs +
  Mock-Returns — Test-Datei-Standard, kein Production-Stub.

Pattern gescannt: `TODO`, `FIXME`, `HACK`, `XXX`, `console.log`, `debugger`,
`return null;`, `return [];`, `return {};`, `catch (...) {}`. **Production-
Files clean.**

### Gate 2 — ESLint volltree
`npx eslint src --max-warnings=0` EXIT=0. **0/0.**

### Gate 3 — TypeScript
`npx tsc --noEmit` volltree EXIT=0. **0 Type-Errors.**

### Gate 4 — Next.js Production Build
`npm run build` mit Test-Stubs fuer SUPABASE/JWT/BUSINESS_SYSTEM_INTAKE EXIT=0.
53 Routen gerendert. Worker-Code ist server-only und wird im Worker-Container
gebaut (separate Build, ENV-Validierung in `validateEnv()` in
`src/workers/condensation/run.ts`).

### Gate 5 — npm audit
`npm audit --omit=dev`: 5 Vulns (1 low, 2 moderate, 2 high) — **alle pre-existing**:
- next < 15.5.7 (pre-V6 carry-over)
- next-intl <= 4.9.1 (pre-V6 carry-over)
- postcss < 8.5.10 (pre-V6 carry-over, ISSUE-026)

**0 SLC-106-MT-6-induced Vulns** (MT-6 fuegt keine npm-Deps hinzu). AC-16
erfuellt.

### Gate 6 — Vitest Full-Regression gegen Coolify-DB
node:20 Docker-Container im selben Netzwerk wie
`supabase-db-bwkg80w04wgccos48gcws8cs-151532171639`:

```
Test Files  88 passed (88)
Tests       987 passed | 14 skipped | 2 todo (1003)
Duration    5.66s
```

**SLC-106-MT-6-spezifische Tests (alle PASS):**
- `handle-job.test.ts` (Worker) — 6 Tests in <100ms (Happy / Retry-2 / Retry-3 /
  Max-Attempts / Backoff-Schedule + Idempotenz).
- `lead-intake.test.ts` (MT-3) — 7 Tests.
- `build-notes.test.ts` (MT-4) — 3 Tests.
- `lead-push-actions-db.test.ts` (MT-5) — 8 DB-Contract Tests.

Pen-Test-Regression `v6-partner-rls.test.ts` 102 Tests (100 PASS + 2 todo).
Keine Regression in V4/V5/V5.1 Pen-Test-Matrizen. Identisch zur Pre-MT-6
Baseline aus RPT-245 (87 Files / 981 PASS) bis auf das neue Worker-Test-File
(+1 File, +6 PASS).

### Gate 7 — Schema-Drift-Check (IMP-507 Mandatory-Post-Migration-Smoke)
`SELECT n.nspname, p.proname, p.prosecdef FROM pg_proc p JOIN pg_namespace n
ON p.pronamespace = n.oid WHERE p.proname = 'rpc_claim_next_ai_job_for_type';`

```
 nspname |            proname             | prosecdef
---------+--------------------------------+-----------
 public  | rpc_claim_next_ai_job_for_type | t
(1 row)
```

**Genau eine Zeile mit `nspname=public` + `prosecdef=t` (SECURITY DEFINER
preserved). Keine `storage`-Geist-Funktion.** Function-Body enthaelt das
`scheduled_at`-Filter-Snippet (per `pg_get_functiondef LIKE '%scheduled_at%'`
verifiziert).

## AC-Mapping (MT-6 Scope)

| AC | Beschreibung | Status MT-6 | Anmerkung |
|----|---|---|---|
| AC-7 | BS-Downtime: status='failed' + Retry-Job | PASS code-side | Retry-Enqueue-Pfad in Worker funktioniert (Test 2). |
| AC-8 | Retry-Job laeuft erfolgreich nach BS-Recovery | PASS code-side | Worker zieht den Job nach scheduled_at-Faelligkeit (Live-Smoke RPT-246 + Test 1 Happy). Live-Smoke = MT-13. |
| AC-9 | Max 3 Versuche / Backoff 5min+30min | PASS | `nextBackoffMs(1)=5min`, `nextBackoffMs(2)=30min` (Test 5). Cap bei attempt==3 (Test 3) + Safety bei attempt>3 (Test 4). |
| AC-10 | Bedrock-Cost-Audit unbeeinflusst | PASS | Worker macht KEINEN KI-Call (verifiziert per Code-Inspect — `pushLeadToBusinessSystem` ist HTTP-only). |
| AC-13 | Idempotenz | PASS | audit.status='success'-Skip-Branch im Worker (Bonus-Test). |
| AC-16 | ESLint 0/0 + Build PASS + Vitest gruen + 0 neue Vulns | PASS | Alle 7 Gates. |

**6 von 6 in-Scope ACs erfuellt** (AC-1..6, AC-11..12, AC-14..15 sind MT-1..MT-5
oder MT-7+ Scope und nicht Teil dieses Gates).

## Wiring-Verification

Worker-Chain steht und ist live in Postgres:

```
MT-5 requestLeadPush (fail) 
  → INSERT ai_jobs lead_push_retry mit payload.scheduled_at=now+5min  ✓ (MT-5)
  → claim-loop polls rpc_claim_next_ai_job_for_type('lead_push_retry')  ✓ (Migration 092a)
  → Filter `payload.scheduled_at <= now()` aktiv  ✓ (Live-Smoke RPT-246)
  → Dispatcher → leadPushRetryHandler  ✓ (claim-loop.ts:120)
  → executeLeadPushRetry  ✓ (handle-job.ts:78)
  → Load audit + consent + Build Payload + Push  ✓ (Test 1 Happy)
  → markAuditSuccess | (markAuditFailed + enqueue) | (markAuditFailed final)  ✓ (Test 1/2/3)
  → rpc_complete_ai_job  ✓ (all Tests + handle-job.ts:213)
```

Run-Entry (`run.ts`) registriert den Handler als 15. Position im
startClaimLoop-Call. Worker-Container muss redeployt werden, damit der neue
Handler im Production-Worker-Loop landet — User-Pflicht zusammen mit MT-11/12
ENV-Setup + Cross-System-Smoke.

## Findings

### Blocker
keine.

### High
keine.

### Medium
keine.

### Low
keine NEU. Carry-over aus RPT-244+245:
- **ISSUE-051** profiles fehlen first_name/last_name fuer Lead-Push-Payload
  (V6.1-Polish). Worker faellt auf `auth.users.user_metadata` (first_name /
  last_name / given_name / family_name / full_name / name) oder Email-
  Lokalteil zurueck — funktional gleich wie MT-5.
- **P-3 (RPT-244)** loadDiagnoseSummary best-effort: Reifegrad-Score-
  Aggregation aus `block_diagnosis.content.subtopics[*].fields.reifegrad` ist
  template-schema-abhaengig. Bei fehlendem Feld faellt der Strukturtext auf
  2 Saetze zurueck — funktional ok. V6.1-Polish-Kandidat.

### Process-Findings (Dev-System-Lehre erfasst)
- **IMP-507** (search_path-Falle bei `CREATE FUNCTION` in self-hosted Supabase):
  Erster Apply 092a landete in `storage`-Schema statt `public`. Reparatur mit
  explizitem `public.`-Qualifier + `DROP FUNCTION IF EXISTS storage.<name>`.
  Lehre + Mandatory-Post-Migration-Smoke in
  `strategaize-dev-system/docs/SKILL_IMPROVEMENTS.md` (commit f71dea7).
  Dieses QA-Gate hat genau den Smoke-Check verifiziert: 1 Zeile in `public`,
  0 Zeilen in `storage`, `prosecdef=t`.

## Open risks / missing checks

- **MT-7 Modal-UI** offen — ohne Modal ist die Server-Action `requestLeadPush`
  unbenutzt. Wiring komplettiert sich erst mit MT-7.
- **MT-9 Pen-Test-Aktivierung** auf `lead_push_consent` + `lead_push_audit`
  noch offen (2 `it.todo` in `v6-partner-rls.test.ts`, AC-15 PARTIAL).
- **MT-11 ENV-Setup** (`BUSINESS_SYSTEM_INTAKE_URL` + `BUSINESS_SYSTEM_INTAKE_API_KEY`
  fuer app + worker) — User-Pflicht. Worker-Container braucht zusaetzlich
  ein Redeploy fuer die neue Handler-Registrierung.
- **MT-12 Cross-System-Smoke** (Onboarding → Business-System) — User-Pflicht.
- **MT-13 Retry-Pfad Live-Smoke** — User-Pflicht (optional).
- **Worker-Container-Redeploy** noetig fuer Aktivierung des neuen Handlers in
  Production (sonst dispatcht der laufende Worker den `lead_push_retry`-Job
  in den Fallback-Handler, was als Fail markiert wird).
- **Pre-existing Audit-Vulns** (next/next-intl/postcss) bleiben Pre-Production-
  Compliance-Gate-Scope, nicht V6-Slice-Block.

## Readiness assessment

**MT-6 code-side ready fuer MT-7.** Worker-Schicht ist quality-gates-validiert,
hat 6 dedizierte Vitest mit DI-Pattern, Migration 092a ist live + Schema-
Drift-frei + Live-RPC-Smoke-bestaetigt (RPT-246). Keine Blocker, keine
offenen Code-Probleme im MT-6-Scope. Das einzige offene Element ist der
Worker-Container-Redeploy + Coolify-ENV-Setup, was User-Pflicht ist und im
MT-11 angesiedelt ist (vor MT-12 Cross-System-Live-Smoke).

## Recommended next step

**Option A (empfohlen):** Session-Handoff. Context-Druck nach MT-1..MT-6 + RPT-
244/245/246/247 (4 Reports in einer Session) ist moderat-hoch. MT-7 ("Ich will
mehr"-Modal) ist eine reine Frontend-Aufgabe mit Modal-Komponente + Form +
useTransition — passt sauber in eine neue, frische Session mit klarem
Resume-Point.

**Option B (falls Context laeuft):** Direkt `/frontend SLC-106 MT-7` —
"Ich will mehr"-Modal mit Pflicht-Checkbox (DEC-091-Pattern aus SLC-079) +
Einstiegspunkt im Diagnose-Bericht (Hinweis: BL-095 Inhalts-Workshop blockiert
SLC-105 Bericht-Page; MT-7 koennte temporaer einen Test-Einstiegspunkt im
Mandanten-Dashboard nutzen, der spaeter in MT-8 als Status-Card umgeformt wird).

**Option C:** User-Pflicht-MTs vorziehen — MT-11 ENV-Setup auf Coolify
(BUSINESS_SYSTEM_INTAKE_URL + BUSINESS_SYSTEM_INTAKE_API_KEY) + Worker-Redeploy.
Ohne ENVs wirft der Worker `BUSINESS_SYSTEM_INTAKE_URL or BUSINESS_SYSTEM_INTAKE_API_KEY
not configured` und die Retry-Logik laeuft nicht. Damit wird MT-6 Worker
produktiv aktivierbar bevor MT-7 die UI-Schicht draufpackt.

## Project record updates

- `/reports/RPT-247.md` — NEU (dieser Report).
- `/slices/INDEX.md` — SLC-106 bleibt `in_progress` (6/13 MTs done).
- `/planning/backlog.json` — BL-093 V6 LEAD_PUSH bleibt `in_progress`.
- `/docs/STATE.md` — Current Focus auf MT-6 /qa PASS aktualisiert.
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues. Alle 0 Findings.
