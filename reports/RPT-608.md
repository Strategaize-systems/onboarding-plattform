---
id: RPT-608
date: "2026-07-08"
skill: /backend
slice: SLC-189
feature: FEAT-106
type: completion
status: completed
reviewOf: null
result: "SLC-189 Backend+UI implementiert: createBerater (Invite ohne tenant_id) + assign/unassignBerater + /admin/berater UI + Sidebar-NAV. 13 Pure-Mock-Tests grün, tsc/eslint/next build PASS. DB-Contract-Test deferred bis MIG-132 live (/deploy)."
title: "SLC-189 /backend — Admin: Berater anlegen + Tenants zuweisen"
---

# SLC-189 /backend — Admin: strategaize_berater anlegen + Tenants zuweisen

## Outcome summary
Vollständig implementiert (Code-Seite). Alle 4 Micro-Tasks umgesetzt: Berater-Anlage-Action (Invite ohne tenant_id), Zuweisungs-Actions (set/unset), Admin-UI `/admin/berater`, Action-Tests. Done-Gate SLC-189 erfüllt: tsc 0 / eslint 0 / 13 Pure-Mock-Vitest grün / `next build` PASS (Route `/admin/berater` im Manifest). DB-Integrationstest bewusst deferred, weil MIG-132 erst im /deploy live appliziert wird (Tabelle `berater_tenant_assignments` existiert live noch nicht).

## Files reviewed
- `slices/SLC-189-admin-berater-anlage-zuweisung.md` (Slice-Spec, Grounding-Gates)
- `src/app/api/admin/tenants/[tenantId]/invite/route.ts` (Port-Quelle generateLink)
- `src/lib/workspace/admin-gate.ts` (assertStrategaizeAdmin, Reuse)
- `src/lib/supabase/admin.ts` (createAdminClient)
- `src/lib/email.ts` (sendInviteEmail-Signatur)
- `src/lib/logger.ts` (captureException/captureInfo-Signaturen)
- `src/app/admin/partners/actions.ts` + `page.tsx` + `[partner_tenant_id]/InvitePartnerAdminForm.tsx` (Server-Action-Gate + Liste + Native-Form-Pattern)
- `src/lib/workspace/berater-gate.test.ts` (vi.mock-Muster)
- `sql/migrations/132_v104_berater_foundation.sql` (Schema-Grounding `berater_tenant_assignments`)
- `src/types/db.ts` (TenantKind — direct_client/partner_organization/partner_client)

## Files created (5)
- `src/app/admin/berater/actions.ts` — Server-Actions `createBerater` (Invite type=invite ohne tenant_id, role=strategaize_berater, eigener verifyUrl + SMTP), `assignBerater` (idempotenter upsert onConflict PK), `unassignBerater` (delete per PK). Gate `assertStrategaizeAdmin` in jeder Action. `'use server'`: nur async Exports + `ActionResult`-Type.
- `src/app/admin/berater/page.tsx` — Server-Page, strategaize_admin-Re-Gate (redirect), lädt Berater (`profiles` role=strategaize_berater), zuweisbare Tenants (`tenant_kind IN (partner_organization, direct_client)` — Mandanten via Cascade ausgeschlossen, DEC-268) + bestehende Assignments via `createAdminClient`.
- `src/app/admin/berater/BeraterAdmin.tsx` — Client-Component: „Berater anlegen"-Form (native HTML + useTransition) + pro Berater Tenant-Checkboxen mit optimistischem set/unset + Rollback bei Server-Fehler.
- `src/app/admin/berater/__tests__/actions.test.ts` — 13 Pure-Mock-Vitest (Gate, generateLink-Args ohne tenant_id, email_exists/re-invite, emailFailed, upsert-Args + Idempotenz, delete-Filter, DB-Fehlerpfade).
- `reports/RPT-608.md` — dieser Report.

## Files changed (5 Records + 1 Code)
- `src/components/admin-sidebar.tsx` — NAV_ITEM „Berater" (Icon `UserCog`) nach „Partner" ergänzt + Import. Rein additiv.
- `slices/INDEX.md` — SLC-189 `planned` → `in_progress`.
- `features/INDEX.md` — FEAT-106 `planned` → `in_progress`.
- `planning/backlog.json` — BL-534 `open` → `in_progress`.
- `docs/STATE.md` — High-Level-State `slice-planning` → `implementing`; Current Focus auf SLC-189 /backend DONE + Rebase-Hinweis.

## Files intentionally not changed
- `sql/migrations/132_v104_berater_foundation.sql` — SLC-189 ist migrationsfrei (Schema steht aus SLC-188/MIG-132).
- Kein neuer RPC/keine neue SQL-Datei — MT-2-Entscheidung (in Slice-Spec vordeklariert): direkter service_role-Write genügt (1 Statement, atomar). Kein neuer DEC nötig.
- `docs/DECISIONS.md` / `docs/MIGRATIONS.md` — keine neuen Architektur-/Schema-Entscheidungen in diesem Slice.

## Problems found
1. **Branch hinter main (Pre-Merge-Rebase Pflicht):** Worktree-Branch `v10-4-rollenmodell-p2` @ `c949aca` ist hinter `main` @ `b89aaa3`. main enthält bereits SLC-188 Slice-/qa-Closure (RPT-606/607) + SLC-188 `done` + MIG-132-code-complete-Records, die auf diesem Branch NICHT vorliegen (slices/INDEX zeigt hier SLC-188 noch `planned`). Deshalb wurde RPT-608 vergeben (RPT-606/607 sind auf main belegt). Vor Merge ist der Pre-Merge-Re-Check (git-release.md) mit `git fetch && git rebase origin/main` zwingend — sonst Records-Divergenz/Kollision. Meine Record-Updates sind bewusst auf SLC-189 gescopt; SLC-188-Status wird beim Rebase von main übernommen.
2. **DB-Contract-Test deferred:** MIG-132 ist noch nicht live appliziert (Apply = /deploy-Phase). `berater_tenant_assignments` existiert in der Live-DB nicht → kein `withTestDb`-Integrationstest möglich. Abgedeckt: Pure-Mock-Vitest über Args/Gate/Idempotenz. Live INSERT/DELETE + PK-No-Op + RLS = /deploy-Smoke.
3. **Kein Browser-Smoke:** UI nur über `next build` (Route im Manifest) + statisch verifiziert, nicht im Browser gerendert. Browser-E2E (Invite→Set-Password→Zuweisung) = /deploy-Phase (R-189-1).

## Runtime verification summary
- Vitest (neu): 13/13 PASS (`src/app/admin/berater/__tests__/actions.test.ts`).
- `npx tsc --noEmit`: 0 Fehler (keine berater-bezogenen Diagnosen).
- `npx eslint src/app/admin/berater src/components/admin-sidebar.tsx`: 0 Findings.
- `npx next build`: „Compiled successfully"; `/admin/berater` als dynamische Route (ƒ) im Manifest.
- `'use server'`-Gate: `grep ^export` in actions.ts → nur `createBerater/assignBerater/unassignBerater` (async) + `ActionResult` (type). Kein sync Export.
- Change-Impact-Sweep: neue Actions nur aus `BeraterAdmin.tsx` konsumiert (kein Wiring-Gap); Sidebar-Änderung additiv, kein Test referenziert NAV_ITEMS.
- NICHT verifiziert (deferred): Browser-Rendering, Live-DB INSERT/DELETE, echte Invite-Mail, RLS-Enforcement.

## Open points / deferred decisions
- /deploy: MIG-132 live-apply (Pre-Apply-Live-Audit MIG-131-Muster) + Berater/Admin-E2E-Browser-Smoke + DB-INSERT/DELETE-Smoke.
- Pre-Merge-Re-Check: Rebase gegen main + Tests re-run + MIG-Kollisionscheck (MIG-132 nur auf SLC-188-Zweig; keine SLC-189-Migration).

## Project record updates
slices/INDEX (SLC-189 in_progress), features/INDEX (FEAT-106 in_progress), backlog.json (BL-534 in_progress), STATE.md (Focus + High-Level-State), RPT-608 erstellt. roadmap V10.4 = bereits `active`. Cockpit zeigt damit SLC-189 als laufend.

## Recommended next step
`/qa SLC-189` (mandatorisch nach jedem /backend). Danach /backend SLC-190 → Gesamt-QA V10.4. Pre-Merge-Rebase gegen main vor Merge.
