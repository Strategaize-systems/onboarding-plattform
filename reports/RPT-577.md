---
id: RPT-577
date: "2026-07-05"
skill: /architecture
slice: null
feature: FEAT-102
type: completion
status: completed
reviewOf: RPT-576
result: "V10.2.1 Architektur — Reconciliation-Cron via Per-Tenant-Loop über tenants-Tabelle (0 Migration), */10 sequentiell + Cap. DEC-262. READY für /slice-planning."
title: "Architecture V10.2.1 — Embedding-Reliability-Härtung (ISSUE-112)"
---

# Architecture V10.2.1 — Embedding-Reliability-Härtung (ISSUE-112)

## Zusammenfassung
Self-Healing Reconciliation-Cron über bestehende V10.2-Primitiven. **0 Migration, 0 neue
Tabellen, kein neues UI.** Q-V10.2.1-A + Q-V10.2.1-B in DEC-262 aufgelöst. READY für
/slice-planning.

## Entscheidungen (DEC-262)
- **Q-A (Enumeration/Gap):** Per-Tenant-Loop über `tenants`-Tabelle (`from("tenants").select("id")`, Reuse `load-cross-tenant.ts`-Primitive) + pro Mandant identische Count-Gap-Query wie V10.2-Coverage-Guard (`count(knowledge_unit)` vs `count(knowledge_chunks source_type='knowledge_unit' status='active')`, `head:true`). **0 Migration.** Aggregat-RPC verworfen (Skalierungs-Option geparkt).
- **Q-B (Frequenz/Guard):** `*/10 * * * *`, **sequentielle** Verarbeitung (Bedrock-Throttle-freundlich), `MAX_TENANTS_PER_RUN=25`-Safety-Cap (Rest heilt nächster Tick, Cap-Hit geloggt).

## Komponenten
- **Cron-Route** `src/app/api/cron/knowledge-embed-reconcile/route.ts` (GET, nodejs, force-dynamic, x-cron-secret → 503/403/200/500; Pattern-Reuse `pending-signup-cleanup`).
- **Orchestrator** `src/lib/workspace/reconcile-embeddings.ts` (`reconcileEmbeddings(admin, deps?)`, injizierbare Deps, hermetische Tests).
- **rag.ts de-drift-Refactor:** Count-Gap-Logik als exportierter Helper `getTenantCoverage(admin, tenantId)` (heute privat in `DEFAULT_RAG_DEPS`); Cron + RAG-Guard teilen dieselbe Query.
- **Reuse (unverändert):** `reembedTenantKnowledge` (V10.2, idempotent, ledger-frei, fail-open), `createAdminClient`, `captureInfo/captureException`, Titan V2 (Bedrock eu-central-1).
- **Ops:** Coolify-Scheduled-Task `knowledge-embed-reconcile` (`*/10`, node-fetch + x-cron-secret) + bestehendes `CRON_SECRET`.

## Datenfluss
Coolify-Tick → GET-Route (Auth) → `reconcileEmbeddings`: `tenants.select("id")` → sequentiell je Mandant `getTenantCoverage` → bei `chunkCount < kuCount` `reembedTenantKnowledge` (Titan-Batch → Upsert) → Summary `{tenantsChecked, tenantsWithGap, chunksReembedded, failures, capped}` → `captureInfo` → 200 JSON. Fehler pro Mandant fail-open.

## Security / Data-Residency
`createAdminClient` (service-role) hinter x-cron-secret-Gate = trusted server-side Reconciliation (security-audit-standard); alle Writes tenant-scoped (kein Cross-Tenant-Leak); Logs nur Counts + tenant_id (keine PII); Titan Frankfurt unverändert (EU).

## Migration
**Keine.** knowledge_chunks unverändert, Idempotenz über bestehenden Unique-Constraint.

## Offene technische Fragen
Keine blockierenden. Backend-Detail: Cap-Wert (Default 25); `getTenantCoverage` filtert `status='active'` mit (identisch V10.2-Guard).

## Records aktualisiert
- docs/DECISIONS.md — DEC-262 (accepted)
- docs/ARCHITECTURE.md — §V10.2.1 Addendum (10 Sektionen)
- docs/STATE.md — High-Level State → architecture; Current Focus + Phase → V10.2.1 architecture DONE
- reports/RPT-577.md (dieser Report)
- (keine MIGRATIONS.md-Änderung — 0 Migration)

## Nächster Schritt
`/slice-planning V10.2.1` — 1 Slice SLC-185 (Cron-Route + Orchestrator + rag.ts-Helper-Extraktion + Route-Tests + Coolify-Task), Acceptance = SC1–SC6 aus PRD §V10.2.1. Kein Migrations-Slice.
