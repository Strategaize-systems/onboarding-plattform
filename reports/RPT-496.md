---
id: RPT-496
date: "2026-06-19"
skill: /qa
slice: SLC-V9.8-A
feature: FEAT-089
type: review
status: reviewed
reviewOf: RPT-495
result: "SLC-V9.8-A /qa PASS — 0 Blocker/High/Medium/Low. Code-Side clean (tsc0/eslint0/hermetisch 55/55, 0 Stubs, Wiring-Chain bestaetigt) + DB-Sidecar 6/6 GREEN gegen live Coolify-DB (Mig-123 themes-Spalte/GIN/Containment/Idempotenz + e2e themes-Propagation). AC-A-1..A-5 alle erfuellt. SLC-V9.8-A + FEAT-089 → done. Mig 123 noch nicht live (Live-Apply = /deploy)."
title: "QA SLC-V9.8-A — Tag-Export-Propagation (themes → knowledge_unit)"
---

# QA SLC-V9.8-A — Tag-Export-Propagation

## 1. QA-Scope
Verifikation des /backend-Ergebnisses aus RPT-495 (SLC-V9.8-A, FEAT-089): Migration 123
(`knowledge_unit.themes text[]` + GIN) + Propagation `email_synthesized_unit.themes` →
`knowledge_unit.themes` im Promote-Pfad. Branch `v9-8-tag-vokabular` (Worktree, HEAD `4810685`).
QA umfasst: Code-Side (tsc/eslint/Stub-Scan/Wiring), hermetische Vitest, und die zwei
DB-Sidecar-Tests gegen die live Coolify-DB (node:20-Sidecar, `coolify-test-setup.md`).

## 2. Ergebnis: **PASS** (0 Blocker / 0 High / 0 Medium / 0 Low)

## 3. Verifikation im Detail

### Code-Side
- **tsc:** EXIT 0 (`npx tsc --noEmit`, gesamtes Projekt).
- **eslint:** EXIT 0 (6 geaenderte/neue Files).
- **Stub-Scan:** 0 echte Stubs in den Kern-Files (kein TODO/FIXME/leerer Handler/Placeholder-Return im Produktionscode).
- **Wiring-Chain bestaetigt:** Import-Pfad-SELECT auf `email_synthesized_unit` enthaelt `themes`
  (actions.ts:667) → Typ-Cast `themes: string[] | null` (684) → `unitForImport.themes = unit.themes` (775)
  → `mapSynthesizedUnitToKnowledgeUnit` setzt `themes: Array.isArray(...) ? ... : []` (handbook-import.ts:282)
  → `.insert(insertInput)` spreadet `themes` (803). Lueckenlos.

### Hermetische Vitest
- `handbook-import.test.ts` (19) + `curation/__tests__/actions.test.ts` (36) = **55/55 PASS**.

### DB-Sidecar gegen live Coolify-DB (node:20)
- Server `root@159.69.207.29`, DB-Container `supabase-db-bwkg80w04wgccos48gcws8cs-083208599632`,
  Netz `bwkg80w04wgccos48gcws8cs_strategaize-net`. Test-Repo `/opt/onboarding-plattform-test`
  (V9.8-Files via Bundle eingespielt). Raw `pg.Client` → node:20 (glibc, kein supabase-js-Runtime).
- `migration-123-knowledge-unit-themes.test.ts` (4) + `v95-bulk-e2e.test.ts` (2) = **6/6 PASS**.
- Beweist gegen die echte Schema-Basis (Mig 122 live): themes-Spalte (`_text`/NOT NULL/Default `{}`),
  GIN-Index `idx_knowledge_unit_themes` (amname=gin), Containment `@>`/`&&`, Idempotenz (2. Apply),
  e2e-Stage-Chain (6 Patterns → 2 Units → Curation als tenant_admin via RLS → Promote via echtem Mapper)
  + themes-Propagation `['pricing']` → `knowledge_unit.themes` + CHECK-Constraints intakt.

## 4. Akzeptanzkriterien
- **AC-A-1** (Mig 123: themes text[] NOT NULL DEFAULT '{}' + GIN, additiv, kein Backfill): **erfuellt** (DB-Sidecar Spalte+Default+Idempotenz).
- **AC-A-2** (Promote propagiert themes verlustfrei, null/leer → '{}'): **erfuellt** (Mapper + Caller + hermetisch + e2e-Sidecar).
- **AC-A-3** (0 Regression title/body/block_key/confidence/metadata + rollbackLoop): **erfuellt** (bestehende Tests unveraendert GREEN, Propagation rein additiv, e2e-Stage-Chain intakt).
- **AC-A-4** (Queryable, Containment GIN-gestuetzt, DB-Test): **erfuellt** (Sidecar: GIN-Index + @>/&& Containment match/miss).
- **AC-A-5** (tsc 0, eslint 0, hermetische Vitest GREEN, next build PASS): **erfuellt** (tsc0/eslint0/55-55; `next build` mit-Env EXIT0 in RPT-495 verifiziert).

## 5. Stub-Detection
Keine echten Stubs. `next build` ohne Env scheitert an `supabaseUrl is required` — bekanntes
ENV-Muster (betrifft auch main), nicht code-bedingt; mit Env EXIT0.

## 6. Offene Risiken / fehlende Checks
- **Mig 123 ist NICHT live** — die DB-Tests self-applien die Migration in der gerollbackten
  Test-Tx (R-A-1). Realer Live-Apply + PostgREST-Reload (`NOTIFY pgrst`) erfolgt im /deploy V9.8,
  **BLOCKING: Migration VOR Code-Redeploy**, sonst wirft der Promote-INSERT `column "themes" does not exist`.
- Test-Sidecar `/opt/onboarding-plattform-test` traegt jetzt die V9.8-Files (untracked/ueberschrieben
  auf einem stale May-Checkout) — kein Produktions-Impact, dedizierter Test-Workdir.

## 7. Readiness
SLC-V9.8-A ist Code-Side + DB-verifiziert **ready**. Letzter offener Schritt der V9.8-Familie ist
SLC-V9.8-B; Live-Apply von Mig 123 ist Sache des /deploy.

## 8. Project-Record-Updates
- `/slices/INDEX.md` — SLC-V9.8-A in_progress → **done**.
- `/features/INDEX.md` — FEAT-089 in_progress → **done**.
- `/docs/STATE.md` — Thread-A-Focus auf /qa PASS + naechster Schritt /backend SLC-V9.8-B.
- `/planning/backlog.json` — BL-505 bleibt **in_progress** (V9.8-Umbrella, deckt auch FEAT-088/SLC-V9.8-B).
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues.

## 9. Next workflow step
`/backend SLC-V9.8-B` (FEAT-088 Controlled-Tenant-Tag-Vokabular: on-the-fly Vokabular-Loader aus
`knowledge_unit.themes` + Synthese-Prompt-Injektion + Worker-Wiring, 0 Migration). Liest die in
SLC-V9.8-A erzeugte Spalte. Nach SLC-V9.8-B: Gesamt-/qa V9.8 → /final-check → /go-live → EIN
Master-Merge → /deploy (inkl. Live-Apply Mig 123 VOR Redeploy) → /post-launch.
