---
id: RPT-441
date: "2026-06-11"
skill: /backend
slice: SLC-V9.1-C
feature: FEAT-078
type: completion
status: completed
result: "SLC-V9.1-C Storage-Retention-Cron /backend Code-Side DONE (4/4 MTs). Run-level resolution DEC-208. 25 hermetic Vitest GREEN, tsc=0, ESLint=0. Live-Smoke deferred to /deploy."
title: "SLC-V9.1-C Storage-Retention-Cron /backend Code-Side DONE (run-level, DEC-208)"
---

# SLC-V9.1-C — Storage-Retention-Cron /backend (Code-Side DONE)

## Outcome
Alle 4 Micro-Tasks code-side fertig. Status `in_progress` (Live-Smoke AC-9 founder-gated -> /deploy; Gesamt-V9.1-/qa offen). Cumulative-Worktree `v9-1-forward-bucket-email`, KEIN Master-Merge.

## Kern-Entscheidung DEC-208 (Run-Level statt Message-Level)
Slice-Spec MT-2 + ARCHITECTURE Flow D beschrieben message-level Retention (`email_message.deleted_at`, `metadata->>'email_message_id'`, `INSERT audit_log`). Pattern-Inspect zeigte: as-built MIG-058 traegt `retention_until`+`soft_delete_at` NUR auf `email_bulk_run`; `email_message` (FK ON DELETE CASCADE) hat keine Retention-Spalten; `handbook-import.ts` verknuepft per `metadata->>bulk_run_id`; OP hat kein `audit_log` (-> `error_log`). Founder-Entscheid: Option B Run-Level. KEIN neues Migration.

## Micro-Tasks
- **MT-1**: `retention-policy.ts` (ENV 60/90 + throw soft>=hard), `retention-idempotency.ts` (`isRunImportedToHandbook` via knowledge_unit), `storage-delete.ts` (`deleteStorageObject` bulk-email Bucket). 15 Vitest GREEN. Commit `ede5c3b`.
- **MT-2**: `handle-bulk-email-retention-sweep.ts` — `runRetentionSweep` Orchestrierung ueber `RetentionStore`-Abstraktion (`createRetentionStoreFromSupabase`): Soft-Delete-Phase -> Hard-Delete-Phase (Idempotency-Skip, per-Run Storage-Delete, Storage-Error-Keep R3, Cascade-Run-DELETE) -> awaited `error_log`-Audit. 6 hermetische Vitest GREEN + 4 skip-guarded Coolify-DB SQL-Integration-Cases (IMP-1183). Commit `9a04fbd`.
- **MT-3**: `/api/cron/bulk-email-retention-sweep/route.ts` — OP-x-cron-secret-Pattern (503/403/200/500), synchroner Sweep. 4 Vitest GREEN. Commit `ff0f6ea`.
- **MT-4**: RUNBOOK-Section + DEC-208 + ARCHITECTURE Flow D Korrektur + Slice-Spec-Banner+AC-Korrektur + MIGRATIONS-Annotation + slices/INDEX + STATE.

## Gates
- tsc --noEmit EXIT=0, ESLint EXIT=0 (neue Files).
- 25 hermetische Vitest GREEN (15 MT-1 + 6 MT-2 + 4 MT-3), 4 Integration skip-guarded.
- Full-Suite: 508 pre-existing TEST_DATABASE_URL-gated Failures (throw ohne DB, OP-Konvention, on-server GREEN) — KEINE Regression (Aenderungen rein additiv + Docs, kein existing-Source-File angefasst).

## Offene Punkte
- **AC-V9.1-C-9 Live-Smoke** deferred -> /deploy (Endpoint nicht deployed + Coolify-Scheduled-Task `0 2 * * *` = Founder-Pre-Cond D4 / OQ-R1-1).
- Coolify-DB SQL-Integration-Test laeuft erst on-server mit TEST_DATABASE_URL.
- Gesamt-V9.1-/qa + Master-Merge in SLC-V9.1-D.

## Nächster Schritt
`/qa SLC-V9.1-C` (Code-Side, Pflicht nach /backend).
