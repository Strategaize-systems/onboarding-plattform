---
id: RPT-535
date: "2026-06-25"
skill: /backend
slice: SLC-172
feature: FEAT-092
type: completion
status: completed
result: "SLC-172 MT-3 DONE (Code-Side) — Diagnose-Reader (SubtopicDiagnosisCard, Ampel/Reifegrad/Empfehlung je Unterthema) + deterministisches Modul-Routing (blueprint-routing.ts, kein LLM) + Erweiterung der MT-1-Overview [sessionId]/page.tsx (block_diagnosis A–G + metadata.routing-Card, geseedete Module verlinkt / Rest 'geplant'). Keine Migration. Gates tsc 0 / eslint 0 / vitest 12 neu (9 routing + 3 render) + 22 MT-1/MT-2-Regress = 34 GREEN, 0 Regression. Voller LLM-Diagnose-Output + Browser-Smoke → V10-Deploy."
title: "Backend SLC-172 MT-3 — Diagnose-Reader + deterministisches Modul-Routing"
---

# /backend — SLC-172 MT-3 (block_diagnosis-Subtopic-Reader + deterministisches Modul-Routing)

**Outcome: fully complete (Code-Side).** Keine Migration. Worktree `…-v10`, Branch `v10-slc170b-blueprint-seed`. NICHT committet/gepusht (Founder/Coolify-Hold).

## 1. Outcome summary
- **`blueprint-routing.ts`** (pure, hermetisch): `parseRoutingMeta` (Drift-toleranter Parser für `template.metadata.routing[]`), `deriveSubtopicAmpel` (flacht die `block_diagnosis`-Inhalte A–G zu `subtopic.key → {ampel,name}` ab), `computeModuleRouting` (aktiviert ein Routing-Ziel nur, wenn die Subtopic-Ampel ∈ `activate_when.ampel` — Seed gelb/rot — **kein LLM-Routing**, AC-172-3), `coerceAmpel` (robuste Ampel-Coercion aus `string|number|null`).
- **`SubtopicDiagnosisCard.tsx`** (read-only Presentational): eine Card pro A–G-Block, Layout-Reuse von `BlockSectionCard` (Top-Strip via `getBlockColor`, Akzent-Badge), pro Unterthema Ampel-Badge (green/amber/red konsistent zu `DiagnosisWorkspace`) + Reifegrad-Mini-Bar (0–10) + Ist-Situation + Empfehlung + Nächster Schritt. Bewusst ohne `EditableText`/`ReactMarkdown` → `renderToString`-tauglich, kein `TextOverrideProvider` nötig.
- **`[sessionId]/page.tsx`** (MT-1-Overview **erweitert, nicht ersetzt**): liest `block_diagnosis` je A–G + `template.metadata` (Routing direkt, da `getTemplateById` `metadata` nicht garantiert) + die geseedeten `stb_modul_*`-Slugs; rendert die Diagnose-Cards (A–G sortiert) + eine Routing-Card „Relevante Module". Pending-Hinweis solange keine Diagnose existiert (Worker läuft erst beim Deploy). Module ohne geseedetes Template (V10 nur m04/05/06) werden als „geplant"-Badge ohne Link gezeigt statt auf eine 404-Modul-Seite zu verlinken (Deviation-Rule-2 UX-Schutz).

## 2. Files reviewed
- `/sql/migrations/126_v10_stb_blueprint_seed.sql` — `metadata.routing[]` (13 Ziele) + `diagnosis_schema.blocks` A–G/13 Subtopics + Feld-Definitionen (ampel/reifegrad/empfehlung).
- `/sql/migrations/050_block_diagnosis.sql` — `block_diagnosis(content jsonb, …)`-Schema + RLS (`block_diagnosis_tenant_read`).
- `/src/workers/diagnosis/types.ts` — `DiagnosisContent`/`DiagnosisSubtopic` (autoritative `content`-Shape).
- `/src/app/admin/debrief/[sessionId]/[blockKey]/page.tsx` + `DiagnosisWorkspace.tsx` — kanonischer Subtopic/Ampel-Render (Farb-Konsistenz), `block_diagnosis`-Fetch (`select id, content, status …`).
- `/src/components/diagnose/BerichtRenderer.tsx` + `/src/app/dashboard/diagnose/[capture_session_id]/bericht/components/BlockSectionCard.tsx` — Layout-Vorbild.
- `/src/lib/diagnose/block-colors.ts` (`getBlockColor`), `/src/lib/stb-vertikale/modul-capture.ts` (`modulBasePath`, `MODUL_KEY_REGEX`), `/src/lib/stb-vertikale/blueprint.ts` (`Ampel`).
- `/src/app/dashboard/stb/blueprint/[sessionId]/page.tsx` (MT-1-Overview-Basis), `/reports/RPT-533.md` + `/reports/RPT-534.md`.

## 3. Files changed
- `/src/app/dashboard/stb/blueprint/[sessionId]/page.tsx` — Diagnose-Reader-Section + Routing-Card + `ModuleChip`-Helper ergänzt (Imports: routing-Helfer, `SubtopicDiagnosisCard`, `modulBasePath`, `DiagnosisContent`). Bestehende MT-1-Capture-Overview unverändert erhalten.
- `/docs/STATE.md` — Current Focus + Current Phase + Immediate Next Step auf MT-3 /backend DONE → NEXT /qa MT-3.

## 4. Files created
- `/src/lib/stb-vertikale/blueprint-routing.ts` — pure deterministisches Routing (s. §1).
- `/src/components/stb/SubtopicDiagnosisCard.tsx` — read-only Block-Diagnose-Card.
- `/src/lib/stb-vertikale/__tests__/blueprint-routing.test.ts` — 9 hermetische Logik-Tests.
- `/src/components/stb/__tests__/subtopic-diagnosis-card.test.tsx` — 3 renderToString-Render-Tests.
- `/reports/RPT-535.md` — dieser Report.

## 5. Files intentionally not changed
- `/src/workers/diagnosis/handle-diagnosis-job.ts` — der Diagnose-Worker bleibt unangetastet (MT-2-Reuse); MT-3 ist reiner Reader.
- `/slices/INDEX.md`, `/features/INDEX.md`, `/planning/backlog.json`, `/planning/roadmap.json` — SLC-172/FEAT-092/BL-511 bleiben `in_progress` (MT-3 /qa + V10-Gesamt-QA offen), V10 `active`. Kein Status-Wechsel auf `done`, da das Slice erst nach /qa abgeschlossen ist.
- `/docs/DECISIONS.md`, `/docs/MIGRATIONS.md` — keine neue Decision/Migration (Routing war in DEC-244 + MIG-126 designt; MT-3 setzt nur um). Die „geplant"-Badge-UX ist Deviation-Rule-2, keine Architektur-Änderung → kein DEC.

## 6. Problems found
- **Keine Blocker.** Verifikations-Limit (kein Defekt): Der Reader rendert nur, wenn `block_diagnosis`-Rows existieren — die entstehen erst nach einem echten `diagnosis_generation`-Worker-Lauf, der ohne V10-Deploy nicht läuft (Founder/Coolify-Hold). Der Render gegen die reale Diagnose-Form ist über die hermetischen Render-Tests (Fixtures in `DiagnosisContent`-Shape) abgesichert; der End-to-End-Pfad (Worker → `block_diagnosis` → Reader → sichtbare Cards/Routing) gehört zur V10-Deploy-Validation.
- **Routing-Ziel-Module mehrheitlich nicht geseedet:** Die 13 Routing-Ziele referenzieren m01/m02/m03/m06/m07/m08/m15/m16/m26/m27/m28/m35/m36/m38/m39/m42 — in V10 sind nur m04/05/06 geseedet (Out-of-Scope: volle 46-Modul-Matrix). Bewusst gelöst: nicht-geseedete Module erscheinen als „geplant"-Badge ohne Link (keine Dead-Links), die Empfehlung selbst (Ampel-getrieben) ist trotzdem deterministisch sichtbar. Keine erfundene `modul_key→Name`-Tabelle (wäre Scope-Creep); Anzeige = Modul-Key (z. B. „Modul M07").

## 7. Open points / deferred decisions
- **/qa SLC-172 MT-3** — AC-172-3 (Routing-Determinismus, über die hermetischen Tests + Code-Review) + AC-172-4 (DATEV-Begriffs-Abgrenzung in der Reader-Copy).
- **V10-Deploy-Validation:** voller LLM-Diagnose-Output (AC-172-2 sichtbare Cards) + Live-Browser-Smoke des Readers + MIG-127 Live-Apply.
- Danach **V10 Gesamt-QA** (SLC-170b + 172 + 173 + 174 + 175) → /final-check → /go-live → /deploy → /post-launch.

## 8. Project record updates
STATE.md (Focus/Phase/Next Step). Slices/Features/Backlog/Roadmap unverändert (Slice erst nach /qa `done`) — bewusst, regelkonform. Keine neue Migration/DEC/Issue.

## 9. Verification
- `npx tsc --noEmit` → **EXIT 0**.
- `npx eslint` (5 neue/geänderte Files) → **0**.
- `npx vitest run blueprint-routing.test.ts subtopic-diagnosis-card.test.tsx blueprint-diagnosis.test.ts blueprint.test.ts` → **34/34 GREEN** (9 routing-logic + 3 card-render neu; 7 MT-2 + 15 MT-1 Regress), **0 Regression**.
- Tests decken: `coerceAmpel` (gültig/case/whitespace/ungültig/non-string), `deriveSubtopicAmpel` (A–G-Flattening + unbekannte Ampel→null), `computeModuleRouting` (gelb/rot→Modul, grün/unbewertet→kein Modul, Reihenfolge-Determinismus), `parseRoutingMeta` (valide + Drift-Skip + Nicht-Objekt), `SubtopicDiagnosisCard` (Block-Titel/Ampel-Label/Reifegrad/Empfehlung/Nächster Schritt, „nicht bewertet"-Fallback, leere Subtopics).
- **Kein** Browser-/DB-Smoke (Reader braucht echte `block_diagnosis`-Rows aus einem Worker-Lauf → V10-Deploy; respektiert den OP-Hold).

## 10. Recommended next step
**`/qa SLC-172 MT-3`** (AC-172-3 + AC-172-4, Code-Side + Render-Verifikation; voller Diagnose-Output + Browser-Smoke → V10-Deploy). Danach **V10 Gesamt-QA** über alle Slices, dann /final-check.
