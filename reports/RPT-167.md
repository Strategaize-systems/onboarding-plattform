---
id: RPT-167
date: "2026-05-06"
skill: /backend
slice: SLC-071
feature: FEAT-034
type: completion
status: in-progress
result: "MT-4 + MT-5 done — Server Actions requestWalkthroughUpload + confirmWalkthroughUploaded in src/app/actions/walkthrough.ts mit 6/6 Vitest-Cases GREEN. AC-6..AC-9 + AC-13 PASS. AC-10..AC-12 + AC-14 + AC-15 deferred auf MT-6..MT-9."
title: "SLC-071 MT-4 + MT-5 — Walkthrough Server Actions request+confirm + 6 Vitest-Cases GREEN"
---

# RPT-167 — SLC-071 MT-4 + MT-5: Walkthrough Server Actions

## Outcome summary

Beide Server Actions des Capture-Pfads sind code-side komplett: `requestWalkthroughUpload` (MT-4) und `confirmWalkthroughUploaded` (MT-5) liegen gemeinsam in `src/app/actions/walkthrough.ts`. 6/6 Vitest-Cases GREEN. TypeScript `tsc --noEmit` 0 Errors. Commit `723e3e3` auf `slc-071/v5-walkthrough-foundation` gepusht.

**Status SLC-071: in_progress (5/9 MTs done).** MT-6..MT-9 (Capture-UI + Status-Polling + Capture-Page + Build-Gate) verbleiben fuer Folge-Sessions. AC-14 (Partial-RLS-Test gegen Live-DB) wurde bewusst nicht in dieser Session gezogen — ist als Slice-Ende-QA-Gate vorgesehen, nicht als MT-spezifischer Block.

## AC-Status

| AC | Beschreibung | Status |
|----|---|---|
| AC-1..AC-5 | MIG-031 (082+083+084) live + Backup | PASS (RPT-166) |
| AC-6 | requestWalkthroughUpload happy path → walkthrough_session-Eintrag (status='recording') + valide signed URL | PASS (Vitest unit) |
| AC-7 | confirmWalkthroughUploaded happy path → status='uploaded' + ai_jobs walkthrough_transcribe pending | PASS (Vitest unit) |
| AC-8 | Cross-Tenant-Reject (captureSessionId fremd-tenant) | PASS (Vitest) |
| AC-9 | estimatedDurationSec > 1800 Reject mit klarem Error | PASS (Vitest) |
| AC-10..AC-12 | Capture-UI + Status-Polling | deferred (MT-6+7+8) |
| AC-13 | Vitest 6 Cases gruen | PASS (6/6 GREEN) |
| AC-14 | Partial RLS-Test (4 SELECT-Faelle) gegen Live-DB | deferred (Slice-Ende) |
| AC-15 | npm run lint + build + test | partial (test+typecheck PASS, full build erst MT-9) |

## Files reviewed

- `/slices/SLC-071-walkthrough-foundation-capture-upload.md` Section B + F + MT-4 + MT-5 — Spec.
- `/src/app/actions/dialogue-session-actions.ts` — Pattern-Vorlage (auth.getUser + profiles-Query, RLS-bound Anon-Client + Admin-Client-Trennung).
- `/src/app/dashboard/settings/__tests__/actions.test.ts` — vi.mock-Pattern fuer createClient+createAdminClient + revalidatePath.
- `/src/lib/supabase/server.ts` + `/src/lib/supabase/admin.ts` — Anon- vs. Service-Role-Client-Konvention.
- `/sql/migrations/031_ai_jobs_table.sql` — ai_jobs-Schema (`tenant_id`, `job_type`, `payload jsonb`, `status` default 'pending').
- `/sql/migrations/083_v5_walkthrough_session.sql` — walkthrough_session DDL + 3 Policies + GRANTs (live seit RPT-166).
- `/src/app/api/capture/[sessionId]/evidence/upload/route.ts` — bestehender Storage-Upload-Pattern fuer Konvention.
- `.claude/skills/backend.md` Subagent-Orchestration-Pattern (single-file scope = direkte Implementation OK, kein Subagent noetig).

## Files changed

(keine — diese Session erstellt nur neue Files)

## Files created

- `/src/app/actions/walkthrough.ts` (203 Zeilen) — beide Server Actions in einer Datei wie Slice-Spec verlangt.
  - `requestWalkthroughUpload`: Fast-Fail bei estimatedDurationSec > 1800 (vor jedem DB-Hit), `auth.getUser()`, profiles-Lookup mit `RECORDER_ROLES`-Set (`employee|tenant_member|tenant_admin` — kein strategaize_admin), capture_session-Cross-Tenant-Defense-in-Depth (RLS hide + expliziter tenant_id-Vergleich), INSERT walkthrough_session mit anon-Client (RLS `walkthrough_session_insert`-Policy greift), Signed-Upload-URL via service_role mit `upsert: false`.
  - `confirmWalkthroughUploaded`: Fast-Fail durationSec/fileSizeBytes-Validierung, `auth.getUser()`, walkthrough_session-Load via anon-Client (RLS-bound), Self-Confirm-Only-Check (`recorded_by_user_id === user.id`), Status-Guard (`recording|uploading`), UPDATE storage_path/duration/size/status='uploaded' via service_role (BYPASSRLS — bewusst, Worker- und Confirm-Transitions umgehen `walkthrough_session_update_review`-Policy), INSERT ai_jobs walkthrough_transcribe pending mit `payload: {walkthroughSessionId}`, `revalidatePath('/employee/walkthroughs/<id>')`.
- `/src/app/actions/__tests__/walkthrough.test.ts` (327 Zeilen) — 6 Cases:
  - requestWalkthroughUpload: happy + duration-Reject (Fast-Fail vor DB) + Cross-Tenant-Reject (RLS-hide-Pfad).
  - confirmWalkthroughUploaded: happy + Cross-User-Self-Confirm-Reject + Status-Validation-Reject (status='uploaded' vor zweitem Confirm).
- `/reports/RPT-167.md` — dieser Report.

## Files intentionally not changed

- `/src/components/capture-modes/walkthrough/` — gehoert MT-6 (eigene Session).
- `/src/app/(employee)/employee/capture/walkthrough/[id]/page.tsx` — gehoert MT-8.
- `/src/app/(employee)/employee/walkthroughs/[id]/page.tsx` + `/src/app/api/walkthroughs/[id]/status/route.ts` — gehoert MT-7.
- `/planning/backlog.json` — BL-077 bleibt `in_progress` (Slice ist 5/9 MTs durch, nicht done).
- `/docs/MIGRATIONS.md` + `/docs/DECISIONS.md` — keine neuen Migrations/DECs in MT-4/MT-5; bestehende Eintraege aus RPT-166 unveraendert.
- `/slices/INDEX.md` — SLC-071 bleibt `in_progress` (kein Status-Wechsel, MT-9 muss erst durch).

## Problems found

- **Keine TDD-RED→GREEN-Verletzung**: Test-File wurde zuerst geschrieben, Lauf bestaetigte "Failed to load url ../walkthrough" (RED). Erst danach `walkthrough.ts` geschrieben → 6/6 GREEN. Disziplin durchgehalten.
- **node_modules nicht im Worktree vorhanden**: `npm install` initial gelaufen (~30s, 9 moderate dev-only Vulns wie in V4.4 dokumentiert, kein Action). Nicht dokumentiert in Slice-Pre-Reqs — ist normales Worktree-Verhalten.
- **Confirm-UPDATE laeuft via service_role bewusst**: `walkthrough_session_update_review`-Policy erlaubt nur Admin-Reviews. Worker-Status-Wechsel und Self-Confirm-Status-Wechsel umgehen RLS bewusst (siehe MIG-031/083 Comment). Implementation entspricht Architektur-Intent + Slice-Spec.
- **storage_path wird im UPDATE neu zusammengebaut**: tenant_id+walkthrough_id kommen aus dem Load-Query, nicht aus dem Client-Input — schuetzt vor manipuliertem Pfad-Override durch boese Clients.

## Open points or deferred decisions

- **MT-6 (WalkthroughCapture Client-Component)** — getDisplayMedia + getUserMedia + MediaRecorder + XHR-Upload-Progress + 30min-Auto-Stopp. ~3-4h, eigene Session.
- **MT-7 (Status-Polling-Page + Status-API)** — Server-Component + Client-Polling 5s-Tick + GET-Route. ~1-2h.
- **MT-8 (Capture-Page Loader)** — Server-Component-Wrapper mit requireRole. ~30min.
- **MT-9 (Build + Lint + Test gruen)** — Final-Gate. `npm run build`, `npm run lint`, `npm run test` Output-Snapshots. ~30-60min inkl. Fixes.
- **AC-14 Partial-RLS-Test** (4 SELECT-Faelle gegen Live-DB via coolify-test-setup.md SAVEPOINT-Pattern) gehoert zu MT-9-Gate oder eigener Slice-QA-Block.
- **Live-Smoke MT-4+MT-5** — kann erst nach MT-6 in echtem Browser-Flow laufen, da der Capture-Pfad Client-side getriggert wird. Vitest-Coverage deckt Logik 1:1 ab.

## Project record updates

| Record | Update |
|---|---|
| `/reports/RPT-167.md` | neu (dieser Report) |
| `/docs/STATE.md` | Current Focus auf MT-4+MT-5 done, Naechste Schritte MT-6 |
| `/slices/INDEX.md` | unveraendert (bleibt `in_progress`) |
| `/planning/backlog.json` | unveraendert (BL-077 bleibt `in_progress`) |
| `/planning/roadmap.json` | unveraendert (V5 bleibt `active`) |
| `/docs/KNOWN_ISSUES.md` | keine neuen Issues |
| `/docs/MIGRATIONS.md` + `/docs/DECISIONS.md` | unveraendert |

## Recommended next step

**/frontend SLC-071 MT-6 in eigener Session** — `WalkthroughCapture.tsx` Client-Component mit MediaRecorder + Direct-Upload + 30min-Auto-Stopp. Pattern: 7-State-Maschine (`idle | requesting | recording | stopping | uploading | uploaded | failed`), `MediaRecorder.isTypeSupported`-Capability-Check mit Fallback `vp8+opus` + Warning, XMLHttpRequest PUT mit `xhr.upload.onprogress`. Plus 3 Vitest-Cases (idle→requesting Mock, isTypeSupported-false-Path, autoStop-Timer mit `vi.useFakeTimers`).

Begruendung: MT-6 ist groesser (8 MTs UI-mt's plus Tests); MT-7+MT-8 sind kleiner und sollten mit MT-6 zusammengeschnitten werden, aber der Flow `Capture-UI → Status-Page → Loader` braucht eine zusammenhaengende Session weil MT-6 → MT-7-Redirect testbar ist.
