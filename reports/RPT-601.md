---
id: RPT-601
date: "2026-07-07"
skill: /discovery
slice: null
feature: null
type: completion
status: completed
reviewOf: null
result: "READY für /requirements — V10.4 = Rollenmodell V2 P2 (strategaize_berater), BS-Port bestätigt (owner-per-row → tenant-scoped can_see_tenant), sauber als ein Paket gecuttet"
title: "Discovery V10.4 — Rollenmodell V2 Paket P2 (strategaize_berater)"
---

# Discovery V10.4 — Rollenmodell V2 Paket P2 (`strategaize_berater`)

## Hauptziel
Eine zugewiesene, tenant-gescopte Berater-Rolle einführen (`strategaize_berater`, 5. Rolle): Strategaize-Consultants betreuen **nur zugewiesene Kanzleien/Kunden** inkl. deren Mandanten/Daten, mit gescoptem „Mein Tag". Pattern-Reuse BS MIG-033/034/035 (DEC-263, BL-530).

## Grounding (real, file:line)
- **BS-Quelle bestätigt** (Explore-Sweep): `can_see_owner(target_owner)` = admin OR self OR (teamlead AND same team) in `sql/migrations/035_v7_rls_switch.sql:54-72`; Helper is_admin/is_teamlead/get_my_team_id (SECURITY DEFINER + search_path-Lock); `owner_user_id` auf 8 Kern-Tabellen (MIG-033); `inviteUserAndCreateProfile` in `cockpit/src/lib/auth/invite.ts:32-101` (generateLink invite → callback-URL → profiles.insert(role+team_id) → NodeMailer → Rollback); `inviteMember`/`changeRole`/`deleteProfile` in `cockpit/src/lib/team/actions.ts` (assertRole-Gate, DEC-230 Teamlead-Restriktion); `visibleFor`-UI-Gating in `settings/sections.ts`.
- **OP-Live bestätigt:** profiles.role CHECK = 4 Werte (strategaize_admin/tenant_admin/employee/partner_admin) nach MIG-131 → P2 fügt 5. hinzu. `tenants` hat `tenant_kind` + **`parent_partner_tenant_id`** → Kanzlei→Mandant-Cascade fahrbar.

## Zentraler Mechanismus-Lift (Pflicht-Check)
BS gated **pro Zeile** (owner_user_id). OP ist **tenant-scoped** — Zuweisung an Kanzlei/Tenant, nicht Zeile. Übersetzung `can_see_owner` → **`can_see_tenant(tenant_id)`** = is_admin() OR direkt zugewiesener Berater OR (Mandant → Berater der Kanzlei via parent_partner_tenant_id). Kein naiver 1:1-Lift.

## Scope
**In (V10.4=P2):** Rolle strategaize_berater (CHECK+handle_new_user, MIG analog MIG-131) · Zuweisung `betreuer_user_id` auf `tenants` (Cascade auf Mandanten) · Helper `can_see_tenant` + Erweiterung tenant-scoped RLS-Policies · Admin-Invite/Zuweisung (inviteUserAndCreateProfile-Port) · Sidebar/Layout-Gating (Berater in /admin/tenants gefiltert + /admin/mein-tag gescopt, nicht Voll-Admin) · Mein Tag mit can_see_tenant-Scope.
**Out:** P3 Flags/F1 · P4 F3-Workspace + Berater-delegierte Invites · P5 /admin-URL-Entmischung · Multi-Berater-pro-Tenant (Tabelle) · Teams/Teamlead-Ebene · Direkt-Kunden-Self-Signup.

## Versions-Cut
V10.4=P2 → V10.5=P3 → V10.6=P4 → V10.7=P5 (DEC-263-Reihenfolge; P2 baut Mein-Tag-Scoping, P4 reused).

## Feasibility-Verdikt
Machbar, sauber als ein Paket. Kernprozess KI/Mensch irrelevant (reine Access-Mechanik); kein Cold-Start (Zuweisung = Stammdaten); keine Provider-Kosten (Mein-Tag reuse). Hauptarbeit = RLS-Policy-Erweiterung (Umfang gegen Live-pg_policies enumerieren, Playbook sql-migration-hetzner Check 4).

## Offene Fragen (für /requirements, blockieren V1-Grenze nicht)
1. Zuweisungs-Granularität: Einzelspalte betreuer_user_id + Cascade (Empfehlung, 1 Berater/Tenant) vs. Zuordnungstabelle (Multi-Berater) — Vorschlag Einzelspalte jetzt.
2. Exaktes /admin-Routen-Set für Berater in P2 (Minimum vs. Reviews/Debrief seiner Kunden) — Voll-Entmischung ist P5.
3. RLS-Tabellen-Set für den can_see_tenant-Zweig — Enumeration gegen Live-pg_policies (verbindlich).

## Readiness
**READY für /requirements** (BL-530). Parallel offen: /post-launch V10.3 ~16:25 UTC.
