---
id: RPT-119
date: "2026-04-30"
skill: /qa
slice: SLC-047
feature: FEAT-031
type: completion
status: needs-rework
reviewOf: RPT-118
result: "SLC-047 QA: Code-PASS (alle automatisierbaren Pflicht-Gates gruen, 26/26 Tests gegen Coolify-DB, tsc=0, build clean, Wiring-konsistent, Stub-frei). Browser-Smoke MT-7 (SC-V4.2-9 Pflicht-Gate) BLOCKED bis User-Deploy + DB-Reset + Live-504-Fix. Plus separates Live-Issue ISSUE-030 (V4.1-Stable extern unerreichbar)."
title: "QA SLC-047 — Wizard-Modal Frontend: Code-PASS, MT-7 Browser-Smoke pre-deploy ausstehend"
---

# RPT-119 — QA SLC-047 (Wizard-Modal Frontend)

## 1. QA Scope Reviewed

- 14 Acceptance Criteria der Slice-Spec `/slices/SLC-047-wizard-modal-frontend.md`.
- 7 Micro-Tasks (MT-1..MT-7) der Slice-Spec.
- 4 Pflicht-Gates aus Slice-Spec ("Pflicht-QA-Vorgaben"): Browser-Smoke MT-7, Step-Persistenz-Live, Multi-Admin-Race, Cross-Role-Test, Skip-Pfade, npm test+build.
- Alle Implementation-Files: `Wizard.tsx`, `wizard-helpers.ts`, 4 Step-Komponenten, `dashboard/layout.tsx`, 2 Test-Files.
- Wiring-Chain: Step3 → `inviteEmployee` (V4 SLC-034); Wizard → 4 SLC-046 Server-Actions (`setWizardStarted`, `setWizardStep`, `setWizardSkipped`, `setWizardCompleted`); Layout → `getWizardStateForCurrentUser`.
- Cross-Role-Block doppelt (Layout-Helper + Server-Actions-Guard).
- Live-DB-Schema (MIG-029 Spalten + Default `pending`).
- Multi-Admin-Race-Verhalten via SQL-Simulation gegen Live-Coolify-DB.
- Stub-Detection in allen neuen Files.
- Test-Run lokal + gegen Coolify-Live-DB.
- Live-URL-Reachability (3 Versuche).

## 2. Pass / Fail / Mixed

**MIXED — Code-PASS, Browser-Smoke MT-7 BLOCKED (deploy-conditional).**

- **Code + Wiring + Logic + Live-DB + Multi-Admin-Race**: alle PASS.
- **Browser-Smoke MT-7 (SC-V4.2-9 Pflicht-Gate)**: BLOCKED. Drei voneinander unabhaengige Vorbedingungen muessen vor Smoke erfuellt sein:
  1. SLC-047 ist nur committed (e4edcfc), nicht deployed → User-Coolify-Deploy noetig.
  2. Demo-Tenant hat `onboarding_wizard_state='completed'` + 4 capture_sessions → DB-Reset auf einem Test-Tenant noetig.
  3. Live-Site liefert persistent HTTP 504 Gateway Timeout extern (siehe ISSUE-030, separates V4.1-Live-Issue, NICHT SLC-047-Verschulden).

## 3. Findings (gruppiert nach Severity)

### Blocker
Keine.

### High

- **Pflicht-Gate Browser-Smoke MT-7 (SC-V4.2-9) ausstehend**: Nicht-Tech-User-Persona-Durchlauf des kompletten Wizards ist zentraler Akzeptanz-Beweis fuer FEAT-031. Aktuell weder durchgefuehrt noch zeitnah durchfuehrbar (3 Vorbedingungen). Severity High weil Pflicht-Gate aus Slice-Spec, nicht weil Code-Lage gefaehrdet ist.

### Medium

- **Step-2 Template-Auswahl persistiert NICHT ueber Browser-Reload**: Der Wizard speichert `selectedTemplateId` nur im React-State (`Wizard.tsx:71`). Nach Reload waehrend Step 2/3/4 wird Default = erstes Template wiederhergestellt. Nicht in Acceptance-Criteria spezifiziert (AC-9 fordert nur Step-Persistenz). Akzeptabel — User kann Auswahl wiederholen. Optional zu fixen in V4.3 Maintenance: zusaetzliche Spalte `tenants.onboarding_wizard_template_id` oder LocalStorage-Echo.

### Low

- **Doppelter `supabase.auth.getUser()` in `dashboard/layout.tsx`**: einmal indirekt in `getWizardStateForCurrentUser` (SLC-046), einmal direkt in `layout.tsx:31` fuer Tenant-Name-Fetch. Performance-Mikro-Issue (zwei statt eines Auth-Roundtrips, nur wenn shouldShow=true). Nicht-Blocker, V4.3-Polish-Kandidat.
- **Slice-Spec-Drift `inviteEmployees` (Plural) vs. real `inviteEmployee` (Singular)**: Bereits in RPT-118 dokumentiert + IMP-225 (Dev-System) zur Vermeidung in zukuenftigen Slice-Specs eingetragen. Code waehlt korrekt Singular im Loop. Funktional korrekt.
- **Fehlende React-Component-Tests via jsdom/testing-library**: Bereits in RPT-118 dokumentiert + IMP-224 (Dev-System) als bewusste Pattern-Entscheidung (Logic-Extraction + Browser-Smoke als Render-Gate). Konsistent mit Projekt-Pattern (0 .test.tsx-Dateien). Render-Verhalten waere durch MT-7 abgedeckt — der ist aber blocked (siehe High).

### Stub-Detection

`grep -E "TODO|FIXME|HACK|XXX|console\.log|debugger|@ts-ignore|@ts-nocheck"` ueber `src/components/onboarding-wizard/` UND `src/app/dashboard/layout.tsx`: **0 Treffer**. Keine offenen Platzhalter, keine Debug-Reste, keine TS-Eskapaden.

### Wiring-Verifikation (frontend↔backend Chain)

| Step | Status | Detail |
|---|---|---|
| Wizard → setWizardStarted | ✓ | Return-Type `{ok, alreadyStarted}` \| `{ok:false,error}` matched 1:1 mit `Wizard.tsx:87-94` |
| Wizard → setWizardStep | ✓ | `step:1\|2\|3\|4` Param passt, `wrong_state` Error wird in `translateError` gemappt |
| Wizard → setWizardSkipped | ✓ | Allowed aus pending+started, Server-Action verhindert Idempotenz-Fehler |
| Wizard → setWizardCompleted | ✓ | Nur aus 'started' erlaubt, Wizard ruft nur auf Step 4 |
| Step3 → inviteEmployee | ✓ | Return-Type `{ok:true, invitationId, emailFailed?:boolean}` \| `{ok:false, error:string}` matched mit `Step3EmployeeInvite.tsx:115-122`. Loop-Aggregation korrekt. Alle 6 Error-Codes (`invalid_email`, `duplicate_pending_invitation`, `forbidden`, `unauthenticated`, `rpc_failed`, `unknown_error`) in `ERROR_LABEL`-Mapping. |
| Layout → getWizardStateForCurrentUser | ✓ | shouldShow-Branching + state.step korrekt weitergereicht; doppelter Cross-Role-Check (Layout + Server-Action) |

### Cross-Role-Verifikation

`get-wizard-state.ts:50` → `if (!profile \|\| profile.role !== "tenant_admin" \|\| !profile.tenant_id) return DEFAULT_HIDDEN;`
`wizard-actions.ts:50` (`requireTenantAdmin`) → identische Pruefung, blockt strategaize_admin/tenant_member/employee mit `forbidden` falls jemand die Server-Actions trotzdem aufrufen wuerde (Defense-in-Depth).

Live-DB-Inventur: 1 strategaize_admin, 1 tenant_admin, 1 employee. Cross-Role-Block strukturell verifiziert. Browser-Display-Verifikation pending Deploy.

### Multi-Admin-Race (Live-Coolify-DB)

SQL-Simulation gegen Live-Coolify-DB:
```sql
WITH first AS (UPDATE tenants SET state='started' WHERE id=X AND state='pending' RETURNING 'won'),
     second AS (UPDATE tenants SET state='started' WHERE id=X AND state='pending' RETURNING 'won')
SELECT * FROM first UNION ALL SELECT * FROM second;
```
Resultat: nur `admin1_won` (1 Row). `admin2_won` 0 Rows = `alreadyStarted=true`-Pfad. ✓

### Skip-Pfade pro Step

| Step | Skip-Verhalten | Code-Verifikation |
|---|---|---|
| 1 | "Spaeter" → setWizardSkipped → close | `Wizard.tsx:225-232` ✓ |
| 2 | "Spaeter" → setWizardSkipped → close | `Wizard.tsx:225-232` ✓ |
| 3 | "Spaeter einladen" → setWizardStep(4), KEIN globaler Skip | `Wizard.tsx:153 onSkipStep={() => void handleSetStep(4)}` ✓ |
| 4 | "Schliessen + nicht mehr zeigen" → setWizardSkipped | `Wizard.tsx:255-262` ✓ |
| 4 | "Erledigt" → setWizardCompleted | `Wizard.tsx:264-272` ✓ |
| 1..3 | ESC / Outside-Click → setWizardSkipped (via `onInteractOutside` + `onOpenChange`) | `Wizard.tsx:191-201`, `:176-186` ✓ |

### Tests

| Suite | Lokal | Coolify-DB |
|---|---|---|
| `wizard-helpers.test.ts` (17 Tests) | ✓ 17/17 in 8ms | ✓ 17/17 in 6ms |
| `persistence.test.ts` (9 Tests) | ✓ 9/9 in 5ms | ✓ 9/9 in 4ms |
| **Gesamt** | **26/26 PASS in 524ms** | **26/26 PASS in 419ms** |

`tsc --noEmit`: Exit 0.
`npm run build`: PASS (Next.js 16.2.4, /dashboard route enthaelt das neue Layout).

### Live-DB-Schema (MIG-029)

- `tenants.onboarding_wizard_state` text DEFAULT `'pending'::text` ✓
- `tenants.onboarding_wizard_step` integer ✓
- `tenants.onboarding_wizard_completed_at` timestamp with time zone ✓

## 4. Open Risks / Missing Checks

- **MT-7 Browser-Smoke** (Pflicht-Gate SC-V4.2-9): nicht durchgefuehrt. 3 Vorbedingungen oben.
- **Echter Browser-Render-Smoke** der Modal-Animation, RadioGroup-Interaktion, Toast-Display, Responsive-Layout (Mobile + Desktop): nicht durchgefuehrt.
- **Echter Browser-Reload-Persistenz-Test** (state='started' + step=3 → Reload → User landet wirklich auf Step 3): Logic-getestet via `deriveInitialUiState`, nicht End-to-End-getestet.
- **Echter Browser-Cross-Role-Test** (strategaize_admin sieht keinen Wizard im Browser): Logic + DB-Schema verifiziert, nicht Browser-Render-verifiziert.
- **Echter Error-Boundary-Smoke** mit kontrolliertem JS-Throw → setWizardSkipped + Fallback-UI: Code-strukturell-korrekt, nicht Browser-getestet.
- **Echter Step-3 Live-SMTP-Pfad** (Mitarbeiter-Einladung wird tatsaechlich versendet): Wiring zu `inviteEmployee` vorhanden, SMTP-Pfad in V4 SLC-034 schon Live-verifiziert. Nicht erneut getestet.

## 5. Readiness Assessment

**Code-PASS / Browser-Smoke pre-deploy ausstehend.**

- Code-Qualitaet: hoch. Stub-frei, Wiring konsistent, Cross-Role doppelt geprueft, Tests grun lokal + Coolify-DB.
- Implementation-Vollstaendigkeit: 100% gegen Slice-Spec (alle 14 ACs strukturell verifizierbar).
- Browser-Verifikation: 0% (3 Vorbedingungen offen).
- Konfidenz fuer Logic + Build + Type-Safety + Wiring: hoch.
- Konfidenz fuer User-Erlebnis + Modal-Render + Live-DB-Persistenz: niedrig (nicht End-to-End-verifiziert).

**Empfehlung:** SLC-047 wird auf Status `done` belassen (Code-Lage rechtfertigt das), aber die Slice ist NICHT release-bereit bis MT-7 erfolgt ist. Cockpit-Records reflektieren diesen Zwischenstand explizit.

## 6. Recommended Next Step

1. **User-Coolify-Deploy** der Commit `e4edcfc` auf https://onboarding.strategaizetransition.com.
2. **Doctor: ISSUE-030** Live-504-Gateway-Timeout fixen (Coolify-Proxy/Traefik-Routing). Pre-Existing V4.1-Bruch, separater Pfad.
3. **DB-Reset** auf einem Test-Tenant: `UPDATE tenants SET onboarding_wizard_state='pending', onboarding_wizard_step=1, onboarding_wizard_completed_at=NULL WHERE id=<test-tenant-id>;` UND sicherstellen dass dieser Tenant 0 capture_sessions hat (oder Test-Tenant frisch anlegen).
4. **MT-7 Browser-Smoke** mit Nicht-Tech-User-Persona durchlaufen: Login → Wizard erscheint → Step 1 Begruessung → Step 2 Template → Step 3 1-2 Mitarbeiter einladen → Step 4 "Wissenserhebung starten". Reload-Test zwischen Step 2 und 3. Cross-Role-Test mit strategaize_admin-Login (Wizard darf NICHT erscheinen).
5. **Re-QA SLC-047** nach Smoke: Resultat in RPT-119a oder RPT-120 als Folge-Review (`reviewOf: RPT-119`).
6. **Parallel moeglich:** /backend SLC-048 (Cron + SMTP + Unsubscribe).

## 7. Project Record Updates (in dieser /qa-Session)

| Record | Update |
|---|---|
| `/docs/KNOWN_ISSUES.md` | ISSUE-030 hinzugefuegt: Live-504 Gateway Timeout extern |
| `/reports/RPT-119.md` | dieser Report |
| `/docs/STATE.md` | Current Focus: SLC-047 Code-PASS in /qa, Next Step = User-Deploy + Doctor ISSUE-030 + MT-7 Re-QA |
| `/slices/INDEX.md` | Status SLC-047: bleibt `done` (Code), Notiz "Browser-Smoke pending Deploy" |
| `/planning/backlog.json` | BL-048 status bleibt `done` (Code), unveraendert |
