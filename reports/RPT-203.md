---
id: RPT-203
date: "2026-05-11"
skill: /backend
slice: SLC-092
feature: FEAT-038
type: completion
status: completed
result: "ISSUE-046 Inline-Hotfix vor REL-014 — UUID_RE-Validate-Guard in Embed-Route, 13/13 Vitest PASS, Lint clean, ISSUE-046 → resolved"
title: "ISSUE-046 Hotfix — Embed-Route UUID-Validation (400 statt 500)"
---

# ISSUE-046 Hotfix — Embed-Route UUID-Validation

## 1. Outcome Summary

ISSUE-046 (Embed-Route gibt 500 statt 400 bei invalidem UUID-Format) **inline gefixt vor REL-014**. User-Entscheidung 2026-05-11: Hotfix vor REL-014 statt V5.2-Slice (Recommended-Option), damit REL-014 ohne offene Medium-Issues geht.

Fix: UUID_RE-Konstante + Pre-Validate-Guard in `src/app/api/walkthrough/[sessionId]/embed/route.ts`. Pattern-Reuse aus `src/app/admin/handbook/actions.ts:39`. Bei `GET /api/walkthrough/not-a-uuid/embed` jetzt **400 BAD_REQUEST** statt 500 INTERNAL_ERROR, getUser+rpc gar nicht erst aufgerufen. Aufwand exakt wie geschaetzt (~10min).

**Verifikation:** 13/13 Vitest PASS in 19ms (vorher 12/12 + neuer 400-Pfad-Test mit Mock-Call-Count-Assertions). Lint clean. Live-Smoke gegen Onboarding-Server steht nach User-Coolify-Redeploy aus.

## 2. Files Reviewed

- /reports/RPT-202.md (SLC-092 /qa Report mit ISSUE-046-Befund Detail)
- /docs/KNOWN_ISSUES.md (ISSUE-046-Eintrag mit Next-Action-Pattern-Hinweis)
- /src/app/admin/handbook/actions.ts (Pattern-Vorbild Zeile 23+39 fuer UUID_RE-Konstante + Guard)
- /src/app/api/walkthrough/[sessionId]/embed/route.ts (zu fixende Route)
- /src/app/api/walkthrough/[sessionId]/embed/__tests__/route.test.ts (12 bestehende Tests)

## 3. Files Changed

- **/src/app/api/walkthrough/[sessionId]/embed/route.ts** — UUID_RE-Konstante eingefuehrt (Z. 27) + Pre-Validate-Guard `if (!sessionId || !UUID_RE.test(sessionId))` (Z. 34) statt nur `if (!sessionId)`. Error-Message von "sessionId required" auf "sessionId muss ein valides UUID sein" praezisiert. Pattern-Reuse aus `actions.ts:39` (strict 8-4-4-4-12-Format). Catch-Block fuer echte RPC-Fehler unveraendert.
- **/src/app/api/walkthrough/[sessionId]/embed/__tests__/route.test.ts** — NEUER Test-Case "400 wenn sessionId kein valides UUID-Format hat (ISSUE-046)" zwischen den bestehenden "400 wenn sessionId leer"- und "401"-Cases. Asserts: status=400, body.error.code=BAD_REQUEST, getUserMock NICHT aufgerufen, rpcMock NICHT aufgerufen (verifiziert dass Guard VOR Auth+RPC greift). 13/13 Tests PASS.
- **/docs/KNOWN_ISSUES.md** — ISSUE-046 Status `open` → `resolved`. Resolution-Block mit Implementation-Detail + Vitest-Verifikation + Pattern-Reuse-Reference + Live-Smoke-Pending-Note.
- **/docs/STATE.md** — Current Focus auf Hotfix-Outcome aktualisiert, Immediate Next Steps neu sortiert (User-Coolify-Redeploy + Live-Smoke 400-Pfad als Schritt 1, dann Gesamt-/qa + Final-Check + Go-Live + Deploy). Option-5 zur Pre-Deploy-Entscheidung aus Liste entfernt (Frage geklaert).

## 4. Files Created

- **/reports/RPT-203.md** — dieser Hotfix-Completion-Report.

## 5. Files Intentionally Not Changed

- **/slices/INDEX.md** — V5.1 SLC-091 + SLC-092 bleiben auf `done`. Hotfix ist In-Slice-Korrektur, kein neuer Slice.
- **/features/INDEX.md** — FEAT-038 bleibt `in_progress` (wird `done` nach REL-014-Deploy).
- **/planning/backlog.json** — BL-082 bleibt `in_progress` (gleiche Logik).
- **/planning/roadmap.json** — V5.1 bleibt `active`.
- **/docs/MIGRATIONS.md** — keine Schema-Aenderung durch Hotfix.
- **/docs/DECISIONS.md** — kein ADR noetig (Pattern-Reuse, keine neue Architekturentscheidung).

## 6. Problems Found

**Keine.** Hotfix landete sauber in einem Iterationsschritt:
- Vitest 13/13 PASS in 19ms (Erstlauf)
- Lint 0 Warnings/Errors auf beide modifizierten Files
- Pre-Validate-Guard-Pattern bestand mit identischer Form bereits in `actions.ts:39` — kein neuer Pattern noetig.
- Error-Body kompatibel zur bestehenden Client-Logik (gleicher `{error:{code,message}}`-Shape).

## 7. Open Points / Deferred Decisions

1. **Live-Smoke nach User-Coolify-Redeploy:** Smoke-Test gegen Onboarding-Server (`curl -i https://onboarding.strategaizetransition.com/api/walkthrough/not-a-uuid/embed` muss `400` + JSON-Body mit `code: "BAD_REQUEST"` liefern statt 500). Erst dann `/qa`-Gesamt-V5.1 starten.
2. **Pattern-Lift in andere API-Routes:** Andere V5+-Routen, die UUIDs aus dynamischen Route-Params akzeptieren, koennten denselben Pre-Validate-Guard brauchen. Out-of-Scope hier — wenn relevant, im naechsten Maintenance-Sprint mitprueffen.

## 8. Project Record Updates

- /docs/KNOWN_ISSUES.md — ISSUE-046 `open` → `resolved` + Resolution-Block.
- /docs/STATE.md — Current Focus + Immediate Next Steps aktualisiert.
- /reports/RPT-203.md — dieser Report (NEU).
- /slices/INDEX.md — unveraendert (V5.1 bleibt 2/2 done).
- /planning/backlog.json + /planning/roadmap.json + /features/INDEX.md — unveraendert (Status-Transitions kommen erst nach REL-014-Deploy).

## 9. Recommended Next Step

**User-Coolify-Redeploy** auf den Hotfix-Commit, danach **Live-Smoke 400-Pfad** (1 curl), danach **Gesamt-/qa V5.1** ueber SLC-091 + SLC-092 zusammen mit Pre-V5.1-Regression-Checks (Bridge-Engine + Capture-Flow + Handbook-Generate-Cycle weiter funktional), danach **/final-check V5.1**, danach **/go-live V5.1**, danach **/deploy als REL-014**.

Die `/final-check`-Conditional-GO-Wahrscheinlichkeit ist durch diesen Hotfix reduziert — REL-014 geht jetzt ohne offene Medium-Issues.
