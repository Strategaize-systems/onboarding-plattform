---
id: RPT-538
date: "2026-06-29"
skill: /final-check
slice: V10-Gesamt
feature: FEAT-092
type: review
status: reviewed
reviewOf: RPT-537
result: "CONDITIONALLY READY for /go-live — V10 StB-Vertikale Pre-Release-Audit. Code-Side clean: tsc 0 (whole-repo), eslint 0 (StB surface), vitest 122/122 (15 files), next build EXIT 0 (placeholder env), 0 new deps (pure reuse), 0 hardcoded secrets, 0 console/TODO leftovers, 0 raw-HTML/XSS surface. Migrations 126/127 idempotent; RPC SECURITY DEFINER + SET search_path=public + service_role-only GRANT + tenant-scoped. Env-gate fail-closed (=== 'true', default OFF). 0 Blocker/High/Medium introduced by V10. Conditions are all /deploy-stage: MIG-127 live-apply, live worker/diagnosis smoke, RLS cross-tenant sidecar, SLC-172/175 browser-smoke, and the BEDROCK_V9_SONNET_MODEL_ID env (ISSUE-099 coupling) must be the eu-prefixed Sonnet 4. Pre-existing open Highs (084/085 V8.1 mock texts, 093/099 V9 Bedrock) are out of V10 scope. NEXT /go-live."
title: "Final-Check V10 (StB-Vertikale) — CONDITIONALLY READY for /go-live"
---

# Final-Check — OP V10 StB-Vertikale (Pre-Release-Audit)

**Verdict: CONDITIONALLY READY for `/go-live`.** V10 is code-side release-credible; the remaining conditions are all `/deploy`-stage live-validation items (consistent with the Founder/Coolify-Hold), plus one env-coupling check. No code defect blocks the release gate. Audited against branch `v10-slc170b-blueprint-seed` (worktree `…-v10`), 17 commits / 60 files ahead of `main`. No deploy performed.

## 1. Final-check scope reviewed
The full V10 StB-Vertikale scope (Gesamt-QA RPT-537 PASS is the precondition; this is the broader pre-release audit beyond slice QA): code quality/hygiene, security/privacy, data-residency, legal/compliance posture, testing depth, CI/CD + deploy readiness, observability/ops, post-go-live readiness. Independent gate re-runs + secrets/console/XSS scans + migration security review + dependency-delta + KNOWN_ISSUES cross-check.

## 2. Overall assessment
**CONDITIONALLY READY.** Proceed to `/go-live` (release-gate). The conditions below are satisfied/verified during `/deploy`, not before the gate.

## 3. Findings by severity
- **Blocker:** none.
- **High:** none introduced by V10. (Pre-existing repo-level open Highs noted in §5 — out of V10 scope.)
- **Medium:** none introduced by V10.
- **Low:**
  - **L-1 (deploy-coupling, ISSUE-099):** `synthesize-module-output.ts` `resolveModelId` reads `BEDROCK_V9_SONNET_MODEL_ID` (then `LLM_MODEL`, then the correct `eu.anthropic.claude-sonnet-4-20250514-v1:0` default). If the Coolify env is set to a RAW (non-`eu.`-prefixed) model id — the ISSUE-099 root cause — V10's live synthesis would also fail. Code default is correct; this is an env/deploy condition, caught by the deferred live-worker smoke.
  - **L-2 (deploy-clarity):** `NEXT_PUBLIC_ENABLE_STB_VERTIKALE` is not documented in `.env.example` (the repo keeps no populated `.env.example`). Default is fail-closed OFF, so functionally safe; document the flag at deploy so it stays OFF until Modul-1 complete (`module-lifecycle-discipline`).
  - **L-3 / L-4 (by-design, from RPT-537):** 17/18 routing `modul_key`s point to not-yet-seeded modules (UI shows "geplant" badge, no dead link); MIG-127 pseudo-`block_checkpoint` has no `quality_report` (worker reads it optionally). Both recorded, no action.

## 4. Audit findings by dimension

### Code quality & maintainability — PASS
0 `console.*`/`debugger`/`TODO`/`FIXME` in the new StB surface (`src/lib/stb-vertikale`, `src/components/stb`, `src/app/dashboard/stb`). No stubs (RPT-537 C4). `tsc --noEmit` whole-repo **EXIT 0**; `eslint` StB surface **EXIT 0**. Pure reuse of existing render/print/diagnosis/worker patterns.

### Security & privacy — PASS
0 hardcoded secrets in the V10 `src/**`+`sql/**` diff. No `dangerouslySetInnerHTML` / `rehype-raw` / `sanitize:false` in the StB surface (react-markdown default-safe; tenant-own authenticated content). RLS: `modul_output` tenant-read policy (MIG-124), no manual tenant filter in reader. New RPC `rpc_seed_blueprint_diagnosis_input` (MIG-127): `SECURITY DEFINER` **+ `SET search_path = public`** (hardened against the storage-before-public trap — the rule from `security-audit-fable5-standard.md`), `GRANT EXECUTE … TO service_role` only, tenant resolved server-side with NULL-guard. Env-gate `isStbVerticalEnabled()` fail-closed (`=== "true"`).

### Data-residency — PASS
V10 LLM paths stay EU: module-output worker hardcodes `BEDROCK_MODULE_REGION = eu-central-1`; the reused diagnosis path uses `chatWithLLM` (`src/lib/llm.ts`, the known-good `eu.`-prefixed Sonnet 4 adapter). No US endpoint, no `api.openai.com`/`api.anthropic.com` direct. ISSUE-107 audit-trail goes to `error_log` (provider/region/model). Conforms to `data-residency.md`.

### Legal & compliance — PASS (scope-appropriate)
V10 ships behind an OFF env-gate (Internal-Test-Mode, single-Founder, no customer data) → `module-lifecycle-discipline`. No new public surface, no new PII collection beyond the tenant's own questionnaire answers (RLS-isolated). DSGVO/consent obligations belong to the later customer-live gate, not this internal release.

### Testing depth — PASS
`vitest` StB scope **122/122 GREEN** (15 files): blueprint routing/diagnosis, workspace-read grouping/sort, components (cards, KI-Hebel, subtopic-diagnosis), module-output worker. Hermetic; DB-backed paths (RLS, live worker) are skip-gated and explicitly deferred to deploy-validation (honest limit, not a gap).

### CI/CD & deployment readiness — CONDITIONAL
`next build` **EXIT 0** with placeholder Supabase env (the env-less failure at `/api/public/partner/[slug]` is pre-existing ISSUE-101, not V10). No `package.json`/lockfile change → zero supply-chain delta. Migration ordering for `/deploy`: **MIG-126 already DB-live**; **MIG-127 needs live-apply** (`sql-migration-hetzner.md`), both idempotent (`ON CONFLICT DO UPDATE` / `CREATE OR REPLACE`). Env conditions: keep `NEXT_PUBLIC_ENABLE_STB_VERTIKALE` OFF; ensure `BEDROCK_V9_SONNET_MODEL_ID` = `eu.anthropic.claude-sonnet-4-20250514-v1:0` (L-1 / ISSUE-099).

### Observability & ops — PASS (scope-appropriate)
Reused workers log via the existing logger/`error_log`; cost via `ai_cost_ledger` (module-output) — Blueprint micro-assess calls intentionally not ledgered (ISSUE-107, Low, EU audit in `error_log`). No new observability obligation for an OFF-gated internal release.

### Post-go-live readiness — PASS
Records current on the branch (slices/features/backlog/roadmap, STATE, KNOWN_ISSUES, MIGRATIONS, DECISIONS). Reports RPT-523…537 present. Deferred deploy-validation items are explicitly tracked (RPT-537 §5).

## 5. Explicit release risks
1. **Deploy-coupling (L-1, ISSUE-099):** shared `BEDROCK_V9_SONNET_MODEL_ID` env — verify it is the `eu.`-prefixed Sonnet 4 before/at deploy, else V10 live synthesis fails.
2. **Deferred live-validation (inherent to any deploy, under Founder-Hold):** MIG-127 live-apply, live Bedrock worker output (`output_kind`/evidence survive `reconcileEvidence`), full `diagnosis_generation` cards, RLS cross-tenant sidecar (`coolify-test-setup.md` node:20 SAVEPOINT), SLC-172/175 live browser-smoke. All belong to `/deploy`, not the go-live gate.
3. **Pre-existing open Highs NOT in V10 scope (out-of-scope, surfaced for honesty):** ISSUE-084/085 (V8.1 mandanten-report mock texts, Founder-approval pending — V8.1 release-blockers, unrelated to StB-Vertikale), ISSUE-093 (V9 Haiku-3 deprecation — V10 uses no Haiku), ISSUE-099 (see L-1, env-fixable). V10 neither introduces nor worsens these; its surface is `dashboard/stb/*` behind the OFF gate.

## 6. Required fixes before go-live
None (code-side). `/go-live` may proceed. The conditions above are `/deploy`-stage gates.

## 7. Accepted residual risks
- L-1..L-4 accepted as documented (env-condition + deploy-clarity + two by-design).
- Live-validation deferral accepted (matches Founder/Coolify-Hold + prior-version precedent, e.g. V9 SLC-165 AC-8).
- Pre-existing out-of-scope open Highs remain tracked in KNOWN_ISSUES; they are not V10 go-live blockers but must be closed before their own features (V8.1 / customer-live) ship.

## 8. Recommended next step
**`/go-live V10`** (release-gate GO/NO-GO). Then `/deploy V10` (MIG-127 live-apply + `BEDROCK_V9_SONNET_MODEL_ID` env check + full worker/diagnosis + browser + RLS-sidecar smoke) → `/post-launch V10`. Per `module-lifecycle-discipline`: internal Workflow-Closure only — no customer-outreach / legal / DSGVO-pre-live until Modul 1+2+3 complete. No cross-repo / topic switch before the release gate.

## 9. Files reviewed (audit)
- Gates/scan targets: `src/lib/stb-vertikale/**`, `src/components/stb/**`, `src/app/dashboard/stb/**`, `src/workers/stb-vertikale/**`
- `sql/migrations/126_v10_stb_blueprint_seed.sql`, `sql/migrations/127_v10_slc172_blueprint_diagnosis_seed_rpc.sql`
- `src/lib/stb-vertikale/feature-gate.ts`, `synthesize-module-output.ts`, `blueprint-diagnosis.ts`, `blueprint-routing.ts`, `workspace-read.ts`
- `package.json` (no delta), `docs/KNOWN_ISSUES.md` (issue cross-check), `reports/RPT-537.md` (Gesamt-QA precondition)
