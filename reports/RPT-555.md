---
id: RPT-555
date: "2026-07-03"
skill: /qa
slice: SLC-180
feature: FEAT-097
type: completion
status: completed
reviewOf: RPT-554
result: "SLC-180 /qa PASS (Code-Side). Stub-Scan clean, Wiring-Chain 6/6 verifiziert, Shared-Wizard 0-Regression, AC-180-1..4 erfuellt. 1 Low: AC-180-5 Browser-Smoke /deploy-gated. SLC-180 = done, FEAT-097 = done. NEXT /backend SLC-181."
title: "SLC-180 Inline-Rückfrage-UI + Followup-Merge — /qa"
---

# SLC-180 — Inline-Rückfrage-UI + Followup-Merge (/qa)

## QA scope reviewed
Die 6 SLC-180-Files (`module-context.ts` mergeAnswers, `submit-action.ts` followup-carry, neue `InlineRueckfrage.tsx`, `questionnaire-form.tsx` Wiring, modul block-page Prop, `module-context.test.ts`). Verifiziert: Stub-Patterns, Wiring-Chain, Shared-Wizard-Regression, AC-180-1..5, Test-Suite.

## Result: **PASS (Code-Side)**

## Findings

### Blocker / High / Medium
Keine.

### Low
- **AC-180-5 Browser-Smoke nicht ausgeführt** (Render + Interaktion + 0 Console-Errors). Der StB-Modul-Flow braucht auth'd StB-Session + geseedete M-04-Template-Session + Bedrock-Haiku-Creds; lokal ohne laufende App/Coolify-DB nicht renderbar. **/deploy-gated** — konsistent zur Behandlung der Live-gated Lows in SLC-178 (Badge) + SLC-179 (Live-Bedrock/Write-Path). Der statische/Build-Teil von AC-180-5 (tsc0/eslint0/next build) ist erfüllt.

### Reviewed & accepted (kein Finding)
- `runAssessment` leerer `catch {}` — dokumentierter **fail-open** (spiegelt `assessModulAnswer`-Contract; Fehler werden server-seitig in `error_log` protokolliert). Kein stiller Swallow im kritischen Pfad.
- `catch (err)` in `transcribeRecording` (Zeile 543) — pre-existing Voice-Handler mit `console.error`, nicht SLC-180.

## Stub detection
Scan über die 5 Non-Test-Files: **0 Treffer** (TODO/FIXME/HACK/debugger/console.log/empty-catch/hardcoded-URL/dummy-data).

## Wiring verification (FE+BE-Slice, SaaS mandatory)
Kette **6/6 verbunden**:
1. Gate: `modulKey && activeQ` (q-form:396) + Render-Gates (987/993) → nur StB-Modul-Flow.
2. Trigger: `handleSaveAnswer` → `runAssessment` → `assessModulAnswer(sessionId, modulKey, activeQ.frage_id, textToSave)` (430) — **frage_id** (Business-ID, Backend matcht darauf).
3. Persist: `saveAnswer(sessionId, "followup."+blockKey, q.id, text)` → Key `followup.<block>.<uuid>`.
4. Submit-Carry: `followupPrefix` (submit-action:128-131) trägt Key verbatim in Checkpoint (separater Pass nach Eltern-Antworten → Ordering für mergeAnswers garantiert).
5. Merge: `mergeAnswers` `startsWith("followup.")` → `[Nachfrage]`-Append an Eltern-Antwort (module-context:155-158).
6. Synthese: `assembleQaPairs` liest angereicherte Antwort via `answers.get(q.id)`.
Key-Split-Invariante `split(".").pop()` robust: StB-Block-Keys (`stufe1_kern` etc.) + UUIDs enthalten keine Punkte (gleiche Invariante wie bestehendes `evidence.*`).

## Regression check
**0-Regression Shared-Wizard**: alle neue Logik `modulKey`-gated; general capture page + employee capture page reichen `modulKey` NICHT durch → alle Guards short-circuiten → GF-/Employee-Flow verhaltensidentisch.

## Acceptance
- AC-180-1 (inline Rückfrage bei geflaggter Kern-Frage): **erfüllt** (Code; Browser /deploy).
- AC-180-2 (followup.<block>.<qid>-Merge → Synthese-Intake): **erfüllt, unit-tested** (2 neue Tests: followup allein + evidence+followup kombiniert).
- AC-180-3 (Trigger-Hit in metadata + F-E-Heilung): **erfüllt** (server-seitig SLC-179; Client re-assesst angereicherte Antwort).
- AC-180-4 (Guardrail max 2/Block): **erfüllt** (server-seitig `MAX_RUECKFRAGEN_PER_BLOCK`, SLC-179-Tests).
- AC-180-5 (Browser-Smoke + tsc0/eslint0/build): **teilweise** — Build/Static PASS; Browser-Smoke /deploy-gated (Low).

## Test execution
`vitest run src/lib/stb-vertikale` → **130 passed / 14 files**. `module-context.test.ts` 8/8 (2 neu). 3 fail nur in `persist.test.ts` = TEST_DATABASE_URL-gated (pre-existing, kein SLC-180-Bezug). tsc0, eslint0, `next build` EXIT 0.

## Readiness
SLC-180 Code-Side ready. FEAT-097 (SLC-179 + SLC-180) komplett. Browser-/Live-Validierung (AC-180-5) bündelt mit /deploy — die V10.1-Gesamt-QA + /deploy verifizieren die Live-E2E-Kette (Save→Assess→Rückfrage→Merge→Heilung) an der laufenden App.

## Next workflow step
**`/backend SLC-181`** (SOP-Brücke, FEAT-098) — SLC-181 ist der letzte offene V10.1-Slice. Danach **Gesamt-QA V10.1** über die gesamte Version, dann `/final-check` → `/go-live` → EIN Master-Merge (Pre-Merge-Re-Check 177→181) → `/deploy` (Live-E2E + AC-180-5 Browser-Smoke) → `/post-launch`. Kein Push/Merge bis dahin (Founder-Hold).
