---
id: RPT-626
date: "2026-07-09"
skill: /qa
slice: SLC-191
feature: FEAT-108
type: completion
status: completed
reviewOf: null
result: "SLC-191 /qa PASS-CODE / PENDING-LIVE — 33/33 Modul-Tests + 193/193 pdf-Suite grün, tsc0/eslint0, build PASS; 0 Blocker/0 High; Route-Auth/Tier/PDF = Live-Smoke bei Gesamt-QA/Deploy"
title: "QA SLC-191 — Exit-/Devil's-Advocate-Report Kern (MT-0..MT-5)"
---

# QA SLC-191 — Exit-/Devil's-Advocate-Report Kern

/qa nach /backend SLC-191 (alle 6 MTs code-done). Verdict-Aggregat: **PASS-CODE / PENDING-LIVE**. Reine Logik ist TDD-verifiziert; der Route-Serving-Pfad (Auth/Tier/PDF-Response) ist Code-side belegt (build+tsc+eslint) und wird bei Gesamt-QA V10.5 / Deploy live gesmoked (Code noch nicht deployed).

## 1. QA-Scope
Neue Dateien (8): `src/lib/pdf/exit-report/{types.ts, data.ts, data.test.ts, owner-dependence.ts, owner-dependence.test.ts, framing.ts, framing.test.ts, renderer.tsx, renderer.test.ts, index.ts}` + `src/app/admin/debrief/[sessionId]/exit-report/route.ts`. Branch `v10-5-exit-report` (Worktree v10-5), Commits `89c059a`(MT-1) `b92d944`(MT-2) `827cebe`(MT-3) `57b93c0`(MT-4) `e19bee7`(MT-5).

## 2. Test-Ergebnisse
- **Exit-report-Modul: 33/33 GREEN** (data 11, owner-dependence 12, framing 8, renderer 2).
- **Gesamte `src/lib/pdf/`-Suite: 193/193 GREEN** (17 Files) — inkl. der Reuse-Quelle fahrplan-report (25) + mandanten-report-v2. **0 Regression** (Änderung rein additiv, kein Bestandsfile modifiziert).
- tsc `--noEmit` EXIT=0 · eslint EXIT=0 · `next build` EXIT=0 (Route als dynamic `ƒ /admin/debrief/[sessionId]/exit-report` registriert).
- Silent-Skip-Audit: TEST_DATABASE_URL-Warnung erscheint, betrifft die Pure-Tests nicht (0 DB-Tests im Modul; alle 33 liefen tatsächlich).

## 3. Per-MT-Verdicts
| MT | Inhalt | Verdict |
|---|---|---|
| MT-0 | Grounding-Spike (Live-DB) | **PASS-LIVE** (RPT-625; diagnosis_schema-Objekt-Form + Linkage + Fixture-Entscheid live geklärt) |
| MT-1 | types + Loader (buildExitReportInput/loadExitReportInput) | **PASS-CODE** (11 Fixture-Tests); `loadExitReportInput`-Live-Fetch PENDING-LIVE |
| MT-2 | Owner-Dependence-Index (DEC-273) | **PASS-CODE** (12 TDD, pure, deterministisch) |
| MT-3 | Käufer-Framing 3-Spalten | **PASS-CODE** (8 TDD, band-stabil) |
| MT-4 | Renderer + Barrel | **PASS-CODE** (2 renderToBuffer-Smokes, non-empty %PDF); visuelle PDF-Prüfung PENDING-LIVE |
| MT-5 | GET-Route | **PASS-CODE** (build+tsc+eslint); Auth/Tier/PDF-Response PENDING-LIVE |

## 4. Stub-Detection
Scan der 8 neuen Dateien: **0 echte Stubs.** Die `return null`-Treffer (data.ts:34/40, owner-dependence.ts:36/42) sind dokumentierte defensive Parse-Helfer (`asRecord`/`normAmpel`/`normRisiko`) — reviewed & accepted. Keine TODO/FIXME/HACK, kein console.log/debugger, kein empty-catch, keine Platzhalter-Returns, keine Hardcodes.

## 5. Wiring-Chain-Audit
1. **Caller-Existenz:** `route.ts` ruft `loadExitReportInput` + `renderExitReportPdf` (Production-Caller, kein Test-only). ✓
2. **Erreichbarkeit:** Route im Build als dynamic registriert; URL-gated (admin/berater). **Kein UI-Nav-Link** (URL-Zugriff wie fahrplan-report — s. Findings L-1). ✓ (URL-erreichbar)
3. **Render-Ende:** `renderExitReportPdf` → non-empty PDF-Buffer → `NextResponse` application/pdf (smoke-verifiziert). ✓
4. **Datenpfad:** `loadExitReportInput` liest capture_session + template(blocks,diagnosis_schema) + block_diagnosis/quality_report (via `loadFahrplanInput`); alle Spalten gegen Live-Schema geerdet (MT-0). ✓
5. **Barrel:** `route.ts` importiert aus `@/lib/pdf/exit-report` (Barrel), tsc-aufgelöst. ✓

## 6. Findings
### Low
- **L-1 (dieser Slice, Observation):** Die Exit-Report-Route hat **keinen UI-Nav-Link/Button** — Zugriff nur per URL. Konsistent mit der Fahrplan-Report-Route (die ebenfalls URL-gated ist). Ein Download-Button (z.B. im Debrief) wäre ein sinnvoller Folge-Slice, kein Blocker.
- **L-2 (PRE-EXISTING, out-of-scope Observation → ISSUE-120):** Bei der Answer-Key-Grounding fiel ein latenter Mismatch im **bestehenden V9.75-Code** auf: Der Debrief-Read-Pfad (`admin/debrief/[sessionId]/[blockKey]/page.tsx:193`) liest Answers mit `${blockKey}.${q.frage_id}`, während der Capture-Write-Pfad (`capture/.../actions.ts:55` + `questionnaire-form.tsx`) mit `${blockKey}.${q.id}` schreibt (q.id=uuid ≠ frage_id=F-BP-xxx). Nicht von V10.5 eingeführt; als **ISSUE-120** (Medium, needs-confirmation) dokumentiert. V10.5 selbst nutzt die **korrekte** Write-Konvention (`q.id`).

## 7. Offene Risiken / PENDING-LIVE (Nachhol bei Gesamt-QA V10.5 / Deploy)
- Route-Auth/Tier/PDF-Response-Smoke: 401 unauth · 403 fremd-tenant/free-tier · 403 berater-ohne-Zuweisung · 200 pdf (admin + berater-mit-Zuweisung). Erst gegen den **deployten** Container (Code ist branch-only).
- Echte-Session-Render: `loadExitReportInput` + `renderExitReportPdf` gegen eine real geseedete blueprint-Session mit befülltem diagnosis_schema + owner-dep-Antworten (die einzige Live-Session `c1c1…043a` ist ein stale Seed, MT-0) — Owner-Dependence-Index + Scorecard inhaltlich (PDF-Text-Extract) prüfen.
- Visuelle PDF-Prüfung (Hero/Scorecard/Findings-Layout).

## 8. Project-Record-Updates
- `reports/RPT-626.md` (dieser Report)
- `docs/KNOWN_ISSUES.md` — ISSUE-120 (pre-existing debrief answer-key-Mismatch, Medium)
- `slices/INDEX.md` — SLC-191 `planned` → `in_progress` (Mixed-Result: code-done, Live pending; kein `done` vor Live-Smoke)
- `slices/SLC-191-exit-report-kern.md` — Status → code-done
- `docs/STATE.md` — Current Phase auf SLC-191 code-done, next SLC-192
- BL-515 bereits `in_progress` (keine Änderung); roadmap V10.5 active (keine Änderung)

## 9. Next workflow step
**`/backend` SLC-192** (Positionierung: positioning/coverage[TDD] + Renderer-Einbettung + Wording-Review) — kumulativ auf `v10-5-exit-report` nach SLC-191. Danach `/qa` SLC-192, dann **Gesamt-QA V10.5** (holt die PENDING-LIVE-Smokes gegen den deployten Stand nach) → `/final-check` → `/go-live` → `/deploy` → `/post-launch`. (Kein Skip zu anderem Thema: V10.5 hat weder /final-check noch /go-live noch /post-launch.)
