---
id: RPT-446
date: "2026-06-11"
skill: /backend
slice: SLC-V9.1-D
feature: FEAT-079
type: completion
status: completed
reviewOf: null
result: "MT-7 docs done — COMPLIANCE.md Section 10 + RUNBOOK Setup-UI walkthrough + slice-spec as-built reconcile (IMP-1192) + DEC-196 superseded. No master-merge, no records-flip (gated behind Gesamt-V9.1-/qa)."
title: "SLC-V9.1-D MT-7 — COMPLIANCE + RUNBOOK + spec-prose reconcile (IMP-1192)"
---

# SLC-V9.1-D MT-7 — Docs & Reconcile

## 1. Outcome summary
**Complete (docs portion of MT-7).** Delivered the documentation deliverables of MT-7 and the IMP-1192 spec-prose reconciliation. All four artifacts are grounded in the **as-built** implementation (DEC-205/206/208/209), explicitly not the stale pre-implementation spec prose.

The remaining MT-7 items — **master-merge** and **records-flip to `done`** — are intentionally **NOT done**: they are gated behind a passing Gesamt-V9.1-/qa per AC-V9.1-D-10/12, `module-lifecycle-discipline`, and IMP-950. SLC-V9.1-D stays `in_progress`.

## 2. Files reviewed
- `/slices/SLC-V9.1-D-setup-ui-admin-audit.md` — spec, to reconcile (not as oracle)
- `/reports/RPT-443.md` (/backend), `/reports/RPT-445.md` (/qa) — as-built source of truth
- `/docs/DECISIONS.md` — DEC-196/205/206/208/209 (+ DEC-194/197/198/199/200/201 context)
- `/docs/COMPLIANCE.md`, `/docs/RUNBOOK.md` — existing docs (V6.2 / V9.1-partial)
- `/src/app/dashboard/bulk-email-import/forward-setup/actions.ts` + `page.tsx` + `MailClientInstructions.tsx`
- `/src/lib/inbound-email/imap-sync.ts`, `reject-log.ts`
- `/src/workers/retention/handle-bulk-email-retention-sweep.ts`
- `/src/lib/bulk-email/dsgvo-consent.ts`

## 3. Files changed
- `/docs/DECISIONS.md` — **DEC-196 `accepted` → `superseded`** with Superseded-Note pointing to DEC-205. DEC-196's own consequence anticipated this exact supersession on an EU-Frankfurt vendor switch; IONOS-IMAP (Germany) is that switch. Removes the eu-west-1 region-drift premise.
- `/docs/COMPLIANCE.md` — added **Section 10 "V9.1 Forward-Bucket-Email — Datenschutz-Addendum"** (+ IVZ entry + header status note). Covers: new data category (forwarded business emails), as-built data flow + storage regions (IONOS DE + Bedrock Frankfurt), **Cross-Region-TIA reconcile** (no cross-region transfer remains), the **4 audit/event mechanisms as-built** (error_log + `email_validation_reject_log`, never `audit_log`), retention/deletion (run-level, DEC-208), spam-defense (single-mailbox token-skip DEC-206), and a "what V9.1 does not change" note.
- `/docs/RUNBOOK.md` — appended **"V9.1 Setup-UI Founder-Walkthrough"** (5 steps + post-go-live verification SQL). As-built: 3 mail-client tabs (Gmail/Outlook/IONOS), no `X-Strategaize-Forward-Token` header in single-mailbox mode, test-send IONOS-gated (OQ-R1-1), DSGVO activation deliberately not test-send-gated.
- `/slices/SLC-V9.1-D-setup-ui-admin-audit.md` — added **"AS-BUILT RECONCILE" block** (IMP-1192) with a 12-row drift table mapping stale spec prose → as-built reality + source DEC/RPT; header `Status: planned` → `in_progress` with current code-side state. ACs left verbatim for audit, flagged as superseded by the table.
- `/docs/STATE.md` — Current Focus + Current Phase updated to "MT-7 Docs done, next Gesamt-V9.1-/qa".

## 4. Files created
- `/reports/RPT-446.md` — this completion report.

## 5. Files intentionally not changed
- `/slices/INDEX.md`, `/features/INDEX.md`, `/planning/backlog.json`, `/planning/roadmap.json` — SLC-V9.1-D / FEAT-079 / BL-158 stay `in_progress`; roadmap v9-1 stays `active`. Flipping to `done`/`released` is gated behind Gesamt-V9.1-/qa + the full release workflow (IMP-950). Changing them now would be a false-closure record.
- Source code under `src/` — MT-7 is docs/closure only; no code change needed (and none made).
- `/docs/KNOWN_ISSUES.md` — ISSUE-094 already resolved in RPT-445; no new issue from this step.

## 6. Problems found
- **Spec prose was materially stale (the IMP-1192 trigger).** 12 distinct drifts between spec and as-built (vendor/region, audit mechanism, endpoint schema, token header, retention granularity, cost column, mail-client tab count, conversational-assistant origin, action naming, file paths, join level). All now captured in the slice reconcile table and reflected in COMPLIANCE/RUNBOOK. No code change implied — this was documentation catching up to reality.
- **DEC-196 was still `accepted` despite its premise being obsolete.** Corrected to `superseded`. The associated CI "region-lock-check eu-west-1 whitelist" mentioned in DEC-196's consequence is now obsolete; flagged in the Superseded-Note (no such CI check found active in this pass — not verified exhaustively, noted as open point).
- **No code/test execution performed** — this is a docs-only step. tsc/ESLint/vitest unchanged from RPT-445's green baseline; not re-run (no code touched).

## 7. Open points / deferred decisions
- **Gesamt-V9.1-/qa** across SLC-V9.1-A…D (cross-slice wiring) — the immediate next step before any merge.
- **Master-merge** `v9-1-forward-bucket-email` → `main` (`--no-ff`) + **records-flip** (SLC/FEAT/BL → done, roadmap eventually `released`) — gated behind Gesamt-/qa PASS, then the /final-check → /go-live → /deploy → /post-launch chain (IMP-950).
- **DEC-196 region-lock CI check**: confirm whether an `INBOUND_REGION`/eu-west-1 whitelist test exists in CI and remove if so (not located in this pass).
- **Founder D4** (conversational-first wording freigabe, ~1h) and **OQ-R1-1** (IONOS mailbox + IMAP ENVs + 2 scheduled-tasks) remain founder actions; OQ-R1-1 gates all V9.1 Live-Smokes incl. SLC-V9.1-C AC-9.

## 8. Project record updates
- `/docs/DECISIONS.md`, `/docs/COMPLIANCE.md`, `/docs/RUNBOOK.md`, `/slices/SLC-V9.1-D-*.md`, `/docs/STATE.md` updated as above. Cockpit-critical status records (INDEX/backlog/roadmap) intentionally left `in_progress`/`active` — correct for the current pre-Gesamt-/qa state.

## 9. QA note
MT-7 is a documentation/reconcile step, not `/frontend`/`/backend`/`/slice-planning` implementation — no per-step `/qa` is triggered. The substantive QA gate now due is the **Gesamt-V9.1-/qa** over the whole version.

## 10. Recommended next step
**Gesamt-V9.1-/qa** across SLC-V9.1-A…D (cross-slice wiring + RLS matrix completeness), then `/final-check` → `/go-live` → `/deploy` (incl. IONOS Live-Smokes + SLC-V9.1-C AC-9) → `/post-launch`. Master-merge only after Gesamt-/qa PASS. Per `module-lifecycle-discipline`: Internal-Test-Mode, no customer-outreach.
