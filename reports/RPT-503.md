---
id: RPT-503
date: "2026-06-20"
skill: /deploy
slice: V9.8 (SLC-V9.8-A + SLC-V9.8-B)
feature: FEAT-088 + FEAT-089
type: completion
status: completed
reviewOf: null
result: "Deploy V9.8 IN PROGRESS — Master-Merge (643c1b2 --no-ff) + Push origin/main done, Mig 123 LIVE-applied + verifiziert auf Coolify-DB (themes text[] NOT NULL DEFAULT '{}' + GIN idx_knowledge_unit_themes + Containment-Query laeuft) VOR Code-Redeploy (R-A-1 erfuellt). VERBLEIBEND (Founder-gated): Coolify-Redeploy app+worker (Auto-Webhook inaktiv) + Live-Smoke (R-B-3). Production aktuell stabil — alter Code beruehrt themes nicht, Migration additiv, 0 Breakage im Push→Redeploy-Gap."
title: "Deploy V9.8 — Tag-Vokabular (Migration live, Redeploy pending)"
---

# Deploy V9.8 — Tag-Vokabular (Phase 2, in progress)

## 1. Was deployed wurde (bis hierher)
Version V9.8 (SLC-V9.8-A FEAT-089 + SLC-V9.8-B FEAT-088). Ziel: Production OP-Stack
(Coolify + Hetzner, 159.69.207.29). Nach /go-live CONDITIONAL GO (RPT-501).

## 2. Phase 1 — Prerequisites
Established Update-Deploy (V9.75 ging vor 2 Tagen auf denselben Stack). Alle Infra-
Prerequisites erfuellt (Server, Coolify, DNS, ENVs). 0 neue Deps/ENVs/Cron-Jobs/Container.

## 3. Phase 2 — Execution

### Pre-Merge-Re-Check (GREEN)
- Rebase: clean (no-op — origin/main `2e80b03` ⊆ HEAD, 0 Parallel-Push).
- Tests post-rebase: PASS (101/101 hermetisch, Gesamt-/qa RPT-499; Rebase no-op = State unveraendert).
- MIG-Kollision: keine (123 unique, 120/121/122/123 clean).
- Pattern-Drift: keine (keine Parallel-Slices).
- Cross-Repo-Pattern: N/A (OP-spezifisches tag-vocabulary).
- Manual Diff-Review: 23 Files, alle erwarteter V9.8-Scope.

### Master-Merge (DONE)
- `git merge --no-ff v9-8-tag-vokabular` → main, Merge-Commit **`643c1b2`**.
- `git push origin main` → **`2e80b03..643c1b2`** auf origin/main.

### Migration 123 LIVE (DONE — R-A-1 erfuellt, VOR Code-Redeploy)
- `sql-migration-hetzner.md`-Pattern: base64 → SSH → `psql -U postgres`.
- DB-Container `supabase-db-bwkg80w04wgccos48gcws8cs-083208599632`.
- PRE: themes-Spalte count=0 (existierte nicht). APPLY: BEGIN/ALTER TABLE/CREATE INDEX/NOTIFY/COMMIT.
- **Verifiziert:** `themes` ARRAY(text[]) NOT NULL DEFAULT `'{}'`, GIN-Index
  `idx_knowledge_unit_themes` vorhanden, Containment-Query `themes @> ARRAY['x']` laeuft.
- `NOTIFY pgrst, 'reload schema'` ausgefuehrt (PostgREST-Schema-Cache neu geladen).

### VERBLEIBEND (Founder-gated)
- **Coolify-Redeploy app+worker:** Auto-Webhook inaktiv → kein Rebuild auf Push gestartet
  (app+worker noch `Up 2 days` auf altem Code `9e28f3d`). Founder triggert Redeploy in der
  Coolify-UI (oder via Deploy-Webhook/API) auf main HEAD `643c1b2`.
- **Live-Smoke (R-B-3):** nach Redeploy — ein Bulk-Synthese-Run beobachten (Vokabular-Block
  im Prompt + Tag-Reuse), app+worker healthy/RestartCount=0/0 fatal.

## 4. Stabilitaet im aktuellen Gap (push gemacht, redeploy pending)
**0 Breakage.** Der laufende alte Code (`9e28f3d`) beruehrt die neue `themes`-Spalte nicht
(weder Promote-INSERT noch Synthese-READ existieren dort). Die Migration ist additiv +
idempotent. Production bleibt voll funktional bis zum Redeploy.

## 5. Project record updates
- `/docs/MIGRATIONS.md` — MIG-123 Status auf LIVE-applied 2026-06-20 (im Merge enthalten).
- `/docs/STATE.md` — High-Level State `deploying`; Current Phase: Mig 123 live + Code gepusht,
  Redeploy + Live-Smoke pending.
- `/reports/RPT-503.md` — dieser Report.
- `/docs/RELEASES.md` + `/planning/roadmap.json` — REL-Eintrag + `released`-Status erst nach
  Redeploy + Live-Smoke-PASS (bewusst zurueckgehalten — Live-Smoke ist noch offen).
- `/planning/backlog.json` — BL-505 bleibt `in_progress` bis Live-Smoke-PASS.

## 6. Recommended next step
Founder triggert **Coolify-Redeploy app+worker** auf main HEAD `643c1b2`. Danach: **Live-Smoke
(R-B-3)** → bei PASS REL-035 + roadmap `released` + BL-505 done + `/post-launch V9.8` T+24h.
