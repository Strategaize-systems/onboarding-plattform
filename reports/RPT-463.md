---
id: RPT-463
date: "2026-06-12"
skill: /qa
slice: SLC-V9.5-D
feature: FEAT-080
type: review
status: reviewed
reviewOf: RPT-462
result: "Gesamt-/qa V9.5 (SLC-V9.5-A..D) PASS — 0 Blocker / 0 High / 0 Medium / 3 Low. AC-D-6 End-to-End 2/2 + RLS 8/8 GREEN auf Coolify-DB."
title: "Gesamt-/qa V9.5 (SLC-V9.5-A..D) — PASS (MT-5)"
---

# Gesamt-/qa V9.5 — Bulk-Import Deep-Extraction (SLC-V9.5-A..D)

## Scope

Branch `v9-5-bulk-deep-extraction` HEAD aea95f1 (14 Commits ahead origin/main
b3e7425) im Worktree `c:/strategaize/strategaize-onboarding-plattform-v95`.
Cross-Slice-Wiring A..D, End-to-End-Fixture-Run gegen die Coolify-DB
(AC-D-6), behavioraler RLS-Re-Run, P1/P2-Pattern-Scan (R-D-2), Quality-Gates.
Slice-/qa pro Slice lag bereits vor (RPT-456/458/460/462, alle PASS).

## Verdict: PASS — 0 Blocker / 0 High / 0 Medium / 3 Low

## 1. Cross-Slice-Wiring A..D (statisch, drift-frei)

| Link | Evidenz |
|---|---|
| A: 4 Adapter-Defaults = eu-Profiles | `eu.anthropic.claude-sonnet-4-20250514-v1:0` in email-pattern/email-synthesis/email-critic/ai-assisted-setup/augment; `eu.anthropic.claude-haiku-4-5-20251001-v1:0` in bedrock-haiku (DEC-218) |
| Extraktor→Synthese (AC-B-5) | Enqueue genau 1 Statement-Block nach `pattern_extracted`-Flip, vor `rpc_complete_ai_job` (`handle-pattern-extraction-job.ts:454`) |
| Dispatch | `email_bulk_synthesis` in claim-loop JOB_TYPES + Routing auf `emailBulkSynthesisHandler` (claim-loop.ts:157) + run.ts uebergibt `handleEmailBulkSynthesisJob` als 20. Handler |
| B: Worker-Persist | Status `pattern_extracted→synthesizing→synthesized`, Unit-INSERT + batched `_source`-INSERT exakt MIG-111-Spalten (handle-synthesis-job.ts:602/635) |
| C: Critic-Gate | `critiqueUnits` 1-Call/Run am Filter-Hook, `selectSurvivingUnits` strict KEEP && evidence>=2 (handle-synthesis-job.ts:141/567) |
| D: Curation | `getCurationData`/Actions auf `email_synthesized_unit`, finishCuration-Guard `synthesized|curating` → `importing` (actions.ts:511/542), Import → `completed` |
| D: Import-Target (AC-D-1) | `mapSynthesizedUnitToKnowledgeUnit` → knowledge_unit-INSERT + `rpc_trigger_handbook_snapshot` strukturell unveraendert |

R-D-1-Scan: 0 stale `email_pattern`-Referenzen im Curation/Import-Pfad (nur
Doku-Kommentare). AC-D-2-Scan: 0 `participant_pseudonyms`/`pseudonymMap`-Refs.

## 2. End-to-End-Fixture-Run auf Coolify-DB (AC-D-6) — 10/10 GREEN

Neuer DB-gated Test `src/lib/db/__tests__/v95-bulk-e2e.test.ts` (Commit
aea95f1) + RLS-Re-Run `email-synthesized-unit-rls.test.ts`, ausgefuehrt im
node:20-Sidecar (`/opt/onboarding-test`, Netz
`bwkg80w04wgccos48gcws8cs_strategaize-net`, DB
`supabase-db-bwkg80w04wgccos48gcws8cs-083144313168`):

```
✓ v95-bulk-e2e.test.ts (2 tests) 136ms
✓ email-synthesized-unit-rls.test.ts (8 tests) 385ms
Tests: 10 passed (10)
```

Bewiesen auf dem LIVE-Schema (BEGIN/ROLLBACK, keine Daten-Spuren):
- **Status-Kette** `pattern_extracted → synthesizing → synthesized →
  importing → completed` passiert den MIG-111-18-Werte-CHECK; Bogus-Status
  wird rejected (CHECK intakt).
- **MIG-111/112-CHECKs live:** `ai_jobs.job_type='email_bulk_synthesis'` +
  `ai_cost_ledger.role` `email_bulk_synthesis`/`email_bulk_critic` akzeptiert.
- **DEC-217:** `synthesis_cost_eur`-UPDATE hebt GENERATED `total_cost_eur`
  delta-exakt (+0.42) an.
- **Reduktions-Quote + Evidenz-Aggregation:** 6 Roh-Patterns → 2 Units;
  sum(evidence_count)=6=`_source`-Rows; `cardinality(source_pattern_ids)` =
  `evidence_count` pro Unit.
- **Curation behavioral durch RLS:** Accept/Reject-UPDATEs als
  `tenant_admin` (withJwtContext, nicht Superuser) — rowCount 1.
- **Import via echtem Mapper:** `mapSynthesizedUnitToKnowledgeUnit`-Output →
  knowledge_unit-INSERT (inkl. DEC-193 metadata-Fallback-Pfad), Attribution
  `**Belege**: 3 Quell-Patterns` + Run-Detail-Link im body, confidence-Tier
  high (0.9), Import-Marker auf der Unit gesetzt.
- **Snapshot-RPC behavioral:** `rpc_trigger_handbook_snapshot` als
  tenant_admin → `handbook_snapshot_id` zurueck, kein error.
- **P1/P2-Scan (R-D-2):** Roh-Patterns enthalten P1/P2/P3 — promoteter
  knowledge_unit title+body 0 Treffer auf `\bP\d+\b`.
- **RLS durchgaengig:** 8/8 Matrix (strategaize_admin cross-tenant,
  tenant_admin own-only SELECT/INSERT/UPDATE, tenant_member default-deny,
  Cross-Tenant-INSERT/UPDATE blockiert) auf beiden MIG-111-Tabellen.

## 3. Quality-Gates

- tsc --noEmit = 0.
- ESLint scoped auf alle 35 V9.5-Diff-Files = 0. Repo-weit 1 pre-existing
  Error (`text-overrides/page.tsx`) + 1 Warning (`mandanten-report cover.tsx`)
  — beide NICHT im V9.5-Diff, bekannt seit RPT-447 (V9.1).
- Vollsuite lokal: 1848 pass / 516 DB-gated / 36 skipped = exakt
  RPT-462-Baseline, 0 Regress. Die 2 neuen E2E-Tests sind zusaetzlich
  DB-gated (lokal ohne TEST_DATABASE_URL erwartbar fail, Sidecar GREEN).
- Stub-Scan: clean pro Slice-/qa (RPT-456/458/460/462), keine neuen Files
  ausser dem E2E-Test.

## Findings

### Low
- **L-1: Bedrock-Live-Smokes A/B/C stehen aus (Founder-gated → /deploy).**
  Echte LLM-Synthese/Critic + Live-AWS-Verify der eu-Haiku-4.5-Profile-ID
  (ISSUE-100) + P1/P2-Scan auf Real-Korpus. Geplant, kein Defekt.
- **L-2: Browser-Render der Curation-UI nicht direkt verifiziert**
  (OP-Konvention, Browser-Smoke im /deploy-Fenster).
- **L-3: Pre-existing `next build` Page-Data-Fail `/api/public/partner/[slug]`**
  ohne Runtime-ENVs (ISSUE-101, git-stash-bewiesen, Compile PASS) — im
  /final-check als bekannter Build-Umstand fuehren.

## Records-Updates

- `/slices/INDEX.md` SLC-V9.5-D → done (alle 4 V9.5-Slices done)
- `/planning/backlog.json` BL-159 → done (BL-160/161 bereits done)
- `/features/INDEX.md` FEAT-080 → done (FEAT-081/082 bereits done)
- `/docs/STATE.md` Current Focus → Gesamt-/qa PASS + Merge-Pfad

## Next workflow step

**Pre-Merge-Re-Check (6 Schritte, git-release.md)** → **EIN Master-Merge
`v9-5-bulk-deep-extraction` → `main` --no-ff** (gleiche Session) →
**/final-check V9.5** (L-3/ISSUE-101 mitgeben) → /go-live → /deploy
(Bedrock-Live-Smokes A/B/C + Browser-Smoke) → /post-launch.
Koordination: V9.1 /post-launch T+24h (~2026-06-13 08:31 UTC) hat Vorrang
sobald faellig.
