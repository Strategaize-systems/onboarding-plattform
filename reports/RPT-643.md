---
id: RPT-643
date: "2026-07-10"
skill: /qa
slice: null
feature: FEAT-110
type: completion
status: completed
reviewOf: null
result: "Gesamt-/qa V20 (SLC-193/194/195 Cross-Slice) PASS code-side — Integration-Gates grün: next build EXIT 0 auf kumulativem HEAD, eslint 0 (voller Changed-Set), tsc 0, Full-Suite 619-fail-DB-env-gated/2346-pass = 0 Regression, 0 Stubs (32 Source-Files), 0 Shared-File-Drift, MIG-133/134 sequenziell kollisionsfrei. 0 Blocker/High/Medium. Alle Live-Smokes (evidence-403, App-Session-Creation, MIG-Live-Apply, Handbook-XSS-Render, SVG-Reject, CSP-Funktional+enforce-Flip, Login-Lockout) gebündelt an /deploy — konsistent kumulativer un-deployter Branch. NEXT /final-check."
title: "Gesamt-/qa V20 — Cross-Slice PASS (code-side), Live-Smokes gebündelt an /deploy"
---

# Gesamt-/qa V20 — SLC-193/194/195 (Security-Hardening konsolidiert)

## QA-Scope
Version-weite Cross-Slice-Integration über die 3 V20-Slices im kumulativen Worktree `v20-security-hardening` (HEAD `828dd1f`, off main `942cebe`, Clean-Tree, un-deployt):
- **SLC-193** FEAT-110 DB/Authz-Hardening (MIG-133/134, tier-Guard, evidence-Ownership, berater-RPC, profiles.role-Guard) — per-Slice /qa RPT-638 PASS(code-done)
- **SLC-194** FEAT-111 XSS/Output/Headers (rehype-sanitize, SVG-Block+Magic-Byte, CSP+COOP Report-Only, branding-Farb-Revalidierung) — per-Slice /qa RPT-640 MIXED(code-done)
- **SLC-195** FEAT-112 Auth/Secrets (Login-Lockout, verifyCronSecret-Sweep, Logger-Redaction, recording-ready-Guard, slug-.eq) — per-Slice /qa RPT-642 PASS

Gesamtsurface: 65 Files, +2446/−69. Delivery Mode SaaS. Per Verdikt-Taxonomie werden bereits erreichte per-Slice-PASS-LIVE NICHT re-verifiziert (kein Deploy dazwischen); Fokus dieser Gesamt-QA = Cross-Slice-Integration + Konsolidierung der PENDING-LIVE-Punkte.

## Verdict: PASS (code-side) — 0 Blocker / 0 High / 0 Medium
Die drei Slices integrieren konfliktfrei und bilden zusammen einen kohärenten Security-Hardening-Stand. Alle code-seitigen Integration-Gates grün. Sämtliche Live-Smokes sind — wie beim kumulativen un-deployten Branch vorgesehen (EIN Deploy nach allen V20-Slices) — an `/deploy` gebündelt; das ist Deploy-Validation, kein offener Defekt.

## Integration-Gates (auf kumulativem HEAD `828dd1f`)
| Gate | Ergebnis |
|---|---|
| `next build` (alle 3 Slices zusammen, inkl. CSP-Config in next.config.ts) | **EXIT 0** |
| `tsc --noEmit` | **EXIT 0** |
| `eslint` voller Changed-Set (Source + Tests, 63 Files) | **EXIT 0** |
| Full-Suite `vitest run` | **619 failed / 2346 passed / 65 skipped** |
| Regression | **0 neu** — 619 = `TEST_DATABASE_URL`-env-gated Baseline (konstant über alle 3 Slices); Pass-Count monoton 2293→2319→2346 (+26 SLC-194, +27 SLC-195) |
| Stub-Scan (32 geänderte Source-Files) | **0** TODO/FIXME/debugger/placeholder/`as any` |
| Shared-File-Drift (Datei in >1 Slice modifiziert) | **0** — Slices sequenziell, Datei-Scopes disjunkt (193: stb/bulk-email/diagnose-actions + evidence + MIG · 194: handbook/branding/csp/next.config · 195: rate-limit/login/cron/logger/recording-ready/partner-signup) |
| Migration-Numbering | **kollisionsfrei** — main@132 latest; Branch fügt 133_v20_authz_hardening + 134_v20_search_path_sweep sequenziell an |

## Cross-Slice-Integrations-Analyse
- **Migrationen (193):** MIG-133 → MIG-134 strikt sequenziell, keine Nummern-Kollision mit main (132). In-tx-Full-Apply-Probe 9/9 CHK PASS bereits in RPT-638 gegen die Live-Coolify-DB bewiesen (BEGIN..ROLLBACK, DB unverändert). Live-Apply erst /deploy.
- **Globale CSP/Header (194) × Routen aller Slices:** `Content-Security-Policy-Report-Only` + COOP gelten app-weit — auch für die 195-Login-/Public-Routes und 193-evidence-Routen. Report-Only ⇒ **non-breaking**; `next build` validiert die Config; keine Slice-Route wird durch die Header blockiert. Enforce-Flip bewusst nach dem Deploy-Browser-Smoke.
- **Logger-Redaction (195) × Fehlerpfade (alle Slices):** `redactSecrets` sitzt zentral in `logToDb` — greift für jeden `captureException/Warning/Info`-Call der ganzen Version; Full-Suite bestätigt: kein Test asseriert auf einen redacteten Key → 0 Regression.
- **Kein funktionaler Überlapp:** DB-Authz (193), Output/Headers (194), Auth/Secrets (195) sind orthogonale Schichten; kein Slice hängt zur Laufzeit von einer noch-nicht-applizierten Migration eines anderen ab (die 133/134-abhängigen App-Pfade sind erst nach Live-Apply relevant und dort mit Auto-Grant-Tier abgedeckt — s. Live-Checklist).

## Konsolidierte Live-Smoke-Checklist für /deploy (BLOCKING vor Done/Deployed)
*(security-headers-live-smoke.md / coolify-test-setup.md — KEIN curl-only-PASS; gegen den deployten Container nach MIG-Apply)*
1. **MIG-133/134 Live-Apply + DB-Verify** (node:20-Sidecar): committed Vitest `tier-guard-insert` / `berater-rpc-coalesce` / `search-path-sweep` gegen die persistente DB (mappen 1:1 auf die 9 In-tx-Probe-CHK). MIG-134 idempotent (2×-Apply).
2. **evidence/list + download 403 cross-tenant** (ISSUE-124) — JWT-Smoke gegen deployten Stand.
3. **App-Session-Creation End-to-End** mit gesenktem DEFAULT `tier='free'` + Auto-Grant: StB-Blueprint/Modul, bulk-email, diagnose, walkthrough (Feature funktioniert mit entitled Tier). Founder-Test/Browser.
4. **profiles.role-Guard** live (Escalation-Reject, Defense-in-Depth zweite Schicht — L-1 RPT-638).
5. **Handbook `<iframe srcdoc>`-XSS** rendert im Browser nicht aus (Sanitize-Kette ist browser-unabhängig, Confirm = Formsache).
6. **SVG-Upload-Reject** live gegen die laufende storage-api + Logo-Route `nosniff`.
7. **CSP-Funktional-Browser-Smoke** via `tests/_probe/csp-check.mjs` — 0 Eigen-Asset-Violations über Login/Dashboard/Handbook/Diagnose/Jitsi-iframe → **enforce-Flip-Entscheid** (Header-Key → `Content-Security-Policy`, ISSUE-127-Closure) mit realen Violation-Daten. COEP deferred.
8. **Login-Lockout behavioral** (optional, Founder-Selbsttest) — account-scoped Lockout über rotierende IPs.

## Findings
Keine Blocker / High / Medium.

**Low / informational (Carry-Forward, keine offenen Defekte):**
- L-1: alle Live-Smokes oben an /deploy gebündelt — struktureller Effekt des kumulativen un-deployten Branches, kein Slice-Defekt.
- L-2: ISSUE-124/125/127/129 bleiben `open` (code-addressed; Live-Resolve nach /deploy-Apply + Live-Verify). ISSUE-121/122/123/126/128/130/131/132 sind bereits resolved (per-Slice).
- L-3: 24 der 619 Full-Suite-Fails sind pre-existing Mock-Infra-Flakes (Baseline-identisch @942cebe), der Rest `TEST_DATABASE_URL`-env-gated — beide out-of-scope V20, 0 Regression (RPT-638 L-2).
- L-4 (Beobachtung, kein Finding): STATE.md ist stark angewachsen (>320 KB, Current-Focus-Zeile ~15 KB) — Cockpit-parsbar, aber Hygiene-Kandidat für /docs-Kondensierung nach dem V20-Deploy.

## Readiness
V20 ist **code-side release-ready** über alle 3 Slices. Cross-Slice-Integration konfliktfrei, alle statischen/Build/Test-Gates grün, 0 Regression. Verbleibende Verifikationen sind ausnahmslos Deploy-Live-Gates (kein offener Code-Defekt). Slices bleiben `code-done` (in_progress) bis /deploy die Live-Checklist schließt.

## Project record updates
STATE Focus/Next-Steps → Gesamt-/qa V20 PASS, NEXT /final-check. Slices/Features/Backlog unverändert `in_progress` (code-done, keine `done`/`deployed`-Flips vor /deploy — Release-Gate-Defense). RPT-643 erstellt.

## Next workflow step
**`/final-check` V20** — Pre-Release-Audit über den ganzen V20-Scope (Code-Hygiene, Security, Dependencies [+`rehype-sanitize@6`], Secrets, Test-State, mechanische BLOCKING-Gates). Danach `/go-live` → `/code-review ultra` (DEC-219-Closure) → `/deploy` (MIG-133/134 Live-Apply node:20-Sidecar + die konsolidierte Live-Smoke-Checklist) → `/post-launch`.

*(Self-check: Gesamt-/qa V20 ist erledigt; /final-check, /go-live, /post-launch für V20 stehen aus → korrekter nächster Schritt ist /final-check, kein anderer Workstream. Kein `released`/`deployed`/`stable`-Status geschrieben.)*
