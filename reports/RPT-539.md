---
id: RPT-539
date: "2026-06-29"
skill: /go-live
slice: V10-Gesamt
feature: FEAT-092
type: review
status: reviewed
reviewOf: RPT-538
result: "CONDITIONAL GO — V10 StB-Vertikale internal Workflow-Closure release-gate. Code-side release-ready (Gesamt-QA RPT-537 PASS + final-check RPT-538 CONDITIONALLY READY: tsc 0, eslint 0, vitest 122/122, next build EXIT 0, 0 new deps, hardened migrations/RPC, env-gate fail-closed OFF, 0 Blocker/High/Medium from V10). GO is conditional on /deploy-stage items that are Founder-gated and cannot execute under the push/Coolify-hold: (1) Founder lifts hold + merge v10-slc170b-blueprint-seed→main, (2) MIG-127 live-apply (126 already DB-live), (3) BEDROCK_V9_SONNET_MODEL_ID=eu.anthropic.claude-sonnet-4-20250514-v1:0 (ISSUE-099), (4) keep NEXT_PUBLIC_ENABLE_STB_VERTIKALE OFF, (5) live worker/diagnosis + SLC-172/175 browser + RLS cross-tenant sidecar smoke, (6) all containers healthy post-redeploy. No customer-outreach/legal/DSGVO (module-lifecycle-discipline). NEXT /deploy V10 (Founder-gated)."
title: "Go-Live V10 (StB-Vertikale) — CONDITIONAL GO (internal Workflow-Closure)"
---

# Go-Live — OP V10 StB-Vertikale (Release-Gate)

**Decision: CONDITIONAL GO.** V10 is code-side release-ready. The GO is conditional on the `/deploy`-stage items below, all of which are Founder-gated and cannot run while the push/Coolify-hold is in effect. This is an **internal Workflow-Closure** gate per `module-lifecycle-discipline` — NOT a customer go-live (no outreach, no legal/DSGVO pre-live, single-Founder Internal-Test-Mode, feature behind OFF env-gate).

## 1. Release target considered
Merge `v10-slc170b-blueprint-seed` → `main` and redeploy the OP app (Coolify/Hetzner `159.69.207.29`) with the V10 StB-Vertikale code present but **gated OFF** (`NEXT_PUBLIC_ENABLE_STB_VERTIKALE`). Backing migrations: MIG-126 (already DB-live), MIG-127 (live-apply pending).

## 2. Readiness decision
**CONDITIONAL GO.** Code-side readiness is established and independently verified:
- Gesamt-QA Cross-Slice **PASS** (RPT-537): all 5 integration contracts CONFIRMED, tsc 0, 122/122.
- Final-check **PASS / CONDITIONALLY READY** (RPT-538): tsc 0, eslint 0, vitest 122/122, next build EXIT 0, 0 new deps, secrets/console/XSS clean, migrations 126/127 idempotent, RPC `SECURITY DEFINER + search_path=public + service_role-only + tenant-scoped`, env-gate fail-closed. 0 Blocker/High/Medium introduced by V10.

The decision is **conditional** (not unconditional GO) because the live-validation that proves the release in production has not run — it is deploy-stage and Founder-gated.

## 3. Blockers
None code-side. The only thing preventing execution is the **Founder/Coolify push-hold** (a deliberate gate, not a defect).

## 4. Accepted risks
- **Deferred live-validation** (worker output, diagnosis cards, browser-smoke, RLS cross-tenant sidecar) — accepted at the gate, runs at `/deploy`. Consistent with prior versions (e.g. V9 SLC-165 AC-8) and the Founder-hold.
- **L-1..L-4** from RPT-538 (env-coupling ISSUE-099, `.env.example` doc gap, two by-design) — accepted/documented.
- **Pre-existing out-of-scope open Highs** (ISSUE-084/085 V8.1 mock texts, 093 V9 Haiku) — not introduced/worsened by V10; V10's surface is OFF-gated. They remain tracked and must close before their own features ship; they do **not** block V10's internal closure.

## 5. Required fixes / conditions before release (all /deploy-stage, Founder-gated)
1. **Founder lifts the push/Coolify-hold**, then merge `v10-slc170b-blueprint-seed` → `main` (Pre-Merge-Re-Check per `git-release.md`: rebase on main, re-run tests, MIG-number collision check, manual diff review).
2. **MIG-127 live-apply** via `sql-migration-hetzner.md` (MIG-126 already DB-live). Idempotent.
3. **`BEDROCK_V9_SONNET_MODEL_ID = eu.anthropic.claude-sonnet-4-20250514-v1:0`** confirmed in Coolify (ISSUE-099 coupling; else V10 synthesis fails live).
4. **`NEXT_PUBLIC_ENABLE_STB_VERTIKALE` stays OFF** (Internal-Test-Mode).
5. **Live smoke:** `module_output_synthesis` + `diagnosis_generation` worker output (output_kind/evidence survive reconcile), SLC-172/175 browser-smoke, RLS cross-tenant sidecar (node:20 SAVEPOINT, `coolify-test-setup.md`).
6. **All containers `(healthy)`** post-redeploy; reverse-proxy routing + auth flow intact.

## 6. Operational concerns
Current production (V9.8 STABLE) is the rollback target. V10 redeploy carries low blast-radius: the new surface is OFF-gated, so a V10 regression cannot reach users through the UI; the reused diagnosis/synthesis workers are the only always-on V10-touching paths and are guarded by the tier-gate (`blueprint`). Rollback = redeploy prior image + (MIG-126/127 are additive/idempotent, no destructive down-migration needed).

## 7. Recommended next step
**`/deploy V10`** — Founder-gated. Hold here until the Founder lifts the push/Coolify-hold; then execute conditions 1–6 with live verification. No REL-XXX recorded yet — the release record is created at `/deploy` once live. No cross-repo/topic switch before `/deploy` + `/post-launch` close V10.
