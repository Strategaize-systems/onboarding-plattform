---
id: RPT-491
date: "2026-06-18"
skill: /deploy
slice: null
feature: FEAT-085, FEAT-086, FEAT-087
type: completion
status: completed
reviewOf: null
result: "DEPLOYED — Mig 121+122 live-applied + verifiziert, main gepusht (9e28f3d), Coolify-Redeploy app+worker (healthy/RestartCount=0), Live-Smoke PASS (Tier-Gate + Change-Guard + Roster). REL-034. Internal-Test-Mode, kein Customer-Go-Live."
title: "Deploy V9.75 — Production (DEPLOYED, Live-Smoke PASS)"
---

# Deploy V9.75 — Production

Deployment der V9.75 Exit-Readiness-Produktisierung auf OP-Production (Hetzner 159.69.207.29, Coolify-Resource `bwkg80w04wgccos48gcws8cs`). **Internes Workflow-Closure-Release — KEIN Customer-Go-Live** (module-lifecycle-discipline).

## Phase 1 — Prerequisites
Update-Deploy auf laufenden Stack. Server/Coolify/DNS/SSL/ENV alle aus V9.7-Deploy (REL-033) in place. 0 neue ENVs, 0 neue Deps, 0 neue Cron-Jobs. Keine First-Deploy-Setup-Schritte noetig.

## Phase 2 — Execution

### 1. Live-Apply Migration 121 + 122 (vor Code-Redeploy)
SSH-stdin-Stream an `docker exec -i <db> psql -U postgres -v ON_ERROR_STOP=1` (sql-migration-hetzner.md). DB-Container zur Laufzeit aufgeloest (`supabase-db-bwkg80w04wgccos48gcws8cs-150241698111` pre-redeploy).
- **121** (Tier-Gating-Foundation): COMMIT, EXIT=0. Verifiziert live: `capture_session.tier` DEFAULT 'handbook' (6 Bestands-Sessions backfillt = AC-A-1), `ai_jobs.session_tier`, 4 Matrix-Fns (`fn_tier_rank`/`fn_min_tier_for_job`/`fn_tier_allows`/`fn_session_tier_allows`), 2 Guard-Trigger (change-guard + anti-forge-insert).
- **122** (Mitarbeiter-Register): COMMIT, EXIT=0. Verifiziert live: `employee_roster_draft` Tabelle, RLS enabled, 2 Policies, Dedup-Index.

### 2. Push main
`git push origin main` → `5f91c83..9e28f3d`. origin/main = HEAD = `9e28f3d`.

### 3. Coolify-Redeploy app + worker (Founder-getriggert)
Stack neu erstellt (Container-Timestamp-Suffix → `-0832085*`). Worker-Rebuild Pflicht (Schicht-2-Tier-Defense in claim-loop.ts).
- app `-083208563693`: Up `(healthy)`, RestartCount=0, live commit `SOURCE_COMMIT=9e28f3d`, `/api/health` 200.
- worker `-083208585062`: Up `(healthy)`, RestartCount=0, clean Start (12 Handler registriert, Environment validated, 0 Crash — ISSUE-103-Klasse tritt nicht erneut auf).
- alle 16 Stack-Container Up.

### 4. Live-Smoke (PASS)
- **Tier-Matrix** (live DB): `fn_tier_rank` free/bp/hb=0/1/2; `email_bulk_parse`+`handbook_snapshot_generation` min_tier=handbook (gated); `lead_push_retry` ungated; `fn_tier_allows(free|blueprint, email_bulk_parse)=false`, `(handbook, …)=true`, `(free, lead_push_retry)=true`.
- **Change-Guard**: non-service_role (`postgres`) `UPDATE capture_session.tier` → rejected ("change denied for role postgres (service_role required)"). (in TX, rolled back — keine Prod-Daten geaendert)
- **Register**: `employee_roster_draft` selektierbar, RLS enabled, 0 Rows.
- **Logs**: app + worker 0 error/fatal/panic seit Boot; error_log 0 error/fatal in 10 min.

## Phase 3 — Learning
Keine neuen Probleme. ISSUE-103-Lehre (Worker-devDep) bestaetigt nicht-rezidiv: V9.75 fuegte 0 neue npm-Imports im Worker-Code hinzu (nur type-only `SupabaseClient` + bestehender Logger). Playbook unveraendert akkurat.

## Records aktualisiert
- `docs/RELEASES.md` — REL-034 (V9.75).
- `docs/MIGRATIONS.md` — MIG-121 + MIG-122 auf LIVE 2026-06-18.
- `planning/roadmap.json` — v9-75 → released.
- `features/INDEX.md` — FEAT-085/086/087 → deployed.
- `docs/STATE.md` — High-Level State → post-launch, Focus/Next-Steps.

## Recommended next step
`/post-launch V9.75` T+24h (~2026-06-19) — Burn-In-Bestaetigung (Container-Health, error_log, Cron-Loop, ai_jobs) → bei PASS Last-Stable=V9.75.
