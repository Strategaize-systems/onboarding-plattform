---
id: RPT-184
date: "2026-05-07"
skill: /backend
slice: SLC-077
feature: FEAT-037
type: completion
status: completed
result: "SLC-077 (V5 Option 2 Stufe 2 Schritt-Extraktion) /backend code-side komplett — 5 MTs done, Migration 085 LIVE auf Hetzner appliziert, Worker-Handler walkthrough_extract_steps + Bedrock-Prompt + Zod-Schema + 16/16 Vitest gruen + Live-Quality-Test (Variante B) gegated etabliert. Worker-Bundle 220.6kb. Live-Smoke wartet auf User-Coolify-Worker-Redeploy: 3 walkthrough_session in extracting + 3 ai_jobs walkthrough_extract_steps pending werden automatisch durchgepickt."
title: "SLC-077 — V5 Option 2 Stufe 2 Schritt-Extraktion /backend done"
---

# SLC-077 — V5 Option 2 Stufe 2 Schritt-Extraktion /backend done

## Outcome Summary

**Code-side PASS** — alle 5 MTs done, alle Slice-Spec-Pflicht-Gates erfuellt (ausser Live-Smoke der wartet auf User-Coolify-Worker-Redeploy). Slice-Status SLC-077 → `done` (Cockpit-Records bereits aktualisiert; finale Verifikation via /qa SLC-077).

| Phase | Ergebnis | Beleg |
|---|---|---|
| MT-1 Migration 085 Apply | **LIVE** | `walkthrough_step` Tabelle live: 17 Columns + UNIQUE + 4 Indizes + 2 RLS-Policies + 1 Trigger + 3 FKs verifiziert |
| MT-2 Bedrock-Prompt + Zod-Schema | DONE | `step_extract.ts` (System-Prompt + Few-Shot) + `step_extract.schema.ts` (Zod-Array) |
| MT-3 Worker-Handler + Wiring | DONE | `handle-extract-steps-job.ts` + claim-loop + run.ts erweitert |
| MT-4 Vitest-Suite ≥5 Fixtures | **16/16 PASS** | 5 strukturierte Fixtures (Auftragsannahme/Reklamation/Onboarding/Monatsabschluss/Inventur) + N=0 + Codeblock-Fence + 7 Failed-Pfade + Cost-Ledger-Silent-Failure |
| MT-4b Live-Quality-Test (Variante B) | DONE (gegated) | `step_extract-quality.test.ts` mit 5 Bedrock-Calls gegated via `ENABLE_STEP_QUALITY_TEST=1`, sanity-PASS lokal |
| MT-5 N=0 Edge-Case | DONE | Eigene Vitest-Tests: leeres Array + Codeblock-Fence-Variante, Pipeline geht trotzdem extracting → mapping |
| Worker-Build | PASS | 220.6kb (vs SLC-076 205.9kb, +14.7kb) |
| TypeScript + Lint | clean | tsc --noEmit ohne Fehler, ESLint 0/0 auf alle neuen+geaenderten Files |

## Scope reviewed

- `slices/SLC-077-walkthrough-step-extraction.md` — Slice-Spec mit 5 MTs
- `reports/RPT-182.md` + `reports/RPT-183.md` — SLC-076 /backend + /qa Outcomes (Pattern-Vorlage + IMP-369..371-Lessons)
- `docs/ARCHITECTURE.md` V5 Option 2 Sektion (Stufe 2 Pseudocode + walkthrough_step Schema + 4-Rollen-RLS-Matrix)
- `docs/MIGRATIONS.md` MIG-032 — Migration 085 Format-Skizze
- `src/workers/walkthrough/handle-redact-pii-job.ts` — Pattern-Vorlage (Idempotency, Pipeline-Trigger, Cost-Ledger Silent-Failure-Pattern, Failed-Path)
- `src/lib/walkthrough/pipeline-trigger.ts` — `advanceWalkthroughPipeline` (extracting → mapping bereits in PIPELINE_TRANSITIONS definiert, kein neuer Code)
- `src/lib/ai/prompts/walkthrough/pii_redact.ts` — Vorlage fuer step_extract.ts (System-Prompt-Trennung per IMP-369)
- `src/workers/condensation/claim-loop.ts` + `run.ts` — Wiring-Pattern fuer neue Job-Types
- `src/lib/llm.ts` — Bedrock-Adapter `chatWithLLM` (ConverseCommand, eu-central-1)
- Live-DB Schema-Drift-Check (IMP-363-Pflicht): `walkthrough_session.status` CHECK enthielt bereits `extracting`, `auth.user_role()`+`auth.user_tenant_id()` als Helper-Funktionen aus profiles, `_set_updated_at`-Trigger-Funktion existiert.

## Files created

- `sql/migrations/085_v5opt2_walkthrough_step.sql` — Greenfield-Migration. CREATE TABLE walkthrough_step + 2 Indizes + ENABLE RLS + 2 Policies (SELECT 4-Rollen, UPDATE strategaize_admin/tenant_admin) + GRANTs + BEFORE UPDATE Trigger. Idempotent via IF NOT EXISTS / DROP POLICY IF EXISTS.
- `src/lib/ai/prompts/walkthrough/step_extract.ts` — `buildStepExtractSystemPrompt()` + `buildStepExtractUserMessage()`. System-Prompt: Aufgaben-Definition + 8 Regeln (Methodik-Sprache, keine PII-Tokens, transcript_snippet wortgetreu, optionale Felder nur wenn genannt, step_number sequenziell, N=0 erlaubt) + Few-Shot-Beispiel (Auftragsannahme-Prozess, 3 Schritte). User-Message liefert nur den redacted-Text (IMP-369 Disziplin).
- `src/lib/ai/prompts/walkthrough/step_extract.schema.ts` — Zod-Array-Schema. step_number int>=1, action string min 1, transcript_snippet string min 1; responsible/timeframe/success_criterion/dependencies optional+nullable. transcript_offset_start/_end NICHT vom LLM erwartet (Worker berechnet via indexOf).
- `src/workers/walkthrough/handle-extract-steps-job.ts` — Worker-Handler `walkthrough_extract_steps`. UUID-Payload-Validierung, Status-Skip bei != 'extracting', redacted-KU per `evidence_refs->>walkthrough_session_id`-Lookup + source='walkthrough_transcript_redacted', Bedrock-Call temperature=0/maxTokens=8000, Codeblock-Fence-Stripping, JSON.parse + Zod-Validation, **Idempotency via DELETE-before-INSERT** (Slice-Spec R3 UNIQUE-Conflict-Mitigation), Bulk-INSERT mit step_number=1..N + transcript_offset_start/_end via originalText.indexOf(snippet), ai_cost_ledger mit role='walkthrough_step_extractor' + IMP-371 Error-Check, Pipeline-Trigger advance, rpc_complete_ai_job, Failed-Path setzt status='failed' + re-throw.
- `src/workers/walkthrough/__tests__/fixtures/walkthrough-extracts.ts` — 6 Walkthrough-Fixtures (5 strukturiert + 1 unstrukturiert): id, description, body, expectedMinSteps, expectedActionPatterns (RegExp[]), mockBedrockOutput (deterministisches JSON-Array fuer Worker-Mock-Tests).
- `src/workers/walkthrough/__tests__/handle-extract-steps-job.test.ts` — 16 Vitest-Tests. 5 Happy-Path (je 1 pro strukturierte Fixture), 2 N=0 (plain + Codeblock-Fence), 1 Cost-Ledger-Silent-Failure, 1 Skip-Path, 7 Failed-Paths (UUID, no-KU, Bedrock-Error, empty-Bedrock, invalid-JSON, Zod-Validation, Step-INSERT-Fail). Mock-Builder mit chained-eq-Builder fuer .eq().eq().order().limit().maybeSingle()-Pattern und Bulk-INSERT mit count='exact' (Promise-Rueckgabe ohne .select).
- `src/lib/ai/prompts/walkthrough/__tests__/step_extract-quality.test.ts` — Live-Quality-Test (Variante B) gegen 5 strukturierte Fixtures. Misst: Schritt-Anzahl >= expectedMinSteps, 0 PII-Halluzinationen ([KUNDE]/[EMAIL]/etc. in action-Strings), Action-Pattern-Coverage (jedes erwartete Pattern matcht mindestens 1 action), step_numbers eindeutig+monoton. Gegated via `ENABLE_STEP_QUALITY_TEST=1`. Erwartete Kosten ~$0.05 pro Run.
- `reports/RPT-184.md` — diese Slice-Closing-Reportdatei.

## Files changed

- `src/workers/condensation/claim-loop.ts` — JOB_TYPES um `walkthrough_extract_steps` erweitert (11 → 12 types). startClaimLoop-Signatur um 13. Argument `walkthroughExtractStepsHandler?` erweitert. Dispatch-Branch hinzugefuegt.
- `src/workers/condensation/run.ts` — Import `handleExtractStepsJob`, Boot-Log-Zeile, `startClaimLoop`-Aufruf um 13. Argument erweitert.
- `slices/INDEX.md` — SLC-077 status `planned` → `done`.
- `docs/STATE.md` — Current Focus + Phase + Immediate Next Steps auf SLC-077-/backend-done aktualisiert. Naechster Schritt = User-Coolify-Worker-Redeploy + /qa SLC-077.
- `docs/MIGRATIONS.md` — MIG-032 Live-Status-Block fuer Migration 085 (LIVE 2026-05-07, Apply-Details, Verifikation).

## Files intentionally not changed

- `docs/DECISIONS.md` — DEC-080..091 sind bereits angelegt (in /architecture V5 Option 2). SLC-077 fuegt keine neuen DECs hinzu — alle Architektur-Entscheidungen waren vor /backend dokumentiert.
- `docs/KNOWN_ISSUES.md` — keine neuen Issues. Keine In-Phase-Bugs entdeckt (bewusste Pattern-Reuse aus SLC-076 hat Trailer-Pollution + Silent-Failure-Klasse a priori vermieden).
- `planning/backlog.json` — BL-081 (FEAT-037) bleibt `in_progress` weil SLC-078 + SLC-079 noch fehlen.
- `planning/roadmap.json` — V5-Status bleibt `active`, Progress wird ueber backlog-items berechnet.
- `src/lib/walkthrough/pipeline-trigger.ts` — keine Aenderung. PIPELINE_TRANSITIONS hat `extracting → mapping (walkthrough_map_subtopics)` bereits in SLC-076 gesetzt; SLC-077 nutzt diesen Pfad ohne Code-Aenderung.
- `src/workers/walkthrough/handle-redact-pii-job.ts` — keine Aenderung. SLC-077 reuses pipeline-trigger und folgt dem Pattern, ohne Stufe 1 anzufassen.

## Schema-Drift-Check (IMP-363) durchgefuehrt

Vor Migration-Apply + INSERT-Code:
- `walkthrough_session.status` CHECK = `('recording','uploading','uploaded','transcribing','pending_review','approved','rejected','failed','redacting','extracting','mapping')` — `extracting` bereits in MIG-087 verankert.
- `auth.user_role()` + `auth.user_tenant_id()` aus public.profiles (SECURITY DEFINER, etabliert seit V1).
- `_set_updated_at()` Trigger-Funktion existiert.
- 3 walkthrough_session in `extracting` (f51951ca-..., 33ea58be-..., 75098a5d-...) + 3 ai_jobs walkthrough_extract_steps pending — bereit fuer Live-Smoke.

## Migration 085 Apply-Details

- File: `sql/migrations/085_v5opt2_walkthrough_step.sql` (92 Zeilen, idempotent)
- Apply: base64-Pipe → Server-Tmpfile → `docker exec -i supabase-db-... psql -U postgres -d postgres < /tmp/...sql`
- Output: `CREATE TABLE / CREATE INDEX (×2) / ALTER TABLE (RLS) / DROP POLICY IF EXISTS+CREATE POLICY (×2) / GRANT (×2) / DROP TRIGGER IF EXISTS+CREATE TRIGGER` — alle Statements erfolgreich, NOTICE-Zeilen nur fuer "does not exist, skipping" beim DROP-Pattern (erwartet bei Greenfield-Apply).
- Verifikation:
  - `\d walkthrough_step` zeigt 17 Columns inkl. CHECK step_number >= 1, FK-Constraints (tenants ON DELETE CASCADE, walkthrough_session ON DELETE CASCADE, auth.users), 4 Indizes (PK, UNIQUE walkthrough_session_id+step_number, partial idx_walkthrough_step_session WHERE deleted_at IS NULL, idx_walkthrough_step_tenant), 2 Policies (walkthrough_step_select FOR SELECT, walkthrough_step_update FOR UPDATE), 1 Trigger trg_walkthrough_step_set_updated_at BEFORE UPDATE.
  - `pg_policy` zeigt 2 Policies (polcmd r=SELECT, w=UPDATE).
  - `pg_trigger` zeigt trg_walkthrough_step_set_updated_at.

## Vitest (lokal)

```
src/workers/walkthrough/__tests__/handle-extract-steps-job.test.ts    16 tests PASS
src/workers/walkthrough/__tests__/handle-redact-pii-job.test.ts        9 tests PASS
src/workers/walkthrough/__tests__/handle-transcribe-job.test.ts        7 tests PASS
src/lib/walkthrough/__tests__/pipeline-trigger.test.ts                 7 tests PASS
src/lib/ai/prompts/walkthrough/__tests__/step_extract-quality.test.ts  6 tests (1 PASS Sanity, 5 SKIPPED Bedrock-gated)
src/lib/ai/pii-patterns/__tests__/redaction-recall.test.ts            11 tests (2 PASS Sanity, 9 SKIPPED Bedrock-gated)
─────────────────────────────────────────────────────────────────────────────
Walkthrough-Pipeline gesamt                                          42/42 PASS, 14 skipped (Bedrock-Gates)
```

`tsc --noEmit` clean. ESLint 0/0 auf alle neuen+geaenderten Files. `npm run build:worker` PASS (Bundle 220.6 KB).

## Findings

### Blocker
keine.

### High
keine.

### Medium
keine. **Pattern-Reuse aus SLC-076 hat IMP-369..371-Klassen a priori vermieden:**
- IMP-369 (Bedrock-Prompt-Trennung): step_extract.ts startet sauber mit System-Prompt-only-Anweisungen, User-Message ist nur Eingabe-Text.
- IMP-370 (Live-Recall-Test als Pflicht-Gate): step_extract-quality.test.ts ist Pflicht-Bestandteil von /qa SLC-077.
- IMP-371 (Supabase-Mutation-Error-Check): jeder INSERT/UPDATE/DELETE im neuen Worker mit `{ error }` destructure + captureWarning (non-fatal Cost-Ledger) oder throw (fatal).

### Low
keine. Worker-Code sauber, alle Mock-Tests grun, Live-Smoke wartet nur auf Coolify-Worker-Redeploy.

## Open risks / missing checks

- **Live-Smoke wartet auf User-Coolify-Worker-Redeploy** auf den SLC-077-Commit. Sobald der Worker den neuen Code laedt, werden die 3 pending ai_jobs walkthrough_extract_steps automatisch gepickt — perfekter End-to-End-Smoke ohne manuelles Setup.
- **Bedrock-Live-Quality-Test (Variante B)** ist gegated via `ENABLE_STEP_QUALITY_TEST=1` und wird in /qa SLC-077 real gegen Bedrock ausgefuehrt. Erwartet: 5 Calls × ~1-2k Output-Tokens = ~$0.05 pro Run. Threshold: jede strukturierte Fixture liefert >= expectedMinSteps Schritte (3-4), 0 PII-Halluzinationen in action-Strings, Action-Pattern-Coverage 100%.
- **Cleanup-Cron-Race vs. SLC-074:** 3 Sessions in `extracting` >1h sind NICHT mehr pruefkritisch sobald SLC-077-Worker live ist (Pipeline schreitet automatisch zu mapping vor; mapping-Jobs bleiben pending bis SLC-078-Handler kommt, was wieder das gleiche Cleanup-Cron-Risiko fuer SLC-078 impliziert — bewusst akzeptiert weil die Reihenfolge der Slices SLC-077 → SLC-078 unmittelbar ist).
- **Pre-Production-Compliance-Gate** (DSGVO, Anwaltspruefung, Azure-EU) bleibt bewusst aufgeschoben (Memory feedback_compliance_gate_later). V5 Option 2 bleibt Internal-Test-Mode.

## Readiness Assessment

**SLC-077 /backend ist code-side ready** fuer /qa SLC-077 + Live-Smoke. Alle Pflicht-Gates der Slice-Spec sind erfuellt:
- Migration 085 live + verifiziert ✓
- Worker-Handler + Wiring code-funktional + Build PASS ✓
- ≥5 Vitest-Fixtures gruen ✓
- N=0 Edge-Case getestet ✓
- DSGVO-Region eu-central-1 ✓ (LLM_MODEL=eu.anthropic.claude-sonnet-4-20250514-v1:0)
- IMP-369..371-Disziplin in Prompt-Trennung + Cost-Ledger-Error-Check + Live-Quality-Test-Gate ✓
- Schema-Drift-Check (IMP-363) durchgefuehrt ✓

## Recommended next step

1. **User-Coolify-Worker-Redeploy** auf den SLC-077-/backend-Commit → 3 pending ai_jobs durchlaufen automatisch.
2. **`/qa SLC-077`** mit:
   - Live-Smoke-Verifikation: alle 3 Sessions wandern `extracting → mapping`, je >=1 walkthrough_step-Row in DB persistiert, action-Strings PII-frei, Pipeline-Trigger feuert, Folge-Job walkthrough_map_subtopics queued.
   - `ENABLE_STEP_QUALITY_TEST=1 npx vitest run --run step_extract-quality` (auf Hetzner gegen Live-Bedrock, ~$0.05).
   - Stub-Scan auf 7 neue + 3 modifizierte Files.
   - ai_cost_ledger-Eintrag fuer role='walkthrough_step_extractor' verifizieren.
3. **`/backend SLC-078`** Stufe 3 Auto-Mapping (Migration 086 + walkthrough_map_subtopics Worker mit Bridge-Engine-Pattern Reverse-Direction + Coverage-Test SC-V5-7 ≥70%, ~4 MTs).
