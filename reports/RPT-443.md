---
id: RPT-443
date: "2026-06-11"
skill: /backend
slice: SLC-V9.1-D
feature: FEAT-079
type: completion
status: completed
reviewOf: null
result: "SLC-V9.1-D backend layer (5 MTs) done + verified; frontend (page + 5 components) + docs deferred to /frontend"
title: "SLC-V9.1-D /backend — Setup-UI backend layer + DSGVO-consent migration + admin-stats + bugfix"
---

# SLC-V9.1-D /backend — Backend Layer

## Outcome
**Partially complete (backend layer done, frontend pending).** Delivered + verified the full server-side layer of SLC-V9.1-D — 5 of ~7 MTs. The remaining MTs are pure React UI (page assembly + 5 components) + docs, a distinct `/frontend` continuation. SLC-V9.1-D stays `in_progress`. No master-merge, no `released` (per guardrails).

## What was done (committed)
- **MT-0 (`825d3fd`)** — MIG-063 / `118_v91_*.sql`: additive migration on `email_inbound_endpoint` (+`setup_token_created_at`, +3 `dsgvo_consent_*` cols, status CHECK +`pending_setup`). **LIVE-applied to Hetzner** (159.69.207.29) + verified + PostgREST reloaded. DEC-209. IMP-1189 schema-validation caught the drift vs as-built MIG-057 (spec assumed cols/`pending_setup` that didn't exist).
- **MT-2 + MT-6 (`23b683b`)** — 5 server-actions + `poll-inbound` helper. Audit via `error_log` (OP has no `audit_log`). tenant_admin gate + tenant-scoped writes. slug stored without `bulk-` prefix; test-send to single-mailbox catchall, **no token header (DEC-R1-3)**. poll-inbound joins `email_message`→`email_bulk_run.endpoint_id` (not message-level).
- **MT-5 (`118bc2e`)** — `forward-source-stats` (pure `aggregateForwardStats` + thin fetch) + admin-audit page section (reject-rate >20% highlight). **Fixed pre-existing prod bug**: `vw_bulk_email_cost_monthly` column is `month`, not `month_start` (current-month cost was silently always 0). Vendor is a constant `imap-ionos` label (no vendor col; DEC-205 IONOS-IMAP superseded the SES design).
- **MT-3a (`23a1405`)** — `summarizeSetupIntent` Bedrock-Sonnet helper (eu-central-1, Conversational-First). Invocation 1:1 from `bedrock-sonnet/email-pattern.ts`.

## Files created
- `/sql/migrations/118_v91_inbound_endpoint_dsgvo_consent_setup_lifecycle.sql` — MIG-063 (LIVE)
- `/src/lib/bulk-email/poll-inbound.ts` + `/src/lib/bulk-email/__tests__/poll-inbound.test.ts`
- `/src/lib/bulk-email/dsgvo-consent.ts` — shared disclaimer text + version constant
- `/src/app/dashboard/bulk-email-import/forward-setup/actions.ts` + `__tests__/actions.test.ts`
- `/src/lib/bulk-email/forward-source-stats.ts` + `/src/lib/bulk-email/__tests__/forward-source-stats.test.ts`
- `/src/lib/bulk-email/ai-assisted-setup.ts` + `/src/lib/bulk-email/__tests__/ai-assisted-setup.test.ts`
- `/reports/RPT-443.md` (this report)

## Files changed
- `/docs/MIGRATIONS.md` — MIG-063 entry
- `/docs/DECISIONS.md` — DEC-209
- `/src/app/admin/audit/bulk-email/page.tsx` — Forward-Source section + `month_start`→`month` bugfix
- `/src/app/admin/audit/bulk-email/__tests__/page.test.ts` — forward-stats path coverage + mock extension
- `/docs/STATE.md` — current focus/phase → SLC-V9.1-D backend-layer done
- `/slices/INDEX.md` — SLC-V9.1-D `planned`→`in_progress`

## Files intentionally not changed
- `/src/lib/email.ts` — temporarily added a `headers` param for a token header, then reverted (net-zero) after the resolver showed forwarded mails carry no token header (DEC-R1-3). Surgical.
- `/planning/backlog.json` (BL-158) + `/features/INDEX.md` (FEAT-079) — already `in_progress`; no change.
- `/docs/COMPLIANCE.md`, `/docs/RUNBOOK.md` — MT-7 docs, deferred to /frontend pass + closure.

## Verification
- 32 new hermetic vitest GREEN across 5 test files; existing admin page test extended (now 9). tsc=0, ESLint=0 on all new/changed files.
- Full local suite: 1792 pass / 509 fail / 36 skip — **all 509 failures are environmental** (~506 `TEST_DATABASE_URL not set` DB-integration tests that throw rather than skip locally; 3 pre-existing non-DB failures in files I did not touch: 2 diagnose redirect-mock tests + 1 borderline cost-float assertion). No regressions from this work.
- MIG-063 LIVE-verified on Hetzner (4 cols present, status CHECK = 4 values).

## Problems found
- **Major spec/as-built drift (IMP-1189)** — spec assumed `audit_log`, vendor/local_part/domain cols, `email_message.endpoint_id`, `pending_setup`, DSGVO cols, `month_start`. As-built differs on all. Resolved: error_log audit, slug-only endpoint, run-level join, additive migration (DEC-209), constant vendor label, `month` column. Slice spec text is now partly stale vs implementation — MT-7 should reconcile the spec prose.
- **Pre-existing prod bug fixed** — admin cost-aggregate used non-existent `month_start` → always 0. Now `month`.
- **DB-integration verification is deferred** — server actions (auth.getUser) + poll-inbound real query are covered by hermetic mocks; true DB + end-to-end is the IONOS-gated Live-Smoke (OQ-R1-1, founder action, not done).

## Open points / deferred
- **/frontend (MT-1/3b/4):** `forward-setup/page.tsx`, `MailClientInstructions`, `SetupTokenDisplay`, `ConversationalSetupAssistant` (no KI-Filter-Assistant exists in OP — build from scratch or port from sibling repo per BLOCKING conversational-first rule), `DsgvoDisclaimerModal`, `TestSendButton`.
- **MT-7 (docs/closure):** COMPLIANCE.md (V9.1 Cross-Region-TIA + 4 event-types), RUNBOOK walkthrough, spec-prose reconciliation, then Gesamt-V9.1-/qa → Master-Merge (NOT before).
- **Founder D4:** Conversational-First wording freigabe (~1h).
- **OQ-R1-1:** IONOS mailbox + ENVs + scheduled-tasks — gates all V9.1 Live-Smokes.

## Recommended next step
`/frontend SLC-V9.1-D` (page + 5 components, fresh session — frontend-heavy) → slice `/qa` → MT-7 docs → Gesamt-V9.1-/qa → /final-check → /go-live → /deploy (incl. Live-Smokes) → /post-launch. Per [[module-lifecycle-discipline]]: Internal-Test-Mode, no customer-outreach.
