---
id: RPT-640
date: "2026-07-10"
skill: /qa
slice: SLC-194
feature: FEAT-111
type: completion
status: completed
reviewOf: null
result: "SLC-194 /qa MIXED (code-done): 4 AC PASS (sanitize real-pipeline, SVG-Block+Magic-Byte, branding-Farb-Revalidierung, Gates) + 2 AC PASS-CODE/PENDING-LIVE (Handbook-XSS-Browser-Render, CSP-Funktional-Smoke+enforce-Flip → /deploy). 0 Blocker/0 High/0 Medium. Stub-Scan 0, Wiring-Chain 5/5 intakt, Full-Suite 0 Regression (619 DB-env-gated/2319 pass). CSP-Probe nach tests/_probe/ portiert. NEXT /backend SLC-195."
title: "SLC-194 /qa — MIXED (4 PASS + 2 PENDING-LIVE), 0 Blocker; CSP-Funktional-Smoke + enforce-Flip an /deploy"
---

# SLC-194 — /qa (V20 XSS/Output/Headers-Hardening)

## QA-Scope
Code-side + statische Verifikation der 4 MTs (rehype-sanitize, SVG-Block+Magic-Byte, CSP+COOP Report-Only, branding-Farb-Revalidierung) im Worktree `v20-security-hardening` (HEAD `2abb96f`, un-deployt). Security-Header-Slice → security-headers-live-smoke.md (P-089) gelesen: der funktionale Browser-Smoke ist BLOCKING für ein volles PASS-LIVE und wird — da un-deployt + CSP Report-Only — als PENDING-LIVE an /deploy geführt (Probe portiert).

## Verdict: MIXED (code-done) — 0 Blocker / 0 High / 0 Medium

| AC | Verdikt | Beleg |
|---|---|---|
| AC-194-1 Schema stript script/iframe/srcdoc/on*, erhält `<a id>`+`<video>` | **PASS** | sanitize-schema.test.ts fährt die **reale** unified-Kette (remark-parse → remark-rehype{allowDangerousHtml} → rehype-raw → rehype-sanitize{schema} → hast-to-html), 8 Fälle grün — kein Mock der Sanitize-Stufe. |
| AC-194-2 `<iframe srcdoc>`-Payload rendert nicht aus | **PASS-CODE / PENDING-LIVE** | Pipeline-Test beweist: kein `<iframe`/`srcdoc` im HTML-Output. Sanitization passiert am hast→html-Transform (browser-unabhängig) → Browser-DOM-Confirm an /deploy = Formsache. |
| AC-194-3 SVG-Upload abgelehnt (MIME+Magic-Byte); Logo-Route ohne svg + nosniff | **PASS** | image-signature.test.ts (8: PNG/JPEG-Magic, SVG/HTML→null, PNG-declared-svg-bytes→Reject); actions.ts-Wiring gelesen (sniff VOR Upload, `sniffed!==mime`→Reject); route.test.ts Legacy-SVG→octet-stream + nosniff. Live-Upload-Reject → /deploy. |
| AC-194-4 CSP Report-Only (0 Eigen-Asset-Violations) → enforcing; Kernflows+Jitsi intakt | **PASS-CODE / PENDING-LIVE** | csp.test.ts (8, Direktiven-Struktur) + `next build` PASS (Config valide) + next.config-Wiring (Report-Only+COOP). Funktionaler Browser-Smoke + connect-src-Feature-Flow-Enumeration + enforce-Flip → /deploy (Probe `tests/_probe/csp-check.mjs` bereit). |
| AC-194-5 branding-Render revalidiert Farben | **PASS** | __tests__/resolve.test.ts Case 9 (CSS-Injection→Default) + Case 10 (missing→Default); Wiring: sanitizeHexColor auf primary+secondary. |
| AC-194-6 tsc0/eslint0/build/vitest 0 Regression + neue Tests; +1 Dep dokumentiert | **PASS** | tsc 0, eslint 0 (13 Files), next build EXIT 0; Full-Suite 619-fail(env-gated)/2319-pass = 0 Regression; rehype-sanitize@6 in package.json + DEC/Records. |

**Aggregat:** 4 PASS-Code-side (davon AC-194-1 real-pipeline) + 2 PASS-CODE/PENDING-LIVE. Mixed → Slice bleibt `code-done` (nicht `done`).

## Stub-Scan
0 Stubs über alle 9 Impl-Files (TODO/FIXME/debugger/console.log/empty-catch/placeholder-return/`as any`). Neue Konstrukte sind vollständig implementiert.

## Wiring-Chain-Audit (5/5 intakt)
1. **Handbook-XSS:** `[rehypeSanitize, handbookSanitizeSchema]` in BEIDEN Reader-Plugin-Stacks (grep-count 2), Position NACH rehypeRaw, VOR slug/autolink → sanitisiert genau das von rehypeRaw geparste tenant-HTML.
2. **SVG-Block:** `sniffImageMime` in uploadLogo VOR dem Storage-Upload aufgerufen (Bad-Content erreicht Storage nie); Client-Vorfilter BrandingEditor + Server-Allowlist konsistent (image/svg+xml in beiden raus).
3. **Logo-Route:** `X-Content-Type-Options: nosniff` auf der 200-Response; svg aus MIME_BY_EXT (Legacy → octet-stream).
4. **CSP:** `buildCSP` + `COOP_VALUE` importiert, beide Header-Keys in next.config headers() gesetzt; Build validiert die Config.
5. **branding-Render:** `sanitizeHexColor` auf primary (Default-Fallback) UND secondary (null-Fallback) → nur `#rrggbb` gelangt ins layout.tsx-`<style>`.

## Test-Execution / Silent-Skip-Audit
- `vitest run` (voll): **619 failed / 2319 passed / 65 skipped**. Die 619 Fails sind AUSSCHLIESSLICH `TEST_DATABASE_URL`-env-gated DB-Suiten (coolify-test-setup.md) = exakter Vor-Slice-Baseline-Stand → **0 neue Failures**. Kein Silent-Skip meiner Tests: alle 26 neuen Pure-Mock-Tests LIEFEN und bestanden (in Targeted-Runs einzeln bestätigt: sanitize 8, image-signature 8, csp 8, resolve +2, route +1). Include-Pattern deckt `src/**/*.test.ts` → neue Files erfasst.
- DB-Suiten laufen im /deploy node:20-Sidecar (env-bootstrap), nicht hier — dokumentierter Deferral, kein Silent-Skip.

## CSP-Feature-Flow-Coverage (Vorarbeit für den /deploy-Enforce-Flip, P-089)
Browser-seitige externe Origins enumeriert:
- **connect-src:** Client-fetches gehen an `/api/*` (self) inkl. Whisper/Transcribe (`/api/{tenant,admin}/transcribe` = same-origin); Supabase-Origin (NEXT_PUBLIC_SUPABASE_URL) in connect-src. Kein Realtime-WSS/externer Analytics-/CDN-Fetch im Client gefunden (pkware/google = Kommentar bzw. Test-Fixture, nicht runtime).
- **script-src + frame-src:** Jitsi ist EMBEDDED (external_api.js + iframe) → Jitsi-Domain in beiden. WSS/Media laufen in der Jitsi-Origin des iframes (kein Parent-connect-src).
- **Permissions-Policy:** camera/microphone=(self + Jitsi-Domain) unverändert (next.config) — korrekt für die iframe-Delegation.
- **img-src** `'self' data: blob:` — kein User-HTML mit Remote-Bildern im Render-Pfad (Handbook-Content ist sanitisiert, kein srcdoc-iframe).
Rest-Risiko: unter Report-Only werden etwaige übersehene Origins als Console-Violations sichtbar (genau der Zweck) — Voll-Enumeration + funktionaler Flow-Smoke sind Done-Gate des enforce-Flips an /deploy.

## Findings

### Low
- **L-1 (informational): CSP funktionaler Browser-Smoke + enforce-Flip an /deploy.** Un-deployt + Report-Only (non-breaking) → der P-089-Pflicht-Smoke (0 CSP-Console-Errors, hasReactProps/Fiber/onSubmit, Kernflows Login/Dashboard/Handbook/Diagnose/Jitsi-iframe) läuft gegen den deployten Container. Probe `tests/_probe/csp-check.mjs` portiert (Strategaize-Standard). Enforce-Flip (Header-Key → `Content-Security-Policy`) erst nach diesem Smoke mit realen Violation-Daten. Bekanntes Risiko: React inline `style={{}}`-Attribute + Next.js-RSC-inline-Scripts → style-src/script-src bleiben bewusst `'unsafe-inline'` (DEC-288, BS-konform), daher erwartet der Enforce-Flip KEINE Eigen-Asset-Blockade.
- **L-2 (informational): Live-Verifikation der XSS/Upload-Pfade an /deploy/Gesamt-QA.** Handbook-`<iframe srcdoc>`-Render (Browser) + realer SVG-Upload-Reject gegen die laufende storage-api sind code-verifiziert (reale Pipeline / Magic-Byte-Unit + Wiring), der deployte-Container-Confirm ist an /deploy.

## Open risks / missing checks
- Alle Live-Verifikationen (AC-194-2 Browser-Render, AC-194-4 CSP-Funktional-Smoke, AC-194-3 realer Upload-Reject) sind an /deploy bzw. Gesamt-QA V20 gebündelt — konsistent mit dem kumulativen Branch (ein Deploy nach allen V20-Slices). Kein Blocker, da CSP Report-Only (non-breaking) und die Sanitize-Kette browser-unabhängig am hast→html-Transform greift.
- ISSUE-127 bleibt bewusst offen (Report-Only + COEP-Deferral) bis enforce-Flip.

## Readiness assessment
Code-side release-ready für den kumulativen Branch. Keine funktionalen Blocker. Die verbleibenden Verifikationen sind Live-Gates (Deploy-Validation), keine offenen Defekte. Slice-Status bleibt **code-done**.

## Project record updates
KNOWN_ISSUES ISSUE-121/122/130 = resolved, ISSUE-127 = open (Progress) bereits in /backend gesetzt. SLC-194/FEAT-111/BL-538 bleiben in_progress (code-done). STATE Focus/Next-Steps auf „/qa done → /backend SLC-195". RPT-640 erstellt; tests/_probe/csp-check.mjs portiert.

## Next workflow step
**`/backend` SLC-195 (FEAT-112 Auth/Secrets)** — SLC-194 ist NICHT der letzte V20-Slice; SLC-195 (Login-Lockout, recording-ready, cron-timingSafe-Sweep, logger-redaction, slug-.eq) folgt sequenziell im selben kumulativen Worktree. Danach: **Gesamt-/qa V20** (holt die gebündelten Live-Smokes nach: Handbook-XSS-Render + SVG-Upload-Reject + **CSP-Funktional-Smoke via tests/_probe/csp-check.mjs** + CSP-enforce-Flip-Entscheid) → /final-check → `/code-review ultra` (DEC-219-Closure) → /deploy (MIG-133/134 Live-Apply) → /post-launch. Next IDs RPT-641/DEC-289.
