---
id: RPT-072
date: "2026-04-24"
skill: /doctor
slice: null
feature: null
type: completion
status: completed
reviewOf: null
result: "V3.1 Maintenance code done: 3 Backlog-Items geschlossen, prod-audit clean, docker-compose bereinigt"
title: "V3.1 Maintenance — BL-038 + BL-039 + BL-040 Code-Fixes"
---

# V3.1 Maintenance — Completion Report

## Outcome summary

V3.1 Maintenance code-seitig **fully complete**. Drei Backlog-Items geschlossen, drei KNOWN_ISSUES aufgeloest (davon eins `wontfix` als bewusster Skip). `npm audit --omit=dev` zeigt **0 Vulnerabilities** im Produktions-Pfad. TypeScript-Build + `tsc --noEmit` gruen.

Status: code done. Runtime-Verification (Bedrock-Smoke via Worker-Condensation-Job + Coolify-Container-Status) erfolgt nach User-Deploy.

## Files reviewed

- `/package.json`, `/package-lock.json`
- `/docker-compose.yml`
- `/src/lib/db/__tests__/admin-rls.test.ts`
- `/docs/KNOWN_ISSUES.md` (ISSUE-018, ISSUE-019)
- `/docs/DECISIONS.md` (DEC-040 als Anker fuer DEC-041)
- `/docs/STATE.md`
- `/planning/backlog.json` (BL-038..040)

## Files changed

1. `/package.json` — `"overrides": { "@xmldom/xmldom": "^0.8.13" }` hinzugefuegt (npm-Override forciert patched Version via transitive `mammoth`-Dependency).
2. `/package-lock.json` — regeneriert durch `npm install`; `@aws-sdk/client-bedrock-runtime@3.1036.0`, `@xmldom/xmldom@0.8.13 overridden`.
3. `/src/lib/db/__tests__/admin-rls.test.ts` — drei Assertions (Zeilen 88, 100, 197) per `WHERE tenant_id IN ($1, $2)` isoliert mit Test-Tenant-Parametern. Inline-Kommentare referenzieren ISSUE-018.
4. `/docker-compose.yml` — Service `supabase-studio`: `healthcheck: disable: true` mit Inline-Kommentar, der DEC-041 referenziert.
5. `/docs/DECISIONS.md` — **DEC-041** neu angelegt (supabase-studio Healthcheck-Skip) mit Reason + Consequence.
6. `/docs/KNOWN_ISSUES.md` — ISSUE-018 auf `resolved` (Resolution Date + Resolution-Text), ISSUE-019 auf `resolved` (mit Hinweis auf zusaetzlichen mammoth-Pfad), ISSUE-020 neu als `wontfix` (supabase-studio healthcheck, bewusster Skip).
7. `/docs/STATE.md` — Current Focus + Phase auf "V3.1 Maintenance Code Done / V4 Slice-Planning done". Next-Step "User deployt V3.1 manuell" + Bedrock-Smoke nach Deploy. V3.1-Sektion in Active Scope mit Done-Details erweitert.
8. `/planning/backlog.json` — BL-038/039/040 Status `open` -> `done`, Descriptions um konkrete Aenderungen + Report-Referenz erweitert.

## Files created

1. `/reports/RPT-072.md` — dieser Report.

## Files intentionally not changed

- `/docs/MIGRATIONS.md` — V3.1 enthaelt keine Schema-Aenderungen.
- `/docs/RELEASES.md` — REL-006 fuer V3.1 wird erst nach User-Deploy angelegt (Date = Deploy-Datum, nicht Code-Datum).
- `/features/INDEX.md` — V3.1 Maintenance adressiert keine Feature-Status-Transitions.
- `/planning/roadmap.json` — V3.1 bleibt `planned` bis Deploy (dann `released`).
- Alle V4-Slice-Files (SLC-033..040) — unveraendert, Fokus hier nur V3.1.

## Problems found

**Keine kritischen.** Fuenf Beobachtungen zur Transparenz:

1. **ISSUE-019 Scope-Drift:** Die urspruengliche BL-038-Beschreibung nannte nur `@aws-sdk/token-providers` (SSO-Pfad, nicht-Prod). Beim Upgrade stellte sich heraus: xmldom kommt auch ueber `mammoth` (Evidence-Mode DOCX-Extraction, echter Prod-Pfad). Zusatz-Fix via npm-Override war noetig. Deviation-Rule 2 (auto-add missing critical functionality) angewendet — echtes Security-Gap im Prod-Pfad.
2. **ISSUE-018 Line-Drift:** Die Issue-Description nannte Zeilen 199 und 224, aber die aktuelle Datei hatte die Count-Assertions auf Zeilen 88, 100 und 197. Drei Stellen statt zwei gefixt (inkl. Zeile 100 "tenant_admin von A sieht NUR KU-A" mit `rowCount === 1`, die genauso anfaellig war).
3. **Dev-Dependency-Vulns uebrig:** `npm audit` zeigt 6 moderate in vitest/vite/esbuild-Chain (Dev-Server-Advisory). Fix via `npm audit fix --force` wuerde vitest@4 installieren = Breaking Change. Out-of-V3.1-Scope. Kein Prod-Risiko. Belassen fuer eigene Wartungs-Iteration.
4. **Lokale Integration-Tests konnten nicht laufen:** `TEST_DATABASE_URL` war lokal nicht gesetzt → 24 von 119 Tests failed mit "TEST_DATABASE_URL is not set". Das ist Test-Setup-Thema (Coolify-DB-Zugriff), nicht Regression. 95 Unit-Tests passed. Integration-Test-Verifikation fuer BL-039 erfolgt durch User nach Deploy oder per Test-Run gegen Coolify-DB (reference_coolify_test_setup.md-Pattern).
5. **BL-040 Root-Cause nicht final debugged:** `disable: true` ist der pragmatische Fix. Die konkrete Ursache (welche ENV-Variable welchen Studio-Endpoint nicht bedient) wurde nicht via SSH auf Hetzner untersucht, da das den 20-Min-Scope gesprengt haette. DEC-041 dokumentiert den Tradeoff offen.

## Open points or deferred decisions

- **Runtime-Bedrock-Smoke-Test** — erfolgt nach User-Deploy. Erwartung: Condensation-Worker verarbeitet einen Test-Job erfolgreich, `ai_cost_ledger`-Eintrag entsteht.
- **Integration-Test-Run gegen Coolify-DB** fuer `admin-rls.test.ts` nach Deploy — bestaetigt BL-039-Fix praktisch.
- **REL-006-Eintrag** wird bei Deploy angelegt (Datum + Deploy-Commit).
- **Vitest-Major-Upgrade** — eigene Wartungs-Iteration.

## Project record updates

- `/docs/KNOWN_ISSUES.md` — ISSUE-018/019/020 aktualisiert.
- `/docs/DECISIONS.md` — DEC-041 hinzugefuegt.
- `/docs/STATE.md` — Phase + Focus + Next-Steps + V3.1-Section aktualisiert.
- `/planning/backlog.json` — BL-038/039/040 `done`.
- `/reports/RPT-072.md` — dieser Report.

Nicht in dieser Session:
- `/docs/RELEASES.md` REL-006 — nach Deploy.
- `/planning/roadmap.json` V3.1 Status `released` — nach Deploy.

## Recommended next step

**User deployt V3.1 manuell ueber Coolify** (Hetzner 159.69.207.29 onboarding-worker + onboarding-app Services). Nach Deploy:

1. Coolify zeigt supabase-studio Container ohne healthcheck (oder `healthy` ohne Check-Spam) — AC fuer BL-040.
2. Manuelle Verifikation: einen Condensation-Job anstossen (z.B. erneuten Block-Submit in Demo-Session) → Worker-Logs zeigen Bedrock-Call gruen, KU entsteht — AC fuer BL-038.
3. Optional: Tests lokal mit `TEST_DATABASE_URL=... npm run test -- admin-rls` oder gegen Coolify-DB laufen lassen — AC fuer BL-039.
4. REL-006 in `/docs/RELEASES.md` ergaenzen, V3.1 in roadmap.json auf `released`, dieser Report bleibt als Code-Done-Snapshot.

Parallel kann **`/backend SLC-033`** (V4 Schema-Fundament) gestartet werden — es beruehrt keine der V3.1-Aenderungen direkt.
