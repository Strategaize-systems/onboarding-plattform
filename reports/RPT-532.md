---
id: RPT-532
date: "2026-06-24"
skill: /qa
slice: SLC-172
feature: FEAT-092
type: review
status: reviewed
reviewOf: RPT-531
result: "PASS — SLC-172 MT-1 (Blueprint-Wizard + adaptive Vertiefung) combined Backend+Frontend. 0 Blocker/High/Medium, 1 Low. Code-Gates re-verifiziert (tsc 0, eslint 0, 15/15, 0 Stubs) + Runtime-Smoke gegen LIVE-Coolify-DB + echtem EU-Bedrock: realer MIG-126-Seed ergibt exakt 5 Kopplungen, chatWithLLM green/red sauber, metadata jsonb-Spalte live. Voller Browser-Klick + metadata-Write-Round-Trip nicht ausgeführt (V10 nicht deployed; Medium-Confidence-Limit, kein Defekt)."
title: "QA SLC-172 MT-1 — Blueprint-Wizard + adaptive Vertiefung (combined, Runtime-Smoke)"
---

# QA — SLC-172 MT-1 (Blueprint-Wizard + adaptive Vertiefung-Reveal)

**Verdict: PASS** (combined Backend+Frontend). 0 Blocker / 0 High / 0 Medium / 1 Low.
Worktree `c:/strategaize/strategaize-onboarding-plattform-v10`, Branch `v10-slc170b-blueprint-seed`, HEAD `398ab80` (clean).

## 1. QA-Scope
SLC-172 **MT-1** (Capture-Eintritt + adaptive Vertiefung, Choice A / DEC-249). MT-2 (KU-Seed-RPC MIG-127 + Diagnose-Trigger) und MT-3 (Reader + Routing) sind NICHT in diesem QA-Scope → AC-172-2/3/5 sind MT-2/MT-3-Concern und hier nicht bewertet. Bewertet: **AC-172-1** (Capture + Checkpoint-Write) und **AC-172-6** (adaptive Vertiefung).

## 2. Ergebnis je AC
| AC | Verdict | Grundlage |
|---|---|---|
| AC-172-1 (Capture-Lauf + `block_checkpoint`-Submit) | PASS (reuse-/code-verifiziert) | Wizard = `QuestionnaireWorkspace` 1:1 reused/unangetastet (R-172-2); Checkpoint-Mechanik (`rpc_create_block_checkpoint`) unverändert; Overview liest `block_checkpoint` korrekt (Query verifiziert). Kein Live-Klick (s. Limit). |
| AC-172-6 (adaptive Vertiefung gelb/rot) | PASS (runtime-verifiziert) | (a) realer MIG-126-Seed → exakt 5 Kopplungen; (b) echter EU-Bedrock green/red; (c) hermetische surfacing/filter-Tests 15/15. |

## 3. Code-Gates (re-verifiziert, nicht aus RPT-531 übernommen)
- `npx tsc --noEmit` → **EXIT 0**.
- `npx eslint` (6 Files) → **0 Errors**.
- `npx vitest run blueprint.test.ts` → **15/15 GREEN**.
- Stub-Scan (TODO/FIXME/placeholder-return/empty-handler/console/hardcoded) über `blueprint.ts` + `dashboard/stb/blueprint/**` → **0 Treffer**.

## 4. Statische Korrektheit / Wiring
- **Answer-Key-Konsistenz (kritisch):** `assessBlueprintKernAnswers` liest `session.answers[\`${block.key}.${q.id}\`]`; die geteilte `questionnaire-form.tsx` schreibt unter `${q.blockKey}.${q.id}` (UUID `id`, NICHT `frage_id`). **Konsistent.** Die Overview-„answeredCoupledKern"-Prüfung nutzt denselben Key. ✓
- **frage_id-Domäne:** Kopplung/Surfacing/Stash laufen durchgehend über `frage_id`; `assessAnswerAmpel` stasht per `frageId`, Reader liest per `frage_id`. **Konsistent.** ✓
- **Tenant-Isolation (Defense-in-Depth):** Overview + Block-Detail + beide Actions prüfen `session.tenant_id === profile.tenant_id` zusätzlich zu RLS. Block-Detail → `notFound()` für nicht-surfacte Vertiefung (nicht direkt aufrufbar). ✓
- **Stash-Ziel:** `capture_session.metadata` ist live `jsonb` (verifiziert) — additive fetch-merge-write korrekt, nicht in `answers` (record<string,string>). ✓

## 5. Runtime-Smoke (gegen LIVE-Coolify-DB 159.69.207.29 + echtem EU-Bedrock)
**(1) Realer Seed → Kopplungslogik (AC-172-6-Fundament).** Live-Query auf `template` (slug `stb_blueprint_kanzlei`, v1.0, geseedet): die 20 Fragen tragen `ebene` ∈ {`Kern`,`Vertiefung`} (capitalized; `ebeneOf` lowercased → match) und die Vertiefungsfragen teilen ihr `unterbereich` mit genau einer Kern-Frage:

| Vertiefung | unterbereich | → Kern |
|---|---|---|
| F-BP-016 | a2_erloesmix_marge | F-BP-004 |
| F-BP-017 | b1_personalengpass | F-BP-005 |
| F-BP-018 | c1_beratungsverschiebung | F-BP-007 |
| F-BP-019 | d1_ki_einsatz | F-BP-009 |
| F-BP-020 | f1_inhaberabhaengigkeit | F-BP-013 |

→ `deriveVertiefungCouplings` ergibt **exakt 5 Paare** (deckt sich mit Spec + Fixture); stufe1 = 15 Kern-Fragen (Gratis-Test bleibt 15 bei grün). **Das ist der Check, den der Fixture-Unit-Test nicht leisten kann** — das adaptive Fundament hält gegen echte Daten.

**(2) Echter EU-Bedrock-Assess (`chatWithLLM` + `parseAmpel`, Prompt 1:1 aus actions.ts).** Via gated Smoke (`RUN_BEDROCK_SMOKE`, Creds aus App-Container per Live-Smoke-ENV-Bootstrap, danach gewiped):
- Solide Kern-Antwort → raw `{"ampel":"green"}` → `parseAmpel` = `green` → **keine** Vertiefung surfacet. ✓
- Klar problematische Kern-Antwort → raw `{"ampel":"red"}` → `parseAmpel` = `red` → Vertiefung **surfacet**. ✓
- Connectivity + Auth zu eu-central-1 bewiesen (transienter 503-Throttle dazwischen, s. Low/ISSUE-108).

**(3) Stash-Spalte.** `capture_session.metadata jsonb` live bestätigt.

## 6. Findings
### Low
- **L-1 (→ ISSUE-108): Bedrock-Throttle → fail-open→yellow kann über-surfacen.** `assessBlueprintKernAnswers` macht bis 5 sequentielle Bedrock-Calls; bei `ServiceUnavailableException: Too many connections` (im Smoke beobachtet) fällt der Call fail-open auf `yellow` → Vertiefung erscheint evtl. trotz grüner Antwort. **Sichere Richtung (über-fragen), by-design.** Idempotenter Re-Klick korrigiert. Optionaler Retry-Backoff als Polish notiert.

## 7. Confidence-Limitierungen (kein Defekt, ehrlich deklariert)
- **Voller Browser-Klick-Durchlauf NICHT ausgeführt** (Medium-Limit). V10 ist nicht deployed (Branch-only, Founder/Coolify-Hold) und der Env-Gate `NEXT_PUBLIC_ENABLE_STB_VERTIKALE` ist live OFF. Mitigation: der Wizard ist 1:1 reused/unangetastet (R-172-2, dieselbe Komponente wie SLC-173/Modul-Flow), und alle **neuen** Seams (Overview-Filter, Block-Detail-`notFound`, Reveal-Button → Batch-Assess → Stash → Surfacing) sind dekomponiert verifiziert (realer Seed + echter Bedrock + hermetische Pure-Tests). Der einzige nicht-exerzierte Pfad ist der Standard-`useTransition`+Server-Action+`router.refresh` Button-Klick (Port aus `enqueue-button.tsx`). → Voll-Browser-Smoke beim V10-Deploy-Validation nachholen (analog SLC-175 AC-2).
- **metadata-Write-Round-Trip NICHT live ausgeführt.** Spalte (`jsonb`) + Merge-Logik (plain JS fetch-spread-write) + Reader-Key code/schema-verifiziert; ein Live-Write wurde bewusst unterlassen, um keine Test-`capture_session` in die Prod-DB zu schreiben (QA-Discipline: keine Prod-Pollution).

## 8. Files
### Files reviewed
- `/src/lib/stb-vertikale/blueprint.ts`
- `/src/app/dashboard/stb/blueprint/actions.ts`
- `/src/app/dashboard/stb/blueprint/[sessionId]/page.tsx`
- `/src/app/dashboard/stb/blueprint/[sessionId]/reveal-button.tsx`
- `/src/app/dashboard/stb/blueprint/[sessionId]/block/[blockKey]/page.tsx`
- `/src/lib/stb-vertikale/__tests__/blueprint.test.ts`
- `/src/app/capture/[sessionId]/block/[blockKey]/questionnaire-form.tsx` (Answer-Key-Quelle, unverändert)
- `/src/lib/llm.ts` (chatWithLLM-Provider/Region)
- `/src/lib/db/template-queries.ts` (Schema/Table)

### Files created
- `/reports/RPT-532.md` — dieser QA-Report.
- `/tests/_smoke/blueprint-assess.smoke.test.ts` — gated (`RUN_BEDROCK_SMOKE`) Live-Smoke des echten EU-Bedrock-Assess-Pfads (chatWithLLM + parseAmpel). QA-Artefakt; läuft nicht in normaler CI.

### Files changed
- `/docs/STATE.md` — Current Focus + Phase auf MT-1 /qa PASS + NEXT MT-2 aktualisiert.
- `/docs/KNOWN_ISSUES.md` — ISSUE-108 (Low) ergänzt.

### Files intentionally not changed
- `/slices/INDEX.md`, `/planning/backlog.json`, `/planning/roadmap.json` — SLC-172 = `in_progress`, BL-511 = `in_progress`, V10 = `active`: korrekt, MT-2 + MT-3 stehen aus. Kein Status-Wechsel.
- `/src/app/capture/[sessionId]/block/[blockKey]/questionnaire-form.tsx` — geteilte Wizard-Komponente (R-172-2), nicht angetastet.

## 9. Next workflow step
**`/backend SLC-172 MT-2`** — KU-Seed-RPC `rpc_seed_blueprint_diagnosis_input` (MIG-127, atomar/idempotent) + tenant-scoped Diagnose-Trigger-Klon. Vor Implementierung MIG-050/052 lesen, um zu klären, ob `block_diagnosis` einen Checkpoint mit `block_key=X` verlangt (7 thin Checkpoints) oder den Capture-Checkpoint reused. **Route-Hinweis:** `[sessionId]/page.tsx` ist die MT-1-Overview → MT-2 erweitert sie um die Diagnose-Sektion, ersetzt sie NICHT (Overview ≠ Workspace-Reader, analog Modul-Flow).

SLC-172 ist NICHT der letzte V10-Slice (MT-2/MT-3 offen) → V10 Gesamt-QA / /final-check / /go-live / /deploy / /post-launch noch nicht fällig. Voller Browser-Smoke von MT-1 + SLC-175 Live-Smoke beim späteren V10-Deploy-Validation.
