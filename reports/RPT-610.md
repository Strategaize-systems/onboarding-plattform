---
id: RPT-610
date: "2026-07-08"
skill: /backend
slice: SLC-190
feature: FEAT-107
type: completion
status: completed
reviewOf: null
result: "SLC-190 Berater-Sicht code-complete: Query-Layer-Scope (allowedTenantIds) + middleware/Layout/BeraterSidebar + Mein-Tag scoped via resolveWorkspaceScope. +29 Pure-Mock-Vitest gruen, tsc/eslint 0, next build PASS. DB-Sidecar + Live-E2E PENDING-LIVE (MIG-132 im /deploy)."
title: "SLC-190 /backend — Berater-Sicht: /admin gescopt + Mein-Tag allowedTenantIds-Scope"
---

# SLC-190 /backend — Berater-Sicht (FEAT-107, V10.4 P2, letzter Slice)

## Outcome summary
Alle 4 Micro-Tasks code-complete implementiert. `strategaize_berater` betritt `/admin/*`
ueber eine gefilterte Shell und sieht in „Mein Tag" nur seine zugewiesenen Tenants; der
Admin-Pfad ist unveraendert (0 Regression). Durchsetzung = additiver Query-Layer-Filter
`allowedTenantIds` (DEC-269/270), KEIN RLS-Churn. tsc 0 / eslint 0 / `next build` PASS;
+29 neue Pure-Mock-Vitest gruen, 0 neue Logic-Failures gegen die Baseline (git-stash-Vergleich).
DB-Sidecar-Test + Live-Berater/Admin-E2E sind PENDING-LIVE (MIG-132 wird erst im `/deploy` live appliziert).

## Files reviewed (Grounding)
- `slices/SLC-190-berater-sicht-scoped-loader.md` (Slice-Spec, Verified-Against-Code-Reality-Tabelle)
- `sql/migrations/132_v104_berater_foundation.sql` (RPC `berater_assigned_tenant_ids(p_uid uuid)->uuid[]`, `can_see_tenant`, RLS)
- `src/lib/supabase/middleware.ts`, `src/app/admin/layout.tsx`, `src/components/admin-sidebar.tsx`
- `src/app/admin/mein-tag/{page,actions,rag-action}.ts(x)`, `src/lib/workspace/{admin-gate,berater-gate}.ts`
- `src/lib/workspace/reports/{index,mandanten-uebersicht,review-queue,wo-stockt-es,activity-timeline,system-status}.ts`
- `src/lib/cockpit/load-cross-tenant.ts`, `src/components/workspace/{WorkspaceShell,ReportButtons}.tsx`
- `src/app/admin/berater/actions.ts` (SLC-189 RPC-Aufrufmuster), `src/__tests__/rls/berater-foundation-rls.test.ts` (SLC-188 Sidecar-Muster)
- 24 `/admin/**/page.tsx`-Gates (Grep): tenants/partners/reviews/text-overrides/funnel/berater/debrief u.a. re-gaten alle auf `strategaize_admin` → Berater wird redirected (kein Layout-Leak)
- `vitest.config.ts`, `src/test/{db,setup}.ts` (DB-Test-Gating), Playbook `coolify-test-setup.md`

## Files created (9)
1. `src/lib/workspace/tenant-scope.ts` — `scopeTenants(query, column, allowedTenantIds)`-Helper (undefined=kein Filter, definiert=`.in`; TS2589-sicher ueber internes Cast, kein rekursiver Generic).
2. `src/lib/workspace/workspace-scope.ts` — `resolveWorkspaceScope()` (admin=allowedTenantIds undefined / berater=RPC-Ergebnis / sonst null) + `loadBeraterAssignedTenants(userId)` (id+name via Admin-Client).
3. `src/components/berater-sidebar.tsx` — gefilterte Sidebar (Mein Tag + read-only Mandantenliste; keine Partner/Funnel/Text-Overrides/Tenants).
4. `src/lib/workspace/__tests__/tenant-scope.test.ts` (3 Tests).
5. `src/lib/workspace/__tests__/workspace-scope.test.ts` (7 Tests).
6. `src/lib/workspace/reports/__tests__/loader-tenant-scope.test.ts` (5 Tests — `.in`-Injektion je Loader + „ohne Filter keine .in").
7. `src/app/admin/mein-tag/actions.test.ts` (7 Tests — Berater-Whitelist + allowedTenantIds-Durchreichung).
8. `src/app/admin/mein-tag/rag-action.test.ts` (7 Tests — bindTenant fail-closed).
9. `src/__tests__/rls/berater-visibility.test.ts` (DB-Sidecar, 3 Tests, PENDING-LIVE).

## Files changed (12)
**MT-1 Loader-Scoping (Query-Layer):**
- `src/lib/workspace/reports/index.ts` — `loadReport(admin, key, allowedTenantIds?)` + Export `BERATER_REPORT_KEYS` (4 Keys, system_status ausgeschlossen); `system_status` ignoriert den Filter (nur Whitelist schliesst ihn aus).
- `src/lib/cockpit/load-cross-tenant.ts` — optionaler `allowedTenantIds`; `tenants.id` + Sub-Queries `tenant_id` gefiltert (template ohne tenant-Spalte).
- `src/lib/workspace/reports/mandanten-uebersicht.ts`, `review-queue.ts`, `wo-stockt-es.ts`, `activity-timeline.ts` — je +Param, `.in`-Filter auf jede tenant-tragende Query (R-190-1: Pflicht-Filter auf jedem Pfad).

**MT-2 Runtime-Routing + Layout:**
- `src/lib/supabase/middleware.ts` — Login-Redirect-Zweig `strategaize_berater` → `/admin/mein-tag` (reale Enforcement; role-check.ts ist nur der getestete Spiegel). Kein /admin-Block noetig (nur employee/partner_admin geblockt).
- `src/app/admin/layout.tsx` — role-Gate um `strategaize_berater` erweitert; rendert `BeraterSidebar`-Shell + laedt zugewiesene Tenants.

**MT-3 Mein-Tag Berater-Pfad:**
- `src/app/admin/mein-tag/page.tsx` — `resolveWorkspaceScope`; Tenant-Liste = alle (Admin) bzw. zugewiesene (Berater); `reportKeys`-Prop.
- `src/app/admin/mein-tag/actions.ts` — beide Actions via `resolveWorkspaceScope`; Berater-Report-Whitelist (`isKeyAllowedForScope`) + `allowedTenantIds` an `loadReport` (auch der Fazit-Pfad gescopt).
- `src/app/admin/mein-tag/rag-action.ts` — `resolveWorkspaceScope`; `bindTenant(admin, tenantId, allowedTenantIds)` fail-closed (fremder Tenant → no_tenant VOR DB-Lookup) fuer askRag + reembed.
- `src/components/workspace/WorkspaceShell.tsx`, `ReportButtons.tsx` — optionale `reportKeys`-Prop (Berater-Button-Set ohne System-Status; verhindert garantiert fehlschlagenden Button — additive Erweiterung ueber die literale Spec-Fileliste hinaus, defense-in-depth zur Server-Whitelist).

## Migrations / policies / data-model
Keine neue Migration (SLC-190 = 0 Migration, wie geplant). Keine RLS-Aenderung — Durchsetzung
liegt im Query-Layer (DEC-269/270). Konsumiert MIG-132 (RPC `berater_assigned_tenant_ids`,
noch nicht live → Berater-Pfade laufen live erst nach MIG-132-Apply im /deploy).

## Deviations (Deviation-Rules)
- **Rule 2 (kritische Funktionalitaet ergaenzt):** `generateReportFazitAction` zusaetzlich zu
  `loadWorkspaceReportAction` gescopt — sonst haette das KI-Fazit cross-tenant-Daten fuer den
  Berater zusammengefasst. Server-Whitelist + `allowedTenantIds` auf beiden Actions.
- **Additive Frontend-Erweiterung:** `reportKeys`-Prop an WorkspaceShell/ReportButtons ist NICHT
  in der literalen MT-Fileliste, aber von DEC-270 (Berater-Report-Set) gefordert; verhindert einen
  garantiert fehlschlagenden System-Status-Button. Optional, Default = alle → Admin unveraendert.

## Problems found
- **STATE.md „Immediate Next Steps" ist stale** (Item 1 referenziert noch V10.3 SLC-186). Pre-existing,
  bekannter deferred Cleanup (Memory-Notiz nahe ISSUE-136) — NICHT in diesem Slice adressiert. Current-Focus-Feld
  wurde aktualisiert.
- **Branch `v10-4-rollenmodell-p2` ist HINTER `main`** (main hat SLC-188 /qa RPT-606/607). Pre-Merge-Rebase
  gegen main ist Pflicht vor Merge (git-release.md); RPT-610 wurde per `git log --all` kollisionsgeprueft (frei).
- **DB-abhaengige Tests scheitern lokal** mit „TEST_DATABASE_URL is not set" (605 Baseline-Fails, environmental).
  Das ist die etablierte PENDING-LIVE-Baseline (kein lokaler Postgres, `feedback_no_local_docker`); die neue
  `berater-visibility.test.ts` laeuft im /deploy ueber den node:22-Sidecar. Baseline-Vergleich (git stash):
  Passed 2209→2238 (+29 meine), non-DB-Fail-Signaturen identisch → 0 neue Logic-Failures.

## Verification
- **tsc:** `npx tsc --noEmit` → 0 Fehler (TS2589 im Helper geloest).
- **eslint:** 0 auf allen geaenderten/neuen Files.
- **next build:** EXIT 0 (`'use server'`-Gate ok — actions.ts/rag-action.ts exportieren nur async + Types).
- **Pure-Mock-Vitest (targeted):** 10 Files / 39 Tests gruen — inkl. der 5 bestehenden Loader-Tests (0 Regression) + 29 neue.
- **Full-Suite-Delta (git stash Baseline):** +29 passed, +3 PENDING-LIVE DB (berater-visibility), 0 neue non-DB-Failures.
- **NICHT verifiziert (PENDING-LIVE, = /deploy):** Berater-Login-Landing im Browser, sichtbare Scope-Reduktion (nur zugewiesene Tenants), Admin-Unveraendert-Smoke, DB-Sidecar `berater-visibility.test.ts`, MIG-132-Live-Apply.

## Project record updates
- `slices/INDEX.md`: SLC-190 → `in_progress`
- `features/INDEX.md`: FEAT-107 → `in_progress`
- `planning/backlog.json`: BL-535 → `in_progress`
- `docs/STATE.md`: Current Focus auf SLC-190 code-done neu geschrieben
- `docs/DECISIONS.md`: DEC-270 existiert bereits (accepted, deckt Implementierung exakt) — keine Aenderung
- `reports/RPT-610.md`: dieser Report

## Recommended next step
**`/qa SLC-190`** (Pflicht nach /backend). Danach: Gesamt-QA V10.4 (SLC-188+189+190) → /final-check →
/go-live → /deploy (Pre-Merge-Rebase gegen main ZUERST, dann MIG-132-Live-Apply + DB-Sidecar + Berater/Admin-E2E-Smoke) → /post-launch.
