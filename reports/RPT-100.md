---
id: RPT-100
date: "2026-04-28"
skill: /qa
slice: SLC-041
feature: FEAT-029
type: completion
status: completed
reviewOf: RPT-099
result: "PASS — alle 14 ACs verifiziert (12 voll, 2 strukturell+abgeleitet). Pflicht-Gates erfuellt: 4-Rollen-RLS-Matrix 12/12 PASS gegen Live-DB, Worker-Backwards-Compat 7/7 PASS, Soft-Fail-Trigger lebend bestaetigt (RAISE WARNING gesehen), Build + 24/24 Unit-Tests gruen."
title: "/qa SLC-041 — Block-Review Backend-Foundation Pflicht-Gate-Verifikation"
---

## Outcome
SLC-041 (V4.1 FEAT-029 Backend-Foundation) hat alle 14 Acceptance Criteria erfuellt. 12 ACs voll lebend verifiziert (Live-DB-Schema, RLS-Matrix gegen Live-DB, Trigger-Verhalten Happy-Path + ON CONFLICT + Soft-Fail, Worker-Pre-Filter, Server-Actions, Helper, Build, Tests). 2 ACs strukturell verifiziert mit abgeleiteter Evidenz: AC-3 Backfill-Idempotenz durch ON CONFLICT DO NOTHING-Pattern (Live-Backfill produzierte 0 Rows weil keine pre-V4.1 employee_questionnaire-KUs existieren — wie in RPT-099 erwartet); AC-14 metadata-Counter durch Worker-Code-Pfad sichtbar verkabelt (kein Live-Snapshot-Run mit gemischten Reviews durchgefuehrt — natuerlich abgedeckt sobald SLC-042 den Trigger-Dialog liefert). Status: PASS, kein Blocker, 1 Medium-Severity Verifikations-Gap (AC-14 nur strukturell, nicht via Live-Snapshot). Empfehlung: weiter zu /frontend SLC-042.

## QA Scope
- Slice-Spec: /slices/SLC-041-block-review-backend.md (14 ACs + 4 Pflicht-Gates)
- Implementation: 8 neue Files + 5 modifizierte Files aus RPT-099 (Commit 59b6919)
- Live-DB: Hetzner Onboarding-Server 159.69.207.29, Container supabase-db-bwkg80w04wgccos48gcws8cs-173534197991
- Test-Repo auf Server: /opt/onboarding-test (synchronisiert auf Commit 59b6919)
- Test-Pattern: docker run node:20 + TEST_DATABASE_URL gegen Live-DB im Coolify-Netzwerk

## Result
**PASS** — alle Pflicht-Gates erfuellt. 1 Medium-Finding (AC-14 strukturell verifiziert, nicht via End-to-End Live-Snapshot). Kein Blocker.

## Findings

### High
_keine_

### Medium
- **AC-14 nur strukturell verifiziert.** `handbook_snapshot.metadata` enthaelt nach Worker-Lauf den Counter `{ pending_blocks, approved_blocks, rejected_blocks }` — aber das wurde nicht durch einen Live-Snapshot-Run mit gemischten Block-Reviews verifiziert, sondern nur durch Code-Inspektion in `src/workers/handbook/handle-snapshot-job.ts:212-216`. Der Pfad ist eindeutig verkabelt (Worker schreibt das Objekt unconditional auf jedem Lauf), wird aber durch SLC-042 (TriggerHandbookButton) erstmals natuerlich live durchlaufen. Mitigation: SLC-042 Browser-Smoke wird das automatisch validieren. **Wenn SLC-042 verzoegert ist:** Manuelle Verifikation moeglich via Worker-Job-Trigger gegen vorhandene Demo-Tenant-Session.
- **Stub-Detection: keine Hits**, aber 2 pre-existing TS-Errors in `src/app/admin/bridge/__tests__/action-helpers.test.ts:127,129` ("Unused @ts-expect-error directive") — bereits vor SLC-041 vorhanden, nicht von diesem Slice verursacht, blockieren `npm run build` nicht (TypeScript Test-Files werden vom Next.js-Build nicht geprueft). Tracking-Empfehlung: in SLC-042 oder als BL-Item adressieren.

### Low
- **Backfill-Effekt = 0 Rows** auf Live-DB. Keine pre-V4.1 employee_questionnaire-KUs existieren. Backwards-Compat-Code-Pfad (`hasAnyRows = false → alle KUs durch`) ist daher noch nicht via reale Production-Daten ausgeloest, aber durch 7/7 Filter-Tests strukturell abgedeckt.
- **Soft-Fail-Trigger via `ALTER ... DROP CONSTRAINT`-Pattern provoziert.** Eleganter Test, vermutlich nicht typisch fuer Dev-Setups. Funktioniert hier weil Test in Transaktion + Rollback eingebettet ist. Pattern fuer Dokumentation in IMP wert: "Soft-Fail-Trigger-Tests via temporaerem Constraint-Bruch".

## Acceptance Criteria — Live-Verifikation

| AC | Status | Evidenz |
|----|--------|---------|
| AC-1 Schema + UNIQUE + 2 Indizes | **PASS** | `\d public.block_review` zeigt 10 Spalten, UNIQUE(tenant_id, capture_session_id, block_key), idx_block_review_status_created (partial WHERE status='pending'), idx_block_review_tenant_status |
| AC-2 RLS aktiv, 4 Policies | **PASS** | `\d` Policies-Sektion zeigt block_review_admin_full (FOR ALL) + block_review_tenant_admin_select (FOR SELECT). tenant_member + employee = RLS-Default-Deny. Verifiziert durch RLS-Matrix-Tests. |
| AC-3 Backfill setzt approved + idempotent | **PASS strukturell** | ON CONFLICT (tenant_id, capture_session_id, block_key) DO NOTHING ist trivial idempotent. Live-Backfill = 0 Rows (keine pre-V4.1 KUs auf der DB), wie in RPT-099 erwartet. |
| AC-4 Trigger pending bei employee_questionnaire-Submit + ON CONFLICT DO NOTHING | **PASS lebend** | Live-Test gegen Session 6201f95e-9527-4383-b279-100550bd36d6: nach INSERT capture_events('answer_submitted','qa-test-ac4') zeigt `SELECT status FROM block_review WHERE block_key='qa-test-ac4'` → `pending`. Nach UPDATE auf `approved` und zweitem capture_events-INSERT: status bleibt `approved`. |
| AC-5 Trigger Soft-Fail (RAISE WARNING) | **PASS lebend** | Live-Test mit vergiftetem CHECK-Constraint (status auf approved/rejected limitiert): INSERT capture_events erfolgreich (`INSERT 0 1`), psql zeigt `WARNING: block_review trigger soft-fail: new row for relation "block_review" violates check constraint "block_review_status_check" SQLSTATE 23514`, kein block_review-Row erzeugt. |
| AC-6 loadApprovedBlockKeys → Set | **PASS** | 7 Filter-Tests + 12 RLS-Tests (in 5 SELECT-Tests) decken Set-Korrektheit ab. |
| AC-7 Worker-Pre-Filter mit + ohne block_review-Daten | **PASS** | 7 Filter-Tests: Backwards-Compat (`hasAnyRows=false`), Mixed approved/pending, All-rejected. Alle gruen. |
| AC-8 GF-KUs unbeeinflusst | **PASS** | Filter-Test "GF-KUs durchgereicht" gruen — `ku.source !== 'employee_questionnaire'` ueberspringt Filter komplett. |
| AC-9 Server-Actions strategaize_admin-only | **PASS** | 12 Action-Tests: tenant_admin → forbidden, tenant_member → forbidden, unauthenticated → unauthenticated, strategaize_admin → ok. |
| AC-10 reviewed_by + reviewed_at | **PASS** | 4 Action-Tests verifizieren reviewed_by=auth.uid() und reviewed_at=now() inklusive note-Field. |
| AC-11 getReviewSummary aggregiert korrekt | **PASS** | 5 Tests: leer, mixed, DISTINCT-Aggregation, 2 Error-Pfade. |
| AC-12 4-Rollen-RLS-Matrix 8+ Test-Faelle | **PASS** | 12/12 PASS gegen Live-DB (5 SELECT + 4 WRITE + 3 Trigger-Cases). Re-run gegen Live-DB im /qa: 851ms, 12 passed. |
| AC-13 Build + Tests gruen | **PASS** | `npm run build` erfolgreich, keine TypeScript-Fehler in Build-Pfad. 24/24 Unit-Tests + 12/12 RLS-Tests = 36/36. Pre-existing Test-File-Errors in bridge/action-helpers.test.ts beeintraechtigen den Build nicht. |
| AC-14 metadata enthaelt {pending,approved,rejected} | **PASS strukturell** | handle-snapshot-job.ts:212-216 schreibt das Objekt unconditional. Spalte `metadata jsonb NOT NULL DEFAULT '{}'` live in handbook_snapshot. Nicht durch End-to-End Live-Snapshot-Run mit gemischten Reviews validiert — natuerliche Validierung kommt mit SLC-042. |

## Pflicht-Gates aus Slice-Spec

| Gate | Status | Evidenz |
|------|--------|---------|
| RLS-Test-Matrix 8+ Test-Faelle gegen Live-DB | **PASS** | 12/12 PASS, 851ms |
| Worker-Backwards-Compat | **PASS** | 7/7 Filter-Tests gruen, Empty-State + Mixed + All-Rejected + GF-KU-Unbeeinflusst alle abgedeckt |
| Trigger Soft-Fail (RAISE WARNING) | **PASS** | Live mit vergiftetem Constraint provoziert, WARNING gesehen |
| Backfill-Idempotenz | **PASS** | Strukturell durch ON CONFLICT DO NOTHING; Live-Run produzierte 0 Rows (keine pre-V4.1-Daten) |
| `npm run build` + `npm run test` gruen | **PASS** | Build success, 36/36 Tests gruen |
| IMP-112 Re-Read vor Write | **PASS** | RPT-099 dokumentiert Re-Reads + dieser /qa hat Files vor Verifikation gelesen |
| Cockpit-Records-Update | **PASS** | STATE.md, slices/INDEX.md, features/INDEX.md, MIGRATIONS.md MIG-028 (live-Sektion) durch RPT-099 + dieser /qa aktualisiert |

## Stub-Detection

Scan ueber alle SLC-041-Files (Migration, Helper, Server-Actions, Tests):
- TODO/FIXME/HACK/XXX/debugger: **0 Hits**
- Empty handlers (`catch {}`, `() => {}`): **0 Hits**
- Placeholder-Returns (`return null/[]/`{}`): **0 Hits in Production-Code** (Test-Defaults sind erwartbar)
- Hardcoded Credentials/IDs: **0 Hits**
- Dummy-Daten in Production-Code: **0 Hits**

Pre-existing TS-Errors in `bridge/__tests__/action-helpers.test.ts:127,129` aus SLC-035 — nicht von SLC-041, nicht in Build-Pfad.

## Wiring Chain Verification

| Schritt | Quelle | Verifiziert |
|---------|--------|-------------|
| capture_events INSERT → Trigger feuert | Live-Test | PASS (qa-test-ac4 → pending erzeugt) |
| Trigger → Lookup capture_session.capture_mode | Trigger-Source | PASS (employee_questionnaire-Filter aktiv) |
| Trigger → INSERT block_review (pending) | Live-Test | PASS |
| Worker → loadBlockReviewState (Read block_review) | handle-snapshot-job.ts:118-122 | Verkabelt + 7 Filter-Tests gruen |
| Worker → applyBlockReviewFilter (KU-Filter) | handle-snapshot-job.ts:123 | Verkabelt + 7 Filter-Tests gruen |
| Worker → renderHandbook(filtered KUs) | handle-snapshot-job.ts:168-175 | Verkabelt, Renderer kennt Pre-Filter nicht (Architektur-Trennung intentional) |
| Worker → UPDATE handbook_snapshot.metadata | handle-snapshot-job.ts:212-216 | Verkabelt strukturell, Live-Snapshot-Run noch ausstehend (siehe AC-14) |
| Server-Action approveBlockReview → block_review UPSERT | actions.ts:60-75 | Verkabelt + 12 Action-Tests gruen, Live-Smoke kommt mit SLC-042 |

## Files reviewed
- /slices/SLC-041-block-review-backend.md
- /reports/RPT-099.md
- /sql/migrations/079_block_review.sql
- /src/workers/handbook/block-review-filter.ts
- /src/workers/handbook/handle-snapshot-job.ts
- /src/app/admin/blocks/[blockKey]/review/actions.ts
- /src/app/admin/blocks/[blockKey]/review/__tests__/actions.test.ts
- /src/lib/handbook/get-review-summary.ts
- /src/lib/handbook/__tests__/get-review-summary.test.ts
- /src/__tests__/rls/block-review-rls.test.ts
- /src/workers/handbook/__tests__/block-review-filter.test.ts
- /docs/STATE.md, /docs/MIGRATIONS.md, /slices/INDEX.md, /features/INDEX.md

## Files changed
- /docs/STATE.md — Current Focus aktualisiert auf "SLC-041 QA PASS, RPT-100 created", Immediate Next Steps neu sortiert (1. /frontend SLC-042).

## Files created
- /reports/RPT-100.md — dieser QA-Report.

## Files intentionally not changed
- /docs/KNOWN_ISSUES.md — keine neuen Issues. AC-14 strukturell verifiziert (Medium) ist durch SLC-042 natuerlich abgedeckt, kein eigener Issue noetig.
- /docs/DECISIONS.md — keine neuen Entscheidungen aus dem QA.
- /docs/SKILL_IMPROVEMENTS.md — keine neuen IMPs (das Soft-Fail-Trigger-Test-Pattern ist erwaehnenswert, aber spezifisch genug fuer einen Dev-System-IMP an den Handoff zu uebergeben).
- /slices/INDEX.md — SLC-041 Status bleibt `done` (RPT-099 hatte korrekt gesetzt).
- /features/INDEX.md — FEAT-029 bleibt `in_progress` (4 von 5 V4.1-Slices noch offen).
- /planning/backlog.json — keine Aenderung.
- /planning/roadmap.json — V4.1 bleibt `active`.

## Problems found

### Problem 1 — AC-14 nur strukturell verifiziert (Medium)
- **Was:** handbook_snapshot.metadata-Counter wurde nicht durch End-to-End Live-Snapshot-Run validiert.
- **Wo:** /src/workers/handbook/handle-snapshot-job.ts:212-216
- **Warum nicht voll verifiziert:** Worker-Job ausloesen erfordert TriggerHandbookButton (SLC-042) oder manuellen API-Call mit gemischten block_review-States. Letzteres haette Demo-Daten erzeugt, die in /qa-Scope nicht passen (kein Test-Tenant mit gemischten Mitarbeiter-Bloecken vorhanden).
- **Risiko:** Niedrig. Code-Pfad ist linear und unconditional. SLC-042 deckt diese Verifikation natuerlich ab.
- **Aktion:** Im SLC-042 Browser-Smoke explizit das Counter-Field nach Snapshot-Generation pruefen. Kein eigenes Issue noetig.

### Problem 2 — Backfill 0 Rows live (Low)
- **Was:** `INSERT INTO block_review ... SELECT DISTINCT ...` produzierte 0 Rows beim Live-Deploy.
- **Warum:** Onboarding-Live-DB hat noch keine knowledge_unit-Eintraege mit `source='employee_questionnaire'`. Demo-Tenant 0...0de hat zwar 2 employee_questionnaire-Sessions, aber die haben keine konsolidierten KUs hinterlegt.
- **Risiko:** Niedrig. Backwards-Compat-Pfad ist durch Filter-Tests strukturell abgedeckt. Erste reale Mitarbeiter-Bloecke werden ab V4.1-Release den Pfad natuerlich durchlaufen.
- **Aktion:** keine.

### Problem 3 — Pre-existing TS-Errors in bridge/action-helpers.test.ts (Low)
- **Was:** 2 "Unused @ts-expect-error directive" auf Zeilen 127+129.
- **Warum:** stammt aus SLC-035, nicht von SLC-041.
- **Aktion:** in SLC-042 oder als BL-Item adressieren. Blockt nichts.

## Open points or deferred decisions
- AC-14 voll lebend verifizieren: spaetestens beim ersten SLC-042 Browser-Smoke (TriggerHandbookButton → Worker-Job → Snapshot-Read).
- Pre-existing TS-Errors in bridge-Tests: in SLC-042 oder als kleines BL-Item aufnehmen.
- IMP-Kandidat (Dev-System): "Soft-Fail-Trigger-Tests via temporaerem CHECK-Constraint-Bruch in Tx + ROLLBACK". Pattern hat sich in diesem /qa als praktisch erwiesen — keinen Code-Change am Trigger noetig. Wird im Handoff erfasst.

## Project record updates
- /docs/STATE.md — Current Focus + Next Steps aktualisiert.
- /reports/RPT-100.md — dieser Report angelegt.
- /slices/INDEX.md — keine Aenderung (SLC-041 bleibt done).
- /features/INDEX.md — keine Aenderung (FEAT-029 bleibt in_progress, 1/5 V4.1-Slices done).
- /docs/KNOWN_ISSUES.md — keine Aenderung.
- /docs/MIGRATIONS.md MIG-028 — keine Aenderung (RPT-099 hat live-Sektion bereits gepflegt).
- /planning/backlog.json — keine Aenderung.
- /planning/roadmap.json — V4.1 bleibt active.

## Recommended next step
**`/frontend SLC-042`** — TriggerHandbookButton Quality-Gate-Confirm-Dialog + Cockpit-Card "Mitarbeiter-Bloecke reviewed". 5 MTs / ~1.5 Tage. Beim ersten Browser-Smoke wird AC-14 (handbook_snapshot.metadata-Counter) automatisch live verifiziert. Pre-existing TS-Errors aus bridge/action-helpers.test.ts koennen im selben Slice mit-aufgeraeumt werden (1-Minute-Fix).

Alternativ: erst Pre-existing TS-Errors als kleinen BL-Fix vorab erledigen, dann SLC-042. Aber kein Blocker.
