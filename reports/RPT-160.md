---
id: RPT-160
date: "2026-05-05"
skill: /go-live
slice: V4.4
feature: V4.4
type: review
status: completed
reviewOf: RPT-159
result: "GO — Internal-Test-Mode-SaaS, 0 Blocker, 0 High, 1 Medium pre-existing, MIG-030 schon live, Rollback-Pfad klar"
title: "Go-Live V4.4 — GO fuer REL-012"
---

# RPT-160 — Go-Live V4.4 — GO

## 1. Release Target Considered

- **Version:** V4.4 Maintenance-Sammelrelease (SLC-061 Lint-Sweep + SLC-062 SQL-Backfill MIG-030)
- **Release-ID:** REL-012
- **Target-Environment:** Production (`https://onboarding.strategaizetransition.com`, Hetzner-Coolify, 159.69.207.29)
- **Delivery-Mode:** SaaS (Internal-Test-Mode)
- **Deploy-Commit-Kandidat:** `9c1757b` (HEAD nach RPT-159) — finaler Deploy-Commit wird im /deploy-Step nach REL-012-Draft-Update bestimmt
- **Last Stable:** V4.3 REL-011 (Deploy-Commit `77b3852`, Container Up 5h healthy)

## 2. Readiness Decision: **GO**

V4.4 ist release-ready fuer Production. Deploy als REL-012 freigegeben.

## 3. Blockers
- keine

## 4. Accepted Risks

| Risk | Severity | Mitigation |
|------|----------|------------|
| 3 moderate prod npm-vulns (postcss bundled in next, ISSUE-026) | Medium | npm-Override-immun, Internal-Test-Mode mitigates exposure, identisch zu V4.3-Baseline |
| 9 dev-only npm-vulns | Low | Bleiben in Build-Stage, nicht im Production-Image |
| 120 pre-existing console.* in 27 Files | Low | Worker-Operational-Logging-Standard, V5+-BL fuer Logger-Konsolidierung |
| 5 pre-existing offene Issues (ISSUE-007/021/022/026/028) | Low | Alle V1-V4.1, dokumentiert + akzeptiert in jeweiligen Slice-/qa-Reports |
| Browser-Smoke fuer Bridge-Edit-Dialog/Jitsi/FileUpload deferred | Low | Vitest 535/535 als Surrogat akzeptiert, V4.4-Code-Diff rein code-neutral |
| BL-067 Berater-Help-Review noch offen | Low | DEFERRED parallel per User-Direktive, kein Release-Blocker |

## 5. Required Fixes Before Release
**Keine.**

## 6. Operational Concerns

### Self-hosted Stack Readiness Check (Docker Compose)

| Check | Status |
|-------|--------|
| All containers healthy | ✓ App + Worker `Up 5 hours (healthy)` (V4.3-Baseline, REL-012 wird auf gleichem Stack deployed) |
| Env var propagation verified | ✓ V4.3-Production stable seit 5h, Bedrock-ENV korrekt geladen (eu-central-1 + Claude-Sonnet-4) |
| Internal connectivity verified | ✓ Worker-Bedrock-Calls + Supabase-DB-Calls funktional (per RPT-150 + 5h Stable-Run) |
| Auth flow tested end-to-end | ✓ Browser-Smoke RPT-158: Login-Cookie aktiv, /dashboard reachable, /capture/.../block/A reachable |
| No hairpin NAT reliance | ✓ Server-Code nutzt internal Docker-URLs (V4.4 hat 0 URL-Aenderungen) |
| Health checks proven | ✓ /api/health → `{"status":"ok"}`, container healthcheck → `(healthy)` |
| Reverse proxy routing confirmed | ✓ Coolify-Proxy → Container-Routing seit V4.3 stable |

### MIG-030 Migration-Status (Special)

V4.4 hat eine Besonderheit: **MIG-030 ist bereits LIVE auf der Production-DB** (appliziert in /backend SLC-062, RPT-156). Der App-Code-Deploy enthaelt also keine Schema-/DML-Aenderungen mehr — nur Lint-Fixes auf 9 src/-Files + Doku/Records.

**Konsequenz:**
- App-Code-Deploy ist von DB-State entkoppelt (DB ist bereits auf REL-012-Stand seit `13:10 UTC` 2026-05-05).
- App-Rollback auf V4.3 (Commit 77b3852) hat KEINE DB-Rollback-Pflicht — V4.3-Code liest die korrigierten Umlaute korrekt (template.blocks/sop_prompt JSONB).
- Pre-Apply-Backup `/opt/onboarding-plattform-backups/pre-mig-030_20260505_131019.csv` (14032 bytes) bleibt als Recovery-Point falls DB-Rollback je noetig wird (sehr unwahrscheinlich, da V4.4-Lint-Fixes orthogonal zu DB).

## 7. Deploy Plan REL-012

### Deploy-Commit
HEAD nach RPT-160 (wird im /deploy-Step gesetzt nach REL-012-Draft-Update von RELEASES.md). Aktuell: `9c1757b`. Final wird der Commit nach diesem RPT + RELEASES.md-Update.

### Deploy-Pfad (User-Manual via Coolify)
1. User oeffnet Coolify-UI → Resource `bwkg80w04wgccos48gcws8cs` → Deployments-Tab.
2. User klickt **Redeploy** auf den HEAD-Commit (post-RPT-160 + REL-012-Draft).
3. Coolify pulled neuen Code, baut Image, restartet `app` + `worker` Container.
4. Health-Check kickt auto an (`/api/health`).

### Post-Deploy-Smoke (Pflicht-Checks)
1. `docker ps --filter 'name=app-bwkg' --filter 'name=worker-bwkg'` → beide `(healthy)` innerhalb 2-3 min nach Redeploy.
2. `curl -sI https://onboarding.strategaizetransition.com/login` → HTTP 200 oder 307 → `/login`. TTFB <200ms.
3. `curl -s https://onboarding.strategaizetransition.com/api/health` → `{"status":"ok"}`.
4. Coolify-Logs `app` + `worker` letzte 15min: 0 FATAL/ERROR/Crash.
5. Production-Container error_log letzte 15min: 0 errors/fatals.
6. Worker-Bedrock-ENV korrekt geladen (eu-central-1, Claude-Sonnet-4).
7. Browser-Smoke (User-Pflichteil): Login → /dashboard → /dashboard/handbook/<id> Reader laden, Cross-Search, Help-Sheet — alle V4.3-Flows funktional.

### 24-48h Post-Launch-Window
- 06.05. 09:00 Cron-Run `capture-reminders-daily` sollte sauber durchlaufen (V4.4-Code unbetroffen von Cron-Logik).
- 0 Errors/Crashes in app+worker container logs im 24h-Fenster.

## 8. Rollback Plan

### App-Rollback
- **Trigger:** Post-Deploy-Smoke schlaegt fehl (Container unhealthy, /api/health Error, Login-Page Error).
- **Action:** Coolify-UI → Deploy-Commit `77b3852` (V4.3 REL-011 Stand) → Redeploy.
- **RTO:** ~5min (Coolify-Redeploy + HTTP-200-Smoke).
- **RPO:** 0 Datenverlust.

### DB-Rollback (NICHT erforderlich)
V4.4 hat keine App-Code-Schema-Migration. MIG-030 (DML-only Backfill) ist bereits live + idempotent. V4.3-Code liest die korrigierten Umlaute korrekt aus `template.blocks` und `template.sop_prompt`.

**Falls DB-Rollback je noetig (Edge-Case):**
- Pre-Apply-Backup `/opt/onboarding-plattform-backups/pre-mig-030_20260505_131019.csv` (14032 bytes) → Restore via SQL-Restore-Pattern (CSV-Re-Import in `template`-Tabelle fuer `slug='mitarbeiter_wissenserhebung'`).
- Restore-Befehl-Skizze:
  ```sql
  UPDATE template
  SET blocks = (SELECT blocks FROM <restore_table> WHERE slug='mitarbeiter_wissenserhebung'),
      sop_prompt = (SELECT sop_prompt FROM <restore_table> WHERE slug='mitarbeiter_wissenserhebung')
  WHERE slug='mitarbeiter_wissenserhebung';
  ```

## 9. Recommended Next Step

**`/deploy V4.4 als REL-012`** — User-Coolify-Redeploy auf HEAD-Commit (post-RPT-160 + REL-012-Draft-Update von RELEASES.md). Live-Smoke nach Redeploy (App+Worker Health, /api/health, error_log, Login-Page TTFB).
