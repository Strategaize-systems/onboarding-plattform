---
id: RPT-513
date: "2026-06-22"
skill: /qa
slice: SLC-170
feature: FEAT-091
type: completion
status: completed
reviewOf: RPT-512
result: "MIG-125 M-04 Template-Seed DB-Sidecar (node:20 gegen Coolify-DB) 4/4 GREEN (AC-170-1..5), 0 Findings; tsc 0 / eslint 0 im Worktree re-verifiziert. PASS code-side. MIG-125 LIVE-Apply weiterhin pending /deploy (R-170)."
title: "/qa SLC-170 — MIG-125 StB M-04 Template-Content-Seed (DB-Sidecar PASS)"
---

# /qa SLC-170 — MIG-125 StB M-04 Template-Content-Seed

## 1. Outcome
**PASS (code-side).** Der DB-Sidecar-Test fuer MIG-125 (`migration-125-template-seed.test.ts`) laeuft 4/4 GREEN gegen die echte Coolify-Postgres-DB (node:20-Container im `bwkg80w04wgccos48gcws8cs_strategaize-net`, self-applied in gerollbackter Test-Transaktion). Alle Akzeptanzkriterien AC-170-1..5 sind damit DB-bewiesen, nicht nur statisch. `tsc --noEmit` 0 und `eslint` (Test-Datei) 0 wurden im Worktree unabhaengig re-verifiziert (nicht aus RPT-512 uebernommen). 0 Findings (0 Blocker / 0 High / 0 Medium / 0 Low).

MIG-125 ist weiterhin **nicht live** — der LIVE-Apply auf die Coolify-DB ist Sache des `/deploy` (R-170, BLOCKING) und muss VOR jeglichem Capture-Code (SLC-173) erfolgen.

## 2. Test-Lauf (DB-Sidecar, coolify-test-setup.md)
- Server `root@159.69.207.29`; DB-Container `supabase-db-bwkg80w04wgccos48gcws8cs-160450719820` (Suffix zur Laufzeit aufgeloest).
- Netz `bwkg80w04wgccos48gcws8cs_strategaize-net`; Test-Repo `/opt/onboarding-plattform-test` (auf `71ce2d9`), node_modules vorhanden, Helfer (`src/test/db.ts`, `auth-context.ts`, `fixtures/tenants.ts`) + `TemplateBlockSchema` vorhanden.
- 3 Files via tar -> base64 -> extract uebertragen: `sql/migrations/125_v10_stb_template_seed.sql`, `src/lib/db/__tests__/migration-125-template-seed.test.ts`, `src/lib/db/template-queries.ts` (Schema-Quelle, zur Drift-Vermeidung mitkopiert; unveraendert).
- Lauf: `node:20 npx vitest run src/lib/db/__tests__/migration-125-template-seed.test.ts` (Test-File explizit getargetet, IMP-1299). `node:20` reicht (Test importiert nur `zod` + `pg` + Helfer; `SupabaseClient` ist type-only -> kein supabase-js-Runtime).
- Ergebnis: **Test Files 1 passed (1) / Tests 4 passed (4)**, Dauer ~700ms.
- Server-Temp-Files (`/tmp/slc170.b64`, `/tmp/slc170.tar.gz`) nach Lauf entfernt.

## 3. Acceptance Criteria (alle erfuellt, DB-verifiziert)
- **AC-170-1** (1 Row `stb_modul_m04` v1.0; Stufe-1-Kern required=true + Stufe-2-Vertiefung required=false; `metadata.ki_hebel[]` Reifegrad 1-4): erfuellt — Test prueft `block_count=2`, `question_count=26`, `ki_hebel`-Laenge 13, `modul_key='m04'`, `modul_id='M-04'`.
- **AC-170-2** (idempotent, `ON CONFLICT (slug, version) DO UPDATE`): erfuellt — Test wendet MIG-125 zweimal an und prueft Single-Row (`COUNT=1`).
- **AC-170-3** (M-04 vollstaendig nach Spec; `output_contract` mirror `modul_output.output_kind`): erfuellt — 26 eindeutige `frage_id`, Positionen 1..26 lueckenlos; KI-Hebel decken Reifegrad-Set {1,2,3,4}; `output_contract.kinds = [entscheidung, standard, implementierungsschritt]`.
- **AC-170-4** (RLS-lesbar fuer Tenant, Reuse): erfuellt — `withJwtContext`-Read als authentifizierter Tenant-User liefert die Row (`template_read_all`, MIG-022).
- **AC-170-5** (DB-Sidecar: ladbar, Stufe-1/2-Split parst gegen `TemplateBlockSchema`, Re-Apply idempotent; tsc/eslint 0): erfuellt — `z.array(TemplateBlockSchema).parse(blocks)` liefert 2 Blocks; Kern 10 Fragen alle `ebene='Kern'`, Vertiefung 16 Fragen alle `ebene='Workspace'`; tsc 0 / eslint 0.

## 4. Static-Review-Befunde (vor DB-Lauf)
- Migration kapselt das INSERT in `DO $mig125$ BEGIN ... END $mig125$;` ohne standalone `BEGIN;`/`COMMIT;` -> laeuft unveraendert in der aeusseren `withTestDb`-Transaktion (BEGIN/ROLLBACK). `RAISE NOTICE` im DO-Block unkritisch.
- `TemplateQuestionSchema.ebene` ist `z.string()` (kein Enum) -> "Kern"/"Workspace" parsen problemlos; Scoring-Flags optional/default false (Seed setzt sie explizit false); `title` als `z.record(string,string)` deckt {de,en,nl}.
- Seed-Content-Counts vor DB-Lauf gegen die SQL gegengeprueft: Kern 10 + Vertiefung 16 = 26 Fragen; ki_hebel-Reifegrad-Folge {2,2,1,2,2,2,1,2,3,3,4,4,4} -> Set {1,2,3,4}.

## 5. Files reviewed
- `/sql/migrations/125_v10_stb_template_seed.sql` — Seed-Migration (geprueft, unveraendert).
- `/src/lib/db/__tests__/migration-125-template-seed.test.ts` — DB-Sidecar-Test (geprueft, unveraendert).
- `/src/lib/db/template-queries.ts` — Zod-Shape `TemplateBlockSchema`/`TemplateQuestionSchema` (geprueft, unveraendert).
- `/src/test/db.ts` — `withTestDb` BEGIN/ROLLBACK-Pattern (geprueft).
- `/docs/stb-vertikale/M-04-seed-source.md` — Quell-Mapping (geprueft, unveraendert).
- `/slices/SLC-170-stb-template-content-seed.md`, `/reports/RPT-512.md` — Slice-Spec + /backend-Bericht (Verifikationsbasis).

## 6. Files changed
- `/docs/STATE.md` — Current Focus + Current Phase + Immediate Next Step #1 auf SLC-170 /qa PASS -> NEXT Pre-Merge-Re-Check + Master-Merge + /deploy. Edit via Python (Datei zu gross fuer Edit-Tool, IMP-1313); neue Current-Focus-Zeile bewusst gebounded, aeltere Inline-History auf reports/ + Memory verwiesen.

## 7. Files intentionally not changed
- `/slices/INDEX.md` — SLC-170 bleibt `in_progress`: Slice-Abschluss (-> `done`) erfolgt erst beim Master-Merge im /deploy (SLC-169-Konvention). /qa ist Code-Side-Verifikation, nicht Slice-Closure.
- `/features/INDEX.md` (FEAT-091 in_progress), `/planning/backlog.json` (BL-510 in_progress), `/planning/roadmap.json` (V10 active) — korrekt fuer den aktuellen Stand, kein Statuswechsel durch /qa.
- `/docs/KNOWN_ISSUES.md` — keine Findings, kein Eintrag.

## 8. Problems found
Keine. 0 Blocker / 0 High / 0 Medium / 0 Low. Der DB-Sidecar lief gegen die echte Coolify-DB (kein lokaler Postgres-Surrogat); die Migration ist self-applied in einer gerollbackten Transaktion und hinterlaesst keine Daten.

## 9. Open points / deferred
- `/deploy` (R-170, BLOCKING): MIG-125 LIVE-Apply auf Coolify-DB **vor** Capture-Code (SLC-173). Pre-Apply-Backup + base64-Apply (Migration-Header / sql-migration-hetzner.md).
- Pre-Merge-Re-Check (6 Schritte) vor Master-Merge: Rebase auf origin/main, Tests post-rebase, MIG-Collision (124/125), Pattern-Drift, Diff-Review. SLC-169-Master-Merge (`0234334`) liegt ggf. noch lokal/ungepusht -> beim Push beachten; /deploy R-169-2 = MIG-124 LIVE-Apply vor Worker-Code (SLC-174).
- SLC-170b (content-gated, Founder-Autoring): Blueprint zuerst (entsperrt SLC-172), dann M-06 + restlicher 18-Cut.

## 10. Recommended next step
**Pre-Merge-Re-Check + `git merge --no-ff` (Branch `v10-slc170-template-seed`) + `/deploy`** — MIG-125 LIVE-Apply (R-170) vor Capture-Code, danach `/post-launch`.
