---
id: RPT-471
date: "2026-06-15"
skill: /architecture
slice: null
feature: FEAT-083
type: completion
status: completed
reviewOf: null
result: "V9.7 OKF-Architektur fixiert — Q-V9.7-A..E aufgelöst (DEC-220..223) + 5 Live-Schema-Grounding-Korrekturen (DEC-224/225). Isolierter Emitter src/lib/handbook/okf/*, 0 Migrationen, 0 neue Deps. READY für /slice-planning."
title: "Architecture V9.7 — OKF Handbuch-Export"
---

# Architecture V9.7 — OKF Handbuch-Export

## Outcome
**Fully complete. READY für /slice-planning V9.7.** Alle 5 Open Questions (Q-V9.7-A..E) aufgelöst, dazu 5 Korrekturen aus Live-Schema-Grounding gegen die Coolify-DB. Architektur ist code-gegroundet (echter Worker-/Schema-Read, nicht aus Dateinamen). **0 DB-Migrationen, 0 neue Dependencies, 0 neue ENVs/Cron, 0 neue Tabellen.**

## Cross-Session-Sweep (Founder-Auftrag „finde alles")
Vollständiger Bestandsaufnahme über beide Repos (OP + Dev-System, inkl. Git-History/uncommitted). Gefunden:
- **OP** (committed `a39989b`, ungepusht): FEAT-083/084, BL-162/163, roadmap v9-7 active, RPT-468/469, STATE/features-INDEX.
- **Dev-System** (Branch `okf-adoption`): Rule `strategaize-okf-profile.md` (Profil 1.0), Rule `okf-spec-monitoring.md` (Cron `okf-spec-watch` live), `docs/OKF_OP_HANDBOOK_EXPORT_SLICE.md` (Proposal), `docs/OKF_STRATEGY_2026-06-13.md`, PITCH/SALES-OKF-Abschnitte, IMP-1265/1266.
- **Neu von anderer Session (commit `0c146c2`, 2026-06-15):** `docs/OKF_IS_BRAND_PROFILE_EXPORT_SLICE.md` — Sibling-Proposal, das die Cross-Repo-Sequenz fixiert: **OP-OKF-Emitter ist die kanonische Quelle, die IS später per Pattern-Reuse portiert** (IS-OKF erst nach OP-Emitter + nach IS V4.3). Architektur-relevant: V9.7 baut das Referenz-Pattern für alle Strategaize-Repos.

## Entscheidungen (DEC-220..225)
- **DEC-220** (Q-A): same Download-ZIP, zweiter Ordner `okf/`, kein neuer Endpoint.
- **DEC-221** (Q-B+Q-C): keine Section-Container-Files; root `index.md` Section-gruppiert; Section-Ordner + Filenames `<type>-<slug>-<id8>.md`; bundle-root-absolute Cross-Links.
- **DEC-222** (Q-D): block_diagnosis = 1 Concept/Row, subtopics als Body-Subsections (Subtopics haben keine UUID → kein strategaize_id pro Subtopic).
- **DEC-223** (Q-E + DSGVO): evidence_refs sind PII-UUIDs `{recorded_by_user_id, walkthrough_session_id}` → NICHT als `# Citations`; nur `evidence_count`. recorded_by_user_id nie im Output (SC-V9.7-9).
- **DEC-224** (Grounding): confidence text-Enum 1:1 (kein numeric-Mapping); `ai_draft`→observation; `tags` weggelassen (keine themes-Spalte → V9.8/BL-505); timestamp←updated_at; curation_status←status 1:1.
- **DEC-225** (Isolation/Contract): OKF-Details nur in `src/lib/handbook/okf/*`; Emitter konsumiert dieselben gefilterten in-memory Arrays; weiche Fehler-Degradation (Empfehlung); `yaml@^2.9.0`-Reuse; Conformance-TDD-RED zuerst.

## Live-Schema-Grounding (Coolify-DB 2026-06-15, korrigiert Proposal/Profil)
- `knowledge_unit.confidence` = text CHECK `['low','medium','high']` (NICHT numerisch).
- `unit_type` CHECK = `['finding','risk','action','observation','ai_draft']` — `ai_draft` nicht im OKF-Type-Register.
- **Keine `themes`-Spalte** auf knowledge_unit (Profil/FEAT-083 nahmen sie als tags-Quelle an).
- `evidence_refs` jsonb (8/41 non-empty) = Walkthrough-Provenance-UUIDs inkl. PII — NICHT die im Profil angenommenen `.link`-Citations.
- `block_diagnosis.content` = `{block_key, subtopics:[{key,name,fields}]}`; block_diagnosis/sop haben created_at/updated_at, keine confidence.
- Worker-Loader-SELECTs holen aktuell NICHT evidence_refs/timestamps → müssen erweitert werden (generische Felder, kein DB-Change).

## Komponenten (Vorschlag, /slice-planning fixiert Pfade)
NEU: `src/lib/handbook/okf/emit.ts` + `bundle.ts` + `conformance.ts` (+Test) + `types.ts`.
GEÄNDERT: `src/workers/handbook/handle-snapshot-job.ts` (SELECTs + additiver Call), `types.ts` (Row-Felder), `zip-builder.ts` (Multi-Folder).
UNVERÄNDERT: `renderer.ts`, `index-builder.ts`, Download-Endpoint, alle DB-Tabellen.

## Files changed
- `/docs/ARCHITECTURE.md` — V9.7 Architecture Addendum (§1-10) angehängt.
- `/docs/DECISIONS.md` — DEC-220..225 ergänzt.
- `/docs/STATE.md` — High-Level State→architecture, Current Focus/Phase auf /architecture DONE + /slice-planning next.

## Files created
- `/reports/RPT-471.md` — dieser Report.

## Files intentionally not changed
- `/docs/MIGRATIONS.md` — V9.7 hat 0 Migrationen (OKF = Export-Schicht).
- `/planning/roadmap.json` — v9-7 bleibt `active` (korrekt). `/planning/backlog.json` — BL-162/163 bleiben `open` bis /backend.

## Problems found
- 3 faktische Fehler im vorab-authored Proposal/Profil (handbook.json-Phantom bereits in /discovery; jetzt zusätzlich: numeric-confidence-Mapping, evidence_refs[0].link, themes-Spalte) — alle code-gegroundet korrigiert (DEC-223/224). Dev-System-Folge: Profil-Mapping-Beispiel präzisieren (IMP-Kandidat, Schwester zu IMP-1265).

## Open points → /slice-planning
1. OKF-Fehler-Degradation final: weich (Empfehlung) vs. hart.
2. Cross-Link-Regel KU↔diagnosis via gemeinsamem block_key.
3. Test-Layout-Konvention (colocated vs __tests__/, IMP-1262) vor Test-Pfad-Auflistung prüfen.

## Recommended next step
`/slice-planning V9.7` — FEAT-083 (Emitter, TDD-RED Conformance zuerst) → FEAT-084 (Bundle-Assembly + Worker-Wiring + ZIP-Multi-Folder), Cumulative-Single-Branch, AC-Matrizen gegen SC-V9.7-1..10.
