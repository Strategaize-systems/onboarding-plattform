---
id: RPT-245
date: "2026-05-14"
skill: /qa
slice: SLC-106
feature: FEAT-046
type: completion
status: completed
reviewOf: RPT-244
result: "Quality-Gates Full PASS — Stub-Detection 0, ESLint 0/0, tsc 0, build PASS, Vitest 981 PASS"
title: "V6 SLC-106 MT-1..MT-5 — Quality-Gates Full nach /backend"
---

## QA-Scope reviewed

QA-Run gegen V6 SLC-106 MT-1..MT-5 (Migration 092 LIVE + Outbound HTTP-Adapter
+ buildNotesFromDiagnose Helper + Server Action `requestLeadPush`). Code-side
fertig laut RPT-243+RPT-244. /qa deckt das Bundle der 5 abgeschlossenen MTs als
formale Quality-Gates-Etappe vor MT-6 ab — Stub-Detection + Lint + Type-Check +
Build mit dummy-ENV + Audit + Full-Regression-Vitest gegen Coolify-DB im
node:20-Container (Netzwerk `bwkg80w04wgccos48gcws8cs_strategaize-net`).

## Result

**PASS code-side.** Alle 6 Quality-Gates gruen. 0 Blocker, 0 High, 0 Medium, 0 Low
NEU. 5 pre-existing residual Vulns (alle pre-V6 carry-over, AC-16 erfuellt).
Wiring-Verification-Hinweis dokumentiert (Server-Action existiert noch ohne
Caller — wird in MT-7 angehaengt). Live-Cross-System-Smoke gegen Business-System
ist ausdruecklich MT-12 Scope und nicht Teil dieses Gates.

## Quality Gates

### Gate 1 — Stub-Detection auf neuen MT-3/MT-4/MT-5 Files
- `src/lib/integrations/business-system/lead-intake.ts` (MT-3, 64 Zeilen): 0 Findings.
- `src/lib/integrations/business-system/types.ts` (MT-3 + MT-4 DTO): 0 Findings.
- `src/lib/integrations/business-system/build-notes.ts` (MT-4, 37 Zeilen): 0 Findings.
- `src/app/dashboard/diagnose/[capture_session_id]/lead-push-actions.ts` (MT-5, 435 Zeilen): 0 Findings.
- `src/lib/integrations/business-system/__tests__/build-notes.test.ts` (MT-4): Test-File, dummy-Daten erwartet.
- `src/lib/integrations/business-system/__tests__/lead-intake.test.ts` (MT-3): Test-File, dummy-Daten erwartet.
- `src/app/dashboard/diagnose/[capture_session_id]/__tests__/lead-push-actions-db.test.ts` (MT-5): Test-File, dummy-Daten erwartet.

Pattern gescannt: `TODO`, `FIXME`, `HACK`, `XXX`, `console.log`, `debugger`,
`return null;`, `return [];`, `return {};`, `catch(...){}`, `onClick={() => {}}`.
**Production-Files clean.**

### Gate 2 — ESLint
`npx eslint src --max-warnings=0` EXIT=0. **0/0.**

### Gate 3 — TypeScript
`npx tsc --noEmit` volltree EXIT=0. **0 Type-Errors.**

### Gate 4 — Next.js Production Build (dummy-ENV)
`npm run build` mit Test-Stubs fuer SUPABASE/JWT/BUSINESS_SYSTEM_INTAKE EXIT=0.
Build-Output zeigt 53 Routen — die Diagnose-Bericht-Page (SLC-105) ist erwartungs-
gemaess noch nicht im Tree, die Server-Action `lead-push-actions.ts` ist daher
aktuell ohne Caller (Wiring-Hinweis siehe Findings).

### Gate 5 — npm audit
`npm audit --omit=dev` zeigt 5 Vulns (1 low, 2 moderate, 2 high) — **alle pre-existing**:
- next < 15.5.7 — pre-V6, package.json zuletzt von SLC-053 (`2645ba7`) und SLC-044 (`9418bbc`) modifiziert
- next-intl <= 4.9.1 — pre-V6
- postcss < 8.5.10 — pre-V6 (carry-over von ISSUE-026)

**0 SLC-106-induced Vulns. AC-16 erfuellt** ("SLC-106 fuegt keine npm-Deps hinzu").
Diese Vulns blocken V6-Internal-Test-Mode nicht — werden im Pre-Production-Compliance-
Gate (V6.x oder spaeter) gesammelt adressiert (siehe Last Stable Version Notes).

### Gate 6 — Vitest Full-Regression gegen Coolify-DB
node:20 Docker-Container im selben Netzwerk wie `supabase-db-bwkg80w04wgccos48gcws8cs-151532171639`:

```
Test Files  87 passed (87)
Tests       981 passed | 14 skipped | 2 todo (997)
Duration    5.96s
```

**SLC-106-spezifische Tests (alle PASS):**
- `lead-intake.test.ts` (MT-3) — 7 Tests in 26ms (Happy / HTTP 4xx / HTTP 5xx / Timeout / Network-Error / Invalid-Response / ENV-fehlt).
- `build-notes.test.ts` (MT-4) — 3 Tests in 3ms (typical / empty / score-edges).
- `lead-push-actions-db.test.ts` (MT-5) — 8 DB-Contract Tests in 214ms (Happy / UNIQUE-23505 / CHECK status / CHECK attempt_number / FK 23503 / CASCADE / ai_jobs CHECK whitelisted / UPDATE failed-transition).

**Direkt benachbarte Pflicht-Tests:** `v6-partner-rls.test.ts` 102 Tests (100 PASS + 2 todo) — die 2 todo bleiben fuer MT-9 Pen-Test-Aktivierung auf `lead_push_consent` + `lead_push_audit` reserviert. **Keine Regression** in V4/V5/V5.1 Pen-Test-Matrizen.

## AC-Mapping (MT-1..MT-5 Scope)

| AC | Beschreibung | Status MT-1..MT-5 | Anmerkung |
|----|---|---|---|
| AC-1 | Mandant kann Modal oeffnen + Submit | DEFERRED → MT-7 | Wiring-Pfad fehlt noch (Modal-UI in MT-7). |
| AC-2 | Ohne Checkbox: privacy_checkbox_required | PASS (Server) | Server-Action lehnt sauber ab; UI-Defense in MT-7. |
| AC-3 | Lead landet im Business-System | DEFERRED → MT-12 | Code-Pfad PASS, Live-Cross-System-Smoke = MT-12. |
| AC-4 | First-Touch-Lock funktioniert (already_pushed) | PASS | Idempotenz-Pre-Check + UNIQUE-Constraint verifiziert in 2 Tests. |
| AC-5 | lead_push_consent korrekte Audit-Felder | PASS | INSERT-Pfad mit consent_text_version + IP + UA + mandant/partner-IDs. |
| AC-6 | lead_push_audit success-Eintrag | PASS | UPDATE-Pfad bei Happy-Run, status='success' + business_system_contact_id. |
| AC-7 | BS-Downtime: status='failed' + Retry-Job | PARTIAL | Enqueue-Pfad implementiert + Test PASS; Worker-Pickup = MT-6. |
| AC-8 | Retry-Job laeuft erfolgreich | DEFERRED → MT-6 | Worker-Handler noch nicht implementiert. |
| AC-9 | Max 3 Versuche / Backoff 5min+30min | DEFERRED → MT-6 | Synchron-Pfad enqueued Attempt 2 mit 5min Backoff; Cap+30min = MT-6. |
| AC-10 | Bedrock-Cost-Audit unbeeinflusst | PASS | Lead-Push ist HTTP-only, kein KI-Call (verifiziert per Code-Inspect). |
| AC-11 | notes-Field 2-3 Saetze, kein Roh-Bericht | PASS | buildNotesFromDiagnose liefert 2-4 deutsche Saetze + MAX_NOTES_CHARS=1500 (R-106-7). |
| AC-12 | ENV-Variablen Coolify-Secrets | DEFERRED → MT-11 | User-Pflicht. |
| AC-13 | Idempotenz | PASS | Pre-Check + 23505 + UNIQUE-Index als Defense-in-Depth. |
| AC-14 | Migration 092 idempotent | PASS | MT-2 LIVE-Apply mit Idempotenz-Re-Run in RPT-243 verifiziert. |
| AC-15 | Pen-Test-Suite SLC-101 gruen + 12 neue Faelle | PARTIAL | SLC-101 100/100 PASS; 2 todo placeholder fuer MT-9 (10 zusaetzliche Faelle sind MT-9-Scope). |
| AC-16 | ESLint 0/0 + Build PASS + Vitest gruen + 0 neue Vulns | PASS | Alle 6 Gates. |

**12 von 16 ACs in MT-1..MT-5 Scope erfuellt. 4 ACs sind ausserhalb dieses Bundles
(MT-6/MT-7/MT-11/MT-12) und korrekt deferred.**

## Wiring-Verification

Die Server-Action `requestLeadPush` ist heute **nicht von einer Page importiert**
(grep auf `requestLeadPush` zeigt nur die Definition + Tests + Type-Import). Das
ist erwartet und kein Stub — die Wiring-Chain wird in MT-7 ("Ich will mehr"-Modal
auf SLC-105 Bericht-Page) komplettiert:

```
IchWillMehrModal (MT-7) -> requestLeadPush (MT-5, vorhanden) -> 
  pushLeadToBusinessSystem (MT-3, vorhanden) -> Business-System /api/leads/intake -> 
    lead_push_audit UPDATE (MT-5, vorhanden) -> 
    ai_jobs lead_push_retry (MT-5, vorhanden) -> 
    Worker handleLeadPushRetryJob (MT-6, fehlt noch)
```

Backend-Schicht steht. Naechste Schicht (Worker) = MT-6, danach UI = MT-7,
abschliessend Live-Smoke = MT-11..MT-13.

## Findings

### Blocker
keine.

### High
keine.

### Medium
keine.

### Low
keine NEU. Drei `Low` aus RPT-244 bleiben dokumentiert und V6.1-Polish-Kandidat:
- **P-1 (RPT-244)** Schema-Drift Slice-Spec vs. tatsaechliche `ai_jobs`-Tabelle: 
  Slice-Spec sprach von `scheduled_at` als Top-Level-Spalte, das Schema (Migration 031) 
  hat nur `payload jsonb`. Implementation routet `scheduled_at` durch 
  `payload.scheduled_at`. **Pflicht-Note fuer MT-6**: Worker-Pull muss 
  `WHERE (payload->>'scheduled_at')::timestamptz <= now()` als Filter benutzen, 
  sonst werden Retry-Jobs sofort gezogen. (Bleibt von /qa nicht als Issue erfasst —
  ist eine Spec-Note, kein App-Code-Defekt.)
- **ISSUE-051** profiles fehlen first_name/last_name fuer Lead-Payload (V6.1 polish, 
  bereits dokumentiert 2026-05-14 commit 0ed3356).
- **P-3 (RPT-244)** loadDiagnoseReportSummary best-effort fuer V6 (template-schema-
  abhaengige Reifegrad-Aggregation; bei null-Feldern faellt der Notes-Text auf zwei
  Saetze zurueck — funktional ok, V6.1-Polish-Kandidat).

## Open risks / missing checks

- **MT-6 Worker-Job-Handler** noch offen — AC-7..9 Retry-Pfad ist nur code-side
  enqueue-fertig. Live-Retry-Verhalten kommt mit MT-6 + MT-13.
- **MT-7 "Ich will mehr"-Modal** offen — ohne dieses Modal ist die Server-Action
  unbenutzt im Build. Wiring schliesst erst mit MT-7.
- **MT-9 Pen-Test-Aktivierung** auf `lead_push_consent` + `lead_push_audit`
  noch offen (2 `it.todo` in `v6-partner-rls.test.ts`, AC-15 PARTIAL).
- **MT-11..MT-13 User-Pflicht-Smokes** (ENV-Setup + Cross-System-Live + Retry-Live)
  bleiben User-Pflicht nach Coolify-Redeploy am Slice-Ende.
- **Pre-existing Audit-Vulns** (next/next-intl/postcss) bleiben Pre-Production-
  Compliance-Gate-Scope, nicht V6-Slice-Block (Internal-Test-Mode konsistent).

## Readiness assessment

**MT-1..MT-5 code-side ready fuer MT-6.** Foundation-Schicht (Migration + Adapter +
Helper + Server-Action) ist quality-gates-validiert, hat 18 dedizierte Unit/Vitest
und sitzt regression-frei zwischen 87 Test-Files mit 981 PASS. Keine Blocker,
keine offenen Code-Probleme — die deferred ACs (MT-6/7/11/12) sind im Slice-Plan
explizit als naechste MTs ausgewiesen, nicht versteckt.

## Recommended next step

**`/backend SLC-106 MT-6`** — Worker-Job-Handler `lead_push_retry` (~1.5-2h).

Pflicht-Notes fuer MT-6:
1. **payload.scheduled_at-Filter** beim Worker-Pull (P-1 aus RPT-244): 
   `WHERE (payload->>'scheduled_at')::timestamptz <= now()` zusaetzlich zu 
   status='pending' und job_type='lead_push_retry'.
2. **Backoff-Schedule**: Attempt 2 mit 5min nach Initial-Fail (MT-5 setzt das schon),
   Attempt 3 mit weiteren 30min nach Attempt-2-Fail.
3. **Cap bei 3 Versuchen**: bei Attempt > 3 sofort `markAuditFailed` + error_log
   `category='lead_push_failure'` ohne neuen ai_jobs-Insert (AC-9, DEC-112).
4. **Worker-Registry**: bestehendes `src/workers/walkthrough/`-Pattern als Vorlage,
   neuer Job-Type-Dispatcher in `claim-loop.ts` (siehe `src/workers/condensation/claim-loop.ts`
   als kanonisches Pattern).
5. **5 Vitest** verlangt vom Slice (Happy / Retry-2 / Retry-3 / Max-Attempts /
   Backoff-Schedule-Verify).

Nach MT-6 ist `/qa SLC-106 MT-6` mandatorisch (Stub-Detection auf Worker-Files +
Backoff-Schedule-Verifikation).

## Project record updates

- `/reports/RPT-245.md` — NEU (dieser Report).
- `/slices/INDEX.md` — SLC-106 bleibt `in_progress` (5/13 MTs done).
- `/planning/backlog.json` — BL-093 V6 LEAD_PUSH bleibt `in_progress`.
- `/docs/STATE.md` — Current Focus auf MT-1..MT-5 /qa PASS aktualisiert.
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues. ISSUE-051 (P-2 aus RPT-244) ist
  bereits dokumentiert.
