---
id: RPT-020
date: "2026-04-17"
skill: /qa
slice: SLC-004
feature: FEAT-003
type: completion
status: completed
reviewOf: RPT-019
result: "QA PASS — No blockers, no high findings. Wiring verified, stubs clean, unit tests green."
title: "SLC-004 QA — Capture-Session-Start + Block-Listing"
---

## QA Scope

Full QA of SLC-004 (Capture-Session-Start + Block-Listing) against spec acceptance criteria, including:
- Acceptance criteria verification (4 criteria)
- Stub detection scan (all SLC-004 files)
- Wiring verification (full frontend-to-DB chain)
- Code quality review (auth, security, error handling)
- Unit test verification

## Result: PASS

### Acceptance Criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| AC-1 | tenant_admin clicks "Session starten" → redirect to /capture/[sessionId] | PASS (code) | Server Action does INSERT + redirect. Browser verification requires Redeploy. |
| AC-2 | List shows all N blocks of the template | PASS (code) | BlockList renders `template.blocks` sorted by `order`. 9 blocks from exit_readiness template. |
| AC-3 | Initial status of all blocks = "offen" | PASS (code+test) | `deriveBlockStatus([]) === "open"` tested. No checkpoints on fresh session = all blocks open. |
| AC-4 | Cross-tenant access blocked (2-tenant test) | PASS (code+test) | RLS policies + defense-in-depth `tenant_id !== profile.tenant_id` check. Test written (requires Hetzner DB). |

### Findings

No blockers, no high findings, no medium findings.

#### Low

- **L-1: No navigation link from dashboard to /capture/new**
  Dashboard still shows Blueprint-era "runs" list. Users need to know the URL `/capture/new` manually. Acceptable for V1 development phase — a navigation update should come when more capture functionality is ready.

- **L-2: Status labels hardcoded in German**
  `STATUS_CONFIG` in block-list.tsx uses German labels (Offen/Eingereicht/Ausgewertet/Finalisiert). Multi-language support deferred — consistent with existing i18n approach in the project.

- **L-3: Browser verification pending**
  Full acceptance criteria require a Redeploy to verify in browser. Build passes, wiring is verified by code review, but actual browser rendering is not yet confirmed.

### Stub Detection

| File | Clean |
|------|-------|
| `src/app/capture/actions.ts` | Yes — no TODOs, no stubs, proper error returns |
| `src/app/capture/new/page.tsx` | Yes — standard auth guard pattern |
| `src/app/capture/new/start-session-client.tsx` | Yes — loading state, error state, empty state all handled |
| `src/app/capture/[sessionId]/page.tsx` | Yes — auth + tenant check + notFound() for missing data |
| `src/app/capture/[sessionId]/block-list.tsx` | Yes — no stubs, locale-aware title rendering |
| `src/lib/capture/derive-block-status.ts` | Yes — pure function, no side effects |
| `src/lib/capture/derive-block-status.test.ts` | N/A (test file) |
| `src/app/capture/__tests__/cross-tenant-access.test.ts` | N/A (test file) |

### Wiring Verification

Full chain verified — see table above. All links connected:
- StartSessionClient → Server Action → DB INSERT → redirect
- Session Page → getCaptureSession → getTemplateById → block_checkpoint query → BlockList render
- Cross-tenant isolation: RLS + application-level defense-in-depth

No wiring gaps found.

### Code Quality

- **Auth pattern**: Consistent with existing pages (dashboard, runs). Auth guard in both server components.
- **Security**: Defense-in-depth tenant check alongside RLS. Role check in server action.
- **Error handling**: Server action returns error objects. Client component renders error state. Empty template state handled.
- **Type safety**: Zod schemas validate DB responses. TypeScript throughout.
- **Test coverage**: 6 unit tests for deriveBlockStatus (all transitions). 2 RLS isolation tests (Hetzner-dependent).

### Readiness Assessment

SLC-004 is **ready for Redeploy + browser verification**. Code quality is solid, wiring is complete, no stubs found. The only gap is browser-level verification, which requires Redeploy.

### Recommended Next Step

1. **Redeploy** on Hetzner (Coolify manual deploy)
2. **Browser smoke-test**: Login as tenant_admin → navigate to /capture/new → start session → verify block list
3. Then: SLC-005 Questionnaire-UI-Portierung
