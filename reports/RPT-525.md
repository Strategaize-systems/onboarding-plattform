---
id: RPT-525
date: "2026-06-22"
skill: /frontend
slice: SLC-175
feature: FEAT-095
type: completion
status: completed
reviewOf: null
result: "SLC-175 MT-2 /frontend done — presentational layer (ModuleOutputCard, KiHebelList, PrintButton, WorkspaceOverview, ModulWorkspaceView), CSS print-view (window.print, Founder-decided over React-PDF), full stb.* i18n de/en/nl, server pages refactored to delegate. tsc 0, eslint 0, 11 new render tests GREEN, next build EXIT 0 (placeholder env). 0 new failures in offline suite. Browser-smoke (AC-175-2) is a /qa concern (R-175-2: empty without a synthesis run). Slice stays in_progress until MT-2 /qa."
title: "SLC-175 MT-2 /frontend — Triple-Render + KI-Hebel-Liste + CSS-Print + i18n"
---

# SLC-175 MT-2 /frontend — Triple-Render + KI-Hebel-Liste + CSS-Print + i18n

## 1. Outcome summary
MT-2 builds the presentational layer on top of MT-1's read-layer/route-foundation: extracted reusable components, full `stb.*` i18n (de/en/nl), and a CSS print-view. **Fully complete (code-side)**; slice stays `in_progress` until MT-2 `/qa` (which adds AC-matrix re-verify + the browser-smoke decision). No migration, no backend change.

Two scope-affecting choices were settled with the Founder via AskUserQuestion before any code:
- **AC-175-2 "druckbar" → CSS print-view** (`window.print()` + Tailwind `print:` utilities, the FEAT-028 Handbuch-Reader approach) **instead of React-PDF (FEAT-086)** — same DOM, no divergent PDF document/route to maintain, faithful to the on-screen reader. AC-175-2 hedges "wo sinnvoll druckbar", and the React-PDF FEAT-086 path has no UI trigger today (admin direct-URL only).
- **Full `stb.*` i18n de/en/nl** (slice-literal) even though the rest of the StB vertical (SLC-171/173) and all PDF renderers are hardcoded German.

## 2. Files reviewed
- `/slices/SLC-175-stb-modul-workspace-reader.md` — MT-2 spec / ACs.
- `/src/lib/stb-vertikale/workspace-read.ts` — data shape consumed (MT-1, unchanged).
- `/src/app/dashboard/stb/workspace/page.tsx` + `/src/app/dashboard/stb/workspace/[modulKey]/page.tsx` + `/src/app/dashboard/stb/workspace/loading.tsx` — MT-1 routes (presentation extracted from the first two).
- `/src/app/dashboard/stb/layout.tsx` — env-gate (StbLayout, unchanged).
- `/src/components/handbook/HandbookReader.tsx` (FEAT-028) — reuse vibe for reader/print (CSS print modifiers).
- `/src/lib/pdf/fahrplan-report/renderer.tsx` + `/src/app/admin/debrief/[sessionId]/fahrplan-report/route.ts` (FEAT-086) — evaluated as the React-PDF reuse path (declined per Founder decision).
- `/src/app/dashboard/dashboard-client.tsx` — `useTranslations`/`useLocale` client pattern.
- `/src/app/layout.tsx` — confirms `NextIntlClientProvider` wraps the app (client `useTranslations` works without a locale prop).
- `/src/messages/{de,en,nl}.json` — message catalogs.
- `/src/app/.../bericht/__tests__/V8OutroSection.test.tsx` — render-test pattern (`renderToString`, avoid @testing-library).

## 3. Files changed
- `/src/app/dashboard/stb/workspace/page.tsx` — overview page now keeps auth + RLS-scoped read (`readWorkspaceOutputs`/`summarizeModulOutputs`) and delegates rendering to `WorkspaceOverview` (client). Hardcoded German + inline list removed.
- `/src/app/dashboard/stb/workspace/[modulKey]/page.tsx` — detail page keeps auth + RLS read + best-effort template heading, delegates rendering to `ModulWorkspaceView` (client). Inline triple/KI-Hebel markup removed.
- `/src/messages/de.json` — added `stb.workspace` namespace (titles, intro, states, kind labels, KI-Hebel, reifegrad, print).
- `/src/messages/en.json` — same namespace, English.
- `/src/messages/nl.json` — same namespace, Dutch.

## 4. Files created
- `/src/components/stb/ModuleOutputCard.tsx` — presentational card for one Liefer-Triple section (Entscheidung/Standard/Implementierungsschritt); print-friendly; i18n kind label + "no entry".
- `/src/components/stb/KiHebelList.tsx` — KI-Hebel list with maturity-level badge (Reifegrad 1-4, "ohne" last); print-friendly; empty state.
- `/src/components/stb/PrintButton.tsx` — `window.print()` trigger, hidden in print (`print:hidden`).
- `/src/components/stb/WorkspaceOverview.tsx` — overview-view client component (module cards, counts, last-generated date via locale, empty/error states).
- `/src/components/stb/ModulWorkspaceView.tsx` — detail-view client component (heading, DATEV-Abgrenzung intro, triple cards, KI-Hebel, print button, empty/error states).
- `/src/components/stb/__tests__/workspace-components.test.tsx` — 11 hermetic render tests (renderToString + next-intl mock): card content/empty, KI-Hebel reifegrad/none/empty, detail states (content/error/empty), overview states (list/empty/error).

## 5. Files intentionally not changed
- `/src/lib/stb-vertikale/workspace-read.ts` — data/grouping layer is correct from MT-1; MT-2 is presentation-only. The embedded German `label` on TripleSection is left as a harmless fallback; the UI translates by `kind`.
- `/src/app/dashboard/stb/workspace/loading.tsx` — existing skeleton is adequate (AC-175-4 loading); no i18n strings.
- `/src/app/dashboard/stb/page.tsx` — MT-1 landing link unchanged.
- `/src/lib/pdf/**` — React-PDF infra deliberately not used (Founder chose CSS print).
- `/src/app/globals.css` — no new global print rules needed; component-level Tailwind `print:` utilities suffice (matches the codebase's stated approach).

## 6. Problems found
- **Offline full vitest suite shows 607 failing tests**, but **none are caused by MT-2**: all are either `TEST_DATABASE_URL is not set` DB-integration tests (no Coolify DB reachable offline) or the pre-documented L-1 offline-only failures in `diagnose`/`walkthrough`/`cost` (areas MT-2 does not touch). Verified by filtering failure reasons — the only non-DB failures are `__REDIRECT__` assertions in the diagnose flow. The MT-2-relevant hermetic tests are 11/11 GREEN.
- **`next build` needs build-time Supabase env** for `/api/public/partner/[slug]` (module-level client init) — pre-existing (flagged in SLC-173). With placeholder envs the build is EXIT 0 and both `dashboard/stb/workspace` routes are dynamic (ƒ). Not introduced by MT-2.
- **i18n inconsistency (accepted, Founder-decided):** the reader is now the only translated part of the StB vertical; siblings stay hardcoded German. Documented as a conscious choice, not a defect.

## 7. Open points or deferred decisions
- **Browser-smoke (AC-175-2)** is a `/qa` step and is data-blocked (R-175-2): without a real synthesis run on the Founder's tenant the reader only shows the empty-state. Decision needed (also raised to Founder): trigger a real run vs. accept empty-state browser-smoke + hermetic coverage.
- **AC-175-6 (status-edit `proposed`→`accepted`/`edited`)** remains deferred to V10.x per slice R-175-3 (render-only in V10).
- **Carry ISSUE-106** (re-synthesis force/delete path) unchanged — out of reader scope.

## 8. Project record updates
- `/docs/STATE.md` — Current Focus + Current Phase updated to MT-2 /frontend done, /qa next.
- `/slices/INDEX.md`, `/features/INDEX.md`, `/planning/backlog.json` — SLC-175 / FEAT-095 / BL-514 remain `in_progress` (correct: slice is `done` only after MT-2 /qa-PASS). No status change this step.
- `/planning/roadmap.json` — V10 already `active`; unchanged.

## 9. QA after implementation
MANDATORY next action: `/qa SLC-175 MT-2`. Gates already run this step (tsc 0, eslint 0, 11 new tests GREEN, build EXIT 0) feed into /qa; /qa adds AC-matrix re-verify + the browser-smoke decision.

## 10. Recommended next step
`/qa SLC-175 MT-2`. After /qa-PASS, set SLC-175/FEAT-095/BL-514 → done. SLC-175 is not the last V10 slice (SLC-170b + SLC-172 remain), so V10 Gesamt-QA/`/final-check` are not yet due.
