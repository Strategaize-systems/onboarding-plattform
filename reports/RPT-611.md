---
id: RPT-611
date: "2026-07-08"
skill: /qa
slice: SLC-190
feature: FEAT-107
type: completion
status: completed
reviewOf: RPT-610
result: "SLC-190 /qa PASS-CODE / PENDING-LIVE — 4/4 MT code-verifiziert, +29 Pure-Mock-Vitest gruen (0 Regression), Wiring end-to-end vollstaendig, keine Stubs, 0 Blocker/High/Medium. Live-Berater/Admin-E2E + DB-Sidecar PENDING-LIVE (MIG-132-Apply erst im /deploy)."
title: "QA SLC-190 — Berater-Sicht: /admin gescopt + Mein-Tag allowedTenantIds-Scope"
---

# QA — SLC-190 Berater-Sicht (FEAT-107, V10.4 P2, letzter Slice)

## 1. QA scope reviewed
Code-Stand `729e326` (Branch `v10-4-rollenmodell-p2`, Worktree). Verifiziert: die 4 Micro-Tasks aus
SLC-190 gegen DEC-269/270 + SC-V10.4-2/3/5. Betrachtet: Query-Layer-Scope (tenant-scope.ts + 5 Loader),
Routing/Layout/Sidebar (middleware, admin/layout, berater-sidebar), Mein-Tag-Entry-Points
(page/actions/rag-action via resolveWorkspaceScope), Tests + Wiring + Stub-Scan.

## 2. Aggregat-Verdikt: **PASS-CODE / PENDING-LIVE** (Mixed → Slice-Status `code-done`, nicht `done`)

Kein Live-Teil ist ohne MIG-132 (RPC `berater_assigned_tenant_ids`) testbar → höchstens PASS-CODE.
Kein Blocker / High / Medium. 3 Low-Beobachtungen (reviewed & accepted).

### Per-AC-Verdikte

| AC | Inhalt | Verdikt | Evidenz |
|---|---|---|---|
| MT-1 | Loader-Scoping via `allowedTenantIds` (5 Loader + loadReport) | **PASS-CODE / PENDING-LIVE** | `loader-tenant-scope.test.ts` (5): `.in`-Injektion je Loader auf korrekter Spalte (`tenant_id`, tenants=`id`) + „ohne Filter keine .in"; `tenant-scope.test.ts` (3): undefined=kein Filter, []→`.in(col,[])`, [ids]→exakt. Bestands-Loader-Tests (5) grün. Live-Datenfilter (echte Rows) = DB-Sidecar PENDING-LIVE. |
| MT-2 | middleware Login-Redirect + Layout-Gate + BeraterSidebar | **PASS-CODE / PENDING-LIVE** | middleware-Branch + layout-Branch (committed, gelesen) + `next build` PASS; role-check.ts-Mirror (defaultLandingForRole→/admin/mein-tag) IST getestet (SLC-188); 24 /admin-Subpage-Gates per Grep = strategaize_admin-only (Berater redirected, kein Layout-Leak). Browser-Login-E2E = PENDING-LIVE. |
| MT-3 | Mein-Tag Berater-Pfad (page/actions/rag scoped) | **PASS-CODE / PENDING-LIVE** | `actions.test.ts` (7): Berater system_status→invalid_key (loadReport NICHT aufgerufen), erlaubter Report→loadReport mit allowedTenantIds, Admin→undefined; `rag-action.test.ts` (7): fremder Tenant→no_tenant VOR DB-Lookup, zugewiesener→RAG läuft, Admin→ok. Live-Selector-Scoping = PENDING-LIVE. |
| MT-4 | RLS-/Scoping-Regression + DB-Sidecar | **PASS-CODE (Mock) / STATIC-ONLY (DB, PENDING-LIVE)** | Pure-Mock-Loader-Scope PASS. `berater-visibility.test.ts` geschrieben, läuft lokal NICHT (kein TEST_DATABASE_URL) → 0 gelaufen, als PENDING-LIVE ausgewiesen (nicht als PASS gezählt); läuft im /deploy via node:22-Sidecar. |
| SC-V10.4-5 | Admin 0 Regression | **PASS-CODE / PENDING-LIVE** | `allowedTenantIds===undefined` = exaktes Alt-Verhalten (loader-scope „ohne Filter keine .in"); 5 Bestands-Loader-Tests grün; Full-Suite-Baseline (git stash): 0 neue Logic-Failures. |

## 3. Findings by severity

**Blocker / High / Medium:** keine.

**Low (reviewed & accepted):**
- **L-1 — Kein dedizierter middleware.ts-Unit-Test** für den Berater-Redirect. Abgedeckt durch den
  getesteten role-check.ts-Mirror (`defaultLandingForRole`) + `next build` + Live-Smoke im /deploy.
  Slice-Spec MT-2 sagte „middleware.test.ts (MODIFY/NEU falls vorhanden)" — es existiert keine.
  Akzeptiert; Nachhol = /deploy-Browser-Smoke.
- **L-2 — Berater per Direkt-URL auf strategaize_admin-only /admin-Subpage:** Layout rendert kurz die
  BeraterSidebar-Chrome, die Page re-gated selbst (`role !== strategaize_admin`) → redirect `/dashboard`
  (Berater ohne tenant → leere Dashboard-Sicht). **Kein cross-tenant-Leak** (Page-Gate greift, fail-safe);
  über die Sidebar nicht erreichbar (nur `/admin/mein-tag` verlinkt). Reine UX-Kante, akzeptiert.
- **L-3 — page.tsx nutzt `createAdminClient` auch für den Admin-Tenant-List-Read** (vorher user-client/RLS).
  Output identisch (alle Tenants id+name), Page ist via resolveWorkspaceScope gegated. Keine Regression,
  reiner Pattern-Hinweis.

## 4. Stub-Scan / Wiring / Silent-Skip

- **Stub-Scan:** 0 Treffer (TODO/FIXME/HACK/leere Handler/console/hardcoded/return-null-Stub) über alle
  geänderten Impl-Files.
- **Wiring-Chain-Audit (Ende-zu-Ende):**
  - `resolveWorkspaceScope` — 3 Production-Caller (page, actions×2, rag-action×2). ✓
  - `scopeTenants` — alle 5 Loader (4 reports + load-cross-tenant). ✓
  - `reportKeys` — Fluss page → WorkspaceShell → ReportButtons (+ Filter-Logik). ✓
  - `BERATER_REPORT_KEYS` — Source index.ts → actions.ts (Whitelist) + page.tsx (Prop). ✓
  - `loadBeraterAssignedTenants` → layout.tsx; `BeraterSidebar` gerendert für Berater. ✓
  - Navigations-/Reachability: BeraterSidebar → `/admin/mein-tag` (existiert); middleware-Redirect-Ziel existiert. ✓
  - Kein Rest-`assertStrategaizeAdmin` in mein-tag; `admin-gate.ts` korrekt retained (berater/actions + transcribe-route). ✓
- **Silent-Skip-Audit:** alle 6 neuen Test-Files im `vitest.config` include-Pattern (`src/**/*.test.ts`);
  keine `.skip`/`skipIf` in den Pure-Mock-Tests → **39 Assertions gelaufen, 39 grün**. DB-Sidecar
  explizit PENDING-LIVE (TEST_DATABASE_URL-gated, nicht als PASS gezählt).

## 5. Test execution
- **Targeted Pure-Mock:** 10 Files / **39 Tests PASS** (inkl. 5 Bestands-Loader-Tests = 0 Regression + 29 neue).
- **tsc** 0 · **eslint** 0 · **`next build`** EXIT 0 (`'use server'`-Gate ok).
- **Full-Suite-Delta (git stash Baseline):** passed 2209→2238 (+29), non-DB-Fail-Signaturen identisch → 0 neue Logic-Failures.
- **DB-gated (605 Baseline-Fails):** environmental (kein lokaler Postgres), etablierte PENDING-LIVE-Baseline.

## 6. Open risks / missing checks (PENDING-LIVE — Nachhol im /deploy)
1. MIG-132-Live-Apply (Pre-Apply-Live-Audit, MIG-131-Muster) — bis dahin liefert der Berater-Pfad live leere Tenants.
2. DB-Sidecar `berater-visibility.test.ts` (node:22-Sidecar): Filter liefert Kanzlei∪Cascade, Fremd=0, Admin=alle.
3. Browser-E2E: Berater-Login → landet /admin/mein-tag → BeraterSidebar + nur zugewiesene Tenants im Selector;
   system_status-Button fehlt; RAG fremder Tenant → Fehler; Admin-Login unverändert. 0 Console-Errors.

### 2-Minuten-Live-Smoke-Checkliste (für /deploy, nach MIG-132-Apply)
1. Berater-User (role=strategaize_berater, 1 Kanzlei zugewiesen) einloggen → landet auf `/admin/mein-tag`, BeraterSidebar sichtbar (Mein Tag + zugewiesene Mandanten), KEINE Tenants/Partner/Funnel-NAV.
2. TenantSelector zeigt NUR zugewiesene Tenants (+ Cascade-Mandanten), nicht den Fremd-Tenant.
3. Report-Buttons: 4 Buttons, KEIN „System-Status". „Mandanten-Übersicht" laden → nur zugewiesene Zeilen.
4. RAG-Frage mit zugewiesenem Tenant → Antwort; (DevTools) fremder tenantId → `no_tenant`.
5. Direkt `/admin/tenants` aufrufen → redirect `/dashboard` (kein Cockpit-Leak).
6. Admin-Login unverändert: alle Tenants, 5 Report-Buttons inkl. System-Status. 0 Console-Errors.

## 7. Readiness
Code-seitig release-ready für den V10.4-Scope; keine offenen Code-Defekte. Der Slice ist `code-done`
(Mixed-Verdict), NICHT `done` — Live-Verifikation + MIG-132-Apply stehen strukturell im /deploy aus
(module-lifecycle: interne Workflow-Closure, kein Customer-Live).

## Next workflow step
SLC-190 ist der **letzte V10.4-Slice**. Nächster Schritt: **`/qa` Gesamt-QA über V10.4** (SLC-188 + SLC-189 + SLC-190,
Cross-Slice-Contracts: RPC/Gate/Assignments ↔ Loader-Scope) — Pflicht VOR `/final-check`. Danach
`/final-check` → `/go-live` → `/deploy` (Pre-Merge-Rebase gegen main ZUERST, dann MIG-132-Apply + DB-Sidecar + E2E-Smoke) → `/post-launch`.
