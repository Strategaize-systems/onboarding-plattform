---
id: RPT-533
date: "2026-06-24"
skill: /backend
slice: SLC-172
feature: FEAT-092
type: completion
status: completed
result: "SLC-172 MT-2 DONE — rpc_seed_blueprint_diagnosis_input (MIG-127, atomar/idempotent) + tenant-scoped triggerBlueprintDiagnosis. DEC-250 loest die offene Checkpoint-Frage (7 Pseudo-Checkpoints A–G). Gates tsc 0 / eslint 0 / vitest 22/22. DB-Sidecar PASS (transaktional, ROLLBACK, 0 Persistenz): 7 CP / 13 KU / A–G-aligned / Antwort-reconciled / Idempotenz 7/13 / Tenant-Scope."
title: "Backend SLC-172 MT-2 — Blueprint-Diagnose KU-Seed-RPC + Trigger"
---

# /backend — SLC-172 MT-2 (Blueprint-Diagnose KU-Seed-RPC + tenant-scoped Trigger)

**Outcome: fully complete (Code-Side + DB-verifiziert).** MIG-127 noch NICHT live-applied (Founder/Coolify-Hold → /deploy).

## 1. Outcome summary
- `rpc_seed_blueprint_diagnosis_input(p_session_id)` (MIG-127, PL/pgSQL, SECURITY DEFINER, atomar/idempotent) seedet aus den Blueprint-Capture-Antworten je Diagnose-Block A–G die Diagnose-Inputs.
- `triggerBlueprintDiagnosis(sessionId)` (Server-Action) = tenant-scoped, tier-gated Klon von `triggerDiagnosisGeneration`: Seed-RPC → 7 `diagnosis_generation`-Jobs; der bestehende Worker `handle-diagnosis-job` bleibt UNVERAENDERT.
- **Offene /architecture-Frage gelöst (DEC-250):** `block_diagnosis` verlangt keinen block_key-gleichen Checkpoint (MIG-050), aber `knowledge_unit.block_checkpoint_id` ist NOT NULL → der Seed legt **pro A–G einen Pseudo-`block_checkpoint`** (`checkpoint_type='blueprint_diagnosis_seed'`) an. Invariante `checkpoint.block_key==KU.block_key==diagnosis.block_key` bleibt erhalten (Precedent: MIG-094 `auto_final`, MIG-110 `email_bulk_import`).

## 2. Files reviewed
- `/sql/migrations/050_block_diagnosis.sql`, `/sql/migrations/052_rpc_diagnosis.sql` (Checkpoint-FK-Frage)
- `/sql/migrations/021_core_capture_schema.sql`, `/sql/migrations/030_capture_session_answers_column.sql`, `/sql/migrations/040_orchestrator_extensions.sql`, `/sql/migrations/091_*`, `/sql/migrations/094_*`, `/sql/migrations/110_*` (checkpoint_type-CHECK-Historie, KU-Write-Precedent)
- `/sql/migrations/126_v10_stb_blueprint_seed.sql` (diagnosis_schema A–G / Capture-Block-Mapping / frage_id↔uuid)
- `/sql/migrations/031_ai_jobs_table.sql`, `/sql/migrations/121_v975_tier_gating_foundation.sql` (ai_jobs / session_tier)
- `/src/workers/diagnosis/handle-diagnosis-job.ts`, `/src/workers/diagnosis/diagnosis-prompt.ts` (Worker-Reuse, KU-Konsum)
- `/src/app/admin/debrief/[sessionId]/[blockKey]/diagnosis-actions.ts` (Trigger-Vorbild), `/src/lib/auth/assert-session-tier.ts`
- `/src/lib/stb-vertikale/blueprint.ts`, `/src/app/dashboard/stb/blueprint/actions.ts` (MT-1 — Answer-Key-Shape `${blockKey}.${uuid}`)
- `/src/app/admin/debrief/[sessionId]/roster-actions.test.ts` (Mock-Pattern), `/reports/RPT-532.md`

## 3. Files changed
- `/docs/DECISIONS.md` — DEC-250 ergänzt (Checkpoint-Modell, Auflösung der DEC-249/MIG-127-Offenfrage).
- `/docs/MIGRATIONS.md` — MIG-127 von RESERVIERT → geschrieben + DB-verifiziert; Scope/Affected/Risk auf Pseudo-Checkpoint + CHECK-Erweiterung präzisiert.
- `/docs/STATE.md` — Current Focus + Current Phase auf MT-2 /backend DONE + NEXT /qa MT-2.

## 4. Files created
- `/sql/migrations/127_v10_slc172_blueprint_diagnosis_seed_rpc.sql` — CHECK-Erweiterung (`blueprint_diagnosis_seed`) + RPC `rpc_seed_blueprint_diagnosis_input`.
- `/src/lib/stb-vertikale/blueprint-diagnosis.ts` — `triggerBlueprintDiagnosis` (tenant-scoped Seed+Enqueue).
- `/src/lib/stb-vertikale/__tests__/blueprint-diagnosis.test.ts` — 7 hermetische Action-Tests (Auth/Tenant/Tier-Gate/Seed-Roundtrip/Enqueue-Idempotenz/7-Job-Shape/Fehlerpfade).
- `/reports/RPT-533.md` — dieser Report.

## 5. Files intentionally not changed
- `/src/workers/diagnosis/handle-diagnosis-job.ts` — Reuse 1:1 (DEC-244/249); der Worker liest die geseedeten KUs unverändert.
- `/slices/INDEX.md`, `/features/INDEX.md`, `/planning/backlog.json`, `/planning/roadmap.json` — SLC-172/FEAT-092/BL-511 bleiben `in_progress` (MT-3 offen), V10 `active`. Korrekt, kein Status-Wechsel.

## 6. Problems found
- **Keine Blocker.** Verifikations-Limit (kein Defekt): voller End-to-End-Diagnose-Output (AC-172-2 `block_diagnosis`-Rows mit Ampel/Reifegrad) ist erst nach echtem Worker-Lauf sichtbar — der Worker läuft nicht ohne V10-Deploy (Founder/Coolify-Hold). Code-Pfad Seed→Jobs→Worker→`rpc_create_diagnosis` ist type-/schema-/DB-verifiziert; der LLM-Diagnose-Output selbst gehört zur Deploy-Validation (analog SLC-175 AC-2-Defer).
- MIG-127 ist DB-seitig NUR transaktional (ROLLBACK) getestet, nicht permanent appliziert — bewusst, respektiert den OP-Hold. Permanenter Live-Apply im /deploy.

## 7. Open points / deferred decisions
- MT-3 (Reader `SubtopicDiagnosisCard` + deterministisches Modul-Routing aus `metadata.routing[]`) — nächster Slice-Schritt nach /qa MT-2.
- Live-Apply MIG-127 + voller Worker-Diagnose-Smoke → V10-Deploy-Validation.

## 8. Project record updates
DEC-250 (neu), MIG-127 (Status+Scope), STATE.md (Focus/Phase). Slices/Features/Backlog/Roadmap unverändert (MT-3 offen) — bewusst.

## 9. Verification
- `npx tsc --noEmit` → EXIT 0. `npx eslint` (2 neue Files) → 0. `npx vitest run blueprint-diagnosis.test.ts blueprint.test.ts` → **22/22 GREEN** (7 neu + 15 MT-1-Regress).
- **DB-Sidecar (AC-172-5)** transaktional gegen Live-Coolify-DB `supabase-db-bwkg80w04...160450719820` (BEGIN; reale MIG-127 `\i`; Test-Session; RPC; Asserts; ROLLBACK): **PASS-1** 7 Checkpoints / 13 KUs / KU.block_key==Checkpoint.block_key / 7 distinct A–G / reale F-BP-001-Antwort im Block-A-KU-Body; **PASS-2** Idempotenz-Re-Run stabil 7/13; **PASS-3** Tenant-Scope auf allen Rows. Post-Check: Function 0 / Test-Session 0 persistiert → 0 permanenter Eingriff (Hold gewahrt).

## 10. Recommended next step
**`/qa SLC-172 MT-2`** (AC-172-2 + AC-172-5, Code-Side + DB-Sidecar-Bestätigung; voller Diagnose-Output-Smoke → V10-Deploy). Danach `/backend SLC-172 MT-3` (Reader + Routing).
