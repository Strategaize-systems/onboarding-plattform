---
id: RPT-619
date: "2026-07-09"
skill: /deploy
type: completion
status: completed
result: "ISSUE-119-Hotfix deployed (REL-042, SOURCE_COMMIT da5fb7c). Delivery-Chain + Live-Smoke PASS: /capture/new rendert wieder, kein ZodError. ISSUE-119 resolved."
title: "Deploy REL-042 — ISSUE-119 Hotfix (/capture/new resilient)"
---

# RPT-619 — Deploy REL-042: ISSUE-119 Hotfix

## Was deployed wurde
Code-only-Hotfix (RPT-618): `listTemplates()` in `src/lib/db/template-queries.ts` macht per-Row `safeParse` statt Throw-on-first → `/capture/new` crasht nicht mehr an 2 Legacy-`template`-Rows. Kein Migration, keine ENV-Aenderung, keine neue Runtime-Dep. Auf V10.4-Basis.

## Phase 1 — Prerequisites / Pre-Deploy-Server-Precheck: PASS
- Host `coolify-ubuntu-4gb-nbg1-1` @ 159.69.207.29 (erwartet). SSH ok (Agent-autonom).
- Disk 17% (359G frei), Mem 20 GiB available (Swap 0, aber Headroom >> 4 GiB). Kein Block.
- Container app/worker/kong/db `(healthy)` seit 23h, RestartCount ok.
- Gate 1 (Migration): N/A. Gate 2 (ENV): N/A. Keine offenen First-Deploy-Prereqs (laufender Stack).
- Delivery-Chain-pre (Gate 3): lokal HEAD == origin/main == `da5fb7c`, keine unpushten Commits. Live vorher `8329b23`.

## Phase 2 — Execution
- **Deploy-Trigger:** OP hat keinen Auto-Deploy-Webhook → Coolify-API-Redeploy autonom (Playbook `coolify-autonomous-redeploy.md`): temp Sanctum-Token (name `temp-agent-deploy-hotfix-issue119`, id 5), `POST /api/v1/deploy?uuid=bwkg80w04wgccos48gcws8cs` → deployment_uuid `po00ws808g88wsggwc8cs8oc`. `is_api_enabled` war bereits `t` (nicht veraendert).
- **Poll** `application_deployment_queues`: in_progress → **finished** (~2,3 min, 14 Polls a 10s). Full-Compose-Recreate (neue Container-Namen `…-0919…`).
- **Delivery-Chain-Verify (Gate 3):** neuer app-Container SOURCE_COMMIT `da5fb7c` ✓; app/worker/kong/db `(healthy)` (app nach ~kurzem health-starting-Fenster, extern 503 transient → nach healthy `/login` 200 in 0,27s); intern `/api/health` `{status:ok}`.
- **PFLICHT-Cleanup:** temp-Token `DELETE 1`; `is_api_enabled` unveraendert gelassen (war `t`); lokale Token-Files geloescht.
- **Live-Smoke (Step 8):** temp tenant_admin (`e2esmoke-gf@…`, tenant `…aa`) via GoTrue-Admin-API geseedet → Playwright-Login → `/capture/new` rendert **„Neues Assessment starten" + Erfassungsmodus + voller Template-Picker** (Exit-Readiness, Mitarbeiter-Wissenserhebung + M-xx-Module), KEIN „This page couldn't load"/ZodError. Die 2 Legacy-Rows (`partner_diagnostic`, `exit-readiness-teaser-v1`) korrekt absent. Fixtures per SQL-DELETE-by-ID entfernt, Verify 3/3 = 0 ([[Dev-IMP-1702]], keine echten PWs [[never_touch_user_passwords]]).

## Gate 4 — Cockpit-Consistency-Sweep
Hotfix aendert KEINE Version/Feature/Slice-Stati (V10.4 bleibt released/stable). REL-042 = Patch auf V10.4. roadmap/features/slices/backlog unveraendert; ISSUE-119 → resolved. Konsistent.

## Verdict
Deploy erfolgreich, Delivery-Chain + Live-Smoke PASS. ISSUE-119 **resolved**. Direkt-Strang „Neue Erhebung" wieder funktionsfaehig — entsperrt den Founder-Selbsttest.

## Problems found / Learnings
- Keine neuen Deploy-Blocker. Playbook `coolify-autonomous-redeploy` 1:1 angewandt (Token-Trap via getrenntem INSERT/SELECT + `head -1` vermieden).
- Lokaler Haupt-Checkout hatte `zxcvbn` nicht installiert (ISSUE-114-Familie) → `npm install` + `.next`-Regen noetig, damit tsc/next build clean laufen (nicht deploy-blockierend, aber Session-Reibung).

## Offene Punkte (kein Blocker)
- Produktfrage (Founder): sollen `stb_modul_*`-Templates im Direkt-Client-Picker erscheinen? Live bestaetigt ~18 M-xx-Module sichtbar → optionaler Whitelist-/Purpose-Filter als Folge-Slice.
- ISSUE-118 (Berater-Login-Landing) weiterhin offen — separater kleiner Fix.

## Recommended next step
Founder testet den Direkt-Strang selbst (jetzt live entsperrt). Danach: ISSUE-118-Fix und/oder `/discovery` fuer den naechsten Build (Exit-Ready Produkt 1 BL-515..518 oder Design-System BL-536). `/post-launch` fuer diesen isolierten Hotfix ist optional (kein Version-Release); Burn-In laeuft mit dem V10.4-Stack.
