---
id: RPT-140
date: "2026-05-04"
skill: /frontend
slice: SLC-055
feature: V4.3
type: completion
status: completed
result: "SLC-055 /frontend Code-side PASS — UX-Findings-Bundle (Tooltip-Target-Fix + Help-Konsolidierung)"
title: "SLC-055 /frontend Completion Report"
---

# SLC-055 /frontend — UX-Findings-Bundle (Code-Side)

## Outcome Summary
- **Vollstaendig code-side** abgeschlossen, 7/7 MTs erledigt.
- **Verifikations-Konfidenz**: hoch fuer Build/Tests/Statisch, **partiell** fuer Browser-Verhalten (Live-Browser-Smoke 1280×800 + 375×667 ist Bundle-Live-Smoke nach SLC-054 per Slice-Pflicht-QA).

## Geaenderte/Neue ACs
- AC-1, AC-2, AC-3 (Tooltip-Card-Header-Wrapper) — code-side erfuellt; Mobile-Tap-Verhalten verifizierungspflichtig im Browser.
- AC-4, AC-5, AC-6, AC-7, AC-9 (LC-Tab "Diese Seite") — code-side erfuellt.
- AC-8 (pageKeyFromPathname Mapping) — 9/9 Vitest-Tests PASS.
- AC-10 (ein-Trigger-pro-Page) — code-side erfuellt: HelpButton + HelpTrigger oeffnen beide das Learning-Center mit `currentPageKey`. HelpSheet-Datei geloescht.
- AC-11 (V4.2-Regression) — Wizard-Help-Tooltips/Reader-Help unveraendert (kein Touch); HelpSheet entfernt, Folge-Cleanup Markdown-Prop von HelpTrigger als BL-Eintrag fuehrbar (siehe Open Points).
- AC-12 (`npm run build` + `npm run test`) — Build PASS (Next 16.2.4 Turbopack, mit dummy SUPABASE-ENVs wegen statischer Page-Data-Collection); 362/362 ENV-unabh. Tests PASS (Baseline 353 + 9 neue pageKeyFromPathname-Tests). 149 DB-Tests benoetigen TEST_DATABASE_URL (Coolify-DB), pre-existing Pattern.

## Files Reviewed
- `/slices/SLC-055-ux-findings-bundle.md`
- `/docs/STATE.md`
- `/docs/DECISIONS.md` (DEC-064 + DEC-067)
- `/src/components/learning-center/learning-center-panel.tsx`
- `/src/components/help-button.tsx`
- `/src/components/help/HelpTrigger.tsx`
- `/src/components/help/HelpSheet.tsx`
- `/src/lib/help/load.ts`
- `/src/components/cockpit/InactiveEmployeesCard.tsx`
- `/src/app/dashboard/dashboard-client.tsx`
- `/src/app/admin/tenant-admin-shell.tsx`
- `/src/app/admin/layout.tsx`
- `/src/messages/de.json`, `en.json`, `nl.json`
- 6 Server-Pages mit HelpTrigger-Use (dashboard, dashboard/handbook, dashboard/handbook/[snapshotId], capture/[sessionId], admin/bridge, admin/reviews)

## Files Changed
- `/src/components/learning-center/learning-center-panel.tsx` — Tab-Type `"videos" | "guide" | "this-page"`, neue Props `currentPageKey?` und `initialTab?`, Default-Tab "this-page" wenn pageKey vorhanden, Fetch-Logic `/api/help/<pageKey>` bei Tab-Aktivierung, FileText-Icon fuer Tab "Diese Seite".
- `/src/components/help-button.tsx` — komplett refaktoriert: self-contained, kein onClick-Prop mehr, nutzt `usePathname()` + `pageKeyFromPathname`, eingebetteter LearningCenterPanel.
- `/src/components/help/HelpTrigger.tsx` — Trigger oeffnet jetzt LC mit initialTab="this-page" + `currentPageKey`, statt eigenes HelpSheet. `markdown`-Prop ist deprecated/ignoriert (LC laedt selbst via API).
- `/src/components/cockpit/InactiveEmployeesCard.tsx` — Tooltip-Trigger ist jetzt der gesamte Card-Header (`<div role=button>`) mit tabIndex=0, aria-describedby, cursor-help, focus-ring; `?`-Icon ist nur noch visueller Hint (`aria-hidden`); `delayDuration=0` fuer schnelles Touch-Open; onClick + onKeyDown verhindern Link-Navigation.
- `/src/app/dashboard/dashboard-client.tsx` — alte LC-State entfernt, einfaches `<HelpButton />` ohne Props.
- `/src/app/admin/tenant-admin-shell.tsx` — `<HelpButton />` (vorher `onClick={() => {}}` no-op).
- `/src/messages/de.json`, `en.json`, `nl.json` — neuer i18n-Key `learning.tabThisPage`.
- `/slices/INDEX.md` — SLC-055 Status `done`.
- `/planning/backlog.json` — BL-062 + BL-063 Status `done`.
- `/docs/STATE.md` — Current Focus + Phase + Immediate Next Steps aktualisiert.

## Files Created
- `/src/components/learning-center/page-key-from-pathname.ts` — Helper fuer URL→HelpPageKey-Mapping (5 bekannte Pfade + Fallback `null`).
- `/src/components/learning-center/__tests__/page-key-from-pathname.test.ts` — 9 Vitest-Tests, alle PASS.
- `/src/components/learning-center/this-page-tab.tsx` — Pure Render-Komponente fuer Markdown via react-markdown + remark-gfm; Loading-Skeleton + Fallback-Banner bei fehlendem pageKey/markdown.
- `/src/app/api/help/[pageKey]/route.ts` — Static GET-Route, liefert Help-Markdown als `text/markdown`, mit Cache-Header.
- `/reports/RPT-140.md` — dieser Report.

## Files Deleted
- `/src/components/help/HelpSheet.tsx` — keine Verwendung mehr nach Refactor; HelpTrigger oeffnet jetzt LC.

## Files intentionally not changed
- `/src/lib/help/load.ts` — bleibt als SSR-Loader fuer ggf. zukuenftige Server-Seitige Inkonsistenzen, wird via `/api/help/[pageKey]` weiter genutzt.
- 6 Server-Pages mit HelpTrigger — Aufruf-Signatur unveraendert (`pageKey={...}` + `markdown={...}`); `markdown`-Prop nun ignoriert. Folge-Cleanup als BL eintragbar.
- `/src/components/handbook/ReaderShell.tsx` + `HandbookSelectionShell.tsx` — kein HelpButton/HelpTrigger-Touch noetig (HelpTrigger bleibt in den Page-Bodies).

## UI States Covered
- Tooltip-Card-Header: Hover (Desktop), Focus (Tab-Key), Tap (Mobile via delayDuration=0). Visuelles `?`-Icon bleibt.
- LC Tab "Diese Seite": Loading-Skeleton, Markdown-Render, Fallback-Banner bei pageKey=null oder Fetch-Fehler.
- LC Default-Tab: "this-page" wenn pageKey gesetzt, sonst "videos".
- LC Mirror-Mode: Tabs ausgeblendet, vereinfachte Inhalte (unveraendert zu V4.2).
- LC Tab-Reset bei jedem Open: Default-Tab wird neu evaluiert (verhindert Stale-Tab-State zwischen verschiedenen Trigger-Aufrufen).

## Problems Found
- **P1 — Build benoetigt SUPABASE_ENV-Vars** auch fuer statische Page-Data-Collection. Das ist pre-existing (nicht durch SLC-055 verursacht), wurde aber im Build-Run sichtbar. Mitigation: Build im Hetzner-Coolify-Kontext laeuft mit echten ENVs; lokal mit Dummy-Werten umgehbar.
- **P2 — Test-Environment ist `node`**, kein jsdom konfiguriert. Slice E erwaehnt `learning-center-panel.test.tsx` und `InactiveEmployeesCard.test.tsx` als geaenderte/neue Tests. Diese erfordern React-DOM-Render-Tests, was im Repo aktuell nicht etabliert ist. Browser-Smoke (Pflicht-QA-Vorgabe Slice-Zeile 114-116) uebernimmt die Verifikation. Folge-Setup als IMP fuer Dev-System diskutabel.
- **P3 — `markdown`-Prop von HelpTrigger ist deprecated, aber nicht entfernt**, damit bestehende 6 Server-Pages nicht brechen. Folge-Cleanup als optionaler BL-Eintrag.
- **P4 — Browser-Verifikation noch nicht gelaufen** (Bundle-Live-Smoke nach SLC-054 per Slice-Pflicht-QA-Vorgabe). Code-side hoch konfident, Browser-Verhalten verifizierungspflichtig: (a) Mobile-Tap auf Card-Header oeffnet Tooltip ohne Link-Navigation, (b) LC-Tab "Diese Seite" rendert korrektes Markdown auf allen 5 Pages, (c) ein-Trigger-pro-Page-Wahrnehmung beim End-User.

## Open Points / Deferred Decisions
- **D1 — HelpTrigger `markdown`-Prop entfernen**: Nach Bundle-Live-Smoke kann der Prop von 6 Server-Pages entfernt werden (Schritt-Cleanup, ~10 Zeilen). Optional als BL nach V4.3.
- **D2 — Komponenten-Render-Test-Setup** (jsdom + react-testing-library) als Dev-System-IMP eintragbar wenn weitere SaaS-Slices Component-Tests brauchen.
- **D3 — Cleanup unbenutzter `loadHelpMarkdown`-Imports** in den 6 Server-Pages: `loadHelpMarkdown(pageKey)` wird noch aufgerufen und an HelpTrigger gegeben, obwohl HelpTrigger es nicht mehr nutzt. Tote Berechnung. Folge-PR.

## Runtime Verification Summary
- TypeScript: `npx tsc --noEmit` PASS, keine Fehler.
- Vitest: 362/362 ENV-unabh. PASS (149 DB-Tests skipped/failed wegen fehlender TEST_DATABASE_URL — Baseline-Pattern).
- Next.js Build: PASS mit dummy SUPABASE-ENVs, `/api/help/[pageKey]` als Static-Route registriert.
- Browser-Smoke: ausstehend (Bundle-Live-Smoke nach SLC-054 per Slice-Pflicht-QA).
- Verifikations-Methodik: Build, statische Code-Review, neue Unit-Tests fuer pageKeyFromPathname. Kein Browser-Render, kein User-Interaction-Test, kein Persistence-Test (LC ist stateful nur waehrend Open).

## Recommended Next Step
**/qa SLC-055** (Code-side QA, kein Browser-Smoke vor SLC-054 noetig per Slice-Vorgabe Pflicht-QA-Block). Pruef-Foki:
- 4-Rollen-RLS-Matrix bleibt 100% PASS (kein DB-Touch).
- V4.2-Regression-Smoke: Wizard funktioniert, Reader funktioniert.
- AC-Walkthrough Code-side gegen alle 12 ACs.
- ESLint/TypeScript clean.
