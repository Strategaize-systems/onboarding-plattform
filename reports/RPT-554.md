---
id: RPT-554
date: "2026-07-03"
skill: /frontend
slice: SLC-180
feature: FEAT-097
type: completion
status: completed
reviewOf: null
result: "SLC-180 Inline-Rückfrage-UI + Followup-Merge /frontend Code-Side komplett (OPTIONAL-INLINE, DEC-255). tsc0/eslint0/vitest module-context 8/8 + stb-vertikale 130 pass/next build EXIT0. Browser-Smoke offen (App+Bedrock-gated). NEXT /qa SLC-180."
title: "SLC-180 Inline-Rückfrage-UI + Followup-Merge — /frontend"
---

# SLC-180 — Inline-Rückfrage-UI + Followup-Merge (/frontend)

## Outcome
Code-Side vollständig. Founder-Modus **OPTIONAL-INLINE** (DEC-255): die Live-Rückfrage (SLC-179 `assessModulAnswer`) wird nach dem Speichern einer geflaggten Kern-Frage inline unter der Antwort gerendert; beantwortbar (Merge via `followup.<block>.<qid>`) oder verwerfbar; **nie** ein Block-Submit-Gate. Verifikation: hoch für Build/Static/Unit, offen für Browser/Interaktion (App+Bedrock-gated).

## Scope umgesetzt
1. `mergeAnswers` um `followup.`-Präfix erweitert → `[Nachfrage]`-Append an Eltern-Antwort (Muster wie `evidence.*`). **AC-180-2**.
2. `submit-action.ts` trägt `followup.<blockKey>.*`-Keys verbatim in `block_checkpoint.content.answers` (separater Pass nach dem Eltern-Antwort-Pass → Eltern-Keys zuerst, mergeAnswers-Ordering garantiert).
3. Neue Komponente `InlineRueckfrage` (`src/components/stb/`) — Textarea + "Antwort ergänzen"/"Verwerfen", rein präsentativ.
4. Wiring in `questionnaire-form.tsx` nach `handleSaveAnswer`, **gegated via `modulKey`-Opt-in-Prop** (nur StB-Modul-Flow), nur geflaggte Kern-Fragen (`ko_hart|ko_soft|deal_blocker|owner_dependency|sop_trigger`), fire-and-forget + fail-open. **AC-180-1**. F-E-Heilung via Re-Assess der angereicherten Antwort. **AC-180-3**.
5. `modulKey`-Prop nur aus StB-Modul-block-page durchgereicht; general/employee capture opten NICHT ein.

Guardrail (max 2/Block) + F-E-Heilung sind server-seitig in SLC-179 (`computeAssessOutcome`) — **AC-180-4** dort erfüllt, hier konsumiert.

## Files
**Changed:**
- `/src/lib/stb-vertikale/module-context.ts` — `mergeAnswers` followup.-Präfix ([Nachfrage]-Append), Doc-Kommentar.
- `/src/app/capture/[sessionId]/block/[blockKey]/submit-action.ts` — followup.<block>.*-Carry in Checkpoint.
- `/src/app/capture/[sessionId]/block/[blockKey]/questionnaire-form.tsx` — modulKey-Prop, Live-Scoring-Wiring, InlineRueckfrage-Render, followup-Submit + F-E-Heilung, Clear-on-question-switch.
- `/src/app/dashboard/stb/modul/[modulKey]/[sessionId]/block/[blockKey]/page.tsx` — `modulKey`-Prop-Durchreichung.
- `/src/lib/stb-vertikale/__tests__/module-context.test.ts` — 2 neue followup.-Merge-Tests.

**Created:**
- `/src/components/stb/InlineRueckfrage.tsx` — Inline-Rückfrage-Präsentationskomponente.

**Records:** `/slices/INDEX.md` (SLC-180 → in_progress), `/docs/STATE.md` (Current Focus), `/docs/DECISIONS.md` (DEC-255), `/reports/RPT-554.md`.

## Gates
- tsc `--noEmit`: **EXIT 0**
- eslint (6 Files): **EXIT 0**
- vitest `module-context.test.ts`: **8/8** (2 neu); `stb-vertikale` gesamt: **130 pass**, 3 fail = `persist.test.ts` (TEST_DATABASE_URL-gated, pre-existing, kein SLC-180-Bezug).
- `next build`: **EXIT 0** (mit Placeholder-Supabase-Env). Ohne Env: pre-existing Build-Debt `supabaseUrl is required` in `/api/public/partner/[slug]` (nicht von SLC-180 berührt).

## Problems found
- **Browser-Smoke (AC-180-5) NICHT ausgeführt** — der StB-Modul-Flow braucht auth'd StB-Session + geseedete M-04-Template-Session + Bedrock-Haiku-Creds; lokal ohne laufende App/Coolify-DB nicht renderbar. Verlagert auf /qa+/deploy (Projekt-Rhythmus).
- **`next build` env-Debt** (pre-existing, IMP-Kandidat schon notiert): ein Modul instanziiert Supabase-Client zur Build-Eval-Zeit ohne Env. Mit Placeholder-Env grün.
- **STATE.md Current-Focus-Duplikat** (IMP-1373, pre-existing): 2. veraltete Current-Focus-Zeile (SLC-178) bewusst nicht entfernt (Scope-Disziplin); nur Top-Zeile aktualisiert.
- `assessing`-Indikator ist global (nicht pro-Frage gescoped) — 1-2s-Transient nach Speichern; die Rückfrage selbst ist per `questionId` gescoped, erscheint also nie auf der falschen Frage.

## Open points
- `/qa SLC-180`: Browser-Smoke (Render + Interaktion + 0 Console-Errors), E2E Save→Assess→Rückfrage→Followup-Merge→Heilung, Vitest-Gesamtlauf.
- `persist.test.ts` deckt den followup-Carry im Submit nicht ohne DB — bei /deploy via node:20-Sidecar mitverifizieren.

## Recommended next step
`/qa SLC-180` — inhaltliche Prüfung + Browser-Smoke (AC-180-5). Kein Push/Merge (Founder-Hold); beim späteren Merge Pre-Merge-Re-Check 177→178→179→180.
