---
id: RPT-487
date: "2026-06-17"
skill: /qa
slice: SLC-V9.75-C
feature: FEAT-087
type: completion
status: completed
reviewOf: RPT-482
result: "SLC-V9.75-C Stufe-1 Mitarbeiter-Register + Bruecke /backend + /qa PASS. 4 MTs (Migration 122 / CRUD-Actions / Promote-Bruecke / Debrief-Panel). Hermetisch 14/14 GREEN + DB-Sidecar 6/6 GREEN (live Coolify-DB: Schema/FK-SET-NULL/weiche-Dedup/Tenant-RLS-Pen-Test), tsc0/eslint0, next build PASS. employee_invitation/RPC unveraendert (DEC-224). Code auf Worktree `v9-75-exit-readiness`, ready fuer B+C Master-Merge."
title: "/qa SLC-V9.75-C — PASS (Mitarbeiter-Register, 14 hermetisch + 6 DB-Sidecar GREEN, build PASS)"
---

# /qa SLC-V9.75-C — Stufe-1 Mitarbeiter-Register + Bruecke

## Outcome
**PASS.** Leichtes Name+Funktion-Register (ohne E-Mail) im Debrief/Meeting-View + Bruecke zur **unveraenderten** `rpc_create_employee_invitation` (DEC-224). Diese Iteration umfasst /backend (4 MTs) + /qa-Verifikation inkl. Live-DB-Sidecar.

## QA-Scope reviewed
- Migration 122 (`employee_roster_draft` + Tenant-RLS + weiche Dedup) gegen live Coolify-DB (node:20-Sidecar, AC-C-1/C-3/C-4).
- Roster-CRUD-Actions + Promote-Bruecke (hermetisch, AC-C-2/C-5, R-C-1).
- Debrief-Panel-Einbindung (additive Komponente, R-C-2) + blueprint+-Gate (AC-C-5).
- Wiring-Kette Component → Action → DB/RPC.

## Verifikation
- **DB-Sidecar (live Coolify-DB, node:20):** `122-v975-roster.test.ts` **6/6 GREEN** — Schema (Name NOT NULL, role_hint/block_key/promoted_invitation_id nullable), FK `promoted_invitation_id`→employee_invitation **ON DELETE SET NULL**, weiche Dedup (`ON CONFLICT DO NOTHING`, gleicher Name CI + Funktion → 1 Zeile; andere Funktion → 2 Zeilen), **Tenant-RLS** (eigener Tenant rw; Tenant B sieht/schreibt A-Zeilen NICHT, SAVEPOINT-Pen-Test mit `row-level security`-Rejection). Container `supabase-db-bwkg80w04wgccos48gcws8cs-150241698111`, Netz `bwkg80w04wgccos48gcws8cs_strategaize-net`. Migration 122 ist 121-unabhaengig → Sidecar-Run vor Live-Apply 121 moeglich.
- **Hermetisch:** `roster-actions.test.ts` **14/14 GREEN** — Validation (Session-ID/Name/E-Mail), Rollen-Gate (tenant_admin/strategaize_admin), blueprint+-Stufen-Gate (free→tier_gate_denied), weiche Dedup (23505→deduped, kein Hard-Fail), Promote-Idempotenz (Erfolg→Stempel; `duplicate_pending_invitation`→„bereits eingeladen" ohne Duplikat/Stempel; Re-Promote-Block bei gesetztem `promoted_invitation_id`).
- **tsc --noEmit: EXIT 0.** **ESLint (Slice-Dateien): EXIT 0.**
- **`next build`: PASS** (mit gesetzten Build-ENVs; lokaler Env-Fehler nur an untouched Route `/api/public/partner/[slug]`, env-only, kein Code-Regress).
- **Neighbor-Regression:** `fahrplan-report/route.test.ts` (SLC-B) weiter **6/6 GREEN** → page.tsx-Edit rein additiv, 0 Regression.

## AC-Abdeckung
- AC-C-1 (Name+Funktion ohne E-Mail, session-/tenant-scoped, block_key-Tag) ✓ (Sidecar)
- AC-C-2 (Promote → genau 1 Invitation via RPC; pending-Duplikat → „bereits eingeladen", kein Duplikat; Erfolg setzt promoted_invitation_id) ✓ (hermetisch)
- AC-C-3 (Tenant-RLS, kein Cross-Tenant) ✓ (Sidecar SAVEPOINT-Pen-Test)
- AC-C-4 (weiche Dedup UNIQUE + ON CONFLICT DO NOTHING) ✓ (Sidecar + hermetisch 23505-Swallow)
- AC-C-5 (Register-UI/Actions ab blueprint+; add/edit/delete + promote funktional) ✓
- AC-C-6 (tsc/eslint EXIT 0, Vitest GREEN, next build PASS) ✓

## Wiring-Verifikation
- RosterPanel (`[blockKey]/RosterPanel.tsx`) → Actions (`../roster-actions`) — Import aufgeloest (tsc) ✓
- Action → DB `employee_roster_draft` (Migration 122) ✓
- Promote → RPC `rpc_create_employee_invitation` (072, unveraendert) ✓
- Gate → `fn_tier_rank` (121, ships gemeinsam in V9.75) ✓; GRANT authenticated ✓
- page.tsx → laedt tier+roster, rendert Panel nur blueprint+ ✓

## Findings
- **Blocker/High/Medium: keine.**
- **Stub-Scan:** keine echten Stubs. Zwei `return null` in `roster-actions.ts` sind der `trimOrNull`-Sanitizer (legitim).
- **Low / bekannte Grenze:** `rpc_create_employee_invitation` setzt `tenant_id = auth.user_tenant_id()` (NULL fuer strategaize_admin) → ein strategaize_admin-Promote degradiert zu `{ok:false, error:'rpc_failed'}` (kein Crash; NOT-NULL-Violation als rpcError gefangen). Konsistent mit 072-Design (strategaize_admin laedt via Direct-DB ein). Promote ist fuer tenant_admin gedacht; RPC bewusst unveraendert (DEC-224, Out-of-Scope).

## Files created (Worktree `v9-75-exit-readiness`)
- `sql/migrations/122_v975_employee_roster_draft.sql` (`b027c87`)
- `src/__tests__/migrations/122-v975-roster.test.ts` (`b027c87`)
- `src/app/admin/debrief/[sessionId]/roster-actions.ts`, `roster-actions.test.ts` (`c98142e`)
- `src/app/admin/debrief/[sessionId]/[blockKey]/RosterPanel.tsx` (`45dccc5`)

## Files changed (Worktree)
- `src/app/admin/debrief/[sessionId]/[blockKey]/page.tsx` (additiv: tier+roster-Load, blueprint+-Gate, RosterPanel-Render) (`45dccc5`)

## Files changed (canonical main, dieser /qa)
- `reports/RPT-487.md` (neu), `docs/STATE.md` (Current Focus/Phase → SLC-C /qa PASS, ready Merge).

## Problems found
- Keine. DB-Verifikation lief gegen die live Coolify-DB (kein lokaler Postgres, [coolify-test-setup.md]). `next build`-Env-Bedarf ist kein V9.75-Spezifikum.

## Open points
- **B+C Master-Merge steht aus** (Code auf Worktree, ready). Pre-Merge-Re-Check (Rebase gegen main + Diff; MIG 122 kollisionsfrei, main bei 121).
- Bei Merge: slices/INDEX SLC-V9.75-C→done, features/INDEX FEAT-087→done, backlog BL-508→done (zusammen mit B).
- Live-Apply Migration **121 + 122** → /deploy V9.75.

## Next workflow step
**Gesamt-/qa V9.75** ueber B+C (vor dem Master-Merge), danach **ein** Master-Merge fuer B+C (Founder-gated) + Records-Finalisierung → **/final-check V9.75**. SLC-V9.75-C ist die letzte geplante V9.75-Slice; nach dem Merge folgt der Versions-Abschluss-Pfad (/final-check → /go-live → /deploy → /post-launch). Kein Customer-Outreach ([[module-lifecycle-discipline]]).
