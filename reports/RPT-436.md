---
id: RPT-436
date: "2026-06-10"
skill: /architecture
slice: SLC-V9.1-A
feature: FEAT-075 + FEAT-076
type: completion
status: completed
reviewOf: RPT-429
result: "V9.1 Architecture-Revision R1 — DEC-194 (AWS SES) superseded durch DEC-205 (IMAP-Sync-Reuse aus BS gegen IONOS). Single-Mailbox jetzt, Catchall spaeter. MT-3/4/5 raus/ersetzt, neuer IMAP-Sync-MT + MIG-061. Next: /slice-planning-Revision."
title: "Architecture-Revision R1 V9.1 — Inbound-Transport SES → IMAP-Reuse (IONOS)"
---

# V9.1 Architecture-Revision R1 — Inbound-Transport: AWS SES → IMAP-Reuse gegen IONOS

## 1. Auslöser
Founder-Direktive 2026-06-10 (Reuse-First): kein neues AWS-SES/S3/SNS/Lambda-Konstrukt; die im Business-System produktiv laufende IMAP-Sync-Lösung gegen IONOS wiederverwenden.

## 2. Verifizierte Fakten (vor der Entscheidung)
- **DNS-Provider = IONOS** (nicht Hetzner): NS `ns10xx.ui-dns.*`, Root-MX `mx0x.ionos.de`. Hetzner = nur Compute.
- **AWS-Account existiert** (Bedrock, eu-central-1) — aber SES-Email-Receiving (eu-west-1) wäre ein **separater, neu aufzusetzender** Dienst (+ ~24h Sandbox-Approval).
- **BS-IMAP-Sync existiert** und läuft: `strategaize-business-system/cockpit/src/lib/imap/sync-service.ts` + `api/cron/imap-sync/route.ts` + `imapflow@^1.3.1`.
- OP hat `mailparser@^3.9.9` bereits, aber **kein** `imapflow` (neue Dependency nötig).
- `bulk.strategaizetransition.com` MX existiert nicht (SES war faktisch nie aufgesetzt → kein Wegwerf-Verlust).

## 3. Entscheidung (DEC-205, supersedes DEC-194)
Inbound-Transport = **IMAP-Pull** statt SES-Push: Coolify-Cron `POST /api/cron/inbound-email-imap-sync` (OP-`x-cron-secret`-Pattern) → aus BS portierter `imap-sync.ts` (ImapFlow, inkrementeller UID-Fetch via neuer Tabelle `email_inbound_sync_state`) → pro Mail: `simpleParser` → Endpoint-Resolve → **bestehender** Validation-Layer → `storage-persist` → `rpc_inbound_record_message`.

**Routing:** Single-Mailbox jetzt (1 IONOS-Postfach → 1 aktiver Endpoint via `INBOUND_DEFAULT_ENDPOINT_SLUG`), IONOS-Catchall + Slug-Routing später (DEC-204-Extraktion wiederverwendet, nur Aktivierung — kein Code-Umbau).

## 4. Reuse-Bilanz (zentral)
| Erhalten (LIVE/QA'd) | Wegfall |
|---|---|
| MIG-057..060 Schema + rpc (LIVE) | SES-Adapter `vendors/aws-ses.ts` + `hmac.ts` |
| Validation-Layer (setup-token/allowlist/tenant-lookup/reject-log/storage-persist) | Webhook `src/app/api/inbound/email/route.ts` (Logik → Cron) |
| Recipient-Slug-Extraktion (DEC-204, für Catchall) | Lambda `infra/lambda/forward-ses-to-op-webhook/` + `scripts/deploy-lambda.sh` |

Nur der **Transport** wird getauscht — und er ist selbst ein BS-Reuse. Kein AWS-Sandbox-24h-Approval mehr; Live-Smoke gegen IONOS sofort möglich.

## 5. Files changed
- `/docs/ARCHITECTURE.md` — V9.1-Addendum REVISION-R1-Block (neuer Flow A, Komponente IMAP-Sync, Routing Single/Catchall, ENV-Strategie, MIG-061-Verweis, SLC-V9.1-A-Impact-Tabelle); SES-Abschnitte als obsolet markiert (nicht gelöscht).
- `/docs/DECISIONS.md` — DEC-205 (neu, accepted); DEC-194 + DEC-204 → `superseded` mit Superseded-Note.
- `/docs/MIGRATIONS.md` — MIG-061 `email_inbound_sync_state` (PLANNED, Draft).
- `/docs/STATE.md` — Current Focus + Phase auf Architecture-Revision R1, nächster Schritt /slice-planning-Revision.

## 6. Open technical questions
- **OQ-R1-1 (IONOS-Postfach):** Konkretes IONOS-Postfach + IMAP-Credentials (Founder legt an / liefert `IMAP_HOST/USER/PASSWORD`). IONOS-IMAP-Host i.d.R. `imap.ionos.de:993` SSL.
- **OQ-R1-2 (Default-Endpoint):** Wie wird der eine aktive Endpoint im Single-Mailbox-Modus gesetzt — ENV-Slug vs. „einzige active Row"? Empfehlung: ENV `INBOUND_DEFAULT_ENDPOINT_SLUG`, Fallback einzige active Row.
- **OQ-R1-3 (Setup-Token im Single-Mailbox):** Im Founder-only-Single-Mailbox ist die Mailbox selbst zugangsgeschützt — Setup-Token-Layer optional. Empfehlung: Layer behalten (defense-in-depth), aber im Single-Modus tolerant (kein Header → akzeptiert, da Mailbox-Trust). In /slice-planning final entscheiden.

## 7. Recommended next step
`/slice-planning` (Revision SLC-V9.1-A): MT-3 streichen, MT-4 → IMAP-Cron umschneiden, MT-5 (Lambda + deploy-script) löschen, neuen MT „IMAP-Sync-Port aus BS" + MIG-061 + `imapflow`-Dependency + Cron-MT + Live-Smoke-MT gegen IONOS. Danach /backend. Kein Master-Merge / kein /go-live vor Gesamt-V9.1-/qa → /final-check → /go-live → /post-launch.
