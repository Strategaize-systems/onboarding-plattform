---
id: RPT-476
date: "2026-06-15"
skill: /go-live
feature: FEAT-083, FEAT-084
type: completion
status: completed
result: "V9.7 OKF Handbuch-Export: CONDITIONAL GO — deploy-ready, Release konditioniert auf post-deploy Live-Smoke (okf/-Inspektion + Conformance, R-B-4)."
title: "Go-Live V9.7 — OKF Handbuch-Export (CONDITIONAL GO)"
---

# Go-Live V9.7 — OKF Handbuch-Export

## Release target
V9.7 (SLC-V9.7-A Concept-Emitter + SLC-V9.7-B Bundle-Assembly + Worker-Wiring), code-complete + gemergt main `3f58e26` (HEAD `4f5bc71`). Internal-Test-Mode (Single-Founder, kein Customer-Outreach, [[module-lifecycle-discipline]]) — `/go-live` = internes Workflow-Closure, NICHT Customer-Go-Live.

## Readiness decision
**CONDITIONAL GO.** Code ist deploy-ready (final-check READY RPT-475: 0 Blocker/0 High/0 Medium). Der Release wird erst nach dem post-deploy **Live-Smoke (R-B-4)** als bestaetigt gewertet: echter Snapshot-Download → ZIP enthaelt `okf/`-Ordner + `handbuch/` byte-identisch → `checkOkfConformance` gegen das reale Bundle PASS. Kern-Deliverable (narratives Handbuch) ist unabhaengig davon durch weiche Degradation (DEC-225) geschuetzt.

## Blockers
Keine.

## Accepted risks
- **R-B-4 (Live-Smoke deploy-gated):** der gewirte Worker-OKF-Pfad lief noch nie gegen echte Daten (Helper unit-getestet, Wiring nicht). Mitigation: weiche Degradation — bei jedem OKF-Fehler/Non-Conformance bleibt `okf/` weg, Snapshot wird trotzdem `ready`, Handbuch-Download unberuehrt. Worst-Case = OKF-Feature inert, kein Daten-/Deliverable-Schaden.
- Content-Paritaet narrativ vs OKF = Nicht-Ziel (DEC-225, R-B-2).

## Required fixes before release
Keine vor dem Deploy. Deploy-Schritt-Pflichten (kein Code-Fix):
1. `main` pushen (Push gebündelt mit /deploy; Coolify-Webhook inaktiv → manueller Redeploy; wget nicht curl, IMP-1252; `INBOUND_MAILBOX_ADDRESS`-ENV beim Redeploy setzen).
2. Post-deploy Live-Smoke (okf/-Inspektion + Conformance) ausfuehren → dann REL-Eintrag + roadmap V9.7 → released.

## Operational concerns
- **0 Migrationen, 0 neue ENVs/Cron, 0 neue Deps** — minimaler Deploy-Footprint (reiner Coolify-Image-Rebuild).
- **Rollback:** trivial — additive Aenderung; Revert des Merge-Commits `3f58e26` ODER Redeploy des Vorgaenger-Images (V9.5/REL-032, live `633dadd`). Kein Schema-Rollback noetig.
- **Download-Endpoint unveraendert** — keine API-Contract-Aenderung.

## Recommended next step
`/deploy V9.7` (Push + Coolify-Rebuild + post-deploy Live-Smoke; bei PASS REL-Eintrag + roadmap released) → `/post-launch V9.7`.
