---
id: RPT-485
date: "2026-06-17"
skill: /qa
slice: SLC-V9.75-A
feature: FEAT-085
type: completion
status: completed
reviewOf: RPT-484
result: "SLC-V9.75-A Fix-Iteration (/backend) + /qa Re-Run PASS. ISSUE-105 (Blocker) gefixt: bridge_generation/evidence_extraction/dialogue_extraction werden jetzt am Dispatch gegated+gestempelt (Schicht 1) + Worker-Backstop loest bridge_run_id/dialogue_session_id auf (Schicht 2). Schicht-1-Gate auch fuer diagnose knowledge_unit_condensation (Medium). Verifikation: tsc0, eslint0 (slice), 34 DB-Sidecar + 19 hermetisch GREEN (live Coolify-DB), next build PASS. ISSUE-105 + ISSUE-097 code-side resolved. Ready fuer Pre-Merge-Re-Check + Master-Merge."
title: "/qa SLC-V9.75-A — PASS nach Fix (ISSUE-105 geschlossen, 53 Tests GREEN, build PASS)"
---

# /qa SLC-V9.75-A (Re-Run) — Tier-Gating Foundation, Fix-Iteration

## Outcome
**PASS.** Der in RPT-484 gefundene Blocker (ISSUE-105) ist gefixt und end-to-end verifiziert. Diese Iteration umfasst sowohl die /backend-Fix-Arbeit als auch die /qa-Re-Verifikation (alle Gates frisch in dieser Session gefahren).

## Fix (ISSUE-105) — was geaendert wurde
Die drei gated job_types, deren Dispatch-Sites V9.75 zuvor NICHT angefasst hatte (→ `session_tier = NULL` → Worker fail-closed fuer jede Session), stempeln + gaten jetzt am Dispatch (Schicht 1):

| job_type | Fix | Datei |
|---|---|---|
| `bridge_generation` | `rpc_trigger_bridge_run` (073) als CREATE-OR-REPLACE in Mig 121 §4: Tier-Gate (blueprint) + `session_tier := v_session.tier` | `sql/migrations/121_*.sql` |
| `evidence_extraction` | `assertSessionTierAllows` + `session_tier`-Stempel; bei zu niedriger Stufe wird der Extraktions-Job uebersprungen (kein 403 — Datei bleibt gespeichert) | `src/app/api/capture/[sessionId]/evidence/upload/route.ts` |
| `dialogue_extraction` | erbt den `session_tier`-Stempel des Eltern-Jobs (`dialogue_transcription`, am recording-ready-Dispatch gegated+gestempelt) | `src/workers/dialogue/handle-transcription-job.ts` |

Zusatz (Medium aus RPT-484): `submitDiagnoseRun` (`knowledge_unit_condensation`) erhaelt einen Schicht-1-Dispatch-Gate VOR dem Status-Submit + Stempel (`src/app/dashboard/diagnose/actions.ts`).

Defense-in-Depth (Schicht 2): `resolveSessionForGate` (`src/workers/condensation/claim-loop.ts`) loest jetzt auch `bridge_run_id` → `bridge_run.capture_session_id` und `dialogue_session_id` → `dialogue_session.capture_session_id` auf — der Worker-Backstop ist damit gegen einen kuenftig vergessenen Stempel robust, statt blind fail-closed zu toeten (das war der Kern von ISSUE-105).

## Verifikation (AC-A-8 vollstaendig)
- **tsc --noEmit: EXIT 0.**
- **ESLint (Slice-Dateien scoped): EXIT 0.** (Die 2 pre-existing Repo-Altlast-Befunde aus RPT-484 bleiben unberuehrt — kein V9.75-Bezug.)
- **`next build`: PASS (EXIT 0).** (Erstlauf scheiterte an fehlendem Build-ENV `SUPABASE_URL` auf einer V9.75-fremden Route `/api/public/partner/[slug]` — reines Build-Env, kein Regress; mit gesetzten ENVs sauber durch.)
- **DB-Sidecar (node:22, `bwkg80w04wgccos48gcws8cs_strategaize-net`, Server 159.69.207.29, live-apply Mig 121 in Test-Tx):**
  - `121-v975-tier-gating.test.ts`: **21/21** (inkl. neuer `rpc_trigger_bridge_run`-Gate+Stempel-Test: free→tier_gate_denied/kein bridge_run/kein Job; blueprint→Job stamped `blueprint`).
  - `v975-tier-bypass.test.ts`: **13/13** (unveraendert, 0 Regression).
- **Hermetisch (lokal node v22):** `claim-loop.test.ts` **14/14** (inkl. 2 neuer Backstop-Tests: `bridge_run_id`→resolve+allow, `dialogue_session_id`→resolve+deny) + `assert-session-tier.test.ts` **5/5**.
- **Summe: 53/53 Tests GREEN** (34 DB + 19 hermetisch).
- **Regression der RPT-484-Probe:** die 3 Payloads, die zuvor fail-closed waren, sind jetzt entweder am Dispatch gestempelt (laufen) oder vom Backstop aufloesbar.

## Findings
- **Blocker: keiner** (ISSUE-105 geschlossen).
- **Stub-Scan:** keine Stubs/Platzhalter/leere Handler in den geaenderten Dateien.
- **Wiring:** alle gated Dispatch-Pfade (jetzt 12) stempeln `session_tier`; walkthrough-Chain (via `advanceWalkthroughPipeline`) und email_bulk-Pfade (Worker-Resolve via `bulk_run_id`) waren bereits korrekt.

## Files changed (Fix, Worktree `v9-75-exit-readiness` Commit `892a7ee`)
- `sql/migrations/121_v975_tier_gating_foundation.sql` — §4 `rpc_trigger_bridge_run` CREATE-OR-REPLACE (Gate+Stempel) + Header/Rollback-Notizen
- `src/app/api/capture/[sessionId]/evidence/upload/route.ts` — Gate+Stempel evidence_extraction
- `src/workers/dialogue/handle-transcription-job.ts` — session_tier-Vererbung an dialogue_extraction
- `src/app/dashboard/diagnose/actions.ts` — Schicht-1-Gate+Stempel knowledge_unit_condensation
- `src/workers/condensation/claim-loop.ts` — `resolveSessionForGate` +bridge_run_id/+dialogue_session_id
- `src/__tests__/migrations/121-v975-tier-gating.test.ts` — bridge-RPC Gate+Stempel-Test (+2)
- `src/workers/condensation/__tests__/claim-loop.test.ts` — Backstop-Resolve-Tests (+2)

## Files changed (canonical main, dieser /qa)
- `reports/RPT-485.md` (neu), `docs/KNOWN_ISSUES.md` (ISSUE-105 + ISSUE-097 → resolved), `docs/STATE.md` (Current Phase → /qa PASS, ready Merge)

## Problems found
- Keine offenen technischen Probleme. `next build` braucht gesetzte Build-ENVs (dokumentiert; gilt fur jeden lokalen OP-Build, kein V9.75-Spezifikum).

## Open points
- **Master-Merge steht aus** (Code auf Worktree `892a7ee`, nicht gemergt). Vor Merge: Pre-Merge-Re-Check (git-release.md) — bei nur EINER Slice in flight v.a. Rebase gegen main + MIG-121-Kollisionscheck (main hat aktuell bis MIG-120; 121 frei).
- **Pending Records an der Merge-Boundary** (cumulative-single-branch): 2 DECs (anti-forge ai_jobs-Insert-Guard; bulk-email NULL-Session Worker-Carve-out), MIG-121-Eintrag, slices/INDEX SLC-V9.75-A→done, features/INDEX FEAT-085→done, backlog BL-506→done, roadmap v9-75→active, ARCHITECTURE §4 Datei-Attributions-Korrektur (IMP-1278), dev-system pattern-reuse-Zeile (IMP-1282).
- Live-Apply Mig 121 + Dispatch-Deploy erfolgt im /deploy V9.75 (sql-migration-hetzner.md).

## Next workflow step
**Pre-Merge-Re-Check + Master-Merge** des Worktree-Branch `v9-75-exit-readiness` in `main`, dann Records-Finalisierung (s.o.), dann **SLC-V9.75-B** (Fahrplan-PDF-Renderer). Master-Merge ist eine signifikante Aktion → Founder-Bestaetigung vor dem Merge.
