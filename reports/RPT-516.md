---
id: RPT-516
date: "2026-06-22"
skill: /backend
slice: SLC-171
feature: FEAT-090
type: completion
status: completed
reviewOf: null
result: "SLC-171 /backend Code-Side DONE — StB-Vertical-Marker (capture_session.metadata, DEC-243, kein DDL) + Env-Gate NEXT_PUBLIC_ENABLE_STB_VERTIKALE (Default OFF) + Route-Group-Guard. tsc 0 / eslint 0 / 21 Tests PASS / next build PASS. Keine Migration. NEXT /qa."
title: "SLC-171 StB-Onboarding-Rahmen + Env-Gate — /backend"
---

# SLC-171 — StB-Onboarding-Rahmen + Env-Gate (/backend)

## Outcome
Code-Side **vollstaendig**. Beide Micro-Tasks implementiert + hermetisch getestet, alle Quality-Gates gruen. Keine Migration (AC-171: `MIG reserviert: keine` eingehalten). Branch `v10-slc171-onboarding-envgate` (Worktree `…-v10`, von main `3e02231`), NICHT gemergt/gepusht — Pre-Merge-Re-Check + Master-Merge erst nach `/qa`-PASS.

## Schema-Drift-Fix (DEC-243)
Der Slice-Spec setzte einen `tenant.metadata.stb_vertical_stage`-Marker "kein DDL" voraus. Pre-Flight-Schema-Inspect: **`tenants` hat keine metadata/settings-jsonb-Spalte** — ein tenant-level-Marker waere nur via neuer Migration moeglich, was die V10-Invariante "1 Migration gesamt = 124" verletzt. Founder-delegierte Entscheidung -> Marker lebt in **`capture_session.metadata.stb_vertical_stage`** (etablierter no-DDL-Slot, MIG-103/DEC-165). Folge: MT-1 ist ein thin Helper; der Set-Aufruf wird in SLC-173 (Capture-Erstellung) verdrahtet — MT-1 hat keinen Provisioning-Touchpoint mehr. Details: DEC-243.

## Akzeptanzkriterien
- AC-171-1 (StB onboardet via bestehendem Provisioning, `tenant_admin`, kein neuer Pfad): bestaetigt via DEC-238-Code-Map (`provisionSelfSignupTenant` / Invitation `role_hint='tenant_admin'`). Reiner Reuse, kein neuer Provisioning-Code in diesem Slice.
- AC-171-2 (Tenant als StB-Vertikale-Stufe-1 erkennbar, kein neues Schema): erfuellt via `capture_session.metadata.stb_vertical_stage='1'`-Marker (DEC-243-Reinterpretation: erkennbar an den StB-Capture-Sessions des Tenants).
- AC-171-3 (Tenant-Isolation/RLS): `capture_session`-RLS unveraendert, Marker reitet auf bestehender Row -> kein neues Policy-Surface. DB-Sidecar-RLS-Verify in `/qa`.
- AC-171-4 (Env-Gate; OFF -> /dashboard/stb/* nicht erreichbar, ON -> sichtbar; Default OFF): erfuellt via `StbLayout`-Redirect + Platzhalter-`page.tsx`; `isStbVerticalEnabled()` fail-closed (nur exakt "true").
- AC-171-5 (tsc 0, eslint 0, next build PASS, Guard hermetisch getestet): **erfuellt**.

## Quality-Gates
- `npx tsc --noEmit`: EXIT 0.
- `npx eslint src/lib/stb-vertikale src/app/dashboard/stb`: EXIT 0.
- `npx vitest run src/lib/stb-vertikale`: **21/21 PASS** (6 feature-gate + 15 tenant-marker).
- `npx next build`: PASS (mit Env-Vars; `/dashboard/stb` als dynamische Route registriert). Erster Build-Lauf ohne Env brach an pre-existing `/api/public/partner/[slug]` mit `supabaseUrl is required` ab — rein environmental (kein Bezug zu SLC-171), mit Env-Stubs gruen.

## Files created
- `src/lib/stb-vertikale/tenant-marker.ts` — MT-1: Marker-Helper auf `capture_session.metadata.stb_vertical_stage`. Pure `readStbVerticalStage`/`isStbVerticalSession`/`mergeStbVerticalStage` + DB-Writer `setStbVerticalStage` (fetch-merge-write, idempotent, Admin-Client). JS-Merge weil Supabase-JS keinen jsonb-||-Operator hat (`feedback_fetch_merge_write_supabase_jsonb.md`); Pattern aus `src/lib/llm/v8-1-augmentation/cache.ts`.
- `src/lib/stb-vertikale/feature-gate.ts` — MT-2: `isStbVerticalEnabled()` = `process.env.NEXT_PUBLIC_ENABLE_STB_VERTIKALE === "true"` (Default OFF, fail-closed). OP-Konvention (vgl. `NEXT_PUBLIC_WHISPER_ENABLED`).
- `src/app/dashboard/stb/layout.tsx` — MT-2: Route-Group-Guard (Redirect auf `/dashboard` bei OFF). Auth bereits via Middleware.
- `src/app/dashboard/stb/page.tsx` — MT-2: minimaler Platzhalter, damit AC-171-4 "ON -> sichtbar" end-to-end pruefbar ist. Wird von SLC-175 (Reader) ersetzt.
- `src/lib/stb-vertikale/__tests__/tenant-marker.test.ts` — 15 Tests (Pure-Helpers + gemockter Admin-Client: happy/idempotent/key-preservation/not_found/read_failed/write_failed).
- `src/lib/stb-vertikale/__tests__/feature-gate.test.ts` — 6 Tests (OFF/absent/true/false/empty/"1"/"TRUE").
- `reports/RPT-516.md` — dieser Report.

## Files changed
- `docs/DECISIONS.md` — DEC-243 (Marker-Location-Entscheidung).
- `docs/STATE.md` — Current Focus + Current Phase auf SLC-171 /backend done -> NEXT /qa.
- `slices/INDEX.md` — SLC-171 Status `planned` -> `in_progress`.
- `slices/SLC-171-stb-onboarding-rahmen-envgate.md` — Architektur-Anker-Vermerk zu DEC-243.
- `planning/backlog.json` — BL-509 Status `open` -> `in_progress` + Note.
- `.env.local.example` — Doku-Block fuer `NEXT_PUBLIC_ENABLE_STB_VERTIKALE` (auskommentiert, Default OFF).

## Files reviewed, intentionally not changed
- `src/lib/signup/auto-provision.ts`, `src/app/dashboard/wizard-actions.ts`, `src/app/accept-invitation/[token]/actions.ts` — Provisioning-Pfade (DEC-238); reiner Reuse, kein Edit in diesem Slice.
- `src/lib/supabase/admin.ts` — Admin-Client-Pattern uebernommen, nicht geaendert.
- `sql/` — keine Migration (DEC-243).

## Problems found
- **Spec-Schema-Drift** (geloest, DEC-243): Slice-Spec nahm `tenant.metadata` an; existiert nicht. Wiederholungsfall der Klasse `feedback-slice-spec-db-schema-drift` (vgl. DEC-165) -> Kandidat fuer Dev-System-IMP im Handoff.
- `.env.local.example` (nicht `.env.example` wie im Slice-File-List genannt) ist die korrekte OP-Konvention fuer NEXT_PUBLIC_-Flags — Slice-Wording leicht ungenau, Implementierung folgt der Repo-Realitaet.

## Open points / deferred
- Set-Aufruf von `setStbVerticalStage` bei Capture-Erstellung -> **SLC-173** (per DEC-243 bewusst deferred).
- DB-Sidecar-RLS-Verify (`capture_session`-Isolation, AC-171-3) -> `/qa` (node:20-Sidecar, coolify-test-setup.md).
- Commits noch nicht gemergt/gepusht — Master-Merge nach `/qa`-PASS + Pre-Merge-Re-Check.

## Recommended next step
`/qa SLC-171` — Stub-/Wiring-Audit, AC-Matrix, Full-Test-Suite, DB-Sidecar-RLS-Verify fuer `capture_session`-Isolation, dann Pre-Merge-Re-Check + Master-Merge.
