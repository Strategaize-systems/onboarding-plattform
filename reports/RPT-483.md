---
id: RPT-483
date: "2026-06-17"
skill: /backend
slice: SLC-V9.75-A
feature: FEAT-085
type: completion
status: completed
reviewOf: RPT-482
result: "SLC-V9.75-A /backend IN PROGRESS — MT-0 (Worktree) + MT-1 (Migration 121 Tier-Gating-Foundation: tier-Spalte + session_tier-Stempel + 4 Matrix-Funktionen + service_role-aware Change-Guard-Trigger) + TDD-Test code-side DONE. tsc0/eslint0. DEC-219-Refinement (set-tier via service_role-Admin-UPDATE, keine DEFINER-RPC). Live-Sidecar-Run + MT-2..MT-5 offen."
title: "/backend SLC-V9.75-A MT-0+MT-1 — Tier-Gating Foundation (Migration 121)"
---

# /backend SLC-V9.75-A — Tier-Gating Foundation (MT-0 + MT-1)

## Outcome
**IN PROGRESS — MT-0 + MT-1 code-side DONE, committed `92ceddf` (Worktree-Branch).** Foundation für das server-side Tier-Gating steht: Schema + Matrix-Single-Source + Column-Level-Change-Guard. MT-2..MT-5 (RPC-Guards, TS-Guard+Wiring, Worker-Defense, Bypass-Tests) + Live-Sidecar-Run offen.

## MT-0 — Worktree
`git worktree add c:/strategaize/strategaize-onboarding-plattform-v975 -b v9-75-exit-readiness main` (aus main `4928027`), `npm install` (exit 0, 979 pkgs), tsc-Baseline grün.

## MT-1 — Migration 121 + TDD-Test (committed `92ceddf`)
- **`sql/migrations/121_v975_tier_gating_foundation.sql`:**
  - `capture_session.tier text NOT NULL DEFAULT 'handbook' CHECK (free/blueprint/handbook)` — NOT-NULL-DEFAULT backfillt Bestand (AC-A-1, R-A-4).
  - `ai_jobs.session_tier text NULL` — Worker-Defense-Stempel (DEC-221).
  - `fn_tier_rank(text)` (free=0/blueprint=1/handbook=2/unknown=-1), `fn_min_tier_for_job(text)` (20 job_types: 5 blueprint, 14 handbook, lead_push_retry ungated), `fn_tier_allows(text,text)` (pure, fail-closed bei NULL), `fn_session_tier_allows(uuid,text)` (SECURITY DEFINER, löst tier aus Session) — Matrix-Single-Source DEC-220, AC-A-7.
  - `capture_session_tier_change_guard` BEFORE-UPDATE-Trigger (`current_user <> 'service_role'`) — Column-Level-Schutz gegen tenant_admin-Self-Promotion (AC-A-5, ISSUE-097-Kern). Reuse BS V8.14 SLC-912 `profiles.role`-Pattern (strategaize-pattern-reuse.md).
  - Grants (authenticated+service_role auf die 4 Funktionen), NOTIFY pgrst reload.
- **`src/__tests__/migrations/121-v975-tier-gating.test.ts`:** Schema (tier NOT NULL/DEFAULT/CHECK, Bestands-Backfill, session_tier nullable), Matrix (fn_tier_rank, fn_min_tier_for_job für **alle 20** job_types via JOB_MIN_TIER-Map, fn_tier_allows-Fälle inkl. NULL-fail-closed, fn_session_tier_allows mit Session-Seed), Trigger (existiert, tenant_admin tier-Change → insufficient_privilege via SAVEPOINT, non-tier-Update erlaubt, service_role tier-Change erlaubt). Reuse `withTestDb`/`withJwtContext` + 106-Fixture-Pattern (tenant + auth.users mit raw_user_meta_data + capture_session).

## Verification
- **tsc --noEmit:** EXIT=0. **eslint** (Test-File): EXIT=0.
- **Live-Sidecar-Vitest gegen Coolify-DB:** deferred auf /qa (node:20-Sidecar, Live-Apply Mig 121) — kein lokaler Docker-Test (feedback_no_local_docker / module-lifecycle). Der Test ist so geschrieben, dass er die Migration pro Tx frisch anwendet (auch vor Live-Apply).

## DEC-219-Refinement (Implementation-Finding)
set-tier-Pfad = **service_role-Admin-Client-UPDATE** (TS-Action `setCaptureSessionTier`, MT-3), **KEINE** SECURITY-DEFINER-RPC: eine DEFINER-Funktion läuft als Owner `postgres`, den der `current_user <> 'service_role'`-Trigger ebenso blockt wie `authenticated`. Das ist die kanonisch korrekte BS-`changeRole`-Reuse. Migration 121 enthält daher keine `set_capture_session_tier`-RPC. Dokumentiert in DECISIONS.md DEC-219 (Refinement-Zeile).

## Files created (worktree)
- `sql/migrations/121_v975_tier_gating_foundation.sql`
- `src/__tests__/migrations/121-v975-tier-gating.test.ts`

## Files changed (main, Records)
- `/docs/DECISIONS.md` — DEC-219 Refinement-Zeile (set-tier service_role statt DEFINER-RPC).
- `/docs/STATE.md` — Focus/Phase → SLC-V9.75-A MT-0+MT-1 done, MT-2 next.
- `/slices/INDEX.md` — SLC-V9.75-A status planned → in_progress.
- `/reports/RPT-483.md` — dieser Report.

## Problems found
- Keine. Hinweis: Migration 121 ist bewusst noch unvollständig (MT-2 RPC-Guards + MT-4 Claim-Return werden in dieselbe Datei ergänzt) — WIP auf dem Worktree-Branch, kein Live-Apply vor Slice-Completion.

## Open points (MT-2..MT-5, in Worktree fortsetzen)
- **MT-2:** CREATE OR REPLACE `rpc_create_block_checkpoint` (032), `rpc_enqueue_recondense_job` (047), `rpc_trigger_handbook_snapshot` (074) — Inline `IF NOT fn_session_tier_allows(p_capture_session_id, '<job_type>') THEN RAISE EXCEPTION 'tier_gate_denied'` + `session_tier`-Stempel auf den ai_jobs-INSERT. Bodies 047/074 noch lesen. In Migration 121 ergänzen + Test erweitern.
- **MT-3:** `src/lib/auth/assert-session-tier.ts` (`assertSessionTierAllows` via RPC `fn_session_tier_allows`) + Wiring an 5 TS-Dispatches + `setCaptureSessionTier`-Action (createAdminClient + strategaize_admin) + Paritäts-Test.
- **MT-4:** `rpc_claim_next_ai_job_for_type` (035) +`session_tier` im Return (in Mig 121) + `src/workers/condensation/claim-loop.ts` fail-closed-Check nach Claim.
- **MT-5:** Bypass-Test-Matrix (`src/__tests__/security/v975-tier-bypass.test.ts`) + ISSUE-097-Closure.
- Dann **/qa SLC-V9.75-A** (node:20-Sidecar Live-Apply + Vitest gegen Coolify-DB).

## Recommended next step
**/backend SLC-V9.75-A MT-2** im Worktree `v9-75-exit-readiness` (3 RPC-Dispatch-Guards). Frische Session empfohlen (Context) — Resume-Punkt: bodies 047/074 lesen, dann Guards in Migration 121 ergänzen.
