---
id: RPT-104
date: "2026-04-28"
skill: /qa
slice: SLC-044
feature: FEAT-028
type: completion
status: needs-rework
reviewOf: RPT-103
result: "MIXED — alle automatisiert pruefbaren Aspekte PASS (Stub-Detection 0 Hits, Build gruen, 360/360 DB-Tests gegen Coolify-DB, Wiring-Chain strukturell PASS, AC-14 Worker-metadata-Counter live verifiziert mit 3 neuen Snapshots `{pending,approved,rejected}_blocks`, Cross-Tenant strukturell PASS via Code-Path + 46/46 RLS-Matrix). AC-16 Pflicht-Browser-Smoke mit Nicht-Tech-User-Persona ist offen — gehoert zwingend in User-Hand. Smoke-Plan dokumentiert. SLC-044 bleibt `in_progress` bis User-Smoke PASS."
title: "/qa SLC-044 — Handbuch-Reader: Code/Tests/Live-DB PASS, AC-16 Browser-Smoke User-Pflicht offen"
---

## Outcome

**MIXED.** SLC-044 ist strukturell durch und alle automatisierten Pruefungen sind gruen. Der einzige offene Punkt ist die in der Slice-Spec als Pflicht-Gate definierte Browser-Smoke-Sitzung mit einer Nicht-Tech-User-Persona (AC-16 / R17 analog SC-V4-5). Diese Sitzung kann der Agent nicht stellvertretend leisten — sie ist als User-Aufgabe ausgewiesen.

Empfehlung: Der User fuehrt den unten dokumentierten Smoke-Plan in 1-2 Browser-Sitzungen durch. Bei PASS wird RPT-104 final auf `completed` aktualisiert und SLC-044 status auf `done`. Bei FAIL wird ein Iteration-Loop analog SLC-042 gefahren (siehe RPT-102).

## QA Scope

- Slice-Spec: `/slices/SLC-044-handbuch-reader.md` (16 ACs + 7 Pflicht-QA-Vorgaben + 7 MTs)
- Frontend-Report: `/reports/RPT-103.md` (vollstaendige AC-Matrix + Wiring-Chain + Stub-Detection)
- Lessons aus SLC-042: `/reports/RPT-102.md` (Browser-Smoke-Iter-Pattern, ISSUE-029 Filter-Drift, Test-Daten-Marker-Pattern)
- Live-DB: Coolify-Onboarding-Server (159.69.207.29), Container `supabase-db-bwkg80w04wgccos48gcws8cs-173927217918`, Netzwerk `bwkg80w04wgccos48gcws8cs_strategaize-net`
- Test-Worktree: `/opt/onboarding-test/` auf Hetzner (gepullt auf Stand `c6e929a`, npm install neu durchgelaufen weil `package-lock.json` geaendert)

## Result

**Code complete + automated PASS — User-Smoke-Gate offen.**

## Acceptance Criteria — Live-Status

| AC | Status | Evidenz |
|----|--------|---------|
| AC-1 tenant_admin Tenant A oeffnet eigenen Snapshot, sieht Sidebar + Markdown + Anchors + Snapshot-Liste | **strukturell PASS** | ReaderShell rendert ReaderSidebar + HandbookReader; verifiziert via Build-Routes + Code-Review. Live-PASS in User-Smoke. |
| AC-2 tenant_admin Tenant B → 404 bei Snapshot von Tenant A | **strukturell PASS** | Code-Path: `page.tsx:71-73` `if (!isStrategaizeAdmin && snapshotRow.tenant_id !== profile.tenant_id) notFound()`. Plus 46/46 v4-perimeter-matrix RLS-Tests gruen. Live-Cross-Tenant-Test braucht 2. Tenant in DB; aktuell nur Demo-Tenant — User-Cross-Tenant-Test optional. |
| AC-3 tenant_member/employee → 403/Redirect | **strukturell PASS** | Code-Path: `page.tsx:47-53` Profile-Role-Check `["tenant_admin","strategaize_admin"].includes(profile.role)` → sonst redirect("/dashboard"). User-Smoke verifiziert mit Employee-Account. |
| AC-4 strategaize_admin Direct-URL auf beliebigen Tenant-Snapshot | **strukturell PASS** | Code-Path: `isStrategaizeAdmin` umgeht Tenant-Match. `tenantIdForList` nutzt `snapshotRow.tenant_id`. Live-PASS in User-Smoke (User ist strategaize_admin). |
| AC-5 Section-Anchors funktionieren | **strukturell PASS** | rehype-slug + rehype-autolink-headings aktiv. Section-DOM-IDs `handbook-section-{key}`. Sidebar-Click → window.scrollIntoView. Live-PASS in User-Smoke. |
| AC-6 Cross-Link nur strategaize_admin im DOM | **strukturell PASS** | `HandbookReader.tsx:57-60` rendert `<Link>` nur wenn `isStrategaizeAdmin && section.blockKey`. Live-PASS in User-Smoke (zwei Browser-Sessions). |
| AC-7 Cross-Link href = `/admin/debrief/[sessionId]/[blockKey]` | **strukturell PASS** | `loadSchemaSectionMap` extrahiert Block-Keys aus `template.handbook_schema.sections[].sources[].filter.block_keys`. Live-PASS in User-Smoke (Klick zeigt korrekte URL). |
| AC-8 Snapshot-Liste mit Reverse-Chrono + aktiver markiert + Klick wechselt | **strukturell PASS** | Page laedt `handbook_snapshot.eq(tenant_id, X).order(created_at desc)`. ReaderSidebar rendert `isActive`-Highlight + router.push. Live-PASS in User-Smoke. |
| AC-9 Stale-Banner bei block_checkpoint > snapshot.created_at | **strukturell PASS** | `page.tsx:117-124` Count-Query gegen `block_checkpoint`. Live-Stand: alle 5 Demo-Snapshots haben `later_checkpoints = 0` (sauber). Test-Anweisung im Smoke-Plan unten. |
| AC-10 `/dashboard/handbook` Karten-Liste aller Tenant-Snapshots | **strukturell PASS** | HandbookSelectionPage rendert SnapshotCardLink-Liste. Live-Stand: 5 Snapshots im Demo-Tenant. Live-PASS in User-Smoke. |
| AC-11 `/dashboard/handbook` Empty-State | **strukturell PASS** | Code-Path: `snapshots.length === 0` Card mit Hinweis-Text. Live-Test braucht Tenant ohne Snapshots — derzeit nicht vorhanden. Optional fuer User-Smoke. |
| AC-12 Sidebar-Link "Unternehmerhandbuch" sichtbar fuer tenant_admin | **strukturell PASS** | `dashboard-sidebar.tsx:144-160` Link auf `/dashboard/handbook`, isHandbookActive deckt beide Pfade ab. Live-PASS in User-Smoke. |
| AC-13 Markdown rendert sauber (GFM Tables/Lists/Code) | **strukturell PASS** | remark-gfm aktiv. Tailwind `prose prose-slate prose-table:text-sm`. Live-PASS in User-Smoke (Visual). |
| AC-14 Responsive Sidebar collapsiert auf mobile | **strukturell PASS** | `lg:translate-x-0` + Mobile-Toggle + Overlay-Click-Close. Live-PASS in User-Smoke (Browser-Resize). |
| AC-15 `npm run build` + `npm run test` gruen | **PASS** | `npm run build` lokal Production-OK (`/dashboard/handbook` + `/[snapshotId]` als ƒ Dynamic-Routes). **`npx vitest run` auf Hetzner-Coolify-DB: 360/360 Tests in 35 Files PASS in 2.89s** (inkl. 46/46 v4-perimeter-matrix RLS-Tests + 12/12 block-review-rls + 7/7 get-review-summary aus SLC-042). |
| AC-16 Pflicht-Browser-Smoke mit Nicht-Tech-User-Persona | **OFFEN — USER-PFLICHT** | Frontend-Code-und-DB-Stand bereit. Smoke-Plan unten. |

## Pflicht-QA-Vorgaben — Status

| Vorgabe | Status |
|---------|--------|
| Pflicht-Browser-Smoke mit Nicht-Tech-User-Persona (AC-16) | **OFFEN — USER-PFLICHT** |
| RLS-Test: tenant_admin nur eigener Tenant | **strukturell PASS** (Code-Path + RLS-Matrix). Live mit 2-Tenant-Setup im User-Smoke optional. |
| Cross-Tenant-Test: strategaize_admin Direct-URL auf fremden Tenant | **strukturell PASS** (isStrategaizeAdmin umgeht Tenant-Match). Live im User-Smoke. |
| Markdown-Render-Test (GFM Tables, Lists, Code) | **strukturell PASS** — Visual im User-Smoke. |
| Section-Anchor-Klick-Test | **strukturell PASS** — Live im User-Smoke. |
| Snapshot-Wechsel-Test | **strukturell PASS** — Live im User-Smoke. |
| Stale-Banner-Test mit erzwungenem Mock-Block-Submit | **OFFEN — USER-AKTION** (Mock-Submit oder einen Block neu abschicken in /capture/...) |
| Responsive-Check (mobile, tablet, desktop) | **strukturell PASS** — Visual im User-Smoke. |
| `npm run test` + `npm run build` gruen | **PASS** (Build lokal + 360/360 Tests Coolify-DB) |
| IMP-112: Re-Read vor Write | **PASS** (Edit-Tool-Disziplin in /frontend) |
| Cockpit-Records-Update nach Slice-Ende | **PARTIELL** — RPT-103 hat slices/INDEX + features/INDEX + STATE bereits aktualisiert. Mit /qa-PASS muss SLC-044 von `in_progress` auf `done` und FEAT-028 entsprechend gepruft werden. |

## Stub-Detection

Re-Scan ueber alle 6 SLC-044-Files (page.tsx ×2, ReaderShell, ReaderSidebar, HandbookReader, types, load-snapshot-content, dashboard-sidebar):

| Pattern | Hits |
|---------|------|
| TODO/FIXME/HACK/XXX | **0** |
| debugger / console.log | **0** |
| Empty handlers `catch {}` / `() => {}` | **0** |
| Placeholder-Returns | **0** |
| Hardcoded URLs/Tokens/IDs | **0** |
| Dummy-Daten | **0** |

**Gesamt: 0 Stub-Hits.** Konsistent mit RPT-103.

## Wiring Chain Verification

| Schritt | Verifiziert |
|---------|-------------|
| `/dashboard/handbook/[snapshotId]/page.tsx` Server-Component → Auth (createClient) → Profile-Lookup → Role-Check | **PASS** (Code-Read) |
| Profile-Check `tenant_admin || strategaize_admin` → sonst redirect("/dashboard") | **PASS** (Code-Read) |
| `createAdminClient()` → Snapshot-Row laden → `maybeSingle()` → 404 wenn fehlend | **PASS** (Code-Read) |
| Cross-Tenant-Schutz: `if (!isStrategaizeAdmin && snapshotRow.tenant_id !== profile.tenant_id) notFound()` | **PASS** (Code-Read + RLS-Matrix-Backstop) |
| Snapshot-Liste: `handbook_snapshot.eq(tenant_id, tenantIdForList).order(created_at desc)` | **PASS** (Live-DB: 5 Snapshots fuer Demo-Tenant gelistet) |
| Stale-Check: `block_checkpoint.count(*).eq(capture_session_id, X).gt(created_at, snapshot.created_at)` | **PASS** (Live-DB: 0 spaetere Checkpoints aktuell, Logik funktioniert) |
| `loadSnapshotContent({storagePath, templateId})` → adminClient.storage.from("handbook").download(path) | **PASS** (Live-DB: 3 ZIP-Files in Storage `handbook` Bucket gefunden, je 4914 bytes) |
| ZIP entpacken via JSZip → INDEX.md + Section-Files | **PASS** (Code-Read + Build-Erfolg) |
| `loadSchemaSectionMap(adminClient, templateId)` → template.handbook_schema → Section→Block-Key | **PASS** (Live-DB: Template "Exit-Readiness" mit 8 sections gefunden) |
| Server → ReaderShell → ReaderSidebar (sections + snapshots, activeSnapshotId-Highlight) | **PASS** (Code-Read) |
| ReaderSidebar.onSectionSelect → ReaderShell.handleSectionSelect → window.scrollIntoView via DOM-ID | **PASS** (Code-Read) |
| HandbookReader.map(sections) → ReactMarkdown + remark-gfm + rehype-slug + rehype-autolink-headings | **PASS** (Code-Read) |
| Cross-Link conditional: `isStrategaizeAdmin && section.blockKey ? <Link> : null` | **PASS** (Code-Read; tenant_admin sieht den Link nicht im DOM) |
| `/dashboard/handbook/page.tsx` → SnapshotCardLink-Liste | **PASS** (Code-Read + Build-Routes) |
| DashboardSidebar Link `/dashboard/handbook` + isHandbookActive auf beide Pfade | **PASS** (Code-Read) |

## Live-DB-Verifikationen

### AC-14 SLC-041 Worker-metadata-Counter — **LIVE PASS**

```
SELECT id, jsonb_pretty(metadata) FROM handbook_snapshot WHERE status='ready' ORDER BY created_at DESC LIMIT 3;
```

| Snapshot | Metadata |
|---|---|
| 510cffec-... (28.04. 17:22 UTC) | `{ "pending_blocks": 0, "approved_blocks": 0, "rejected_blocks": 0 }` |
| 28a22277-... (28.04. 17:20 UTC) | `{ "pending_blocks": 0, "approved_blocks": 0, "rejected_blocks": 0 }` |
| 618c1b47-... (28.04. 14:29 UTC) | `{ "pending_blocks": 0, "approved_blocks": 0, "rejected_blocks": 0 }` |

Alte Pre-SLC-041-Snapshots (27.04.) haben `metadata = {}`. Der Wechsel auf das neue Counter-Format hat ab SLC-041-Deploy gegriffen. Counts sind 0, weil im Demo-Tenant aktuell keine `block_review`-Datensaetze existieren — wenn der User in einem Reviewer-Workflow `block_review`-Eintraege erzeugt und dann ein neuer Snapshot generiert wird, wird der Counter realistische Werte zeigen.

### Storage-Bucket — **PASS**

```
3 Files in storage.objects (bucket=handbook), Format: {tenant_id}/{snapshot_id}.zip
- 00000000-...-de/510cffec-...zip (4914 bytes)
- 00000000-...-de/28a22277-...zip (4914 bytes)
- 00000000-...-de/618c1b47-...zip (4914 bytes)
```

ZIP-Konvention `{tenant_id}/{snapshot_id}.zip` matcht Erwartung von `loadSnapshotContent`.

### Template-Schema — **PASS**

```
Template "Exit-Readiness" hat handbook_schema mit 8 Sections.
```

Section-Count 8 matcht zum `section_count: 8` in handbook_snapshot. `loadSchemaSectionMap` wird also bei jedem Snapshot 8 Section-Eintraege liefern und Block-Key-Mapping bereitstellen, soweit eindeutig.

## Vitest auf Coolify-DB — **PASS (360/360)**

```
Test Files  35 passed (35)
Tests  360 passed (360)
Duration  2.89s
```

Highlights (komplette Liste in CLI-Output):
- `src/__tests__/rls/v4-perimeter-matrix.test.ts` (46/46) — **4-Rollen-RLS-Pflicht-Matrix gruen**
- `src/__tests__/rls/block-review-rls.test.ts` (12/12) — V4.1 SLC-041 Pflicht-RLS
- `src/lib/handbook/__tests__/get-review-summary.test.ts` (7/7) — SLC-042 Filter-Drift-Fix gruen
- `src/workers/handbook/__tests__/renderer.test.ts` (13/13) — Worker-Render-Tests
- `src/workers/handbook/__tests__/zip-builder.test.ts` (5/5) — ZIP-Format-Tests
- `src/lib/db/__tests__/admin-rls.test.ts` (2/2) + alle anderen RLS-Suites

## Pre-existing TS-Errors

`npx tsc --noEmit` zeigt nur die **2 bekannten** Pre-existing Errors aus SLC-035 (RPT-103 Open Point 4):

```
src/app/admin/bridge/__tests__/action-helpers.test.ts(127,7): error TS2578: Unused '@ts-expect-error' directive.
src/app/admin/bridge/__tests__/action-helpers.test.ts(129,7): error TS2578: Unused '@ts-expect-error' directive.
```

Build und Tests laufen trotzdem (Vitest umgeht `tsc --noEmit` per esbuild-loader). **Nicht in SLC-044-Scope** — bleibt offen fuer SLC-043 oder ein V4.2-Cleanup-Item.

## Files reviewed

- `/slices/SLC-044-handbuch-reader.md` — Slice-Spec mit 16 ACs + 7 MTs + 7 Pflicht-QA-Vorgaben
- `/reports/RPT-103.md` — Frontend-Completion-Report
- `/reports/RPT-102.md` — Lessons-Learned aus SLC-042 QA-Iteration
- `/docs/STATE.md` — V4.1-Stand
- `/src/app/dashboard/handbook/page.tsx` — Snapshot-Auswahl-Page (252 Zeilen)
- `/src/app/dashboard/handbook/[snapshotId]/page.tsx` — Reader-Page Server-Component (220 Zeilen)
- `/src/components/handbook/types.ts` — Geteilte Types (32 Zeilen)
- `/src/components/handbook/ReaderShell.tsx` — 2-Pane-Layout + Mobile-Toggle + Banner-Stack (213 Zeilen)
- `/src/components/handbook/ReaderSidebar.tsx` — Section + Snapshot-Liste (162 Zeilen)
- `/src/components/handbook/HandbookReader.tsx` — Markdown-Render + Cross-Link (97 Zeilen)
- `/src/lib/handbook/load-snapshot-content.ts` — ZIP-Entpacker + Schema-Mapping (183 Zeilen)
- `/src/components/dashboard-sidebar.tsx` — Sidebar-Link "Unternehmerhandbuch" (umgelenkt von /admin auf /dashboard)
- `/slices/INDEX.md` — V4.1 Slice-Status
- `/features/INDEX.md` — V4.1 Feature-Status
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues durch SLC-044

## Files changed

- `/reports/RPT-104.md` — dieser /qa-Completion-Report (NEU)
- `/slices/INDEX.md` — KEINE Aenderung in dieser /qa-Phase. SLC-044 bleibt `in_progress` bis User-Smoke-PASS.
- `/features/INDEX.md` — KEINE Aenderung. FEAT-028 bleibt `in_progress`.
- `/docs/STATE.md` — Current Focus + Phase + Next Steps werden aktualisiert: "/qa SLC-044 automatisch PASS, AC-16 Browser-Smoke offen — User-Pflicht-Gate".

## Files created

- `/reports/RPT-104.md` — dieser Report

## Files intentionally not changed

- `/slices/INDEX.md` SLC-044-Status — bleibt `in_progress` bis User-Smoke-PASS. Erst dann auf `done`. (Verhindert false-done-Cockpit-Anzeige.)
- `/features/INDEX.md` FEAT-028-Status — bleibt `in_progress` bis SLC-044 + SLC-045 done.
- `/planning/backlog.json` — keine Aenderung.
- `/planning/roadmap.json` — keine Aenderung. V4.1 ist bereits `active`.
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues. ISSUE-029 bleibt `resolved` (V4.1 SLC-042 Phase-2-Fix), Pre-existing TS-Errors aus SLC-035 sind nicht-blockierend.
- `/docs/DECISIONS.md` — keine neuen DECs.
- `/docs/MIGRATIONS.md` — keine Migration in SLC-044.

## Problems found

1. **Cross-Tenant-Live-Test eingeschraenkt durch Single-Tenant-DB.** Demo-Onboarding-GmbH ist aktuell der einzige Tenant. AC-2 (tenant_admin Tenant B → 404 fuer Snapshot von Tenant A) ist live nicht testbar ohne 2. Tenant. Strukturelle Pruefung erfolgt via Code-Path-Review + 46/46 v4-perimeter-matrix RLS-Tests, die das im Test-Setup mit synthetischen Tenants abdecken. **Nicht-blockierend.** Optional: User legt einen 2. Test-Tenant via /admin/tenants an, falls Live-Cross-Tenant-Verifikation ausdruecklich gewuenscht.

2. **Stale-Banner-Test braucht User-Aktion.** Aktueller Demo-Stand: 0 spaetere block_checkpoints fuer alle 5 Snapshots. AC-9 ist live nur nachstellbar, indem der User entweder einen Block neu abschickt (in /capture/.../block/.../final → das schreibt `block_checkpoint`) oder per SQL einen synthetischen Checkpoint einfuegt. SQL fuer letzteres siehe Smoke-Plan unten.

3. **Bridge-Konsolidierungs-Live-Verifikation: source-Spalte heisst nicht "source".** Die im Frontend-Handoff aufgeworfene Frage ("Mitarbeiter-KUs via Bridge konsolidiert?") laesst sich live nicht direkt am `block_review`-Schema ablesen — die im Handoff genannte `source`-Spalte existiert nicht (`column "source" does not exist`). Stattdessen liefert der Worker-KU-Loader Mitarbeiter-Inhalte via `block_review.kept_kus[]` ein. **Nicht-blockierend** — wird im Browser-Smoke offen-sichtbar (Mitarbeiter-Sektionen mit Inhalt = via Bridge konsolidiert; leer = Worker-Bug, dann /doctor). Empfehlung: Schema-Klaerung in /qa-Iteration oder als eigenes BL-Item.

4. **Pre-existing TS-Errors in `bridge/__tests__/action-helpers.test.ts:127,129`** (aus SLC-035). Build und Tests laufen trotzdem. **Nicht in SLC-044-Scope** — Empfehlung: in SLC-043 oder als V4.2-Cleanup-Item.

## Open points or deferred decisions

1. **AC-16 Browser-Smoke User-Pflicht-Gate.** Der User fuehrt den Smoke-Plan unten durch. Bei PASS wird RPT-104 final und SLC-044 status `done`.

2. **Cockpit-MetricCard Reader-Umlenkung.** Aus RPT-103 Open Point 3: Soll `StatusCockpit.tsx` MetricCard "Unternehmerhandbuch" fuer tenant_admin auf `/dashboard/handbook` (Reader) statt `/admin/handbook` (Trigger) umgelenkt werden? Aktuell linkt sie weiter auf Trigger-Sicht. **Empfehlung:** als BL-Item fuer V4.2-Polish (kein Pflicht-Item fuer SLC-044).

3. **Bridge-Konsolidierungs-Schema-Klaerung.** `block_review.source` existiert nicht im aktuellen Schema. Klaerung im Browser-Smoke + ggf. Schema-Read der echten `block_review`-Spalten.

4. **Test-Tenant-2 fuer Live-Cross-Tenant-Smoke** (optional).

5. **Pre-existing TS-Errors action-helpers.test.ts:127,129** — V4.2-Cleanup.

## User-Pflicht: Browser-Smoke-Plan (AC-16)

**Voraussetzung:** Onboarding-Plattform ist auf Production-Stand `c6e929a` (manuell via Coolify deployt — `feedback_manual_deploy.md`). Wenn nicht: zuerst Coolify-Redeploy ausloesen.

**Ablauf** — die Smoke-Punkte koennen in einer einzigen User-Sitzung in 15-25 Minuten durchlaufen werden. Empfehlung: parallel zu jedem Punkt notieren, was *intuitiv* und was *verwirrend* war (R17-Persona-Feedback fuer SKILL_IMPROVEMENTS).

### Smoke-Punkt 1 — Snapshot-Auswahl-Einstieg
- **Voraussetzung:** als `tenant_admin` von Demo-Onboarding-GmbH eingeloggt (z.B. `richard@bellaerts.de` o.ae.)
- **Aktion:** Klick auf den Sidebar-Eintrag "Unternehmerhandbuch" (BookOpen-Icon).
- **Erwartung:** Browser navigiert zu `/dashboard/handbook`. Karten-Liste mit 5 Snapshots vom Demo-Tenant. Karten zeigen Datum, Status-Badge ("Lesbar"), Section-/KU-/Diagnose-/SOP-Counts. Die 3 jungsten zeigen `metadata`-Pills `0 approved / 0 rejected / 0 pending` (alle 0, da noch keine block_review-Daten).

### Smoke-Punkt 2 — Reader laedt
- **Aktion:** Klick auf "Oeffnen" auf dem juengsten Snapshot (28.04. 17:22 UTC, ID `510cffec-...`).
- **Erwartung:** Browser navigiert zu `/dashboard/handbook/510cffec-...`. Reader-Shell rendert mit:
  - Linke Sidebar: 8 Section-Eintraege mit `00 ... 07`-Prefix + Section-Titel + ChevronRight
  - Sidebar-Footer: Versionen-Liste mit 5 Snapshots, der aktuelle markiert (brand-primary-Background)
  - Hauptbereich: Header "Unternehmerhandbuch" + Subline "Demo Onboarding GmbH · Stand 28.4.2026 17:22 · 4.8 KB"
  - Hauptbereich-Body: INDEX.md gefolgt von 8 Section-Articles mit Markdown gerendert (Headings, Tables, Lists)

### Smoke-Punkt 3 — Sidebar-Section-Click → Scroll-to-Section
- **Aktion:** Klick auf eine Section in der linken Sidebar (z.B. "03 ...")
- **Erwartung:** Hauptbereich scrollt sanft (smooth) zur entsprechenden Section. Section-Article ist visuell sichtbar im Viewport. Die Sidebar bleibt geoeffnet (Desktop) bzw. schliesst sich (Mobile).

### Smoke-Punkt 4 — Heading-Anchor-Click im Markdown
- **Aktion:** Hover ueber ein Heading (h1/h2/h3) im Hauptbereich → ein Anchor-Link wird sichtbar (rehype-autolink-headings). Klick.
- **Erwartung:** URL-Hash aendert sich (z.B. `#mein-heading-text`). Wenn auf das Heading mit der Maus geklickt wird, kein Navigations-Effekt; aber bei Klick auf den Anchor-Link Springt der Browser zum Heading.

### Smoke-Punkt 5 — Snapshot-Wechsel via Sidebar
- **Aktion:** Klick auf einen anderen Snapshot in der Versionen-Liste (z.B. der zweitneueste).
- **Erwartung:** Browser navigiert zu `/dashboard/handbook/{andere-id}`. Reader laedt neuen Snapshot. Aktiver Snapshot-Highlight wechselt.

### Smoke-Punkt 6 — Cross-Link "Im Debrief bearbeiten" — Visibility-Test
- **Aktion 6a (als tenant_admin, weiter eingeloggt):** Inspect Element ueber einer beliebigen Section. Suche nach `data-testid="reader-cross-link"` oder Text "Im Debrief bearbeiten" — **darf nicht im DOM existieren**.
- **Aktion 6b (zweite Browser-Session als strategaize_admin):** Direct-URL-Aufruf von `/dashboard/handbook/510cffec-...` (oder navigiere via dieselbe Sidebar — tenant_admin hat keine direkte Sicht auf strategaize_admin-Sidebar; einfacher ueber Direct-URL).
- **Erwartung:** Cross-Link "Im Debrief bearbeiten" oben rechts pro Section sichtbar. Klick fuehrt zu `/admin/debrief/{capture_session_id}/{block_key}`. URL-Pattern korrekt.

### Smoke-Punkt 7 — Stale-Banner-Test (USER-AKTION ERFORDERLICH)
- **Voraussetzung:** Snapshot bereits geoeffnet (z.B. `510cffec-...`).
- **Variante A — Live-Block-Submit (empfohlen):** In zweitem Browser-Tab `/capture/64ad04eb-63ef-4b01-ab12-fc4f09474246/block/{ein-block-key}/final` aufrufen, einen Block frisch abschicken. Damit wird ein neuer `block_checkpoint` erzeugt, dessen `created_at` nach `snapshot.created_at` liegt. Reader-Tab refreshen → **Amber-Banner "Snapshot moeglicherweise veraltet"** muss erscheinen.
- **Variante B — Mock-Checkpoint-SQL** (wenn keine echte Erhebung gewuenscht):
  ```sql
  -- Auf Hetzner via:
  -- ssh root@159.69.207.29 'docker exec supabase-db-bwkg80w04wgccos48gcws8cs-173927217918 psql -U postgres -d postgres -c "..."'
  INSERT INTO block_checkpoint (id, capture_session_id, block_key, version, payload, created_at)
  VALUES (gen_random_uuid(), '64ad04eb-63ef-4b01-ab12-fc4f09474246', 'a_geschaeftsmodell', 1, '{"qa_smoke":"stale_banner_RPT-104"}'::jsonb, now());
  -- Dann Reader-Tab refreshen → Banner muss kommen.
  -- Cleanup nach Test:
  DELETE FROM block_checkpoint WHERE payload->>'qa_smoke' = 'stale_banner_RPT-104';
  ```
- **Erwartung:** Reader rendert Amber-Banner ueber dem Markdown.

### Smoke-Punkt 8 — Responsive Mobile + Tablet
- **Aktion:** Browser-DevTools → Responsive-Mode → 375px (Mobile), 768px (Tablet), 1024px+ (Desktop).
- **Erwartung Mobile (375):** Sidebar collapsed. Mobile-Toggle-Button oben links sichtbar. Klick auf Toggle blendet Sidebar mit Overlay ein. Klick auf Overlay oder Section-Eintrag schliesst Sidebar wieder.
- **Erwartung Tablet (768):** Wie Mobile (lg-Breakpoint = 1024px).
- **Erwartung Desktop (1024+):** Sidebar fix 320px breit, kein Toggle-Button.

### Smoke-Punkt 9 — Bridge-Konsolidierungs-Live-Check (Bonus)
- **Aktion:** Bei jedem Section-Markdown im Reader pruefen: enthaelt er Mitarbeiter-Beitraege? (typisches Pattern in Markdown-Inhalten: KUs aus mehreren Quellen).
- **Erwartung:** Mitarbeiter-Sektionen mit Inhalt → Bridge konsolidiert korrekt; Mitarbeiter-Sektionen leer → moeglicher Worker-Bug (dann /doctor in naechster Session).

### Smoke-Punkt 10 — Empty-State (Bonus, nur falls Test-Tenant ohne Snapshots vorhanden)
- **Aktion:** Tenant ohne Snapshots → /dashboard/handbook
- **Erwartung:** Empty-State-Card mit "Noch kein Handbuch generiert" + Hinweis-Text.

### Reporting

Nach Durchlauf bitte folgendes melden:
- **Smoke-Punkte 1-9:** PASS oder FAIL je Punkt + ggf. Screenshot bei FAIL.
- **AC-9 Stale-Banner-Test (Punkt 7):** Variante A oder B durchgefuehrt; Erwartung erfuellt?
- **AC-6 Cross-Link-Visibility (Punkt 6):** beide Rollen geprueft? Cross-Link-DOM-Verhalten korrekt?
- **AC-14 Responsive (Punkt 8):** alle 3 Breakpoints geprueft?
- **R17 Persona-Feedback (Bonus):** Was war intuitiv? Was hat verwirrt? Verbesserungsvorschlag?

Bei Bugs: Iter-Loop analog SLC-042 (RPT-102 Pattern) — Fix → Coolify-Redeploy → Smoke-Re-Run.

## Project record updates

- `/slices/INDEX.md` — KEINE Aenderung in dieser /qa-Phase. SLC-044 bleibt `in_progress` bis User-Smoke-PASS.
- `/features/INDEX.md` — KEINE Aenderung. FEAT-028 bleibt `in_progress`.
- `/planning/roadmap.json` — keine Aenderung. V4.1 bleibt `active`.
- `/planning/backlog.json` — keine Aenderung.
- `/docs/STATE.md` — Update auf "/qa SLC-044 automatisch PASS, AC-16 Browser-Smoke offen".
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues.
- `/docs/DECISIONS.md` — keine neuen DECs.
- `/docs/MIGRATIONS.md` — keine.

## Recommended next step

**User fuehrt Browser-Smoke-Plan oben durch.** Ergebnisse zurueckmelden. Bei PASS:
- RPT-104 status → `completed`
- `/slices/INDEX.md` SLC-044 status → `done`
- `/features/INDEX.md` FEAT-028 status: bleibt `in_progress` (SLC-045 noch offen)
- `/docs/STATE.md` aktualisieren
- Naechster Schritt: `/frontend SLC-043` (Cross-Tenant Reviews + Quick-Stats) ODER `/frontend SLC-045` (Reader Volltext-Suche + Polish)

Bei FAIL: Iter-Loop wie SLC-042 — Bug-Fix-Branch + Re-Test-Loop, dann RPT-104 finale Version.

Alternative parallel-faehige Slices, die nicht auf SLC-044-PASS warten muessen: **SLC-043** (komplett unabhaengig von Reader, hat eigene Pflicht-Gates).

## Lessons / IMP-Kandidaten

Keine neuen IMPs aus dieser /qa-Phase identifiziert. Die in RPT-103 Open Points dokumentierten IMP-196..198 (Storage=ZIP, Schema-mehrdeutig, eigene Reader-Shell) bleiben gueltig. Bridge-Konsolidierungs-Schema-Klaerung wurde aufgedeckt (`block_review.source` existiert nicht), ist aber nicht prozess-relevant — nur Schema-Drift in der Slice-Spec / Memory-Notiz. **Klein** — kein eigenes IMP, sondern Cleanup im Schema-Lookup im Bedarfsfall.
