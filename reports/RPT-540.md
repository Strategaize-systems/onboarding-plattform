---
id: RPT-540
date: "2026-06-29"
skill: /deploy
slice: V10-Gesamt
feature: FEAT-092
type: completion
status: completed
result: "V10 StB-Vertikale DEPLOYED to production (internal Workflow-Closure, feature behind OFF env-gate). Founder lifted push/Coolify-hold + corrected the Bedrock model env. Sequence: MIG-127 live-applied+verified -> merge v10-slc170b-blueprint-seed->main (9df7340 --no-ff)+push -> Coolify manual redeploy app+worker. Post-deploy verified: SOURCE_COMMIT=9df7340 on app+worker, all model envs = eu.anthropic.claude-sonnet-4-20250514-v1:0 (ISSUE-099 resolved), NEXT_PUBLIC_ENABLE_STB_VERTIKALE OFF, containers healthy, app /login HTTP 200, worker module_output_synthesis handler registered + no model errors, modul_output RLS enabled+3 policies. Deferred (gated, not a defect): end-to-end StB worker LLM run + SLC-172/175 browser-smoke + behavioral RLS cross-tenant sidecar. REL-036 recorded. NEXT /post-launch V10 (T+24h)."
title: "Deploy V10 (StB-Vertikale) — DEPLOYED (OFF-gated, internal Workflow-Closure)"
---

# Deploy — OP V10 StB-Vertikale

**Outcome: DEPLOYED to production.** Founder lifted the push/Coolify-hold and corrected the Bedrock model env (2026-06-29). V10 code is live behind the OFF env-gate (Internal-Test-Mode, `module-lifecycle-discipline` — no customer-outreach). One prerequisite blocker (ISSUE-099) was found during recon and fixed before redeploy. Deferred items are gated live-validation, not defects.

## Phase 1 — Prerequisites (assessed)
Update deploy of a running stack (Next.js app + worker + Supabase/Kong on Hetzner/Coolify `159.69.207.29`). Prereqs server/DNS/SSL already satisfied (V9.8 live). Read-only recon found:
- Containers healthy (app/worker/kong/auth/db, up 8d).
- Deployed commit was `f3b2ed6` (V9.8); `origin/main` had advanced to `e991ab3` (SLC-171) without redeploy → **Coolify deploy is manual, not auto-on-push.**
- MIG-126 (`stb_blueprint_kanzlei`), MIG-124 (`modul_output`+`rpc_enqueue_module_output`) live; MIG-127 not applied (expected).
- **BLOCKER (ISSUE-099):** `LLM_MODEL` / `BEDROCK_V9_SONNET_MODEL_ID` / `BEDROCK_V9_HAIKU_MODEL_ID` were raw (`claude-sonnet-4-20250514-v1:0`, no `eu.anthropic.` prefix) → would break V10 synthesis + diagnosis LLM calls. Founder corrected all three in Coolify UI to `eu.anthropic.claude-sonnet-4-20250514-v1:0`.

## Phase 2 — Execution
Pre-Merge-Re-Check (git-release.md): rebase no-op (main strict ancestor of branch), tests covered by final-check (tsc 0 / eslint 0 / vitest 122/122 / next build EXIT 0), no MIG-number collision (124–127 unique), no pattern drift, manual diff scope = StB surface + migrations + i18n only. **PASS.**

1. **MIG-127 live-applied** via ssh-stdin → `psql -U postgres` (no paste-truncation). Verified: `rpc_seed_blueprint_diagnosis_input` present, `block_checkpoint_checkpoint_type_check` includes `blueprint_diagnosis_seed`, `GRANT EXECUTE … service_role`. (MIG before code redeploy, per ordering.)
2. **Merge** `v10-slc170b-blueprint-seed` → `main` `9df7340 --no-ff`; **push** `e991ab3..9df7340` to `origin/main` (34 commits, incl. unpushed SLC-173/174 from the hold).
3. **Coolify manual redeploy** app+worker (Founder-triggered).

## Phase 2 — Post-deploy verification
- New `app-…104639029998` + `worker-…104639052590`, both **(healthy)**, `SOURCE_COMMIT=9df7340` on both.
- **ISSUE-099 fixed:** `LLM_MODEL` + `BEDROCK_V9_SONNET_MODEL_ID` + `BEDROCK_V9_HAIKU_MODEL_ID` = `eu.anthropic.claude-sonnet-4-20250514-v1:0` on both containers; worker boot log: `Environment validated`, `LLM_MODEL=eu.anthropic.claude-sonnet-4-20250514-v1:0`, `module_output_synthesis handler registered`, no `invalid model identifier`.
- `NEXT_PUBLIC_ENABLE_STB_VERTIKALE` absent → fail-closed **OFF** (Internal-Test-Mode).
- App `/login` external **HTTP 200**.
- `modul_output` RLS **enabled** + 3 policies (`admin_full`, `tenant_admin_update`, `tenant_read`).

## Deferred live-validation (gated — not defects)
The StB UI is intentionally OFF-gated and no production StB jobs exist to exercise, so these run when the feature is deliberately enabled or via a test-tenant:
- End-to-end StB worker LLM run (`module_output_synthesis` + `diagnosis_generation` producing valid output, now that the model env is correct).
- SLC-172/175 live browser-smoke (reader/wizard render).
- Behavioral RLS cross-tenant sidecar (node:20 SAVEPOINT, `coolify-test-setup.md`) — static policy presence verified; behavioral run deferred.

## Records updated
- `/slices/INDEX.md` — SLC-169/170/171/172/173/174/175 → `deployed` (SLC-170b stays `in_progress`, content-gated).
- `/features/INDEX.md` — FEAT-090/092/093/094/095 → `deployed` (FEAT-091 stays `in_progress`).
- `/planning/roadmap.json` — V10 `active` → `released`.
- `/docs/RELEASES.md` — **REL-036** added.
- `/docs/KNOWN_ISSUES.md` — **ISSUE-099 → resolved** (env fix verified in both containers).
- `/docs/MIGRATIONS.md` — MIG-127 → LIVE-applied 2026-06-29.
- `/docs/STATE.md` — High-Level State `deploying`, Current Focus/Phase/Next → deployed, NEXT `/post-launch V10`.

## Problems found / Phase-3 learnings
- **ISSUE-099 was active in production** (raw Bedrock model env overriding the correct code default). Fixed. Lesson: the eu-Inference-Profile prefix (`eu.anthropic.`) must be enforced for `LLM_MODEL` + `BEDROCK_V9_*` envs cross-repo; a raw override silently shadows the correct compose/code default.
- **Coolify injects resource-level env vars** (e.g. `BEDROCK_V9_*`) into services even when not declared in the service `environment:` block; only `LLM_MODEL` is in docker-compose. Verified both reach app+worker post-redeploy.
- **Coolify deploy is manual** for this resource (push does not auto-deploy). 
- These dev-system/playbook learnings (eu-prefix env guard, Coolify env-injection, manual-deploy) are **deferred to the dev-system Self-Hosted Playbook / IMP** per the active coordination-hold on the dev-system repo (immoscheckheft still holds it); captured here in the meantime.

## Next step
**`/post-launch V10`** (T+24h burn-in): containers healthy/no restarts, worker/cron alive, error_log clean, no regression. On PASS → Last-Stable = V10. No customer-outreach (`module-lifecycle-discipline`).
