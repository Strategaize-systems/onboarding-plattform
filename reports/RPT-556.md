---
id: RPT-556
date: "2026-07-04"
skill: /backend
slice: SLC-181
feature: FEAT-098
type: completion
status: completed
reviewOf: null
result: "SLC-181 SOP-Brücke /backend Code-Side komplett (DEC-256: standard+impl.schritt→sop, dünne MIG-130-Provenance). 4 neue Files, Legacy src/workers/sop/* unberührt. tsc0/eslint0/vitest 139 pass (+9)/next build EXIT0. MIG-130 nicht live (=/deploy). NEXT /qa SLC-181 → Gesamt-QA V10.1."
title: "SLC-181 SOP-Brücke (modul_output → sop) — /backend"
---

# SLC-181 — SOP-/Handbuch-Brücke (/backend)

## Outcome
Code-Side vollständig, letzter V10.1-Slice. Dünne Brücke: accepted Prozess-`modul_output` → bestehende `sop`-Tabelle, idempotent über MIG-130-Provenance. Legacy `src/workers/sop/*` unberührt (DEC-253/D). MIG-130 als Datei erstellt, noch NICHT live appliziert (=/deploy).

## Founder-Kontrakt (F-D → DEC-256, 2026-07-04)
- **Auswahl:** nur `standard` + `implementierungsschritt` (status=accepted) → SOP; `entscheidung`+`ki_hebel` draußen.
- **Scoring:** Modul-Reife (SLC-178-Reuse) = Prioritäts-Annotation in `sop.content` (green→normal, sonst high), FILTERT NICHT.
- **Persistenz:** dünne MIG-130 (`sop.source_modul_output_id`) statt reiner Reuse → idempotenter Re-Run + Provenance.

## Backend scope implementiert
- **MIG-130** (`sql/migrations/130_v101_sop_bridge_provenance.sql`): additive nullable FK-Spalte `source_modul_output_id` (→ modul_output, ON DELETE SET NULL) + non-partieller UNIQUE-Index (NULLs DISTINCT → Legacy-Rows unbeschränkt/unberührt). Idempotent. **Nicht live** (Live-Apply /deploy).
- **MT-1** `sop-bridge.ts` (pure `mapModulOutputsToSopRows`): Auswahl-Kontrakt (Kinds + accepted + checkpoint-present) + Scoring-Annotation → sop-Row-Objekte. Hermetisch, 9 Tests. **AC-181-1/AC-181-2.**
- **MT-2** `sop-bridge-action.ts` (Server-Action `bridgeModulOutputsToSop`, on-demand pro Modul): auth → Rolle (`tenant_admin`/`strategaize_admin`) → Session-Ownership → RLS-Reads (modul_output + block_checkpoint für block_key + template für Reife-Flags) → `computeModulReifeAmpel` (SLC-178-Reuse) → `mapModulOutputsToSopRows` → **service_role**-Upsert `ON CONFLICT (source_modul_output_id) DO NOTHING` (`sop` hat keine authenticated-INSERT-Policy). **AC-181-3** (RLS gewahrt, kein neuer BYPASSRLS außer dem nötigen service_role-Write).

## Files
**Created:**
- `/sql/migrations/130_v101_sop_bridge_provenance.sql` — MIG-130.
- `/src/lib/stb-vertikale/module-delivery/sop-bridge.ts` — pure Auswahl+Mapping.
- `/src/lib/stb-vertikale/module-delivery/sop-bridge.test.ts` — 9 Tests.
- `/src/lib/stb-vertikale/module-delivery/sop-bridge-action.ts` — Server-Action.

**Changed (records):** `/slices/INDEX.md`, `/features/INDEX.md`, `/planning/backlog.json`, `/docs/STATE.md`, `/docs/DECISIONS.md` (DEC-256), `/docs/MIGRATIONS.md` (MIG-130), `/reports/RPT-556.md`.

**Intentionally untouched:** `/src/workers/sop/*` (handle-sop-job.ts, sop-prompt.ts, types.ts) — DEC-253/D verbietet Legacy-Änderung; git-verifiziert 0 Änderung.

## Migrations / data model
MIG-130 (1 nullable Spalte + 1 Index auf `sop`). Kein neuer Table, keine RLS-/Policy-Änderung. Zwei SOP-Erzeugungspfade koexistieren bewusst (R-181-1, Konsolidierung spätere Version).

## Gates
- tsc `--noEmit`: **EXIT 0** · eslint (3 neue Files): **EXIT 0**
- vitest `stb-vertikale`: **139 pass / 15 Files** (+9 sop-bridge). `sop-bridge.test.ts` 9/9.
- `next build`: **EXIT 0** (Placeholder-Supabase-Env; pre-existing supabaseUrl-Build-Debt in `/api/public/partner`).

## Problems found
- **MIG-130 nicht live** — DDL-Datei erstellt, Live-Apply via `sql-migration-hetzner.md` erst im /deploy (postgres-User, VOR Redeploy). Bis dahin schlägt der service_role-Upsert (`source_modul_output_id`-Spalte fehlt) real fehl — nur relevant sobald die Action live aufgerufen wird (Feature INERT/OFF, kein Aufrufer wired).
- **Kein UI-Aufrufer** — SLC-181 ist Backend-only (Server-Action). Ein UI-Trigger ("Modul-SOPs erzeugen") ist bewusst kein Scope dieses Slices (späterer Slice/Version). Die Action ist damit derzeit ungenutzt (kompiliert/gebaut, nicht verdrahtet).
- **DB-Sidecar-Test offen** — Idempotenz + RLS des sop-Upsert gegen echte DB braucht TEST_DATABASE_URL (node:20-Sidecar, coolify-test-setup.md) → /qa+/deploy.

## Open points
- `/qa SLC-181`: Stub-Scan, Wiring-Review (Auswahl/Persistenz), MIG-130-Idempotenz-Sidecar (falls im /qa DB verfügbar, sonst /deploy).
- Da SLC-181 der **letzte V10.1-Slice** ist: nach /qa SLC-181 folgt **Gesamt-QA V10.1** über alle Slices (176–181) vor /final-check.

## Recommended next step
`/qa SLC-181` (mandatory nach /backend). Danach — weil letzter Slice — **Gesamt-QA V10.1**, dann /final-check → /go-live → EIN Master-Merge (Pre-Merge-Re-Check 177→181; DEC-254-Kollision + MIG-129/130) → /deploy (MIG-129+130 Live-Apply) → /post-launch. Kein Push/Merge (Founder-Hold).
