---
id: RPT-642
date: "2026-07-10"
skill: /qa
slice: SLC-195
feature: FEAT-112
type: completion
status: completed
reviewOf: null
result: "SLC-195 V20 Auth/Secrets-Cleanup /qa PASS — alle 5 AC (Pure-Mock) + AC-195-6 verifiziert. 52/52 SLC-195-Tests grün, Full-Suite 619-fail(DB-env-gated)/2346-pass = 0 neue Regression, tsc0/eslint0, 0 Stubs, cron-Sweep-Gate 0 Treffer, slug .eq bestätigt. 0 Blocker/High/Medium. Optionaler Live-Login-Lockout-Smoke → Gesamt-QA/Founder-Selbsttest. NEXT = Gesamt-/qa V20."
title: "SLC-195 V20 Auth/Secrets-Cleanup — /qa PASS (5 AC Pure-Mock, letzter V20-Slice)"
---

# SLC-195 — V20 Auth/Secrets-Cleanup — /qa

## 1. QA scope reviewed
5 Micro-Tasks des letzten V20-Slice (kumulativer Worktree `v20-security-hardening`, HEAD `3f6b418`, Clean-Tree), pure Code / 0 Migration:
- MT-1 account-scoped Login-Lockout (P-081)
- MT-2 `verifyCronSecret` timingSafe-Helper + 7-Routen-Sweep
- MT-3 Logger-Redaction (`redactSecrets`, P-092-Port)
- MT-4 recording-ready timingSafe + Path-Traversal-Guard
- MT-5 partner-slug `.ilike`→`.eq`

Delivery Mode SaaS → volle Wiring-Chain- + Stub-Audit-Pflicht. Verifikations-Medium: Pure-Mock-Vitest (der Slice ist per Spec Pure-Mock-verifizierbar; keine neue UI-Interaction, kein neuer Live-Serving-Pfad).

## 2. Ergebnis: PASS (Pure-Mock LIVE ausgeführt)
Aggregiert über die 6 AC — 0 Blocker / 0 High / 0 Medium. Ein optionaler Live-Login-Lockout-Smoke gegen den deployten Stand ist per Spec Founder-Selbsttest und wird im Gesamt-/qa V20 / post-Deploy nachgezogen (kein Blocker, nichts von V20 ist gemerged/deployt).

### Per-AC-Verdicts
| AC | Inhalt | Verdict | Evidenz |
|---|---|---|---|
| AC-195-1 | Login-Lockout account-scoped, IP-Rotation wirkungslos, generische Fehlermeldung | PASS-LIVE (Pure-Mock) | `login/actions.test.ts` 4 grün: Lockout über rotierende IPs, `clear()` bei Erfolg, generische Message; `rate-limit.test.ts` peek/clear +12 grün. Optionaler Live-Behavioral-Smoke → Gesamt-QA/Founder. |
| AC-195-2 | `verifyCronSecret` timing-safe; Grep `!==`-CRON_SECRET in cron-Routes = 0 (alle 7 migriert) | PASS-LIVE | `cron-secret.test.ts` 5 grün (korrekt/falsch/Length-Mismatch/null-fail-closed); Done-Gate-Grep `!==.*CRON_SECRET` = **0**; `verifyCronSecret` in allen **7** Routes (bulk-email-retention-sweep, capture-reminders, email-bulk-pipeline-trigger, inbound-email-imap-sync, knowledge-embed-reconcile, pending-signup-cleanup, walkthrough-cleanup). |
| AC-195-3 | `redactSecrets` maskiert Secret-Keys rekursiv, logger nutzt es | PASS-LIVE | `redact.test.ts` 6 grün (Keys→[REDACTED], Nicht-Secrets intakt, verschachtelt, Circular); `logger.ts:30` wendet `redactSecrets(entry.metadata)` vor `error_log`-INSERT an. |
| AC-195-4 | recording-ready timing-safe + `../`-Traversal abgewiesen | PASS-LIVE | `recording-ready/route.test.ts` 7 grün: 401 (timingSafe via `verifyServiceKey`), 400 auf `..`/non-`.mp4`/outside-base, 400 fail-closed ohne `JIBRI_RECORDINGS_DIR`, 404 passed-guard. Guard: `path.resolve` + base-Prefix + `.mp4` + `..`-Reject. |
| AC-195-5 | partner-slug `.eq` (2 Routes), Wildcard wirkungslos | PASS-LIVE | `partner/__tests__/route.test.ts` 6 + `signup/__tests__/route.test.ts` 12 grün; `.eq("slug", …toLowerCase())` in beiden Routes; 0 aktive `.ilike`-Query (Rest-Treffer sind nur Kommentare). |
| AC-195-6 | tsc 0 / eslint 0 / Vitest 0 Regression + neue Tests grün | PASS-LIVE | `tsc --noEmit` EXIT 0; `eslint` (8 geänderte Files) EXIT 0; Full-Suite **619 failed / 2346 passed / 65 skipped** = exakter RPT-641-Baseline (619 = `TEST_DATABASE_URL`-env-gated), **0 neue Failures**. `next build` = RPT-641-Evidenz (nicht re-run; tsc-Clean + 0 Regression tragen). |

## 3. Findings nach Severity
Keine Blocker / High / Medium.

**Low / Beobachtung (nicht slice-verursacht, kein Handlungsbedarf jetzt):**
- `login/actions.ts:55` `catch`-Zweig gibt `Verbindungsfehler: ${msg}` an den Client — nur Connection-Exception-Pfad (kein Auth-Ergebnis, also kein Enumeration-Leak), Bestandsverhalten vor SLC-195. Kosmetisch; könnte künftig ebenfalls generisch werden. Kein Finding gegen die AC.
- MT-1 Tradeoff „DoS-by-lockout" (5 Fails sperren einen Account 15 min) ist der Standard-P-081-Tradeoff, in ISSUE-126-Resolution dokumentiert — akzeptiert.
- ISSUE-132 Teil-b (Regex-Scrub interpolierter Secrets in `message`/`stack`) bewusst deferred (nicht P-092-Kern, Low-Residual) — bereits in RPT-641 dokumentiert.

## 4. Stub- / Wiring-Audit
- **Stub-Scan** über alle 8 geänderten Impl-Files: 0 TODO/FIXME/HACK/`debugger`/empty-handler; 0 hardcoded Secret-Literale. Alle Rückgaben real.
- **Wiring-Chain** (Ende-zu-Ende):
  - `loginAccountLimiter`: peek (`actions.ts:32`) VOR signIn → `check()` bei Fehlversuch (`:46`) → `clear()` bei Erfolg (`:59`). Production-Caller vorhanden.
  - `verifyCronSecret`: importiert + aufgerufen in allen 7 cron-Routes (Grep bestätigt).
  - `redactSecrets`: einziger Caller `logToDb` (`logger.ts:30`) — auf jedem `captureException/Warning/Info`-Pfad.
  - recording-ready: Guard sitzt VOR `readFile(resolvedPath)` (`:101`) — resolveder Pfad wird gelesen, nicht der Roh-Input.
  - slug `.eq`: beide Public-Routes; Mock-Builder in beiden bestehenden Route-Tests auf `.eq` mitgezogen (Change-Impact, 0 Regression).

## 5. Offene Risiken / fehlende Checks
- **Live-Login-Lockout-Behavioral-Smoke** gegen deployten Stand: bewusst nicht ausgeführt (nichts deployt; optional/Founder-Selbsttest). Der Mechanismus ist Pure-Mock vollständig belegt; das Live-Residual ist reine Ende-zu-Ende-Bestätigung.
- **Carry-Forward für Gesamt-/qa V20 (BLOCKING, security-headers-live-smoke.md, KEIN curl-only):** (a) Handbook-`<iframe srcdoc>`-XSS rendert nicht aus, (b) SVG-Upload-Reject + Logo-Route nosniff, (c) CSP Report-Only live + Browser-Kernflow `tests/_probe/csp-check.mjs` (0 Eigen-Asset-Violations) → enforce-Flip-Entscheid. Diese gehören dem SLC-193/194-Scope und werden in der Gesamt-QA post-Deploy geholt.

## 6. Readiness
SLC-195 code-side **ready** — alle 5 fachlichen AC Pure-Mock verifiziert, Hygiene-AC grün, 0 Regression. Slice bleibt `in_progress` (code-done) bis Gesamt-/qa V20 + Deploy-Live-Smokes, konsistent mit SLC-194 (RPT-640). Damit sind **alle 3 V20-Slices (SLC-193/194/195) /qa-durch** — bereit für die Gesamt-QA über die Version.

## 7. Recommended next step (Next workflow step)
**Gesamt-/qa V20** über SLC-193/194/195 (Cross-Slice) — inkl. der oben gebündelten BLOCKING Live-Smokes (Handbook-XSS, SVG-Reject, CSP-Funktional-Smoke + enforce-Flip). Danach → `/final-check` → `/code-review ultra` (DEC-219-Closure) → `/deploy` (MIG-133/134 Live-Apply, node:20-Sidecar) → `/post-launch`.

*(Self-check: SLC-195 ist der letzte V20-Slice; Gesamt-/qa V20 steht noch aus; /final-check, /go-live, /post-launch für V20 noch nicht gelaufen → korrekter nächster Schritt ist Gesamt-/qa V20, nicht ein anderer Workstream.)*
