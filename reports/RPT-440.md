---
id: RPT-440
date: "2026-06-10"
skill: /qa
slice: SLC-V9.1-B
feature: FEAT-077
type: completion
status: completed
reviewOf: null
result: "PASS (Code-Side) — 0 Blocker / 0 High / 0 Medium / 3 Low. 80/80 hermetische Vitest GREEN, tsc/ESLint clean, Stub-Scan clean, MIG-062 DB-vs-Code-Paritaet LIVE bestaetigt. AC-B-9 Live-Smoke founder-gated (offen)."
title: "QA SLC-V9.1-B Continuous-Cost-Cap-Service + Pipeline-Trigger (Code-Side)"
---

# QA SLC-V9.1-B — Continuous-Cost-Cap-Service + Pipeline-Trigger (Code-Side)

## 1. QA-Scope
Code-Side-Verifikation der 5 MTs von SLC-V9.1-B (FEAT-077), Worktree `v9-1-forward-bucket-email` HEAD `cf480d3`. Live-Smoke (AC-B-9) ist founder-gated (OQ-R1-1) und nicht Teil dieser QA.

## 2. Ergebnis: **PASS (Code-Side)** — 0 Blocker / 0 High / 0 Medium / 3 Low

## 3. Gates
- **Stub-Scan**: CLEAN (8 Code-Files: continuous-cost-cap, per-email-approval, notify-founder, pipeline-trigger, route, banner-data, handle-pattern-extraction-job, page.tsx) — keine TODO/FIXME/console.log/Empty-Catch/Placeholder-Returns.
- **TypeScript**: `tsc --noEmit` EXIT 0 (full).
- **ESLint**: 0/0 auf allen neuen/geaenderten Files.
- **Vitest (hermetisch)**: 80/80 GREEN ueber 8 Slice-Test-Files. Full-Suite: 1742 passed, 508 failed = **ausschliesslich `TEST_DATABASE_URL`-gated** (server-only DB-Tests per coolify-test-setup.md, kein Regress — non-DB-Error-Grep leer).

## 4. DB-vs-Code-Paritaet (MIG-062 LIVE, Coolify-Postgres @ 159.69.207.29)
- `vw_bulk_email_cost_daily` Cols `tenant_id,day,total_cost_eur,run_count` ⇄ Adapter liest `tenant_id/day/total_cost_eur` ✓
- `vw_bulk_email_cost_monthly` Cols `tenant_id,month,total_cost_eur,run_count` ⇄ Adapter liest `month/total_cost_eur` ✓
- `email_bulk_run.status` CHECK enthaelt `paused` + `awaiting_approval` ✓ (Worker/Trigger schreiben diese)
- `email_bulk_run` hat alle vom Trigger gelesenen Cols (`status,email_count,created_at,inbound_source,tenant_id,updated_at`) ✓
- `ai_jobs_job_type_check` enthaelt `email_bulk_pre_filter/thread_redact/pattern_extract` (Trigger-Enqueue-Targets) ✓
- Daily-View Grants: `authenticated` + `service_role` ✓

## 5. Wiring-Verifikation (Chain real verbunden)
- Cron-Route → `runPipelineTrigger` (Import ✓, 503/403/200/500 getestet).
- `runPipelineTrigger` → `checkContinuousCostCap` + Supabase-Adapter → LIVE-Views (Cols ✓); → `ai_jobs`-Enqueue + Status-Updates (Werte CHECK-valide ✓); → `notifyFounderCapHit` (safeNotify-Wrap).
- **Enqueuete Stage-Jobs haben LIVE-Consumer**: `email_bulk_pre_filter/thread_redact/pattern_extract` sind in `claim-loop.ts` JOB_TYPES + `run.ts` Handler-Registrations registriert (im Gegensatz zum verworfenen Dead-Job `email_bulk_pipeline_trigger`). Trigger ist also funktional, nicht nur enqueuend ins Leere.
- Worker-Gate → `estimatePatternExtractionCost`/`requiresPerEmailApproval`/`notifyFounderApprovalRequired` (Imports ✓), Status `awaiting_approval` CHECK-valide ✓.
- Page → `selectFlaggedRuns`/`flaggedStatusLabel` → Banner (Import ✓, Render-Pfad getestet).

## 6. AC-Verifikation (9/10 code-side erfuellt)
- AC-B-1 ✓ 3-Schichten-Cap (Daily 5 + Monthly 100 + Per-Email 0.50), Vitest GREEN.
- AC-B-2 ✓ Cron mit Cron-Secret-Check, Summary-Return. (Hinweis: 403 statt Spec-401 — OP-Konvention, L-1.)
- AC-B-3 ✓ Trigger continuous→`parsed` + `email_bulk_pre_filter`-Enqueue bei email_count≥25 ODER Daily-Roll-Over (DEC-207: korrigierte Status/Job-Type ggue. Spec-Literal).
- AC-B-4 ✓ Per-Email-Approval-Gate → `awaiting_approval` + Logger-Audit + Notify (im Pattern-Extract-Worker, korrigierte Location).
- AC-B-5 ✓ `approval_token` bypassed Gate.
- AC-B-6 ✓ `notifyFounderCapHit` + `notifyFounderApprovalRequired` via V8.1 sendMail.
- AC-B-7 ✓ Admin-Banner bei `status IN (paused, awaiting_approval)`.
- AC-B-8 ✓ RUNBOOK V9.1-Section (Cap-Reset + Approval-Reset + Manueller Trigger).
- AC-B-9 ⏳ Live-Smoke — **founder-gated (OQ-R1-1)**, code-side nicht verifizierbar.
- AC-B-10 ✓ tsc 0, ESLint 0, alle hermetischen Vitest GREEN.

## 7. Findings

### Low
- **L-1 — Cron-Fail-Status 403 statt Spec-401.** `route.ts` nutzt 403 (1:1 OP-Konvention `inbound-email-imap-sync`). Konsistenz > Spec-Wortlaut. Akzeptiert.
- **L-2 — ISSUE-094 (PRE-EXISTING, nicht aus diesem Slice).** `page.tsx:173-174` (SLC-168/V9) selektiert `month_start` auf `vw_bulk_email_cost_monthly`, aber die View-Spalte heisst `month`. Query schlaegt fehl → try/catch → Monats-Cost-Aggregat zeigt leer/0. Mein Banner-Code ist nicht betroffen (liest aus geladenen Runs). Empfehlung: 1-Zeichen-Fix in SLC-V9.1-D (beruehrt dieselbe Page) oder Quick-Follow-up.
- **L-3 — AC-B-9 Live-Smoke deferred** (founder-gated). Kein Defekt, sondern offener Workflow-Schritt.

## 8. Offene Risiken / fehlende Checks
- DB-Integration-Tests (RLS, Schema-Parity gegen Coolify-DB) laufen server-seitig (node:20 Docker) — Teil der Gesamt-V9.1-/qa, nicht code-side hier abgedeckt.
- Live-Verhalten des Stage-Orchestrators (mehrere Cron-Ticks, echte Cap-Hits, Founder-Email-Zustellung, Banner-Render im Browser) erst im Live-Smoke verifizierbar.
- Per-Email-Approval-Outlier triggert unter Production-Baseline (0.006 €/Mail) nie — bewusster Outlier-Guard (DEC-197), kein Routine-Pfad.

## 9. Readiness
SLC-V9.1-B ist **code-side ready**. Bleibt `in_progress` bis AC-B-9 Live-Smoke + Gesamt-V9.1-/qa. Per [[module-lifecycle-discipline]] + IMP-950: kein Master-Merge / kein released vor V9.1-Workflow-Closure.

## 10. Next workflow step
**`/backend SLC-V9.1-C`** (Storage-Retention-Cron, FEAT-078) — naechster Slice der Version (V9.1 hat noch SLC-V9.1-C + D offen). Alternativ IONOS-Live-Smoke sobald Founder OQ-R1-1 durch (schliesst AC-R1-8 + AC-B-9 gebuendelt). Gesamt-V9.1-/qa → /final-check → /go-live → /deploy → /post-launch erst nach SLC-V9.1-D + Master-Merge.
