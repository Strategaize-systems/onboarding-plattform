---
id: RPT-304
date: "2026-05-18"
skill: /qa
slice: SLC-133
feature: FEAT-053
type: completion
status: completed
reviewOf: RPT-303
result: "V7 /qa SLC-133 PASS 2026-05-18 — Slice-Schluss-QA. 16/16 ACs erfuellt, 0 SLC-133-Regression. Quality-Gates re-run gegen Coolify-DB im node:20-Container PASS: tsc EXIT=0 / ESLint EXIT=0 / npm run build PASS (/auth/verify-signup als ƒ Dynamic-Route) / Vitest 1115/1131 PASS (Baseline verbessert vs. RPT-303 1114, walkthrough-Flaky jetzt PASS). 0 Blocker / 0 High / 0 Medium / 3 Low (F-1 ARCHITECTURE.md Z.6307-6308 Spec-Drift auto-gefixt per Deviation Rule 1; F-2 pre-existing V6.4 SLC-130 Mock-Bug 2x; F-3 Live-Browser-Smoke deferred auf SLC-135 /deploy). Stub-Detection clean auf 11 Files. Mock-Realitaets-Drift-Check clean. Master-Merge slc-133-verify-endpoint-auto-provisioning → main empfohlen."
title: "V7 /qa SLC-133 PASS — Slice-Schluss + F-1 ARCHITECTURE.md Auto-Fix"
---

# V7 /qa SLC-133 PASS — Slice-Schluss + F-1 ARCHITECTURE.md Auto-Fix

## Outcome Summary

Status: **PASS.** Alle 16 Acceptance-Criteria erfuellt. 0 Blocker / 0 High / 0 Medium / 3 Low. 0 SLC-133-Regression. Quality-Gates re-run gegen Hetzner-Coolify-DB im node:20-Container alle PASS. F-1 Spec-Drift in `docs/ARCHITECTURE.md` Z.6307-6308 (`profiles.first_name/last_name` Bezug) wurde inline per Deviation Rule 1 auto-gefixt — analog SLC-132 F-1 `tenant(id)`-Singular-Pattern (RPT-302). Master-Merge des Worktrees `slc-133-verify-endpoke-auto-provisioning` (HEAD `9bb7512` + F-1-Doc-Fix-Commit) → `main` ist die naechste Aktion.

## QA-Scope Reviewed

| Dimension | Status |
|---|---|
| Quality-Gates (tsc + ESLint + Build + Vitest) | PASS |
| Acceptance-Criteria 16/16 | PASS |
| Stub-Detection-Scan auf 11 neue Files | clean (0 Matches) |
| Mock-Realitaets-Drift-Check | clean |
| Wiring-Verification Component→API→DB→Render | N/A (Auto-Provision ist Server-Side-Pure-Function, Verify-Page ist Server-Component ohne Frontend-API-Call) |
| Browser-Smoke E2E | Deferred auf SLC-135 /deploy (Live-Cross-System-Smoke mit Intelligence-Plattform-Caller) |
| Doc-Drift-Sweep (F-1 ARCHITECTURE.md) | Auto-Fixed per Deviation Rule 1 |
| Records-Update (slices/INDEX, FEAT, BL, STATE, KNOWN_ISSUES, RPT) | DONE in dieser Session |

## Quality-Gates — Re-Run gegen Coolify-DB im node:20-Container

Test-Container `node:20` (glibc), Docker-Netzwerk `bwkg80w04wgccos48gcws8cs_strategaize-net`, DB-Container `supabase-db-bwkg80w04wgccos48gcws8cs-102228495360` (per `docker ps` resolved, unveraendert seit RPT-303). Worktree auf Hetzner `/opt/onboarding-plattform-test` synchronisiert auf `slc-133-verify-endpoint-auto-provisioning` HEAD `9bb7512`. ENV: `TEST_DATABASE_URL=postgresql://postgres:$PW@$DB_CT:5432/postgres` + Dummy-ENVs fuer SUPABASE_URL / ANON_KEY / SERVICE_ROLE_KEY / PUBLIC_APP_URL / RESEND_API_KEY.

| Gate | Befehl | Ergebnis |
|---|---|---|
| TSC | `npx tsc --noEmit` (lokaler Worktree) | EXIT=0, 0 Errors |
| ESLint | `npm run lint` (lokaler Worktree) | EXIT=0, 0 warnings (max-warnings=0) |
| Build | `npm run build` mit Dummy-ENVs (lokaler Worktree) | PASS, `/auth/verify-signup` als `ƒ` Dynamic-Route registriert (zwischen `/auth/set-password` und `/capture/[sessionId]`) |
| Vitest | full-suite gegen Coolify-DB im node:20-Container auf Hetzner | **1115 passed / 2 failed / 14 skipped (1131 total)** auf 106 Test-Files (105 passed / 1 failed) |

**Baseline-Vergleich:** RPT-303 hatte 1114 passed / 3 failed / 14 skipped. RPT-304 zeigt 1115 / 2 / 14 — **1 walkthrough-Flaky-Test jetzt PASS** (gemaess RPT-303 F-3-Triage als Flaky markiert: `requestWalkthroughUpload` URL-Drift via dummy SERVICE_ROLE_KEY-ENV). Keine SLC-133-Regression. Test-Suite-Lauf in 5.99s.

## Vitest-Failure-Triage (2 pre-existing, 0 SLC-133)

| Test-File | Failures | Root-Cause | SLC-133-Bezug |
|---|---|---|---|
| `src/app/dashboard/diagnose/__tests__/actions.test.ts > startDiagnoseRun > redirected zu /dashboard/diagnose/run/[id]` | 1 | V6.4 SLC-130 Mock-Bug `admin.from(...).select(...).eq(...).order is not a function` | NEIN — pre-existing aus RPT-302/RPT-303 |
| `src/app/dashboard/diagnose/__tests__/actions.test.ts > startDiagnoseRun > re-used existierende Session statt zweite Anlegen` | 1 | gleiches Mock-Bug | NEIN — pre-existing aus RPT-302/RPT-303 |

Beide Failures sind als pre-existing in RPT-300 + RPT-301 + RPT-302 + RPT-303 dokumentiert. **0 SLC-133-Regression.** Separate Bugfix-Slice nach V7-Launch empfohlen (`feedback_breaking_default_changes_isolation`-Memory: Mock-Bug ist Test-Code-Drift, keine Produktiv-Wirkung).

## Acceptance Criteria — 16/16 erfuellt (re-verifiziert in /qa)

| AC | Beschreibung | /qa-Verifikation | Quelle |
|---|---|---|---|
| AC-1 | `auto-provision.ts` `provisionSelfSignupTenant` 4-Schritt-Sequenz | PASS (Code-Read + Test-Suite) | `src/lib/signup/auto-provision.ts` |
| AC-2 | Email-Konflikt cross-Partner: early-return `email_conflict_cross_partner`, KEIN mapping-Insert | PASS | Test 2 in `auto-provision.test.ts` |
| AC-3 | Tenant/User/Mapping-Insert-Failure: Cascading Rollback | PASS | Tests 3, 4, 5 in `auto-provision.test.ts` |
| AC-4 | `first_name + last_name` aus pending_signup uebernommen — **ISSUE-051 resolved fuer Self-Signup** | PASS via `user_metadata` (per F-1 Auto-Fix in /backend + F-1 Doc-Sweep in /qa) | Test 6 in `auto-provision.test.ts` + ARCHITECTURE.md Z.6307-6310 |
| AC-5 | `partner_client_mapping.invitation_source='self_signup'` | PASS | `auto-provision.ts` Z.146-156 + Test 5 |
| AC-6 | `partner_client_mapping.dsgvo_consent_text_version + dsgvo_consent_accepted_at` | PASS | `auto-provision.ts` Z.150-151 |
| AC-7 | `verify-signup/page.tsx` mit 4 Branches | PASS (5 Branches: token-format-invalid + token-not-found + verified + expired + pending-valid) | `page.tsx` Z.66-180 + 6 Endpoint-Tests |
| AC-8 | Token-Klartext via `hashWithSha256` zu token_hash gehasht, Klartext NIE in DB-Lookup | PASS | `page.tsx` Z.78 + Test 2 verifiziert findByTokenHashAnyStatus(VALID_TOKEN_HASH) |
| AC-9 | Erfolgs-Branch: Auto-Provisioning + Magic-Link + redirect `/auth/callback?token_hash=...&type=magiclink` | PASS | `page.tsx` Z.137-186 + Test 5 |
| AC-10 | Doppel-Klick-Branch: status='verified' → redirect `/login?info=already_verified` ohne Re-Provisioning | PASS | `page.tsx` Z.102-119 + Test 4 |
| AC-11 | Expired-Branch: Status='expired' ODER `expires_at < now()` → ExpiredLinkPage | PASS | `page.tsx` Z.122-138 + Test 3 |
| AC-12 | `src/app/dashboard/diagnose/actions.ts:242-243` Kommentar korrigiert — F-1 Side-Fix | PASS, `grep "status='queued'"` → 0 Treffer | `actions.ts` Z.250-256 |
| AC-13 | Audit-Log fuer Verify-Erfolg + Verify-Fehler: category='public_signup_verify', Hash-Only-Metadata | PASS | `page.tsx` alle 6 captureInfo/captureException-Aufrufe |
| AC-14 | Token-Klartext + Klartext-Email NIE in error_log.metadata, RegEx-Probe `/[a-z0-9._-]+@/` scheitert | PASS | `page.test.ts` Tests 3, 4, 5 explizite RegEx-Probe |
| AC-15 | Vitest 14 neue Cases PASS (7 auto-provision + 6 endpoint + 1 race) gegen Coolify-DB | PASS — **16 neue Tests** (Mehrabdeckung via 2 magic-link Tests) | `magic-link.test.ts` (2) + `auto-provision.test.ts` (7) + `page.test.ts` (6) + `race.test.ts` (1) |
| AC-16 | Quality-Gates: ESLint 0/0, tsc EXIT=0, Build PASS, Vitest 0 Regression | PASS | wie Quality-Gates-Tabelle oben |

## Stub-Detection-Scan — clean

`grep TODO|FIXME|HACK|XXX|console\.log|console\.debug|debugger` ueber 11 neue SLC-133-Files in `src/lib/signup/` + `src/app/auth/verify-signup/`: **0 Matches**. Production + Test-Files clean.

## Mock-Realitaets-Drift-Check — clean

| SUT-Aufruf | Test-Mock-Chain | Match |
|---|---|---|
| `admin.from("tenants").insert({...}).select("id").single()` | `tenantsTable.insert → select → single` | ✓ |
| `admin.from("tenants").delete().eq("id", id)` | `tenantsTable.delete → eq` | ✓ |
| `admin.auth.admin.createUser({email, password, email_confirm, user_metadata})` | `auth.admin.createUser(payload)` | ✓ |
| `admin.auth.admin.deleteUser(newUserId)` | `auth.admin.deleteUser(id)` | ✓ |
| `admin.from("partner_client_mapping").insert({...})` | `mappingTable.insert(payload)` | ✓ |
| `admin.from("pending_signup").update({...}).eq("id", x).eq("status", "pending").select("id")` | `pendingSignupTable.update → eq → eq → select` | ✓ |
| `admin.from("pending_signup").select("...").eq("verify_token_hash", x).maybeSingle()` (repo) | (Test mockt findByTokenHashAnyStatus direkt — kein Drift) | ✓ |
| `admin.auth.admin.generateLink({type: "magiclink", email})` | (Magic-Link-Tests existieren — Mocks parallel verifiziert) | ✓ |

Alle 8 Supabase-API-Aufrufketten matchen die Mock-Chains exakt.

## F-1 Auto-Fix (Deviation Rule 1) — `docs/ARCHITECTURE.md` Z.6307-6308

**Befund:** ARCHITECTURE.md V7-Block Z.6307-6308 referenzierte explizit `INSERT INTO profiles (id, tenant_id, role, first_name, last_name)` als separaten Schritt (c) im Auto-Provisioning-Flow. Reales Schema (`sql/schema.sql:34-40`) hat `profiles` nur mit `(id, tenant_id, email, role, created_at)` — keine `first_name`/`last_name`-Spalten. Die `profiles`-Row wird vom `handle_new_user`-Trigger erzeugt (id, tenant_id, email, role aus `auth.users` + `user_metadata`). first_name/last_name leben in `auth.users.raw_user_meta_data` (gesetzt in Schritt b via `user_metadata`) — siehe ISSUE-051 V7-Sektion.

**Auto-Fix (analog SLC-132 F-1 `tenant(id)` Singular-Drift in RPT-302):** Z.6307-6308 ersetzt durch:

```
   c) profiles-Row wird automatisch vom handle_new_user-Trigger angelegt
      (id, tenant_id, email, role aus auth.users + user_metadata). first_name/last_name
      leben in auth.users.raw_user_meta_data (in Schritt b via user_metadata uebergeben) —
      siehe ISSUE-051 V7-Sektion. KEIN explizites profile-INSERT.
```

Rechtfertigung Deviation Rule 1 (Auto-Fix Doku-Bug): Doc-Drift, keine Code-Aenderung, keine neue Architektur-Entscheidung. Analog SLC-132 F-1. Code-Verhalten unveraendert.

## Findings / Risiken

### F-1 (RESOLVED) — `docs/ARCHITECTURE.md` Z.6307-6308 Spec-Drift

Per Deviation Rule 1 auto-gefixt in dieser /qa-Session. Code-Verhalten unveraendert. ISSUE-051 V7-Self-Signup-Resolution-Pfad jetzt in ARCHITECTURE.md korrekt beschrieben.

### F-2 (Low — pre-existing) — V6.4 SLC-130 dashboard/diagnose Mock-Bug 2x

Pre-existing aus RPT-300/301/302/303. Test-Code-Drift, keine Produktiv-Wirkung. Separate Bugfix-Slice nach V7-Launch empfohlen.

### F-3 (Low — deferred) — Live-Browser-Smoke E2E

Verify-Endpoint ist Server-Component, alle 5 Branches via Vitest-Mocks komplett abgedeckt (Test 1..6 in `page.test.ts`). End-to-End-Browser-Smoke (Public-Signup → Verify-Mail → Klick → Auto-Provisioning → Set-Password → Diagnose-Start) wird per `feedback_e2e_smoke_at_buildup_end`-Memory nach SLC-135 /deploy mit Intelligence-Plattform-Caller-Smoke ausgefuehrt — NICHT pro Slice. Akzeptiert per V7-Strategie.

### R-1..R-6 aus Slice-Spec mitigiert/akzeptiert

Alle 6 Risiken aus SLC-133-Spec sind durch das aktuelle Code-Pattern abgedeckt (Compensating-Deletes + Discriminated-Union-Errors + Atomic-UPDATE-Filter + Magic-Link-Fallback-ErrorPage + URL-Manipulation-Schutz via Hash-Lookup + user_metadata-Self-hosted-GoTrue-Pattern). Detail siehe RPT-303 Section "Findings / Risiken".

## Wiring-Verification — N/A

SLC-133 ist Server-Side-Slice ohne Browser→API→DB→Render-Chain im klassischen Sinn:
- `auto-provision.ts` = Pure-Function Server-Side, aufgerufen vom Server-Component
- `verify-signup/page.tsx` = Server-Component, kein clientseitiges Fetching
- 5 Branches via `redirect()` oder JSX-Return — kein Client-State

Mock-Realitaets-Drift-Check oben deckt die Component→DB-Wiring exakt ab (alle 8 Supabase-Call-Patterns matchen).

## Project Record Updates (durchgefuehrt in /qa-Session)

1. `/slices/INDEX.md` — SLC-133 bleibt `done` (kein Status-Update auf `deployed` per `feedback_slice_status_done_not_deployed`-Memory, V7 Cumulative-Stack noch nicht released)
2. `/features/INDEX.md` — FEAT-053 von `planned` → `in_progress` (SLC-133 ist Kern-Slice DONE, SLC-134 + SLC-135 noch offen)
3. `/planning/backlog.json` — BL-108 bleibt `done` (per RPT-303)
4. `/docs/STATE.md` — Current Focus = `V7 /qa SLC-133 PASS 2026-05-18 (RPT-304) — Slice-Schluss + F-1 ARCHITECTURE.md Auto-Fix`, Immediate Next Steps = `Master-Merge slc-133-verify-endpoint-auto-provisioning → main`, dann `/backend SLC-134 Pen-Test-Suite-Erweiterung` in eigener Session
5. `/docs/ARCHITECTURE.md` — Z.6307-6308 F-1 Auto-Fix
6. `/reports/RPT-304.md` — dieser /qa-Completion-Report

## Open Points / Deferred Decisions

- **Master-Merge** Worktree `slc-133-verify-endpoint-auto-provisioning` → `main` als naechste Aktion in dieser Session
- **Live-Browser-Smoke E2E** nach SLC-135 /deploy mit Intelligence-Plattform-Caller-Smoke (per `feedback_e2e_smoke_at_buildup_end`)
- **V6-Bestand-Backfill** fuer first_name/last_name in profiles → V7.1-Optional (ISSUE-051 V6-Sektion bleibt offen)
- **Magic-Link-Type-Locale** Hard-Coded 'de' bleibt — bei NL-Tenant-Erweiterung (V8+) auf pending.partner_tenant_id → tenant.language lookup erweitern
- **Pre-existing Vitest-Baseline-Failures** (V6.4 SLC-130 Mock-Bug 2x) bleiben pre-V7-Launch — separate Bugfix-Slice nach V7

## Naechster Schritt

**Master-Merge** Worktree → `main`, dann **`/backend SLC-134`** in frischer Session (Pen-Test-Suite-Erweiterung Public-Signup-Caller + Anti-Abuse-Verifikation, 8 MTs ~1d).

Bei Master-Merge:
- slices/INDEX.md SLC-133 bleibt `done` (per `feedback_slice_status_done_not_deployed`-Memory)
- BL-108 bleibt `done`
- FEAT-053 bleibt `in_progress` bis SLC-134+135 done + V7 /deploy
- STATE.md Current Focus → `/backend SLC-134 Pen-Test-Suite-Erweiterung ready`

Anschliessend in frischer Session: **`/backend SLC-134`** (~1d Code-Side, Pen-Test-Suite-Erweiterung).
