---
id: RPT-472
date: "2026-06-15"
skill: /slice-planning
slice: SLC-V9.7-A
feature: FEAT-083
type: completion
status: completed
reviewOf: null
result: "V9.7 in 2 sequenzielle Slices geschnitten — SLC-V9.7-A (Concept-Emitter, 5 MTs) → SLC-V9.7-B (Bundle+Conformance+Worker-Wiring, 5 MTs). 0 Migrationen, 0 neue Deps. 3 Architektur-Offenpunkte gefixt. READY für /backend SLC-V9.7-A."
title: "Slice-Planning V9.7 — OKF Handbuch-Export"
---

# Slice-Planning V9.7 — OKF Handbuch-Export

## Slicing-Summary
FEAT-083 + FEAT-084 in **2 strikt sequenzielle Slices** geschnitten (B importiert A's `emit`), Cumulative-Single-Branch-Worktree `v9-7-okf-export`, /qa pro Slice, **EIN** Master-Merge in SLC-V9.7-B. SaaS-Mode → Micro-Task-Decomposition + Worktree-Isolation Pflicht (beide erfüllt). **0 DB-Migrationen, 0 neue Dependencies, 0 ENV/Cron, 0 neue Tabellen.**

## Slice-Liste
| Slice | Feature/BL | Inhalt | MTs | Status |
|---|---|---|---|---|
| SLC-V9.7-A | FEAT-083 / BL-162 | OKF Concept-Emitter (pure, isoliert): types.ts + emit.ts (KU/diagnosis/sop + Mapper + serializeConcept), TDD-RED je Typ. Kein Worker-Touch. | MT-0..MT-5 | planned |
| SLC-V9.7-B | FEAT-084 / BL-163 | Bundle-Assembly (index.md/log.md/Cross-Links) + Konformitäts-Check (TDD-RED zuerst) + zip-builder Multi-Folder + Worker-Wiring (weiche Degradation) + Gesamt-/qa + Master-Merge. | MT-1..MT-5 | planned |

## Architektur-Offenpunkte gefixt (aus /architecture §9)
1. **OKF-Fehler-Degradation = WEICH** (DEC-225 final): OKF additiv nach `renderHandbook`; bei Fehler/Non-Konformität → error_log + ZIP ohne `okf/`, Snapshot bleibt `ready`. Narratives Handbuch (Kern-Deliverable) bricht nie.
2. **Cross-Link-Regel = block_key-Gruppierung**, gerendert in Bundle-Assembly (hat den vollen Concept-Graph) → bundle-root-absolute, auflösbare Links.
3. **Test-Layout = `__tests__/`** (OP-Konvention live verifiziert: 212 in __tests__ vs 3 colocated; IMP-1262 erfüllt).

## Reuse-Befunde (Pflicht-Search)
- `src/lib/handbook/slugify.ts` existiert (via `__tests__/slugify.test.ts`) → wiederverwenden, NICHT neu schreiben.
- `src/workers/handbook/__tests__/zip-builder.test.ts` existiert → MT-3 erweitert ihn (Regression-Gate für narratives ZIP).
- `yaml@^2.9.0` + `archiver@^7.0.1` bereits Dependency → keine neue Dep.

## Parallel-Execution-Tabelle
| Slice | Parallel-Group | MIG Reserved | File-Touchpoints (Kern) | Notes |
|---|---|---|---|---|
| SLC-V9.7-A | S1 (seq) | keine | src/lib/handbook/okf/{types,emit}.ts + __tests__ | unabhängig, zuerst; KEIN Worker-Touch |
| SLC-V9.7-B | S1 (seq) | keine | src/lib/handbook/okf/{bundle,conformance}.ts + __tests__, src/workers/handbook/{handle-snapshot-job,types,zip-builder}.ts | nach A; Master-Merge |

Keine Parallelität (Single-Founder, A→B Hard-Dependency). Keine MIG → keine MIG-Reservierung nötig.

## Risiken / Dependencies
- A→B Hard-Dependency (B importiert emit). Ein Worktree, ein Master-Merge.
- R-B-1 zip-builder Backward-Compat (bestehende Tests müssen GREEN bleiben).
- R-B-2 Content-Parität narrativ vs OKF = Nicht-Ziel (DEC-225); Diagnose-Selektion `confirmed` kann minimal von per-Section `min_status` abweichen — bewusst akzeptiert.
- R-B-4 Live-Smoke (echter Snapshot-Download + okf/-Inspektion) ist /deploy-gated.

## Files created
- `/slices/SLC-V9.7-A-okf-concept-emitter.md`, `/slices/SLC-V9.7-B-okf-bundle-assembly.md`
- `/reports/RPT-472.md`

## Files changed
- `/slices/INDEX.md` — V9.7-Sektion (Tabelle + Execution-Order + Parallel-Tabelle).
- `/docs/STATE.md` — High-Level State→slice-planning, Focus/Phase.

## Files intentionally not changed
- `/planning/backlog.json` — BL-162/163 bleiben `open` (flip auf in_progress erst /backend).
- `/planning/roadmap.json` — v9-7 bleibt `active`. `/docs/MIGRATIONS.md` — 0 Migrationen.

## Problems found
Keine. Test-Konvention + Reuse-Helper live verifiziert (kein Spec-Pfad-Drift, IMP-1262).

## Recommended next step
`/backend SLC-V9.7-A` — MT-0 Worktree-Setup `v9-7-okf-export` (echtes npm install, IMP-1112) + MT-1 (types/Mapper/serializer TDD-RED) → MT-2/3/4 (KU/diagnosis/sop Emitter) → MT-5 Slice-/qa. Danach SLC-V9.7-B.
