---
id: RPT-542
date: "2026-07-02"
skill: /qa
slice: SLC-170b
feature: FEAT-091
type: completion
status: completed
reviewOf: null
result: "PASS — MIG-128 Fachmodul-Seed (16 Module) live-verifiziert; alle 16 Rows durch echtes extractModuleContext/Zod validiert (16/16, 311 Fragen)"
title: "QA MIG-128 StB-Fachmodul-Seed (16 Module, Welle 3-5)"
---

# QA — MIG-128 StB-Fachmodul-Seed (SLC-170b Welle 3-5)

## 1. QA-Scope
MIG-128 (Seed von 16 `template`-Rows: `stb_modul_m01/02/03/06/07/08/15/16/26/27/28/35/36/38/39/42` v1.0), generiert aus den 16 abgenommenen `docs/stb-vertikale/M-<xx>-seed-source.md` via `gen-mig128-fachmodule-seed.py`, appliziert auf die Coolify-OP-DB (159.69.207.29). Verifikation gegen AC-170b-1/2/3/4 (SLC-170b-Spec) + den echten App-Zod-Gate.

## 2. Ergebnis: **PASS**

## 3. Findings nach Severity
- **Blocker / High / Medium:** keine.
- **Low (2, behoben/akzeptiert):**
  - **L-1 (behoben vor Apply):** `table_rows()`-Parser ließ die Markdown-Header-Zeile stehen → `themen=7` statt 6 (Header „Bereich" als Pseudo-Bereich). Gefixt (Header vor `|---|`-Trenner verworfen), re-verifiziert `themen=6` über alle 16 Module.
  - **L-2 (akzeptiert, kein Defekt):** QA-Sidecar-Validator initial mit einarmigem `z.record(z.string())` → `_zod`-Crash unter zod 4.3.5 (Test-Script-Bug, nicht Daten). Korrigiert auf zweiarmiges `z.record(z.string(), z.string())` (verbatim wie `template-queries.ts`); danach 16/16 PASS.

## 4. Verifikations-Nachweise
- **Live-Verify (psql gegen Coolify-DB):** 17 `stb_modul_*` Rows (16 neu + m04/MIG-125), je `blocks=2`; Fragen/Hebel exakt (17/8 Standard · 16/8 M-42 · 24/11 M-06/26/27/28/35 · 22/11 M-07 · 26/13 M-04); `modul_key` gesetzt; Row-Count nach Re-Apply stabil = **idempotent (AC-170b-2)**.
- **Echter App-Zod-Gate (AC-170b-1):** node:20-Sidecar im Coolify-Netz (`bwkg80w04wgccos48gcws8cs_strategaize-net`), Schemata **verbatim** aus `module-context.ts` (`ModuleMetadataSchema`) + `template-queries.ts` (`TemplateBlockSchema`), zod 4.3.5 (= App-Version). Ergebnis **OK=16 / FAIL=0**, 311 eindeutige Fragen. Pro Modul geprüft: `modul_key`==slug-Suffix, `output_contract.kinds`==Triple, `themenmodell`=6 mit nicht-leeren `unterpunkte`, `ki_hebel` Reifegrad 1-4, `blocks`=[stufe1_kern required / stufe2_vertiefung optional], `ebene`∈{Kern,Vertiefung}.
- **Content-Fidelity (AC-170b-3):** Verbatim-Spotcheck F-M42-001 (Text + `unterbereich=u1a_rolle_heute`) matcht die Quelle. Generator parst §1/§3/§4/§5 der abgenommenen seed-source.md (kein erfundener Inhalt).
- **DB-Sidecar-Test (AC-170b-4):** `src/lib/db/__tests__/migration-128-fachmodule-seed.test.ts` committet (Self-Apply-Idempotenz + echtes `extractModuleContext` + Counts + Verbatim), Modell nach dem grünen `migration-126`-Test.
- **Stub-Scan:** N/A für Daten-Seed; Generator (`.py`) nutzt assert-basierte Sanity-Checks (Slug/modul_key/Kern/Hebel/Themen non-empty, UUID-Kollision), keine TODO/Platzhalter/Debug-Reste.
- **tsc/eslint:** Kein Produktions-TS geändert (nur `.py`/`.sql`/`.md`/`.json`-Records + 1 Test-File). Der Test-File wird im Pre-Merge-Re-Check/CI tsc-geprüft (Worktree ohne `node_modules`, IMP-1360).

## 5. Offene Risiken / nicht geprüft
- **Feature OFF-gated:** `NEXT_PUBLIC_ENABLE_STB_VERTIKALE` OFF — kein Live-Code-Pfad liest die neuen Rows bis SLC-173/FEAT-092 mit Flag ON deployen. Voller E2E (Modul-Capture → `module_output_synthesis`-Worker → Workspace-Reader über die neuen Module) ist Deploy-/Feature-Aktivierungs-Validation (deferred, kein Defekt).
- Der committete vitest-Test wurde lokal nicht getrysct (kein Worktree-`node_modules`); die eigentliche /qa-Validierung lief über den Standalone-Real-Zod-Sidecar (PASS). Test läuft im Pre-Merge-Re-Check.

## 6. Readiness
Seed ist **ready** — 16 Module live, durch den echten App-Zod-Gate bestätigt, idempotent, content-treu. Branch `v10-slc170b-fachmodul-seed` NICHT gemergt (Founder-Hold auf Push/Merge).

## 7. Next workflow step
SLC-170b ist der **letzte offene V10-Slice** (Group A Content-Seed) — die V10-Implementierungs-Slices sind bereits code-side abgeschlossen und V10 ist STABLE (Gesamt-QA RPT-537 + /final-check RPT-538 + /go-live RPT-539 + /deploy REL-036 + /post-launch RPT-541 sind für den V10-Kern gelaufen). MIG-128 ist ein additiver, OFF-gated Daten-Seed **ohne Code-/Schema-Änderung**.

**Empfohlener nächster Schritt: Pre-Merge-Re-Check + Merge `v10-slc170b-fachmodul-seed` → main (Founder-gated, git-release.md 6-Schritt).** MIG-128 ist DB-seitig bereits live (reiner Doku-/Generator-/Records-/Test-Merge, kein Coolify-Rebuild nötig). Ein separater `/final-check`/`/go-live`/`/deploy`-Zyklus ist für diesen reinen Seed nicht erforderlich (V10 ist bereits released/stable; kein neuer Code, kein neues Release-Artefakt) — der Merge schließt SLC-170b formal ab. Danach optional FEAT-092 Live-Diagnose-Validation bei bewusster Feature-Aktivierung.
