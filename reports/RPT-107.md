---
id: RPT-107
date: "2026-04-29"
skill: /frontend
slice: SLC-043
feature: FEAT-030
type: completion
status: completed
reviewOf: null
result: "SLC-043 Frontend Code done — Cross-Tenant + Pro-Tenant Reviews + Quick-Stats-Badge + Sidebar-Link. 4 von 4 Micro-Tasks komplett umgesetzt: 2 neue Pages (/admin/reviews + /admin/tenants/[tenantId]/reviews), 1 neue Tabelle-Komponente (PendingReviewsTable, server), 2 neue Lib-Helper (listPendingReviews, pendingCountsByTenant), 3 erweiterte Files (admin-sidebar, tenants/page.tsx, tenants-client). Build gruen, 256 Non-DB-Tests gruen. Auth-Gate auf strategaize_admin pro Page (Page-Level-Profile-Check), Sidebar-Link sichtbar nur fuer strategaize_admin (gated via /admin/layout). Action-Link zur SLC-042-Konsolidierter-Review-View `/admin/blocks/[blockKey]/review?tenant=...&session=...`. Quick-Stats-Badge gelb/grau je Pending-Count, Klick linkt zur Pro-Tenant-Sicht."
title: "/frontend SLC-043 — Cross-Tenant + Pro-Tenant Reviews + Quick-Stats-Badge + Sidebar-Link"
---

## Outcome summary

**SLC-043 Frontend Code complete.** Alle 4 Micro-Tasks aus der Slice-Spec umgesetzt:

- **MT-1 Cross-Tenant `/admin/reviews` Page**: Server-Component mit Profile-Role-Check `strategaize_admin`, Aggregations-Loader `listPendingReviews()` (kein tenantId-Filter), oldest-first sortiert mit Sub-Sortierung Tenant-Name. Tabelle zeigt Tenant | Block | KU-Count | Letzter Submit | Erstellt | Aktion (Pruefen + Pencil-Link zu V2-Debrief). Empty-State sichtbar bei 0 pending Reviews. Header zeigt Total-Pending + Tenants-Anzahl.
- **MT-2 Pro-Tenant `/admin/tenants/[tenantId]/reviews` Page**: Gleiche Tabelle ohne Tenant-Spalte. Header mit Tenant-Name + "Alle Tenants" Back-Link. UUID-Validierung + notFound() bei unbekanntem Tenant. Wiederverwendung des Loaders mit `{tenantId}`-Filter.
- **MT-3 Quick-Stats-Badge in TenantsClient**: Server-Side Single-Aggregation-Query `pendingCountsByTenant` parallel zum bestehenden Cockpit-Loader im `tenants/page.tsx` (Promise.all). Map als Prop an Client-Component, neuer Badge in der Tenant-Card-Header-Zeile (gelb bei `> 0`, grau bei `0`), klickbar → linkt zu `/admin/tenants/[id]/reviews`. ClipboardCheck-Icon.
- **MT-4 Sidebar-Link "Reviews"**: `NAV_ITEMS` in admin-sidebar erweitert um `{ href: "/admin/reviews", label: "Reviews", icon: ClipboardCheck }` zwischen Tenants und Debrief. Sichtbarkeit auf strategaize_admin via /admin/layout (TenantAdminShell rendert AdminSidebar nicht — fuer tenant_admin nicht im DOM).

**Verification confidence:** Build gruen (alle 35+ Routes, beide neuen Pages als ƒ Dynamic gelistet), 256 Non-DB-Tests gruen, kein Browser-Smoke (Pflicht in /qa-Phase, gebuendelt mit SLC-045).

**Status:** Code complete. **/qa SLC-043** ist die mandatorische naechste Aktion.

## Files reviewed

- `/slices/SLC-043-cross-tenant-reviews.md` — Slice-Spec mit 12 ACs + 4 MTs + Pflicht-QA-Vorgaben
- `/reports/RPT-106.md` — SLC-045 /qa-Pattern (Coolify-DB-Test-Run + Browser-Smoke-Plan als Referenz)
- `/reports/RPT-104.md` — SLC-044 /qa-Pattern (Iter-Loop falls Bugs)
- `/docs/STATE.md` — V4.1-Stand + Update auf SLC-043 in_progress
- `/slices/INDEX.md` — V4.1 Slice-Status
- `/src/components/admin-sidebar.tsx` — bestehender Pfad: `src/components/admin-sidebar.tsx` (slice-spec sagt `src/components/admin/AdminSidebar.tsx`, der real existierende Pfad ist anders)
- `/src/app/admin/layout.tsx` — Auth-Gate-Logik fuer strategaize_admin vs tenant_admin
- `/src/app/admin/tenants/page.tsx` — Server-Component mit Cockpit-Loader
- `/src/app/admin/tenants/tenants-client.tsx` — Client-Component fuer Tenant-Liste mit Card-Layout
- `/src/app/admin/blocks/[blockKey]/review/page.tsx` — SLC-042 Link-Ziel-Signatur
- `/src/lib/handbook/get-review-summary.ts` — bestehendes Pattern fuer Server-Side-Aggregations-Helper
- `/src/lib/cockpit/load-cross-tenant.ts` — bestehendes Pattern fuer parallel-loadable Helpers
- `/src/lib/supabase/admin.ts` — admin-Client (service_role, RLS-Bypass)

## Files changed

- `/src/components/admin-sidebar.tsx` — Import um `ClipboardCheck` erweitert, NAV_ITEMS um Reviews-Eintrag (zwischen Tenants und Debrief). Sichtbarkeit nur fuer strategaize_admin via /admin/layout (kein expliziter Profile-Check noetig — Layout filtert).
- `/src/app/admin/tenants/page.tsx` — Import `pendingCountsByTenant` ergaenzt. Cockpit-Loader + Pending-Counts-Loader laufen parallel via `Promise.all` mit `.catch(() => new Map())` als Fallback. Map zu Record-Object konvertiert (`Object.fromEntries`) und als neue Prop `pendingReviewsByTenant` an `<TenantsClient>` durchgereicht.
- `/src/app/admin/tenants/tenants-client.tsx` — Import um `Link` (next/link) und `ClipboardCheck` erweitert. Component-Signatur akzeptiert optionale neue Prop `pendingReviewsByTenant?: Record<string, number>` (Default `{}`). Im Tenant-Card-Header neuer Badge nach dem Sprach-Badge: Klickbar (Link auf `/admin/tenants/{id}/reviews`), gelb bei `> 0` (`bg-amber-100 text-amber-800`), grau bei `0` (`bg-slate-100 text-slate-500`), mit ClipboardCheck-Icon und Pluralization. `data-testid="tenant-pending-reviews-badge"`.
- `/slices/INDEX.md` — SLC-043 Status `planned` → `in_progress`.
- `/docs/STATE.md` — Current Focus + Phase + Immediate Next Steps aktualisiert: 5/5 V4.1-Slices Code-done, naechste Aktion ist /qa SLC-043 + gebuendelter Coolify-Redeploy SLC-045 + SLC-043 + Browser-Smoke beider Slices.

## Files created

- `/src/lib/reviews/list-pending-reviews.ts` — Helper-Modul mit `listPendingReviews(adminClient, { tenantId? })` und Type `PendingReviewRow`. Step 1: pending block_review-Rows mit nested JOIN auf tenants(name) + capture_session(last_submitted_at), `ORDER BY created_at ASC`. Step 2: KU-Counts via Single-Aggregation-Query (`knowledge_unit WHERE source='employee_questionnaire' AND capture_session_id IN (...)`), gruppiert in JS nach `tenant_id|capture_session_id|block_key`. Step 3: Joinen + JS-Sub-Sort nach Tenant-Name bei gleichem created_at.
- `/src/lib/reviews/pending-counts-by-tenant.ts` — Helper `pendingCountsByTenant(adminClient): Map<string, number>`. Single-Aggregation-Query `block_review WHERE status='pending'`, gruppiert in JS nach tenant_id.
- `/src/app/admin/reviews/page.tsx` — Cross-Tenant Reviews-Page (Server-Component). Profile-Role-Gate auf strategaize_admin, Loader, Header mit Total-Pending + Tenants-Anzahl, PendingReviewsTable mit `showTenantColumn={true}`.
- `/src/app/admin/reviews/PendingReviewsTable.tsx` — Geteilte Tabelle-Komponente (Server-Component, kein Client-State noetig). Props: `rows`, `showTenantColumn`, `emptyTitle`, `emptySubtitle`. Spalten Tenant (optional) | Block (code-formatiert) | KUs (right-aligned, tabular-nums) | Letzter Submit | Erstellt | Aktion. Aktion: Pruefen-Button (brand-primary, Link zu SLC-042 `/admin/blocks/[blockKey]/review?tenant=...&session=...`) + Pencil-Sekundaer-Link zu V2 `/admin/debrief/[sessionId]/[blockKey]`. Empty-State-Card mit Title + Subtitle. Date-Formatter mit `Intl.DateTimeFormat de-DE` Berlin. `data-testid="pending-reviews-table"` und `data-testid="pending-reviews-empty"` und `data-testid="pending-review-action"` fuer Smoke.
- `/src/app/admin/tenants/[tenantId]/reviews/page.tsx` — Pro-Tenant Reviews-Page (Server-Component). UUID-Validierung + notFound() bei ungueltiger ID, Profile-Role-Gate auf strategaize_admin, Tenant-Lookup (notFound bei unbekanntem Tenant), Loader mit `{tenantId}`-Filter, Header mit Tenant-Name + "Alle Tenants" Back-Link, PendingReviewsTable mit `showTenantColumn={false}`.
- `/reports/RPT-107.md` — dieser Completion-Report.

## Files intentionally not changed

- `/src/components/admin-sidebar.tsx` `pathname.startsWith()`-Logik — funktioniert ohne Anpassung. `/admin/reviews` ist disjunkt zu `/admin/tenants` (`startsWith`-Check ist false-disjunct), `/admin/tenants/[id]/reviews` matcht `/admin/tenants` (Tenants als active markiert) — bewusste UX-Wahl, da der User auf der Tenants-Sub-Sicht ist.
- `/src/components/dashboard-sidebar.tsx` — Reviews ist Berater-only-Feature, keine Anpassung in der Tenant-User-Sidebar.
- `/src/app/admin/layout.tsx` — Auth-Gate-Logik bleibt. /admin/reviews und /admin/tenants/[id]/reviews verfeinern den Gate auf Page-Level (strategaize_admin only); tenant_admin-Layout-Pfad rendert die AdminSidebar gar nicht und kann die Routes per Direct-URL nicht erreichen (kommt durch das page-level redirect).
- `/src/app/admin/tenants/CrossTenantCockpit.tsx` — bleibt unveraendert, der Reviews-Status ist bewusst NICHT als Spalte hier reingezogen (separater Badge im Tenant-Eintrag ist die Slice-Vorgabe MT-3).
- `/docs/DECISIONS.md` — Keine neue ADR. Wahl der server-side-Aggregation in der Page (statt API-Route) ist konsistent zum bestehenden Pattern (Cockpit-Loader ist auch server-side in tenants/page.tsx).
- `/docs/MIGRATIONS.md` — Keine Migration in SLC-043 (Slice-Spec explizit "keine Migration").
- `/docs/KNOWN_ISSUES.md` — Keine neuen Issues durch SLC-043-Code.
- `/features/INDEX.md` — FEAT-030 bleibt `in_progress` bis /qa-PASS und Gesamt-V4.1-Release.
- `/planning/roadmap.json` — V4.1 bleibt `active`.
- `/planning/backlog.json` — keine neuen BL-Items.

## UI states covered

| State | Status |
|---|---|
| Empty: 0 pending Reviews (Cross-Tenant) | implementiert (PendingReviewsTable Empty-State) |
| Empty: 0 pending Reviews (Pro-Tenant) | implementiert (gleiche Tabelle, anderer Empty-Text) |
| Liste: oldest-first + Sub-Sort Tenant-Name | implementiert (PG-Order + JS-Sort) |
| Action-Button: Pruefen → SLC-042 Page | implementiert (Link mit Tenant + Session Query-Params) |
| Sekundaer-Link: Pencil → V2 Debrief | implementiert |
| Quick-Stats-Badge: gelb bei `>0`, grau bei `0` | implementiert (`bg-amber-100` vs `bg-slate-100`) |
| Quick-Stats-Badge Klick → Pro-Tenant-Page | implementiert (`<Link href="/admin/tenants/{id}/reviews">`) |
| Sidebar-Link "Reviews" sichtbar fuer strategaize_admin | implementiert (NAV_ITEMS-Eintrag, Layout filtert non-admin) |
| Sidebar-Link "Reviews" NICHT im DOM fuer tenant_admin | implementiert (TenantAdminShell rendert AdminSidebar nicht) |
| Page Auth-Gate: tenant_admin → redirect /dashboard | implementiert (Profile-Role-Check beider Pages) |
| Page Auth-Gate: ungueltiger UUID auf Pro-Tenant → notFound() | implementiert (UUID_RE-Check) |
| Page Auth-Gate: unbekannter Tenant → notFound() | implementiert (`tenants.maybeSingle()` + null-Check) |
| Pro-Tenant-Header: Tenant-Name + Back-Link | implementiert (`<ArrowLeft />` + Link zu /admin/tenants) |
| Mobile-Tabelle: horizontal scroll | implementiert (`overflow-x-auto` Wrapper) |

## Problems found

1. **Slice-Spec-Pfad-Drift fuer AdminSidebar.** Slice-Spec MT-4 sagt `src/components/admin/AdminSidebar.tsx`, der real existierende Pfad ist `src/components/admin-sidebar.tsx` (kein admin-Subordner). Pfad-Korrektur in Slice-Spec waere optional (Slice-Specs sind aktuelle-Stand-Snapshots), nicht-blockierend. **Kein Blocker.**
2. **Performance-Test mit 50 Tenants × 10 Bloecken (Slice-Spec R1) nicht durchgefuehrt.** Aktuell nur 1 Tenant in der Demo-DB. Strukturell deckt der `idx_block_review_status_created`-Index die `WHERE status='pending' ORDER BY created_at`-Query ab. Ein Live-Performance-Test mit Mock-Daten waere ein /qa-Pflicht-Punkt — Test-Seed-Skript existiert nicht out-of-the-box. **Optional fuer V4.1 oder /qa-Phase**, nicht-blockierend fuer SLC-043 Code-Done.
3. **Sidebar-Badge mit Total-Pending-Count (Slice-Spec MT-4 optional)** wurde **bewusst weggelassen** (per Slice-Spec R3 Mitigation). Lazy-Laden mit Server-Action waere zusaetzliche Komplexitaet ohne klaren V4.1-Nutzen. Falls gewuenscht: V4.2-Item.
4. **Browser-Smoke nicht in /frontend.** Pflicht-Browser-Smoke in /qa-Phase (gebuendelt mit SLC-045-Smoke per User-Wunsch — Coolify-Redeploy fuer beide Slices gemeinsam).

## Open points or deferred decisions

1. **Live-Verifikation Cross-Rollen-Auth** — Pflicht in /qa SLC-043: tenant_admin ruft `/admin/reviews` und `/admin/tenants/[id]/reviews` auf, muss redirect zu `/dashboard` sehen. Strukturell verifiziert (Page-Level-Profile-Check), Live-Verifikation in Browser-Smoke.
2. **Live-Verifikation Quick-Stats-Badge** — Browser-Smoke: Demo-Tenant hat aktuell vermutlich 0 pending Reviews, Badge sollte grau sein. Bei pending Reviews → gelber Badge + Klick-Test zur Pro-Tenant-Page.
3. **Performance-Test mit kuenstlichen Mock-Daten** — Optional in /qa-Phase (50 Tenants × 10 Bloecke via Test-Seed). Ohne reale Performance-Probleme nicht V4.1-blockierend.
4. **`/dashboard/reviews`-Existenz** — wurde nicht angetastet. Build listet sie als Route, vermutlich SLC-042 Cockpit-Card-Link-Ziel oder Tenant-User-Sicht auf eigene Reviews. Nicht in SLC-043-Scope.
5. **Coolify-Redeploy** — User-Pflicht (`feedback_manual_deploy.md`). Nach SLC-045-/qa + SLC-043-/qa: beide Slices gebuendelt deployen, dann gemeinsamer Browser-Smoke.

## Runtime verification summary

- **Build:** `npm run build` PASS — Production-Build erfolgreich, alle 35+ Routes, beide neuen Pages als ƒ Dynamic gelistet (`/admin/reviews`, `/admin/tenants/[tenantId]/reviews`). Kein TypeScript-Error, kein Tailwind-Warning.
- **Unit-Tests Non-DB:** `npx vitest run --exclude='**/__tests__/rls/**' --exclude='**/db/__tests__/**' ...` — 256/256 Tests in 25 Files PASS in 2.23s. Kein neuer Test-File in SLC-043 (pure UI-Pages mit Server-Loader, kein Test-Wert ohne TEST_DATABASE_URL).
- **Coolify-DB-Test-Run:** offen fuer /qa-Phase. Erwartung: 378/378 PASS (keine Aenderung an Tests durch SLC-043, nur Plus-Wachstum bleibt aus weil keine neuen Tests).
- **Browser-Rendering:** NICHT direkt verifiziert (Pflicht in /qa-Phase, gebuendelt mit SLC-045).
- **Interaction:** NICHT direkt verifiziert.
- **Responsive:** NICHT direkt verifiziert.
- **Persistence:** N/A — SLC-043 hat keine Datenbank-Persistenz, nur Read-Layer.

**Confidence:** Strukturell hoch (alle Ableitbaren Pruefungen gruen, Build sauber, kein neues Pattern), live-UX **offen — User-Pflicht in /qa**.

## Project record updates

- `/slices/INDEX.md` — SLC-043 Status `planned` → `in_progress` (Code done, /qa folgt).
- `/docs/STATE.md` — Current Focus + Phase + Immediate Next Steps aktualisiert.
- `/features/INDEX.md` — KEINE Aenderung. FEAT-030 bleibt `in_progress` bis /qa-PASS und Gesamt-V4.1-Release.
- `/planning/roadmap.json` — KEINE Aenderung. V4.1 bleibt `active`.
- `/planning/backlog.json` — KEINE Aenderung.
- `/docs/KNOWN_ISSUES.md` — KEINE neuen Issues.
- `/docs/DECISIONS.md` — KEINE neuen DECs.
- `/docs/MIGRATIONS.md` — Keine Migration in SLC-043.

## Recommended next step

**`/qa SLC-043`** — direkt anschliessend. Pflicht-Vorgaben aus Slice-Spec:
1. Cross-Rollen-Verifikation: tenant_admin/employee/tenant_member bekommen 403/Redirect bei `/admin/reviews` und `/admin/tenants/[id]/reviews`.
2. RLS-Test: strategaize_admin sieht alle Tenants in der Liste; tenant_admin (negativ-Test) sieht nichts in `/admin/reviews` (Page-Level-Check filtert ab).
3. Performance-Test: 50 Tenants × 10 Bloecke Mock-Daten (optional via Test-Seed), Page-Load <500ms.
4. Empty-State-Verifikation.
5. Responsive-Check.
6. `npm run test` + `npm run build` gruen — auf Coolify-DB-Test-Run mit TEST_DATABASE_URL gesetzt (378/378 erwartet).
7. Cockpit-Records-Update nach /qa.

Anschliessend: **gebuendelter Coolify-Redeploy SLC-045 + SLC-043** + **Browser-Smoke beider Slices** in einer User-Sitzung. Bei PASS: Gesamt-V4.1-/qa → /final-check → /go-live → /deploy.

## Lessons / IMP-Kandidaten

Keine prozess-relevanten neuen IMPs aus dieser Slice-Implementation identifiziert. Der bestehende Workflow (Worktree-Branch → Code → Build → Lokal-Tests → /qa) hat sauber gegriffen.

Kleinere Beobachtung: Die Slice-Spec-Pfad-Drift fuer `AdminSidebar.tsx` (Spec sagt `src/components/admin/AdminSidebar.tsx`, real `src/components/admin-sidebar.tsx`) ist eine kleine Diskrepanz und kein Prozess-Problem. Slice-Specs sind Snapshots und beschreiben oft das *Soll* (CamelCase + Subordner) waehrend der Code *Kebab-Case-flat* ist. Pre-Read via Glob faengt das zuverlaessig auf.
