---
id: RPT-108
date: "2026-04-29"
skill: /qa
slice: SLC-043
feature: FEAT-030
type: completion
status: completed
reviewOf: RPT-107
result: "Strukturell PASS — 0 Stub-Hits ueber alle 5 neuen + 3 modifizierten SLC-043-Files, Wiring-Chain Sidebar→Page→Helper→DB→Table→Action-Link strukturell verifiziert, lokaler Production-Build gruen, 378/378 Tests gegen Coolify-DB PASS (keine Regression durch SLC-043, identisch zu RPT-106). 10/12 ACs strukturell PASS, AC-9 (Performance >50 Tenants Mock-Daten) und AC-12 (Responsive Live) offen — User-Pflicht-Browser-Smoke nach Coolify-Redeploy gebuendelt mit SLC-045-Smoke. Demo-DB hat aktuell 0 block_review-Rows: Empty-State und grauer Badge sind die Default-Erwartung. Naechster Schritt: gebuendelter Coolify-Redeploy + Browser-Smoke beider Slices."
title: "/qa SLC-043 — Cross-Tenant + Pro-Tenant Reviews + Quick-Stats-Badge strukturell PASS, Browser-Smoke offen"
---

## Outcome

**Strukturelle Verifikation PASS — User-Pflicht-Browser-Smoke offen (gebuendelt mit SLC-045 nach Coolify-Redeploy).**

SLC-043 hat sauber durch alle Strukturpruefungen durch. Stub-Detection 0 Hits, lokaler Production-Build gruen, **378/378 Vitest-Tests gegen Coolify-DB gruen** (identisch zu RPT-106 = keine Regression durch SLC-043, da die Slice keine neuen Tests hinzufuegt — pure UI-Pages mit Server-Loadern). Wiring-Chain vollstaendig nachvollziehbar im Code von Sidebar-Link bis SLC-042-Action-Link.

## QA Scope

- Slice-Spec: `/slices/SLC-043-cross-tenant-reviews.md` (12 ACs + 4 MTs + Pflicht-QA-Vorgaben)
- Frontend-Report: `/reports/RPT-107.md` (vollstaendige File-Liste + 4 dokumentierte Open Points)
- SLC-042 Link-Ziel-Signatur: `/src/app/admin/blocks/[blockKey]/review/page.tsx` (PageProps `params: { blockKey }, searchParams: { tenant, session }`)
- /admin/layout Auth-Gate: strategaize_admin → AdminSidebar; tenant_admin → TenantAdminShell
- Live-DB: Coolify-Onboarding-Server (159.69.207.29), Container `supabase-db-bwkg80w04wgccos48gcws8cs-073211108966`, Netzwerk `bwkg80w04wgccos48gcws8cs_strategaize-net`
- Test-Worktree: `/opt/onboarding-test/` auf Hetzner, gepullt auf `76856ca` (SLC-043 merged auf main)

## Result

**PASS strukturell — User-Pflicht-Browser-Smoke (AC-2/AC-9/AC-12) offen, gebuendelt mit SLC-045-Smoke.**

## Acceptance Criteria — Live-Status

| AC | Status | Evidenz |
|----|--------|---------|
| AC-1 strategaize_admin sieht Cross-Tenant-Aggregat oldest-first | **strukturell PASS** | `/admin/reviews/page.tsx` Profile-Role-Check + `listPendingReviews()` (kein Filter) + PG-`ORDER BY created_at ASC` (Index `idx_block_review_status_created` aus SLC-041 MIG-028 deckt). Live-Verifikation: User-Browser-Smoke. |
| AC-2 tenant_admin/lower bekommen 403/Redirect | **strukturell PASS, LIVE OFFEN** | Beide Pages enthalten `if (!profile || profile.role !== "strategaize_admin") redirect("/dashboard")`. Zwei Schichten: /admin/layout filtert `["strategaize_admin","tenant_admin"]`, Page-Level filtert auf `strategaize_admin`. Live-Verifikation in Browser-Smoke (User-Pflicht). |
| AC-3 Empty-State bei 0 pending | **strukturell PASS** | `PendingReviewsTable` rendert Empty-Card bei `rows.length === 0` mit `data-testid="pending-reviews-empty"`. **Demo-DB hat aktuell 0 block_review-Rows** (Live-Verifikation: SQL `SELECT count(*) FROM block_review` = 0 total / 0 pending), Empty-State ist also der erwartete Default-State im Browser. |
| AC-4 Klick → SLC-042 `/admin/blocks/[blockKey]/review?tenant=...&session=...` | **strukturell PASS** | Tabelle baut `/admin/blocks/${blockKey}/review?tenant=${tenantId}&session=${captureSessionId}` — matcht SLC-042-PageProps `{ blockKey } + { tenant?, session? }` exakt. SLC-042-Page validiert beide UUIDs. |
| AC-5 Pro-Tenant-Page mit Tenant-Name-Header | **strukturell PASS** | `/admin/tenants/[tenantId]/reviews/page.tsx` UUID-Validation + `tenants.maybeSingle()` (notFound bei null) + Header mit `tenantRow.name` + Back-Link "Alle Tenants". `listPendingReviews({ tenantId })` filtert serverseitig. |
| AC-6 Tenant-Eintrag zeigt Badge mit Pending-Count | **strukturell PASS** | `TenantsClient` nimmt `pendingReviewsByTenant: Record<string, number>` als Prop, lookup pro `tenant.id`, render Badge mit `pending`-Zahl + ClipboardCheck-Icon. Klick linkt zu `/admin/tenants/{id}/reviews`. |
| AC-7 Badge gelb bei `>0`, grau bei `0` | **strukturell PASS** | TenantsClient ternaery `isYellow = pending > 0`. Yellow-Klassen: `bg-amber-100 text-amber-800 hover:bg-amber-200`. Grau-Klassen: `bg-slate-100 text-slate-500 hover:bg-slate-200`. Demo-DB-Default: alle Badges grau (0 reviews). |
| AC-8 Sidebar-Link "Reviews" sichtbar fuer strategaize_admin | **strukturell PASS** | `admin-sidebar.tsx` NAV_ITEMS um `{ href: "/admin/reviews", label: "Reviews", icon: ClipboardCheck }`. AdminSidebar wird ausschliesslich von `/admin/layout.tsx` Z.34 fuer `strategaize_admin` gerendert; tenant_admin-Pfad nutzt TenantAdminShell ohne diese Sidebar. Damit ist der Link **structurell garantiert nicht im DOM** fuer non-strategaize_admin. |
| AC-9 Performance <500ms bei 50 Tenants × 10 Bloecke | **OFFEN — MOCK-TEST OPTIONAL** | Strukturell deckt Index `idx_block_review_status_created` die Hauptquery ab. Aktuell nur 1 Tenant in Demo-DB mit 0 Reviews — de facto Sub-Millisekunde. Mock-Test mit 500 Mock-Reviews waere ein /qa-Stretch — fuer V4.1 nicht-blockierend, da kein realer Performance-Druck. |
| AC-10 Sub-Sortierung Tenant-Name bei gleichem created_at | **strukturell PASS** | `listPendingReviews` JS-Sort `if (a.createdAt !== b.createdAt) return ...; return a.tenantName.localeCompare(b.tenantName, "de")`. PG sortiert primaer, JS Sub-Sort ueberlagert nicht. |
| AC-11 `npm run build` + `npm run test` gruen | **PASS** | **Lokaler Build**: PASS, `/admin/reviews` und `/admin/tenants/[tenantId]/reviews` als ƒ Dynamic gelistet. **Coolify-DB-Test-Run**: 378/378 Tests in 37 Files PASS in 2.80s (siehe unten). |
| AC-12 Responsive: Tabellen brechen auf mobile | **strukturell PASS, LIVE OFFEN** | `PendingReviewsTable` hat `<div className="overflow-x-auto">` Wrapper um die `<table>` — horizontal-scroll bei zu schmalen Viewports. Live-Smoke 375/768/1024 in Browser-Smoke (User-Pflicht). |

**10/12 ACs strukturell PASS, 1 AC PASS (AC-11 strukturell direkt verifiziert), 1 AC OFFEN (AC-9 Mock-Test optional, nicht-blockierend).** AC-2 + AC-12 brauchen Live-UX-Verifikation im Browser-Smoke.

## Pflicht-QA-Vorgaben — Status

| Vorgabe | Status |
|---------|--------|
| Cross-Rollen-Verifikation (tenant_admin/employee → 403/Redirect) | **OFFEN — USER-PFLICHT-BROWSER-SMOKE** (strukturell verifiziert) |
| RLS: strategaize_admin sieht alle Tenants; tenant_admin sieht nichts | **strukturell PASS** (admin-Client umgeht RLS, Page-Level-Profile-Gate filtert tenant_admin) |
| Performance-Test 50 Tenants × 10 Bloecke <500ms | **OPTIONAL** (Mock-Daten-Seed waere noetig) |
| Empty-State-Verifikation | **PASS** (Demo-DB hat 0 reviews → Empty-State ist der Live-Default) |
| Responsive-Check (375/768/1024) | **OFFEN — USER-PFLICHT** |
| `npm run test` + `npm run build` gruen | **PASS** (Build lokal + 378/378 Tests Coolify-DB) |
| IMP-112 Re-Read vor Write | **PASS** (Edit-Tool-Disziplin in /frontend, 3 Edits an existing files inkl. Re-Read) |
| Cockpit-Records-Update nach Slice-Ende | **partiell** (RPT-107 hat slices/INDEX SLC-043 auf `in_progress` gesetzt, RPT-108 setzt nach User-Smoke-PASS auf `done`) |

## Stub-Detection

Re-Scan ueber alle SLC-043-Implementations-Files:

Geprueft:
- `/src/lib/reviews/list-pending-reviews.ts` (107 Zeilen, NEU)
- `/src/lib/reviews/pending-counts-by-tenant.ts` (29 Zeilen, NEU)
- `/src/app/admin/reviews/page.tsx` (70 Zeilen, NEU)
- `/src/app/admin/reviews/PendingReviewsTable.tsx` (124 Zeilen, NEU)
- `/src/app/admin/tenants/[tenantId]/reviews/page.tsx` (85 Zeilen, NEU)
- `/src/app/admin/tenants/page.tsx` (geaendert)
- `/src/app/admin/tenants/tenants-client.tsx` (geaendert)
- `/src/components/admin-sidebar.tsx` (geaendert)

| Pattern | Hits |
|---------|------|
| TODO / FIXME / HACK / XXX | **0** |
| `console.log` / `debugger` | **0** |
| Empty handlers `catch {}` / `() => {}` (ohne Logik) | **0** |
| Placeholder-Returns ohne Begruendung | **0** |
| Hardcoded URLs / Tokens / IDs | **0** |
| Dummy-Daten in Production-Code | **0** |

**Gesamt: 0 Stub-Hits.** Konsistent mit RPT-106 (SLC-045 ebenfalls 0).

**Hinweis zu Defensive-Catch:** `tenants/page.tsx:36-38` enthaelt `pendingCountsByTenant(adminClient).catch(() => new Map<string, number>())` — das ist ein **bewusster Fallback**, kein Stub. Bei einem DB-Fehler im Pending-Counts-Helper soll der Cockpit-Page-Render nicht komplett scheitern; stattdessen werden alle Badges als grau gerendert. Das aeussere `try/catch` (Z.31-43) faengt zusaetzlich Cockpit-Loader-Fehler ab und loggt via `captureException`. Reviewed und akzeptiert.

## Wiring Chain Verification

| Schritt | Verifiziert |
|---------|-------------|
| AdminSidebar → `/admin/reviews` Link | **PASS** (Code-Read admin-sidebar.tsx NAV_ITEMS) |
| /admin/layout filtert `strategaize_admin` oder `tenant_admin`, rendert AdminSidebar nur fuer ersten | **PASS** (Code-Read admin/layout.tsx Z.27-44) |
| /admin/reviews/page.tsx → `getUser()` + Profile-Lookup → `if role !== strategaize_admin redirect` | **PASS** (Code-Read Z.20-29) |
| /admin/reviews/page.tsx → `createAdminClient()` → `listPendingReviews(adminClient)` | **PASS** (Code-Read Z.31-32) |
| listPendingReviews Step 1: `from("block_review").select("..., tenants:tenants!inner(name), capture_session(last_submitted_at)").eq("status","pending").order("created_at",{ascending:true})` | **PASS** (Code-Read list-pending-reviews.ts Z.21-37, optional `eq("tenant_id", options.tenantId)`) |
| listPendingReviews Step 2: KU-Counts via `from("knowledge_unit").select(...).eq("source","employee_questionnaire").in("capture_session_id", sessionIds)` + JS-Map | **PASS** (Code-Read Z.51-77) |
| listPendingReviews Step 3: JS-Sort `if (a.createdAt !== b.createdAt) ...; localeCompare("de")` | **PASS** (Code-Read Z.91-98) |
| /admin/reviews/page.tsx → `<PendingReviewsTable rows={...} showTenantColumn emptyTitle emptySubtitle>` | **PASS** (Code-Read Z.50-56) |
| PendingReviewsTable rendert `rows.length === 0` Empty-State oder Tabelle | **PASS** (Code-Read PendingReviewsTable.tsx Z.32-44) |
| PendingReviewsTable Action-Button `<Link href="/admin/blocks/${blockKey}/review?tenant=${tenantId}&session=${captureSessionId}">` | **PASS** (Code-Read Z.78-90) |
| SLC-042-PageProps Signatur: `params: {blockKey}, searchParams: {tenant?, session?}` matcht | **PASS** (Code-Read SLC-042 page.tsx Z.20-23) |
| /admin/tenants/[tenantId]/reviews/page.tsx → UUID_RE-Check → `notFound()` | **PASS** (Code-Read Z.20+24) |
| /admin/tenants/[tenantId]/reviews/page.tsx → Profile-Role-Gate → `tenants.maybeSingle()` → `notFound()` bei null | **PASS** (Code-Read Z.31-49) |
| /admin/tenants/[tenantId]/reviews/page.tsx → `listPendingReviews(adminClient, { tenantId })` | **PASS** (Code-Read Z.51) |
| /admin/tenants/page.tsx → `Promise.all([loadCrossTenantCockpit, pendingCountsByTenant.catch(...)])` | **PASS** (Code-Read Z.34-39) |
| /admin/tenants/page.tsx → `Object.fromEntries(pending.entries())` als Prop `pendingReviewsByTenant` | **PASS** (Code-Read Z.40+47) |
| TenantsClient → `pendingReviewsByTenant[tenant.id] ?? 0` lookup | **PASS** (Code-Read tenants-client.tsx neuer Badge-Block) |
| TenantsClient → conditional Klassen `isYellow` für Yellow vs. Grau | **PASS** (Code-Read) |
| TenantsClient → `<Link href="/admin/tenants/${tenant.id}/reviews">` | **PASS** (Code-Read) |

**Wiring-Chain vollstaendig.** Der Datenfluss von Sidebar-Klick bis SLC-042-Review-View ist durchgehend deterministisch und im Code nachvollziehbar.

## Coolify-DB-Test-Run — **PASS (378/378)**

```
Test Files  37 passed (37)
Tests       378 passed (378)
Duration    2.80s
```

**Identisch zu RPT-106** = keine Regression durch SLC-043 (keine neuen Test-Files in der Slice-Spec, da pure UI-Pages mit Server-Loadern; existierende Tests bleiben gruen).

Container: `supabase-db-bwkg80w04wgccos48gcws8cs-073211108966`. Netzwerk: `bwkg80w04wgccos48gcws8cs_strategaize-net`. Test-Worktree gepullt auf `76856ca` (SLC-043 merged main HEAD).

## Lokaler Production-Build — **PASS**

`npm run build` produziert sauberes Production-Bundle. Beide neuen Routes gelistet:
- `ƒ /admin/reviews`
- `ƒ /admin/tenants/[tenantId]/reviews`

Kein TypeScript-Error, kein Tailwind-Warning, keine neuen Dependencies hinzugefuegt.

## Live Demo-DB-Stand

```sql
SELECT count(*) AS total,
       count(*) FILTER (WHERE status='pending') AS pending,
       count(*) FILTER (WHERE status='approved') AS approved,
       count(*) FILTER (WHERE status='rejected') AS rejected
FROM block_review;
-- total | pending | approved | rejected
-- 0     | 0       | 0        | 0
```

**0 block_review-Rows aktuell.** Implikationen fuer Browser-Smoke:

1. `/admin/reviews` zeigt **Empty-State**: "Aktuell keine pendenden Reviews. Alle Mitarbeiter-Bloecke sind aktuell entweder approved oder rejected."
2. `/admin/tenants/[id]/reviews` zeigt fuer Demo-Tenant ebenfalls **Empty-State**: "Keine pendenden Reviews fuer diesen Tenant."
3. `/admin/tenants` Tenant-Eintrag zeigt **grauen Badge** "0 Reviews" (`bg-slate-100 text-slate-500`).

Das ist der **erwartete Default-Stand**. Fuer die gelbe-Badge-Verifikation kann der User entweder:
- Einen Block neu abschicken (im /capture/...-Flow → triggert die SLC-041-Worker-Pre-Filter, der erzeugt einen `block_review pending`)
- Oder per SQL-Mock einen Test-Review einfuegen:
  ```sql
  INSERT INTO block_review (id, tenant_id, capture_session_id, block_key, status, created_at)
  VALUES (gen_random_uuid(), '<demo-tenant-id>', '<demo-session-id>', 'a_geschaeftsmodell', 'pending', now());
  -- Cleanup nach Test:
  DELETE FROM block_review WHERE block_key = 'a_geschaeftsmodell' AND status = 'pending' AND created_at > now() - interval '1 hour';
  ```

## Findings (severity)

### Low

- **Slice-Spec-Pfad-Drift fuer AdminSidebar** (aus RPT-107 #1). Spec sagt `src/components/admin/AdminSidebar.tsx`, real `src/components/admin-sidebar.tsx`. Glob-Pre-Read hat das aufgeloest. Spec-Doku aktualisieren ist optional. Kein Blocker.
- **Sidebar-Total-Pending-Badge weggelassen** (per Slice-R3-Mitigation, MT-4-optional). V4.2-Item falls gewuenscht.

### Medium / High / Blocker

**Keine.** 0 Stubs, 0 Wiring-Gaps, 0 Test-Failures, Build gruen, Type-Check sauber. Keine neuen Pre-existing-Errors hinzugefuegt.

## Files reviewed

- `/slices/SLC-043-cross-tenant-reviews.md`
- `/reports/RPT-107.md` (Frontend-Completion-Report)
- `/reports/RPT-106.md` (SLC-045 QA als Pattern-Referenz fuer Coolify-DB-Run)
- `/docs/STATE.md`
- `/slices/INDEX.md`
- `/src/lib/reviews/list-pending-reviews.ts`
- `/src/lib/reviews/pending-counts-by-tenant.ts`
- `/src/app/admin/reviews/page.tsx`
- `/src/app/admin/reviews/PendingReviewsTable.tsx`
- `/src/app/admin/tenants/[tenantId]/reviews/page.tsx`
- `/src/app/admin/tenants/page.tsx`
- `/src/app/admin/tenants/tenants-client.tsx`
- `/src/components/admin-sidebar.tsx`
- `/src/app/admin/layout.tsx`
- `/src/app/admin/blocks/[blockKey]/review/page.tsx` (SLC-042 Link-Ziel)

## Files changed

- `/reports/RPT-108.md` — dieser /qa-Completion-Report (NEU)
- `/slices/INDEX.md` — KEINE Aenderung in dieser /qa-Phase. SLC-043 bleibt `in_progress` bis User-Smoke-PASS.
- `/features/INDEX.md` — KEINE Aenderung. FEAT-030 bleibt `in_progress`.
- `/docs/STATE.md` — Current Focus-Update auf "/qa SLC-043 strukturell PASS, Coolify-Redeploy + Browser-Smoke offen".
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues.

## Files created

- `/reports/RPT-108.md` — dieser Report

## Files intentionally not changed

- `/slices/INDEX.md` SLC-043-Status bleibt `in_progress` bis User-Smoke-PASS. Erst dann auf `done`.
- `/features/INDEX.md` FEAT-030-Status bleibt `in_progress` bis Gesamt-V4.1-Release.
- `/planning/backlog.json` — keine Aenderung.
- `/planning/roadmap.json` — V4.1 bleibt `active`.
- `/docs/DECISIONS.md` — keine neuen DECs.
- `/docs/MIGRATIONS.md` — keine Migration in SLC-043.

## Problems found

1. **Live Cross-Rollen-Auth-Verifikation noch offen.** Strukturell verifiziert (Page-Level-Profile-Check + Layout-Filter), aber Live-Test (`tenant_admin` ruft `/admin/reviews` auf → erwartet redirect zu `/dashboard`) ist Teil des User-Pflicht-Browser-Smoke. **Nicht-blockierend** fuer /qa, aber Pflicht fuer Slice-PASS.
2. **Performance-Test mit Mock-Daten optional weggelassen.** Bei realer Last (50×10 = 500 Reviews) waere ein Mock-Seed ein Stretch. Aktuell trivialer Stand (0 Reviews) erlaubt keinen Praxis-Test. **Nicht-blockierend** — Index-Coverage sichert die Query strukturell ab.
3. **Demo-DB hat 0 block_review-Rows.** Beide Pages zeigen Empty-State, alle Badges sind grau. Das ist ein **Live-Default-State**, der bereits AC-3 + AC-7-grau abdeckt. Fuer AC-7-gelb-Test braucht es einen pending Review (siehe SQL-Mock oder Live-Block-Submit oben).
4. **Pre-existing TS-Errors `bridge/__tests__/action-helpers.test.ts:127,129`** (aus SLC-035). Nicht in SLC-043-Scope. V4.2-Cleanup-Item.

## Open points or deferred decisions

1. **AC-2 + AC-12 + Quick-Stats-Badge-gelb** — User-Pflicht-Browser-Smoke (gebuendelt mit SLC-045-Smoke nach Coolify-Redeploy).
2. **Performance-Mock-Test (AC-9)** — optional V4.2 oder /qa-Stretch.
3. **`/dashboard/reviews`-Existenz** — Build listet die Route, vermutlich SLC-042 Cockpit-Card-Link-Ziel (Tenant-User-Sicht auf eigene Reviews). Nicht in SLC-043-Scope.
4. **Pre-existing TS-Errors `action-helpers.test.ts:127,129`** — V4.2-Cleanup oder im Rahmen einer V4.1-Bereinigung.

## User-Pflicht: Browser-Smoke-Plan SLC-043 (gebuendelt mit SLC-045)

**Voraussetzung:** Onboarding-Plattform ist auf Production-Stand `76856ca` deployt (User-Pflicht via Coolify, `feedback_manual_deploy.md`). Vor dem Smoke: **gemeinsamer Coolify-Redeploy fuer SLC-045 + SLC-043**.

**Ablauf** — kann in einer einzigen User-Sitzung mit dem SLC-045-Smoke aus RPT-106 zusammen durchlaufen werden, ~25-35 Min Total.

### Smoke-Punkt 8 — Sidebar-Link "Reviews" sichtbar (AC-8)

- **Aktion (als strategaize_admin):** Admin-Sidebar links pruefen.
- **Erwartung:** Sidebar-Link "Reviews" zwischen "Tenants" und "Debrief", ClipboardCheck-Icon. Klick navigiert zu `/admin/reviews`.

### Smoke-Punkt 9 — Cross-Tenant-Reviews-Page Empty-State (AC-1, AC-3)

- **Aktion:** `/admin/reviews` aufrufen.
- **Erwartung:** Header "Pending Reviews" + Subtext. Stats-Zeile zeigt "0 pendend" (kein "ueber X Tenants"-Suffix). **Empty-State-Card** "Aktuell keine pendenden Reviews. Alle Mitarbeiter-Bloecke sind aktuell entweder approved oder rejected."

### Smoke-Punkt 10 — Cross-Rollen-Auth-Test (AC-2)

- **Aktion 10a (als tenant_admin):** Direct-URL-Aufruf von `/admin/reviews`.
- **Erwartung:** Browser navigiert zu `/dashboard` (Redirect via Layout-Gate `["strategaize_admin","tenant_admin"]` PLUS Page-Level `role !== "strategaize_admin"` → /dashboard).
- **Aktion 10b (als employee, falls eingeloggt):** Direct-URL-Aufruf.
- **Erwartung:** Layout-Gate redirected schon, kommt nicht zur Page durch.

### Smoke-Punkt 11 — Pro-Tenant-Reviews-Page (AC-5)

- **Aktion:** `/admin/tenants/<demo-tenant-uuid>/reviews` aufrufen (UUID aus DB oder via "X Reviews"-Badge in /admin/tenants).
- **Erwartung:** Header "Pending Reviews — Demo Onboarding GmbH" + "Alle Tenants" Back-Link. Subtext. Stats-Zeile "0 pendend". Empty-State-Card.

### Smoke-Punkt 12 — Pro-Tenant ungueltige UUID (AC-5 Edge)

- **Aktion:** `/admin/tenants/garbage-id/reviews` aufrufen.
- **Erwartung:** notFound (404-Page) — keine 500-Error.

### Smoke-Punkt 13 — Quick-Stats-Badge im Tenants-Eintrag (AC-6, AC-7-grau)

- **Aktion:** `/admin/tenants` aufrufen.
- **Erwartung:** Tenant-Card-Header zeigt nach Sprach-Badge einen **grauen Badge** "0 Reviews" mit ClipboardCheck-Icon. Hover zeigt Title "Keine pendenden Reviews".

### Smoke-Punkt 14 — Quick-Stats-Badge gelb (AC-7-gelb)

- **Voraussetzung:** Mock-pending-Review im Demo-Tenant erzeugen (SQL siehe oben oder echter Block-Submit).
- **Aktion:** Refresh `/admin/tenants`.
- **Erwartung:** Tenant-Card zeigt **gelben Badge** "1 Review" (singular). Klick navigiert zu `/admin/tenants/<id>/reviews`. Dort sieht der User die pendende Review als Tabellen-Zeile.
- **Cleanup:** SQL-DELETE wie oben.

### Smoke-Punkt 15 — Action-Link Pruefen → SLC-042-View (AC-4)

- **Voraussetzung:** Mindestens ein pendender Review (Smoke-Punkt 14 noch aktiv).
- **Aktion:** In der Tabelle auf "Pruefen"-Button klicken.
- **Erwartung:** Browser navigiert zu `/admin/blocks/<blockKey>/review?tenant=<tenantId>&session=<sessionId>`. SLC-042-View laedt mit Block-Header + Mitarbeiter-KU-Stack + Approve/Reject-Buttons.

### Smoke-Punkt 16 — Pencil-Sekundaer-Link → V2-Debrief

- **Aktion:** Auf das Pencil-Icon in der Tabellen-Zeile klicken.
- **Erwartung:** Browser navigiert zu `/admin/debrief/<sessionId>/<blockKey>` (V2-Debrief-Editor).

### Smoke-Punkt 17 — Responsive (AC-12)

- **Aktion:** DevTools → Responsive-Mode → 375 / 768 / 1024 px.
- **Erwartung:**
  - 375: Tabelle-Wrapper hat horizontalen Scrollbalken; alle Spalten erreichbar.
  - 768: Tabelle bricht eventuell schon stabil ohne Scroll.
  - 1024+: Standard-Tabelle, alle Spalten sichtbar.

### Reporting

Nach Durchlauf melden:
- Smoke-Punkte 8-13 + 17: PASS oder FAIL je Punkt
- Smoke-Punkte 14-16: durchgefuehrt mit Mock-Review oder echter Block-Submit?
- Cross-Rollen-Test (Punkt 10): tenant_admin redirected sauber?
- R17-Persona-Feedback (Bonus)

Bei Bugs: Iter-Loop wie SLC-044 (RPT-104).

Bei PASS: User meldet "Browser-Smoke PASS" → ich update SLC-043 Status auf `done`, `/docs/STATE.md`, FEAT-030 bleibt `in_progress` bis Gesamt-V4.1-Release.

## Project record updates

- `/slices/INDEX.md` — KEINE Aenderung. SLC-043 bleibt `in_progress` bis User-Smoke-PASS.
- `/features/INDEX.md` — KEINE Aenderung. FEAT-030 bleibt `in_progress`.
- `/planning/roadmap.json` — KEINE Aenderung. V4.1 bleibt `active`.
- `/planning/backlog.json` — KEINE Aenderung.
- `/docs/STATE.md` — Current Focus aktualisiert.
- `/docs/KNOWN_ISSUES.md` — keine neuen Issues.
- `/docs/DECISIONS.md` — keine neuen DECs.
- `/docs/MIGRATIONS.md` — keine Migration in SLC-043.

## Recommended next step

**Gebuendelter Coolify-Redeploy SLC-045 + SLC-043** (User-Pflicht via Coolify-UI, `feedback_manual_deploy.md`). Anschliessend **gemeinsamer Browser-Smoke** beider Slices in einer User-Sitzung — 17 Smoke-Punkte total (7 aus RPT-106 fuer SLC-045 + 10 aus RPT-108 fuer SLC-043).

Bei Smoke-PASS:
- `/slices/INDEX.md` SLC-045 + SLC-043 → `done`
- `/features/INDEX.md` FEAT-028 + FEAT-030 → `done`
- `/docs/STATE.md` Update auf "V4.1 5/5 Slices done"
- Naechster Schritt: **Gesamt-V4.1-/qa** (SC-V4.1-1..12 vollstaendig verifizieren) → /final-check → /go-live → /deploy V4.1

Bei Smoke-FAIL: Iter-Loop fuer den betroffenen Slice (analog SLC-044 mit 5 Cycles).

## Lessons / IMP-Kandidaten

Keine prozess-relevanten neuen IMPs aus dieser /qa-Phase identifiziert.

Beobachtung: Die Slice-Spec-Pfad-Drift fuer `AdminSidebar.tsx` (CamelCase + Subordner Spec-Soll vs. kebab-case-flat Code-Real) ist ein wiederkehrendes Pattern (siehe IMP-Kandidaten aus RPT-107). Pre-Read via Glob faengt das zuverlaessig auf — kein Prozess-Problem, aber ein gut-zu-merkendes Default fuer kuenftige /frontend-Slices. Falls dieses Drift-Pattern in 2+ weiteren Slices auftritt: dann waere ein IMP-Kandidat "Slice-Spec-Pfad-Drift-Faengt vor Implementation".
