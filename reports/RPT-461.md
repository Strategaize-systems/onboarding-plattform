---
id: RPT-461
date: "2026-06-12"
skill: /backend
slice: SLC-V9.5-D
feature: FEAT-080
type: completion
status: completed
reviewOf: null
result: "Curation-Contract-Shift auf email_synthesized_unit (MT-1..4) implementiert, Pseudonym-Lookup entfernt. Gates GREEN. MT-5 (Gesamt-/qa + Master-Merge) offen."
title: "SLC-V9.5-D Curation-Contract-Shift MT-1..4 — /backend+/frontend"
---

# SLC-V9.5-D Curation-Contract-Shift — MT-1..4 Completion Report

## Outcome

MT-1..4 vollstaendig (Commit a0dc12f). Die GF-Curation operiert ab jetzt auf
den konsolidierten `email_synthesized_unit`-Rows (DEC-214-Folge); der
thread-lokale Pseudonym-Lookup im Handbook-Import ist entfernt (AC-D-2);
das Promotion-Target (knowledge_unit-INSERT + Snapshot-Trigger + Rollback-
Loop) ist strukturell unveraendert (AC-D-1 / SC-V9.5-5). KEINE Migration
(nutzt MIG-111). **MT-5 (Gesamt-/qa V9.5 + Pre-Merge-Re-Check + Master-Merge)
ist bewusst offen — eigener naechster Schritt; SLC-V9.5-D bleibt in_progress.**

## Files changed (Commit a0dc12f)

- `/src/app/dashboard/bulk-email-import/[run_id]/curation/helpers.ts` —
  `CurationUnit` ersetzt `CurationPattern` (aggregated_confidence,
  evidence_count, source_pattern_ids; kein thread_id); `extractSnippetTexts`
  fuer `{text, source_pattern_id}`-jsonb-Snippets; `CurationData.units`;
  `UpdateUnitCurationResult`; `ImportToHandbookResult.unitsImported`.
- `/src/app/dashboard/bulk-email-import/[run_id]/curation/actions.ts` —
  alle 5 Actions auf `email_synthesized_unit`: `getCurationData` (ORDER
  aggregated_confidence DESC), `updateUnitCuration`, `bulkAcceptUnits`
  (gte aggregated_confidence), `bulkRejectAllUnits`,
  `finishCurationAndStartHandbookImport` (Status-Guard `synthesized|curating`,
  AC-D-4 — loest R-D-3 Status-Stall nach SLC-V9.5-B/C auf), `importToHandbook`
  (Unit-SELECT, KEIN email_thread-Lookup, Rollback-Pfad auf Unit-Spalten).
- `/src/lib/bulk-email/handbook-import.ts` — `SynthesizedUnitForImport` +
  `mapSynthesizedUnitToKnowledgeUnit` ersetzen `PatternForImport` +
  `mapPatternToKnowledgeUnit`; `renderSourceAttributionMarkdown` ohne
  Pseudonym-Zeile, stattdessen `**Belege**: N Quell-Patterns` (R-D-4);
  `SourceAttribution`-Metadata mit synthesized_unit_id + source_pattern_ids
  + evidence_count.
- `/src/app/dashboard/bulk-email-import/[run_id]/curation/CurationClient.tsx` —
  Props/State/Handler auf Units; Texte "Wissens-Bausteine".
- `/src/app/dashboard/bulk-email-import/[run_id]/curation/page.tsx` —
  EDITABLE = `synthesized|curating`; `pattern_extracted`/`synthesizing`
  jetzt PENDING; Texte aktualisiert.
- `/src/app/dashboard/bulk-email-import/[run_id]/curation/__tests__/actions.test.ts` —
  36 Tests auf Unit-Welt (+2 neue Status-Guard-Cases: pattern_extracted
  rejected, synthesized akzeptiert; Happy-Path prueft AC-D-2 body/metadata
  ohne Pseudonyme).
- `/src/lib/bulk-email/__tests__/handbook-import.test.ts` — 16 Tests auf
  neuen Mapper (inkl. Belege-Zeile, kein **Pseudonyme**, source_pattern_ids-
  Default []).

## Files renamed

- `.../curation/components/PatternCard.tsx` → `UnitCard.tsx` (git mv;
  + Evidenz-Count-Badge AC-D-5, Snippet-Texte via extractSnippetTexts,
  Hinweis "Klarnamen bereits in der Synthese entfernt").
- `.../curation/components/EditPatternModal.tsx` → `EditUnitModal.tsx` (git mv).

## Intentionally unchanged

- `/src/workers/bulk-email/handle-synthesis-job.ts` (Synthese/Critic — Out-of-Scope).
- `knowledge_unit`-Schema, `rpc_trigger_handbook_snapshot`, `handle-snapshot-job.ts`
  (AC-D-1: Target unveraendert).
- `email_bulk_run.patterns_imported`-Spalte (zaehlt jetzt Units; Schema-Rename
  waere eine unnoetige Migration — dokumentiert).
- Andere `email_pattern`-Pfade ausserhalb der Bulk-Curation-Route (Spec
  Out-of-Scope; `_source`-Provenance bleibt Audit-only).

## Quality gates

- tsc=0; ESLint=0 (alle Touched-Files); targeted 52/52 GREEN.
- Full-Suite 1848 pass / 516 DB-gated / 36 skip — 0 Regression vs C-Baseline.
- R-D-1-Scan: 0 stale `email_pattern`-Refs im Curation-/Import-Pfad.
- AC-D-2-Scan: 0 `participant_pseudonyms`/`email_thread`-Refs (nur Doku-Kommentare).
- `next build`: Compile PASS; Page-Data-Collection-Fail `/api/public/partner/[slug]`
  via git-stash-Gegenprobe als PRE-EXISTING ENV-gated bewiesen (kein D-Regress).

## Problems found

- Pre-existing Build-Page-Data-Fail (s.o.) — nicht D-verursacht, ENV-gated.
- Implementation direkt statt Subagent-Orchestrierung (Skill empfiehlt
  Subagents bei 3+ Tasks/SaaS): bewusste Abweichung — alle Contract-Files
  waren vollstaendig im Orchestrator-Kontext geladen; ein Handoff haette
  Kontext-Verlust riskiert. Review-Disziplin pro MT wurde beibehalten.

## Open points

- **MT-5:** Gesamt-/qa V9.5 (Cross-Slice A..D, End-to-End auf Coolify-DB
  AC-D-6, RLS) → Pre-Merge-Re-Check (6 Schritte) → Master-Merge --no-ff.
- Browser-Smoke der Curation-UI + Bedrock-Live-Smokes (A/B/C) Founder-gated → /deploy.

## Next step

Slice-/qa Code-Side (RPT-462) ist mitgelaufen. Naechster Schritt: **MT-5**
(empfohlen als frische Session — Gesamt-/qa + Merge ist ein eigener Block).
