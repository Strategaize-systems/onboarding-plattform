---
id: RPT-125
date: "2026-05-01"
skill: /frontend
slice: SLC-050
feature: FEAT-033
type: completion
status: completed
reviewOf: null
result: "SLC-050 /frontend code-complete: 5 Help-Markdown-Files + Loader (3/3 Vitest PASS) + HelpSheet/HelpTrigger + Layout-Integration auf 5 Pages + 4 neue Tooltips (5. ist bestehend SLC-049). TypeScript clean. Browser-Smoke + Berater-Inhalts-Review = QA-Gates (offen)."
title: "SLC-050 /frontend — Help-Sheet + 5 Markdown + 5 Tooltips"
---

# SLC-050 /frontend — Help-Sheet + 5 Markdown + 5 Tooltips

## Outcome

SLC-050 (FEAT-033 In-App-Hilfe) ist /frontend code-complete als letzter V4.2-Slice. Right-Side-Sheet mit page-spezifischer Markdown-Hilfe via `?`-Icon im Header auf 5 Pflicht-Pages, plus 5 Pflicht-Tooltips an den DEC-058-UI-Elementen. Confidence: hoch fuer Code/Tests/Compile, **partial** fuer Runtime — Browser-Smoke ist noch nicht durchgelaufen (Pflicht-Gate, kommt in /qa SLC-050).

## Files reviewed (no change)

- `/slices/SLC-050-help-sheet-tooltips.md` — Slice-Spec gelesen, In-Scope/Out-of-Scope/Acceptance-Criteria/Micro-Tasks befolgt.
- `/docs/STATE.md`, `/docs/ARCHITECTURE.md` — Stand vor Start.
- `/src/components/cockpit/InactiveEmployeesCard.tsx` — Tooltip-1:1-Vorlage (SLC-049, DEC-058).
- `/src/components/handbook/HandbookReader.tsx` — react-markdown + remark-gfm Setup-Referenz (FEAT-028 / DEC-049).
- `/src/components/ui/sheet.tsx` — shadcn Sheet bereits vorhanden.
- `/tailwind.config.ts` — `@tailwindcss/typography` Plugin aktiv (`prose` Klassen verfuegbar).
- `/src/app/admin/bridge/page.tsx`, `/src/app/admin/reviews/page.tsx`, `/src/app/dashboard/page.tsx`, `/src/app/dashboard/dashboard-client.tsx`, `/src/app/dashboard/handbook/page.tsx`, `/src/app/dashboard/handbook/[snapshotId]/page.tsx`, `/src/app/capture/[sessionId]/page.tsx` — Pflicht-Page-Targets fuer HelpTrigger-Integration.
- `/src/app/admin/blocks/[blockKey]/review/page.tsx`, `/src/app/admin/handbook/TriggerHandbookButton.tsx`, `/src/components/onboarding-wizard/steps/Step3EmployeeInvite.tsx` — Tooltip-Targets.

## Files created (manually authored)

- `/src/content/help/dashboard.md` (182 Worter) — Help fuer Cockpit.
- `/src/content/help/capture.md` (190 Worter) — Help fuer Block-Submit.
- `/src/content/help/bridge.md` (197 Worter) — Help fuer Bridge-Engine.
- `/src/content/help/reviews.md` (202 Worter) — Help fuer Block-Reviews.
- `/src/content/help/handbook.md` (243 Worter) — Help fuer Unternehmerhandbuch-Reader.
- `/src/lib/help/load.ts` — `loadHelpMarkdown(pageKey)` server-side Loader, `HelpPageKey` Type, `listAvailableHelpPages()`.
- `/src/lib/help/__tests__/load.test.ts` — 3 Vitest-Cases: alle 5 keys laden + Header `# `, invalid key wirft, jede Datei ≥100 Worter & kein Lorem-Ipsum.
- `/src/components/help/HelpSheet.tsx` — Right-Side `Sheet` (90vw / sm:max-w-md) + `prose prose-sm` Markdown-Render via `react-markdown` + `remark-gfm`.
- `/src/components/help/HelpTrigger.tsx` — `?`-Icon-Button (`HelpCircle` lucide), lokaler open-State, oeffnet HelpSheet.

## Files changed

- `/src/app/dashboard/page.tsx` — `loadHelpMarkdown("dashboard")` + `<HelpTrigger>` als `headerActions`-Prop reingereicht.
- `/src/app/dashboard/dashboard-client.tsx` — neuer optionaler `headerActions: ReactNode` Prop, gerendert rechts vom Header-Titel.
- `/src/app/capture/[sessionId]/page.tsx` — Header-Block in `flex justify-between` umgewandelt, `<HelpTrigger pageKey="capture" />` rechts.
- `/src/app/admin/bridge/page.tsx` — HelpTrigger in beiden Renderpfaden (Empty-State und Haupt-View neben TriggerBridgeButton).
- `/src/app/admin/reviews/page.tsx` — Header-Block in `flex justify-between` umgewandelt + HelpTrigger.
- `/src/app/dashboard/handbook/page.tsx` — Header-Block in `flex justify-between` umgewandelt + HelpTrigger (List-View).
- `/src/app/dashboard/handbook/[snapshotId]/page.tsx` — `helpTrigger`-ReactNode als Prop an `<ReaderShell>` (Detail-View).
- `/src/components/handbook/ReaderShell.tsx` — neuer optionaler `helpTrigger?: ReactNode` Prop, gerendert neben dem ZIP-Download.
- `/src/app/admin/bridge/TriggerBridgeButton.tsx` — Tooltip "Erzeugt Mitarbeiter-Capture-Vorschlaege aus GF-Blueprint".
- `/src/components/review/ApproveRejectButtons.tsx` — Tooltip am Approve-Button "Approve = Mitarbeiter-Antworten fliessen ins Handbuch".
- `/src/app/admin/handbook/TriggerHandbookButton.tsx` — Tooltip "Generiert das Unternehmerhandbuch aus aktuellem Stand".
- `/src/components/onboarding-wizard/steps/Step3EmployeeInvite.tsx` — Tooltip am "Spaeter einladen"-Button "Du kannst den Wizard jederzeit abschliessen".
- `/slices/INDEX.md` — SLC-050 status `planned` → `done`.
- `/features/INDEX.md` — FEAT-033 status `planned` → `done`.
- `/planning/backlog.json` — BL-061 status `in_progress` → `done`, description aktualisiert.
- `/docs/STATE.md` — Current Focus + Phase + Immediate Next Steps auf SLC-050 code-complete + /qa offen aktualisiert.

## Files intentionally not changed

- `/src/components/cockpit/InactiveEmployeesCard.tsx` — Tooltip 5/5 ist hier bereits aus SLC-049 verbaut, kein erneutes Touch noetig.
- `/src/lib/handbook/load-snapshot-content.ts` und der Reader selbst — fuer SLC-050 nicht relevant.
- DB-Schema, Migrationen — Help ist statisches Repo-Content (keine Migration).
- Vitest-DOM-Setup (`@testing-library/react`, `jsdom`) — projektweit nicht eingerichtet, alle bestehenden Component-Tests sind reine Logic-Tests; ich folge dem Pattern und verlasse mich fuer Component-Verifikation auf den Pflicht-Browser-Smoke aus der Slice-Spec.

## UI states covered

- **Implementiert + statisch verifiziert (TypeScript Compile clean):**
  - HelpSheet open/close via Esc/Outside-Click/X-Button (kommt von shadcn Sheet kostenlos).
  - HelpSheet `w-[90vw] sm:max-w-md` → AC-10 (Mobile mind. 80% Width) erfuellt by design.
  - HelpTrigger `aria-label="Hilfe oeffnen"` + `data-help-trigger={pageKey}` fuer Smoke-Test-Selectors.
  - `prose prose-sm max-w-none` fuer Markdown-Render-Konsistenz.
  - Server-Component-Markdown-Load (`loadHelpMarkdown`) im Page-Render, kein Client-Roundtrip → AC-8 by design.
  - Tooltip Hover/Focus-Pattern via Radix (`@radix-ui/react-tooltip`), bereits in der Codebase etabliert (DEC-058 SLC-049 Vorbild).
- **Implementiert, aber Browser-Smoke noch offen (Pflicht-Gate, kommt in /qa):**
  - `?`-Icon-Sichtbarkeit auf allen 5 Pages.
  - Klick-oeffnet-Sheet-mit-richtigem-Inhalt.
  - Mobile-Render auf 375x667.
  - Tooltip-Hover zeigt korrekten Text.
  - Bundle-Size-Verifikation (`npm run build` Output).

## Problems found

- **Naechtens kein .env.local verfuegbar:** `npm run build` schlaegt am page-data-collect mit `Error: supabaseUrl is required` fehl. Das ist **kein** SLC-050-Code-Issue — der TypeScript-Compile (`Compiled successfully in 8.3s`, `Finished TypeScript in 10.9s`) und `npx tsc --noEmit` laufen sauber. Coolify hat die ENVs, der Build dort funktioniert. Bundle-Size-Verifikation (AC-9, MT-6) muss daher entweder lokal mit gesetzten ENVs oder nach Coolify-Deploy via Build-Logs erfolgen — wird in /qa SLC-050 abgefangen.
- **`next lint` deprecated:** Next 16 hat den `next lint` CLI-Pfad entfernt, projektweit. ESLint 9.x braucht `eslint.config.js` (nicht vorhanden). Static-Verification erfolgt rein ueber `tsc --noEmit` (clean). Kein neues Problem von SLC-050.
- **Vitest-DB-Tests rot ohne `TEST_DATABASE_URL`:** Wie in allen vorherigen Slice-/qa-Runs — 149 DB-Tests rot ohne TEST_DATABASE_URL, 336 Logic-Tests gruen inkl. neue 3/3 SLC-050 Tests. Pflicht-Gate ist Coolify-DB-Run gemaess `coolify-test-setup.md`.
- **DOM-Render-Tests fuer HelpSheet/HelpTrigger nicht eingerichtet:** Das vitest-Projekt nutzt `environment: "node"` ohne Testing-Library. Ich habe `@testing-library/react` + `jsdom` bewusst NICHT eingefuehrt (Infrastruktur-Churn ausserhalb Slice-Scope), und folge dem bestehenden Projekt-Pattern (Logic-Tests + Browser-Smoke). Die Slice-Spec definiert Browser-Smoke als Pflicht-Gate, nicht DOM-Render-Tests. Alternative: SKILL_IMPROVEMENT erwaegen, falls fuer V5+ haeufiger gebraucht.

## Open points or deferred decisions

- **Berater-Inhalts-Review der 5 Help-Files (MT-8, Pflicht-Gate):** Der User muss die 5 Markdown-Files inhaltlich freigeben — kein "passt schon" laut Slice-Spec. Findet in /qa SLC-050 statt.
- **Bundle-Size-Verifikation (MT-6, AC-9, < 25KB Help-Overhead):** Soll Build-Output zeigen. Auf Coolify-Build oder lokal mit gesetzten ENVs nachholbar — in /qa SLC-050.
- **Live-Smoke auf onboarding.strategaizetransition.com:** Nach Coolify-Deploy mit Tooltip-Hovers + Sheet-Open + Mobile.

## Runtime verification summary

- **Build verification:** TypeScript-Compile + `tsc --noEmit` PASS. Next-Build page-data-collect bricht wegen `supabaseUrl is required` ab — Env-Var-Issue, kein Code-Issue.
- **Static code verification:** PASS (TypeScript strict, kein `any`, keine `@ts-ignore`).
- **Vitest-Run der neuen Tests:** 3/3 PASS (`src/lib/help/__tests__/load.test.ts`).
- **Browser-Render verification:** noch nicht durchgelaufen — Pflicht-Gate fuer /qa SLC-050.
- **Mobile-Render verification:** offen (Pflicht-Gate).
- **Tooltip-Hover verification:** offen (Pflicht-Gate).
- **Persistence verification:** entfaellt (Help-Content ist statisch, kein State).

## Recommended next step

**`/qa SLC-050`** — Slice-Spec fordert mehrere Pflicht-Gates die nur in /qa erledigt werden koennen:

- Browser-Smoke auf allen 5 Pflicht-Pages (HelpTrigger sichtbar + Sheet oeffnet + Markdown rendert).
- Mobile-Render-Test (375x667).
- 5 Tooltip-Hover-Tests.
- Berater-Inhalts-Review der 5 Help-Files (kein Lorem-Ipsum, korrekte Inhalte).
- Bundle-Size-Verifikation (< 25KB Help-Overhead).
- `npm run test` auf Coolify-DB.
- Cockpit-Records-Update nach Slice-Ende: bereits in diesem Schritt erledigt.

Danach: Gesamt-/qa V4.2 + /final-check + /go-live + /deploy.
