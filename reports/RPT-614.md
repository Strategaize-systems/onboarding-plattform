---
id: RPT-614
date: "2026-07-08"
skill: /go-live
slice: V10.4
feature: FEAT-105+FEAT-106+FEAT-107
type: completion
status: completed
reviewOf: RPT-613
result: "Go-Live V10.4 CONDITIONAL GO. Live-Baseline verifiziert (app/worker/kong/db healthy 42h; MIG-132-Pre-Apply-Annahmen driftfrei: role-CHECK=4 Werte, berater_tenant_assignments absent, on_auth_user_created present, fn absent, 0 berater-User). 0 Blocker aus V10.4. 5 bindende Deploy-Gates: Pre-Merge-Rebase + MIG-132-Live-Apply (Pre-Apply-Dump) + 2 DB-Sidecars (0-Regression changed-surface) + Browser-E2E + Container-Health. Rollback RPO 0 (additiv). Naechster Schritt: /deploy (REL-041)."
title: "Go-Live V10.4 — strategaize_berater P2 (CONDITIONAL GO)"
---

# Go-Live V10.4 — Rollenmodell V2 P2 `strategaize_berater`

## 1. Release-Target
Interne Workflow-Closure V10.4 auf den self-hosted Coolify/Hetzner-Stack (`159.69.207.29`,
Prod-App `onboarding.strategaizetransition.com`). **Kein Customer-Go-Live** (module-lifecycle-discipline:
Founder-only Internal-Mode; keine Pilots/Outreach/Legal-Pre-Live).

## 2. Readiness-Entscheidung: **CONDITIONAL GO**

Entry-Conditions erfuellt: Gesamt-QA V10.4 PASS (RPT-612), `/final-check` CONDITIONALLY READY (RPT-613,
alle 3 Mechanical Gates PASS), 0 Blocker. Die Unmerged-Branch-/Records-Fragmentierung (Branch hinter main)
ist bekannt und via Gate G-1 (Pre-Merge-Rebase) bindend aufzuloesen. „Conditional", weil das Feature bis
zum MIG-132-Apply live unverifiziert ist — die Bedingungen sind Deploy-Ausfuehrungs-Reihenfolge, kein Fix.

## 3. Live-Baseline-Verifikation (2026-07-08, gegen Prod)
- **Container:** app / worker / supabase-kong / supabase-db alle `Up 42h (healthy)` (= stabiler V10.3-Stand).
- **MIG-132-Pre-Apply-Annahmen driftfrei** (`docker exec psql` gegen Live-DB):
  - `profiles_role_check` = **4 Werte** (admin/tenant_admin/employee/partner_admin) — CHECK 4→5 appliziert kollisionsfrei.
  - `berater_tenant_assignments` = **absent** — CREATE TABLE appliziert.
  - `on_auth_user_created`-Trigger = **present (1)** — `handle_new_user` CREATE-OR-REPLACE gueltig.
  - `berater_assigned_tenant_ids` = **absent (0)** — Function-Neu-Anlage sauber.
  - `strategaize_berater`-User live = **0** — konsistent (Anlage erst via SLC-189 nach Apply).
- Damit ist MIG-132 (additiv, in-tx-ROLLBACK-validiert RPT-606) live applizierbar ohne Konflikt.

## 4. Blockers
Keine.

## 5. Accepted risks
- **Live-Verhalten des Berater-Pfads bis Apply unverifiziert** — mitigiert durch in-tx-Validierung + Pre-Apply-Dump + Post-Apply-Sidecars + Browser-E2E (G-2..G-4).
- **L-1 (RPT-612/613):** 2 Legacy-/admin-Pages schuetzen den Berater implizit via RLS-0-Rows (fail-safe, nur Direkt-URL) — BL-533-Kandidat, nicht release-blockierend.
- **3 pre-existing offene High** (ISSUE-093 Bedrock-Haiku-Deprecation, ISSUE-085/084 Founder-Content-Mocks) — nicht V10.4, kein Closure-Blocker.
- **`/security-audit` deferred** bis Pre-Customer-Live (V10.4 beruehrt Auth/RLS → Pflicht VOR Multi-User, jetzt nicht).

## 6. Required fixes before release
Keine Code-Fixes. Nur die Deploy-Gates (§7) in der vorgegebenen Reihenfolge ausfuehren.

## 7. Deploy-Gates fuer `/deploy` (bindend, in Reihenfolge)

**G-1 — Pre-Merge-Re-Check + Rebase ZUERST** (git-release.md 6 Schritte):
`git fetch && git rebase origin/main` im Worktree (Branch Code @ `5abbc2c` / Records @ `fbfabce` HINTER
`b89aaa3`; die 2 fehlenden Commits sind **docs-only** → Rebase near-clean; SLC-188 done/BL-530 done sind
auf beiden Seiten gesetzt). Post-Rebase: targeted Vitest re-run · MIG-Kollisionscheck (**MIG-132 = einzige
V10.4-Migration, keine Kollision**) · Manual-Diff-Review (Scope = 34 Files, keine Ueberraschungen) · Merge → main → push.

**G-2 — MIG-132 Live-Apply VOR Redeploy** (sql-migration-hetzner.md):
Pre-Apply `pg_dump -s` → `/root/mig132_rollback/` (MIG-131-Muster). Apply via base64+psql. Post-Apply-Verify:
role-CHECK = **5 Werte**, `berater_tenant_assignments` existiert + 2 Policies + Index, `berater_assigned_tenant_ids`
+ `can_see_tenant` present (GRANTs: authenticated/service_role, REVOKE anon), `NOTIFY pgrst` reload.

**G-3 — DB-Sidecar-Gate = 0 Regressions in der changed Surface** (NICHT „gesamte Server-DB-Suite gruen"):
Nach Apply via node:22-Sidecar (coolify-test-setup.md) die **2 V10.4-Sidecars** laufen lassen —
`berater-foundation-rls.test.ts` (Cascade/can_see_tenant/RLS/handle_new_user + Rollen-Regression) +
`berater-visibility.test.ts` (Filter=Kanzlei∪Cascade, Fremd=0, Admin=alle) — **beide gruen** = die
PENDING-LIVE-Tests aus RPT-612 eingeloest. V10.4 = 1 additive Migration → die breitere Server-DB-Suite ist
reine Regression-Sanity auf sonst unveraendertem Schema; **fixture-fragile pre-existing Fails sind KEIN
Deploy-Blocker** (V10.3-Praezedenz RPT-598/IMP-1393).

**G-4 — Browser-E2E-Smoke** gegen deployed main (RPT-611 §6, `SOURCE_COMMIT` == merged HEAD ZUERST pruefen):
Berater-Login → `/admin/mein-tag` + BeraterSidebar (nur Mein Tag + zugewiesene Mandanten) · TenantSelector
nur zugewiesene · 4 Report-Buttons (KEIN System-Status) · RAG Fremd-Tenant → `no_tenant` · `/admin/tenants`
→ redirect `/dashboard` · Admin-Login unveraendert (alle Tenants, 5 Buttons) · **0 Console-Errors**.

**G-5 — Container-Health post-Redeploy:** app/worker/kong/db alle `(healthy)`, RestartCount stabil.

## 8. Rollback-Awareness
- **Code:** merged HEAD → vorheriger main (pre-V10.4) via Coolify-Redeploy des alten `SOURCE_COMMIT`.
- **DB:** MIG-132-Revert aus Pre-Apply-Dump: `DROP FUNCTION can_see_tenant, berater_assigned_tenant_ids;
  DROP TABLE berater_tenant_assignments; role-CHECK zurueck auf 4 Werte; handle_new_user aus Dump re-apply`.
- **RPO 0:** additive Migration, keine Daten-Transformation; kein Datenverlust bei Revert.

## 9. Operational concerns
- Reihenfolge kritisch: **G-1 vor G-2 vor Redeploy** — Apply gegen ungemergte DB ist ok (additiv), aber der
  Code-Redeploy DARF erst nach erfolgreichem Apply laufen (sonst Berater-Pfad 500 gegen fehlende Tabelle).
- Auto-Deploy-Awareness: main-Push triggert ggf. Coolify-Redeploy → Apply-Fenster VOR dem Push-getriggerten
  Redeploy einplanen (oder Redeploy nach Apply manuell).

## 10. Recommended next step
**`/deploy V10.4`** (REL-041) — G-1..G-5 in Reihenfolge ausfuehren. Danach `/post-launch V10.4`.
Parallel offen (blockt nicht): `/post-launch V10.3` T+24h → Last-Stable-Flip V10.2.1→V10.3.
