# Releases

### REL-001 — SLC-001 Initial Deploy (V1-preview)
- Date: 2026-04-15
- Scope: Baseline-Datenmodell (tenants, profiles) + Core-Capture-Schema (template, capture_session, block_checkpoint, knowledge_unit, validation_layer) + RLS-Policies + Auth-Helper-Funktionen + handle_new_user-Trigger. Next.js-App-Container + 9 Supabase-Container + Whisper-Container auf Hetzner CPX62 (159.69.207.29). Domain: https://onboarding.strategaizetransition.com mit Let's-Encrypt-Cert.
- Summary: Erste produktive Instanz der Onboarding-Plattform. SLC-001 MT-1..MT-4 waren 2026-04-14 im Code fertig, MT-6 (Hetzner-Deploy) ist heute nach Aufraeumen der Hostname-Kollision und SSH-Zugang durch.
- Risks: Test-Infrastruktur fehlt noch (ISSUE-002), d.h. RLS-Integrationstest (MT-5) konnte nicht automatisiert laufen. Login-Flow + Admin-User-Seeding noch manuell auszuprobieren. Keine Bestandskunden, also niedriger operationeller Risikofaktor.
- Rollback Notes: `ssh root@159.69.207.29 'docker compose -p bwkg80w04wgccos48gcws8cs down -v'` entfernt alle 10 Container + Volumes. Coolify-Resource + DNS bleiben fuer Redeploy stehen. DB-Volume `bwkg80w04wgccos48gcws8cs_db-data` enthaelt bisher keine Echt-Daten, Verlust akzeptabel.

### REL-002 — V1 Full Deploy
- Date: 2026-04-18
- Scope: Vollstaendiger V1-Stack mit allen 13 Slices (SLC-007 reverted). 5/6 Features live. 12 Container (App + Worker + 9 Supabase + Whisper). Alle 15 Migrations (021-037) deployed. Next.js 16.2.4 (Security-Patch). npm audit 0 Prod-Vulnerabilities.
- Summary: Erster vollstaendiger V1-Deploy. App-Container mit KI-Chat (Bedrock eu-central-1), Worker-Container mit Multi-Agent Analyst+Challenger Loop, Debrief-UI, Meeting-Snapshot, JSON-Export. AWS-Credentials-Blocker im App-Container gefixt (Go-Live RPT-031). Gesamt-QA PASS (RPT-029), Final-Check PASS (RPT-030).
- Risks: ~41 Blueprint-Legacy-Dateien (kein Runtime-Impact, ISSUE-011). Dashboard zeigt keine Capture-Sessions (ISSUE-012). Error-Logging-Tabelle fehlt in DB. Kein CSP-Header. Kein Backup-Schedule. Kein Impressum/Datenschutz. Alle akzeptiert fuer internen Launch.
- Rollback Notes: Coolify UI → vorheriges Deployment waehlen → Redeploy. DB-Stand unveraendert (keine neuen Migrations in diesem Deploy). Bei Bedarf: `docker compose down -v` — keine Echtdaten, Verlust akzeptabel.
- Post-Launch: STABLE (RPT-032, 2026-04-18). E2E 10/10 PASS. 0 Fehler in Logs. Keine neuen Post-Deploy-Issues. RLS-Isolation verifiziert.

### REL-003 — V1.1 Maintenance Release
- Date: 2026-04-19
- Scope: 2 Slices (SLC-011 Legacy-Cleanup, SLC-012 Dashboard+error_log). 64 Legacy-Dateien geloescht. Dashboard von Blueprint-Runs auf Capture-Sessions umgebaut. error_log-Tabelle erstellt (MIG-012). 5 Issues resolved (ISSUE-003, -006, -011, -012, -013).
- Summary: Maintenance Release. Kein neues Feature, rein subtraktiv + inkrementell. Legacy-Ballast aus Blueprint-Fork entfernt, Dashboard auf echte Daten umgestellt, Error-Logging aktiviert. Gesamt-QA PASS (RPT-035), Final-Check PASS (RPT-036).
- Risks: Keine materiellen. CSP-Header fehlt weiterhin (pre-existing, V2-Scope). Kein Backup-Schedule (pre-existing, keine Echtdaten).
- Rollback Notes: Coolify UI → vorheriges Deployment waehlen. error_log-Tabelle bleibt in DB (additive Migration, kein Schaden).

### REL-004 — V2 Intelligence Upgrade
- Date: 2026-04-21
- Scope: 12 Slices (SLC-013..024), 7 Features (FEAT-010..016), 6 Migrationen (MIG-013..018). 3-Agent Orchestrator Loop, Auto-Gap-Backspelling (2-Runden-Limit), SOP-Generation (Level 2), Template-driven Diagnosis Layer, Evidence-Mode + Bulk-Import + KI-Analyse, Second Template + Switcher UI, Whisper Voice-Input mit Adapter-Pattern.
- Summary: Groesstes Feature-Upgrade seit Launch. Verwandelt die Plattform von einfacher Fragebogen-Erfassung in eine KI-gestuetzte Analyse-Pipeline: Orchestrator steuert Verdichtungsqualitaet, Backspelling schliesst Luecken automatisch, Diagnose-Layer erzeugt strukturierte Bewertungen pro Unterthema, SOPs werden erst nach Diagnose-Bestaetigung generiert. Evidence-Mode ermoeglicht Dokument-Upload mit automatischer Extraktion und Mapping. Voice-Input per Self-hosted Whisper (DSGVO-konform). Gesamt-QA PASS (RPT-053), Final-Check PASS (RPT-054).
- Risks: 1 Medium (supabase-studio unhealthy — nicht produktionskritisch). 3 Low (kein Whisper-Error-Toast, Worker-Logging unstrukturiert, kein Impressum). ISSUE-007 (JWT stale role) akzeptiertes Restrisiko. Kein Backup-Schedule (keine Echtdaten).
- Rollback Notes: Coolify UI → vorheriges Deployment (V1.1). Alle V2-Migrationen sind additiv (neue Tabellen, neue Spalten, neue RPCs) — DB-Rollback nur bei explizitem Bedarf noetig. V1.1-Code ignoriert V2-Tabellen.
- Post-Launch: STABLE (RPT-056, 2026-04-21). 0 Fehler in App/Worker-Logs. 0 Error-Level in error_log. Alle Container healthy (ausser Studio — nicht kritisch). 13 AI-Jobs verarbeitet, 9 Evidence-Files, 2 Whisper-Transkriptionen, 1 Diagnose, 1 SOP. Keine Post-Deploy-Issues.

### REL-005 — V3 Dialogue-Mode
- Date: 2026-04-24
- Scope: 8 Slices (SLC-025..032), 5 Features (FEAT-017..021), 4 Migrationen (MIG-019..022). Eigene Jitsi+Jibri-Instanz auf Onboarding-Server, Meeting-Guide (Basic) mit KI-Vorschlaegen, Dialogue-Session mit Video-Call + Recording + DSGVO-Consent, Post-Meeting KI-Pipeline (Transkript → Knowledge Units + Summary + Gap Detection), Dialogue-Integration in Diagnose/SOP/Debrief. DEC-025..033. Deploy-Commit `e775ff0`. Domain-Mapping: meet-onboarding.strategaizetransition.com → Jitsi-Web, onboarding.strategaizetransition.com → App.
- Summary: Verwandelt die Plattform von einer reinen Fragebogen-Pipeline in einen Dialog-faehigen Erhebungsmodus. Zweiter produktiver Erhebungsmodus neben Questionnaire (Evidence-Mode aus V2 war kein eigenstaendiger Mode). Post-Meeting-Pipeline extrahiert Knowledge Units aus Meeting-Transkript + gleicht gegen Meeting Guide ab (Gap-Detection). Integriert in gleiche Pipeline wie Questionnaire-Mode (Diagnose + SOP + Debrief). Smoke-Test PASS (RPT-065), Gesamt-QA PASS (RPT-065), Final-Check PASS (RPT-066), Go-Live GO (RPT-067). 7 Smoke-Test-Bugs waehrend QA gefixt (u.a. Jitsi-Domain-Mapping, tenant_admin-Layout, Dashboard server-side-loading). UX-Polish post-QA: ISSUE-015 Back-Button (Commit 1009f44), ISSUE-016 Sprachwechsel wontfix (DEC-033), ISSUE-017 24 Demo-Sessions aufgeraeumt.
- Risks: 1 High akzeptabel (npm audit @xmldom/xmldom via AWS-SDK SSO-Pfad, nicht erreichbar mit IAM-Auth — Fix geplant V3.1 BL-038). 2 Medium (admin-rls.test.ts Test-Isolation-Flaw ISSUE-018 BL-039, supabase-studio unhealthy pre-existing BL-040). 5 Low (16× npm Moderate aus gleicher AWS-SDK-Dep-Kette, kein CSP-Header, kein Impressum/Datenschutz, kein Backup-Schedule, 1 console.log). ISSUE-007 (JWT stale role) bleibt akzeptiertes Restrisiko.
- Rollback Notes: Coolify UI → vorheriges Deployment (V2.1-Stand vor 2026-04-22). V3-Migrations 058-064 sind alle additiv (neue Tabellen meeting_guide + dialogue_session, neue Spalten capture_mode + knowledge_unit.source-Erweiterung, neue Storage-Bucket recordings, neue RPCs) — V2-Code ignoriert V3-Tabellen, kein DB-Rollback noetig. Bei Totalausfall: `docker compose down -v` (Verlust der einen Demo-Session 64ad04eb akzeptabel — keine Echtkunden-Daten).

### REL-006 — V3.1 Maintenance Release (+ V4-Schema-Fundament dormant)
- Date: 2026-04-24
- Scope: 3 V3.1-Backlog-Items (BL-038/039/040) + SLC-033 V4 Schema-Fundament code-deployed (dormant, weil keine V4-UI/RPCs). BL-038 `@aws-sdk/client-bedrock-runtime` 3.1024 → 3.1036 + npm-Override `@xmldom/xmldom@^0.8.13` via mammoth → `npm audit --omit=dev` = 0 Vulns (Prod-Kette sauber). BL-039 `admin-rls.test.ts` drei Assertions mit `WHERE tenant_id IN ($1,$2)` isoliert → ISSUE-018 resolved. BL-040 `supabase-studio` `healthcheck: disable: true` in `docker-compose.yml` → DEC-041, ISSUE-020 als `wontfix` geschlossen (bewusster Skip). Plus V4-Schema: 8 Migrationen (065-071 + 075) idempotent auf Coolify-DB appliziert — `employee`-Rolle, `employee_invitation`, `bridge_run`, `bridge_proposal`, `handbook_snapshot`, handbook-Bucket, employee-RLS-Perimeter. V4-Tabellen sind in der Produktions-DB, aber keine App/Worker-Route nutzt sie aktuell. Deploy-Commit `cffc639` (main HEAD zum Deploy-Zeitpunkt). Container: app + worker + supabase-* mit Image-Tag `cffc639addf4e6ab91fa6837b06e1b3c87c96c5f`.
- Summary: Maintenance-Release zum Schliessen der V3-Restrisiken plus Early-Deploy des V4-Schema-Fundaments. Kein V3.1-eigenes Feature, keine User-sichtbare Verhaltensaenderung durch V3.1. V4-Anteil ist bewusst dormant — die V4-UI + RPCs folgen ab SLC-034. Verifikation: alle Container `healthy` 2 Minuten nach Redeploy, Worker-ENV zeigt Bedrock eu-central-1 + Claude Sonnet 4 korrekt, Production-Worker hat `@aws-sdk/client-bedrock-runtime@3.1036.0` + `@xmldom/xmldom@0.8.13` (override wirksam), supabase-studio ohne `.State.Health`-Objekt (DEC-041 wirksam), `admin-rls.test.ts` 2/2 PASS gegen Coolify-DB (BL-039 verifiziert). Kein Bedrock-Roundtrip-Smoke-Test — Cost-Control pro feedback_bedrock_cost_control, echte KI-Arbeit nur user-getriggert.
- Risks: Niedrig. V3.1 ist reiner Maintenance-Release ohne Verhaltensaenderung. V4-Dormant-Anteil birgt theoretisches Risiko, wenn ein Bug in den V4-Migrations versehentlich bestehende V3-Policies/Tabellen beschaedigen wuerde — aber alle 8 Migrationen sind additiv (CREATE TABLE/INDEX/POLICY, DROP CONSTRAINT+ADD CHECK additiv) und idempotent re-runnable. RLS-Test-Matrix-Skelett gegen Coolify-DB gruen (RPT-073, 11 passed + 34 todo). ISSUE-007 (JWT stale role) bleibt akzeptiertes Restrisiko aus V1. Kein CSP-Header, kein Impressum/Datenschutz, kein Backup-Schedule — alle pre-existing aus V1-V3, separate Wartungs-Iteration.
- Rollback Notes: Coolify UI → Deploy-Commit `3bbad32` (pre-V4, V3.1-only) oder `e775ff0` (V3 pre-V3.1). DB-Rollback der 8 V4-Migrationen umgekehrte Reihenfolge: `DROP FUNCTION storage.handle_new_user()` (falls aus Erstanwendung uebrig, siehe IMP-120), `DROP POLICY`s mit `_employee_%` auf capture_session/block_checkpoint/knowledge_unit/validation_layer, DELETE Storage-Bucket handbook + Policies, DROP TABLE handbook_snapshot/bridge_proposal/bridge_run/employee_invitation CASCADE, ALTER TEMPLATE DROP COLUMN employee_capture_schema/handbook_schema, ALTER capture_session/knowledge_unit CHECK auf V3-Stand zuruecksetzen, ALTER profiles.role CHECK auf 3 Werte zuruecksetzen, `handle_new_user()` Whitelist auf 3 Werte zuruecksetzen. V3-Code ignoriert V4-Tabellen — Schema-Rollback nur bei echter Produktionsstoerung noetig. Alle V4-Migrationen dokumentiert in MIG-023.

### REL-008 — V4 Zwei-Ebenen-Verschmelzung (Mitarbeiter + Bridge + Handbuch + Cockpit)
- Date: 2026-04-27
- Scope: V4 Konsolidierungs-Release fuer alle 8 V4-Slices (SLC-033..040) und 6 V4-Features (FEAT-022..027). Code-Anteile waren ueber 3 Tage stueckweise deployed: REL-006 (V3.1 + dormant V4-Schema), REL-007 (SLC-034 Employee-Auth), Bridge-Engine (SLC-035 + SLC-036) Live-deployed 2026-04-25, Employee-Capture (SLC-037) deployed 2026-04-26, Capture-Mode-Hooks (SLC-038) + Handbuch-Backend (SLC-039) + Handbuch-UI+Cockpit (SLC-040) deployed 2026-04-26..27. Letzter Coolify-Redeploy 2026-04-27 ~17:35 UTC mit Compose-Drift-Fix (Commit `d2be0e4`): docker-compose.yml `app`-Service environment-Block um 10 V3-Vars erweitert (SMTP_*, ERROR_ALERT_EMAIL, JITSI_JWT_*, NEXT_PUBLIC_JITSI_DOMAIN, RECORDING_WEBHOOK_SECRET) — vorher nur via Coolify-UI gesetzt, jetzt in versionierter Compose-File deklariert. Compose-File ist self-contained, Disaster-Recovery-Pfad sauber. 14 V4-Migrationen (065-078): employee-Rolle, employee_invitation, capture_mode-Erweiterung, bridge_run + bridge_proposal, template.handbook_schema + template.bridge_prompt, handbook_snapshot + handbook-Storage-Bucket, RPCs (rpc_create_employee_invitation, rpc_accept_employee_invitation_finalize, rpc_revoke_employee_invitation, rpc_trigger_bridge_run, rpc_approve_bridge_proposal, rpc_reject_bridge_proposal, rpc_trigger_handbook_snapshot, rpc_get_handbook_snapshot_path), 4 RLS-Policies (075-078) fuer 4-Rollen-Sichtperimeter (strategaize_admin/tenant_admin/tenant_member/employee). 7 DECs (DEC-034..040) + DEC-041 (supabase-studio Healthcheck) + DEC-042 (V2-tenant_read-Policies auf tenant_admin-only restringiert).
- Summary: Groesster Release seit V2. Verwandelt die Plattform von Single-Perspektiven-Erhebung in eine Zwei-Ebenen-Struktur (Geschaeftsfuehrer als tenant_admin + Mitarbeiter als employee mit eigenem Login + eingeschraenktem Sichtperimeter). Bridge-Engine generiert aus GF-Verdichtung (KUs + Diagnose + SOP) Mitarbeiter-Capture-Aufgaben hybrid (Template-Standard + KI-Verfeinerung + max 3 Free-Form-Slots, DEC-034). Bridge ist explizit on-demand (DEC-037, kein Auto-Run) — Berater-Konsultations-Schritt zwischen GF-Verdichtung und Bridge-Trigger ist bewusst eingeplant (V2-Debrief-UI unter /admin/debrief existiert weiterhin). Mitarbeiter durchlaeuft eigenen Questionnaire-Mode mit auf-Session-eingeschraenktem Sichtperimeter (R16, SC-V4-3). Unternehmerhandbuch konsolidiert Tenant-Daten in 8-Sections-ZIP (deterministischer Code, kein LLM in V4 — DEC-038). Self-Service-Cockpit zeigt 5 Metrik-Karten + Naechster-Schritt-Banner. Capture-Mode-Hooks-Architektur fuer V5 (Walkthrough) + V6 (Diary) etabliert via walkthrough_stub-Spike (FEAT-025, SC-V4-6). Pflicht-Gate SC-V4-3 (4-Rollen-RLS-Test-Matrix mit 32 Pflicht-Faellen + 14 Bonus = 46 Tests gegen Live-DB) gruen (RPT-092). Pflicht-Gate SC-V4-5 (Nicht-Tech-User-Smoke) durch RPT-090 + RPT-091 erfuellt. Gesamt-V4-/qa PASS (RPT-092: 322/322 Tests gruen Live-DB), /final-check ready (RPT-093 conditionally → resolved nach ENV-Drift-Fix), /go-live GO-Decision (RPT-094). Browser-Smoke User-Self-Test PASS in 5/5 Pfaden (Cockpit, Handbuch, Team, Bridge, Cross-Tenant).
- Risks: Niedrig. 0 Blocker. 2 Low residual: ISSUE-026 postcss 8.4.31 als Next.js bundled dep (npm-Override-immun, Build-Time-only, kein Runtime-XSS-Pfad — Aufloesung mit Next-Minor-Bump); ISSUE-028 V3 RECORDING_WEBHOOK_SECRET MISSING in Production-ENV (V3-Defect, Recording-Pipeline im Pilot nicht aktiv genutzt, kein V4-Blocker — V3.x-Maintenance-Backlog). ERROR_ALERT_EMAIL leer in Coolify-UI → Code-Fallback auf SMTP_USER greift (akzeptabel). ISSUE-027 (ENV-Drift) wurde im Vorfeld behoben. Performance innerhalb Budget: Snapshot-Render 25ms, Total-Exec 0.16-0.23s, Bridge-Run 10-27s (LLM-bound), HTTP-TTFB <200ms aller V4-Routen. AWS-Region eu-central-1 (DSGVO-konform). Kein CSP-Header, kein Impressum/Datenschutz, kein Backup-Schedule — alle pre-existing aus V1-V3, separate Wartungs-Iteration. Berater-Review-Quality-Gate vor Handbuch-Generation und konsolidierter Mitarbeiter-Output-Review fehlen als bewusste UX-Schritte (BL-049 + BL-050 in V4.1-Scope).
- Rollback Notes: Coolify UI → Vorgaenger-Image (Commit `912fba0` = pre-Compose-Drift-Fix; oder weiter zurueck zu `82c987e` = pre-Bridge/Handbuch/Cockpit). Alle 14 V4-Migrationen (065-078) sind additiv (CREATE TABLE/INDEX/POLICY/FUNCTION + DROP/ADD CHECK additiv) und idempotent re-runnable. **DB-Rollback NICHT erforderlich** fuer V4-Slices — alte App-Version ignoriert die V4-Tabellen. RTO bei Image-Reset: ~5-7 min (3-5 min Coolify-Redeploy + 2 min Smoke-Test). Bei wirklichem DB-Rollback (theoretisch): Reihenfolge 078→065, alle DROPs siehe MIG-023..027 in `/docs/MIGRATIONS.md`. Im Live-System aktuell 1 Demo-Tenant mit 1 GF-Session + 5 KUs + 2 Bridge-Runs + 6 Proposals + 1 accepted Invitation + 2 Mitarbeiter-Sessions + 4 Handbuch-Snapshots — bei Bedarf via SQL-Cleanup zurueckdrehbar.
- Post-Launch: ausstehend (post-launch nach 1-2 Tagen Pilot-Betrieb).

### REL-007 — SLC-034 Employee-Auth + Invitation-Flow
- Date: 2026-04-24
- Scope: 2/8 V4-Slice (FEAT-022 Employee Role + RBAC Extension, code-Anteil live, RLS-Perimeter-Vervollstaendigung folgt in SLC-037). Migration 072 (3 RPCs: rpc_create_employee_invitation, rpc_revoke_employee_invitation, rpc_accept_employee_invitation_finalize) aktiv in Produktions-DB. Neue Routes: `/admin/team` (tenant_admin Mitarbeiter-Verwaltung mit Liste + Einladungs-Dialog + Revoke + Resend), `/accept-invitation/[token]` (Employee Passwort-Setzen mit Error-Page-Varianten), `/employee` (Mitarbeiter-Dashboard-Skelett mit Rollen-Guard). Middleware: `/accept-invitation` als public path, Login-Redirect role-aware (employee → /employee). E-Mail-Template DE/EN/NL in `src/lib/email.ts`. Sidebar: neuer Nav-Eintrag "Mitarbeiter" fuer tenant_admin (Fix 82c987e nach Browser-Smoke-Finding). Deploy-Commit `82c987e`. DEC-011-Pattern strikt eingehalten: Server-Action orchestriert Auth-Admin-API (`auth.admin.createUser`) + Finalize-RPC + Rollback-on-Fail. Keine Passwort-Verarbeitung im SQL-Layer.
- Summary: Erster echter V4-Feature-Release. Verwandelt die Plattform von einer Single-Perspektiven-Erhebung (tenant_admin) in eine Zwei-Ebenen-Struktur (tenant_admin + employees). Mitarbeiter-Einladung via E-Mail-Token-Link mit klassischem Passwort-Setup (DEC-035). Auth-Admin-API-Pattern (DEC-011) verhindert direkten SQL-INSERT in `auth.users`. 14/14 RPC-Tests gruen, Gesamt-Test-Suite 144/178 PASS (34 todo, 0 failed). Post-Deploy Phase 1 PASS (RPT-077): 16/16 Container healthy, RPCs persistent, 4/4 Token-Page-Szenarien via curl verifiziert. Post-Deploy Phase 2 PASS: User-bestaetigter Browser-End-to-End-Smoke (Einladung → E-Mail zugestellt → Passwort setzen → Auto-Login → /employee-Dashboard → tenant_admin sieht Mitarbeiter in /admin/team-Liste).
- Risks: Niedrig. Neue Feature-Flaeche beschraenkt auf drei Routes + eine Migration. AC-11 (4 direkte RLS-Matrix-Tests fuer employee_invitation) planmaessig auf SLC-037 verschoben (Slice-Doc Out-of-Scope-Abschnitt). Low: display_name aus Invitation landet nicht in profile (Dashboard zeigt email statt display_name) — kosmetisch, kein Blocker. Konflikt-Fall "E-Mail existiert bereits in auth.users" liefert UI-Meldung, kein Rollback (bewusstes Design). Rate-Limit auf acceptEmployeeInvitation = 5/15min pro IP — moeglicherweise bei legitimem Retry zu streng, Monitoring im Alltag empfohlen.
- Rollback Notes: Coolify UI → Deploy-Commit `cffc639` (V3.1 ohne SLC-034-UI). RPCs in Produktions-DB bleiben harmlos bestehen, da ohne UI-Zugriff (das Volume ist persistent). Bei echter Produktionsstoerung DB-Rollback: `DROP FUNCTION public.rpc_create_employee_invitation(text,text,text); DROP FUNCTION public.rpc_revoke_employee_invitation(uuid); DROP FUNCTION public.rpc_accept_employee_invitation_finalize(uuid,uuid);`. employee_invitation-Tabelle bleibt bestehen (aus SLC-033 REL-006). Bei bereits angelegten employee-auth.users: manuell `auth.admin.deleteUser` via Supabase Studio oder Service-Role-Script. Bisher 1 Real-World-Employee-Account angelegt (User-Smoke-Test 2026-04-24).
