# Releases

### REL-018 — V6.3 Diagnose-Werkzeug Live-Schaltung (SLC-105)
- Date: 2026-05-17 (RELEASED — Internal-Test-Mode-Release-Marker. Coolify-Live-Tag bisher `18d0a131a925492d278f7f5c261fc93063b92d32` = SLC-105 Frontend-Code-Side Pre-QA-Commit. Code-Side komplett verifiziert via RPT-284 Live-Smoke 2 Runs gegen `18d0a13`. Migration 095 ai_cost_ledger Constraint-Extension LIVE in DB seit 2026-05-17 ~08:59 UTC. HEAD `3ca4987` enthaelt nur Doku + SQL-File-Diff zwischen `18d0a13` und `c6a1399/2b2973c/3ca4987` — 0 TS-Touches. Coolify-Redeploy auf neuen HEAD-Commit als User-Pflicht-Action zur Sync von Image-Tag + Repository-Doku-State.)
- Scope: 1 Slice (SLC-105 Diagnose-Werkzeug Light-Pipeline-Renderer, 12 MTs alle durch). 1 Feature (FEAT-045 Diagnose-Werkzeug Template + Light-Condensation-Pipeline + Bericht-Renderer). **3 Migrations live** (093 Template-Seed `partner_diagnostic_v1` mit 24 Fragen × 6 Bloecke + Pflicht-Output-Aussage 206 chars [MIG-037], 094 RPC `rpc_finalize_partner_diagnostic` SECURITY DEFINER fuer atomare Tx-Finalize-Operation [MIG-038], 095 ai_cost_ledger CHECK-Constraint-Extension um `light_pipeline_block`-role [MIG-039 Hotfix aus RPT-284 Live-Smoke]). 6 V6.3-DECs (DEC-123 `question_type` in template.blocks JSONB ohne DB-CHECK + DEC-124 Score in knowledge_unit.metadata.score JSONB + DEC-125 computeBlockScores als Pure-TS-Function + DEC-126 Light-Pipeline als Branch im knowledge_unit_condensation-Handler ohne neuen job_type + DEC-127 Migration-Nummer 093 sauberer V6.3-Slot + DEC-128 6-Bar-Tailwind-Visual statt Radar-Chart). 0 neue Container, 0 neue Worker-Slots, 0 neue Cron-Jobs, 0 neue npm-Packages, 0 neue ENVs (Carry-over aus REL-017). 13 neue Files (1 Migration 093 + 1 Migration 094 + 1 Migration 095 + 1 Fixture-SQL + 1 Server-Actions-File + 1 lead-push-actions + 4 Pages + 4 Components + 1 API-Status-Route + 1 light-pipeline-Extension + 6 Test-Files mit 39 neuen Vitest). Deploy-Commit `3ca4987` (HEAD inkl. RPT-285+286 Skill-Doku). Tag `v6.3.0`. Image-Tag `bwkg80w04wgccos48gcws8cs_app:3ca4987...` als Ziel der Coolify-Redeploy-Action (User-Pflicht via UI auf app + worker Resources).
- Summary: V6.3 macht aus der Onboarding-Plattform die **Multiplikator-Funktions-Kern-Plattform** — Mandant macht Self-Service-Diagnose ohne menschlichen Berater-Loop. Mandant ruft `/dashboard/diagnose/start` auf, sieht Partner-Branding-Header (FEAT-044-Reuse) + Hinweis-Text. Klick Start-Button erzeugt neue `capture_session` mit `template.slug='partner_diagnostic'` + Status `running`, weiter nach `/dashboard/diagnose/run/[id]`. Linearer Frage-Flow mit 24 Radio-Frage-Karten (`QuestionFlow.tsx`) entlang 6 Bausteinen, jede Frage hat 4-5 diskrete `score_mapping`-Optionen (0/25/50/75/100 — DEC-123). Submit triggert Server-Action `submitDiagnosis` mit Auto-Save + ai_jobs INSERT (`status='pending'`, `job_type='knowledge_unit_condensation'`, `payload.capture_session_id=<id>`). Worker pickup ueber `claim-loop` filtert `payload.scheduled_at`-NULL als Sofort-Job (Migration 092a), dispatched via `tryDispatchLightPipeline` Branch in `handle-job.ts` — pruft `template.metadata.usage_kind === 'self_service_partner_diagnostic'` (DEC-126), ruft `runLightPipeline`. Pipeline: (1) `computeBlockScores` Pure-TS-Function liefert Score pro Block 0-100 als Durchschnitt der 4 Fragen-Scores (DEC-125), (2) 6 parallele Bedrock-Calls via `chatWithLLM` (eu-central-1 Claude Sonnet) generieren KI-Kommentar pro Block mit Stil-Anker low/mid/high je Score-Range (NIE bewertend, immer kommentierend), (3) `ai_cost_ledger`-INSERT pro Block mit `role='light_pipeline_block'` (jetzt funktional durch Migration 095), (4) atomare Tx via `rpc_finalize_partner_diagnostic` Stored-Proc — schreibt 6 `knowledge_unit.status='accepted'` + 6 `validation_layer.reviewer_role='system_auto'/action='accept'/note='Auto-Finalize per DGN-A'` + 6 `block_checkpoint.checkpoint_type='auto_final'` + `capture_session.status='finalized'` in einer Transaktion, (5) `error_log` mit `level='info' / source='partner_diagnostic_finalized'`. Bei Failure: `error_log` mit `level='error' / source='partner_diagnostic_failed'`. Bericht-Pending-Page hat 3s-Polling auf GET-Status-Endpoint, 60-Polls-Hardstop. Nach Finalize: `/bericht`-Page mit 3-Layer-Auth-Matrix (Mandant + Partner-Admin + Strategaize-Admin), notFound-Fallback bei Auth-Fail. Render: `ScoreVisual` 6 horizontale Tailwind-Bars (DEC-128) + 6 `BlockSection` mit KI-Kommentar prosaisch + Pflicht-Output-Aussage als Markdown-Footer + "Ich will mehr"-Stub-Karte (FEAT-046-Reuse fuer Lead-Push) + Print-Button. **Live-Smoke 2 Runs PASS:** Run 1 35.0s end-to-end (Bug-Detection ISSUE-076 + Migration 095 Hotfix im selben Lauf), Run 2 27.7s end-to-end + 6 ai_cost_ledger-Eintraege total $0.022080 (78% Headroom unter $0.10-Budget) + 2720 input/928 out tokens + 30.0s Bedrock-Parallel-Duration. Cost-Audit-Trail funktional. RPT-279 /architecture Quick-Pass mit 6 DECs, RPT-280 /backend MT-1+2+3 (Migration 093 + computeBlockScores + 18 Vitest), RPT-281 /backend MT-4+5 (Migration 094 RPC + runLightPipeline + Worker-Branch + 12 Vitest), RPT-282 Backend-Pass-QA, RPT-283 /frontend MT-6+7+8 (Pages + Components + 9 Frontend-Vitest), RPT-284 Frontend-Pass-QA + Live-Smoke + ISSUE-076 Resolution via MIG-039, RPT-285 /final-check READY (0 Blocker / 0 High / 0 Medium / 6 Low alle Carry-over oder externe Stop-Gates), RPT-286 /go-live GO (8/8 Self-hosted-Stack-Readiness PASS, 2 dokumentierte Klauseln). Reuse aus V4+V5+V6: Capture-Mode-Architektur (`FEAT-025`), Bedrock-Client (V2), Worker-Infrastruktur (V2/V5), partner_branding_config (FEAT-044), `template`/`capture_session`/`knowledge_unit`/`block_checkpoint`/`validation_layer` (V1-Foundation), `rpc_complete_ai_job` (V4), error_log + ai_cost_ledger (V2/V4).
- Risks: 0 V6.3-eingefuehrte. **2 dokumentierte Klauseln aus RPT-286 (bewusste Akzeptanz, NICHT release-blockierend fuer Internal-Test-Mode):** (a) **Klausel A — BL-104 Anwalts-Review** der V6.2-Compliance-Texte extern pending (Datenschutzerklaerung + Impressum + AVV-Templates) — externes Stop-Gate vor erstem echten Live-Pilot-Partner, NICHT vor Internal-Test-Mode-Release per memory `feedback_compliance_gate_later`. Carry-over aus REL-017 Klausel B. (b) **Klausel B — ISSUE-073 IMPRESSUM_VAT Platzhalter live** ("BTW-Nr. wird nachgereicht") — User-ENV-Update bei NL-BTW-Nummer-Beschaffung, ~2min Coolify-UI-Action, kein Code-Touch. Carry-over aus REL-017 Klausel C. **Pre-existing carry-over** (alle pre-V6.3, kein V6.3-Regress): 5 npm-Audit-Vulns (next/next-intl/postcss alle Build-Time-only, Pre-Production-Compliance-Gate-Scope), Vitest-Baseline `TEST_DATABASE_URL is not set` Local-Run-Drift (Tests gegen Coolify-DB im node:20-Container laufen sauber, dokumentiert in `coolify-test-setup.md`), F-1 Doku-Inkonsistenz `actions.ts:242-243` Kommentar (wird beim naechsten /backend-Touch im selben File mitgenommen), AC-12+AC-13 Cross-Tenant-Read Live-Smoke deferred per User-Decision (Code-Side PASS, organisch beim BL-104-Pilot-Partner). 1 Cross-System Operational-Risk: ISSUE-071 im Business-System-Repo (Disk-Voll-Praevention via Coolify-Cron offen, unaffected by V6.3).
- Rollback Notes: **Code-Rollback**: Coolify-UI → app + worker Resources → "Redeploy" auf Image-Tag `bwkg80w04wgccos48gcws8cs_app:7181077e1b92f46cacb87ac512c5d66acf766096` (=REL-017 V6.2 Stand). RTO ~5min, RPO 0. Pre-V6.3-Code ignoriert die neuen Strukturen automatisch (knowledge_unit.metadata bleibt ungenutzt, ai_cost_ledger.role='light_pipeline_block' wird nicht mehr geschrieben, `template.slug='partner_diagnostic'` bleibt im Template-Repo aber inaktiv). **Migration-Rollback Migration 095 (additiv, falls noetig)**: `ALTER TABLE ai_cost_ledger DROP CONSTRAINT ai_cost_ledger_role_check; ADD CONSTRAINT ai_cost_ledger_role_check CHECK (role IS NULL OR role = ANY (ARRAY['analyst','challenger','chat','memory','embedding','orchestrator','sop_generator','diagnosis_generator','evidence_mapper','dialogue_extractor','bridge_engine','walkthrough_pii_redactor','walkthrough_step_extractor','walkthrough_subtopic_mapper']))` via sql-migration-hetzner.md Pattern (~2min). Nach Rollback: V6.3-Light-Pipeline-INSERTs failen wieder silent (ISSUE-076-Status), UX-funktional, AC-14 wieder broken. **Migration-Rollback Migration 093 + 094**: NICHT erforderlich weil additiv (CREATE OR REPLACE FUNCTION + ALTER TABLE ADD COLUMN mit Default + Template-Seed via ON CONFLICT DO UPDATE). Pre-V6.3-Code ruft `rpc_finalize_partner_diagnostic` nie auf und ignoriert die neuen JSONB-Spalten. RTO Migration-Rollback ~2min. Im Internal-Test-Mode aktuell nicht relevant — Rollback nur als Notfall-Pfad bei unerwartetem V6.3-Regress.
- Post-Launch: Pflicht-Smokes nach User-Coolify-Redeploy auf `3ca4987...`: (1) Container-Health 16/16 Up + 5 kritische (healthy) auf Image-Tag `3ca4987` fuer app+worker (Sync zur Doku-Aktualitaet, Code-Verhalten identisch zu vorigem `18d0a13`); (2) External `/login` HTTP 200 TTFB <200ms-Baseline; (3) 1 Live-Smoke-Diagnose-Run via Mandant-Login → Diagnose-Start → 24 Antworten → Submit → /bericht in <60s + ai_cost_ledger zeigt 6 neue Eintraege mit `role='light_pipeline_block'` + total ~$0.022 + 78%-Headroom (analog RPT-284 Run 2); (4) error_log letzte 30min seit Redeploy 0 `error/light-pipeline` Eintraege (= Migration 095 wirksam); (5) Worker-Log `lead_push_retry handler registered` + alle 15 Handler-Slots. **18-24h-Beobachtungs-Window startet ab User-Coolify-Redeploy-Completion** → /post-launch V6.3 nach ~24h. Beobachtungs-Schwerpunkt: error_log `source='light-pipeline'` (soll 0 bleiben), `source='partner_diagnostic_finalized'` (1 pro Real-Run), `source='partner_diagnostic_failed'` (soll 0 bleiben), ai_cost_ledger-Growth pro Real-User-Diagnose-Run, App + Worker Container-Health stabil. Git-Tag: `v6.3.0` auf commit `3ca4987`. **Pre-Production-Compliance-Gate-Restschritte bleiben** (alle User-Pflicht extern, NICHT release-blockierend): BL-104 Anwalts-Review + ISSUE-073 NL-BTW-Nummer-Lieferung. Erster echter Live-Pilot-Partner blockiert auf BL-104-PASS.

### REL-017 — V6.2 Compliance-Sprint (Datenschutz + Impressum + AVV + COMPLIANCE.md)
- Date: 2026-05-16 (RELEASED — Internal-Test-Mode-Release-Marker. Code ist live deployed via User-Coolify-Redeploy 2026-05-16 ~09:13 UTC. Dieser Release-Marker ist formaler Tag-Setzer + Cockpit-Promotion. Kein neuer Build noetig.)
- Scope: 3 Slices (SLC-120 Datenschutz + Impressum Pages DE + SLC-121 AVV-Templates DE + NL + SLC-122 docs/COMPLIANCE.md). 3 Features (FEAT-048 + FEAT-049 + FEAT-050). 7 V6.2-DECs (DEC-116..122). 0 Migrations, 0 neue Container, 0 neue Worker-Slots, 0 neue Cron-Jobs, 0 neue npm-Packages. 9 neue ENVs (IMPRESSUM_COMPANY/STREET/ZIP/CITY/COUNTRY/KVK/VAT/DIRECTOR/EMAIL) auf bwkg80w04wgccos48gcws8cs-app-Resource. Code-Side-Aenderungen: 4 neue Files (`src/content/legal/datenschutz.de.md` 191Z, `src/app/datenschutz/page.tsx`, `src/app/impressum/page.tsx`, `src/components/branding/StrategaizePoweredFooter.tsx`-Erweiterung) + 2 ENV-Beispiel-Files erweitert + Middleware-publicPaths um /datenschutz + /impressum erweitert + i18n-Keys footer.privacyPolicy + footer.imprint in de/en/nl.json + 2 Auto-Fixes aus /qa (Middleware-publicPaths + Mobile-h1-prose-Klassen). 3 neue Doku-Files unter docs/legal/ + docs/COMPLIANCE.md (486 Zeilen, 8 DSGVO-Standardsektionen + 1 V6.2-DPO-Bewertungs-Sektion). Deploy-Commit `7181077e1b92f46cacb87ac512c5d66acf766096` (= HEAD inkl. RPT-272..276 Skill-Doku). Image-Tag `bwkg80w04wgccos48gcws8cs_app:7181077e1b92f46cacb87ac512c5d66acf766096` live auf app + worker. Container-IDs nach Redeploy: app=091026460475, worker=091026491517.
- Summary: V6.2 schliesst den Pre-Production-Compliance-Sprint vor erstem echten Live-Pilot-Partner. Drei Schichten: (1) **Oeffentliche DSGVO/TMG-Pages** — `/datenschutz` rendert `src/content/legal/datenschutz.de.md` (10 Pflicht-Sektionen Art. 13 DSGVO) via react-markdown-Subset aus HandbookReader-Pattern (DEC-117), `/impressum` rendert 9 IMPRESSUM_*-ENV-Werte mit Pflicht-Wurf bei fehlender Pflicht-ENV (DEC-116), beide pre-auth zugaenglich. Footer erweitert um 3 Links `[Datenschutz] · [Impressum] · [Powered by Strategaize]` (DEC-108 + DEC-118). (2) **AVV-Standardvorlagen** — `docs/legal/AVV-DE.md` + `docs/legal/AVV-NL.md` mit 13 DSGVO-Art-28-Klausel-Bausteinen (Praeambel + 11 Standardklauseln + Unterschriftsfelder), symmetrische DE/NL-Versionen, Cross-Refs auf COMPLIANCE.md Sektionen 1+5+7+8+9, 4 EU-Provider-Tabelle (Hetzner+Bedrock+Azure+IONOS), Platzhalter `[Verantwortlicher: ...]` + `[Auftragsverarbeiter: ...]` fuer Anwalts-Review-Klarung Variante A vs B (BL-104). Versand manuell pro Partner-Onboarding (DEC-120, kein Admin-UI). (3) **Interne Compliance-Doku** — `docs/COMPLIANCE.md` 8-Sektionen-Standardvorlage (Erhobene Daten + Datenfluesse + Speicherorte + Retention + Drittanbieter + DPAs + Loeschkonzept + Datenschutzkonforme Defaults) + V6.2-spezifische DPO-Bewertungs-Sektion (DEC-121, Strategaize TBV bestellt aktuell keinen DPO). Pattern-Reuse aus Business-System V5.2-Struktur, Inhalt komplett neu fuer Multi-Tenant-SaaS-Realitaet. Verantwortlicher = Strategaize Transition BV (NL-Operativ, KvK 42008238). Cross-Slice-Konsistenz cross-slice PASS (RPT-274 ueber 10 Dimensionen: Verantwortlicher-Bezug, Sub-Verwerker-Liste konsistent, Cross-Refs strukturell valide, Stand-Datum konsistent 2026-05-15, Disclaimer-Tenor konsistent, AVV-Variante-A/B, EN/NL-Variante-Status, Pflicht-Footer DEC-108, Stammdaten-Quelle 9 ENVs, IMPRESSUM_VAT-Status). RPT-272 /frontend SLC-120 (7 MTs PASS), RPT-273 /qa SLC-120 (13/14 ACs + AC-14 optional PASS, 2 Auto-Fixes Middleware-publicPaths + Mobile-h1), RPT-274 Gesamt-/qa cross-slice (0 Blocker/0 High/0 Medium/4 Low), RPT-275 /final-check (0 Blocker/6 Low alle akzeptabel oder User-Pflicht), RPT-276 /go-live CONDITIONAL GO mit 3 Klauseln. Live-Smoke 4/4 PASS bei Release-Apply: /datenschutz HTTP 200, /impressum HTTP 200 mit allen 9 IMPRESSUM-Werten gerendert (KvK 42008238, Adresse aan de Markt 19 6071 JD Swalmen, Director Heinrich Bellaerts, Email bd@strategaizetransition.com, VAT-Platzhalter "BTW-Nr. wird nachgereicht"), Footer 3 Links auf /login (non-auth Route), Container 4/4 kritische Up healthy (app+worker+kong+db Up 11min).
- Risks: 0 V6.2-eingefuehrte. **3 explizite Klauseln aus RPT-276 (alle bewusste User-Decision, nicht Internal-Test-Mode-blockierend):** (a) **Klausel A — Coolify-Secrets-Setup** (User-Pflicht durchgefuehrt 2026-05-16: 9 IMPRESSUM_*-ENVs gesetzt + Container-Reload). (b) **Klausel B — Pending legal review (BL-104)** als externes Stop-Gate vor erstem echten Live-Pilot-Partner — bewusste Akzeptanz per memory `feedback_compliance_gate_later` und V6/V6.1-Defer-Klausel. Anwalts-Suche User-Pflicht-externer-Schritt nach /deploy V6.2 parallel zu /post-launch. (c) **Klausel C — Pending real VAT (ISSUE-073 Low)** als bewusste User-Entscheidung 2026-05-16: Coolify-ENV `IMPRESSUM_VAT="BTW-Nr. wird nachgereicht"` als Platzhalter ueberbrueckt bis NL-BTW-Nummer der Strategaize Transition BV beschafft ist. Sobald real-Wert vorliegt: User-Aktion Coolify-ENV-Update + Container-Reload (~2min, 0 Code-Touch). TMG/DDG-konform fuer Internal-Test-Mode. **Pre-existing Carry-over** (alle pre-V6.2, kein V6.2-Regress): ISSUE-051 profiles fehlen first_name/last_name (V6.1+-Polish), 5 npm-Audit-Vulns (next/next-intl/postcss/icu-minify/fast-xml-builder, Build-Time-only, Pre-Production-Compliance-Gate-Scope), Vitest-Baseline pre-existing kaputt (V6-Carry-over, kein V6.2-Regress, Stash-Vergleich bestaetigt). 4 Low aus RPT-274 (L-1 Verantwortlicher-Variante /datenschutz vs AVV-A nominell widerspruechlich → BL-104-Item, L-2 Anbieter-Namens-Inkonsistenz Brand vs juristische EU-Entitaet → BL-104-Item, L-3 Regionsdetails Azure akzeptabel, L-4 IMPRESSUM_VAT = ISSUE-073).
- Rollback Notes: Coolify-UI → Image-Tag `bwkg80w04wgccos48gcws8cs_app:62dddaffe614319573a504cb04541fd2b0c184c8` (=V6.1 REL-016 Stand) → Redeploy. **DB-Rollback NICHT erforderlich** weil V6.2 0 Migrations hat. **ENV-Rollback NICHT erforderlich** weil V6.1-Code die 9 IMPRESSUM_*-ENVs schlicht ignoriert (kein Code-Reader dafuer). RTO ~5min, RPO 0. Im Internal-Test-Mode aktuell nicht relevant — Rollback nur als Notfall-Pfad bei unerwartetem V6.2-Regress.
- Post-Launch: 4 schlanke Pflicht-Smokes alle PASS bei Release-Apply (RPT-277): (1) `/datenschutz` HTTP 200; (2) `/impressum` HTTP 200 mit allen 9 IMPRESSUM-Werten gerendert inkl. VAT-Platzhalter; (3) Footer 3 Links auf /login (non-auth) — Datenschutz + Impressum + "Aufgesetzt mit Strategaize"; (4) Container-Health 4/4 kritische Up healthy auf Image-Tag `7181077e1b...`. **18-24h-Beobachtungs-Window startet 2026-05-16 ~09:13 UTC Europe/Berlin** → /post-launch V6.2 nach ~2026-05-17 ~09:13. Beobachtungs-Schwerpunkt: error_log fuer `/impressum` ENV-Read-Failures, `/datenschutz` fs.readFileSync-Failures, neue Compliance-Page-Traffic-Pattern. Git-Tag: `v6.2.0` auf commit `7181077`. **Pre-Production-Compliance-Gate-Restschritte (alle User-Pflicht extern, NICHT release-blockierend fuer Internal-Test-Mode):** BL-104 Anwalts-Review + ISSUE-073 NL-BTW-Nummer-Lieferung. Erster echter Live-Pilot-Partner blockiert auf BL-104-PASS.

### REL-016 — V6.1 Operational-Polish (Multi-Network + Branding-Polish-Tripel)
- Date: 2026-05-15 (RELEASED — Internal-Test-Mode-Release-Marker. Code ist live deployed via User-Coolify-Reload-Compose + Redeploy 2026-05-15 ~09:22 UTC durch. Dieser Release-Marker ist formaler Tag-Setzer + Cockpit-Promotion. Kein neuer Build noetig.)
- Scope: 1 Slice (SLC-110 V6.1 Permanent-Fix Polish-Tripel). 1 Feature (FEAT-047 V6.1 Operational-Polish). 6 Micro-Tasks: MT-1 docker-compose Multi-Network-Labels + MT-2 dashboard Default-DisplayName-Fallback + MT-3 React cache() um resolveBrandingForTenant + F-110-H1 createClient cache()-Wrap (Quick-Fix nach /qa-Wiring-Gap) + MT-4 Quality-Gates + MT-5 Coolify-Reload+Redeploy + MT-6 /post-launch Light-Smoke. **0 Migrations, 0 neue Tabellen, 0 neue Container, 0 neue ENVs, 0 neue Cron-Jobs, 0 neue npm-Packages.** 2 V6.1-DECs (DEC-114 Multi-Network-Label-Pattern + DEC-115 React-cache fuer Server-Side-Resolver). Deploy-Commit `9b6a78b25... (= HEAD `9b6a78b`). Image-Tag `bwkg80w04wgccos48gcws8cs_app:62dddaffe614319573a504cb04541fd2b0c184c8` live auf app + worker + kong + db (alle same-commit-Image dank Coolify-Compose-Build, commit `62dddaf` = HEAD vor RPT-263-Commit). Container-IDs nach Redeploy: app=092232049513, worker=092232063438.
- Summary: V6.1 SLC-110 schliesst die drei post-V6-Operational-Gaps aus RPT-256..258: (1) **ISSUE-072 Multi-Network-Falle persistent eliminiert** — `traefik.docker.network=bwkg80w04wgccos48gcws8cs`-Label + `traefik.http.services.app-svc.loadbalancer.server.port=3000`-Label + Router-`.service=app-svc`-Wiring in docker-compose.yml. Pattern aus `jitsi-jibri-deployment.md` Punkt 1+3. Live-verifiziert via `docker inspect`. (2) **ISSUE-048 Default-DisplayName-Leak im Welcome-Block geschlossen** — Fallback-Predicate in `src/app/dashboard/page.tsx` erweitert um STRATEGAIZE_DEFAULT_BRANDING.displayName-Vergleich, 5 neue Vitest PASS. (3) **ISSUE-049 Branding-Resolver 2x RPC pro Page-Load geschlossen** — Two-stage Fix: React `cache()` um `resolveBrandingForTenant` (MT-3) plus `cache()` um `createClient` (F-110-H1) sorgt fuer 1 SupabaseClient-Instanz pro Render → Object.is-Args-Memo greift → 1x RPC pro Request statt 2x. Smoke `server-cache.test.ts` 1/1 PASS + `resolve.test.ts` Case 8 PASS. **0 V6.1-eingefuehrte Risks.** RPT-260 Backend-Completion (MT-1..MT-4 Code-Side komplett), RPT-261 /qa MT-4 PARTIAL PASS + F-110-H1-Befund, RPT-262 F-110-H1 Quick-Fix done, RPT-263 /post-launch STABLE.
- Risks: 0 V6.1-eingefuehrte. 4 Pre-Production-Compliance-Gate-Items als deferred Klausel PFLICHT vor erstem echten Live-Partner (NICHT vor Internal-Test-Mode-Release, konsistent zur V6-Klausel REL-015): (a) BL-094 AVV-Template DE+NL inkl. Anwalts-Review, (b) Datenschutzerklaerung-Page `/datenschutz`, (c) Impressum-Page `/impressum`, (d) Anwalts-Review des Modal-Consent-Text `v1-2026-05`. Per memory `feedback_compliance_gate_later`. Pre-existing carry-over (alle pre-V6.1): ISSUE-051 profiles fehlen first_name/last_name (V6.1+-Polish), 5 npm-Audit-Vulns (next/next-intl/postcss/icu-minify/fast-xml-builder, Build-Time-only, Pre-Production-Compliance-Gate-Scope). 1 Cross-System Operational-Risk: ISSUE-071 im Business-System-Repo (Disk-Voll-Praevention via Coolify-Cron offen — User-Pflicht, unaffected by V6.1).
- Rollback Notes: Coolify-UI → Image-Tag `bwkg80w04wgccos48gcws8cs_app:1c1a4b7363c103c84b970524057110e5eefb40ff` (=V6 REL-015 Stand) → Redeploy. **DB-Rollback NICHT erforderlich** — V6.1 hat 0 Migrations. Rollback-Aufwand: ~5 Min Coolify-Image-Tag-Wechsel + Redeploy. Konsequenz: Multi-Network-Falle waere wieder latent (Risk = naechster Coolify-Sentinel-Restart oder Network-Reload kann erneut Outage triggern, wie 2026-05-15 ~06:30 UTC), Branding-Resolver wieder 2x RPC pro Mandanten-Page-Load (Low-Severity ~5-10ms Performance ohne User-Effekt), Welcome-Block-Strategaize-Leak wieder im RPC-Fehler-Edge-Case (~0.01% Trigger-Rate). RTO ~5min, RPO 0. Im Internal-Test-Mode aktuell nicht relevant — Rollback nur als Notfall-Pfad bei unerwartetem V6.1-Regress.
- Post-Launch: 5 Pflicht-Smokes alle PASS bei Release-Apply (RPT-263): (1) Container-Health 16/16 Up + 5 kritische (healthy) auf Image-Tag `62dddaffe6`; (2) External `/login` HTTP 200 TTFB 178ms (V6-Baseline RPT-256: 88ms — leichter Warmup-Overhead nach Redeploy, ~7min Uptime, vergleichbar zur V5.1-Baseline 143ms); (3) ISSUE-072 Permanent-Fix Live-Verifikation via `docker inspect app-...092232049513`: `traefik.docker.network=bwkg80w04wgccos48gcws8cs` + `traefik.http.services.app-svc.loadbalancer.server.port=3000` aktiv; (4) error_log seit Deploy (10min-Fenster) **0 errors / 0 warns / 0 fatals**, nur 1 info (Worker-Startup mit 15 Handler-Slots inkl. V6-`lead_push_retry`); (5) Image-Tag-Match `62dddaffe614319573a504cb04541fd2b0c184c8` fuer app + worker. 3 Issues von `open` auf `resolved` durch V6.1-Release: ISSUE-072 (Multi-Network-Falle) + ISSUE-048 (Default-DisplayName-Leak) + ISSUE-049 (Branding-Resolver 2x RPC). Git-Tag: `v6.1.0` auf commit `9b6a78b`. Post-Launch-Window minimal (~30min) — keine Real-User-Behavior-Aenderung erwartet, Permanent-Fixes sind transparent fuer Endnutzer.

### REL-015 — V6 Multiplikator-Foundation (Partner-Tenant + Lead-Push opt-in)
- Date: 2026-05-14 (RELEASED — Internal-Test-Mode-Release-Marker. Code ist live deployed via Coolify-Redeploy 2026-05-14 ~14:58 UTC durch, dieser Release-Marker ist formaler Tag-Setzer + Cockpit-Promotion. Kein neuer Build noetig.)
- Scope: 5 Slices (SLC-101 Partner-Tenant-Foundation + SLC-102 Partner-Organisation + SLC-103 Mandanten-Mapping + SLC-104 Partner-Branding + SLC-106 Lead-Push opt-in + Outbound-Webhook + DSGVO-Audit). 5 Features (FEAT-041..044 + FEAT-046). SLC-105/FEAT-045 Diagnose-Werkzeug nach V6.1 verschoben (BL-095 Inhalts-Workshop blockiert, urspruenglich als 6. V6-Slice geplant). 6 Migrations live (090 + 091 + 091a + 091b + 092 + 092a — alle additiv, MIG-034 + 035 + 036). 14 V6-DECs (DEC-100..113). 0 neue npm-Packages (Outbound-HTTP nutzt Native-fetch + AbortController, Pen-Tests nutzen pre-existing pg-Driver, Worker reuses ai_jobs-Infrastruktur). Plus 2 V6-Polish-Fixes vor /go-live (ISSUE-052 partner_branding_config-Default-Row auf createPartnerOrganization + ISSUE-053 Worker RECOMMENDED_ENV-Warn-Log fuer BUSINESS_SYSTEM_INTAKE_*). Deploy-Commit `1c1a4b7363c103c84b970524057110e5eefb40ff` (HEAD-Snapshot inkl. RPT-251..255). Image-Tag `bwkg80w04wgccos48gcws8cs_app:1c1a4b7363c103c84b970524057110e5eefb40ff` (worker analog). Container-IDs nach Redeploy: app=145850957466, worker=145850979352.
- Summary: V6 macht aus der Single-Tenant-Plattform eine Multi-Tenant-Multiplikator-Plattform. Tenant-Hierarchie via `tenant_kind direct_client/partner_organization/partner_client` + `parent_partner_tenant_id`. Neue Rolle `partner_admin` mit komplettem RLS-Defense-in-Depth + 5-Rollen-Pen-Test-Matrix (112 Live-Pen-Tests gegen Coolify-DB). Partner-Onboarding-Flow via Strategaize-Admin (`/admin/partners`), Partner-Admin-Dashboard (`/partner/dashboard`), Magic-Link-Invitations (FEAT-031-Reuse). Partner-Branding mit CSS-Custom-Properties Server-Side Inline-Style im Root-Layout (DEC-106, erstmals in der Plattform, kein FOUC), Logo-Storage-Proxy mit Cache-Buster, Pflicht-Footer "Aufgesetzt mit Strategaize" (DEC-108, hardcoded). Mandanten-Tenant-Branch im Dashboard (`partner_client tenant_admin` sehen schlanken Welcome-Block statt V4/V5-Cockpit). Lead-Push-Pipeline mit DSGVO-Pflicht-Checkbox 3-fach gesichert (UI-Disabled + handleSubmit-Guard + Server-Re-Validation, DEC-091 Reuse aus V5 SLC-079), versioniertem Consent-Text (`v1-2026-05`), atomarem 2-Phasen-INSERT mit Compensating-Action (Orphan-Consent-Cleanup bei audit-INSERT-Fail), Outbound-HTTP-Adapter mit 10s-Timeout + AbortController + Bearer-Auth + UTM-Attribution (`utm_source=partner_<tenant_id>` First-Touch-Lock-konform), Cross-System-Audit-Trail (Onboarding lead_push_audit + Business-System contacts + audit_log verzahnt via business_system_contact_id). Worker-Retry-Pfad mit 3-Versuche-Limit (DEC-112: 5min/30min Backoff) ueber `ai_jobs`-Queue-Erweiterung + Migration 092a `payload.scheduled_at`-Filter. **Live-Cross-System-Smoke MT-12 + MT-13 PASS 2026-05-14 (RPT-252)** durch Playwright-MCP-End-to-End — alle 16 SLC-106 ACs erfuellt. /qa Gesamt-PASS (RPT-251 7/7 Gates), /final-check Cross-Slice-Audit conditionally-ready (RPT-253), 2 High-Findings ISSUE-052+053 live-fixed + verifiziert (RPT-254 6/6 Gates), /go-live GO (RPT-255 7/7 Self-hosted-Readiness).
- Risks: 0 V6-eingefuehrte. 4 Pre-Production-Compliance-Gate-Items als deferred Klausel PFLICHT vor erstem echten Live-Partner (NICHT vor Internal-Test-Mode-Release): (a) BL-094 AVV-Template DE+NL inkl. Anwalts-Review, (b) Datenschutzerklaerung-Page (`/datenschutz`) fehlt komplett, (c) Impressum-Page (`/impressum`) fehlt komplett, (d) Anwalts-Review des Modal-Consent-Text `v1-2026-05`. Per memory `feedback_compliance_gate_later`: explizite User-Decision dass diese Items fuer Internal-Test-Mode aufgeschoben werden, bis V6.x/V7 Compliance-Sprint (analog V5.2 fuer V5.1). 4 Low residual carry-over (alle pre-existing oder V6.1-Polish-Kandidaten): ISSUE-051 profiles fehlen first_name/last_name (Lead-Push faellt auf user_metadata-Fallback zurueck), ISSUE-049 Branding-Resolver wird 2x pro Mandanten-Page-Load aufgerufen (React-cache als V6.1-Polish), ISSUE-048 Default-DisplayName-Leak im Welcome-Block (sollte durch ISSUE-052-Fix mitigiert sein, empirische Verifikation nach 2-3 weiteren Partner-Anlagen offen), Negative-Path-Vitest fuer validateEnv RECOMMENDED_ENV (V6.1-Polish). 5 pre-existing npm-Audit-Vulns (next/next-intl/postcss/icu-minify/fast-xml-builder) bleiben Pre-Production-Compliance-Gate-Scope, nicht V6-induziert. 1 Cross-System Operational-Risk: ISSUE-071 im Business-System-Repo (Disk-Voll-Risiko auf 91.98.20.191, Workaround durch Docker-Cleanup gemacht 2026-05-14 ~10:27 Disk 100%->83%, Praevention via Coolify-Cron offen — User-Pflicht).
- Rollback Notes: Coolify-UI → Image-Tag `bwkg80w04wgccos48gcws8cs_app:e8bce6ae13be603310b1cecd1b11d8ecc5eeb059` (=Pre-V6 / V5.1-Stand REL-014 + Hotfixes) → Redeploy. **DB-Rollback NICHT erforderlich** weil Migrations 090..092a alle additiv sind: neue Tabellen (`partner_organization`, `partner_client_mapping`, `partner_branding_config`, `lead_push_consent`, `lead_push_audit`) + neue Spalten an `tenants` (`tenant_kind`, `parent_partner_tenant_id`) mit Default-Werten (`direct_client`/`NULL`) + neue RLS-Policies + neuer ENUM-Wert `ai_jobs.job_type='lead_push_retry'` + neue RPC `rpc_get_branding_for_tenant` + neue RPC `rpc_claim_next_ai_job_for_type` (CREATE OR REPLACE). Pre-V6-Code ignoriert die neuen Strukturen automatisch (kein Query darauf). RTO ~5min, RPO 0. Bei vollstaendigem V6-Rollback aus operativen Gruenden: bestehende `tenant_kind != 'direct_client'`-Tenants muessten manuell migriert werden (~50-Zeilen Recovery-SQL, ad-hoc generierbar) — aber dieser Pfad ist im Internal-Test-Mode aktuell nicht relevant (keine echten Partner-Daten ausser MT12-Test-Datensaetzen die schon cleanup-ed sind).
- Post-Launch: 5 schlanke Pflicht-Smokes alle PASS bei Release-Apply: (1) Container-Health 16/16 Up + 5 kritische (healthy) auf Image-Tag `1c1a4b7`; (2) External `/login` HTTP 200 TTFB 88ms (REL-014-Baseline: 143ms — deutlich besser, Layout-Verschlankung greift); (3) ENV-Propagation app + worker beide `BUSINESS_SYSTEM_INTAKE_URL` + `_API_KEY` live, Worker-Log zeigt `lead_push_retry handler registered` + `Environment validated` ohne Warn-Log (positive path); (4) Cross-System-Smoke MT-12 + MT-13 PASS via Playwright MCP (RPT-252) + Polish-Fix-Smoke RPT-254 mit DB-Roundtrip-Verify partner_branding_config-Default-Row primary_color='#4454b8'; (5) Image-Tag-Match `1c1a4b7363c103c84b970524057110e5eefb40ff` fuer app + worker. Container-Logs 0 ERROR/FATAL/Crash. **18-24h-Beobachtungs-Window startet 2026-05-14 ~15:10 Europe/Berlin** → /post-launch V6 nach ~2026-05-15 ~15:10. Beobachtungs-Schwerpunkt: error_log Cross-System-Pushes (lead_push_attempt/lead_push_failure-Categories), Worker-Health (ai_jobs lead_push_retry-Job-Lifecycle), neue Partner-Anlagen via Welcome-Block-Render-Verifikation fuer ISSUE-048-follow-up. Git-Tag: `v6.0.0` auf commit `1c1a4b7`. **Post-Launch-Update 2026-05-15 (RPT-258): Run-2 Cross-System-Pipeline LIVE-PASS.** Nach Hotfix-Redeploy wurde V6 Cross-System-Pipeline end-to-end verifiziert: PL15-Test-Setup (partner_organization + partner_client + Mandant-User via Admin-API + finalized capture_session) → Direct-HTTP-Push an business.strategaizetransition.com/api/leads/intake HTTP 201 + contact_id=e0446b43 + was_new=true → Audit-Trail Onboarding lead_push_consent + lead_push_audit status='success' → Cross-Verify BS-DB contacts mit korrekter UTM-Attribution + 2-Satz-Notes-Strukturtext (DSGVO-Datensparsamkeit AC-11). Cleanup beidseitig 9/9 + 2/2 = 0 remaining. V6-Pipeline live-validiert (Adapter + BS-Auth + Cross-System-Routing + Audit-Trail + UTM + DSGVO). Server-Action-UI-Klick wurde durch Direct-Adapter ersetzt (Playwright Browser-Profile-Lock, IMP-528 dokumentiert). Server-Action-Logik durch Vitest 8/8 RPT-244 abgedeckt. **Post-Launch-Update 2026-05-15 (RPT-257): BLOCKER ISSUE-072 entdeckt — externer HTTPS-App-Routing haengt nach TLS-Handshake (Multi-Network-Falle: App auf 2 Networks ohne `traefik.docker.network`-Label, Coolify-Proxy waehlt falsches Backend-Interface 10.0.4.17 statt 10.0.3.18). Onset zwischen V6-Deploy (TTFB 88ms OK) und 2026-05-15 ~06:30 UTC (Hang). Internal alle 16/16 Container Up 15h healthy + App antwortet HTTP 200 ueber Container-Name + 0 V6-induced Errors in DB + 0 ai_jobs lead_push_retry + 0 lead_push_audit-Aktivitaet (=keine Real-User-Activity). Vergleichs-Domain meet-onboarding.strategaizetransition.com (Jitsi) auf gleichem Server: HTTP 200 in 37ms — Coolify-Proxy als solcher funktioniert. Code-Side V6 ist NICHT das Problem (Rollback nicht angezeigt). Hotfix-Pfad: User Coolify-UI App-Resource Restart oder `docker network connect bwkg80w04wgccos48gcws8cs_strategaize-net coolify-proxy` (nicht persistent). Permanent-Fix als V6.1-Compose-Edit (~30-60min): docker-compose um `traefik.docker.network` + explicit Service-Port-Labels + Router-`.service=`-Wiring erweitern.**

### REL-014 — V5.1 Walkthrough Handbuch-Integration
- Date: 2026-05-11 (RELEASED — User-Coolify-Redeploy 2026-05-11 ~09:13 Europe/Berlin durch, Post-Deploy-Verifikation alle gruen)
- Scope: 2 Slices (SLC-091 Backend-Foundation + SLC-092 Frontend-Integration), 1 Feature (FEAT-038 Walkthrough Handbuch-Integration), 1 Migration (MIG-033 = Migration 089: rpc_get_walkthrough_video_path SECURITY DEFINER + handbook_schema-DML idempotent fuer Exit-Readiness-Template, bereits 2026-05-10 ~07:01 Europe/Berlin live appliziert, Pre-Apply-Backup `/opt/onboarding-plattform-backups/pre-mig-033_20260510_070528.csv`). 5 V5.1-DECs (DEC-095..099). Plus Inline-Hotfix ISSUE-046 (UUID-Validation 400 statt 500) commit `dc386db` 2026-05-11. Deploy-Commit `945cafe` (HEAD-Snapshot, App-Code-State identisch zu `dc386db` — `9d45475` + `945cafe` enthalten nur RPT-205+206 + STATE/roadmap-Updates, keinen App-Code-Change). Deploy-Image-Tag: `bwkg80w04wgccos48gcws8cs_app:945cafe3fd59f275a2590e589eff76874dd1999f` (worker analog). Container-IDs nach Redeploy: app=091256919077, worker=091256950458.
- Summary: V5.1 macht aus dem V5-Option-2-Methodik-Review-Output (approved walkthrough_step + walkthrough_review_mapping) eine sichtbare Schicht im Unternehmerhandbuch. Worker fuegt eine neue Walkthroughs-Section (order=15 default, per template.handbook_schema konfigurierbar) in den Snapshot ein, gruppiert Schritte nach Subtopic, rendert eine Schritt-Liste pro Walkthrough + inline HTML5 `<video>`-Tag. Storage-Proxy `/api/walkthrough/[sessionId]/embed` mit Range-Support (DEC-096) streamt Videos aus privatem `walkthroughs`-Bucket — RLS-Check per `rpc_get_walkthrough_video_path` SECURITY DEFINER (DEC-099), Stale-Banner triggert wenn approved Walkthrough nach letztem Snapshot existiert (DEC-097), Audit-Log einmalig pro Reader-Page-Load (DEC-098 Spam-Prevention bei Range-Request-Storm). Pre-V5.1-Snapshots ohne Walkthroughs-Section bleiben backwards-kompatibel. ISSUE-046 Inline-Hotfix vor REL-014: UUID_RE-Konstante + Pre-Validate-Guard im Embed-Endpoint (Pattern-Reuse aus `actions.ts:39`) — 400 BAD_REQUEST statt 500 INTERNAL_ERROR bei invalidem UUID-Format. RPT-204 Gesamt-/qa PASS (20 Checks alle PASS gegen Coolify-Production-DB, 744/758 Vitest Live, RLS-Matrix 110/110 Cross-Slice). RPT-205 Final-Check READY (0 Blocker/0 High/3 Low pre-existing). RPT-206 Go-Live GO. RPT-207 Deploy LIVE PASS. 100% Reuse aus V4.1+V5: 0 neue Tabellen + 0 neue Buckets + 0 neue npm-Pakete + 0 neue Worker-Job-Typen + 0 neue Bedrock-Calls.
- Risks: 0 V5.1-eingefuehrt. 3 Low residual: ISSUE-040 fast-xml-builder high-Vuln (Build-Time-only, pre-existing seit 2026-05-08, NICHT V5.1-induziert); ISSUE-026 postcss bundled in next (Build-Time-only, pre-existing seit V4.x, npm-Override-immun); Pre-Production-Compliance-Gate (Anwaltspruefung + Privacy-Page + Azure-EU + ISSUE-042 OpenAI-Key-User-Pflicht) explizit aufgeschoben per User-Decision (memory `feedback_compliance_gate_later`) — V5.1 bleibt Internal-Test-Mode konsistent V4.3..REL-013.
- Rollback Notes: Coolify-UI → Deploy-Commit `93a9d7a` (V5 Option 2 REL-013 Stand) → Redeploy. **DB-Rollback NICHT erforderlich** weil Migration 089 (MIG-033) additiv: `CREATE OR REPLACE FUNCTION rpc_get_walkthrough_video_path` (pre-V5.1-Code ruft die Function nie auf) + idempotenter DML auf `template.handbook_schema` (Walkthroughs-Section angehangen mit order=15, pre-V5.1-Worker kennt `walkthrough` als Source-Typ nicht und ignoriert die Section automatisch). Pre-Apply-Backup `/opt/onboarding-plattform-backups/pre-mig-033_20260510_070528.csv` bleibt erhalten. RTO ~5min, RPO 0.
- Post-Launch: 5 schlanke Pflicht-Smokes alle PASS bei Deploy: (1) Container-Health app+worker Up 4min healthy auf Image-Tag `945cafe`; (2) External `/login` HTTP 200 TTFB 143ms (REL-013-Baseline 146ms — minimal besser), `/api/health` HTTP 200 TTFB 99ms; (3) Reader-Play+Seek bereits in RPT-204 via Playwright MCP durchgefuehrt; (4) ISSUE-046 Hotfix Build-Artifact-Verify im Live-Image-Server-Chunk PASS (UUID-Regex + BAD_REQUEST-JSON inline im minified Code); (5) Image-Tag-Match `945cafe3fd59f275a2590e589eff76874dd1999f` fuer app + worker. Container-Logs 0 ERROR/FATAL/CRASH in beiden Containers seit Redeploy. error_log letzte 15min: 1 Eintrag info (Worker claim-loop started mit allen 14 Job-Handlern inkl. V5.1-Walkthrough-Pipeline). **18-24h-Beobachtungs-Window startet 2026-05-11 ~09:13 Europe/Berlin** → /post-launch V5.1 nach ~2026-05-12 ~09:13. Parallel: V5 Option 2 jetzt offiziell STABLE bestaetigt — alle 3 SC-V5-Conditions PASS, 3 autonome Cron-Runs in Folge (2026-05-09/10/11 jeweils 03:00 UTC in error_log).

### REL-013 — V5 Option 2 (Walkthrough-Mode + Methodik-Review-UI)
- Date: 2026-05-08 (RELEASED — User-Coolify-Redeploy 2026-05-08 ~14:30 Europe/Berlin durch, Post-Deploy-Verifikation alle gruen, 3 User-Pflicht-Smokes Post-Deploy ausstehend)
- Scope: 9 Slices (SLC-071 + SLC-072 + SLC-074..079; SLC-073 superseded by SLC-079 per DEC-079 — Roh-Video-Berater-Review verworfen, Methodik-Schicht aus V5.1 vorgezogen). 4 Features (FEAT-034 Walkthrough Capture-Session + FEAT-035 Whisper-Transkription `walkthrough_transcribe` + FEAT-037 AI-Pipeline 3 Stufen `walkthrough_redact_pii` → `walkthrough_extract_steps` → `walkthrough_map_subtopics` mit Bridge-Engine-Pattern Reverse-Direction + FEAT-040 Methodik-Review-UI mit Subtopic-Tree + Unmapped-Bucket + MoveStepDropdown + ApprovalForm + Pflicht-Privacy-Checkbox DEC-091). 6 Migrations live (082_v5_walkthrough_capture_mode + 083_v5_walkthrough_session + 084_v5_walkthrough_storage_bucket + 085_v5opt2_walkthrough_step + 086_v5opt2_walkthrough_review_mapping + 087_v5opt2_status_and_source_extension — alle additiv mit DROP-IF-EXISTS-Pattern). 17 V5-Option-2-DECs (DEC-079..094, plus DEC-080 Self-Spawn-Pattern + DEC-088 RawTranscriptToggle-Audit-Log + DEC-091 Pflicht-Privacy-Checkbox + DEC-092 Subtopic-Schema-Drift-Korrektur). Coolify-Scheduled-Task `walkthrough-cleanup-daily` aktiv (Container=app, `0 3 * * *` Europe/Berlin) — erster autonomer Run 2026-05-09 03:00. Deploy-Commit `93a9d7a` (HEAD inkl. L-1 Hotfix `4718abc` Logger-Pattern-Refactor + RPT-195 Go-Live-Doku). Deploy-Image-Tag: `bwkg80w04wgccos48gcws8cs_app:93a9d7ab44d64131ec09e50157f0290618e6aedf` (worker analog). Container-IDs nach Redeploy: app=143055314575, worker=143055340864.
- Summary: V5 Option 2 macht aus dem Onboarding-Plattform-Stack einen vollstaendigen methodischen Walkthrough-Pipeline-Stack. Mitarbeiter nimmt Bildschirm + Mikrofon auf (`/employee/walkthroughs`, Self-Spawn-Pattern via DEC-080), Whisper transkribiert lokal (DSGVO-konform), KI redactet PII per Pattern-Library + ≥90%-Recall (SC-V5-6), extrahiert Schritte als walkthrough_step (Migration 085) und mapped diese auf Template-Subtopics (Migration 086) per Bridge-Engine-Pattern Reverse-Direction (FEAT-023 Reuse). Berater reviewt nicht das Roh-Video, sondern die mapped SOP-Schicht im Subtopic-Tree mit Pflicht-Privacy-Checkbox (DEC-077/091). RawTranscriptToggle mit Audit-Log (DEC-088) gibt Berater Roh-Daten-Einsicht ohne Roh-Video-Pfad. Cleanup-Cron-Minimum: rejected age>30d → Storage+DB-Delete, failed age>7d → Storage+DB-Delete, stale-pipeline>1h → status='failed' + error_log mit `category='walkthrough_pipeline_failure'`. RPT-193 Gesamt-/qa PASS (693/693 Vitest gegen Coolify-DB), RPT-194 Final-Check READY (0 Blocker/0 High/0 V5-induced Medium, 5 Low alle pre-existing oder cosmetic, L-1 console-Drift hot-gefixt vor Deploy), RPT-195 Go-Live CONDITIONAL GO (3 Conditions als Post-Deploy-Pflicht).
- Risks: 0 V5-Option-2-induced. 1 Medium pre-existing (ISSUE-026 postcss bundled in next, npm-Override-immun, akzeptiert). 4 Low residual (4 npm-vulns alle ISSUE-026 carry-over, Doku-Drifts L-2..L-5 RPT-194 — `.env.deploy.example` Blueprint-Drift + `.env.local.example` unvollstaendig + docker-compose.yml Header-Branding + MIGRATIONS.md MIG-031 Apply-Status-Drift). Pre-Production-Compliance-Gate (Anwaltspruefung + Privacy-Page + Azure-EU-Whisper + ISSUE-042 OpenAI-Key-User-Pflicht) explizit aufgeschoben per User-Decision (memory `feedback_compliance_gate_later`) — V5 Option 2 bleibt Internal-Test-Mode konsistent mit V4.3+V4.4-Stand.
- Rollback Notes: Coolify-UI → Deploy-Commit `9be520b` (V4.4 REL-012 Stand) → Redeploy. **DB-Rollback NICHT erforderlich** weil Migrations 082..087 alle additiv (CREATE TABLE + CHECK-Constraint-Erweiterungen mit DROP-IF-EXISTS-Pattern). V4.4-Code ignoriert die V5-Tabellen + neuen Status-Werte. Stale-Sessions in `redacting/extracting/mapping`-Status bleiben bis Cleanup-Cron sie nach 1h auf `failed` setzt — danach faellt sie nach 7d in den failed-Storage-Delete-Pfad. Falls V4.4-Code den `/api/cron/walkthrough-cleanup`-Endpoint nicht hat (404): Scheduled-Task in coolify-db.scheduled_tasks `enabled=false` setzen ODER auf hoehere Frequenz reduzieren bis Re-Deploy. RTO ~5min Coolify-Redeploy + HTTP-200-Smoke. RPO 0 Datenverlust.
- Post-Launch: **3 User-Pflicht-Smokes Post-Deploy** vor "STABLE"-Status: (1) SC-V5-1 Mitarbeiter-Self-Test (Nicht-Tech-User-Persona) ueber Capture-Pfad bis status `pending_review`; (2) SC-V5-4 Live-Browser-Klick-Test 3 Admin-Routen + Move + Edit + Approve mit/ohne Privacy-Checkbox + Reject + Cockpit-Card-Counter + Audit-Log-Verifikation in `error_log`; (3) Erster autonomer Coolify-Cron-Run 2026-05-09 03:00 Europe/Berlin via `SELECT * FROM error_log WHERE source='cron:walkthrough-cleanup' AND created_at::date='2026-05-09' ORDER BY created_at DESC` (erwartet ≥1 info-Eintrag mit `category='walkthrough_cleanup'`). Erst nach allen 3 PASS → V5 Option 2 STABLE-Status. /post-launch nach 18-24h-Beobachtungs-Window.
- Post-Deploy-Verifikation: Container app+worker+supabase-kong+supabase-db Up 53s healthy, Worker re-check Up 1min healthy. Image-Tag-Match `93a9d7a` bestaetigt fuer app + worker. Externer `/login` HTTP 200 TTFB 146ms (Pre-Deploy: 122ms — minimal Slowdown post-Deploy normal); `/api/health` HTTP 200 TTFB 94ms (Pre-Deploy: 89ms — vergleichbar). App-Container-Logs: `Next.js 16.2.4 Ready in 0ms`, **0 Errors/Fatals/Crashes**. Worker-Container-Logs: alle 6 V5-Job-Handler registriert (`walkthrough_stub` + `handbook_snapshot_generation` + `walkthrough_transcribe` + `walkthrough_redact_pii` + `walkthrough_extract_steps` + `walkthrough_map_subtopics`), Environment validated (AWS_REGION=eu-central-1, LLM_MODEL=eu.anthropic.claude-sonnet-4-20250514-v1:0, AI_MIN_ITERATIONS=2, AI_MAX_ITERATIONS=8, AI_WORKER_POLL_MS=2000), **0 Errors/Fatals/Crashes** (1 Warning MODULE_TYPELESS_PACKAGE_JSON pre-existing seit V4.x, kein V5-Issue). Coolify-Cron `walkthrough-cleanup-daily` weiter enabled (`0 3 * * *`, container=app).

### REL-012 — V4.4 Maintenance-Sammelrelease (Lint-Sweep + SQL-Backfill MIG-030)
- Date: 2026-05-05 (RELEASED — User-Coolify-Redeploy 2026-05-05 ~14:18 Europe/Berlin durch, Live-Smoke PASS 5/5)
- Scope: 2 Slices SLC-061 + SLC-062. Pre-V5-Hygiene-Sammelrelease. SLC-061 Lint-Sweep (7 react-hooks-Errors + 6 Warnings → 0/0): setTimeout-Wrapping fuer setState-in-effect (BridgeProposalEditDialog + jitsi-meeting), uploadFile-Reorder + useCallback-deps in FileUploadZone, escape-quotes in SearchResultsList, alt-text + Image-Alias in EvidenceFileList, Inline-Disable mit Begruendung in shadcn sidebar, anonymous-default-export-Fix in email-stub.mjs, unused-eslint-disable-Cleanup in document-parser.ts + claim-loop.ts. SLC-062 SQL-Backfill MIG-030 (Migration 081, bereits LIVE 2026-05-05 ~13:10 UTC): Demo-Template `mitarbeiter_wissenserhebung` Umlaut-Backfill in `template.blocks` + `template.sop_prompt` JSONB, 25 Replace-Statements via Prefix-Sharing, idempotent (3rd-Apply md5-stable), Pre-Apply-Backup `/opt/onboarding-plattform-backups/pre-mig-030_20260505_131019.csv` (14032 bytes). 4 V4.4-DECs (DEC-070..073). Deploy-Commit `9be520b` (HEAD nach Go-Live RPT-160 + REL-012-Draft). Deploy-Image-Tag: `bwkg80w04wgccos48gcws8cs_app:9be520b685788ecdd23a784c4c32bb483e88736c` (worker analog). Container-IDs nach Redeploy: app=141543121109, worker=141543138231. Code-Releases im V4.4-Range: 188095f (SLC-061) → cf3183d (qa) → bcd4af8 (SLC-062 MIG-030 live) → 1833d24 (qa) → 233f4c0 (Gesamt-/qa) → 859da80 (Roadmap-Sync) → 9c1757b (Final-Check) → 9be520b (Go-Live + REL-012-Draft).
- Summary: Maintenance-Sammelrelease ohne Feature-Erweiterung. Schliesst Pre-V5-Hygiene-Schulden: V2-V4.2-Lint-Backlog auf Greenfield (`npm run lint` 0E + 0W) und Live-DB-Umlaut-Konsistenz fuer Demo-Template. App-Code-Deploy ist von DB-State entkoppelt — MIG-030 ist bereits live appliziert. App-Rollback hat keine DB-Rollback-Pflicht. SC-V4.4-1/2/3/5/6 verifiziert (Lint, Build, Typecheck, Vitest 535/535, Audit 0 TRUE-POSITIVE, Idempotenz, Browser-Smoke V4.3-Regression). SC-V4.4-4 (BL-067 Berater-Help-Review) DEFERRED parallel per User-Direktive. Gesamt-/qa PASS (RPT-158), Final-Check READY (RPT-159), Go-Live GO (RPT-160).
- Risks: 0 V4.4-eingefuehrt. 1 Medium pre-existing (ISSUE-026 postcss bundled in next, npm-Override-immun, akzeptiert). 4 Low pre-existing (9 dev-only npm-vulns, 120 console.* in workers + libs, 5 offene V1-V4.1-Issues, BL-067 deferred parallel). Pre-Production-Compliance-Gate (Anwaltspruefung + Privacy-Page + Azure-EU + ISSUE-042-Pattern) explizit aufgeschoben per User-Decision — V4.4 bleibt Internal-Test-Mode.
- Rollback Notes: Coolify-UI → Deploy-Commit `77b3852` (V4.3 REL-011 Stand) → Redeploy. **DB-Rollback NICHT erforderlich** — V4.4 hat keine App-Code-Schema-Migration. MIG-030 (DML-only) ist idempotent. V4.3-Code liest die korrigierten Umlaute korrekt aus `template.blocks` und `template.sop_prompt`. Pre-Apply-Backup CSV bleibt als Recovery-Point in `/opt/onboarding-plattform-backups/`. RTO ~5min (Coolify-Redeploy + HTTP-200-Smoke). RPO 0 Datenverlust.
- Post-Launch: INITIAL PASS (RPT-162, 2026-05-05 ~14:32, ~13min nach Deploy als kombinierter V4.3+V4.4-Lauf). 0 Errors/Fatals in app+worker container logs, 0 Errors in error_log seit Deploy, /login HTTP 200 TTFB 98.7ms (leicht besser als Deploy-Baseline 97.6ms), Container-IDs stabil, Worker-Bedrock-ENV korrekt geladen. **Volles 24-48h Window noch nicht erreicht** — Re-Check 2026-05-06 ~14:18 empfohlen (ad-hoc HTTP-Smoke + error_log-Query, kein eigenes /post-launch noetig). 1 V4.2-Carry-Over-Anomalie waehrend Lauf entdeckt: reminder_log Stage1-Mehrfach-Send (ISSUE-035, BL-076) — NICHT V4.4-induziert, kein Release-Bruch.

### REL-011 — V4.3 Maintenance-Sammelrelease (Reader-Polish + UX + Tooling + ADR)
- Date: 2026-05-05
- Scope: 6 Slices SLC-051..056. Keine Schema-Migration. Keine neuen Container. Keine neuen Cron-Jobs. SLC-051 Reader-UX-Bundle (Scroll-Spy via aria-current="location", TOC-Permalink, Skeleton-Loading-State, Mobile-h1-Wrap, h1-Anchor-Hover). SLC-052 Worker-Output-Hygiene (slugifyHeading-Util shared mit Reader, TOC mit In-App-Anchor-Links `[Title](#section-slug)` statt Datei-Pfaden, Umlaut-Konsistenz ae/ue/oe). SLC-053 Tooling-Migrations (`src/middleware.ts` → `src/proxy.ts` Next-16-Convention; ESLint 9 flat-config nativ ueber `eslint-config-next/core-web-vitals`, kein FlatCompat-Adapter). SLC-054 Cross-Snapshot-Suche client-side mit localStorage-Search-History (Recency-Boost + Title-Boost + Body-Match Score, max 20 Snapshots fuer Performance). SLC-055 UX-Findings-Bundle (Help-Konsolidierung Variante 3: Learning-Center mit 3 Tabs "Diese Seite" / "Video-Tutorials" / "Bedienungsanleitung"; Tooltip-Target-Fix Variante 2: Card-Header-Wrapper als Trigger statt 16px Icon-only). SLC-056 ADR State-Maschinen-Pattern (DEC-065) + Spike Turbopack-Layout-Inlining (DEC-066/069 — Anomalie nicht reproduzierbar im Minimal-Case, V4.2-Workaround-Pattern bleibt verbindlich). 9 V4.3-DECs (DEC-062..069). Deploy-Commit `77b3852` (HEAD-Snapshot, App-Code-State identisch zu `0843362` — `77b3852` enthaelt nur Doku-Update RPT-149 + REL-011-Draft + STATE-Phase). Deploy-Image-Tag: `bwkg80w04wgccos48gcws8cs_app:77b385260e3ee5ecdeae0347cec47a34da76d7e4`. User-Coolify-Redeploy 2026-05-05 ~10:53 Europe/Berlin.
- Summary: Maintenance-Sammelrelease ohne Feature-Erweiterung. Schliesst nach V4.2 Self-Service-Onboarding die UX-Polish-Kanten + Tooling-Drift + ADR-Doku-Schulden. Reader-Erlebnis (V4.1 FEAT-028) deutlich verbessert: aktive Sektion ist sichtbar beim Scrollen, Permalink kopierbar, Mobile-h1 wraps sauber, Cross-Snapshot-Suche ueberbrueckt Snapshot-Versionen ohne Reload. Worker-Snapshots haben jetzt In-App-Anchor-TOC statt Datei-Links — react-markdown-Override entfaellt fuer neue Snapshots, Pre-V4.3-Snapshots rendern weiter ueber den Rueckwaerts-Kompatibilitaets-Pfad. Help-Konsolidierung loest die Doppel-`?`-Verwirrung aus V4.2 (3-Tab-Sheet ersetzt 2 konkurrierende Trigger). DEC-065 macht Service-Role-Default fuer State-Maschinen-UPDATEs verbindlich (basierend auf V4.2 ISSUE-031 Erkenntnis: tenants-Tabelle hat keine UPDATE-RLS-Policy, RLS-aware Client returnte 0 Rows silent). Bundle-Live-Smoke (RPT-146) auf Strategaize-Business-Case-Fixture mit 9 Bloecken / 27 KUs / 9 Diagnosen + 2 echten Worker-erzeugten Snapshots PASS. Test-Fixture als wiederverwendbares SQL unter test-fixtures/v43-bundle-smoke/seed-strategaize-business-case.sql abgelegt. Gesamt-/qa PASS (RPT-147, 526/535 Vitest gegen Coolify-DB). Final-Check CONDITIONALLY READY (RPT-148). Go-Live CONDITIONAL GO (RPT-149).
- Risks: 0 V4.3-eingefuehrt. 1 Medium Pre-V4.2-carry-over (ISSUE-034 wizard-actions.test.ts Mock-Drift aus ISSUE-031-Fix — `wizard-actions.ts` nutzt seit Commit `d1978ca` `createAdminClient`, Test mockt nur `createClient` → 9 Tests FAIL gegen Coolify-DB; Live-Code unbetroffen, V4.2-Browser-Smoke 2026-04-30 user-bestaetigt PASS; Hotfix oder V4.4-Item ~30-60min). 7 Low residual: ISSUE-026 postcss in Next-bundled (wontfix bis Next-Minor-Bump), ISSUE-021/022 V4-Bridge-UX (V4.4/V5-Backlog), ISSUE-028 V3 RECORDING_WEBHOOK_SECRET (V3-Operational), ISSUE-007 JWT-Staleness (Pre-V1, Impact 0), ISSUE-033 Turbopack-Anomalie (wontfix), Section-Count-Konsolidierung (bewusst). Pre-Production-Compliance-Gate (Anwaltspruefung + Privacy-Page + Azure-EU + ISSUE-042-Pattern) explizit aufgeschoben per User-Decision (feedback_compliance_gate_later) — V4.3 bleibt Internal-Test-Mode.
- Rollback Notes: Coolify UI → Deploy-Commit `ec311a7` (V4.2 REL-010 Stand) → Redeploy. **DB-Rollback NICHT erforderlich** — V4.3 hat keine Schema-Migration (SC-V4.3-5 Constraint eingehalten), V4.2-Code ist 100% backwards-kompatibel mit allen V4.3-Daten. Worker-Pipeline rendert sowohl Pre-V4.3-TOC-Format (Datei-Pfade) als auch V4.3-In-App-Anchor-Format konsistent (Reader hat components.a-Override fuer Pre-V4.3-Snapshots). proxy.ts → middleware.ts re-rename ist 1-Datei-Operation falls noetig (V4.2-Code erwartet middleware.ts). RTO bei Image-Reset: ~5min (Coolify-Redeploy + HTTP-200-Smoke). RPO 0 Datenverlust.
- Post-Launch: STABLE (RPT-162, 2026-05-05 ~14:32, ~27h36min nach Deploy, Volles 24-48h Window erreicht als kombinierter V4.3+V4.4-Lauf). 0 Errors/Fatals/Warns in error_log seit Deploy (nur 4 info-Eintraege Worker-Restarts + 1 Cron-Run), 0 Errors/Fatals/Crashes in app+worker container logs 24h-Fenster, /login HTTP 200 TTFB 98.7ms (leicht besser als Deploy-Baseline 107ms), 0 Hotfix-Auslosungen, 0 V4.3-induzierte Bugs. **Erster regulaerer Cron-Run `capture-reminders-daily` heute 2026-05-05 09:00 sauber durchgelaufen** — V4.3-Code-Pfad in Production live verifiziert. block_review-Tabelle weiterhin 0 Eintraege (Pilot/Internal-Test-Mode). 1 V4.2-Carry-Over-Anomalie waehrend Lauf entdeckt: reminder_log zeigt 4× Stage1-Reminder an gleichen Empfaenger an 4 aufeinanderfolgenden Tagen (ISSUE-035, BL-076, severity Medium) — NICHT V4.3-induziert, V4.2 Cron-Logik-Drift.

### REL-010 — V4.2 Self-Service-Onboarding (Wizard + Reminders + Help)
- Date: 2026-05-01
- Scope: 5 Slices (SLC-046..050), 3 Features (FEAT-031 Wizard + FEAT-032 Reminders + FEAT-033 In-App-Hilfe), 1 Migration (MIG-029 = sql/migrations/080_v42_self_service.sql, deployed 2026-04-29). Frontend deployed via commit `918377a` (2026-05-01 morgens), CRON_SECRET-ENV-Durchreichung deployed via commit `daf9dfd` (Container Up healthy seit 11:19). Coolify-Scheduled-Task `capture-reminders-daily` (Container=app, `0 9 * * *`, Europe/Berlin) gestern angelegt — erster automatischer Run heute 08:14 Europe/Berlin hat stage1-Reminder an richard@bellaerts.de gesendet (status='sent', User-Bestaetigung Mail in Inbox).
- Summary: V4.2 macht die Plattform self-service-faehig fuer neue Tenants. Erst-Login-Wizard (4 Schritte mit Step-Persistenz + Skip-Mechanismen + Multi-Admin-Lock) fuehrt durch Setup. Capture-Reminders (E-Mail Stufe 1 nach 3 Werktagen + Stufe 2 nach 7 Werktagen, reminder_log Idempotenz, user_settings Opt-Out, Coolify-Scheduled-Task 09:00 Europe/Berlin) sorgen fuer Mitarbeiter-Aktivierung. In-App-Hilfe (Right-Side-Sheet mit Markdown auf 5 Pages: dashboard, capture, bridge, reviews, handbook + 5 Tooltips an UI-Elementen) reduziert Berater-Last. Gesamt-/qa LIVE PASS (RPT-127), Final-Check CONDITIONALLY READY mit CRON_SECRET-Fix (RPT-128), Go-Live CONDITIONAL GO (RPT-129), Deploy LIVE PASS (RPT-130). Pre-Deploy-Items reduziert von 4 auf 3 (ISSUE-032 DKIM als FALSE-POSITIVE resolved — IONOS verwendet provider-spezifische Selektor-Namen s1-ionos/s2-ionos/s42582890, DKIM ist via IONOS-Default seit Domain-Anlage aktiv, IMP-246 in Dev-System erfasst).
- Risks: 0 High. 2 Medium V4-Pre-existing (ISSUE-021/022). 5 Low Pre-existing (ISSUE-007/026/028 + BL-060/061 V4.3-Backlog). Privacy-Page-Gap als V4.3-Compliance-Item dokumentiert.
- Rollback Notes: Coolify-UI → V4.1 (Deploy-Commit ec311a7) → Redeploy. MIG-029 ist additiv (3 ALTER tenants + CREATE TABLE reminder_log + CREATE TABLE user_settings) — V4.1-Code ignoriert neue Spalten/Tabellen, kein DB-Rollback noetig. Bei Bedarf: SQL-Reset Wizard-State auf 'completed' (`UPDATE tenants SET onboarding_wizard_state='completed'`).
- Post-Launch: TBD (18h Beobachtungs-Window). Pflicht: 02.05. 09:00 Cron-Run pruefen (sollte sauber durchlaufen ohne Mails, da richard schon stage1 hat). 06.05. stage2-Trigger pruefen wenn richard immer noch keinen Block-Submit gemacht hat.

### REL-001 — SLC-001 Initial Deploy (V1-preview)
- Date: 2026-04-15
- Scope: Baseline-Datenmodell (tenants, profiles) + Core-Capture-Schema (template, capture_session, block_checkpoint, knowledge_unit, validation_layer) + RLS-Policies + Auth-Helper-Funktionen + handle_new_user-Trigger. Next.js-App-Container + 9 Supabase-Container + Whisper-Container auf Hetzner CPX62 (159.69.207.29). Domain: https://onboarding.strategaizetransition.com mit Let's-Encrypt-Cert.
- Summary: Erste produktive Instanz der Onboarding-Plattform. SLC-001 MT-1..MT-4 waren 2026-04-14 im Code fertig, MT-6 (Hetzner-Deploy) ist heute nach Aufraeumen der Hostname-Kollision und SSH-Zugang durch.
- Risks: Test-Infrastruktur fehlt noch (ISSUE-002), d.h. RLS-Integrationstest (MT-5) konnte nicht automatisiert laufen. Login-Flow + Admin-User-Seeding noch manuell auszuprobieren. Keine Bestandskunden, also niedriger operationeller Risikofaktor.
- Rollback Notes: `ssh root@159.69.207.29 'docker compose -p bwkg80w04wgccos48gcws8cs down -v'` entfernt alle 10 Container + Volumes. Coolify-Resource + DNS bleiben fuer Redeploy stehen. DB-Volume `bwkg80w04wgccos48gcws8cs_db-data` enthaelt bisher keine Echt-Daten, Verlust akzeptabel.

### REL-002 — V1 Full Deploy
- Date: 2026-04-18
- Scope: Vollstaendiger V1-Stack mit allen 13 Slices (SLC-007 reverted). 5/6 Features live. 12 Container (App + Worker + 9 Supabase + Whisper). Alle 15 Migrations (021-037) deployed. Next.js 16.2.4 (Security-Patch). npm audit 0 Prod-Vulnerabilities.
- Summary: Erster vollstaendiger V1-Deploy. App-Container mit KI-Chat (Bedrock eu-central-1), Worker-Container mit Multi-Agent Analyst+Challenger Loop, Debrief-UI, Meeting-Snapshot, JSON-Export. AWS-Credentials-Blocker im App-Container gefixt (Go-Live RPT-031). Gesamt-QA PASS (RPT-029), Final-Check PASS (RPT-030).
- Risks: ~41 Blueprint-Legacy-Dateien (kein Runtime-Impact, ISSUE-011). Dashboard zeigt keine Capture-Sessions (ISSUE-012). Error-Logging-Tabelle fehlt in DB. Kein CSP-Header. Kein Backup-Schedule. Kein Impressum/Datenschutz. Alle akzeptiert fuer internen Launch.
- Rollback Notes: Coolify UI → vorheriges Deployment waehlen → Redeploy. DB-Stand unveraendert (keine neuen Migrations in diesem Deploy). Bei Bedarf: `docker compose down -v` — keine Echtdaten, Verlust akzeptabel.
- Post-Launch: STABLE (RPT-032, 2026-04-18). E2E 10/10 PASS. 0 Fehler in Logs. Keine neuen Post-Deploy-Issues. RLS-Isolation verifiziert.

### REL-003 — V1.1 Maintenance Release
- Date: 2026-04-19
- Scope: 2 Slices (SLC-011 Legacy-Cleanup, SLC-012 Dashboard+error_log). 64 Legacy-Dateien geloescht. Dashboard von Blueprint-Runs auf Capture-Sessions umgebaut. error_log-Tabelle erstellt (MIG-012). 5 Issues resolved (ISSUE-003, -006, -011, -012, -013).
- Summary: Maintenance Release. Kein neues Feature, rein subtraktiv + inkrementell. Legacy-Ballast aus Blueprint-Fork entfernt, Dashboard auf echte Daten umgestellt, Error-Logging aktiviert. Gesamt-QA PASS (RPT-035), Final-Check PASS (RPT-036).
- Risks: Keine materiellen. CSP-Header fehlt weiterhin (pre-existing, V2-Scope). Kein Backup-Schedule (pre-existing, keine Echtdaten).
- Rollback Notes: Coolify UI → vorheriges Deployment waehlen. error_log-Tabelle bleibt in DB (additive Migration, kein Schaden).

### REL-004 — V2 Intelligence Upgrade
- Date: 2026-04-21
- Scope: 12 Slices (SLC-013..024), 7 Features (FEAT-010..016), 6 Migrationen (MIG-013..018). 3-Agent Orchestrator Loop, Auto-Gap-Backspelling (2-Runden-Limit), SOP-Generation (Level 2), Template-driven Diagnosis Layer, Evidence-Mode + Bulk-Import + KI-Analyse, Second Template + Switcher UI, Whisper Voice-Input mit Adapter-Pattern.
- Summary: Groesstes Feature-Upgrade seit Launch. Verwandelt die Plattform von einfacher Fragebogen-Erfassung in eine KI-gestuetzte Analyse-Pipeline: Orchestrator steuert Verdichtungsqualitaet, Backspelling schliesst Luecken automatisch, Diagnose-Layer erzeugt strukturierte Bewertungen pro Unterthema, SOPs werden erst nach Diagnose-Bestaetigung generiert. Evidence-Mode ermoeglicht Dokument-Upload mit automatischer Extraktion und Mapping. Voice-Input per Self-hosted Whisper (DSGVO-konform). Gesamt-QA PASS (RPT-053), Final-Check PASS (RPT-054).
- Risks: 1 Medium (supabase-studio unhealthy — nicht produktionskritisch). 3 Low (kein Whisper-Error-Toast, Worker-Logging unstrukturiert, kein Impressum). ISSUE-007 (JWT stale role) akzeptiertes Restrisiko. Kein Backup-Schedule (keine Echtdaten).
- Rollback Notes: Coolify UI → vorheriges Deployment (V1.1). Alle V2-Migrationen sind additiv (neue Tabellen, neue Spalten, neue RPCs) — DB-Rollback nur bei explizitem Bedarf noetig. V1.1-Code ignoriert V2-Tabellen.
- Post-Launch: STABLE (RPT-056, 2026-04-21). 0 Fehler in App/Worker-Logs. 0 Error-Level in error_log. Alle Container healthy (ausser Studio — nicht kritisch). 13 AI-Jobs verarbeitet, 9 Evidence-Files, 2 Whisper-Transkriptionen, 1 Diagnose, 1 SOP. Keine Post-Deploy-Issues.

### REL-005 — V3 Dialogue-Mode
- Date: 2026-04-24
- Scope: 8 Slices (SLC-025..032), 5 Features (FEAT-017..021), 4 Migrationen (MIG-019..022). Eigene Jitsi+Jibri-Instanz auf Onboarding-Server, Meeting-Guide (Basic) mit KI-Vorschlaegen, Dialogue-Session mit Video-Call + Recording + DSGVO-Consent, Post-Meeting KI-Pipeline (Transkript → Knowledge Units + Summary + Gap Detection), Dialogue-Integration in Diagnose/SOP/Debrief. DEC-025..033. Deploy-Commit `e775ff0`. Domain-Mapping: meet-onboarding.strategaizetransition.com → Jitsi-Web, onboarding.strategaizetransition.com → App.
- Summary: Verwandelt die Plattform von einer reinen Fragebogen-Pipeline in einen Dialog-faehigen Erhebungsmodus. Zweiter produktiver Erhebungsmodus neben Questionnaire (Evidence-Mode aus V2 war kein eigenstaendiger Mode). Post-Meeting-Pipeline extrahiert Knowledge Units aus Meeting-Transkript + gleicht gegen Meeting Guide ab (Gap-Detection). Integriert in gleiche Pipeline wie Questionnaire-Mode (Diagnose + SOP + Debrief). Smoke-Test PASS (RPT-065), Gesamt-QA PASS (RPT-065), Final-Check PASS (RPT-066), Go-Live GO (RPT-067). 7 Smoke-Test-Bugs waehrend QA gefixt (u.a. Jitsi-Domain-Mapping, tenant_admin-Layout, Dashboard server-side-loading). UX-Polish post-QA: ISSUE-015 Back-Button (Commit 1009f44), ISSUE-016 Sprachwechsel wontfix (DEC-033), ISSUE-017 24 Demo-Sessions aufgeraeumt.
- Risks: 1 High akzeptabel (npm audit @xmldom/xmldom via AWS-SDK SSO-Pfad, nicht erreichbar mit IAM-Auth — Fix geplant V3.1 BL-038). 2 Medium (admin-rls.test.ts Test-Isolation-Flaw ISSUE-018 BL-039, supabase-studio unhealthy pre-existing BL-040). 5 Low (16× npm Moderate aus gleicher AWS-SDK-Dep-Kette, kein CSP-Header, kein Impressum/Datenschutz, kein Backup-Schedule, 1 console.log). ISSUE-007 (JWT stale role) bleibt akzeptiertes Restrisiko.
- Rollback Notes: Coolify UI → vorheriges Deployment (V2.1-Stand vor 2026-04-22). V3-Migrations 058-064 sind alle additiv (neue Tabellen meeting_guide + dialogue_session, neue Spalten capture_mode + knowledge_unit.source-Erweiterung, neue Storage-Bucket recordings, neue RPCs) — V2-Code ignoriert V3-Tabellen, kein DB-Rollback noetig. Bei Totalausfall: `docker compose down -v` (Verlust der einen Demo-Session 64ad04eb akzeptabel — keine Echtkunden-Daten).

### REL-006 — V3.1 Maintenance Release (+ V4-Schema-Fundament dormant)
- Date: 2026-04-24
- Scope: 3 V3.1-Backlog-Items (BL-038/039/040) + SLC-033 V4 Schema-Fundament code-deployed (dormant, weil keine V4-UI/RPCs). BL-038 `@aws-sdk/client-bedrock-runtime` 3.1024 → 3.1036 + npm-Override `@xmldom/xmldom@^0.8.13` via mammoth → `npm audit --omit=dev` = 0 Vulns (Prod-Kette sauber). BL-039 `admin-rls.test.ts` drei Assertions mit `WHERE tenant_id IN ($1,$2)` isoliert → ISSUE-018 resolved. BL-040 `supabase-studio` `healthcheck: disable: true` in `docker-compose.yml` → DEC-041, ISSUE-020 als `wontfix` geschlossen (bewusster Skip). Plus V4-Schema: 8 Migrationen (065-071 + 075) idempotent auf Coolify-DB appliziert — `employee`-Rolle, `employee_invitation`, `bridge_run`, `bridge_proposal`, `handbook_snapshot`, handbook-Bucket, employee-RLS-Perimeter. V4-Tabellen sind in der Produktions-DB, aber keine App/Worker-Route nutzt sie aktuell. Deploy-Commit `cffc639` (main HEAD zum Deploy-Zeitpunkt). Container: app + worker + supabase-* mit Image-Tag `cffc639addf4e6ab91fa6837b06e1b3c87c96c5f`.
- Summary: Maintenance-Release zum Schliessen der V3-Restrisiken plus Early-Deploy des V4-Schema-Fundaments. Kein V3.1-eigenes Feature, keine User-sichtbare Verhaltensaenderung durch V3.1. V4-Anteil ist bewusst dormant — die V4-UI + RPCs folgen ab SLC-034. Verifikation: alle Container `healthy` 2 Minuten nach Redeploy, Worker-ENV zeigt Bedrock eu-central-1 + Claude Sonnet 4 korrekt, Production-Worker hat `@aws-sdk/client-bedrock-runtime@3.1036.0` + `@xmldom/xmldom@0.8.13` (override wirksam), supabase-studio ohne `.State.Health`-Objekt (DEC-041 wirksam), `admin-rls.test.ts` 2/2 PASS gegen Coolify-DB (BL-039 verifiziert). Kein Bedrock-Roundtrip-Smoke-Test — Cost-Control pro feedback_bedrock_cost_control, echte KI-Arbeit nur user-getriggert.
- Risks: Niedrig. V3.1 ist reiner Maintenance-Release ohne Verhaltensaenderung. V4-Dormant-Anteil birgt theoretisches Risiko, wenn ein Bug in den V4-Migrations versehentlich bestehende V3-Policies/Tabellen beschaedigen wuerde — aber alle 8 Migrationen sind additiv (CREATE TABLE/INDEX/POLICY, DROP CONSTRAINT+ADD CHECK additiv) und idempotent re-runnable. RLS-Test-Matrix-Skelett gegen Coolify-DB gruen (RPT-073, 11 passed + 34 todo). ISSUE-007 (JWT stale role) bleibt akzeptiertes Restrisiko aus V1. Kein CSP-Header, kein Impressum/Datenschutz, kein Backup-Schedule — alle pre-existing aus V1-V3, separate Wartungs-Iteration.
- Rollback Notes: Coolify UI → Deploy-Commit `3bbad32` (pre-V4, V3.1-only) oder `e775ff0` (V3 pre-V3.1). DB-Rollback der 8 V4-Migrationen umgekehrte Reihenfolge: `DROP FUNCTION storage.handle_new_user()` (falls aus Erstanwendung uebrig, siehe IMP-120), `DROP POLICY`s mit `_employee_%` auf capture_session/block_checkpoint/knowledge_unit/validation_layer, DELETE Storage-Bucket handbook + Policies, DROP TABLE handbook_snapshot/bridge_proposal/bridge_run/employee_invitation CASCADE, ALTER TEMPLATE DROP COLUMN employee_capture_schema/handbook_schema, ALTER capture_session/knowledge_unit CHECK auf V3-Stand zuruecksetzen, ALTER profiles.role CHECK auf 3 Werte zuruecksetzen, `handle_new_user()` Whitelist auf 3 Werte zuruecksetzen. V3-Code ignoriert V4-Tabellen — Schema-Rollback nur bei echter Produktionsstoerung noetig. Alle V4-Migrationen dokumentiert in MIG-023.

### REL-009 — V4.1 Handbuch-Reader + Berater-Review-Workflow
- Date: 2026-04-29
- Scope: V4.1-Release fuer alle 5 V4.1-Slices (SLC-041..045) und 3 V4.1-Features (FEAT-028 Handbuch In-App-Reader, FEAT-029 Berater-Review + Quality-Gate, FEAT-030 Berater-Visibility-Verlinkung). Eine Migration MIG-028 (Migration 079: `block_review`-Tabelle + RLS-Policies + Backfill `approved` fuer pre-V4.1-Daten + INSERT-Trigger fuer neue Mitarbeiter-Submits) bereits am 2026-04-28 in Production deployed; App-Code-Rollout heute auf Coolify. Deploy-Commit `ec311a7` (HEAD). 8 V4.1-DECs (DEC-043..050) inklusive: DEC-043 Reader-Markdown-Stack `react-markdown` + `remark-gfm` + `rehype-slug` + `rehype-autolink-headings`, DEC-044 Block-Approval-Granularitaet (kein KU-Override), DEC-045 weiches Quality-Gate (Confirm-Dialog statt hartem Lock), DEC-046 konsolidierter Review-View block-zentriert, DEC-047 Cross-Tenant + Pro-Tenant beide gebaut, DEC-048 Backfill-Strategie `approved` fuer pre-V4.1, DEC-049 react-markdown statt MDX, DEC-050 single-row Audit-Pattern. Neue Routes: `/dashboard/handbook` (Snapshot-Liste), `/dashboard/handbook/[snapshotId]` (Reader mit Sidebar-Navigation, Markdown-Render, Section-Anchors, Volltext-Suche client-side), `/admin/reviews` (Cross-Tenant-Aggregat aller pendenden Block-Reviews), `/admin/tenants/[id]/reviews` (Pro-Tenant-Review-Sicht), `/admin/blocks/[blockKey]/review` (Konsolidierter Block-Review mit Approve/Reject + Note-Audit), Cockpit-Card "Mitarbeiter-Bloecke reviewed" auf `/dashboard`. Worker-Pre-Filter erweitert um `block_review.status='approved'` (nur fuer `source='employee_questionnaire'`-KUs, Best-Effort bei fehlenden Eintraegen via DEC-048). Storage-RLS via Service-Role-Bypass mit Page-Level-Guards (Auth/Role/Cross-Tenant-Gates).
- Summary: Closed-Loop-Release fuer den Berater-Workflow zwischen V4-Mitarbeiter-Capture und Handbuch-Generation. V4 hatte den Mitarbeiter-Output ungefiltert in die Handbuch-Pipeline geschickt — V4.1 schiebt einen expliziten Berater-Review-Schritt dazwischen, ohne ihn zum harten Block zu machen (DEC-045 weiches Quality-Gate via Confirm-Dialog). Reader liefert die Konsumentensicht des Handbuchs als In-App-Erlebnis (statt nur ZIP-Download), mit Section-Navigation, Volltext-Suche und Snapshot-Versionsliste. Cross-Tenant-Visibility-Sicht laesst strategaize_admin alle pending Block-Reviews aller Tenants oldest-first sortiert durcharbeiten; Pro-Tenant-Sicht ist der Workflow-Einstieg pro Mandant. Gesamt-/qa V4.1 PASS (RPT-109, 378/378 Vitest gegen Coolify-DB), /final-check V4.1 PASS (RPT-110, alle 7 Audit-Dimensionen), /go-live V4.1 GO (RPT-111). 3 unused `@ts-expect-error` waehrend /final-check bereinigt (1 V4.1-Scope SLC-045, 2 pre-existing SLC-035). Browser-Smoke 17/17 user-bestaetigt 2026-04-29 ueber 3 Iter-Cycles. Post-Deploy-HTTP-Sanity-Check 2026-04-29 18:30 UTC: HTTP 307 Login-Redirect, TTFB 102ms, App-Container healthy. Aus V4.1-Iter-Cycles 3 IMPs in Dev-System (IMP-211/212/213) plus IMP-214 aus /final-check (`/qa` muss `tsc --noEmit` global mitlaufen lassen).
- Risks: Niedrig. 0 V4.1-spezifische Blocker. 4 akzeptierte Pre-V4.1-Residual-Risks (alle unveraendert seit V4 REL-008): ISSUE-007 V3 JWT-Staleness nach Rollen-Rename (User loggt sich einmal neu ein), ISSUE-021 V4-Bridge Edit-only-Pfad fehlt (Workaround Edit+Reject, V4.2-Defer), ISSUE-022 strategaize_admin kein /admin/bridge ohne tenant_id-Wechsel (Workaround Tenant-Admin-Account, V4.2-Defer), ISSUE-026 postcss 8.4.31 als Next.js bundled dep (3 moderate audit, wontfix bis Next-Minor-Bump, kein Runtime-XSS-Pfad), ISSUE-028 V3 RECORDING_WEBHOOK_SECRET fehlt in Production-ENV (V3-Operational, Recording-Pipeline im Pilot nicht aktiv genutzt). Eine Low-Notiz: Next.js 16 `middleware`->`proxy`-Convention-Deprecation als BL-059 fuer V4.2 angelegt (nicht funktional blockierend). Performance: Reader-TTFB <200ms, Search-Index-Build <30ms client-side bei typischen Snapshot-Sizes. Demo-DB Test-Daten-Cleanup 0/0 (kein Restmuell). Keine neuen ENVs, keine neuen Container, keine neuen Cron-Jobs. AWS-Region eu-central-1 (DSGVO-konform).
- Rollback Notes: Coolify UI -> Vorgaenger-Image (Commit `d2be0e4` = pre-V4.1, V4-Stand REL-008). MIG-028 ist additiv (CREATE TABLE + CREATE TRIGGER + INSERT-Backfill 0 Rows) und idempotent re-runnable. **DB-Rollback NICHT erforderlich** fuer V4.1-Slices — alte App-Version (V4) ignoriert die `block_review`-Tabelle. RTO bei Image-Reset: ~5 min (Coolify-Redeploy + Smoke). Bei wirklichem Schema-Cleanup (theoretisch): `DROP TRIGGER IF EXISTS tg_block_review_pending_on_employee_submit ON capture_event; DROP FUNCTION IF EXISTS tg_block_review_pending_on_employee_submit(); DROP TABLE IF EXISTS public.block_review CASCADE;`. Worker-Pre-Filter ist Best-Effort (`loadApprovedBlockKeys` returns true bei fehlendem Eintrag) — Pre-V4.1-Snapshots bleiben reproduzierbar (DEC-048). RPO 0 Datenverlust fuer V4-Stand. Worker-Image-Revert MUSS vor SQL-DROP gehen, sonst bricht Worker-Query.
- Post-Launch: STABLE (RPT-117, 2026-04-30, ~18h post-deploy). 0 Error/Fatal/Crash in app+worker container logs (24h-Fenster). 0 error/warn-level Eintraege in `public.error_log` seit Deploy (nur 11 info/claim-loop). Alle V4.1-Routes erreichbar (HTTP 200/307 wie erwartet, TTFB 86-149ms). Container `app` + `worker` healthy seit 18h, Ressourcen-Verbrauch app 0.04% CPU / 70 MiB RAM, worker idle 41 MiB, DB 6.5% CPU / 117 MiB. Demo-Tenant-Daten konsistent (1 tenant, 3 profiles, 4 sessions, 1 invitation, 2 bridges, 7 snapshots — keine Drift seit Deploy). Kein neuer block_review-Eintrag seit Deploy (pilot/internal-test-mode, keine Mitarbeiter-Submissions; Backfill `approved` aus DEC-048 wirksam). Kein neuer Handbuch-Snapshot seit Deploy (keine neuen Capture-Sessions abgeschlossen — erwartbar). MIG-029-Foundation-Backfill (V4.2 SLC-046) hat Live-Daten nicht zerstoert: 1 Tenant auf `onboarding_wizard_state='completed'` (idempotent), 3 user_settings angelegt (= auth.users Count). Keine Hotfix- oder Rollback-Auslosung. 4 akzeptierte Pre-V4.1-Residual-Risks bleiben (ISSUE-007/021/022/026/028) — alle nicht V4.1-spezifisch.

### REL-008 — V4 Zwei-Ebenen-Verschmelzung (Mitarbeiter + Bridge + Handbuch + Cockpit)
- Date: 2026-04-27
- Scope: V4 Konsolidierungs-Release fuer alle 8 V4-Slices (SLC-033..040) und 6 V4-Features (FEAT-022..027). Code-Anteile waren ueber 3 Tage stueckweise deployed: REL-006 (V3.1 + dormant V4-Schema), REL-007 (SLC-034 Employee-Auth), Bridge-Engine (SLC-035 + SLC-036) Live-deployed 2026-04-25, Employee-Capture (SLC-037) deployed 2026-04-26, Capture-Mode-Hooks (SLC-038) + Handbuch-Backend (SLC-039) + Handbuch-UI+Cockpit (SLC-040) deployed 2026-04-26..27. Letzter Coolify-Redeploy 2026-04-27 ~17:35 UTC mit Compose-Drift-Fix (Commit `d2be0e4`): docker-compose.yml `app`-Service environment-Block um 10 V3-Vars erweitert (SMTP_*, ERROR_ALERT_EMAIL, JITSI_JWT_*, NEXT_PUBLIC_JITSI_DOMAIN, RECORDING_WEBHOOK_SECRET) — vorher nur via Coolify-UI gesetzt, jetzt in versionierter Compose-File deklariert. Compose-File ist self-contained, Disaster-Recovery-Pfad sauber. 14 V4-Migrationen (065-078): employee-Rolle, employee_invitation, capture_mode-Erweiterung, bridge_run + bridge_proposal, template.handbook_schema + template.bridge_prompt, handbook_snapshot + handbook-Storage-Bucket, RPCs (rpc_create_employee_invitation, rpc_accept_employee_invitation_finalize, rpc_revoke_employee_invitation, rpc_trigger_bridge_run, rpc_approve_bridge_proposal, rpc_reject_bridge_proposal, rpc_trigger_handbook_snapshot, rpc_get_handbook_snapshot_path), 4 RLS-Policies (075-078) fuer 4-Rollen-Sichtperimeter (strategaize_admin/tenant_admin/tenant_member/employee). 7 DECs (DEC-034..040) + DEC-041 (supabase-studio Healthcheck) + DEC-042 (V2-tenant_read-Policies auf tenant_admin-only restringiert).
- Summary: Groesster Release seit V2. Verwandelt die Plattform von Single-Perspektiven-Erhebung in eine Zwei-Ebenen-Struktur (Geschaeftsfuehrer als tenant_admin + Mitarbeiter als employee mit eigenem Login + eingeschraenktem Sichtperimeter). Bridge-Engine generiert aus GF-Verdichtung (KUs + Diagnose + SOP) Mitarbeiter-Capture-Aufgaben hybrid (Template-Standard + KI-Verfeinerung + max 3 Free-Form-Slots, DEC-034). Bridge ist explizit on-demand (DEC-037, kein Auto-Run) — Berater-Konsultations-Schritt zwischen GF-Verdichtung und Bridge-Trigger ist bewusst eingeplant (V2-Debrief-UI unter /admin/debrief existiert weiterhin). Mitarbeiter durchlaeuft eigenen Questionnaire-Mode mit auf-Session-eingeschraenktem Sichtperimeter (R16, SC-V4-3). Unternehmerhandbuch konsolidiert Tenant-Daten in 8-Sections-ZIP (deterministischer Code, kein LLM in V4 — DEC-038). Self-Service-Cockpit zeigt 5 Metrik-Karten + Naechster-Schritt-Banner. Capture-Mode-Hooks-Architektur fuer V5 (Walkthrough) + V6 (Diary) etabliert via walkthrough_stub-Spike (FEAT-025, SC-V4-6). Pflicht-Gate SC-V4-3 (4-Rollen-RLS-Test-Matrix mit 32 Pflicht-Faellen + 14 Bonus = 46 Tests gegen Live-DB) gruen (RPT-092). Pflicht-Gate SC-V4-5 (Nicht-Tech-User-Smoke) durch RPT-090 + RPT-091 erfuellt. Gesamt-V4-/qa PASS (RPT-092: 322/322 Tests gruen Live-DB), /final-check ready (RPT-093 conditionally → resolved nach ENV-Drift-Fix), /go-live GO-Decision (RPT-094). Browser-Smoke User-Self-Test PASS in 5/5 Pfaden (Cockpit, Handbuch, Team, Bridge, Cross-Tenant).
- Risks: Niedrig. 0 Blocker. 2 Low residual: ISSUE-026 postcss 8.4.31 als Next.js bundled dep (npm-Override-immun, Build-Time-only, kein Runtime-XSS-Pfad — Aufloesung mit Next-Minor-Bump); ISSUE-028 V3 RECORDING_WEBHOOK_SECRET MISSING in Production-ENV (V3-Defect, Recording-Pipeline im Pilot nicht aktiv genutzt, kein V4-Blocker — V3.x-Maintenance-Backlog). ERROR_ALERT_EMAIL leer in Coolify-UI → Code-Fallback auf SMTP_USER greift (akzeptabel). ISSUE-027 (ENV-Drift) wurde im Vorfeld behoben. Performance innerhalb Budget: Snapshot-Render 25ms, Total-Exec 0.16-0.23s, Bridge-Run 10-27s (LLM-bound), HTTP-TTFB <200ms aller V4-Routen. AWS-Region eu-central-1 (DSGVO-konform). Kein CSP-Header, kein Impressum/Datenschutz, kein Backup-Schedule — alle pre-existing aus V1-V3, separate Wartungs-Iteration. Berater-Review-Quality-Gate vor Handbuch-Generation und konsolidierter Mitarbeiter-Output-Review fehlen als bewusste UX-Schritte (BL-049 + BL-050 in V4.1-Scope).
- Rollback Notes: Coolify UI → Vorgaenger-Image (Commit `912fba0` = pre-Compose-Drift-Fix; oder weiter zurueck zu `82c987e` = pre-Bridge/Handbuch/Cockpit). Alle 14 V4-Migrationen (065-078) sind additiv (CREATE TABLE/INDEX/POLICY/FUNCTION + DROP/ADD CHECK additiv) und idempotent re-runnable. **DB-Rollback NICHT erforderlich** fuer V4-Slices — alte App-Version ignoriert die V4-Tabellen. RTO bei Image-Reset: ~5-7 min (3-5 min Coolify-Redeploy + 2 min Smoke-Test). Bei wirklichem DB-Rollback (theoretisch): Reihenfolge 078→065, alle DROPs siehe MIG-023..027 in `/docs/MIGRATIONS.md`. Im Live-System aktuell 1 Demo-Tenant mit 1 GF-Session + 5 KUs + 2 Bridge-Runs + 6 Proposals + 1 accepted Invitation + 2 Mitarbeiter-Sessions + 4 Handbuch-Snapshots — bei Bedarf via SQL-Cleanup zurueckdrehbar.
- Post-Launch: ausstehend (post-launch nach 1-2 Tagen Pilot-Betrieb).

### REL-007 — SLC-034 Employee-Auth + Invitation-Flow
- Date: 2026-04-24
- Scope: 2/8 V4-Slice (FEAT-022 Employee Role + RBAC Extension, code-Anteil live, RLS-Perimeter-Vervollstaendigung folgt in SLC-037). Migration 072 (3 RPCs: rpc_create_employee_invitation, rpc_revoke_employee_invitation, rpc_accept_employee_invitation_finalize) aktiv in Produktions-DB. Neue Routes: `/admin/team` (tenant_admin Mitarbeiter-Verwaltung mit Liste + Einladungs-Dialog + Revoke + Resend), `/accept-invitation/[token]` (Employee Passwort-Setzen mit Error-Page-Varianten), `/employee` (Mitarbeiter-Dashboard-Skelett mit Rollen-Guard). Middleware: `/accept-invitation` als public path, Login-Redirect role-aware (employee → /employee). E-Mail-Template DE/EN/NL in `src/lib/email.ts`. Sidebar: neuer Nav-Eintrag "Mitarbeiter" fuer tenant_admin (Fix 82c987e nach Browser-Smoke-Finding). Deploy-Commit `82c987e`. DEC-011-Pattern strikt eingehalten: Server-Action orchestriert Auth-Admin-API (`auth.admin.createUser`) + Finalize-RPC + Rollback-on-Fail. Keine Passwort-Verarbeitung im SQL-Layer.
- Summary: Erster echter V4-Feature-Release. Verwandelt die Plattform von einer Single-Perspektiven-Erhebung (tenant_admin) in eine Zwei-Ebenen-Struktur (tenant_admin + employees). Mitarbeiter-Einladung via E-Mail-Token-Link mit klassischem Passwort-Setup (DEC-035). Auth-Admin-API-Pattern (DEC-011) verhindert direkten SQL-INSERT in `auth.users`. 14/14 RPC-Tests gruen, Gesamt-Test-Suite 144/178 PASS (34 todo, 0 failed). Post-Deploy Phase 1 PASS (RPT-077): 16/16 Container healthy, RPCs persistent, 4/4 Token-Page-Szenarien via curl verifiziert. Post-Deploy Phase 2 PASS: User-bestaetigter Browser-End-to-End-Smoke (Einladung → E-Mail zugestellt → Passwort setzen → Auto-Login → /employee-Dashboard → tenant_admin sieht Mitarbeiter in /admin/team-Liste).
- Risks: Niedrig. Neue Feature-Flaeche beschraenkt auf drei Routes + eine Migration. AC-11 (4 direkte RLS-Matrix-Tests fuer employee_invitation) planmaessig auf SLC-037 verschoben (Slice-Doc Out-of-Scope-Abschnitt). Low: display_name aus Invitation landet nicht in profile (Dashboard zeigt email statt display_name) — kosmetisch, kein Blocker. Konflikt-Fall "E-Mail existiert bereits in auth.users" liefert UI-Meldung, kein Rollback (bewusstes Design). Rate-Limit auf acceptEmployeeInvitation = 5/15min pro IP — moeglicherweise bei legitimem Retry zu streng, Monitoring im Alltag empfohlen.
- Rollback Notes: Coolify UI → Deploy-Commit `cffc639` (V3.1 ohne SLC-034-UI). RPCs in Produktions-DB bleiben harmlos bestehen, da ohne UI-Zugriff (das Volume ist persistent). Bei echter Produktionsstoerung DB-Rollback: `DROP FUNCTION public.rpc_create_employee_invitation(text,text,text); DROP FUNCTION public.rpc_revoke_employee_invitation(uuid); DROP FUNCTION public.rpc_accept_employee_invitation_finalize(uuid,uuid);`. employee_invitation-Tabelle bleibt bestehen (aus SLC-033 REL-006). Bei bereits angelegten employee-auth.users: manuell `auth.admin.deleteUser` via Supabase Studio oder Service-Role-Script. Bisher 1 Real-World-Employee-Account angelegt (User-Smoke-Test 2026-04-24).
